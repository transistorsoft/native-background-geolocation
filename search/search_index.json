{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Background Geolocation SDK","text":"Background Geolocation SDK <p>Battery-efficient, highly accurate background location tracking for iOS and Android \u2014 trusted by over 20,000+ apps in over 140\u00a0countries\u00a0worldwide.</p> Buy Now React Native React Native SDK Flutter Flutter SDK Capacitor Capacitor SDK Swift Native iOS SDK Kotlin Native Android SDK 20,816+ Production Apps 140+ Countries 16 Use Cases Software AgenciesFleet TrackingField ServiceDelivery &amp; LogisticsWorkforce TrackingRideshare &amp; MobilityHealth &amp; FitnessTravel &amp; HospitalityMarketplace &amp; On-DemandAsset TrackingSecurity &amp; PatrolRetail &amp; CommerceFamily SafetyTelematicsConstructionRoute Optimization <p>Public companies using the Background Geolocation SDK</p>  Intelligent Motion DetectionA sophisticated motion-state machine automatically transitions between tracking and stationary modes, slashing battery usage when the device is still.  GeofencingMonitor unlimited circular regions with sub-metre accuracy \u2014 triggers reliably even when the app is terminated by the OS.  Polygon GeofencingDefine geofences as arbitrary polygon shapes \u2014 parks, building footprints, delivery zones. Available as a paid add-on. Durable HTTP + SQLite QueueEvery location is persisted to SQLite before upload. Automatic retry, JWT auth, and batch upload keep your server in sync.  Survives Termination &amp; RebootWith <code>stopOnTerminate: false</code> and <code>startOnBoot: true</code>, the SDK continues tracking after the app is killed and resumes automatically when the device restarts, without user interaction.  Android Headless TasksContinues executing JavaScript and Dart location callbacks even when the Android app is fully terminated \u2014 no foreground process required.  Location FilterFine-tune location accuracy with a configurable filter pipeline \u2014 smooth GPS noise and set minimum accuracy thresholds.  Odometer &amp; Trip MetricsBuilt-in distance tracking, speed, and heading \u2014 no extra code required. Reset the odometer at trip start and read it at any time.    Transistor's Background Geolocation SDK is the most complete and battle-tested location library available for cross-platform mobile development. It ships with a full native implementation for both iOS (Swift) and Android (Kotlin), with identical APIs across React Native, Flutter, Capacitor, and Cordova."},{"location":"about/","title":"About","text":""},{"location":"about/#background-geolocation-sdk","title":"Background Geolocation SDK","text":"<p>The Background Geolocation SDK is built and maintained by Transistor Software \u2014 a small team of engineers obsessed with one problem: tracking a device's location reliably, accurately, and without draining the battery.</p>"},{"location":"about/#origin-story","title":"Origin Story","text":"<p>The SDK was born in 2013 out of a real-world emergency management problem.</p> <p>A disaster response organization needed to track first-responders \u2014 firefighters, EMTs, search-and-rescue teams \u2014 deployed into the field during hurricanes and earthquakes. The requirements were brutal:</p> <ul> <li>The cellular network is likely degraded or destroyed. Data must be stored locally and uploaded whenever connectivity returns.</li> <li>The app will be terminated by the OS under memory pressure. Tracking must survive and resume automatically on reboot.</li> <li>Responders carry the same devices all day. Battery life is non-negotiable.</li> </ul> <p>These constraints shaped the SDK's architecture from day one:</p> Requirement Solution Network unreliable Built-in SQLite persistence \u2014 every location queued locally before upload App may be terminated Headless background execution \u2014 tracking continues without a UI Device may reboot Boot receiver \u2014 tracking auto-restarts on device power-up Battery critical Motion-detection state machine \u2014 sensors decide when to track"},{"location":"about/#motion-intelligence","title":"Motion Intelligence","text":"<p>The SDK's defining feature is its motion-detection state machine, built on accelerometer, gyroscope, and magnetometer APIs. Rather than polling GPS continuously, the SDK:</p> <ul> <li>Monitors inertial sensors when stationary \u2014 near-zero battery draw</li> <li>Activates GPS automatically when motion is detected</li> <li>Scales GPS frequency and accuracy to the device's speed and <code>distanceFilter</code></li> </ul> <p>The result: location tracking at a fraction of the battery cost of naive continuous GPS.</p>"},{"location":"about/#platform-coverage","title":"Platform Coverage","text":"<p>What began as a single iOS SDK has expanded to every major mobile development platform:</p> Platform Package React Native / Expo <code>react-native-background-geolocation</code> Flutter <code>flutter_background_geolocation</code> Capacitor <code>@transistorsoft/capacitor-background-geolocation</code> Cordova <code>cordova-background-geolocation-lt</code> Swift / iOS (native) <code>background-geolocation</code> Kotlin / Android (native) <code>background-geolocation</code> <p>A single native core \u2014 TSLocationManager on iOS, TSLocationManager on Android \u2014 powers all platforms. Framework SDKs are thin bridges. One codebase, one set of algorithms, everywhere.</p>"},{"location":"about/#built-for-production","title":"Built for Production","text":"<p>The SDK is trusted by thousands of production apps across logistics, field services, fleet management, emergency response, fitness, and more.</p> <p>Key production features:</p> <ul> <li>Durable HTTP upload queue \u2014 automatic retry with exponential back-off</li> <li>JWT / token-refresh auth \u2014 Bearer token auto-refresh before expiry</li> <li>Geofencing \u2014 unlimited circular regions, reliable even when terminated</li> <li>Background Fetch integration \u2014 guaranteed final uploads before iOS reclaims resources</li> <li>Extensive logging \u2014 structured logs, configurable verbosity, native log viewer</li> </ul> <p>Purchase a license \u00b7 GitHub \u00b7 transistorsoft.com</p>"},{"location":"contact/","title":"Contact","text":""},{"location":"contact/#transistor-software","title":"Transistor Software","text":"Legal name9224-2932 Quebec Inc LocationMontreal, QC, Canada Emailinfo@transistorsoft.com Phone514 994 2585 Tax ID853476406RT0001 <p>Purchase a license \u00b7 transistorsoft.com</p>"},{"location":"purchase/","title":"Plans & Pricing","text":"<p>PLANS &amp; PRICING</p> How many applications do you want to build? <p>One purchase covers both iOS &amp; Android \u2014 each key unlocks a single app identifier, for unlimited users and devices.</p> React Native Capacitor Flutter Cordova Swift / Kotlin STARTER <p>For building 1 app</p> $399 <ul> <li>1 application key</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now VENTURE <p>For building 5 apps</p> $649 $599 <ul> <li>5 application keys</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now PRO <p>For building 25 apps</p> $849 $749 <ul> <li>25 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STUDIO <p>For building 100 apps</p> $1249 $999 <ul> <li>100 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STARTER <p>For building 1 app</p> $399 <ul> <li>1 application key</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now VENTURE <p>For building 5 apps</p> $649 $599 <ul> <li>5 application keys</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now PRO <p>For building 25 apps</p> $849 $749 <ul> <li>25 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STUDIO <p>For building 100 apps</p> $1249 $999 <ul> <li>100 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STARTER <p>For building 1 app</p> $399 <ul> <li>1 application key</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now VENTURE <p>For building 5 apps</p> $649 $599 <ul> <li>5 application keys</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now PRO <p>For building 25 apps</p> $849 $749 <ul> <li>25 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STUDIO <p>For building 100 apps</p> $1249 $999 <ul> <li>100 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STARTER <p>For building 1 app</p> $399 <ul> <li>1 application key</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now VENTURE <p>For building 5 apps</p> $649 $599 <ul> <li>5 application keys</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now PRO <p>For building 25 apps</p> $849 $749 <ul> <li>25 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STUDIO <p>For building 100 apps</p> $1249 $999 <ul> <li>100 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STARTER <p>For building 1 app</p> $399 <ul> <li>1 application key</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now VENTURE <p>For building 5 apps</p> $649 $599 <ul> <li>5 application keys</li> <li>Perpetual license</li> <li>Support via GitHub</li> <li>1 year access to latest updates</li> </ul> Buy Now PRO <p>For building 25 apps</p> $849 $749 <ul> <li>25 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now STUDIO <p>For building 100 apps</p> $1249 $999 <ul> <li>100 application keys</li> <li>Perpetual license</li> <li>Premium Support via GitHub &amp; Slack</li> <li>1 year access to latest updates</li> </ul> Buy Now <p>* Plans are easily upgradable after purchase.</p> <p>You are about to purchase</p>"},{"location":"purchase/#bgeo-confirm-title","title":"\u2014","text":"<p>You will be taken to the Transistor Software shop to complete your order.</p> Cancel Proceed to Checkout"},{"location":"capacitor/","title":"Capacitor SDK Reference","text":"capacitor-background-geolocation Buy Now"},{"location":"capacitor/#quick-links","title":"Quick links","text":"Section Description BackgroundGeolocation Primary entry point \u2014 start/stop, events, config AppConfig Lifecycle, heartbeat, scheduler, notifications HttpConfig HTTP upload endpoint, batching, auth headers GeoConfig Accuracy, distance filter, stop detection PersistenceConfig SQLite storage, TTL, templates LoggerConfig Log levels, retention, debug mode <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"capacitor/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<pre><code>export const AccuracyAuthorization = {\n</code></pre> <p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"capacitor/AccuracyAuthorization/#members","title":"Members","text":""},{"location":"capacitor/AccuracyAuthorization/#full","title":"Full","text":"<p>Full accuracy authorized.</p>"},{"location":"capacitor/AccuracyAuthorization/#reduced","title":"Reduced","text":"<p>Reduced accuracy granted.</p>"},{"location":"capacitor/ActivityConfig/","title":"ActivityConfig","text":"<pre><code>export interface ActivityConfig {\n</code></pre> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"capacitor/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"capacitor/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    // Poll activity recognition every 10 seconds (both platforms)\n    activityRecognitionInterval: 10000,\n\n    // Require \u226575% confidence before changing activity (both platforms)\n    minimumActivityRecognitionConfidence: 75,\n\n    // Disable automatic stop detection (both platforms)\n    disableStopDetection: false,\n\n    // Automatically stop when stationary (both platforms)\n    stopOnStationary: true,\n\n    // Delay motion-trigger transitions by 30s (Android only)\n    motionTriggerDelay: 30000,\n\n    // Disable platform motion activity APIs (both platforms)\n    disableMotionActivityUpdates: false,\n\n    // Delay stop-detection by 10s (iOS only)\n    stopDetectionDelay: 10000\n  }\n});\n</code></pre>"},{"location":"capacitor/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeoConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"capacitor/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Legacy (deprecated)\nBackgroundGeolocation.ready({\n  activityRecognitionInterval: 10000,\n  disableStopDetection: true,\n  stopOnStationary: true\n});\n</code></pre> <pre><code>// Current\nBackgroundGeolocation.ready({\n  activity: {\n    activityRecognitionInterval: 10000,\n    disableStopDetection: true,\n    stopOnStationary: true\n  }\n});\n</code></pre>"},{"location":"capacitor/ActivityConfig/#members","title":"Members","text":""},{"location":"capacitor/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>activityRecognitionInterval?: number;\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"capacitor/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>disableMotionActivityUpdates?: boolean;\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"capacitor/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"capacitor/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableMotionActivityUpdates: true\n  }\n});\n</code></pre>"},{"location":"capacitor/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"capacitor/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeoConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeoConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableStopDetection: true,\n  },\n  geolocation: {\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BackgroundGeolocation.changePace).</p> <p></p>"},{"location":"capacitor/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BackgroundGeolocation.changePace <code>false</code> or BackgroundGeolocation.stop explicitly.</p>"},{"location":"capacitor/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>minimumActivityRecognitionConfidence?: number;\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"capacitor/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>motionTriggerDelay?: number;\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    motionTriggerDelay: 30000\n  }\n});\n</code></pre>"},{"location":"capacitor/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>stopDetectionDelay?: number;\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"capacitor/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the GeoConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeoConfig.stopTimeout timer elapses naturally. It does not fire when BackgroundGeolocation.changePace <code>false</code> is called manually.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    stopOnStationary: true,\n  }\n}).then(() =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"capacitor/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<p><code>triggerActivities?:</code><code>TriggerActivity</code><code>|</code><code>TriggerActivity</code><code>[];</code></p> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See TriggerActivity for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>// Only trigger tracking for vehicles\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"in_vehicle\"]\n  }\n});\n\n// Only trigger tracking for on_foot, walking and running\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"on_foot\", \"walking\", \"running\"]\n  }\n});\n</code></pre>"},{"location":"capacitor/ActivityType/","title":"ActivityType","text":"<pre><code>export const ActivityType = {\n</code></pre> <p>iOS Activity Type used with GeoConfig.activityType.</p> <p>Corresponds to Apple's <code>CLActivityType</code> enum: https://developer.apple.com/documentation/corelocation/clactivitytype</p> Name Value Description Other 1 Default/unspecified activity. AutomotiveNavigation 2 Automotive navigation mode. Fitness 3 Fitness-related activity. OtherNavigation 4 Non-automotive navigation mode. Airborne 5 Airborne activity (iOS 15+)."},{"location":"capacitor/ActivityType/#members","title":"Members","text":""},{"location":"capacitor/ActivityType/#airborne","title":"Airborne","text":"<p>Airborne activity (iOS 15+).</p>"},{"location":"capacitor/ActivityType/#automotivenavigation","title":"AutomotiveNavigation","text":"<p>Automotive navigation mode.</p>"},{"location":"capacitor/ActivityType/#fitness","title":"Fitness","text":"<p>Fitness-related activity.</p>"},{"location":"capacitor/ActivityType/#other","title":"Other","text":"<p>Default/unspecified activity.</p>"},{"location":"capacitor/ActivityType/#othernavigation","title":"OtherNavigation","text":"<p>Non-automotive navigation mode.</p>"},{"location":"capacitor/AppConfig/","title":"AppConfig","text":"<pre><code>export interface AppConfig {\n</code></pre> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"capacitor/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"capacitor/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"capacitor/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BackgroundGeolocation.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"capacitor/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BackgroundGeolocation.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"capacitor/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See PermissionRationale for the full dialog configuration reference.</p>"},{"location":"capacitor/AppConfig/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"capacitor/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#members","title":"Members","text":""},{"location":"capacitor/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<p><code>background</code><code>PermissionRationale</code><code>?:</code><code>PermissionRationale</code><code>;</code></p> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside PermissionRationale fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Location updates in Android 11</p>"},{"location":"capacitor/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BackgroundGeolocation.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BackgroundGeolocation.registerHeadlessTask</p>"},{"location":"capacitor/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>heartbeatInterval?: number;\n</code></pre> <p>Rate in seconds at which BackgroundGeolocation.onHeartbeat events fire while the app is in the background.</p>"},{"location":"capacitor/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"capacitor/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BackgroundGeolocation.onHeartbeat</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60\n  }\n});\n\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n\n  // Optionally request a new location during heartbeat.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition]\", location);\n  });\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#notification","title":"notification","text":"<p><code>notification?:</code><code>NotificationConfig</code><code>;</code></p> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"Background tracking engaged\",\n      text: \"My notification text\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>preventSuspend?: boolean;\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BackgroundGeolocation.onHeartbeat</p> <pre><code>// Subscribe to heartbeat events\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n});\n\n// Enable preventSuspend via AppConfig\nBackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60,\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#schedule","title":"schedule","text":"<pre><code>schedule?: string[];\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"{DAY(s)} {START_TIME}-{END_TIME}\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1 17:30-21:00\",     // Sunday: 5:30pm\u20139pm\n      \"2-6 09:00-17:00\",   // Mon\u2013Fri: 9am\u20135pm\n      \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm\u2013midnight\n      \"7 10:00-19:00\"      // Saturday: 10am\u20137pm\n    ]\n  }\n}).then((state) =&gt; {\n  // Start the Scheduler\n  BackgroundGeolocation.startSchedule();\n});\n\n// Listen for schedule state changes\nBackgroundGeolocation.onSchedule((state) =&gt; {\n  console.log(\"[onSchedule] enabled?\", state.enabled);\n});\n\n// Later (e.g., user logout)\nBackgroundGeolocation.stopSchedule();\nBackgroundGeolocation.stop(); // if tracking is currently active\n\n// Modify schedule using setConfig\nBackgroundGeolocation.setConfig({\n  app: {\n    schedule: [\n      \"1-7 09:00-10:00\",\n      \"1-7 11:00-12:00\",\n      \"1-7 13:00-14:00\",\n      \"1-7 15:00-16:00\",\n      \"1-7 17:00-18:00\",\n      \"2,4,6 19:00-22:00\"\n    ]\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01 09:00-17:00\"\n    ]\n  }\n});\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-00:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"capacitor/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BackgroundGeolocation.startSchedule - BackgroundGeolocation.stopSchedule</p>"},{"location":"capacitor/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-12:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>scheduleUseAlarmManager?: boolean;\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\"1-7 09:00-17:00\"],\n    scheduleUseAlarmManager: true\n  }\n});\n</code></pre>"},{"location":"capacitor/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"capacitor/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"capacitor/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"capacitor/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"capacitor/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeoConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"capacitor/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"capacitor/AuthorizationConfig/","title":"AuthorizationConfig","text":"<pre><code>export interface AuthorizationConfig {\n</code></pre> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"capacitor/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"capacitor/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>const token = getMyToken(); // Your own token fetch method\n\nBackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://app.your.server.com/users/locations\",\n    autoSync: true,\n  },\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshPayload: {\n      the_refresh_token_field_name: \"{refreshToken}\"\n    },\n    expires: token.expiresAt,\n  }\n});\n</code></pre>"},{"location":"capacitor/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"capacitor/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"capacitor/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n</code></pre>"},{"location":"capacitor/AuthorizationConfig/#members","title":"Members","text":""},{"location":"capacitor/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\"\n  }\n});\n</code></pre>"},{"location":"capacitor/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>expires?: number;\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"capacitor/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>refreshHeaders?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.domain.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    },\n    refreshHeaders: {}\n  }\n});\n</code></pre>"},{"location":"capacitor/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>refreshPayload?: Record&lt;string, string&gt;;\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    }\n  }\n});\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"capacitor/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken?: string;\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"capacitor/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>refreshUrl?: string;\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p>"},{"location":"capacitor/AuthorizationConfig/#strategy","title":"strategy","text":"<pre><code>strategy: string;\n</code></pre> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"capacitor/AuthorizationEvent/","title":"AuthorizationEvent","text":"<pre><code>export interface AuthorizationEvent {\n</code></pre> <p>Authorization token refresh result delivered to BackgroundGeolocation.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onAuthorization] Token refreshed:\", event.response);\n    // Update your app's stored token.\n    const token = event.response.access_token;\n  } else {\n    console.warn(\"[onAuthorization] Refresh failed:\", event.error);\n  }\n});\n</code></pre>"},{"location":"capacitor/AuthorizationEvent/#members","title":"Members","text":""},{"location":"capacitor/AuthorizationEvent/#error","title":"error","text":"<pre><code>error: string;\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"capacitor/AuthorizationEvent/#response","title":"response","text":"<pre><code>response: any;\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"capacitor/AuthorizationEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"capacitor/AuthorizationEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"capacitor/AuthorizationStatus/","title":"AuthorizationStatus","text":"<pre><code>export const AuthorizationStatus = {\n</code></pre> <p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"capacitor/AuthorizationStatus/#members","title":"Members","text":""},{"location":"capacitor/AuthorizationStatus/#always","title":"Always","text":"<p>Location permissions authorized for always use.</p>"},{"location":"capacitor/AuthorizationStatus/#denied","title":"Denied","text":"<p>Location permissions denied by the user.</p>"},{"location":"capacitor/AuthorizationStatus/#notdetermined","title":"NotDetermined","text":"<pre><code>NotDetermined: 0,\n</code></pre> <p>User has not yet made a choice regarding location permissions.</p>"},{"location":"capacitor/AuthorizationStatus/#restricted","title":"Restricted","text":"<pre><code>Restricted: 1,\n</code></pre> <p>Location permissions are restricted (e.g., parental controls).</p>"},{"location":"capacitor/AuthorizationStatus/#wheninuse","title":"WhenInUse","text":"<p>Location permissions authorized for when-in-use (Android 10+).</p>"},{"location":"capacitor/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<pre><code>export const AuthorizationStrategy = {\n</code></pre> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"capacitor/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"capacitor/AuthorizationStrategy/#jwt","title":"Jwt","text":"<p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"capacitor/AuthorizationStrategy/#sas","title":"Sas","text":"<p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"capacitor/BackgroundGeolocation/","title":"BackgroundGeolocation","text":"<pre><code>export interface BackgroundGeolocation {\n</code></pre> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>","boost":10},{"location":"capacitor/BackgroundGeolocation/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>","boost":10},{"location":"capacitor/BackgroundGeolocation/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeoConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li>Event</li> </ul> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"react-native-background-geolocation\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a Subscription that must be removed when no longer needed:</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre> <pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#compound-configuration","title":"Compound configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"{{pluginName}}\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#event-listeners","title":"Event listeners","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getting-started","title":"Getting started","text":"<pre><code>import BackgroundGeolocation from \"{{pluginName}}\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#events_1","title":"Events","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#onactivitychange","title":"onActivityChange","text":"<p><code>onActivityChange(cb: (event:</code><code>MotionActivityEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#android","title":"Android","text":"<p>MotionActivityEvent.confidence always reports <code>100</code>.</p> <pre><code>const subscription = BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange] \", event);\n});\n</code></pre> <p>activitychange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onauthorization","title":"onAuthorization","text":"<p><code>onAuthorization(cb: (event:</code><code>AuthorizationEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>const subscription = BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS: \", event.response);\n  } else {\n    console.log(\"[authorization] ERROR: \", event.error);\n  }\n});\n</code></pre> <p>authorization</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>onConnectivityChange(cb: (event:</code><code>ConnectivityChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>const subscription = BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] \", event);\n});\n</code></pre> <p>connectivitychange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onenabledchange","title":"onEnabledChange","text":"<p><code>onEnabledChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>const subscription = BackgroundGeolocation.onEnabledChange((isEnabled) =&gt; {\n  console.log(\"[onEnabledChange] isEnabled? \", isEnabled);\n});\n</code></pre> <p>enabledchange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ongeofence","title":"onGeofence","text":"<p><code>on</code><code>Geofence</code><code>(cb: (event:</code><code>GeofenceEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] \", event);\n});\n</code></pre> <p>geofence</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>onGeofencesChange(cb: (event:</code><code>GeofencesChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeoConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  // Create map circles\n  on.forEach((geofence) =&gt; {\n    createGeofenceMarker(geofence);\n  });\n\n  // Remove map circles\n  off.forEach((identifier) =&gt; {\n    removeGeofenceMarker(identifier);\n  });\n});\n</code></pre> <p>geofenceschange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onheartbeat","title":"onHeartbeat","text":"<p><code>onHeartbeat(cb: (event:</code><code>HeartbeatEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The Location provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    heartbeatInterval: 60,\n    preventSuspend: true  // required for iOS\n  }\n});\n\nconst subscription = BackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat] \", event);\n\n  // Optionally fetch a fresh location.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition] \", location);\n  });\n});\n</code></pre> <p>heartbeat</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onhttp","title":"onHttp","text":"<p><code>onHttp(cb: (event:</code><code>HttpEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onHttp((response) =&gt; {\n  const status = response.status;\n  const success = response.success;\n  const responseText = response.responseText;\n  console.log(\"[onHttp] \", response);\n});\n</code></pre> <p>http</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onlocation","title":"onLocation","text":"<p><code>on</code><code>Location</code><code>(cb: (location:</code><code>Location</code><code>) =&gt; void, onError?: (err:</code><code>LocationError</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a LocationError code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with Location.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation] success: \", location);\n}, (error) =&gt; {\n  console.log(\"[onLocation] ERROR: \", error);\n});\n</code></pre> <p>location</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onmotionchange","title":"onMotionChange","text":"<p><code>onMotionChange(cb: (event:</code><code>MotionChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeoConfig.stopTimeout</p> <pre><code>const subscription = BackgroundGeolocation.onMotionChange((event: MotionChangeEvent) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device has just started MOVING \", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device has just STOPPED: \", event.location);\n  }\n});\n</code></pre> <p>motionchange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onnotificationaction","title":"onNotificationAction","text":"<p><code>onNotificationAction(cb: (buttonId: string) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onpowersavechange","title":"onPowerSaveChange","text":"<p><code>onPowerSaveChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const subscription = BackgroundGeolocation.onPowerSaveChange((isPowerSaveMode) =&gt; {\n  console.log(\"[onPowerSaveChange]: \", isPowerSaveMode);\n});\n</code></pre> <p>powersavechange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onproviderchange","title":"onProviderChange","text":"<p><code>onProviderChange(cb: (event:</code><code>ProviderChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>const subscription = BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]: \", event);\n\n  switch (event.status) {\n    case BackgroundGeolocation.AuthorizationStatus.Denied:\n      // Android &amp; iOS\n      console.log(\"- Location authorization denied\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.Always:\n      // Android &amp; iOS\n      console.log(\"- Location always granted\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.WhenInUse:\n      // iOS only\n      console.log(\"- Location WhenInUse granted\");\n      break;\n  }\n});\n</code></pre> <p>providerchange</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#onschedule","title":"onSchedule","text":"<p><code>onSchedule(cb: (state:</code><code>State</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>const subscription = BackgroundGeolocation.onSchedule((state) =&gt; {\n  if (state.enabled) {\n    console.log(\"[onSchedule] scheduled start tracking\");\n  } else {\n    console.log(\"[onSchedule] scheduled stop tracking\");\n  }\n});\n</code></pre> <p>schedule</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#methods","title":"Methods","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#changepace","title":"changePace","text":"<p><code>changePace(isMoving: boolean): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>BackgroundGeolocation.changePace(true);  // location services ON (\"moving\")\nBackgroundGeolocation.changePace(false); // location services OFF (\"stationary\")\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#destroytransistorauthorizationtoken","title":"destroyTransistorAuthorizationToken","text":"<p><code>destroy</code><code>TransistorAuthorizationToken</code><code>(url: string): Promise&lt;void&gt;;</code></p> <p>Destroy a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#findorcreatetransistorauthorizationtoken","title":"findOrCreateTransistorAuthorizationToken","text":"<p><code>findOrCreateTransistorAuthorizationToken(orgname: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>getCurrentPosition(options?:</code><code>CurrentPositionRequest</code><code>): Promise&lt;</code><code>Location</code><code>&gt;;</code></p> <p>Retrieve the current Location.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a LocationError.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#options","title":"Options","text":"<p>See CurrentPositionRequest.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#error-codes_1","title":"Error Codes","text":"<p>See LocationError.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with Location.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // 30 second timeout to fetch location\n  maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.\n  desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.\n  samples: 3,           // How many location samples to attempt.\n  extras: {             // Custom meta-data.\n    \"route_id\": 123\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getdeviceinfo","title":"getDeviceInfo","text":"<p><code>get</code><code>DeviceInfo</code><code>(): Promise&lt;</code><code>DeviceInfo</code><code>&gt;;</code></p> <p>Returns device information.</p> <pre><code>const deviceInfo = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(deviceInfo);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getodometer","title":"getOdometer","text":"<pre><code>getOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the current odometer reading in meters.</p> <p>The SDK continuously accumulates distance traveled between recorded locations.</p> <p>Warning</p> <p>Odometer accuracy depends on location accuracy. Noisy or inaccurate locations introduce error into accumulated distance. Use LocationFilter.odometerAccuracyThreshold to filter low-accuracy samples from odometer calculations.</p> <p>See also - LocationFilter.odometerAccuracyThreshold - <code>resetOdometer</code> / <code>setOdometer</code></p> <pre><code>const odometer = await BackgroundGeolocation.getOdometer();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getstate","title":"getState","text":"<p><code>get</code><code>State</code><code>(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Return the current State of the SDK, including all Config parameters.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] \", state.enabled, state.trackingMode);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ready","title":"ready","text":"<p><code>ready(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n  http: {\n    url: \"http://your.server.com\",\n    headers: { \"my-auth-token\": \"secret-token\" },\n  }\n});\nconsole.log(\"[ready] success\", state);\n</code></pre> <pre><code>BackgroundGeolocation.reset();\n// Reset to documented default-values with overrides\nbgGeo.reset({\n  distanceFilter:  10\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#registerheadlesstask","title":"registerHeadlessTask","text":"<p><code>registerHeadlessTask(callback: (event:</code><code>HeadlessEvent</code><code>) =&gt; Promise&lt;void&gt;): void;</code></p> <p>Android only Registers a headless-task callback for Android background events when the app has been terminated with AppConfig.stopOnTerminate<code>:false</code>.</p> <p>The callback receives a HeadlessEvent with a <code>name</code> (event name) and <code>params</code> (event data).</p> <p>Warning</p> <p>You must call <code>registerHeadlessTask</code> in your application root file (e.g. <code>index.js</code>), not inside a component or behind a UI action.</p> <p>Warning</p> <p>Your function must be declared <code>async</code>. Await all work inside it \u2014 the headless task is automatically terminated after the last line executes.</p> <p>Note</p> <p>Javascript headless callbacks are not supported by Cordova or Capacitor.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#debugging","title":"Debugging","text":"<p>While implementing your headless task, observe Android logs via:</p> <pre><code>$ adb logcat *:S TSLocationManager:V ReactNativeJS:V\n</code></pre> <p>See also - \ud83d\udcd8 Android Headless Mode - AppConfig.enableHeadless</p> <pre><code>const BackgroundGeolocationHeadlessTask = async (event) =&gt; {\n  const params = event.params;\n  console.log(\"[BackgroundGeolocation HeadlessTask] -\", event.name, params);\n\n  switch (event.name) {\n    case \"terminate\":\n      // Use await for async tasks\n      const location = await BackgroundGeolocation.getCurrentPosition({\n        samples: 1,\n        persist: false\n      });\n      console.log(\"[BackgroundGeolocation HeadlessTask] - getCurrentPosition:\", location);\n      break;\n  }\n  // You must await all work you do in your task.\n  // Headless-tasks are automatically terminated after executing the last line.\n  await doWork();\n}\n\nBackgroundGeolocation.registerHeadlessTask(BackgroundGeolocationHeadlessTask);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#removelisteners","title":"removeListeners","text":"<pre><code>removeListeners(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all event listeners.</p> <p>Calls Subscription.remove on all active subscriptions.</p> <pre><code>BackgroundGeolocation.removeListeners();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#reset","title":"reset","text":"<p><code>reset(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Reset the SDK configuration to documented default values.</p> <p>If an optional Config is provided, it is applied after the reset.</p> <pre><code>BackgroundGeolocation.reset();\n// Reset to default values with overrides\nBackgroundGeolocation.reset({\n  geolocation: { distanceFilter: 10 }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#resetodometer","title":"resetOdometer","text":"<pre><code>resetOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>const location = await BackgroundGeolocation.resetOdometer();\nconsole.log(\"[resetOdometer] reset at: \", location);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#setconfig","title":"setConfig","text":"<p><code>set</code><code>Config</code><code>(config: Partial&lt;</code><code>Config</code><code>&gt;): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Update the SDK's Config at runtime.</p> <p>The supplied Config is merged into the current configuration and applied immediately. Use this after <code>ready</code> has been called to change settings dynamically.</p> <pre><code>const state = await BackgroundGeolocation.setConfig({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 100.0,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\nconsole.log(\"[setConfig] success: \", state);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#setodometer","title":"setOdometer","text":"<pre><code>setOdometer(value: number): Promise&lt;number&gt;;\n</code></pre> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>const location = await BackgroundGeolocation.setOdometer(1234.56);\nconsole.log(\"[setOdometer] set at: \", location);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#start","title":"start","text":"<p><code>start(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>const state = await BackgroundGeolocation.start();\nconsole.log(\"[start] success - \", state);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#startgeofences","title":"startGeofences","text":"<p><code>startGeofences(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Add a geofence.\nBackgroundGeolocation.addGeofence({\n  notifyOnExit: true,\n  radius: 200,\n  identifier: \"ZONE_OF_INTEREST\",\n  latitude: 37.234232,\n  longitude: 42.234234\n});\n\n// Listen to geofence events.\nBackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] - \", event);\n});\n\n// Configure and start in geofences-only mode.\nBackgroundGeolocation.ready({\n  http: { url: \"http://my.server.com\", autoSync: true }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#startschedule","title":"startSchedule","text":"<pre><code>startSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>const state = await BackgroundGeolocation.startSchedule();\nconsole.log(\"[startSchedule] success: \", state);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#stop","title":"stop","text":"<p><code>stop(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>BackgroundGeolocation.stop();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#stop-tracking-and-the-scheduler","title":"Stop tracking and the scheduler","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#stopschedule","title":"stopSchedule","text":"<pre><code>stopSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>await BackgroundGeolocation.stopSchedule();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#stop-the-scheduler-and-active-tracking","title":"Stop the scheduler and active tracking","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#watchposition","title":"watchPosition","text":"<p><code>watchPosition(options:</code><code>WatchPositionRequest</code><code>, locationCallback: (location:</code><code>Location</code><code>) =&gt; void, errorCallback?: (errorCode: number) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a Subscription that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>onResume() async {\n  // Start watching position while app in foreground, retaining the return Subscription.\n  this.watchPositionSubscription = await BackgroundGeolocation.watchPosition({\n    interval: 1000,\n    extras: {foo: \"bar\"}\n  }, (location) =&gt; {\n    console.log(\"[watchPosition] -\", location);\n  }, (errorCode) =&gt; {\n    console.log(\"[watchPosition] ERROR -\", errorCode);\n  });\n}\n\nonSuspend() {\n  // Halt watching position when app goes to background.\n  this.watchPositionSubscription.remove();\n  this.watchPositionSubscription = null;\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#geofencing","title":"Geofencing","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#addgeofence","title":"addGeofence","text":"<p><code>add</code><code>Geofence</code><code>(geofence:</code><code>Geofence</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 150,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: false,\n  notifyOnDwell: true,\n  loiteringDelay: 30000,  // 30 seconds\n  extras: {               // Optional arbitrary meta-data\n    zone_id: 1234\n  }\n}).then((success) =&gt; {\n  console.log(\"[addGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[addGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#addgeofences","title":"addGeofences","text":"<p><code>addGeofences(geofences:</code><code>Geofence</code><code>[]): Promise&lt;boolean&gt;;</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>const geofences = [{\n  identifier: \"foo\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}, {\n  identifier: \"bar\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}];\n\nBackgroundGeolocation.addGeofences(geofences);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#geofenceexists","title":"geofenceExists","text":"<pre><code>geofenceExists(identifier: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const exists = await BackgroundGeolocation.geofenceExists(\"HOME\");\nconsole.log(\"[geofenceExists] \", exists);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getgeofence","title":"getGeofence","text":"<p><code>get</code><code>Geofence</code><code>(identifier: string): Promise&lt;</code><code>Geofence</code><code>&gt;;</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofence = await BackgroundGeolocation.getGeofence(\"HOME\");\nconsole.log(\"[getGeofence] \", geofence);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getgeofences","title":"getGeofences","text":"<p><code>getGeofences(): Promise&lt;</code><code>Geofence</code><code>[]&gt;;</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]: \", geofences);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#removegeofence","title":"removeGeofence","text":"<p><code>remove</code><code>Geofence</code><code>(identifier: string): Promise&lt;boolean&gt;;</code></p> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofence(\"Home\").then((success) =&gt; {\n  console.log(\"[removeGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[removeGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#removegeofences","title":"removeGeofences","text":"<pre><code>removeGeofences(identifiers?: string[]): Promise&lt;boolean&gt;;\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofences();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#data-management","title":"Data Management","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#destroylocation","title":"destroyLocation","text":"<p><code>destroy</code><code>Location</code><code>(uuid: string): Promise&lt;void&gt;;</code></p> <p>Remove a single location by Location.uuid.</p> <pre><code>await BackgroundGeolocation.destroyLocation(location.uuid);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#destroylocations","title":"destroyLocations","text":"<pre><code>destroyLocations(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>await BackgroundGeolocation.destroyLocations();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getcount","title":"getCount","text":"<pre><code>getCount(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>const count = await BackgroundGeolocation.getCount();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#getlocations","title":"getLocations","text":"<pre><code>getLocations(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Retrieve all Location records stored in the SDK's SQLite database.</p> <pre><code>const locations = await BackgroundGeolocation.getLocations();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#sync","title":"sync","text":"<pre><code>sync(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const records = await BackgroundGeolocation.sync();\nconsole.log(\"[sync] success: \", records);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#authorization","title":"Authorization","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#getproviderstate","title":"getProviderState","text":"<p><code>getProviderState(): Promise&lt;</code><code>ProviderChangeEvent</code><code>&gt;;</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>const providerState = await BackgroundGeolocation.getProviderState();\nconsole.log(\"- Provider state: \", providerState);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#requestpermission","title":"requestPermission","text":"<p><code>requestPermission(): Promise&lt;</code><code>AuthorizationStatus</code><code>&gt;;</code></p> <p>Manually request location permission using the configured GeoConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeoConfig.locationAuthorizationRequest - GeoConfig.disableLocationAuthorizationAlert - GeoConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log('[providerchange]', event);\n});\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: 'Always' }\n});\n\ntry {\n  const status = await BackgroundGeolocation.requestPermission();\n  console.log('[requestPermission] success: ', status);\n} catch (status) {\n  console.warn('[requestPermission] FAILURE: ', status);\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<p><code>requestTemporaryFullAccuracy(purposeKey: string): Promise&lt;</code><code>AccuracyAuthorization</code><code>&gt;;</code></p> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Supply \"Purpose\" key from Info.plist as 1st argument.\n    BackgroundGeolocation.requestTemporaryFullAccuracy(\"Delivery\").then((accuracyAuthorization) =&gt; {\n      if (accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Full) {\n        console.log('[requestTemporaryFullAccuracy] GRANTED: ', accuracyAuthorization);\n      } else {\n        console.log('[requestTemporaryFullAccuracy] DENIED: ', accuracyAuthorization);\n      }\n    }).catch((error) =&gt; {\n      console.warn(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: \", error);\n    });\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#background-tasks","title":"Background Tasks","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>startBackgroundTask(): Promise&lt;number&gt;;\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ios_2","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#android_2","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>onLocation(location) {\n  console.log(\"[location] \", location);\n\n  // Perform some long-running task (eg: HTTP request)\n  BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n    performLongRunningTask().then(() =&gt; {\n      // When your long-running task is complete, signal completion of taskId.\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    }).catch((error) =&gt; {\n      // Be sure to catch errors: never leave your background-task hanging.\n      console.error(error);\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    });\n  });\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>stopBackgroundTask(taskId: number): Promise&lt;void&gt;;\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n  // Perform some long-running task (eg: HTTP request)\n  performLongRunningTask().then(() =&gt; {\n    // When your long-running task is complete, signal completion of taskId.\n    BackgroundGeolocation.stopBackgroundTask(taskId);\n  });\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#logging","title":"Logging","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#playsound","title":"playSound","text":"<pre><code>playSound(soundId: string | number): void;\n</code></pre> <p>Play a system sound.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ios_3","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#android_3","title":"Android","text":"<p>Provide a sound name string.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#setloglevel","title":"setLogLevel","text":"<p><code>set</code><code>LogLevel</code><code>(level:</code><code>LogLevel</code><code>): Promise&lt;void&gt;;</code></p> <p>Sets the LoggerConfig.logLevel.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#device","title":"Device","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#getsensors","title":"getSensors","text":"<p><code>get</code><code>Sensors</code><code>(): Promise&lt;</code><code>Sensors</code><code>&gt;;</code></p> <p>Returns the availability of motion sensors: accelerometer, gyroscope, and magnetometer.</p> <p>These sensors power the motion activity-recognition system. When any sensor is absent (particularly on low-end Android devices), motion recognition performance degrades significantly.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(sensors);\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>isPowerSaveMode(): Promise&lt;boolean&gt;;\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#ios_4","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#android_4","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const isPowerSaveMode = await BackgroundGeolocation.isPowerSaveMode();\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#properties","title":"Properties","text":"","boost":10},{"location":"capacitor/BackgroundGeolocation/#accuracyauthorization","title":"AccuracyAuthorization","text":"<p><code>AccuracyAuthorization</code><code>: object;</code></p> <p>Enum namespace indicating whether the user granted full or reduced location accuracy. [iOS 14+]</p> <p>Used by <code>ProviderChangeEvent.accuracyAuthorization</code> and <code>requestTemporaryFullAccuracy</code>.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization ===\n      BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Handle reduced-accuracy case\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#activitytype","title":"ActivityType","text":"<p><code>ActivityType</code><code>: object;</code></p> <p>iOS only Enum namespace specifying the type of user activity (AutomotiveNavigation, Fitness, OtherNavigation, etc).</p> <p>Used by GeoConfig.activityType.</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#authorizationstatus","title":"AuthorizationStatus","text":"<p><code>AuthorizationStatus</code><code>: object;</code></p> <p>Enum namespace representing the OS-level authorization state for location services (Denied, Restricted, Always, WhenInUse).</p> <p>Returned from <code>requestPermission()</code> and <code>onProviderChange</code>.</p> <pre><code>const status = await BackgroundGeolocation.requestPermission();\nif (status === BackgroundGeolocation.AuthorizationStatus.Always) {\n  // Good to start tracking\n}\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#authorizationstrategy","title":"AuthorizationStrategy","text":"<p><code>AuthorizationStrategy</code><code>: object;</code></p> <p>Enum namespace defining how the HTTP service performs authorization.</p> <p>Includes basic, JWT, and custom strategies. Used by <code>AuthorizationConfig.strategy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: BackgroundGeolocation.AuthorizationStrategy.Jwt\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#desiredaccuracy","title":"DesiredAccuracy","text":"<p><code>DesiredAccuracy</code><code>: object;</code></p> <p>Enum namespace controlling the native location engine's target accuracy.</p> <p>Higher accuracy consumes more battery. Used by <code>GeoConfig.desiredAccuracy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#event","title":"Event","text":"<p><code>Event</code><code>: object;</code></p> <p>Enum namespace of all event names emitted by the SDK (<code>location</code>, <code>geofence</code>, <code>motionchange</code>, <code>heartbeat</code>, etc).</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#httpmethod","title":"HttpMethod","text":"<p><code>HttpMethod</code><code>: object;</code></p> <p>Enum namespace defining the HTTP method used for location uploads (POST, PUT, etc).</p> <p>Used by <code>HttpConfig.method</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    method: BackgroundGeolocation.HttpMethod.Post\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#kalmanprofile","title":"KalmanProfile","text":"<p><code>KalmanProfile</code><code>: object;</code></p> <p>Enum namespace selecting a preset Kalman filter tuning profile (aggressive, moderate, or relaxed smoothing).</p> <p>Used by <code>LocationFilter.kalmanProfile</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      kalmanProfile: BackgroundGeolocation.KalmanProfile.Aggressive\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#locationfilterpolicy","title":"LocationFilterPolicy","text":"<p><code>LocationFilterPolicy</code><code>: object;</code></p> <p>Enum namespace selecting the filtering engine policy for noise-reduction and smoothing.</p> <p>Used by <code>LocationFilter.policy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: BackgroundGeolocation.LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#locationrequest","title":"LocationRequest","text":"<p><code>LocationRequest</code><code>: object;</code></p> <p>iOS only Enum namespace defining the type of location permission request (Always, WhenInUse, or Any). Used by <code>GeoConfig.locationAuthorizationRequest</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: BackgroundGeolocation.LocationRequest.Always\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#loglevel","title":"LogLevel","text":"<p><code>LogLevel</code><code>: object;</code></p> <p>Enum namespace controlling verbosity of the SDK logger.</p> <p>Used by <code>LoggerConfig.logLevel</code>. Values range from silent (<code>Off</code>) to extremely verbose (<code>Verbose</code>).</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#notificationpriority","title":"NotificationPriority","text":"<p><code>NotificationPriority</code><code>: object;</code></p> <p>Android only Enum namespace controlling Android foreground-service notification priority and icon placement (top, bottom, hidden).</p> <p>Used by <code>NotificationConfig.priority</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: BackgroundGeolocation.NotificationPriority.High\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#persistmode","title":"PersistMode","text":"<p><code>PersistMode</code><code>: object;</code></p> <p>Enum namespace controlling which records the SDK persists to SQLite: locations only, geofences only, both, or none.</p> <p>Used by <code>PersistenceConfig.persistMode</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    persistMode: BackgroundGeolocation.PersistMode.All\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#triggeractivity","title":"TriggerActivity","text":"<p><code>TriggerActivity</code><code>: object;</code></p> <p>Enum namespace defining which physical motion activities can trigger motion-detection transitions (still \u2192 moving).</p> <p>Used by <code>ActivityConfig.triggerActivities</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\n      BackgroundGeolocation.TriggerActivity.InVehicle\n    ]\n  }\n});\n</code></pre>","boost":10},{"location":"capacitor/BackgroundGeolocation/#devicesettings","title":"deviceSettings","text":"<p><code>deviceSettings:</code><code>DeviceSettings</code><code>;</code></p> <p>DeviceSettings API</p>","boost":10},{"location":"capacitor/BackgroundGeolocation/#logger","title":"logger","text":"<p><code>logger:</code><code>Logger</code><code>;</code></p> <p>Logger API</p>","boost":10},{"location":"capacitor/Battery/","title":"Battery","text":"<pre><code>export interface Battery {\n</code></pre> <p>Device battery state at the time a Location was recorded.</p>"},{"location":"capacitor/Battery/#members","title":"Members","text":""},{"location":"capacitor/Battery/#is_charging","title":"is_charging","text":"<pre><code>is_charging: boolean;\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"capacitor/Battery/#level","title":"level","text":"<pre><code>level: number;\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"capacitor/Config/","title":"Config","text":"<pre><code>export interface Config {\n</code></pre> <p>Root configuration object passed to BackgroundGeolocation.ready and BackgroundGeolocation.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"capacitor/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"capacitor/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeoConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BackgroundGeolocation.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BackgroundGeolocation.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"capacitor/Config/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"capacitor/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre>"},{"location":"capacitor/Config/#full-configuration","title":"Full configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from '{{pluginName}}';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"capacitor/Config/#members","title":"Members","text":""},{"location":"capacitor/Config/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"capacitor/Config/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"capacitor/Config/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"capacitor/Config/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeoConfig.</p>"},{"location":"capacitor/Config/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"capacitor/Config/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"capacitor/Config/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"capacitor/Config/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"capacitor/Config/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre> <pre><code>// Base url to Transistor Demo Server.\nconst url = \"http://tracker.transistorsoft.com\";\n\n// Register for an authorization token from server.\nconst token = await\n  BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\"my-company-name\", \"my-username\");\n\nBackgroundGeolocation.ready({\n  url: url + \"/api/locations\",\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: url + \"/v2/refresh_token\",\n    refreshPayload: {\n      refresh_token: \"{refreshToken}\"\n    },\n    expires: token.expires\n  }\n});\n</code></pre>"},{"location":"capacitor/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<pre><code>export interface ConnectivityChangeEvent {\n</code></pre> <p>Network connectivity change delivered to BackgroundGeolocation.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] connected:\", event.connected);\n});\n</code></pre>"},{"location":"capacitor/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"capacitor/ConnectivityChangeEvent/#connected","title":"connected","text":"<pre><code>connected: boolean;\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"capacitor/Coords/","title":"Coords","text":"<pre><code>export interface Coords {\n</code></pre> <p>Geographic coordinates attached to a Location.</p>"},{"location":"capacitor/Coords/#members","title":"Members","text":""},{"location":"capacitor/Coords/#accuracy","title":"accuracy","text":"<pre><code>accuracy: number;\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"capacitor/Coords/#altitude","title":"altitude","text":"<pre><code>altitude?: number;\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"capacitor/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"capacitor/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"capacitor/Coords/#altitude_accuracy","title":"altitude_accuracy","text":"<pre><code>altitude_accuracy?: number;\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"capacitor/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"capacitor/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"capacitor/Coords/#ellipsoidal_altitude","title":"ellipsoidal_altitude","text":"<pre><code>ellipsoidal_altitude?: number;\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"capacitor/Coords/#floor","title":"floor","text":"<pre><code>floor?: number;\n</code></pre> <p>iOS only Floor within a building, when indoor-positioning hardware (e.g. Bluetooth beacons) is available.</p>"},{"location":"capacitor/Coords/#heading","title":"heading","text":"<pre><code>heading?: number;\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"capacitor/Coords/#heading_accuracy","title":"heading_accuracy","text":"<pre><code>heading_accuracy?: number;\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"capacitor/Coords/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"capacitor/Coords/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"capacitor/Coords/#speed","title":"speed","text":"<pre><code>speed?: number;\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"capacitor/Coords/#speed_accuracy","title":"speed_accuracy","text":"<pre><code>speed_accuracy?: number;\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"capacitor/CurrentPositionRequest/","title":"CurrentPositionRequest","text":"<pre><code>export interface CurrentPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.getCurrentPosition.</p> <p>All fields are optional. The SDK always requests location from the native API at maximum accuracy (DesiredAccuracy.High), regardless of the <code>desiredAccuracy</code> threshold set here.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // seconds before giving up\n  maximumAge: 5000,     // accept a cached fix up to 5000 ms old\n  desiredAccuracy: 10,  // stop sampling when accuracy \u2264 10 m\n  samples: 3,           // take up to 3 samples and pick the best\n  extras: {\n    route_id: 123\n  }\n});\n</code></pre>"},{"location":"capacitor/CurrentPositionRequest/#members","title":"Members","text":""},{"location":"capacitor/CurrentPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<pre><code>desiredAccuracy?: number;\n</code></pre> <p>Accuracy threshold in meters. The SDK stops sampling as soon as it receives a location with <code>accuracy \u2264 desiredAccuracy</code> and returns it immediately. Defaults to GeoConfig.stationaryRadius.</p> <p>Note</p> <p>This value is a stopping threshold, not a hardware accuracy setting. The SDK always requests locations at DesiredAccuracy.High from the native API regardless of this value.</p>"},{"location":"capacitor/CurrentPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to the returned location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"capacitor/CurrentPositionRequest/#maximumage","title":"maximumAge","text":"<pre><code>maximumAge?: number;\n</code></pre> <p>Accept the most recently recorded location if it is no older than this value in milliseconds. Default <code>0</code> (always fetch a fresh fix).</p>"},{"location":"capacitor/CurrentPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist the returned location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"capacitor/CurrentPositionRequest/#samples","title":"samples","text":"<pre><code>samples?: number;\n</code></pre> <p>Maximum number of location samples to collect before returning the most accurate result. Default <code>3</code>. Only the final selected location is persisted.</p>"},{"location":"capacitor/CurrentPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in seconds to wait for a location fix. Default <code>30</code>.</p> <p>If the timeout expires before a satisfactory location is found, the Promise rejects with a LocationError.</p>"},{"location":"capacitor/DesiredAccuracy/","title":"DesiredAccuracy","text":"<pre><code>export const DesiredAccuracy = {\n</code></pre> <p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"capacitor/DesiredAccuracy/#members","title":"Members","text":""},{"location":"capacitor/DesiredAccuracy/#high","title":"High","text":"<p>High accuracy (e.g., 0-10m).</p>"},{"location":"capacitor/DesiredAccuracy/#low","title":"Low","text":"<p>Low accuracy (e.g., 100m).</p>"},{"location":"capacitor/DesiredAccuracy/#lowest","title":"Lowest","text":"<p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"capacitor/DesiredAccuracy/#medium","title":"Medium","text":"<p>Medium accuracy (e.g., 10m).</p>"},{"location":"capacitor/DesiredAccuracy/#navigation","title":"Navigation","text":"<p>Navigation-level accuracy (e.g., 0-10m).</p>"},{"location":"capacitor/DesiredAccuracy/#verylow","title":"VeryLow","text":"<p>Very low accuracy (e.g., 1000m).</p>"},{"location":"capacitor/DeviceInfo/","title":"DeviceInfo","text":"<pre><code>export interface DeviceInfo {\n</code></pre> <p>Basic device hardware and OS information returned by BackgroundGeolocation.getDeviceInfo.</p> <pre><code>const info = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(`[DeviceInfo] ${info.manufacturer} ${info.model} (${info.platform})`);\n</code></pre>"},{"location":"capacitor/DeviceInfo/#members","title":"Members","text":""},{"location":"capacitor/DeviceInfo/#framework","title":"framework","text":"<pre><code>framework: string;\n</code></pre> <p>Development framework hosting the SDK.</p> <p>Examples: <code>\"react-native\"</code>, <code>\"capacitor\"</code>, <code>\"cordova\"</code>, <code>\"flutter\"</code></p>"},{"location":"capacitor/DeviceInfo/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer.</p> <p>Examples: <code>\"Apple\"</code>, <code>\"Google\"</code>, <code>\"Samsung\"</code>, <code>\"Huawei\"</code></p>"},{"location":"capacitor/DeviceInfo/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model identifier.</p> <p>Examples: <code>\"iPhone15,2\"</code>, <code>\"Pixel 8 Pro\"</code>, <code>\"SM-G991B\"</code></p>"},{"location":"capacitor/DeviceInfo/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"iOS\"</code> or <code>\"Android\"</code>.</p>"},{"location":"capacitor/DeviceInfo/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>OS version string.</p> <p>Examples: <code>\"18.1\"</code>, <code>\"14\"</code>, <code>\"14.1.1\"</code></p>"},{"location":"capacitor/DeviceSettings/","title":"DeviceSettings Android only","text":"<pre><code>export interface DeviceSettings {\n</code></pre> <p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>const isIgnoring = await BackgroundGeolocation.deviceSettings.isIgnoringBatteryOptimizations();\nif (!isIgnoring) {\n  const req = await BackgroundGeolocation.deviceSettings.showIgnoreBatteryOptimizations();\n  if (!req.seen) {\n    const confirmed = await showMyConfirmDialog({\n      title: 'Settings request',\n      text: 'Please disable battery optimizations for this app'\n    });\n    if (confirmed) {\n      await BackgroundGeolocation.deviceSettings.show(req);\n    }\n  }\n}\n</code></pre>"},{"location":"capacitor/DeviceSettings/#members","title":"Members","text":""},{"location":"capacitor/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>isIgnoringBatteryOptimizations(): Promise&lt;boolean&gt;;\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p>"},{"location":"capacitor/DeviceSettings/#show","title":"show","text":"<p><code>show(request:</code><code>DeviceSettingsRequest</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>const req = await BackgroundGeolocation.deviceSettings.showPowerManager();\nif (!req.seen) {\n  await BackgroundGeolocation.deviceSettings.show(req);\n}\n</code></pre>"},{"location":"capacitor/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<p><code>showIgnoreBatteryOptimizations(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p>"},{"location":"capacitor/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<p><code>showPowerManager(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p>"},{"location":"capacitor/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<pre><code>export interface DeviceSettingsRequest {\n</code></pre> <p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"capacitor/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"capacitor/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>action: string;\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"capacitor/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>lastSeenAt: Date;\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"capacitor/DeviceSettingsRequest/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer (e.g. <code>\"Huawei\"</code>, <code>\"Samsung\"</code>).</p>"},{"location":"capacitor/DeviceSettingsRequest/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model name (e.g. <code>\"P40\"</code>, <code>\"SM-G991B\"</code>).</p>"},{"location":"capacitor/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>seen: boolean;\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"capacitor/DeviceSettingsRequest/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>Android OS version string.</p>"},{"location":"capacitor/Event/","title":"Event","text":"<pre><code>export const Event = {\n</code></pre> <p>Supported event names emitted by the SDK.</p>"},{"location":"capacitor/Event/#members","title":"Members","text":""},{"location":"capacitor/Event/#activitychange","title":"ActivityChange","text":"<p>Emitted when a motion-activity change is detected.</p>"},{"location":"capacitor/Event/#authorization","title":"Authorization","text":"<p>Emitted when the plugin's authorization state changes.</p>"},{"location":"capacitor/Event/#boot","title":"Boot","text":"<p>Emitted when the plugin boots.</p>"},{"location":"capacitor/Event/#connectivitychange","title":"ConnectivityChange","text":"<p>Emitted when the device's network connectivity changes.</p>"},{"location":"capacitor/Event/#enabledchange","title":"EnabledChange","text":"<p>Emitted when the plugin's enabled-state changes.</p>"},{"location":"capacitor/Event/#geofence","title":"Geofence","text":"<p>Emitted when a geofence transition occurs.</p>"},{"location":"capacitor/Event/#geofenceschange","title":"GeofencesChange","text":"<p>Emitted when geofences are added/removed/activated/deactivated.</p>"},{"location":"capacitor/Event/#heartbeat","title":"Heartbeat","text":"<p>Emitted at the configured <code>Config.heartbeatInterval</code> when the plugin is running.</p>"},{"location":"capacitor/Event/#http","title":"Http","text":"<p>Emitted when an HTTP request is sent to the configured <code>Config.http.url</code> endpoint.</p>"},{"location":"capacitor/Event/#location","title":"Location","text":"<p>Emitted when a location is recorded.</p>"},{"location":"capacitor/Event/#motionchange","title":"MotionChange","text":"<p>Emitted when a motion-change is detected.</p>"},{"location":"capacitor/Event/#notification","title":"Notification","text":"<p>Emitted when a user interacts with a plugin-generated notification.</p>"},{"location":"capacitor/Event/#powersavechange","title":"PowerSaveChange","text":"<p>Emitted when the device enters or exits power-saving mode.</p>"},{"location":"capacitor/Event/#providerchange","title":"ProviderChange","text":"<p>Emitted when the device location-provider changes state.</p>"},{"location":"capacitor/Event/#schedule","title":"Schedule","text":"<p>Emitted when a configured schedule is triggered.</p>"},{"location":"capacitor/Event/#terminate","title":"Terminate","text":"<p>Emitted when the plugin is terminated.</p>"},{"location":"capacitor/GeoConfig/","title":"GeoConfig","text":"<pre><code>export interface GeoConfig {\n</code></pre> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"capacitor/GeoConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"capacitor/GeoConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BackgroundGeolocation.ready or BackgroundGeolocation.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    locationAuthorizationRequest: \"Always\"\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"capacitor/GeoConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"capacitor/GeoConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BackgroundGeolocation.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"capacitor/GeoConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BackgroundGeolocation.onProviderChange.</p>"},{"location":"capacitor/GeoConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"capacitor/GeoConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilter for full documentation.</p>"},{"location":"capacitor/GeoConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/GeoConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/GeoConfig/#members","title":"Members","text":""},{"location":"capacitor/GeoConfig/#activitytype","title":"activityType","text":"<p><code>activityType?:</code><code>ActivityType</code><code>;</code></p> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on ActivityType.</p> Name ActivityType.Other ActivityType.AutomotiveNavigation ActivityType.Fitness ActivityType.OtherNavigation ActivityType.Airborne <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    activityType: ActivityType.Other,\n  },\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>allowIdenticalLocations?: boolean;\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"capacitor/GeoConfig/#defertime","title":"deferTime","text":"<pre><code>deferTime?: number;\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"capacitor/GeoConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>disableElasticity?: boolean;\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"capacitor/GeoConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>disableLocationAuthorizationAlert?: boolean;\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BackgroundGeolocation.onProviderChange.</p>"},{"location":"capacitor/GeoConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"capacitor/GeoConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BackgroundGeolocation.onProviderChange - BackgroundGeolocation.start - BackgroundGeolocation.requestPermission</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange] \", event);\n\n  if (!provider.enabled) {\n    alert(\"Please enable location services\");\n  }\n});\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    disableLocationAuthorizationAlert: true\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity\u2013based stop-detection system.</p> <p>When <code>true</code>, the SDK ignores platform motion-activity signals when determining whether the device is stationary. This affects how and when location services are automatically turned off on both platforms.</p>"},{"location":"capacitor/GeoConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. iOS location services then turn off automatically after exactly 15 minutes of no motion \u2014 you lose control over <code>stopTimeout</code>.</p> <p>To prevent iOS from ever automatically disabling location services, also set</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    disableStopDetection: true,\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With <code>disableStopDetection: true</code> and <code>pausesLocationUpdatesAutomatically: false</code>, iOS location services never turn off. This can severely drain the battery. Only use this configuration if you fully control tracking manually (for example, a workout app toggling tracking via BackgroundGeolocation.changePace).</p> <p>iOS stop-detection timing:</p> <p></p>"},{"location":"capacitor/GeoConfig/#android_1","title":"Android","text":"<p>When <code>true</code>, Android location services never turn off automatically. You must disable tracking manually by calling BackgroundGeolocation.changePace with <code>false</code>, or BackgroundGeolocation.stop.</p>"},{"location":"capacitor/GeoConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>distanceFilter?: number;\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"capacitor/GeoConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"capacitor/GeoConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"capacitor/GeoConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>elasticityMultiplier?: number;\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"capacitor/GeoConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>enableTimestampMeta?: boolean;\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS Location.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"capacitor/GeoConfig/#android_2","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"capacitor/GeoConfig/#ios_2","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"capacitor/GeoConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>fastestLocationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    fastestLocationUpdateInterval: 5000 // Receive passive updates at most every 5 s\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"capacitor/GeoConfig/#filter","title":"filter","text":"<p><code>filter?:</code><code>LocationFilter</code><code>;</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"capacitor/GeoConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilter.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilter.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilter.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilter.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilter.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilter.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilter.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilter.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilter.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilter.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilter.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilter.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>geofenceInitialTriggerEntry?: boolean;\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"capacitor/GeoConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>geofenceModeHighAccuracy?: boolean;\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BackgroundGeolocation.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeoConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeoConfig.locationUpdateInterval - GeoConfig.distanceFilter - GeoConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 50,\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>geofenceProximityRadius?: number;\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p>"},{"location":"capacitor/GeoConfig/#ios_3","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"capacitor/GeoConfig/#android_3","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"capacitor/GeoConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>locationAuthorizationAlert?: Record&lt;string, any&gt;;\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationAlert: {\n      titleWhenNotEnabled: \"Location services are not enabled\",\n      titleWhenOff: \"Location services are OFF\",\n      instructions:\n        \"Please enable 'Always' in Location Services to allow background tracking.\",\n      cancelButton: \"Cancel\",\n      settingsButton: \"Open Settings\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<pre><code>locationAuthorizationRequest?: \"Always\" | \"WhenInUse\" | \"Any\";\n</code></pre> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"capacitor/GeoConfig/#ios_4","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Start with When-In-Use\nBackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"WhenInUse\" }\n});\n\nasync function onClickStartTracking() {\n  await BackgroundGeolocation.start();\n\n  // Upgrade to Always at any time.\n  BackgroundGeolocation.setConfig({\n    geolocation: { locationAuthorizationRequest: \"Always\" }\n  });\n}\n</code></pre>"},{"location":"capacitor/GeoConfig/#android_4","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"Always\" },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow access to this device's location in the background?\",\n      message: \"To track your trips, please enable 'Allow all the time' location permission.\",\n      positiveAction: \"Change to Allow all the time\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"capacitor/GeoConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>locationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    distanceFilter: 0,           // Required \u2014 otherwise this field is ignored.\n    locationUpdateInterval: 5000 // Request a fix every ~5 seconds\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"capacitor/GeoConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>pausesLocationUpdatesAutomatically?: boolean;\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BackgroundGeolocation.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"capacitor/GeoConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>showsBackgroundLocationIndicator?: boolean;\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"capacitor/GeoConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>stationaryRadius?: number;\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"capacitor/GeoConfig/#ios_5","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"capacitor/GeoConfig/#android_5","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"capacitor/GeoConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>stopAfterElapsedMinutes?: number;\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BackgroundGeolocation.stop after this many minutes have elapsed since BackgroundGeolocation.start was called.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopAfterElapsedMinutes: 30\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.start();  // SDK will automatically stop in 30 minutes\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the <code>stopTimeout</code> elapses.</p> <p>When <code>true</code>, the SDK stops itself the next time BackgroundGeolocation.onMotionChange fires into the stationary state after the <code>stopTimeout</code> timer elapses.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the SDK transitions to the stationary state due to <code>stopTimeout</code> expiry. It does not fire when you manually call BackgroundGeolocation.changePace with <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopOnStationary: true,\n  },\n  app: {\n    isMoving: true,\n  }\n}, (state) =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"capacitor/GeoConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>stopTimeout?: number;\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BackgroundGeolocation.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"capacitor/GeoConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>useSignificantChangesOnly?: boolean;\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"capacitor/GeoConfig/#ios_6","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"capacitor/GeoConfig/#android_6","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"capacitor/Geofence/","title":"Geofence","text":"<pre><code>export interface Geofence {\n</code></pre> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"capacitor/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"capacitor/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeoConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"capacitor/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BackgroundGeolocation.addGeofence for a single geofence, or BackgroundGeolocation.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"capacitor/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BackgroundGeolocation.onGeofence. Subscribe to changes in the actively monitored set with BackgroundGeolocation.onGeofencesChange.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event.identifier, event.action);\n});\n\nBackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  on.forEach((geofence) =&gt; createGeofenceMarker(geofence));\n  off.forEach((identifier) =&gt; removeGeofenceMarker(identifier));\n});\n</code></pre> <p>Note</p> <p>When all geofences are removed, BackgroundGeolocation.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"capacitor/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"capacitor/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeoConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeoConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"capacitor/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre> <pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"capacitor/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BackgroundGeolocation.startGeofences instead of BackgroundGeolocation.start to monitor geofences without continuous location tracking. Use GeoConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/Geofence/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre>"},{"location":"capacitor/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"capacitor/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"capacitor/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre>"},{"location":"capacitor/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"capacitor/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"capacitor/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"capacitor/Geofence/#members","title":"Members","text":""},{"location":"capacitor/Geofence/#entrystate","title":"entryState","text":"<p><code>entry</code><code>State</code><code>?: number;</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"capacitor/Geofence/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, unknown&gt;;\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"capacitor/Geofence/#hits","title":"hits","text":"<pre><code>hits?: number;\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"capacitor/Geofence/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"capacitor/Geofence/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"capacitor/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>loiteringDelay?: number;\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"capacitor/Geofence/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"capacitor/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>notifyOnDwell?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"capacitor/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>notifyOnEntry?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeoConfig.geofenceInitialTriggerEntry</p>"},{"location":"capacitor/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>notifyOnExit?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"capacitor/Geofence/#radius","title":"radius","text":"<pre><code>radius: number;\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"capacitor/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>stateUpdatedAt?: number;\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"capacitor/Geofence/#vertices","title":"vertices","text":"<p><code>vertices?:</code><code>Vertices</code><code>;</code></p> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre> <p> </p>"},{"location":"capacitor/GeofenceAction/","title":"GeofenceAction","text":"<pre><code>export const GeofenceAction = {\n</code></pre> <p>Geofence transition actions emitted by the SDK</p> <p>Used with GeofenceEvent from BackgroundGeolocation.onGeofence.</p> Action Meaning ENTER Device entered the geofence EXIT Device exited the geofence DWELL Device dwelled inside"},{"location":"capacitor/GeofenceAction/#members","title":"Members","text":""},{"location":"capacitor/GeofenceAction/#dwell","title":"Dwell","text":"<pre><code>Dwell: 'DWELL'\n</code></pre> <p>Device dwelled inside the geofence for at least the configured <code>Config.geofenceProximityRadius</code>.</p>"},{"location":"capacitor/GeofenceAction/#enter","title":"Enter","text":"<pre><code>Enter: 'ENTER',\n</code></pre> <p>Device entered the geofence.</p>"},{"location":"capacitor/GeofenceAction/#exit","title":"Exit","text":"<pre><code>Exit: 'EXIT',\n</code></pre> <p>Device exited the geofence.</p>"},{"location":"capacitor/GeofenceEvent/","title":"GeofenceEvent","text":"<pre><code>export interface GeofenceEvent {\n</code></pre> <p>Geofence transition delivered to BackgroundGeolocation.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\n    \"[onGeofence]\",\n    event.identifier,\n    event.action,\n    event.location\n  );\n});\n</code></pre>"},{"location":"capacitor/GeofenceEvent/#members","title":"Members","text":""},{"location":"capacitor/GeofenceEvent/#action","title":"action","text":"<p><code>action:</code><code>GeofenceAction</code><code>;</code></p> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"capacitor/GeofenceEvent/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"capacitor/GeofenceEvent/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>Geofence</code><code>;</code></p> <p>The complete Geofence record that triggered this event.</p>"},{"location":"capacitor/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"capacitor/GeofenceEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"capacitor/GeofenceEvent/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string;\n</code></pre> <p>ISO-8601 UTC timestamp from the device system clock at the moment the OS delivered the geofence event.</p>"},{"location":"capacitor/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<pre><code>export interface GeofencesChangeEvent {\n</code></pre> <p>Geofence monitoring change delivered to BackgroundGeolocation.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  for (const geofence of event.on) {\n    console.log(\"[onGeofencesChange] Activated:\", geofence.identifier);\n  }\n  for (const identifier of event.off) {\n    console.log(\"[onGeofencesChange] Deactivated:\", identifier);\n  }\n});\n</code></pre>"},{"location":"capacitor/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"capacitor/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>off: string[];\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"capacitor/GeofencesChangeEvent/#on","title":"on","text":"<p><code>on:</code><code>Geofence</code><code>[];</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"capacitor/HeadlessEvent/","title":"HeadlessEvent Android only","text":"<pre><code>export interface HeadlessEvent {\n</code></pre> <p>Event delivered to a headless task registered via BackgroundGeolocation.registerHeadlessTask.</p> <p>Headless tasks allow the app to handle SDK events while running in the background without a UI \u2014 including after the app has been terminated by the user. Each task invocation receives a <code>HeadlessEvent</code> describing which SDK event occurred and carrying the associated payload.</p> <pre><code>const headlessTask = async (event: HeadlessEvent) =&gt; {\n  const { name, params } = event;\n  console.log(\"[HeadlessTask]\", name, params);\n\n  if (name === \"location\") {\n    const location = params as Location;\n    console.log(\"[HeadlessTask] location:\", location);\n  } else if (name === \"http\") {\n    const httpEvent = params as HttpEvent;\n    console.log(\"[HeadlessTask] http status:\", httpEvent.status);\n  }\n};\n\nBackgroundGeolocation.registerHeadlessTask(headlessTask);\n</code></pre>"},{"location":"capacitor/HeadlessEvent/#members","title":"Members","text":""},{"location":"capacitor/HeadlessEvent/#name","title":"name","text":"<p><code>name:</code><code>Event</code><code>;</code></p> <p>The SDK event that triggered this headless task invocation (e.g. <code>location</code>, <code>http</code>, <code>geofence</code>, <code>heartbeat</code>, <code>terminate</code>).</p>"},{"location":"capacitor/HeadlessEvent/#params","title":"params","text":"<pre><code>params: Record&lt;string, any&gt;;\n</code></pre> <p>Event payload corresponding to <code>name</code>. Cast this to the appropriate event type based on the value of <code>name</code> (e.g. Location for <code>location</code>, HttpEvent for <code>http</code>).</p>"},{"location":"capacitor/HeartbeatEvent/","title":"HeartbeatEvent","text":"<pre><code>export interface HeartbeatEvent {\n</code></pre> <p>Periodic heartbeat delivered to BackgroundGeolocation.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>BackgroundGeolocation.onHeartbeat(async (event) =&gt; {\n  console.log(\"[onHeartbeat]\", event.location);\n\n  // Request a fresh location fix if needed.\n  const location = await BackgroundGeolocation.getCurrentPosition({\n    samples: 1\n  });\n  console.log(\"[onHeartbeat] fresh position:\", location);\n});\n</code></pre>"},{"location":"capacitor/HeartbeatEvent/#members","title":"Members","text":""},{"location":"capacitor/HeartbeatEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BackgroundGeolocation.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"capacitor/HttpConfig/","title":"HttpConfig","text":"<pre><code>export interface HttpConfig {\n</code></pre> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"capacitor/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"capacitor/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    params: { user_id: 1234 }\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BackgroundGeolocation.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BackgroundGeolocation.getCount or fetch records with BackgroundGeolocation.getLocations.</p>"},{"location":"capacitor/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"capacitor/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"capacitor/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BackgroundGeolocation.onConnectivityChange fires - BackgroundGeolocation.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p> <pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"capacitor/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BackgroundGeolocation.start <code>\"stop\"</code> \u2014 BackgroundGeolocation.stop <code>\"startGeofences\"</code> \u2014 BackgroundGeolocation.startGeofences <code>\"changePace\"</code> <code>boolean</code> BackgroundGeolocation.changePace <code>\"setConfig\"</code> <code>{Config}</code> BackgroundGeolocation.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BackgroundGeolocation.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BackgroundGeolocation.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BackgroundGeolocation.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"capacitor/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p>"},{"location":"capacitor/HttpConfig/#the-sdk-log-provides-a-trace-of-the-full-http-lifecycle","title":"The SDK log provides a trace of the full HTTP lifecycle","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"capacitor/HttpConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"capacitor/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"capacitor/HttpConfig/#http-lifecycle-log","title":"HTTP lifecycle log","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre>"},{"location":"capacitor/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer ...\" }\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"capacitor/HttpConfig/#members","title":"Members","text":""},{"location":"capacitor/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>autoSync?: boolean;\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BackgroundGeolocation.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BackgroundGeolocation.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"capacitor/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>autoSyncThreshold?: number;\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BackgroundGeolocation.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"capacitor/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>batchSync?: boolean;\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"capacitor/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>disableAutoSyncOnCellular?: boolean;\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BackgroundGeolocation.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BackgroundGeolocation.sync</p>"},{"location":"capacitor/HttpConfig/#headers","title":"headers","text":"<pre><code>headers?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my.server.com\",\n    headers: {\n      Authorization: \"Bearer &lt;a secret key&gt;\",\n      \"X-FOO\": \"BAR\"\n    }\n  }\n});\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"capacitor/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>maxBatchSize?: number;\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"capacitor/HttpConfig/#method","title":"method","text":"<p><code>method?:</code><code>HttpMethod</code><code>;</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    method: \"PUT\"\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#params","title":"params","text":"<pre><code>params?: Record&lt;string, any&gt;;\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      user_id: 1234,\n      device_id: \"abc123\"\n    }\n  }\n});\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"capacitor/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>rootProperty?: string;\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    rootProperty: \"myData\",\n    url: \"https://my.server.com\",\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"capacitor/HttpConfig/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BackgroundGeolocation.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>BackgroundGeolocation.onHttp((response) =&gt; {\n  if (!response.success) {\n    console.log(\"[onHttp] FAILURE:\", response);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    timeout: 3000\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpConfig/#url","title":"url","text":"<pre><code>url?: string;\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  console.log(\"[onHttp]\", event);\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: { user_id: 1234 },\n    headers: { Authorization: \"Basic my-secret-key\" },\n    autoSync: true,\n    method: \"POST\"\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpEvent/","title":"HttpEvent","text":"<pre><code>export interface HttpEvent {\n</code></pre> <p>HTTP response payload delivered to BackgroundGeolocation.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onHttp] Upload succeeded:\", event.status);\n  } else {\n    console.warn(\"[onHttp] Upload failed:\", event.status, event.responseText);\n  }\n});\n</code></pre>"},{"location":"capacitor/HttpEvent/#members","title":"Members","text":""},{"location":"capacitor/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>responseText: string;\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"capacitor/HttpEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"capacitor/HttpEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"capacitor/HttpMethod/","title":"HttpMethod","text":"<pre><code>export const HttpMethod = {\n</code></pre> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"capacitor/HttpMethod/#members","title":"Members","text":""},{"location":"capacitor/HttpMethod/#patch","title":"Patch","text":"<p>HTTP PATCH method.</p>"},{"location":"capacitor/HttpMethod/#post","title":"Post","text":"<p>HTTP POST method.</p>"},{"location":"capacitor/HttpMethod/#put","title":"Put","text":"<p>HTTP PUT method.</p>"},{"location":"capacitor/KalmanProfile/","title":"KalmanProfile","text":"<pre><code>export const KalmanProfile = {\n</code></pre> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"capacitor/KalmanProfile/#members","title":"Members","text":""},{"location":"capacitor/KalmanProfile/#aggressive","title":"Aggressive","text":"<p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"capacitor/KalmanProfile/#conservative","title":"Conservative","text":"<p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"capacitor/KalmanProfile/#default","title":"Default","text":"<p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"capacitor/Location/","title":"Location","text":"<pre><code>export interface Location {\n</code></pre> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"capacitor/Location/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"capacitor/Location/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"capacitor/Location/#javascript-schema","title":"JavaScript schema","text":"<pre><code>{\n   \"timestamp\":     [Date],     // &lt;-- Javascript Date instance\n   \"event\":         [String],   // &lt;-- motionchange|geofence|heartbeat\n   \"is_moving\":     [Boolean],  // &lt;-- The motion-state when location was recorded.\n   \"uuid\":          [String],   // &lt;-- Universally unique identifier\n   \"age\":           [Integer],  // &lt;-- Age of the location in milliseconds\n   \"coords\": {\n       \"latitude\":  [Double],\n       \"longitude\": [Double],\n       \"accuracy\":  [Double],\n       \"speed\":     [Double],\n       \"heading\":   [Double],\n       \"altitude\":  [Double]\n       \"ellipsoidal_altitude\":  [Double]\n   },\n   \"activity\": {\n       \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle],\n       \"confidence\": [0-100%]\n   },\n   \"battery\": {\n       \"level\": [Double],\n       \"is_charging\": [Boolean]\n   },\n   \"odometer\": [Double/meters]\n}\n</code></pre>"},{"location":"capacitor/Location/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>{\n    \"location\": {\n        \"coords\": {\n            \"latitude\":   [Double],\n            \"longitude\":  [Double],\n            \"accuracy\":   [Double],\n            \"speed\":      [Double],\n            \"heading\":    [Double],\n            \"altitude\":   [Double],\n            \"ellipsoidal_altitude\": [Double]\n        },\n        \"extras\": {   // &lt;-- optional meta-data\n            \"foo\": \"bar\"\n        },\n        \"activity\": {\n            \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle|unknown],\n            \"confidence\": [0-100%]\n        },\n        \"geofence\": {  // &lt;-- Present only if a geofence was triggered at this location\n            \"identifier\": [String],\n            \"action\": [String ENTER|EXIT]\n        },\n        \"battery\": {\n            \"level\": [Double],\n            \"is_charging\": [Boolean]\n        },\n        \"timestamp\": [ISO-8601 UTC], // eg:  \"2015-05-05T04:31:54.123Z\"\n        \"age\":       [Integer],      // &lt;-- Age of the location in milliseconds\n        \"uuid\":      [String],       // &lt;-- Universally unique identifier\n        \"event\"      [String],       // &lt;-- motionchange|geofence|heartbeat\n        \"is_moving\": [Boolean],      // &lt;-- The motion-state when recorded.\n        \"odometer\": [Double/meters]\n    }\n }\n</code></pre>"},{"location":"capacitor/Location/#members","title":"Members","text":""},{"location":"capacitor/Location/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivity</code><code>;</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"capacitor/Location/#age","title":"age","text":"<pre><code>age: number;\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"capacitor/Location/#battery","title":"battery","text":"<p><code>battery:</code><code>Battery</code><code>;</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"capacitor/Location/#coords","title":"coords","text":"<p><code>coords:</code><code>Coords</code><code>;</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"capacitor/Location/#event","title":"event","text":"<pre><code>event?: string;\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"capacitor/Location/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"capacitor/Location/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>GeofenceEvent</code><code>;</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"capacitor/Location/#is_moving","title":"is_moving","text":"<pre><code>is_moving: boolean;\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"capacitor/Location/#mock","title":"mock","text":"<pre><code>mock?: boolean;\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"capacitor/Location/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"capacitor/Location/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilter evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilter.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via Location.odometer_error.</li> </ul>"},{"location":"capacitor/Location/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"capacitor/Location/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilter - LocationFilter.odometerAccuracyThreshold - BackgroundGeolocation.resetOdometer - BackgroundGeolocation.getOdometer</p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Distance traveled:\", location.odometer);\n});\n\n// Reset at the start of a trip\nawait BackgroundGeolocation.resetOdometer();\n</code></pre>"},{"location":"capacitor/Location/#odometer_error","title":"odometer_error","text":"<pre><code>odometer_error: number;\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"capacitor/Location/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Odometer:\", location.odometer);\n  if (location.odometer_error &gt; 50) {\n    console.warn(\"High odometer drift \u2014 signal quality degraded\");\n  }\n});\n</code></pre>"},{"location":"capacitor/Location/#provider","title":"provider","text":"<p><code>provider?:</code><code>ProviderChangeEvent</code><code>;</code></p> <p>Location-services provider state at the time this location was recorded.</p> <p>Present only when <code>event</code> is <code>\"providerchange\"</code>.</p>"},{"location":"capacitor/Location/#recorded_at","title":"recorded_at","text":"<pre><code>recorded_at: string | number;\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"capacitor/Location/#sample","title":"sample","text":"<pre><code>sample?: boolean;\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BackgroundGeolocation.getCurrentPosition to find the most accurate fix. These samples are delivered to BackgroundGeolocation.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"capacitor/Location/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string | number;\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"capacitor/Location/#uuid","title":"uuid","text":"<pre><code>uuid: string;\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"capacitor/LocationError/","title":"LocationError","text":"<pre><code>export const LocationError = {\n</code></pre> <p>Error codes returned when the native location API fails to fetch a location.</p> Code Error <code>0</code> Location unknown <code>1</code> Location permission denied <code>2</code> Network error <code>3</code> Attempt to initiate location-services in background with WhenInUse authorization <code>408</code> Location timeout <code>499</code> Location request cancelled"},{"location":"capacitor/LocationError/#members","title":"Members","text":""},{"location":"capacitor/LocationError/#backgroundwheninuse","title":"BackgroundWhenInUse","text":"<pre><code>BackgroundWhenInUse: 3,\n</code></pre> <p>Attempt to initiate location-services in background with WhenInUse authorization.</p>"},{"location":"capacitor/LocationError/#cancelled","title":"Cancelled","text":"<pre><code>Cancelled: 499,\n</code></pre> <p>Location request was cancelled.</p>"},{"location":"capacitor/LocationError/#locationunknown","title":"LocationUnknown","text":"<pre><code>LocationUnknown: 0,\n</code></pre> <p>Location could not be determined at this time.</p>"},{"location":"capacitor/LocationError/#networkerror","title":"NetworkError","text":"<pre><code>NetworkError: 2,\n</code></pre> <p>Network error occurred while attempting to fetch location.</p>"},{"location":"capacitor/LocationError/#permissiondenied","title":"PermissionDenied","text":"<pre><code>PermissionDenied: 1,\n</code></pre> <p>Location permission denied by the user.</p>"},{"location":"capacitor/LocationError/#timeout","title":"Timeout","text":"<pre><code>Timeout: 408,\n</code></pre> <p>Location request timed out.</p>"},{"location":"capacitor/LocationFilter/","title":"LocationFilter","text":"<pre><code>export interface LocationFilter {\n</code></pre> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"capacitor/LocationFilter/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"capacitor/LocationFilter/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#members","title":"Members","text":""},{"location":"capacitor/LocationFilter/#burstwindow","title":"burstWindow","text":"<pre><code>burstWindow?: number;\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"capacitor/LocationFilter/#filterdebug","title":"filterDebug","text":"<pre><code>filterDebug?: boolean;\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"capacitor/LocationFilter/#kalmandebug","title":"kalmanDebug","text":"<pre><code>kalmanDebug?: boolean;\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"capacitor/LocationFilter/#kalmanprofile","title":"kalmanProfile","text":"<p><code>kalmanProfile?:</code><code>KalmanProfile</code><code>;</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"capacitor/LocationFilter/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>maxBurstDistance?: number;\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"capacitor/LocationFilter/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>maxImpliedSpeed?: number;\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"capacitor/LocationFilter/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>odometerAccuracyThreshold?: number;\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"capacitor/LocationFilter/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>odometerUseKalmanFilter?: boolean;\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilter.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilter.useKalman and LocationFilter.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"capacitor/LocationFilter/#policy","title":"policy","text":"<p><code>policy?:</code><code>LocationFilterPolicy</code><code>;</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BackgroundGeolocation.onLocation. For more granular tuning, see LocationFilter.trackingAccuracyThreshold, LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</p> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilter/#rollingwindow","title":"rollingWindow","text":"<pre><code>rollingWindow?: number;\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"capacitor/LocationFilter/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>trackingAccuracyThreshold?: number;\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"capacitor/LocationFilter/#usekalman","title":"useKalman","text":"<pre><code>useKalman?: boolean;\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"capacitor/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<pre><code>export const LocationFilterPolicy = {\n</code></pre> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilter.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BackgroundGeolocation.onLocation.</li> <li>For more granular tuning, see LocationFilter.trackingAccuracyThreshold,   LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</li> </ul> <p>Examples</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p>"},{"location":"capacitor/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"capacitor/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilter.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"capacitor/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"capacitor/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"capacitor/LocationRequest/","title":"LocationRequest","text":"<pre><code>export const LocationRequest = {\n</code></pre> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeoConfig.locationAuthorizationRequest</p>"},{"location":"capacitor/LocationRequest/#members","title":"Members","text":""},{"location":"capacitor/LocationRequest/#always","title":"Always","text":"<p>Request full background + foreground authorization.</p>"},{"location":"capacitor/LocationRequest/#any","title":"Any","text":"<p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"capacitor/LocationRequest/#wheninuse","title":"WhenInUse","text":"<p>Request foreground-only authorization.</p>"},{"location":"capacitor/LogLevel/","title":"LogLevel","text":"<pre><code>export const LogLevel = {\n</code></pre> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"capacitor/LogLevel/#members","title":"Members","text":""},{"location":"capacitor/LogLevel/#debug","title":"Debug","text":"<p>Developer-level debug output.</p>"},{"location":"capacitor/LogLevel/#error","title":"Error","text":"<p>Log only critical failures.</p>"},{"location":"capacitor/LogLevel/#info","title":"Info","text":"<p>Operational information.</p>"},{"location":"capacitor/LogLevel/#off","title":"Off","text":"<p>Disable all logging.</p>"},{"location":"capacitor/LogLevel/#verbose","title":"Verbose","text":"<p>Maximum detail.</p>"},{"location":"capacitor/LogLevel/#warning","title":"Warning","text":"<p>Log warnings + errors.</p>"},{"location":"capacitor/Logger/","title":"Logger","text":"<pre><code>export interface Logger {\n</code></pre> <p>SDK logging API \u2014 access via BackgroundGeolocation.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"capacitor/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"capacitor/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\n</code></pre>"},{"location":"capacitor/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional SQLQuery to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"capacitor/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>const Logger = BackgroundGeolocation.logger;\nBackgroundGeolocation.onLocation((location) =&gt; {\n  Logger.debug(\"Location received: \" + location.uuid);\n});\n</code></pre>"},{"location":"capacitor/Logger/#examples","title":"Examples","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"capacitor/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre>"},{"location":"capacitor/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre>"},{"location":"capacitor/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"capacitor/Logger/#methods","title":"Methods","text":""},{"location":"capacitor/Logger/#debug","title":"debug","text":"<pre><code>debug(message: string): void;\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.debug(\"This is a debug message\");\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"capacitor/Logger/#destroylog","title":"destroyLog","text":"<pre><code>destroyLog(): Promise&lt;void&gt;;\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.destroyLog();\n</code></pre>"},{"location":"capacitor/Logger/#emaillog","title":"emailLog","text":"<p><code>emailLog(email: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional SQLQuery to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"foo@bar.com\");\n\n// Constrain by date range\nawait Logger.emailLog(\"foo@bar.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC,\n  limit: 1000\n});\n</code></pre>"},{"location":"capacitor/Logger/#error","title":"error","text":"<pre><code>error(message: string): void;\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.error(\"Something BAD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"capacitor/Logger/#getlog","title":"getLog","text":"<p><code>getLog(query?:</code><code>SQLQuery</code><code>): Promise&lt;string&gt;;</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional SQLQuery to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Fetch all logs\nconst log = await BackgroundGeolocation.logger.getLog();\nconsole.log(\"[log]\", log);\n\n// Constrain by date range\nconst Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-09-19 11:12\"),\n  end:   Date.parse(\"2019-09-19 11:13\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre>"},{"location":"capacitor/Logger/#info","title":"info","text":"<pre><code>info(message: string): void;\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.info(\"Something informative\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"capacitor/Logger/#notice","title":"notice","text":"<pre><code>notice(message: string): void;\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.notice(\"A Notice\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"capacitor/Logger/#uploadlog","title":"uploadLog","text":"<p><code>uploadLog(url: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional SQLQuery to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"capacitor/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n\n// Constrain by date range\nawait BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\",\n  { start: Date.parse(\"2019-10-20 09:00\"), end: Date.parse(\"2019-10-20 11:59\") }\n);\n</code></pre>"},{"location":"capacitor/Logger/#warn","title":"warn","text":"<pre><code>warn(message: string): void;\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.warn(\"Something WEIRD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"capacitor/Logger/#properties","title":"Properties","text":""},{"location":"capacitor/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>ORDER_ASC: 1;\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"capacitor/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>ORDER_DESC: -1;\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"capacitor/LoggerConfig/","title":"LoggerConfig","text":"<pre><code>export interface LoggerConfig {\n</code></pre> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"capacitor/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"capacitor/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"capacitor/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"capacitor/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"capacitor/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"capacitor/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"capacitor/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"capacitor/LoggerConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>Config({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#members","title":"Members","text":""},{"location":"capacitor/LoggerConfig/#debug","title":"debug","text":"<pre><code>debug?: boolean;\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"capacitor/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"capacitor/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"capacitor/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>logLevel?:</code><code>LogLevel</code><code>;</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  },\n});\n</code></pre>"},{"location":"capacitor/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>logMaxDays?: number;\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logMaxDays: 3\n  }\n});\n</code></pre>"},{"location":"capacitor/MotionActivity/","title":"MotionActivity","text":"<pre><code>export interface MotionActivity {\n</code></pre> <p>The motion activity reported by the device at the time a Location was recorded.</p>"},{"location":"capacitor/MotionActivity/#members","title":"Members","text":""},{"location":"capacitor/MotionActivity/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"capacitor/MotionActivity/#type","title":"type","text":"<p><code>type:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"capacitor/MotionActivityEvent/","title":"MotionActivityEvent","text":"<pre><code>export interface MotionActivityEvent {\n</code></pre> <p>Activity recognition result delivered to BackgroundGeolocation.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange]\", event.activity, event.confidence);\n});\n</code></pre>"},{"location":"capacitor/MotionActivityEvent/#members","title":"Members","text":""},{"location":"capacitor/MotionActivityEvent/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"capacitor/MotionActivityEvent/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"capacitor/MotionActivityType/","title":"MotionActivityType","text":"<pre><code>export const MotionActivityType = {\n</code></pre> <p>Reported device motion activity names (legacy RN + native).</p>"},{"location":"capacitor/MotionActivityType/#members","title":"Members","text":""},{"location":"capacitor/MotionActivityType/#invehicle","title":"InVehicle","text":"<pre><code>InVehicle: 'in_vehicle',\n</code></pre> <p>Device is in vehicle.</p>"},{"location":"capacitor/MotionActivityType/#onbicycle","title":"OnBicycle","text":"<pre><code>OnBicycle: 'on_bicycle',\n</code></pre> <p>Device is on bicycle.</p>"},{"location":"capacitor/MotionActivityType/#onfoot","title":"OnFoot","text":"<pre><code>OnFoot: 'on_foot',\n</code></pre> <p>Device is on foot (walking or running).</p>"},{"location":"capacitor/MotionActivityType/#running","title":"Running","text":"<pre><code>Running: 'running',\n</code></pre> <p>Device is running.</p>"},{"location":"capacitor/MotionActivityType/#still","title":"Still","text":"<pre><code>Still: 'still',\n</code></pre> <p>Device is stationary.</p>"},{"location":"capacitor/MotionActivityType/#unknown","title":"Unknown","text":"<pre><code>Unknown: 'unknown'\n</code></pre> <p>Device activity is unknown.</p>"},{"location":"capacitor/MotionActivityType/#walking","title":"Walking","text":"<pre><code>Walking: 'walking',\n</code></pre> <p>Device is walking.</p>"},{"location":"capacitor/MotionChangeEvent/","title":"MotionChangeEvent","text":"<pre><code>export interface MotionChangeEvent {\n</code></pre> <p>Motion-state change delivered to BackgroundGeolocation.onMotionChange.</p> <p>The SDK fires this event each time it transitions between the moving and stationary states. A location is always recorded at the moment of transition.</p> <pre><code>BackgroundGeolocation.onMotionChange((event) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device is moving:\", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device is stationary:\", event.location);\n  }\n});\n</code></pre>"},{"location":"capacitor/MotionChangeEvent/#members","title":"Members","text":""},{"location":"capacitor/MotionChangeEvent/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK has transitioned to the moving state; <code>false</code> when it has transitioned to the stationary state.</p>"},{"location":"capacitor/MotionChangeEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the motion-state transition was detected.</p>"},{"location":"capacitor/NotificationConfig/","title":"NotificationConfig Android only","text":"<pre><code>export interface NotificationConfig {\n</code></pre> <p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"capacitor/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"capacitor/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The Title\",\n      text: \"The Text\"\n    }\n  }\n});\n\n// Update notification fields at runtime.\n// Only changed keys must be provided; existing values persist.\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      title: \"The New Title\",\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"capacitor/NotificationConfig/#members","title":"Members","text":""},{"location":"capacitor/NotificationConfig/#actions","title":"actions","text":"<pre><code>actions?: string[];\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"capacitor/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>channelId?: string;\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"capacitor/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>channelName?: string;\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      channelName: \"Location Tracker\"\n    }\n  }\n});\n\n// Update at runtime\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      channelName: \"My new channel name\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#color","title":"color","text":"<pre><code>color?: string;\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"capacitor/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>largeIcon?: string;\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"drawable/my_notification_large_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"mipmap/my_notification_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#layout","title":"layout","text":"<pre><code>layout?: string;\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"capacitor/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      layout: \"my_notification_layout\",\n      title: \"The Notification Title\",\n      text: \"The Notification Text\",\n      smallIcon: \"mipmap/my_small_icon\",\n      largeIcon: \"mipmap/my_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#priority","title":"priority","text":"<p><code>priority?:</code><code>NotificationPriority</code><code>;</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: NotificationPriority.High\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>smallIcon?: string;\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"drawable/my_notification_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"mipmap/my_notification_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>sticky?: boolean;\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"capacitor/NotificationConfig/#strings","title":"strings","text":"<pre><code>strings?: Record&lt;string, string&gt;;\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/NotificationConfig/#text","title":"text","text":"<pre><code>text?: string;\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"capacitor/NotificationConfig/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"capacitor/NotificationPriority/","title":"NotificationPriority","text":"<pre><code>export const NotificationPriority = {\n</code></pre> <p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"capacitor/NotificationPriority/#members","title":"Members","text":""},{"location":"capacitor/NotificationPriority/#default","title":"Default","text":"<p>Default notification priority (normal weighting).</p>"},{"location":"capacitor/NotificationPriority/#high","title":"High","text":"<p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"capacitor/NotificationPriority/#low","title":"Low","text":"<p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"capacitor/NotificationPriority/#max","title":"Max","text":"<p>Same as NotificationPriority.High.</p>"},{"location":"capacitor/NotificationPriority/#min","title":"Min","text":"<p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"capacitor/PermissionRationale/","title":"PermissionRationale Android only","text":"<pre><code>export interface PermissionRationale {\n</code></pre> <p>Configures the background location permission rationale dialog on Android 11 and later.</p> <p>Android 11 removed the Allow all the time option from the system location permission dialog. Apps may instead show a custom rationale dialog that explains why background access is needed and routes the user to the system Location Permissions screen to grant it explicitly.</p>"},{"location":"capacitor/PermissionRationale/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Template tags</li> </ul>"},{"location":"capacitor/PermissionRationale/#overview","title":"Overview","text":"<p>The SDK presents this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code> and AppConfig.backgroundPermissionRationale is configured.</p> <p>Selecting the dialog's positive action sends the user directly to the system Location Permissions screen, where they must enable Allow all the time themselves.</p> <ul> <li>The dialog is shown once only. After the user presses the positive action   it will not appear again. Pressing Cancel does not count as acceptance.</li> <li>If the user later resets your app's Location permission to Ask every time,   the dialog becomes eligible to be shown again.</li> </ul> <p> </p> <pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location access.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"capacitor/PermissionRationale/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside any rationale field using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> \"Allow all the time\" (API 30+) Localized label for the background location permission option. <code>{applicationName}</code> App name Localized application name from <code>AndroidManifest</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Android 11 Location Updates</p>"},{"location":"capacitor/PermissionRationale/#members","title":"Members","text":""},{"location":"capacitor/PermissionRationale/#message","title":"message","text":"<pre><code>message?: string;\n</code></pre> <p>Body text of the dialog. Explain why your app needs background location access, similar in purpose to the iOS <code>NSLocationAlwaysAndWhenInUseUsageDescription</code>.</p>"},{"location":"capacitor/PermissionRationale/#negativeaction","title":"negativeAction","text":"<pre><code>negativeAction?: string;\n</code></pre> <p>Text to display on the negative action button (e.g. \"Cancel\").</p>"},{"location":"capacitor/PermissionRationale/#positiveaction","title":"positiveAction","text":"<pre><code>positiveAction?: string;\n</code></pre> <p>Text to display on the positive action button.</p>"},{"location":"capacitor/PermissionRationale/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title for the Android background location permission dialog.</p>"},{"location":"capacitor/PersistMode/","title":"PersistMode","text":"<pre><code>export const PersistMode = {\n</code></pre> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"capacitor/PersistMode/#members","title":"Members","text":""},{"location":"capacitor/PersistMode/#all","title":"All","text":"<p>Persist both location and geofence data.</p>"},{"location":"capacitor/PersistMode/#geofence","title":"Geofence","text":"<p>Persist geofence data only.</p>"},{"location":"capacitor/PersistMode/#location","title":"Location","text":"<p>Persist location data only.</p>"},{"location":"capacitor/PersistMode/#none","title":"None","text":"<p>Do not persist any data.</p>"},{"location":"capacitor/PersistenceConfig/","title":"PersistenceConfig","text":"<pre><code>export interface PersistenceConfig {\n</code></pre> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"capacitor/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"capacitor/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded Location and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 3,\n    maxRecordsToPersist: 1000,\n    extras: {\n      'user_id': 123,\n      'appVersion': '1.2.3'\n    },\n    persistMode: PersistMode.All\n  }\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BackgroundGeolocation.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BackgroundGeolocation.getCount or BackgroundGeolocation.getLocations.</p>"},{"location":"capacitor/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BackgroundGeolocation.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"capacitor/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"capacitor/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  persist: true,\n  extras: { get_current_position: true },\n  samples: 3,\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>{\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    locationTemplate: '{...}',\n    geofenceTemplate: '{...}',\n    persistMode: PersistMode.all,\n    extras: {'user_id': 123},\n    disableProviderChangeRecord: true,\n  },\n};\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#members","title":"Members","text":""},{"location":"capacitor/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>disableProviderChangeRecord?: boolean;\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BackgroundGeolocation.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    disableProviderChangeRecord: true\n }\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      device_id: \"abc123\"  // appended to root JSON of each POST request\n    }\n  },\n  persistence: {\n    extras: {\n      route_id: 1234       // merged onto each location record\n    }\n  }\n});\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"capacitor/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>geofenceTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '{ \"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\" }'\n  }\n});\n\n// Or a compact Array form:\nBackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"capacitor/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>locationTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping Location JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}'\n  }\n});\n\n// Or use a compact Array template:\nBackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n persistence: {\n  locationTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: 'https://my.server.com/locations',\n    rootProperty: 'data',\n  },\n  persistence: {\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}',\n    extras: { foo: \"bar\" }\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"capacitor/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"capacitor/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<pre><code>locationsOrderDirection?: \"ASC\" | \"DESC\";\n</code></pre> <p>Sort order used when selecting records for upload or returning them from BackgroundGeolocation.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"capacitor/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>maxDaysToPersist?: number;\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"capacitor/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>maxRecordsToPersist?: number;\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"capacitor/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>persistMode?:</code><code>PersistMode</code><code>;</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BackgroundGeolocation.onLocation and BackgroundGeolocation.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BackgroundGeolocation.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"capacitor/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>timestampFormat?: 'iso' | 'epoch';\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    timestampFormat: 'epoch',\n  },\n});\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"capacitor/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<pre><code>export interface ProviderChangeEvent {\n</code></pre> <p>Location-services provider state delivered to BackgroundGeolocation.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]\",\n    \"enabled:\", event.enabled,\n    \"status:\", event.status,\n    \"gps:\", event.gps,\n    \"network:\", event.network,\n    \"accuracyAuthorization:\", event.accuracyAuthorization\n  );\n\n  if (!event.enabled) {\n    // Prompt the user to re-enable location services.\n  }\n});\n</code></pre>"},{"location":"capacitor/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"capacitor/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<pre><code>accuracyAuthorization: number;\n</code></pre> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"capacitor/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"capacitor/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>gps: boolean;\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"capacitor/ProviderChangeEvent/#network","title":"network","text":"<pre><code>network: boolean;\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"capacitor/ProviderChangeEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>Platform authorization status code. Use the AuthorizationStatus constants to interpret this value.</p>"},{"location":"capacitor/SQLQuery/","title":"SQLQuery","text":"<pre><code>export interface SQLQuery {\n</code></pre> <p>Constrains a log query by date range, sort order, and record count.</p> <p>Pass to Logger.getLog, Logger.emailLog, or Logger.uploadLog to filter which log entries are included.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\n\n// Date range\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\")\n});\n\n// First 100 records ascending\nawait Logger.emailLog(\"foo@bar.com\", {\n  order: SQLQueryOrder.Asc,\n  limit: 100\n});\n\n// Upload a specific time window\nawait Logger.uploadLog(\"https://my.server.com/users/123/logs\", {\n  start: Date.parse(\"2019-10-20 09:00\"),\n  end:   Date.parse(\"2019-10-20 11:59\")\n});\n</code></pre>"},{"location":"capacitor/SQLQuery/#members","title":"Members","text":""},{"location":"capacitor/SQLQuery/#end","title":"end","text":"<pre><code>end?: number;\n</code></pre> <p>End of the query window (unix timestamp in milliseconds).</p>"},{"location":"capacitor/SQLQuery/#limit","title":"limit","text":"<pre><code>limit?: number;\n</code></pre> <p>Maximum number of records to return.</p>"},{"location":"capacitor/SQLQuery/#offset","title":"offset","text":"<pre><code>offset?: number;\n</code></pre> <p>Number of matching records to skip before returning results (for paging).</p>"},{"location":"capacitor/SQLQuery/#order","title":"order","text":"<p><code>order?:</code><code>SQLQueryOrder</code><code>;</code></p> <p>Sort order for results: <code>SQLQueryOrder.Asc</code> (oldest first) or <code>SQLQueryOrder.Desc</code> (newest first).</p>"},{"location":"capacitor/SQLQuery/#start","title":"start","text":"<pre><code>start?: number;\n</code></pre> <p>Start of the query window (unix timestamp in milliseconds).</p>"},{"location":"capacitor/SQLQueryOrder/","title":"SQLQueryOrder","text":"<pre><code>export const SQLQueryOrder = {\n</code></pre> <p>Sort order for SQLQuery.order.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"capacitor/SQLQueryOrder/#members","title":"Members","text":""},{"location":"capacitor/SQLQueryOrder/#asc","title":"Asc","text":"<p>Ascending order (oldest to newest).</p>"},{"location":"capacitor/SQLQueryOrder/#desc","title":"Desc","text":"<p>Descending order (newest to oldest).</p>"},{"location":"capacitor/Sensors/","title":"Sensors","text":"<pre><code>export interface Sensors {\n</code></pre> <p>Availability of motion sensors on the device, as returned by BackgroundGeolocation.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(\"[Sensors]\", sensors);\n</code></pre>"},{"location":"capacitor/Sensors/#members","title":"Members","text":""},{"location":"capacitor/Sensors/#accelerometer","title":"accelerometer","text":"<pre><code>accelerometer: boolean;\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"capacitor/Sensors/#gyroscope","title":"gyroscope","text":"<pre><code>gyroscope: boolean;\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"capacitor/Sensors/#magnetometer","title":"magnetometer","text":"<pre><code>magnetometer: boolean;\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"capacitor/Sensors/#motion_hardware","title":"motion_hardware","text":"<pre><code>motion_hardware?: boolean;\n</code></pre> <p>iOS only <code>true</code> when the device has a dedicated M-series motion co-processor (iPhone 5S and later). The M-series co-processor offloads motion activity recognition from the main CPU, enabling low-power step counting and activity detection.</p>"},{"location":"capacitor/Sensors/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"ios\"</code> or <code>\"android\"</code>.</p>"},{"location":"capacitor/Sensors/#significant_motion","title":"significant_motion","text":"<pre><code>significant_motion?: boolean;\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"capacitor/State/","title":"State","text":"<p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] enabled:\", state.enabled, \"trackingMode:\", state.trackingMode);\n</code></pre>"},{"location":"capacitor/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"capacitor/State/#members","title":"Members","text":""},{"location":"capacitor/State/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. See ActivityConfig.</p>"},{"location":"capacitor/State/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. See AppConfig.</p>"},{"location":"capacitor/State/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. See AuthorizationConfig.</p>"},{"location":"capacitor/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>didDeviceReboot: boolean;\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"capacitor/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>didLaunchInBackground: boolean;\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"capacitor/State/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BackgroundGeolocation.start or BackgroundGeolocation.startGeofences has been called and not yet stopped.</p>"},{"location":"capacitor/State/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. See GeoConfig.</p>"},{"location":"capacitor/State/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. See HttpConfig.</p>"},{"location":"capacitor/State/#isfirstboot","title":"isFirstBoot","text":"<p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"capacitor/State/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// Toggle the SDK's motion state from stationary to moving.\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log('[onMotionChange] isMoving?', event.isMoving);\n});\n\nawait BackgroundGeolocation.changePace(true);\n// State.isMoving is now true.\n</code></pre>"},{"location":"capacitor/State/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. See LoggerConfig.</p>"},{"location":"capacitor/State/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BackgroundGeolocation.setOdometer - BackgroundGeolocation.getOdometer</p>"},{"location":"capacitor/State/#odometererror","title":"odometerError","text":"<pre><code>odometerError: number;\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilter.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"capacitor/State/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. See PersistenceConfig.</p>"},{"location":"capacitor/State/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"capacitor/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>schedulerEnabled: boolean;\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BackgroundGeolocation.startSchedule has been called. BackgroundGeolocation.stopSchedule sets this to <code>false</code>.</p>"},{"location":"capacitor/State/#trackingmode","title":"trackingMode","text":"<p><code>trackingMode:</code><code>TrackingMode</code><code>;</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>await BackgroundGeolocation.start();\nlet state = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 1'\n\nawait BackgroundGeolocation.startGeofences();\nstate = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 0'\n</code></pre>"},{"location":"capacitor/State/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"capacitor/Subscription/","title":"Subscription","text":"<pre><code>export interface Subscription {\n</code></pre> <p>Handle returned by every <code>BackgroundGeolocation.on*</code> event-listener method.</p> <p><code>Subscription</code> exposes a single <code>remove</code> method used to stop listening to an event and free associated resources. Always call <code>remove()</code> when a listener is no longer needed to prevent memory leaks.</p>"},{"location":"capacitor/Subscription/#removing-an-event-listener","title":"Removing an event-listener","text":"<pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\n// Later, when the listener is no longer needed:\nsubscription.remove();\n</code></pre>"},{"location":"capacitor/Subscription/#managing-multiple-subscriptions","title":"Managing multiple subscriptions","text":"<p>Collect subscriptions in an array and remove them all at once \u2014 for example when a view is destroyed.</p> <pre><code>import BackgroundGeolocation, {\n  Location,\n  Subscription\n} from \"react-native-background-geolocation\";\n\nconst subscriptions: Subscription[] = [];\n\nfunction addListeners() {\n  subscriptions.push(\n    BackgroundGeolocation.onLocation((location: Location) =&gt; {\n      console.log(\"[onLocation]\", location);\n    }),\n    BackgroundGeolocation.onMotionChange((event) =&gt; {\n      console.log(\"[onMotionChange]\", event);\n    }),\n    BackgroundGeolocation.onEnabledChange((enabled: boolean) =&gt; {\n      console.log(\"[onEnabledChange]\", enabled);\n    })\n  );\n}\n\nfunction removeListeners() {\n  subscriptions.forEach((sub) =&gt; sub.remove());\n  subscriptions.length = 0;\n}\n</code></pre>"},{"location":"capacitor/Subscription/#members","title":"Members","text":""},{"location":"capacitor/Subscription/#remove","title":"remove","text":"<pre><code>remove(): void;\n</code></pre> <p>Removes the event-listener and frees its resources. Call this when the listener is no longer needed to prevent memory leaks.</p>"},{"location":"capacitor/TrackingMode/","title":"TrackingMode","text":"<pre><code>export const TrackingMode = {\n</code></pre> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"capacitor/TrackingMode/#members","title":"Members","text":""},{"location":"capacitor/TrackingMode/#geofences","title":"Geofences","text":"<p>Monitor geofences only.</p>"},{"location":"capacitor/TrackingMode/#location","title":"Location","text":"<p>Monitor location + geofences.</p>"},{"location":"capacitor/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<pre><code>export interface TransistorAuthorizationService {\n</code></pre> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"capacitor/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>const url = \"http://tracker.transistorsoft.com\";\nconst orgname = \"my-company-name\";\nconst username = \"my-username\";\n\n// Fetch a token from the server (the SDK caches it automatically).\nconst token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  orgname, username, url\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"capacitor/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"capacitor/TransistorAuthorizationService/#applyif","title":"applyIf","text":"<p><code>applyIf(config: T):</code><code>Config</code><code>;</code></p> <p>Mutate a Config to apply a <code>transistorAuthorizationToken</code> if present.</p> <p>The implementation reads <code>config.transistorAuthorizationToken</code>, removes that property, sets <code>config.http.url</code> to <code>\"&lt;token.url&gt;/api/locations\"</code>, and injects <code>config.authorization</code> with a JWT strategy derived from the token. If no <code>transistorAuthorizationToken</code> is found, the config is returned unchanged.</p> <pre><code>const token = await service.findOrCreate('my-org', 'user@example.com');\n\nconst config = service.applyIf({\n  ...myConfig,\n  transistorAuthorizationToken: token\n});\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"capacitor/TransistorAuthorizationService/#destroy","title":"destroy","text":"<pre><code>destroy(url?: string): Promise&lt;void&gt;;\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"capacitor/TransistorAuthorizationService/#findorcreate","title":"findOrCreate","text":"<p><code>findOrCreate(orgName: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\",\n  \"http://tracker.transistorsoft.com\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"capacitor/TransistorAuthorizationToken/","title":"TransistorAuthorizationToken","text":"<pre><code>export interface TransistorAuthorizationToken {\n</code></pre> <p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"capacitor/TransistorAuthorizationToken/#members","title":"Members","text":""},{"location":"capacitor/TransistorAuthorizationToken/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"capacitor/TransistorAuthorizationToken/#expires","title":"expires","text":"<pre><code>expires: number;\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"capacitor/TransistorAuthorizationToken/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken: string;\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"capacitor/TransistorAuthorizationToken/#url","title":"url","text":"<pre><code>url: string;\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"capacitor/TriggerActivity/","title":"TriggerActivity","text":"<pre><code>export const TriggerActivity = {\n</code></pre> <p>Allowed motion activity names that can trigger motion detection. Mirrors Flutter's TriggerActivity set.</p>"},{"location":"capacitor/Vertices/","title":"Vertices","text":"<pre><code>export type Vertices = [number, number][];\n</code></pre> <p>An array of <code>[latitude, longitude]</code> pairs defining a polygon geofence.</p> <p>See Geofence.vertices.</p>"},{"location":"capacitor/WatchPositionRequest/","title":"WatchPositionRequest","text":"<pre><code>export interface WatchPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.watchPosition.</p> <p>Configures the interval, accuracy, persistence, and metadata for a continuous location stream. All fields are optional.</p> <p>Warning</p> <p><code>watchPosition</code> is intended for foreground use. On iOS it prevents the app from being suspended, which drains the battery. Remove the subscription when your app moves to the background.</p> <pre><code>const subscription = BackgroundGeolocation.watchPosition(\n  { interval: 1000, desiredAccuracy: DesiredAccuracy.High },\n  (location) =&gt; {\n    console.log(\"[watchPosition]\", location);\n  },\n  (errorCode) =&gt; {\n    console.warn(\"[watchPosition] error:\", errorCode);\n  }\n);\n\n// Later, stop watching.\nsubscription.remove();\n</code></pre>"},{"location":"capacitor/WatchPositionRequest/#members","title":"Members","text":""},{"location":"capacitor/WatchPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Target accuracy for location updates from the native API. Defaults to DesiredAccuracy.High.</p>"},{"location":"capacitor/WatchPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to each location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"capacitor/WatchPositionRequest/#interval","title":"interval","text":"<pre><code>interval?: number;\n</code></pre> <p>Interval in milliseconds between location updates.</p>"},{"location":"capacitor/WatchPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist each location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"capacitor/WatchPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in milliseconds to wait for each location fix before firing an error. Default <code>60000</code>.</p>"},{"location":"capacitor/examples/","title":"Examples","text":""},{"location":"capacitor/examples/#basic-tracking","title":"Basic tracking","text":"AngularReactVue <pre><code>import { Injectable, OnDestroy } from '@angular/core';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\n@Injectable({ providedIn: 'root' })\nexport class BackgroundGeolocationService implements OnDestroy {\n  private subscriptions: Subscription[] = [];\n\n  init() {\n    // Register event listeners *before* calling ready()\n    this.subscriptions.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n  }\n\n  // e.g. wired to a toggle button in your template\n  async onToggleTracking() {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      await BackgroundGeolocation.stop();\n    } else {\n      await BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  }\n\n  ngOnDestroy() {\n    this.subscriptions.forEach(s =&gt; s.remove());\n  }\n}\n</code></pre> <pre><code>import React, { useEffect, useRef } from 'react';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;IonButton onClick={onToggleTracking}&gt;Toggle Tracking&lt;/IonButton&gt;\n  );\n}\n</code></pre> <pre><code>&lt;script setup lang=\"ts\"&gt;\nimport { onMounted, onUnmounted } from 'vue';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nconst subscriptions: Subscription[] = [];\n\nonMounted(() =&gt; {\n  // Register event listeners *before* calling ready()\n  subscriptions.push(\n    BackgroundGeolocation.onLocation(location =&gt; {\n      console.log('[onLocation]', location);\n    })\n  );\n\n  // ready() configures the SDK and restores persisted state.\n  // It does NOT start tracking \u2014 call start()/stop() separately.\n  BackgroundGeolocation.ready({\n      geolocation: {\n        desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n        distanceFilter: 10,\n      },\n      app: {\n        stopOnTerminate: false,\n        startOnBoot: true,\n      },\n      logger: {\n        debug: true,\n        logLevel: BackgroundGeolocation.LogLevel.Verbose,\n      },\n  });\n});\n\n// e.g. a toggle-button handler in your template\nasync function onToggleTracking() {\n  const state = await BackgroundGeolocation.getState();\n  if (state.enabled) {\n    await BackgroundGeolocation.stop();\n  } else {\n    await BackgroundGeolocation.start();\n  }\n  console.log('[state]', state.enabled, state.trackingMode);\n}\n\nonUnmounted(() =&gt; {\n  subscriptions.forEach(s =&gt; s.remove());\n});\n&lt;/script&gt;\n</code></pre>"},{"location":"capacitor/setup/","title":"Setup","text":"<p>Requires Capacitor 5 or higher.</p>"},{"location":"capacitor/setup/#installation","title":"Installation","text":"<pre><code>npm install @transistorsoft/capacitor-background-geolocation\nnpx cap sync\n</code></pre>"},{"location":"capacitor/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"capacitor/setup/#ios-infoplist","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"capacitor/setup/#android-androidmanifestxml","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"capacitor/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"capacitor/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"capacitor/setup/#infoplist","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Background modes --&gt;\n&lt;key&gt;UIBackgroundModes&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;location&lt;/string&gt;\n    &lt;string&gt;fetch&lt;/string&gt;\n    &lt;string&gt;processing&lt;/string&gt;\n    &lt;string&gt;audio&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Background task identifier --&gt;\n&lt;key&gt;BGTaskSchedulerPermittedIdentifiers&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;com.transistorsoft.fetch&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"capacitor/setup/#android-setup","title":"Android Setup","text":""},{"location":"capacitor/setup/#androidvariablesgradle","title":"<code>android/variables.gradle</code>","text":"<pre><code>ext {\n    minSdkVersion               = 24\n    compileSdkVersion           = 35\n    targetSdkVersion            = 35\n    playServicesLocationVersion = '21.3.0'\n    tslocationmanagerVersion    = '4.0.+'\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Versions \u2265 21 use <code>tslocationmanager</code>; versions &lt; 21 use <code>tslocationmanager-gms20</code>. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"capacitor/setup/#androidappbuildgradle","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>android {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"capacitor/setup/#androidmanifestxml","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"capacitor/setup/#example","title":"Example","text":"AngularReactVue <pre><code>import { Injectable, OnDestroy } from '@angular/core';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\n@Injectable({ providedIn: 'root' })\nexport class BackgroundGeolocationService implements OnDestroy {\n  private subscriptions: Subscription[] = [];\n\n  init() {\n    // Register event listeners *before* calling ready()\n    this.subscriptions.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n  }\n\n  // e.g. wired to a toggle button in your template\n  async onToggleTracking() {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      await BackgroundGeolocation.stop();\n    } else {\n      await BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  }\n\n  ngOnDestroy() {\n    this.subscriptions.forEach(s =&gt; s.remove());\n  }\n}\n</code></pre> <pre><code>import React, { useEffect, useRef } from 'react';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;IonButton onClick={onToggleTracking}&gt;Toggle Tracking&lt;/IonButton&gt;\n  );\n}\n</code></pre> <pre><code>&lt;script setup lang=\"ts\"&gt;\nimport { onMounted, onUnmounted } from 'vue';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nconst subscriptions: Subscription[] = [];\n\nonMounted(() =&gt; {\n  // Register event listeners *before* calling ready()\n  subscriptions.push(\n    BackgroundGeolocation.onLocation(location =&gt; {\n      console.log('[onLocation]', location);\n    })\n  );\n\n  // ready() configures the SDK and restores persisted state.\n  // It does NOT start tracking \u2014 call start()/stop() separately.\n  BackgroundGeolocation.ready({\n      geolocation: {\n        desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n        distanceFilter: 10,\n      },\n      app: {\n        stopOnTerminate: false,\n        startOnBoot: true,\n      },\n      logger: {\n        debug: true,\n        logLevel: BackgroundGeolocation.LogLevel.Verbose,\n      },\n  });\n});\n\n// e.g. a toggle-button handler in your template\nasync function onToggleTracking() {\n  const state = await BackgroundGeolocation.getState();\n  if (state.enabled) {\n    await BackgroundGeolocation.stop();\n  } else {\n    await BackgroundGeolocation.start();\n  }\n  console.log('[state]', state.enabled, state.trackingMode);\n}\n\nonUnmounted(() =&gt; {\n  subscriptions.forEach(s =&gt; s.remove());\n});\n&lt;/script&gt;\n</code></pre>"},{"location":"cordova/","title":"Cordova SDK Reference","text":"cordova-background-geolocation-lt Buy Now"},{"location":"cordova/#quick-links","title":"Quick links","text":"Section Description BackgroundGeolocation Primary entry point \u2014 start/stop, events, config AppConfig Lifecycle, heartbeat, scheduler, notifications HttpConfig HTTP upload endpoint, batching, auth headers GeoConfig Accuracy, distance filter, stop detection PersistenceConfig SQLite storage, TTL, templates LoggerConfig Log levels, retention, debug mode <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"cordova/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<pre><code>export const AccuracyAuthorization = {\n</code></pre> <p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"cordova/AccuracyAuthorization/#members","title":"Members","text":""},{"location":"cordova/AccuracyAuthorization/#full","title":"Full","text":"<p>Full accuracy authorized.</p>"},{"location":"cordova/AccuracyAuthorization/#reduced","title":"Reduced","text":"<p>Reduced accuracy granted.</p>"},{"location":"cordova/ActivityConfig/","title":"ActivityConfig","text":"<pre><code>export interface ActivityConfig {\n</code></pre> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"cordova/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"cordova/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    // Poll activity recognition every 10 seconds (both platforms)\n    activityRecognitionInterval: 10000,\n\n    // Require \u226575% confidence before changing activity (both platforms)\n    minimumActivityRecognitionConfidence: 75,\n\n    // Disable automatic stop detection (both platforms)\n    disableStopDetection: false,\n\n    // Automatically stop when stationary (both platforms)\n    stopOnStationary: true,\n\n    // Delay motion-trigger transitions by 30s (Android only)\n    motionTriggerDelay: 30000,\n\n    // Disable platform motion activity APIs (both platforms)\n    disableMotionActivityUpdates: false,\n\n    // Delay stop-detection by 10s (iOS only)\n    stopDetectionDelay: 10000\n  }\n});\n</code></pre>"},{"location":"cordova/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeoConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"cordova/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Legacy (deprecated)\nBackgroundGeolocation.ready({\n  activityRecognitionInterval: 10000,\n  disableStopDetection: true,\n  stopOnStationary: true\n});\n</code></pre> <pre><code>// Current\nBackgroundGeolocation.ready({\n  activity: {\n    activityRecognitionInterval: 10000,\n    disableStopDetection: true,\n    stopOnStationary: true\n  }\n});\n</code></pre>"},{"location":"cordova/ActivityConfig/#members","title":"Members","text":""},{"location":"cordova/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>activityRecognitionInterval?: number;\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"cordova/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>disableMotionActivityUpdates?: boolean;\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"cordova/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"cordova/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableMotionActivityUpdates: true\n  }\n});\n</code></pre>"},{"location":"cordova/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"cordova/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeoConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeoConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableStopDetection: true,\n  },\n  geolocation: {\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BackgroundGeolocation.changePace).</p> <p></p>"},{"location":"cordova/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BackgroundGeolocation.changePace <code>false</code> or BackgroundGeolocation.stop explicitly.</p>"},{"location":"cordova/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>minimumActivityRecognitionConfidence?: number;\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"cordova/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>motionTriggerDelay?: number;\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    motionTriggerDelay: 30000\n  }\n});\n</code></pre>"},{"location":"cordova/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>stopDetectionDelay?: number;\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"cordova/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the GeoConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeoConfig.stopTimeout timer elapses naturally. It does not fire when BackgroundGeolocation.changePace <code>false</code> is called manually.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    stopOnStationary: true,\n  }\n}).then(() =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"cordova/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<p><code>triggerActivities?:</code><code>TriggerActivity</code><code>|</code><code>TriggerActivity</code><code>[];</code></p> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See TriggerActivity for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>// Only trigger tracking for vehicles\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"in_vehicle\"]\n  }\n});\n\n// Only trigger tracking for on_foot, walking and running\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"on_foot\", \"walking\", \"running\"]\n  }\n});\n</code></pre>"},{"location":"cordova/ActivityType/","title":"ActivityType","text":"<pre><code>export const ActivityType = {\n</code></pre> <p>iOS Activity Type used with GeoConfig.activityType.</p> <p>Corresponds to Apple's <code>CLActivityType</code> enum: https://developer.apple.com/documentation/corelocation/clactivitytype</p> Name Value Description Other 1 Default/unspecified activity. AutomotiveNavigation 2 Automotive navigation mode. Fitness 3 Fitness-related activity. OtherNavigation 4 Non-automotive navigation mode. Airborne 5 Airborne activity (iOS 15+)."},{"location":"cordova/ActivityType/#members","title":"Members","text":""},{"location":"cordova/ActivityType/#airborne","title":"Airborne","text":"<p>Airborne activity (iOS 15+).</p>"},{"location":"cordova/ActivityType/#automotivenavigation","title":"AutomotiveNavigation","text":"<p>Automotive navigation mode.</p>"},{"location":"cordova/ActivityType/#fitness","title":"Fitness","text":"<p>Fitness-related activity.</p>"},{"location":"cordova/ActivityType/#other","title":"Other","text":"<p>Default/unspecified activity.</p>"},{"location":"cordova/ActivityType/#othernavigation","title":"OtherNavigation","text":"<p>Non-automotive navigation mode.</p>"},{"location":"cordova/AppConfig/","title":"AppConfig","text":"<pre><code>export interface AppConfig {\n</code></pre> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"cordova/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"cordova/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"cordova/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BackgroundGeolocation.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"cordova/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BackgroundGeolocation.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"cordova/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See PermissionRationale for the full dialog configuration reference.</p>"},{"location":"cordova/AppConfig/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"cordova/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#members","title":"Members","text":""},{"location":"cordova/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<p><code>background</code><code>PermissionRationale</code><code>?:</code><code>PermissionRationale</code><code>;</code></p> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside PermissionRationale fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Location updates in Android 11</p>"},{"location":"cordova/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BackgroundGeolocation.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BackgroundGeolocation.registerHeadlessTask</p>"},{"location":"cordova/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>heartbeatInterval?: number;\n</code></pre> <p>Rate in seconds at which BackgroundGeolocation.onHeartbeat events fire while the app is in the background.</p>"},{"location":"cordova/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"cordova/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BackgroundGeolocation.onHeartbeat</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60\n  }\n});\n\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n\n  // Optionally request a new location during heartbeat.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition]\", location);\n  });\n});\n</code></pre>"},{"location":"cordova/AppConfig/#notification","title":"notification","text":"<p><code>notification?:</code><code>NotificationConfig</code><code>;</code></p> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"Background tracking engaged\",\n      text: \"My notification text\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>preventSuspend?: boolean;\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BackgroundGeolocation.onHeartbeat</p> <pre><code>// Subscribe to heartbeat events\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n});\n\n// Enable preventSuspend via AppConfig\nBackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60,\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#schedule","title":"schedule","text":"<pre><code>schedule?: string[];\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"{DAY(s)} {START_TIME}-{END_TIME}\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1 17:30-21:00\",     // Sunday: 5:30pm\u20139pm\n      \"2-6 09:00-17:00\",   // Mon\u2013Fri: 9am\u20135pm\n      \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm\u2013midnight\n      \"7 10:00-19:00\"      // Saturday: 10am\u20137pm\n    ]\n  }\n}).then((state) =&gt; {\n  // Start the Scheduler\n  BackgroundGeolocation.startSchedule();\n});\n\n// Listen for schedule state changes\nBackgroundGeolocation.onSchedule((state) =&gt; {\n  console.log(\"[onSchedule] enabled?\", state.enabled);\n});\n\n// Later (e.g., user logout)\nBackgroundGeolocation.stopSchedule();\nBackgroundGeolocation.stop(); // if tracking is currently active\n\n// Modify schedule using setConfig\nBackgroundGeolocation.setConfig({\n  app: {\n    schedule: [\n      \"1-7 09:00-10:00\",\n      \"1-7 11:00-12:00\",\n      \"1-7 13:00-14:00\",\n      \"1-7 15:00-16:00\",\n      \"1-7 17:00-18:00\",\n      \"2,4,6 19:00-22:00\"\n    ]\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01 09:00-17:00\"\n    ]\n  }\n});\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-00:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"cordova/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BackgroundGeolocation.startSchedule - BackgroundGeolocation.stopSchedule</p>"},{"location":"cordova/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-12:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>scheduleUseAlarmManager?: boolean;\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\"1-7 09:00-17:00\"],\n    scheduleUseAlarmManager: true\n  }\n});\n</code></pre>"},{"location":"cordova/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"cordova/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"cordova/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"cordova/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"cordova/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeoConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"cordova/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"cordova/AuthorizationConfig/","title":"AuthorizationConfig","text":"<pre><code>export interface AuthorizationConfig {\n</code></pre> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"cordova/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"cordova/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>const token = getMyToken(); // Your own token fetch method\n\nBackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://app.your.server.com/users/locations\",\n    autoSync: true,\n  },\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshPayload: {\n      the_refresh_token_field_name: \"{refreshToken}\"\n    },\n    expires: token.expiresAt,\n  }\n});\n</code></pre>"},{"location":"cordova/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"cordova/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"cordova/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n</code></pre>"},{"location":"cordova/AuthorizationConfig/#members","title":"Members","text":""},{"location":"cordova/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\"\n  }\n});\n</code></pre>"},{"location":"cordova/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>expires?: number;\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"cordova/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>refreshHeaders?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.domain.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    },\n    refreshHeaders: {}\n  }\n});\n</code></pre>"},{"location":"cordova/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>refreshPayload?: Record&lt;string, string&gt;;\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    }\n  }\n});\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"cordova/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken?: string;\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"cordova/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>refreshUrl?: string;\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p>"},{"location":"cordova/AuthorizationConfig/#strategy","title":"strategy","text":"<pre><code>strategy: string;\n</code></pre> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"cordova/AuthorizationEvent/","title":"AuthorizationEvent","text":"<pre><code>export interface AuthorizationEvent {\n</code></pre> <p>Authorization token refresh result delivered to BackgroundGeolocation.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onAuthorization] Token refreshed:\", event.response);\n    // Update your app's stored token.\n    const token = event.response.access_token;\n  } else {\n    console.warn(\"[onAuthorization] Refresh failed:\", event.error);\n  }\n});\n</code></pre>"},{"location":"cordova/AuthorizationEvent/#members","title":"Members","text":""},{"location":"cordova/AuthorizationEvent/#error","title":"error","text":"<pre><code>error: string;\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"cordova/AuthorizationEvent/#response","title":"response","text":"<pre><code>response: any;\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"cordova/AuthorizationEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"cordova/AuthorizationEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"cordova/AuthorizationStatus/","title":"AuthorizationStatus","text":"<pre><code>export const AuthorizationStatus = {\n</code></pre> <p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"cordova/AuthorizationStatus/#members","title":"Members","text":""},{"location":"cordova/AuthorizationStatus/#always","title":"Always","text":"<p>Location permissions authorized for always use.</p>"},{"location":"cordova/AuthorizationStatus/#denied","title":"Denied","text":"<p>Location permissions denied by the user.</p>"},{"location":"cordova/AuthorizationStatus/#notdetermined","title":"NotDetermined","text":"<pre><code>NotDetermined: 0,\n</code></pre> <p>User has not yet made a choice regarding location permissions.</p>"},{"location":"cordova/AuthorizationStatus/#restricted","title":"Restricted","text":"<pre><code>Restricted: 1,\n</code></pre> <p>Location permissions are restricted (e.g., parental controls).</p>"},{"location":"cordova/AuthorizationStatus/#wheninuse","title":"WhenInUse","text":"<p>Location permissions authorized for when-in-use (Android 10+).</p>"},{"location":"cordova/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<pre><code>export const AuthorizationStrategy = {\n</code></pre> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"cordova/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"cordova/AuthorizationStrategy/#jwt","title":"Jwt","text":"<p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"cordova/AuthorizationStrategy/#sas","title":"Sas","text":"<p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"cordova/BackgroundGeolocation/","title":"BackgroundGeolocation","text":"<pre><code>export interface BackgroundGeolocation {\n</code></pre> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>","boost":10},{"location":"cordova/BackgroundGeolocation/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>","boost":10},{"location":"cordova/BackgroundGeolocation/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeoConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li>Event</li> </ul> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"react-native-background-geolocation\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a Subscription that must be removed when no longer needed:</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre> <pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#compound-configuration","title":"Compound configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"{{pluginName}}\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#event-listeners","title":"Event listeners","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getting-started","title":"Getting started","text":"<pre><code>import BackgroundGeolocation from \"{{pluginName}}\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#events_1","title":"Events","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#onactivitychange","title":"onActivityChange","text":"<p><code>onActivityChange(cb: (event:</code><code>MotionActivityEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#android","title":"Android","text":"<p>MotionActivityEvent.confidence always reports <code>100</code>.</p> <pre><code>const subscription = BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange] \", event);\n});\n</code></pre> <p>activitychange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onauthorization","title":"onAuthorization","text":"<p><code>onAuthorization(cb: (event:</code><code>AuthorizationEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>const subscription = BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS: \", event.response);\n  } else {\n    console.log(\"[authorization] ERROR: \", event.error);\n  }\n});\n</code></pre> <p>authorization</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>onConnectivityChange(cb: (event:</code><code>ConnectivityChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>const subscription = BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] \", event);\n});\n</code></pre> <p>connectivitychange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onenabledchange","title":"onEnabledChange","text":"<p><code>onEnabledChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>const subscription = BackgroundGeolocation.onEnabledChange((isEnabled) =&gt; {\n  console.log(\"[onEnabledChange] isEnabled? \", isEnabled);\n});\n</code></pre> <p>enabledchange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ongeofence","title":"onGeofence","text":"<p><code>on</code><code>Geofence</code><code>(cb: (event:</code><code>GeofenceEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] \", event);\n});\n</code></pre> <p>geofence</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>onGeofencesChange(cb: (event:</code><code>GeofencesChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeoConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  // Create map circles\n  on.forEach((geofence) =&gt; {\n    createGeofenceMarker(geofence);\n  });\n\n  // Remove map circles\n  off.forEach((identifier) =&gt; {\n    removeGeofenceMarker(identifier);\n  });\n});\n</code></pre> <p>geofenceschange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onheartbeat","title":"onHeartbeat","text":"<p><code>onHeartbeat(cb: (event:</code><code>HeartbeatEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The Location provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    heartbeatInterval: 60,\n    preventSuspend: true  // required for iOS\n  }\n});\n\nconst subscription = BackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat] \", event);\n\n  // Optionally fetch a fresh location.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition] \", location);\n  });\n});\n</code></pre> <p>heartbeat</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onhttp","title":"onHttp","text":"<p><code>onHttp(cb: (event:</code><code>HttpEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onHttp((response) =&gt; {\n  const status = response.status;\n  const success = response.success;\n  const responseText = response.responseText;\n  console.log(\"[onHttp] \", response);\n});\n</code></pre> <p>http</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onlocation","title":"onLocation","text":"<p><code>on</code><code>Location</code><code>(cb: (location:</code><code>Location</code><code>) =&gt; void, onError?: (err:</code><code>LocationError</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a LocationError code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with Location.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation] success: \", location);\n}, (error) =&gt; {\n  console.log(\"[onLocation] ERROR: \", error);\n});\n</code></pre> <p>location</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onmotionchange","title":"onMotionChange","text":"<p><code>onMotionChange(cb: (event:</code><code>MotionChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeoConfig.stopTimeout</p> <pre><code>const subscription = BackgroundGeolocation.onMotionChange((event: MotionChangeEvent) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device has just started MOVING \", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device has just STOPPED: \", event.location);\n  }\n});\n</code></pre> <p>motionchange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onnotificationaction","title":"onNotificationAction","text":"<p><code>onNotificationAction(cb: (buttonId: string) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onpowersavechange","title":"onPowerSaveChange","text":"<p><code>onPowerSaveChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"cordova/BackgroundGeolocation/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const subscription = BackgroundGeolocation.onPowerSaveChange((isPowerSaveMode) =&gt; {\n  console.log(\"[onPowerSaveChange]: \", isPowerSaveMode);\n});\n</code></pre> <p>powersavechange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onproviderchange","title":"onProviderChange","text":"<p><code>onProviderChange(cb: (event:</code><code>ProviderChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>const subscription = BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]: \", event);\n\n  switch (event.status) {\n    case BackgroundGeolocation.AuthorizationStatus.Denied:\n      // Android &amp; iOS\n      console.log(\"- Location authorization denied\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.Always:\n      // Android &amp; iOS\n      console.log(\"- Location always granted\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.WhenInUse:\n      // iOS only\n      console.log(\"- Location WhenInUse granted\");\n      break;\n  }\n});\n</code></pre> <p>providerchange</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#onschedule","title":"onSchedule","text":"<p><code>onSchedule(cb: (state:</code><code>State</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>const subscription = BackgroundGeolocation.onSchedule((state) =&gt; {\n  if (state.enabled) {\n    console.log(\"[onSchedule] scheduled start tracking\");\n  } else {\n    console.log(\"[onSchedule] scheduled stop tracking\");\n  }\n});\n</code></pre> <p>schedule</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#methods","title":"Methods","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#changepace","title":"changePace","text":"<p><code>changePace(isMoving: boolean): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>BackgroundGeolocation.changePace(true);  // location services ON (\"moving\")\nBackgroundGeolocation.changePace(false); // location services OFF (\"stationary\")\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#destroytransistorauthorizationtoken","title":"destroyTransistorAuthorizationToken","text":"<p><code>destroy</code><code>TransistorAuthorizationToken</code><code>(url: string): Promise&lt;void&gt;;</code></p> <p>Destroy a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#findorcreatetransistorauthorizationtoken","title":"findOrCreateTransistorAuthorizationToken","text":"<p><code>findOrCreateTransistorAuthorizationToken(orgname: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>getCurrentPosition(options?:</code><code>CurrentPositionRequest</code><code>): Promise&lt;</code><code>Location</code><code>&gt;;</code></p> <p>Retrieve the current Location.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a LocationError.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#options","title":"Options","text":"<p>See CurrentPositionRequest.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#error-codes_1","title":"Error Codes","text":"<p>See LocationError.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with Location.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // 30 second timeout to fetch location\n  maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.\n  desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.\n  samples: 3,           // How many location samples to attempt.\n  extras: {             // Custom meta-data.\n    \"route_id\": 123\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getdeviceinfo","title":"getDeviceInfo","text":"<p><code>get</code><code>DeviceInfo</code><code>(): Promise&lt;</code><code>DeviceInfo</code><code>&gt;;</code></p> <p>Returns device information.</p> <pre><code>const deviceInfo = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(deviceInfo);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getodometer","title":"getOdometer","text":"<pre><code>getOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the current odometer reading in meters.</p> <p>The SDK continuously accumulates distance traveled between recorded locations.</p> <p>Warning</p> <p>Odometer accuracy depends on location accuracy. Noisy or inaccurate locations introduce error into accumulated distance. Use LocationFilter.odometerAccuracyThreshold to filter low-accuracy samples from odometer calculations.</p> <p>See also - LocationFilter.odometerAccuracyThreshold - <code>resetOdometer</code> / <code>setOdometer</code></p> <pre><code>const odometer = await BackgroundGeolocation.getOdometer();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getstate","title":"getState","text":"<p><code>get</code><code>State</code><code>(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Return the current State of the SDK, including all Config parameters.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] \", state.enabled, state.trackingMode);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#ready","title":"ready","text":"<p><code>ready(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n  http: {\n    url: \"http://your.server.com\",\n    headers: { \"my-auth-token\": \"secret-token\" },\n  }\n});\nconsole.log(\"[ready] success\", state);\n</code></pre> <pre><code>BackgroundGeolocation.reset();\n// Reset to documented default-values with overrides\nbgGeo.reset({\n  distanceFilter:  10\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#registerheadlesstask","title":"registerHeadlessTask","text":"<p><code>registerHeadlessTask(callback: (event:</code><code>HeadlessEvent</code><code>) =&gt; Promise&lt;void&gt;): void;</code></p> <p>Android only Registers a headless-task callback for Android background events when the app has been terminated with AppConfig.stopOnTerminate<code>:false</code>.</p> <p>The callback receives a HeadlessEvent with a <code>name</code> (event name) and <code>params</code> (event data).</p> <p>Warning</p> <p>You must call <code>registerHeadlessTask</code> in your application root file (e.g. <code>index.js</code>), not inside a component or behind a UI action.</p> <p>Warning</p> <p>Your function must be declared <code>async</code>. Await all work inside it \u2014 the headless task is automatically terminated after the last line executes.</p> <p>Note</p> <p>Javascript headless callbacks are not supported by Cordova or Capacitor.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#debugging","title":"Debugging","text":"<p>While implementing your headless task, observe Android logs via:</p> <pre><code>$ adb logcat *:S TSLocationManager:V ReactNativeJS:V\n</code></pre> <p>See also - \ud83d\udcd8 Android Headless Mode - AppConfig.enableHeadless</p> <pre><code>const BackgroundGeolocationHeadlessTask = async (event) =&gt; {\n  const params = event.params;\n  console.log(\"[BackgroundGeolocation HeadlessTask] -\", event.name, params);\n\n  switch (event.name) {\n    case \"terminate\":\n      // Use await for async tasks\n      const location = await BackgroundGeolocation.getCurrentPosition({\n        samples: 1,\n        persist: false\n      });\n      console.log(\"[BackgroundGeolocation HeadlessTask] - getCurrentPosition:\", location);\n      break;\n  }\n  // You must await all work you do in your task.\n  // Headless-tasks are automatically terminated after executing the last line.\n  await doWork();\n}\n\nBackgroundGeolocation.registerHeadlessTask(BackgroundGeolocationHeadlessTask);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#removelisteners","title":"removeListeners","text":"<pre><code>removeListeners(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all event listeners.</p> <p>Calls Subscription.remove on all active subscriptions.</p> <pre><code>BackgroundGeolocation.removeListeners();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#reset","title":"reset","text":"<p><code>reset(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Reset the SDK configuration to documented default values.</p> <p>If an optional Config is provided, it is applied after the reset.</p> <pre><code>BackgroundGeolocation.reset();\n// Reset to default values with overrides\nBackgroundGeolocation.reset({\n  geolocation: { distanceFilter: 10 }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#resetodometer","title":"resetOdometer","text":"<pre><code>resetOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>const location = await BackgroundGeolocation.resetOdometer();\nconsole.log(\"[resetOdometer] reset at: \", location);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#setconfig","title":"setConfig","text":"<p><code>set</code><code>Config</code><code>(config: Partial&lt;</code><code>Config</code><code>&gt;): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Update the SDK's Config at runtime.</p> <p>The supplied Config is merged into the current configuration and applied immediately. Use this after <code>ready</code> has been called to change settings dynamically.</p> <pre><code>const state = await BackgroundGeolocation.setConfig({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 100.0,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\nconsole.log(\"[setConfig] success: \", state);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#setodometer","title":"setOdometer","text":"<pre><code>setOdometer(value: number): Promise&lt;number&gt;;\n</code></pre> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>const location = await BackgroundGeolocation.setOdometer(1234.56);\nconsole.log(\"[setOdometer] set at: \", location);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#start","title":"start","text":"<p><code>start(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>const state = await BackgroundGeolocation.start();\nconsole.log(\"[start] success - \", state);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#startgeofences","title":"startGeofences","text":"<p><code>startGeofences(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Add a geofence.\nBackgroundGeolocation.addGeofence({\n  notifyOnExit: true,\n  radius: 200,\n  identifier: \"ZONE_OF_INTEREST\",\n  latitude: 37.234232,\n  longitude: 42.234234\n});\n\n// Listen to geofence events.\nBackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] - \", event);\n});\n\n// Configure and start in geofences-only mode.\nBackgroundGeolocation.ready({\n  http: { url: \"http://my.server.com\", autoSync: true }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#startschedule","title":"startSchedule","text":"<pre><code>startSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>const state = await BackgroundGeolocation.startSchedule();\nconsole.log(\"[startSchedule] success: \", state);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#stop","title":"stop","text":"<p><code>stop(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>BackgroundGeolocation.stop();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#stop-tracking-and-the-scheduler","title":"Stop tracking and the scheduler","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#stopschedule","title":"stopSchedule","text":"<pre><code>stopSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>await BackgroundGeolocation.stopSchedule();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#stop-the-scheduler-and-active-tracking","title":"Stop the scheduler and active tracking","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#watchposition","title":"watchPosition","text":"<p><code>watchPosition(options:</code><code>WatchPositionRequest</code><code>, locationCallback: (location:</code><code>Location</code><code>) =&gt; void, errorCallback?: (errorCode: number) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a Subscription that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>onResume() async {\n  // Start watching position while app in foreground, retaining the return Subscription.\n  this.watchPositionSubscription = await BackgroundGeolocation.watchPosition({\n    interval: 1000,\n    extras: {foo: \"bar\"}\n  }, (location) =&gt; {\n    console.log(\"[watchPosition] -\", location);\n  }, (errorCode) =&gt; {\n    console.log(\"[watchPosition] ERROR -\", errorCode);\n  });\n}\n\nonSuspend() {\n  // Halt watching position when app goes to background.\n  this.watchPositionSubscription.remove();\n  this.watchPositionSubscription = null;\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#geofencing","title":"Geofencing","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#addgeofence","title":"addGeofence","text":"<p><code>add</code><code>Geofence</code><code>(geofence:</code><code>Geofence</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 150,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: false,\n  notifyOnDwell: true,\n  loiteringDelay: 30000,  // 30 seconds\n  extras: {               // Optional arbitrary meta-data\n    zone_id: 1234\n  }\n}).then((success) =&gt; {\n  console.log(\"[addGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[addGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#addgeofences","title":"addGeofences","text":"<p><code>addGeofences(geofences:</code><code>Geofence</code><code>[]): Promise&lt;boolean&gt;;</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>const geofences = [{\n  identifier: \"foo\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}, {\n  identifier: \"bar\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}];\n\nBackgroundGeolocation.addGeofences(geofences);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#geofenceexists","title":"geofenceExists","text":"<pre><code>geofenceExists(identifier: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const exists = await BackgroundGeolocation.geofenceExists(\"HOME\");\nconsole.log(\"[geofenceExists] \", exists);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getgeofence","title":"getGeofence","text":"<p><code>get</code><code>Geofence</code><code>(identifier: string): Promise&lt;</code><code>Geofence</code><code>&gt;;</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofence = await BackgroundGeolocation.getGeofence(\"HOME\");\nconsole.log(\"[getGeofence] \", geofence);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getgeofences","title":"getGeofences","text":"<p><code>getGeofences(): Promise&lt;</code><code>Geofence</code><code>[]&gt;;</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]: \", geofences);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#removegeofence","title":"removeGeofence","text":"<p><code>remove</code><code>Geofence</code><code>(identifier: string): Promise&lt;boolean&gt;;</code></p> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofence(\"Home\").then((success) =&gt; {\n  console.log(\"[removeGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[removeGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#removegeofences","title":"removeGeofences","text":"<pre><code>removeGeofences(identifiers?: string[]): Promise&lt;boolean&gt;;\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofences();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#data-management","title":"Data Management","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#destroylocation","title":"destroyLocation","text":"<p><code>destroy</code><code>Location</code><code>(uuid: string): Promise&lt;void&gt;;</code></p> <p>Remove a single location by Location.uuid.</p> <pre><code>await BackgroundGeolocation.destroyLocation(location.uuid);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#destroylocations","title":"destroyLocations","text":"<pre><code>destroyLocations(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>await BackgroundGeolocation.destroyLocations();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getcount","title":"getCount","text":"<pre><code>getCount(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>const count = await BackgroundGeolocation.getCount();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#getlocations","title":"getLocations","text":"<pre><code>getLocations(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Retrieve all Location records stored in the SDK's SQLite database.</p> <pre><code>const locations = await BackgroundGeolocation.getLocations();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#sync","title":"sync","text":"<pre><code>sync(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const records = await BackgroundGeolocation.sync();\nconsole.log(\"[sync] success: \", records);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#authorization","title":"Authorization","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#getproviderstate","title":"getProviderState","text":"<p><code>getProviderState(): Promise&lt;</code><code>ProviderChangeEvent</code><code>&gt;;</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>const providerState = await BackgroundGeolocation.getProviderState();\nconsole.log(\"- Provider state: \", providerState);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#requestpermission","title":"requestPermission","text":"<p><code>requestPermission(): Promise&lt;</code><code>AuthorizationStatus</code><code>&gt;;</code></p> <p>Manually request location permission using the configured GeoConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeoConfig.locationAuthorizationRequest - GeoConfig.disableLocationAuthorizationAlert - GeoConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log('[providerchange]', event);\n});\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: 'Always' }\n});\n\ntry {\n  const status = await BackgroundGeolocation.requestPermission();\n  console.log('[requestPermission] success: ', status);\n} catch (status) {\n  console.warn('[requestPermission] FAILURE: ', status);\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<p><code>requestTemporaryFullAccuracy(purposeKey: string): Promise&lt;</code><code>AccuracyAuthorization</code><code>&gt;;</code></p> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>","boost":10},{"location":"cordova/BackgroundGeolocation/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Supply \"Purpose\" key from Info.plist as 1st argument.\n    BackgroundGeolocation.requestTemporaryFullAccuracy(\"Delivery\").then((accuracyAuthorization) =&gt; {\n      if (accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Full) {\n        console.log('[requestTemporaryFullAccuracy] GRANTED: ', accuracyAuthorization);\n      } else {\n        console.log('[requestTemporaryFullAccuracy] DENIED: ', accuracyAuthorization);\n      }\n    }).catch((error) =&gt; {\n      console.warn(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: \", error);\n    });\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#background-tasks","title":"Background Tasks","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>startBackgroundTask(): Promise&lt;number&gt;;\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ios_2","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#android_2","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>onLocation(location) {\n  console.log(\"[location] \", location);\n\n  // Perform some long-running task (eg: HTTP request)\n  BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n    performLongRunningTask().then(() =&gt; {\n      // When your long-running task is complete, signal completion of taskId.\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    }).catch((error) =&gt; {\n      // Be sure to catch errors: never leave your background-task hanging.\n      console.error(error);\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    });\n  });\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>stopBackgroundTask(taskId: number): Promise&lt;void&gt;;\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n  // Perform some long-running task (eg: HTTP request)\n  performLongRunningTask().then(() =&gt; {\n    // When your long-running task is complete, signal completion of taskId.\n    BackgroundGeolocation.stopBackgroundTask(taskId);\n  });\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#logging","title":"Logging","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#playsound","title":"playSound","text":"<pre><code>playSound(soundId: string | number): void;\n</code></pre> <p>Play a system sound.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ios_3","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#android_3","title":"Android","text":"<p>Provide a sound name string.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#setloglevel","title":"setLogLevel","text":"<p><code>set</code><code>LogLevel</code><code>(level:</code><code>LogLevel</code><code>): Promise&lt;void&gt;;</code></p> <p>Sets the LoggerConfig.logLevel.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#device","title":"Device","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#getsensors","title":"getSensors","text":"<p><code>get</code><code>Sensors</code><code>(): Promise&lt;</code><code>Sensors</code><code>&gt;;</code></p> <p>Returns the availability of motion sensors: accelerometer, gyroscope, and magnetometer.</p> <p>These sensors power the motion activity-recognition system. When any sensor is absent (particularly on low-end Android devices), motion recognition performance degrades significantly.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(sensors);\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>isPowerSaveMode(): Promise&lt;boolean&gt;;\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#ios_4","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"cordova/BackgroundGeolocation/#android_4","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const isPowerSaveMode = await BackgroundGeolocation.isPowerSaveMode();\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#properties","title":"Properties","text":"","boost":10},{"location":"cordova/BackgroundGeolocation/#accuracyauthorization","title":"AccuracyAuthorization","text":"<p><code>AccuracyAuthorization</code><code>: object;</code></p> <p>Enum namespace indicating whether the user granted full or reduced location accuracy. [iOS 14+]</p> <p>Used by <code>ProviderChangeEvent.accuracyAuthorization</code> and <code>requestTemporaryFullAccuracy</code>.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization ===\n      BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Handle reduced-accuracy case\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#activitytype","title":"ActivityType","text":"<p><code>ActivityType</code><code>: object;</code></p> <p>iOS only Enum namespace specifying the type of user activity (AutomotiveNavigation, Fitness, OtherNavigation, etc).</p> <p>Used by GeoConfig.activityType.</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#authorizationstatus","title":"AuthorizationStatus","text":"<p><code>AuthorizationStatus</code><code>: object;</code></p> <p>Enum namespace representing the OS-level authorization state for location services (Denied, Restricted, Always, WhenInUse).</p> <p>Returned from <code>requestPermission()</code> and <code>onProviderChange</code>.</p> <pre><code>const status = await BackgroundGeolocation.requestPermission();\nif (status === BackgroundGeolocation.AuthorizationStatus.Always) {\n  // Good to start tracking\n}\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#authorizationstrategy","title":"AuthorizationStrategy","text":"<p><code>AuthorizationStrategy</code><code>: object;</code></p> <p>Enum namespace defining how the HTTP service performs authorization.</p> <p>Includes basic, JWT, and custom strategies. Used by <code>AuthorizationConfig.strategy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: BackgroundGeolocation.AuthorizationStrategy.Jwt\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#desiredaccuracy","title":"DesiredAccuracy","text":"<p><code>DesiredAccuracy</code><code>: object;</code></p> <p>Enum namespace controlling the native location engine's target accuracy.</p> <p>Higher accuracy consumes more battery. Used by <code>GeoConfig.desiredAccuracy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#event","title":"Event","text":"<p><code>Event</code><code>: object;</code></p> <p>Enum namespace of all event names emitted by the SDK (<code>location</code>, <code>geofence</code>, <code>motionchange</code>, <code>heartbeat</code>, etc).</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#httpmethod","title":"HttpMethod","text":"<p><code>HttpMethod</code><code>: object;</code></p> <p>Enum namespace defining the HTTP method used for location uploads (POST, PUT, etc).</p> <p>Used by <code>HttpConfig.method</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    method: BackgroundGeolocation.HttpMethod.Post\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#kalmanprofile","title":"KalmanProfile","text":"<p><code>KalmanProfile</code><code>: object;</code></p> <p>Enum namespace selecting a preset Kalman filter tuning profile (aggressive, moderate, or relaxed smoothing).</p> <p>Used by <code>LocationFilter.kalmanProfile</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      kalmanProfile: BackgroundGeolocation.KalmanProfile.Aggressive\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#locationfilterpolicy","title":"LocationFilterPolicy","text":"<p><code>LocationFilterPolicy</code><code>: object;</code></p> <p>Enum namespace selecting the filtering engine policy for noise-reduction and smoothing.</p> <p>Used by <code>LocationFilter.policy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: BackgroundGeolocation.LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#locationrequest","title":"LocationRequest","text":"<p><code>LocationRequest</code><code>: object;</code></p> <p>iOS only Enum namespace defining the type of location permission request (Always, WhenInUse, or Any). Used by <code>GeoConfig.locationAuthorizationRequest</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: BackgroundGeolocation.LocationRequest.Always\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#loglevel","title":"LogLevel","text":"<p><code>LogLevel</code><code>: object;</code></p> <p>Enum namespace controlling verbosity of the SDK logger.</p> <p>Used by <code>LoggerConfig.logLevel</code>. Values range from silent (<code>Off</code>) to extremely verbose (<code>Verbose</code>).</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#notificationpriority","title":"NotificationPriority","text":"<p><code>NotificationPriority</code><code>: object;</code></p> <p>Android only Enum namespace controlling Android foreground-service notification priority and icon placement (top, bottom, hidden).</p> <p>Used by <code>NotificationConfig.priority</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: BackgroundGeolocation.NotificationPriority.High\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#persistmode","title":"PersistMode","text":"<p><code>PersistMode</code><code>: object;</code></p> <p>Enum namespace controlling which records the SDK persists to SQLite: locations only, geofences only, both, or none.</p> <p>Used by <code>PersistenceConfig.persistMode</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    persistMode: BackgroundGeolocation.PersistMode.All\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#triggeractivity","title":"TriggerActivity","text":"<p><code>TriggerActivity</code><code>: object;</code></p> <p>Enum namespace defining which physical motion activities can trigger motion-detection transitions (still \u2192 moving).</p> <p>Used by <code>ActivityConfig.triggerActivities</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\n      BackgroundGeolocation.TriggerActivity.InVehicle\n    ]\n  }\n});\n</code></pre>","boost":10},{"location":"cordova/BackgroundGeolocation/#devicesettings","title":"deviceSettings","text":"<p><code>deviceSettings:</code><code>DeviceSettings</code><code>;</code></p> <p>DeviceSettings API</p>","boost":10},{"location":"cordova/BackgroundGeolocation/#logger","title":"logger","text":"<p><code>logger:</code><code>Logger</code><code>;</code></p> <p>Logger API</p>","boost":10},{"location":"cordova/Battery/","title":"Battery","text":"<pre><code>export interface Battery {\n</code></pre> <p>Device battery state at the time a Location was recorded.</p>"},{"location":"cordova/Battery/#members","title":"Members","text":""},{"location":"cordova/Battery/#is_charging","title":"is_charging","text":"<pre><code>is_charging: boolean;\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"cordova/Battery/#level","title":"level","text":"<pre><code>level: number;\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"cordova/Config/","title":"Config","text":"<pre><code>export interface Config {\n</code></pre> <p>Root configuration object passed to BackgroundGeolocation.ready and BackgroundGeolocation.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"cordova/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"cordova/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeoConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BackgroundGeolocation.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BackgroundGeolocation.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"cordova/Config/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"cordova/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre>"},{"location":"cordova/Config/#full-configuration","title":"Full configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from '{{pluginName}}';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"cordova/Config/#members","title":"Members","text":""},{"location":"cordova/Config/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"cordova/Config/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"cordova/Config/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"cordova/Config/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeoConfig.</p>"},{"location":"cordova/Config/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"cordova/Config/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"cordova/Config/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"cordova/Config/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"cordova/Config/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre> <pre><code>// Base url to Transistor Demo Server.\nconst url = \"http://tracker.transistorsoft.com\";\n\n// Register for an authorization token from server.\nconst token = await\n  BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\"my-company-name\", \"my-username\");\n\nBackgroundGeolocation.ready({\n  url: url + \"/api/locations\",\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: url + \"/v2/refresh_token\",\n    refreshPayload: {\n      refresh_token: \"{refreshToken}\"\n    },\n    expires: token.expires\n  }\n});\n</code></pre>"},{"location":"cordova/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<pre><code>export interface ConnectivityChangeEvent {\n</code></pre> <p>Network connectivity change delivered to BackgroundGeolocation.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] connected:\", event.connected);\n});\n</code></pre>"},{"location":"cordova/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"cordova/ConnectivityChangeEvent/#connected","title":"connected","text":"<pre><code>connected: boolean;\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"cordova/Coords/","title":"Coords","text":"<pre><code>export interface Coords {\n</code></pre> <p>Geographic coordinates attached to a Location.</p>"},{"location":"cordova/Coords/#members","title":"Members","text":""},{"location":"cordova/Coords/#accuracy","title":"accuracy","text":"<pre><code>accuracy: number;\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"cordova/Coords/#altitude","title":"altitude","text":"<pre><code>altitude?: number;\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"cordova/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"cordova/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"cordova/Coords/#altitude_accuracy","title":"altitude_accuracy","text":"<pre><code>altitude_accuracy?: number;\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"cordova/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"cordova/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"cordova/Coords/#ellipsoidal_altitude","title":"ellipsoidal_altitude","text":"<pre><code>ellipsoidal_altitude?: number;\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"cordova/Coords/#floor","title":"floor","text":"<pre><code>floor?: number;\n</code></pre> <p>iOS only Floor within a building, when indoor-positioning hardware (e.g. Bluetooth beacons) is available.</p>"},{"location":"cordova/Coords/#heading","title":"heading","text":"<pre><code>heading?: number;\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"cordova/Coords/#heading_accuracy","title":"heading_accuracy","text":"<pre><code>heading_accuracy?: number;\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"cordova/Coords/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"cordova/Coords/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"cordova/Coords/#speed","title":"speed","text":"<pre><code>speed?: number;\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"cordova/Coords/#speed_accuracy","title":"speed_accuracy","text":"<pre><code>speed_accuracy?: number;\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"cordova/CurrentPositionRequest/","title":"CurrentPositionRequest","text":"<pre><code>export interface CurrentPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.getCurrentPosition.</p> <p>All fields are optional. The SDK always requests location from the native API at maximum accuracy (DesiredAccuracy.High), regardless of the <code>desiredAccuracy</code> threshold set here.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // seconds before giving up\n  maximumAge: 5000,     // accept a cached fix up to 5000 ms old\n  desiredAccuracy: 10,  // stop sampling when accuracy \u2264 10 m\n  samples: 3,           // take up to 3 samples and pick the best\n  extras: {\n    route_id: 123\n  }\n});\n</code></pre>"},{"location":"cordova/CurrentPositionRequest/#members","title":"Members","text":""},{"location":"cordova/CurrentPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<pre><code>desiredAccuracy?: number;\n</code></pre> <p>Accuracy threshold in meters. The SDK stops sampling as soon as it receives a location with <code>accuracy \u2264 desiredAccuracy</code> and returns it immediately. Defaults to GeoConfig.stationaryRadius.</p> <p>Note</p> <p>This value is a stopping threshold, not a hardware accuracy setting. The SDK always requests locations at DesiredAccuracy.High from the native API regardless of this value.</p>"},{"location":"cordova/CurrentPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to the returned location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"cordova/CurrentPositionRequest/#maximumage","title":"maximumAge","text":"<pre><code>maximumAge?: number;\n</code></pre> <p>Accept the most recently recorded location if it is no older than this value in milliseconds. Default <code>0</code> (always fetch a fresh fix).</p>"},{"location":"cordova/CurrentPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist the returned location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"cordova/CurrentPositionRequest/#samples","title":"samples","text":"<pre><code>samples?: number;\n</code></pre> <p>Maximum number of location samples to collect before returning the most accurate result. Default <code>3</code>. Only the final selected location is persisted.</p>"},{"location":"cordova/CurrentPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in seconds to wait for a location fix. Default <code>30</code>.</p> <p>If the timeout expires before a satisfactory location is found, the Promise rejects with a LocationError.</p>"},{"location":"cordova/DesiredAccuracy/","title":"DesiredAccuracy","text":"<pre><code>export const DesiredAccuracy = {\n</code></pre> <p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"cordova/DesiredAccuracy/#members","title":"Members","text":""},{"location":"cordova/DesiredAccuracy/#high","title":"High","text":"<p>High accuracy (e.g., 0-10m).</p>"},{"location":"cordova/DesiredAccuracy/#low","title":"Low","text":"<p>Low accuracy (e.g., 100m).</p>"},{"location":"cordova/DesiredAccuracy/#lowest","title":"Lowest","text":"<p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"cordova/DesiredAccuracy/#medium","title":"Medium","text":"<p>Medium accuracy (e.g., 10m).</p>"},{"location":"cordova/DesiredAccuracy/#navigation","title":"Navigation","text":"<p>Navigation-level accuracy (e.g., 0-10m).</p>"},{"location":"cordova/DesiredAccuracy/#verylow","title":"VeryLow","text":"<p>Very low accuracy (e.g., 1000m).</p>"},{"location":"cordova/DeviceInfo/","title":"DeviceInfo","text":"<pre><code>export interface DeviceInfo {\n</code></pre> <p>Basic device hardware and OS information returned by BackgroundGeolocation.getDeviceInfo.</p> <pre><code>const info = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(`[DeviceInfo] ${info.manufacturer} ${info.model} (${info.platform})`);\n</code></pre>"},{"location":"cordova/DeviceInfo/#members","title":"Members","text":""},{"location":"cordova/DeviceInfo/#framework","title":"framework","text":"<pre><code>framework: string;\n</code></pre> <p>Development framework hosting the SDK.</p> <p>Examples: <code>\"react-native\"</code>, <code>\"capacitor\"</code>, <code>\"cordova\"</code>, <code>\"flutter\"</code></p>"},{"location":"cordova/DeviceInfo/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer.</p> <p>Examples: <code>\"Apple\"</code>, <code>\"Google\"</code>, <code>\"Samsung\"</code>, <code>\"Huawei\"</code></p>"},{"location":"cordova/DeviceInfo/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model identifier.</p> <p>Examples: <code>\"iPhone15,2\"</code>, <code>\"Pixel 8 Pro\"</code>, <code>\"SM-G991B\"</code></p>"},{"location":"cordova/DeviceInfo/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"iOS\"</code> or <code>\"Android\"</code>.</p>"},{"location":"cordova/DeviceInfo/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>OS version string.</p> <p>Examples: <code>\"18.1\"</code>, <code>\"14\"</code>, <code>\"14.1.1\"</code></p>"},{"location":"cordova/DeviceSettings/","title":"DeviceSettings Android only","text":"<pre><code>export interface DeviceSettings {\n</code></pre> <p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>const isIgnoring = await BackgroundGeolocation.deviceSettings.isIgnoringBatteryOptimizations();\nif (!isIgnoring) {\n  const req = await BackgroundGeolocation.deviceSettings.showIgnoreBatteryOptimizations();\n  if (!req.seen) {\n    const confirmed = await showMyConfirmDialog({\n      title: 'Settings request',\n      text: 'Please disable battery optimizations for this app'\n    });\n    if (confirmed) {\n      await BackgroundGeolocation.deviceSettings.show(req);\n    }\n  }\n}\n</code></pre>"},{"location":"cordova/DeviceSettings/#members","title":"Members","text":""},{"location":"cordova/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>isIgnoringBatteryOptimizations(): Promise&lt;boolean&gt;;\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p>"},{"location":"cordova/DeviceSettings/#show","title":"show","text":"<p><code>show(request:</code><code>DeviceSettingsRequest</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>const req = await BackgroundGeolocation.deviceSettings.showPowerManager();\nif (!req.seen) {\n  await BackgroundGeolocation.deviceSettings.show(req);\n}\n</code></pre>"},{"location":"cordova/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<p><code>showIgnoreBatteryOptimizations(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p>"},{"location":"cordova/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<p><code>showPowerManager(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p>"},{"location":"cordova/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<pre><code>export interface DeviceSettingsRequest {\n</code></pre> <p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"cordova/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"cordova/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>action: string;\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"cordova/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>lastSeenAt: Date;\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"cordova/DeviceSettingsRequest/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer (e.g. <code>\"Huawei\"</code>, <code>\"Samsung\"</code>).</p>"},{"location":"cordova/DeviceSettingsRequest/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model name (e.g. <code>\"P40\"</code>, <code>\"SM-G991B\"</code>).</p>"},{"location":"cordova/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>seen: boolean;\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"cordova/DeviceSettingsRequest/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>Android OS version string.</p>"},{"location":"cordova/Event/","title":"Event","text":"<pre><code>export const Event = {\n</code></pre> <p>Supported event names emitted by the SDK.</p>"},{"location":"cordova/Event/#members","title":"Members","text":""},{"location":"cordova/Event/#activitychange","title":"ActivityChange","text":"<p>Emitted when a motion-activity change is detected.</p>"},{"location":"cordova/Event/#authorization","title":"Authorization","text":"<p>Emitted when the plugin's authorization state changes.</p>"},{"location":"cordova/Event/#boot","title":"Boot","text":"<p>Emitted when the plugin boots.</p>"},{"location":"cordova/Event/#connectivitychange","title":"ConnectivityChange","text":"<p>Emitted when the device's network connectivity changes.</p>"},{"location":"cordova/Event/#enabledchange","title":"EnabledChange","text":"<p>Emitted when the plugin's enabled-state changes.</p>"},{"location":"cordova/Event/#geofence","title":"Geofence","text":"<p>Emitted when a geofence transition occurs.</p>"},{"location":"cordova/Event/#geofenceschange","title":"GeofencesChange","text":"<p>Emitted when geofences are added/removed/activated/deactivated.</p>"},{"location":"cordova/Event/#heartbeat","title":"Heartbeat","text":"<p>Emitted at the configured <code>Config.heartbeatInterval</code> when the plugin is running.</p>"},{"location":"cordova/Event/#http","title":"Http","text":"<p>Emitted when an HTTP request is sent to the configured <code>Config.http.url</code> endpoint.</p>"},{"location":"cordova/Event/#location","title":"Location","text":"<p>Emitted when a location is recorded.</p>"},{"location":"cordova/Event/#motionchange","title":"MotionChange","text":"<p>Emitted when a motion-change is detected.</p>"},{"location":"cordova/Event/#notification","title":"Notification","text":"<p>Emitted when a user interacts with a plugin-generated notification.</p>"},{"location":"cordova/Event/#powersavechange","title":"PowerSaveChange","text":"<p>Emitted when the device enters or exits power-saving mode.</p>"},{"location":"cordova/Event/#providerchange","title":"ProviderChange","text":"<p>Emitted when the device location-provider changes state.</p>"},{"location":"cordova/Event/#schedule","title":"Schedule","text":"<p>Emitted when a configured schedule is triggered.</p>"},{"location":"cordova/Event/#terminate","title":"Terminate","text":"<p>Emitted when the plugin is terminated.</p>"},{"location":"cordova/GeoConfig/","title":"GeoConfig","text":"<pre><code>export interface GeoConfig {\n</code></pre> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"cordova/GeoConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"cordova/GeoConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BackgroundGeolocation.ready or BackgroundGeolocation.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    locationAuthorizationRequest: \"Always\"\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"cordova/GeoConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"cordova/GeoConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BackgroundGeolocation.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"cordova/GeoConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BackgroundGeolocation.onProviderChange.</p>"},{"location":"cordova/GeoConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"cordova/GeoConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilter for full documentation.</p>"},{"location":"cordova/GeoConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/GeoConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/GeoConfig/#members","title":"Members","text":""},{"location":"cordova/GeoConfig/#activitytype","title":"activityType","text":"<p><code>activityType?:</code><code>ActivityType</code><code>;</code></p> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on ActivityType.</p> Name ActivityType.Other ActivityType.AutomotiveNavigation ActivityType.Fitness ActivityType.OtherNavigation ActivityType.Airborne <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    activityType: ActivityType.Other,\n  },\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>allowIdenticalLocations?: boolean;\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"cordova/GeoConfig/#defertime","title":"deferTime","text":"<pre><code>deferTime?: number;\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"cordova/GeoConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>disableElasticity?: boolean;\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"cordova/GeoConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>disableLocationAuthorizationAlert?: boolean;\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BackgroundGeolocation.onProviderChange.</p>"},{"location":"cordova/GeoConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"cordova/GeoConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BackgroundGeolocation.onProviderChange - BackgroundGeolocation.start - BackgroundGeolocation.requestPermission</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange] \", event);\n\n  if (!provider.enabled) {\n    alert(\"Please enable location services\");\n  }\n});\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    disableLocationAuthorizationAlert: true\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity\u2013based stop-detection system.</p> <p>When <code>true</code>, the SDK ignores platform motion-activity signals when determining whether the device is stationary. This affects how and when location services are automatically turned off on both platforms.</p>"},{"location":"cordova/GeoConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. iOS location services then turn off automatically after exactly 15 minutes of no motion \u2014 you lose control over <code>stopTimeout</code>.</p> <p>To prevent iOS from ever automatically disabling location services, also set</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    disableStopDetection: true,\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With <code>disableStopDetection: true</code> and <code>pausesLocationUpdatesAutomatically: false</code>, iOS location services never turn off. This can severely drain the battery. Only use this configuration if you fully control tracking manually (for example, a workout app toggling tracking via BackgroundGeolocation.changePace).</p> <p>iOS stop-detection timing:</p> <p></p>"},{"location":"cordova/GeoConfig/#android_1","title":"Android","text":"<p>When <code>true</code>, Android location services never turn off automatically. You must disable tracking manually by calling BackgroundGeolocation.changePace with <code>false</code>, or BackgroundGeolocation.stop.</p>"},{"location":"cordova/GeoConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>distanceFilter?: number;\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"cordova/GeoConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"cordova/GeoConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"cordova/GeoConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>elasticityMultiplier?: number;\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"cordova/GeoConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>enableTimestampMeta?: boolean;\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS Location.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"cordova/GeoConfig/#android_2","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"cordova/GeoConfig/#ios_2","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"cordova/GeoConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>fastestLocationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    fastestLocationUpdateInterval: 5000 // Receive passive updates at most every 5 s\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"cordova/GeoConfig/#filter","title":"filter","text":"<p><code>filter?:</code><code>LocationFilter</code><code>;</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"cordova/GeoConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilter.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilter.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilter.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilter.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilter.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilter.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilter.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilter.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilter.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilter.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilter.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilter.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>geofenceInitialTriggerEntry?: boolean;\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"cordova/GeoConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>geofenceModeHighAccuracy?: boolean;\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BackgroundGeolocation.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeoConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeoConfig.locationUpdateInterval - GeoConfig.distanceFilter - GeoConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 50,\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>geofenceProximityRadius?: number;\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p>"},{"location":"cordova/GeoConfig/#ios_3","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"cordova/GeoConfig/#android_3","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"cordova/GeoConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>locationAuthorizationAlert?: Record&lt;string, any&gt;;\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationAlert: {\n      titleWhenNotEnabled: \"Location services are not enabled\",\n      titleWhenOff: \"Location services are OFF\",\n      instructions:\n        \"Please enable 'Always' in Location Services to allow background tracking.\",\n      cancelButton: \"Cancel\",\n      settingsButton: \"Open Settings\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<pre><code>locationAuthorizationRequest?: \"Always\" | \"WhenInUse\" | \"Any\";\n</code></pre> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"cordova/GeoConfig/#ios_4","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Start with When-In-Use\nBackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"WhenInUse\" }\n});\n\nasync function onClickStartTracking() {\n  await BackgroundGeolocation.start();\n\n  // Upgrade to Always at any time.\n  BackgroundGeolocation.setConfig({\n    geolocation: { locationAuthorizationRequest: \"Always\" }\n  });\n}\n</code></pre>"},{"location":"cordova/GeoConfig/#android_4","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"Always\" },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow access to this device's location in the background?\",\n      message: \"To track your trips, please enable 'Allow all the time' location permission.\",\n      positiveAction: \"Change to Allow all the time\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"cordova/GeoConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>locationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    distanceFilter: 0,           // Required \u2014 otherwise this field is ignored.\n    locationUpdateInterval: 5000 // Request a fix every ~5 seconds\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"cordova/GeoConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>pausesLocationUpdatesAutomatically?: boolean;\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BackgroundGeolocation.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"cordova/GeoConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>showsBackgroundLocationIndicator?: boolean;\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"cordova/GeoConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>stationaryRadius?: number;\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"cordova/GeoConfig/#ios_5","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"cordova/GeoConfig/#android_5","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"cordova/GeoConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>stopAfterElapsedMinutes?: number;\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BackgroundGeolocation.stop after this many minutes have elapsed since BackgroundGeolocation.start was called.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopAfterElapsedMinutes: 30\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.start();  // SDK will automatically stop in 30 minutes\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the <code>stopTimeout</code> elapses.</p> <p>When <code>true</code>, the SDK stops itself the next time BackgroundGeolocation.onMotionChange fires into the stationary state after the <code>stopTimeout</code> timer elapses.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the SDK transitions to the stationary state due to <code>stopTimeout</code> expiry. It does not fire when you manually call BackgroundGeolocation.changePace with <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopOnStationary: true,\n  },\n  app: {\n    isMoving: true,\n  }\n}, (state) =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"cordova/GeoConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>stopTimeout?: number;\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BackgroundGeolocation.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"cordova/GeoConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>useSignificantChangesOnly?: boolean;\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"cordova/GeoConfig/#ios_6","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"cordova/GeoConfig/#android_6","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"cordova/Geofence/","title":"Geofence","text":"<pre><code>export interface Geofence {\n</code></pre> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"cordova/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"cordova/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeoConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"cordova/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BackgroundGeolocation.addGeofence for a single geofence, or BackgroundGeolocation.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"cordova/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BackgroundGeolocation.onGeofence. Subscribe to changes in the actively monitored set with BackgroundGeolocation.onGeofencesChange.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event.identifier, event.action);\n});\n\nBackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  on.forEach((geofence) =&gt; createGeofenceMarker(geofence));\n  off.forEach((identifier) =&gt; removeGeofenceMarker(identifier));\n});\n</code></pre> <p>Note</p> <p>When all geofences are removed, BackgroundGeolocation.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"cordova/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"cordova/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeoConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeoConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"cordova/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre> <pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"cordova/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BackgroundGeolocation.startGeofences instead of BackgroundGeolocation.start to monitor geofences without continuous location tracking. Use GeoConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/Geofence/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre>"},{"location":"cordova/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"cordova/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"cordova/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre>"},{"location":"cordova/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"cordova/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"cordova/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"cordova/Geofence/#members","title":"Members","text":""},{"location":"cordova/Geofence/#entrystate","title":"entryState","text":"<p><code>entry</code><code>State</code><code>?: number;</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"cordova/Geofence/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, unknown&gt;;\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"cordova/Geofence/#hits","title":"hits","text":"<pre><code>hits?: number;\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"cordova/Geofence/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"cordova/Geofence/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"cordova/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>loiteringDelay?: number;\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"cordova/Geofence/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"cordova/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>notifyOnDwell?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"cordova/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>notifyOnEntry?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeoConfig.geofenceInitialTriggerEntry</p>"},{"location":"cordova/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>notifyOnExit?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"cordova/Geofence/#radius","title":"radius","text":"<pre><code>radius: number;\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"cordova/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>stateUpdatedAt?: number;\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"cordova/Geofence/#vertices","title":"vertices","text":"<p><code>vertices?:</code><code>Vertices</code><code>;</code></p> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre> <p> </p>"},{"location":"cordova/GeofenceAction/","title":"GeofenceAction","text":"<pre><code>export const GeofenceAction = {\n</code></pre> <p>Geofence transition actions emitted by the SDK</p> <p>Used with GeofenceEvent from BackgroundGeolocation.onGeofence.</p> Action Meaning ENTER Device entered the geofence EXIT Device exited the geofence DWELL Device dwelled inside"},{"location":"cordova/GeofenceAction/#members","title":"Members","text":""},{"location":"cordova/GeofenceAction/#dwell","title":"Dwell","text":"<pre><code>Dwell: 'DWELL'\n</code></pre> <p>Device dwelled inside the geofence for at least the configured <code>Config.geofenceProximityRadius</code>.</p>"},{"location":"cordova/GeofenceAction/#enter","title":"Enter","text":"<pre><code>Enter: 'ENTER',\n</code></pre> <p>Device entered the geofence.</p>"},{"location":"cordova/GeofenceAction/#exit","title":"Exit","text":"<pre><code>Exit: 'EXIT',\n</code></pre> <p>Device exited the geofence.</p>"},{"location":"cordova/GeofenceEvent/","title":"GeofenceEvent","text":"<pre><code>export interface GeofenceEvent {\n</code></pre> <p>Geofence transition delivered to BackgroundGeolocation.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\n    \"[onGeofence]\",\n    event.identifier,\n    event.action,\n    event.location\n  );\n});\n</code></pre>"},{"location":"cordova/GeofenceEvent/#members","title":"Members","text":""},{"location":"cordova/GeofenceEvent/#action","title":"action","text":"<p><code>action:</code><code>GeofenceAction</code><code>;</code></p> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"cordova/GeofenceEvent/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"cordova/GeofenceEvent/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>Geofence</code><code>;</code></p> <p>The complete Geofence record that triggered this event.</p>"},{"location":"cordova/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"cordova/GeofenceEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"cordova/GeofenceEvent/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string;\n</code></pre> <p>ISO-8601 UTC timestamp from the device system clock at the moment the OS delivered the geofence event.</p>"},{"location":"cordova/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<pre><code>export interface GeofencesChangeEvent {\n</code></pre> <p>Geofence monitoring change delivered to BackgroundGeolocation.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  for (const geofence of event.on) {\n    console.log(\"[onGeofencesChange] Activated:\", geofence.identifier);\n  }\n  for (const identifier of event.off) {\n    console.log(\"[onGeofencesChange] Deactivated:\", identifier);\n  }\n});\n</code></pre>"},{"location":"cordova/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"cordova/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>off: string[];\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"cordova/GeofencesChangeEvent/#on","title":"on","text":"<p><code>on:</code><code>Geofence</code><code>[];</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"cordova/HeadlessEvent/","title":"HeadlessEvent Android only","text":"<pre><code>export interface HeadlessEvent {\n</code></pre> <p>Event delivered to a headless task registered via BackgroundGeolocation.registerHeadlessTask.</p> <p>Headless tasks allow the app to handle SDK events while running in the background without a UI \u2014 including after the app has been terminated by the user. Each task invocation receives a <code>HeadlessEvent</code> describing which SDK event occurred and carrying the associated payload.</p> <pre><code>const headlessTask = async (event: HeadlessEvent) =&gt; {\n  const { name, params } = event;\n  console.log(\"[HeadlessTask]\", name, params);\n\n  if (name === \"location\") {\n    const location = params as Location;\n    console.log(\"[HeadlessTask] location:\", location);\n  } else if (name === \"http\") {\n    const httpEvent = params as HttpEvent;\n    console.log(\"[HeadlessTask] http status:\", httpEvent.status);\n  }\n};\n\nBackgroundGeolocation.registerHeadlessTask(headlessTask);\n</code></pre>"},{"location":"cordova/HeadlessEvent/#members","title":"Members","text":""},{"location":"cordova/HeadlessEvent/#name","title":"name","text":"<p><code>name:</code><code>Event</code><code>;</code></p> <p>The SDK event that triggered this headless task invocation (e.g. <code>location</code>, <code>http</code>, <code>geofence</code>, <code>heartbeat</code>, <code>terminate</code>).</p>"},{"location":"cordova/HeadlessEvent/#params","title":"params","text":"<pre><code>params: Record&lt;string, any&gt;;\n</code></pre> <p>Event payload corresponding to <code>name</code>. Cast this to the appropriate event type based on the value of <code>name</code> (e.g. Location for <code>location</code>, HttpEvent for <code>http</code>).</p>"},{"location":"cordova/HeartbeatEvent/","title":"HeartbeatEvent","text":"<pre><code>export interface HeartbeatEvent {\n</code></pre> <p>Periodic heartbeat delivered to BackgroundGeolocation.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>BackgroundGeolocation.onHeartbeat(async (event) =&gt; {\n  console.log(\"[onHeartbeat]\", event.location);\n\n  // Request a fresh location fix if needed.\n  const location = await BackgroundGeolocation.getCurrentPosition({\n    samples: 1\n  });\n  console.log(\"[onHeartbeat] fresh position:\", location);\n});\n</code></pre>"},{"location":"cordova/HeartbeatEvent/#members","title":"Members","text":""},{"location":"cordova/HeartbeatEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BackgroundGeolocation.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"cordova/HttpConfig/","title":"HttpConfig","text":"<pre><code>export interface HttpConfig {\n</code></pre> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"cordova/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"cordova/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    params: { user_id: 1234 }\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BackgroundGeolocation.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BackgroundGeolocation.getCount or fetch records with BackgroundGeolocation.getLocations.</p>"},{"location":"cordova/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"cordova/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"cordova/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BackgroundGeolocation.onConnectivityChange fires - BackgroundGeolocation.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p> <pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"cordova/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BackgroundGeolocation.start <code>\"stop\"</code> \u2014 BackgroundGeolocation.stop <code>\"startGeofences\"</code> \u2014 BackgroundGeolocation.startGeofences <code>\"changePace\"</code> <code>boolean</code> BackgroundGeolocation.changePace <code>\"setConfig\"</code> <code>{Config}</code> BackgroundGeolocation.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BackgroundGeolocation.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BackgroundGeolocation.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BackgroundGeolocation.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"cordova/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p>"},{"location":"cordova/HttpConfig/#the-sdk-log-provides-a-trace-of-the-full-http-lifecycle","title":"The SDK log provides a trace of the full HTTP lifecycle","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"cordova/HttpConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"cordova/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"cordova/HttpConfig/#http-lifecycle-log","title":"HTTP lifecycle log","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre>"},{"location":"cordova/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer ...\" }\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"cordova/HttpConfig/#members","title":"Members","text":""},{"location":"cordova/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>autoSync?: boolean;\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BackgroundGeolocation.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BackgroundGeolocation.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"cordova/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>autoSyncThreshold?: number;\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BackgroundGeolocation.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"cordova/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>batchSync?: boolean;\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"cordova/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>disableAutoSyncOnCellular?: boolean;\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BackgroundGeolocation.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BackgroundGeolocation.sync</p>"},{"location":"cordova/HttpConfig/#headers","title":"headers","text":"<pre><code>headers?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my.server.com\",\n    headers: {\n      Authorization: \"Bearer &lt;a secret key&gt;\",\n      \"X-FOO\": \"BAR\"\n    }\n  }\n});\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"cordova/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>maxBatchSize?: number;\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"cordova/HttpConfig/#method","title":"method","text":"<p><code>method?:</code><code>HttpMethod</code><code>;</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    method: \"PUT\"\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#params","title":"params","text":"<pre><code>params?: Record&lt;string, any&gt;;\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      user_id: 1234,\n      device_id: \"abc123\"\n    }\n  }\n});\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"cordova/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>rootProperty?: string;\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    rootProperty: \"myData\",\n    url: \"https://my.server.com\",\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"cordova/HttpConfig/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BackgroundGeolocation.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>BackgroundGeolocation.onHttp((response) =&gt; {\n  if (!response.success) {\n    console.log(\"[onHttp] FAILURE:\", response);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    timeout: 3000\n  }\n});\n</code></pre>"},{"location":"cordova/HttpConfig/#url","title":"url","text":"<pre><code>url?: string;\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  console.log(\"[onHttp]\", event);\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: { user_id: 1234 },\n    headers: { Authorization: \"Basic my-secret-key\" },\n    autoSync: true,\n    method: \"POST\"\n  }\n});\n</code></pre>"},{"location":"cordova/HttpEvent/","title":"HttpEvent","text":"<pre><code>export interface HttpEvent {\n</code></pre> <p>HTTP response payload delivered to BackgroundGeolocation.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onHttp] Upload succeeded:\", event.status);\n  } else {\n    console.warn(\"[onHttp] Upload failed:\", event.status, event.responseText);\n  }\n});\n</code></pre>"},{"location":"cordova/HttpEvent/#members","title":"Members","text":""},{"location":"cordova/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>responseText: string;\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"cordova/HttpEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"cordova/HttpEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"cordova/HttpMethod/","title":"HttpMethod","text":"<pre><code>export const HttpMethod = {\n</code></pre> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"cordova/HttpMethod/#members","title":"Members","text":""},{"location":"cordova/HttpMethod/#patch","title":"Patch","text":"<p>HTTP PATCH method.</p>"},{"location":"cordova/HttpMethod/#post","title":"Post","text":"<p>HTTP POST method.</p>"},{"location":"cordova/HttpMethod/#put","title":"Put","text":"<p>HTTP PUT method.</p>"},{"location":"cordova/KalmanProfile/","title":"KalmanProfile","text":"<pre><code>export const KalmanProfile = {\n</code></pre> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"cordova/KalmanProfile/#members","title":"Members","text":""},{"location":"cordova/KalmanProfile/#aggressive","title":"Aggressive","text":"<p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"cordova/KalmanProfile/#conservative","title":"Conservative","text":"<p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"cordova/KalmanProfile/#default","title":"Default","text":"<p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"cordova/Location/","title":"Location","text":"<pre><code>export interface Location {\n</code></pre> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"cordova/Location/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"cordova/Location/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"cordova/Location/#javascript-schema","title":"JavaScript schema","text":"<pre><code>{\n   \"timestamp\":     [Date],     // &lt;-- Javascript Date instance\n   \"event\":         [String],   // &lt;-- motionchange|geofence|heartbeat\n   \"is_moving\":     [Boolean],  // &lt;-- The motion-state when location was recorded.\n   \"uuid\":          [String],   // &lt;-- Universally unique identifier\n   \"age\":           [Integer],  // &lt;-- Age of the location in milliseconds\n   \"coords\": {\n       \"latitude\":  [Double],\n       \"longitude\": [Double],\n       \"accuracy\":  [Double],\n       \"speed\":     [Double],\n       \"heading\":   [Double],\n       \"altitude\":  [Double]\n       \"ellipsoidal_altitude\":  [Double]\n   },\n   \"activity\": {\n       \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle],\n       \"confidence\": [0-100%]\n   },\n   \"battery\": {\n       \"level\": [Double],\n       \"is_charging\": [Boolean]\n   },\n   \"odometer\": [Double/meters]\n}\n</code></pre>"},{"location":"cordova/Location/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>{\n    \"location\": {\n        \"coords\": {\n            \"latitude\":   [Double],\n            \"longitude\":  [Double],\n            \"accuracy\":   [Double],\n            \"speed\":      [Double],\n            \"heading\":    [Double],\n            \"altitude\":   [Double],\n            \"ellipsoidal_altitude\": [Double]\n        },\n        \"extras\": {   // &lt;-- optional meta-data\n            \"foo\": \"bar\"\n        },\n        \"activity\": {\n            \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle|unknown],\n            \"confidence\": [0-100%]\n        },\n        \"geofence\": {  // &lt;-- Present only if a geofence was triggered at this location\n            \"identifier\": [String],\n            \"action\": [String ENTER|EXIT]\n        },\n        \"battery\": {\n            \"level\": [Double],\n            \"is_charging\": [Boolean]\n        },\n        \"timestamp\": [ISO-8601 UTC], // eg:  \"2015-05-05T04:31:54.123Z\"\n        \"age\":       [Integer],      // &lt;-- Age of the location in milliseconds\n        \"uuid\":      [String],       // &lt;-- Universally unique identifier\n        \"event\"      [String],       // &lt;-- motionchange|geofence|heartbeat\n        \"is_moving\": [Boolean],      // &lt;-- The motion-state when recorded.\n        \"odometer\": [Double/meters]\n    }\n }\n</code></pre>"},{"location":"cordova/Location/#members","title":"Members","text":""},{"location":"cordova/Location/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivity</code><code>;</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"cordova/Location/#age","title":"age","text":"<pre><code>age: number;\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"cordova/Location/#battery","title":"battery","text":"<p><code>battery:</code><code>Battery</code><code>;</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"cordova/Location/#coords","title":"coords","text":"<p><code>coords:</code><code>Coords</code><code>;</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"cordova/Location/#event","title":"event","text":"<pre><code>event?: string;\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"cordova/Location/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"cordova/Location/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>GeofenceEvent</code><code>;</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"cordova/Location/#is_moving","title":"is_moving","text":"<pre><code>is_moving: boolean;\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"cordova/Location/#mock","title":"mock","text":"<pre><code>mock?: boolean;\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"cordova/Location/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"cordova/Location/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilter evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilter.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via Location.odometer_error.</li> </ul>"},{"location":"cordova/Location/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"cordova/Location/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilter - LocationFilter.odometerAccuracyThreshold - BackgroundGeolocation.resetOdometer - BackgroundGeolocation.getOdometer</p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Distance traveled:\", location.odometer);\n});\n\n// Reset at the start of a trip\nawait BackgroundGeolocation.resetOdometer();\n</code></pre>"},{"location":"cordova/Location/#odometer_error","title":"odometer_error","text":"<pre><code>odometer_error: number;\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"cordova/Location/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Odometer:\", location.odometer);\n  if (location.odometer_error &gt; 50) {\n    console.warn(\"High odometer drift \u2014 signal quality degraded\");\n  }\n});\n</code></pre>"},{"location":"cordova/Location/#provider","title":"provider","text":"<p><code>provider?:</code><code>ProviderChangeEvent</code><code>;</code></p> <p>Location-services provider state at the time this location was recorded.</p> <p>Present only when <code>event</code> is <code>\"providerchange\"</code>.</p>"},{"location":"cordova/Location/#recorded_at","title":"recorded_at","text":"<pre><code>recorded_at: string | number;\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"cordova/Location/#sample","title":"sample","text":"<pre><code>sample?: boolean;\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BackgroundGeolocation.getCurrentPosition to find the most accurate fix. These samples are delivered to BackgroundGeolocation.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"cordova/Location/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string | number;\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"cordova/Location/#uuid","title":"uuid","text":"<pre><code>uuid: string;\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"cordova/LocationError/","title":"LocationError","text":"<pre><code>export const LocationError = {\n</code></pre> <p>Error codes returned when the native location API fails to fetch a location.</p> Code Error <code>0</code> Location unknown <code>1</code> Location permission denied <code>2</code> Network error <code>3</code> Attempt to initiate location-services in background with WhenInUse authorization <code>408</code> Location timeout <code>499</code> Location request cancelled"},{"location":"cordova/LocationError/#members","title":"Members","text":""},{"location":"cordova/LocationError/#backgroundwheninuse","title":"BackgroundWhenInUse","text":"<pre><code>BackgroundWhenInUse: 3,\n</code></pre> <p>Attempt to initiate location-services in background with WhenInUse authorization.</p>"},{"location":"cordova/LocationError/#cancelled","title":"Cancelled","text":"<pre><code>Cancelled: 499,\n</code></pre> <p>Location request was cancelled.</p>"},{"location":"cordova/LocationError/#locationunknown","title":"LocationUnknown","text":"<pre><code>LocationUnknown: 0,\n</code></pre> <p>Location could not be determined at this time.</p>"},{"location":"cordova/LocationError/#networkerror","title":"NetworkError","text":"<pre><code>NetworkError: 2,\n</code></pre> <p>Network error occurred while attempting to fetch location.</p>"},{"location":"cordova/LocationError/#permissiondenied","title":"PermissionDenied","text":"<pre><code>PermissionDenied: 1,\n</code></pre> <p>Location permission denied by the user.</p>"},{"location":"cordova/LocationError/#timeout","title":"Timeout","text":"<pre><code>Timeout: 408,\n</code></pre> <p>Location request timed out.</p>"},{"location":"cordova/LocationFilter/","title":"LocationFilter","text":"<pre><code>export interface LocationFilter {\n</code></pre> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"cordova/LocationFilter/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"cordova/LocationFilter/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#members","title":"Members","text":""},{"location":"cordova/LocationFilter/#burstwindow","title":"burstWindow","text":"<pre><code>burstWindow?: number;\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"cordova/LocationFilter/#filterdebug","title":"filterDebug","text":"<pre><code>filterDebug?: boolean;\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"cordova/LocationFilter/#kalmandebug","title":"kalmanDebug","text":"<pre><code>kalmanDebug?: boolean;\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"cordova/LocationFilter/#kalmanprofile","title":"kalmanProfile","text":"<p><code>kalmanProfile?:</code><code>KalmanProfile</code><code>;</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"cordova/LocationFilter/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>maxBurstDistance?: number;\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"cordova/LocationFilter/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>maxImpliedSpeed?: number;\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"cordova/LocationFilter/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>odometerAccuracyThreshold?: number;\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"cordova/LocationFilter/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>odometerUseKalmanFilter?: boolean;\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilter.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilter.useKalman and LocationFilter.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"cordova/LocationFilter/#policy","title":"policy","text":"<p><code>policy?:</code><code>LocationFilterPolicy</code><code>;</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BackgroundGeolocation.onLocation. For more granular tuning, see LocationFilter.trackingAccuracyThreshold, LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</p> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilter/#rollingwindow","title":"rollingWindow","text":"<pre><code>rollingWindow?: number;\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"cordova/LocationFilter/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>trackingAccuracyThreshold?: number;\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"cordova/LocationFilter/#usekalman","title":"useKalman","text":"<pre><code>useKalman?: boolean;\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"cordova/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<pre><code>export const LocationFilterPolicy = {\n</code></pre> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilter.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BackgroundGeolocation.onLocation.</li> <li>For more granular tuning, see LocationFilter.trackingAccuracyThreshold,   LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</li> </ul> <p>Examples</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p>"},{"location":"cordova/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"cordova/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilter.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"cordova/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"cordova/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"cordova/LocationRequest/","title":"LocationRequest","text":"<pre><code>export const LocationRequest = {\n</code></pre> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeoConfig.locationAuthorizationRequest</p>"},{"location":"cordova/LocationRequest/#members","title":"Members","text":""},{"location":"cordova/LocationRequest/#always","title":"Always","text":"<p>Request full background + foreground authorization.</p>"},{"location":"cordova/LocationRequest/#any","title":"Any","text":"<p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"cordova/LocationRequest/#wheninuse","title":"WhenInUse","text":"<p>Request foreground-only authorization.</p>"},{"location":"cordova/LogLevel/","title":"LogLevel","text":"<pre><code>export const LogLevel = {\n</code></pre> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"cordova/LogLevel/#members","title":"Members","text":""},{"location":"cordova/LogLevel/#debug","title":"Debug","text":"<p>Developer-level debug output.</p>"},{"location":"cordova/LogLevel/#error","title":"Error","text":"<p>Log only critical failures.</p>"},{"location":"cordova/LogLevel/#info","title":"Info","text":"<p>Operational information.</p>"},{"location":"cordova/LogLevel/#off","title":"Off","text":"<p>Disable all logging.</p>"},{"location":"cordova/LogLevel/#verbose","title":"Verbose","text":"<p>Maximum detail.</p>"},{"location":"cordova/LogLevel/#warning","title":"Warning","text":"<p>Log warnings + errors.</p>"},{"location":"cordova/Logger/","title":"Logger","text":"<pre><code>export interface Logger {\n</code></pre> <p>SDK logging API \u2014 access via BackgroundGeolocation.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"cordova/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"cordova/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\n</code></pre>"},{"location":"cordova/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional SQLQuery to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"cordova/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>const Logger = BackgroundGeolocation.logger;\nBackgroundGeolocation.onLocation((location) =&gt; {\n  Logger.debug(\"Location received: \" + location.uuid);\n});\n</code></pre>"},{"location":"cordova/Logger/#examples","title":"Examples","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"cordova/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre>"},{"location":"cordova/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre>"},{"location":"cordova/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"cordova/Logger/#methods","title":"Methods","text":""},{"location":"cordova/Logger/#debug","title":"debug","text":"<pre><code>debug(message: string): void;\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.debug(\"This is a debug message\");\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"cordova/Logger/#destroylog","title":"destroyLog","text":"<pre><code>destroyLog(): Promise&lt;void&gt;;\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.destroyLog();\n</code></pre>"},{"location":"cordova/Logger/#emaillog","title":"emailLog","text":"<p><code>emailLog(email: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional SQLQuery to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"foo@bar.com\");\n\n// Constrain by date range\nawait Logger.emailLog(\"foo@bar.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC,\n  limit: 1000\n});\n</code></pre>"},{"location":"cordova/Logger/#error","title":"error","text":"<pre><code>error(message: string): void;\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.error(\"Something BAD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"cordova/Logger/#getlog","title":"getLog","text":"<p><code>getLog(query?:</code><code>SQLQuery</code><code>): Promise&lt;string&gt;;</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional SQLQuery to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Fetch all logs\nconst log = await BackgroundGeolocation.logger.getLog();\nconsole.log(\"[log]\", log);\n\n// Constrain by date range\nconst Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-09-19 11:12\"),\n  end:   Date.parse(\"2019-09-19 11:13\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre>"},{"location":"cordova/Logger/#info","title":"info","text":"<pre><code>info(message: string): void;\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.info(\"Something informative\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"cordova/Logger/#notice","title":"notice","text":"<pre><code>notice(message: string): void;\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.notice(\"A Notice\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"cordova/Logger/#uploadlog","title":"uploadLog","text":"<p><code>uploadLog(url: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional SQLQuery to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"cordova/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n\n// Constrain by date range\nawait BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\",\n  { start: Date.parse(\"2019-10-20 09:00\"), end: Date.parse(\"2019-10-20 11:59\") }\n);\n</code></pre>"},{"location":"cordova/Logger/#warn","title":"warn","text":"<pre><code>warn(message: string): void;\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.warn(\"Something WEIRD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"cordova/Logger/#properties","title":"Properties","text":""},{"location":"cordova/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>ORDER_ASC: 1;\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"cordova/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>ORDER_DESC: -1;\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"cordova/LoggerConfig/","title":"LoggerConfig","text":"<pre><code>export interface LoggerConfig {\n</code></pre> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"cordova/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"cordova/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"cordova/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"cordova/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"cordova/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"cordova/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"cordova/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"cordova/LoggerConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>Config({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#members","title":"Members","text":""},{"location":"cordova/LoggerConfig/#debug","title":"debug","text":"<pre><code>debug?: boolean;\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"cordova/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"cordova/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"cordova/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>logLevel?:</code><code>LogLevel</code><code>;</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  },\n});\n</code></pre>"},{"location":"cordova/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>logMaxDays?: number;\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logMaxDays: 3\n  }\n});\n</code></pre>"},{"location":"cordova/MotionActivity/","title":"MotionActivity","text":"<pre><code>export interface MotionActivity {\n</code></pre> <p>The motion activity reported by the device at the time a Location was recorded.</p>"},{"location":"cordova/MotionActivity/#members","title":"Members","text":""},{"location":"cordova/MotionActivity/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"cordova/MotionActivity/#type","title":"type","text":"<p><code>type:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"cordova/MotionActivityEvent/","title":"MotionActivityEvent","text":"<pre><code>export interface MotionActivityEvent {\n</code></pre> <p>Activity recognition result delivered to BackgroundGeolocation.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange]\", event.activity, event.confidence);\n});\n</code></pre>"},{"location":"cordova/MotionActivityEvent/#members","title":"Members","text":""},{"location":"cordova/MotionActivityEvent/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"cordova/MotionActivityEvent/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"cordova/MotionActivityType/","title":"MotionActivityType","text":"<pre><code>export const MotionActivityType = {\n</code></pre> <p>Reported device motion activity names (legacy RN + native).</p>"},{"location":"cordova/MotionActivityType/#members","title":"Members","text":""},{"location":"cordova/MotionActivityType/#invehicle","title":"InVehicle","text":"<pre><code>InVehicle: 'in_vehicle',\n</code></pre> <p>Device is in vehicle.</p>"},{"location":"cordova/MotionActivityType/#onbicycle","title":"OnBicycle","text":"<pre><code>OnBicycle: 'on_bicycle',\n</code></pre> <p>Device is on bicycle.</p>"},{"location":"cordova/MotionActivityType/#onfoot","title":"OnFoot","text":"<pre><code>OnFoot: 'on_foot',\n</code></pre> <p>Device is on foot (walking or running).</p>"},{"location":"cordova/MotionActivityType/#running","title":"Running","text":"<pre><code>Running: 'running',\n</code></pre> <p>Device is running.</p>"},{"location":"cordova/MotionActivityType/#still","title":"Still","text":"<pre><code>Still: 'still',\n</code></pre> <p>Device is stationary.</p>"},{"location":"cordova/MotionActivityType/#unknown","title":"Unknown","text":"<pre><code>Unknown: 'unknown'\n</code></pre> <p>Device activity is unknown.</p>"},{"location":"cordova/MotionActivityType/#walking","title":"Walking","text":"<pre><code>Walking: 'walking',\n</code></pre> <p>Device is walking.</p>"},{"location":"cordova/MotionChangeEvent/","title":"MotionChangeEvent","text":"<pre><code>export interface MotionChangeEvent {\n</code></pre> <p>Motion-state change delivered to BackgroundGeolocation.onMotionChange.</p> <p>The SDK fires this event each time it transitions between the moving and stationary states. A location is always recorded at the moment of transition.</p> <pre><code>BackgroundGeolocation.onMotionChange((event) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device is moving:\", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device is stationary:\", event.location);\n  }\n});\n</code></pre>"},{"location":"cordova/MotionChangeEvent/#members","title":"Members","text":""},{"location":"cordova/MotionChangeEvent/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK has transitioned to the moving state; <code>false</code> when it has transitioned to the stationary state.</p>"},{"location":"cordova/MotionChangeEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the motion-state transition was detected.</p>"},{"location":"cordova/NotificationConfig/","title":"NotificationConfig Android only","text":"<pre><code>export interface NotificationConfig {\n</code></pre> <p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"cordova/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"cordova/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The Title\",\n      text: \"The Text\"\n    }\n  }\n});\n\n// Update notification fields at runtime.\n// Only changed keys must be provided; existing values persist.\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      title: \"The New Title\",\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"cordova/NotificationConfig/#members","title":"Members","text":""},{"location":"cordova/NotificationConfig/#actions","title":"actions","text":"<pre><code>actions?: string[];\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"cordova/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>channelId?: string;\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"cordova/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>channelName?: string;\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      channelName: \"Location Tracker\"\n    }\n  }\n});\n\n// Update at runtime\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      channelName: \"My new channel name\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#color","title":"color","text":"<pre><code>color?: string;\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"cordova/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>largeIcon?: string;\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"drawable/my_notification_large_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"mipmap/my_notification_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#layout","title":"layout","text":"<pre><code>layout?: string;\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"cordova/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      layout: \"my_notification_layout\",\n      title: \"The Notification Title\",\n      text: \"The Notification Text\",\n      smallIcon: \"mipmap/my_small_icon\",\n      largeIcon: \"mipmap/my_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#priority","title":"priority","text":"<p><code>priority?:</code><code>NotificationPriority</code><code>;</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: NotificationPriority.High\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>smallIcon?: string;\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"drawable/my_notification_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"mipmap/my_notification_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>sticky?: boolean;\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"cordova/NotificationConfig/#strings","title":"strings","text":"<pre><code>strings?: Record&lt;string, string&gt;;\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/NotificationConfig/#text","title":"text","text":"<pre><code>text?: string;\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"cordova/NotificationConfig/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"cordova/NotificationPriority/","title":"NotificationPriority","text":"<pre><code>export const NotificationPriority = {\n</code></pre> <p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"cordova/NotificationPriority/#members","title":"Members","text":""},{"location":"cordova/NotificationPriority/#default","title":"Default","text":"<p>Default notification priority (normal weighting).</p>"},{"location":"cordova/NotificationPriority/#high","title":"High","text":"<p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"cordova/NotificationPriority/#low","title":"Low","text":"<p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"cordova/NotificationPriority/#max","title":"Max","text":"<p>Same as NotificationPriority.High.</p>"},{"location":"cordova/NotificationPriority/#min","title":"Min","text":"<p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"cordova/PermissionRationale/","title":"PermissionRationale Android only","text":"<pre><code>export interface PermissionRationale {\n</code></pre> <p>Configures the background location permission rationale dialog on Android 11 and later.</p> <p>Android 11 removed the Allow all the time option from the system location permission dialog. Apps may instead show a custom rationale dialog that explains why background access is needed and routes the user to the system Location Permissions screen to grant it explicitly.</p>"},{"location":"cordova/PermissionRationale/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Template tags</li> </ul>"},{"location":"cordova/PermissionRationale/#overview","title":"Overview","text":"<p>The SDK presents this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code> and AppConfig.backgroundPermissionRationale is configured.</p> <p>Selecting the dialog's positive action sends the user directly to the system Location Permissions screen, where they must enable Allow all the time themselves.</p> <ul> <li>The dialog is shown once only. After the user presses the positive action   it will not appear again. Pressing Cancel does not count as acceptance.</li> <li>If the user later resets your app's Location permission to Ask every time,   the dialog becomes eligible to be shown again.</li> </ul> <p> </p> <pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location access.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"cordova/PermissionRationale/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside any rationale field using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> \"Allow all the time\" (API 30+) Localized label for the background location permission option. <code>{applicationName}</code> App name Localized application name from <code>AndroidManifest</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Android 11 Location Updates</p>"},{"location":"cordova/PermissionRationale/#members","title":"Members","text":""},{"location":"cordova/PermissionRationale/#message","title":"message","text":"<pre><code>message?: string;\n</code></pre> <p>Body text of the dialog. Explain why your app needs background location access, similar in purpose to the iOS <code>NSLocationAlwaysAndWhenInUseUsageDescription</code>.</p>"},{"location":"cordova/PermissionRationale/#negativeaction","title":"negativeAction","text":"<pre><code>negativeAction?: string;\n</code></pre> <p>Text to display on the negative action button (e.g. \"Cancel\").</p>"},{"location":"cordova/PermissionRationale/#positiveaction","title":"positiveAction","text":"<pre><code>positiveAction?: string;\n</code></pre> <p>Text to display on the positive action button.</p>"},{"location":"cordova/PermissionRationale/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title for the Android background location permission dialog.</p>"},{"location":"cordova/PersistMode/","title":"PersistMode","text":"<pre><code>export const PersistMode = {\n</code></pre> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"cordova/PersistMode/#members","title":"Members","text":""},{"location":"cordova/PersistMode/#all","title":"All","text":"<p>Persist both location and geofence data.</p>"},{"location":"cordova/PersistMode/#geofence","title":"Geofence","text":"<p>Persist geofence data only.</p>"},{"location":"cordova/PersistMode/#location","title":"Location","text":"<p>Persist location data only.</p>"},{"location":"cordova/PersistMode/#none","title":"None","text":"<p>Do not persist any data.</p>"},{"location":"cordova/PersistenceConfig/","title":"PersistenceConfig","text":"<pre><code>export interface PersistenceConfig {\n</code></pre> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"cordova/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"cordova/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded Location and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 3,\n    maxRecordsToPersist: 1000,\n    extras: {\n      'user_id': 123,\n      'appVersion': '1.2.3'\n    },\n    persistMode: PersistMode.All\n  }\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BackgroundGeolocation.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BackgroundGeolocation.getCount or BackgroundGeolocation.getLocations.</p>"},{"location":"cordova/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BackgroundGeolocation.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"cordova/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"cordova/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  persist: true,\n  extras: { get_current_position: true },\n  samples: 3,\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>{\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    locationTemplate: '{...}',\n    geofenceTemplate: '{...}',\n    persistMode: PersistMode.all,\n    extras: {'user_id': 123},\n    disableProviderChangeRecord: true,\n  },\n};\n</code></pre>"},{"location":"cordova/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre>"},{"location":"cordova/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#members","title":"Members","text":""},{"location":"cordova/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>disableProviderChangeRecord?: boolean;\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BackgroundGeolocation.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    disableProviderChangeRecord: true\n }\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      device_id: \"abc123\"  // appended to root JSON of each POST request\n    }\n  },\n  persistence: {\n    extras: {\n      route_id: 1234       // merged onto each location record\n    }\n  }\n});\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"cordova/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>geofenceTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '{ \"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\" }'\n  }\n});\n\n// Or a compact Array form:\nBackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"cordova/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"cordova/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre>"},{"location":"cordova/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>locationTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping Location JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}'\n  }\n});\n\n// Or use a compact Array template:\nBackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n persistence: {\n  locationTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"cordova/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: 'https://my.server.com/locations',\n    rootProperty: 'data',\n  },\n  persistence: {\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}',\n    extras: { foo: \"bar\" }\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"cordova/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"cordova/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<pre><code>locationsOrderDirection?: \"ASC\" | \"DESC\";\n</code></pre> <p>Sort order used when selecting records for upload or returning them from BackgroundGeolocation.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"cordova/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>maxDaysToPersist?: number;\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"cordova/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>maxRecordsToPersist?: number;\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"cordova/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>persistMode?:</code><code>PersistMode</code><code>;</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BackgroundGeolocation.onLocation and BackgroundGeolocation.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BackgroundGeolocation.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"cordova/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>timestampFormat?: 'iso' | 'epoch';\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    timestampFormat: 'epoch',\n  },\n});\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"cordova/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<pre><code>export interface ProviderChangeEvent {\n</code></pre> <p>Location-services provider state delivered to BackgroundGeolocation.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]\",\n    \"enabled:\", event.enabled,\n    \"status:\", event.status,\n    \"gps:\", event.gps,\n    \"network:\", event.network,\n    \"accuracyAuthorization:\", event.accuracyAuthorization\n  );\n\n  if (!event.enabled) {\n    // Prompt the user to re-enable location services.\n  }\n});\n</code></pre>"},{"location":"cordova/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"cordova/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<pre><code>accuracyAuthorization: number;\n</code></pre> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"cordova/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"cordova/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>gps: boolean;\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"cordova/ProviderChangeEvent/#network","title":"network","text":"<pre><code>network: boolean;\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"cordova/ProviderChangeEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>Platform authorization status code. Use the AuthorizationStatus constants to interpret this value.</p>"},{"location":"cordova/SQLQuery/","title":"SQLQuery","text":"<pre><code>export interface SQLQuery {\n</code></pre> <p>Constrains a log query by date range, sort order, and record count.</p> <p>Pass to Logger.getLog, Logger.emailLog, or Logger.uploadLog to filter which log entries are included.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\n\n// Date range\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\")\n});\n\n// First 100 records ascending\nawait Logger.emailLog(\"foo@bar.com\", {\n  order: SQLQueryOrder.Asc,\n  limit: 100\n});\n\n// Upload a specific time window\nawait Logger.uploadLog(\"https://my.server.com/users/123/logs\", {\n  start: Date.parse(\"2019-10-20 09:00\"),\n  end:   Date.parse(\"2019-10-20 11:59\")\n});\n</code></pre>"},{"location":"cordova/SQLQuery/#members","title":"Members","text":""},{"location":"cordova/SQLQuery/#end","title":"end","text":"<pre><code>end?: number;\n</code></pre> <p>End of the query window (unix timestamp in milliseconds).</p>"},{"location":"cordova/SQLQuery/#limit","title":"limit","text":"<pre><code>limit?: number;\n</code></pre> <p>Maximum number of records to return.</p>"},{"location":"cordova/SQLQuery/#offset","title":"offset","text":"<pre><code>offset?: number;\n</code></pre> <p>Number of matching records to skip before returning results (for paging).</p>"},{"location":"cordova/SQLQuery/#order","title":"order","text":"<p><code>order?:</code><code>SQLQueryOrder</code><code>;</code></p> <p>Sort order for results: <code>SQLQueryOrder.Asc</code> (oldest first) or <code>SQLQueryOrder.Desc</code> (newest first).</p>"},{"location":"cordova/SQLQuery/#start","title":"start","text":"<pre><code>start?: number;\n</code></pre> <p>Start of the query window (unix timestamp in milliseconds).</p>"},{"location":"cordova/SQLQueryOrder/","title":"SQLQueryOrder","text":"<pre><code>export const SQLQueryOrder = {\n</code></pre> <p>Sort order for SQLQuery.order.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"cordova/SQLQueryOrder/#members","title":"Members","text":""},{"location":"cordova/SQLQueryOrder/#asc","title":"Asc","text":"<p>Ascending order (oldest to newest).</p>"},{"location":"cordova/SQLQueryOrder/#desc","title":"Desc","text":"<p>Descending order (newest to oldest).</p>"},{"location":"cordova/Sensors/","title":"Sensors","text":"<pre><code>export interface Sensors {\n</code></pre> <p>Availability of motion sensors on the device, as returned by BackgroundGeolocation.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(\"[Sensors]\", sensors);\n</code></pre>"},{"location":"cordova/Sensors/#members","title":"Members","text":""},{"location":"cordova/Sensors/#accelerometer","title":"accelerometer","text":"<pre><code>accelerometer: boolean;\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"cordova/Sensors/#gyroscope","title":"gyroscope","text":"<pre><code>gyroscope: boolean;\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"cordova/Sensors/#magnetometer","title":"magnetometer","text":"<pre><code>magnetometer: boolean;\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"cordova/Sensors/#motion_hardware","title":"motion_hardware","text":"<pre><code>motion_hardware?: boolean;\n</code></pre> <p>iOS only <code>true</code> when the device has a dedicated M-series motion co-processor (iPhone 5S and later). The M-series co-processor offloads motion activity recognition from the main CPU, enabling low-power step counting and activity detection.</p>"},{"location":"cordova/Sensors/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"ios\"</code> or <code>\"android\"</code>.</p>"},{"location":"cordova/Sensors/#significant_motion","title":"significant_motion","text":"<pre><code>significant_motion?: boolean;\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"cordova/State/","title":"State","text":"<p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] enabled:\", state.enabled, \"trackingMode:\", state.trackingMode);\n</code></pre>"},{"location":"cordova/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"cordova/State/#members","title":"Members","text":""},{"location":"cordova/State/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. See ActivityConfig.</p>"},{"location":"cordova/State/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. See AppConfig.</p>"},{"location":"cordova/State/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. See AuthorizationConfig.</p>"},{"location":"cordova/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>didDeviceReboot: boolean;\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"cordova/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>didLaunchInBackground: boolean;\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"cordova/State/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BackgroundGeolocation.start or BackgroundGeolocation.startGeofences has been called and not yet stopped.</p>"},{"location":"cordova/State/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. See GeoConfig.</p>"},{"location":"cordova/State/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. See HttpConfig.</p>"},{"location":"cordova/State/#isfirstboot","title":"isFirstBoot","text":"<p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"cordova/State/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// Toggle the SDK's motion state from stationary to moving.\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log('[onMotionChange] isMoving?', event.isMoving);\n});\n\nawait BackgroundGeolocation.changePace(true);\n// State.isMoving is now true.\n</code></pre>"},{"location":"cordova/State/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. See LoggerConfig.</p>"},{"location":"cordova/State/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BackgroundGeolocation.setOdometer - BackgroundGeolocation.getOdometer</p>"},{"location":"cordova/State/#odometererror","title":"odometerError","text":"<pre><code>odometerError: number;\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilter.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"cordova/State/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. See PersistenceConfig.</p>"},{"location":"cordova/State/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"cordova/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>schedulerEnabled: boolean;\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BackgroundGeolocation.startSchedule has been called. BackgroundGeolocation.stopSchedule sets this to <code>false</code>.</p>"},{"location":"cordova/State/#trackingmode","title":"trackingMode","text":"<p><code>trackingMode:</code><code>TrackingMode</code><code>;</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>await BackgroundGeolocation.start();\nlet state = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 1'\n\nawait BackgroundGeolocation.startGeofences();\nstate = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 0'\n</code></pre>"},{"location":"cordova/State/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"cordova/Subscription/","title":"Subscription","text":"<pre><code>export interface Subscription {\n</code></pre> <p>Handle returned by every <code>BackgroundGeolocation.on*</code> event-listener method.</p> <p><code>Subscription</code> exposes a single <code>remove</code> method used to stop listening to an event and free associated resources. Always call <code>remove()</code> when a listener is no longer needed to prevent memory leaks.</p>"},{"location":"cordova/Subscription/#removing-an-event-listener","title":"Removing an event-listener","text":"<pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\n// Later, when the listener is no longer needed:\nsubscription.remove();\n</code></pre>"},{"location":"cordova/Subscription/#managing-multiple-subscriptions","title":"Managing multiple subscriptions","text":"<p>Collect subscriptions in an array and remove them all at once \u2014 for example when a view is destroyed.</p> <pre><code>import BackgroundGeolocation, {\n  Location,\n  Subscription\n} from \"react-native-background-geolocation\";\n\nconst subscriptions: Subscription[] = [];\n\nfunction addListeners() {\n  subscriptions.push(\n    BackgroundGeolocation.onLocation((location: Location) =&gt; {\n      console.log(\"[onLocation]\", location);\n    }),\n    BackgroundGeolocation.onMotionChange((event) =&gt; {\n      console.log(\"[onMotionChange]\", event);\n    }),\n    BackgroundGeolocation.onEnabledChange((enabled: boolean) =&gt; {\n      console.log(\"[onEnabledChange]\", enabled);\n    })\n  );\n}\n\nfunction removeListeners() {\n  subscriptions.forEach((sub) =&gt; sub.remove());\n  subscriptions.length = 0;\n}\n</code></pre>"},{"location":"cordova/Subscription/#members","title":"Members","text":""},{"location":"cordova/Subscription/#remove","title":"remove","text":"<pre><code>remove(): void;\n</code></pre> <p>Removes the event-listener and frees its resources. Call this when the listener is no longer needed to prevent memory leaks.</p>"},{"location":"cordova/TrackingMode/","title":"TrackingMode","text":"<pre><code>export const TrackingMode = {\n</code></pre> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"cordova/TrackingMode/#members","title":"Members","text":""},{"location":"cordova/TrackingMode/#geofences","title":"Geofences","text":"<p>Monitor geofences only.</p>"},{"location":"cordova/TrackingMode/#location","title":"Location","text":"<p>Monitor location + geofences.</p>"},{"location":"cordova/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<pre><code>export interface TransistorAuthorizationService {\n</code></pre> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"cordova/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>const url = \"http://tracker.transistorsoft.com\";\nconst orgname = \"my-company-name\";\nconst username = \"my-username\";\n\n// Fetch a token from the server (the SDK caches it automatically).\nconst token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  orgname, username, url\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"cordova/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"cordova/TransistorAuthorizationService/#applyif","title":"applyIf","text":"<p><code>applyIf(config: T):</code><code>Config</code><code>;</code></p> <p>Mutate a Config to apply a <code>transistorAuthorizationToken</code> if present.</p> <p>The implementation reads <code>config.transistorAuthorizationToken</code>, removes that property, sets <code>config.http.url</code> to <code>\"&lt;token.url&gt;/api/locations\"</code>, and injects <code>config.authorization</code> with a JWT strategy derived from the token. If no <code>transistorAuthorizationToken</code> is found, the config is returned unchanged.</p> <pre><code>const token = await service.findOrCreate('my-org', 'user@example.com');\n\nconst config = service.applyIf({\n  ...myConfig,\n  transistorAuthorizationToken: token\n});\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"cordova/TransistorAuthorizationService/#destroy","title":"destroy","text":"<pre><code>destroy(url?: string): Promise&lt;void&gt;;\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"cordova/TransistorAuthorizationService/#findorcreate","title":"findOrCreate","text":"<p><code>findOrCreate(orgName: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\",\n  \"http://tracker.transistorsoft.com\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"cordova/TransistorAuthorizationToken/","title":"TransistorAuthorizationToken","text":"<pre><code>export interface TransistorAuthorizationToken {\n</code></pre> <p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"cordova/TransistorAuthorizationToken/#members","title":"Members","text":""},{"location":"cordova/TransistorAuthorizationToken/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"cordova/TransistorAuthorizationToken/#expires","title":"expires","text":"<pre><code>expires: number;\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"cordova/TransistorAuthorizationToken/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken: string;\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"cordova/TransistorAuthorizationToken/#url","title":"url","text":"<pre><code>url: string;\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"cordova/TriggerActivity/","title":"TriggerActivity","text":"<pre><code>export const TriggerActivity = {\n</code></pre> <p>Allowed motion activity names that can trigger motion detection. Mirrors Flutter's TriggerActivity set.</p>"},{"location":"cordova/Vertices/","title":"Vertices","text":"<pre><code>export type Vertices = [number, number][];\n</code></pre> <p>An array of <code>[latitude, longitude]</code> pairs defining a polygon geofence.</p> <p>See Geofence.vertices.</p>"},{"location":"cordova/WatchPositionRequest/","title":"WatchPositionRequest","text":"<pre><code>export interface WatchPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.watchPosition.</p> <p>Configures the interval, accuracy, persistence, and metadata for a continuous location stream. All fields are optional.</p> <p>Warning</p> <p><code>watchPosition</code> is intended for foreground use. On iOS it prevents the app from being suspended, which drains the battery. Remove the subscription when your app moves to the background.</p> <pre><code>const subscription = BackgroundGeolocation.watchPosition(\n  { interval: 1000, desiredAccuracy: DesiredAccuracy.High },\n  (location) =&gt; {\n    console.log(\"[watchPosition]\", location);\n  },\n  (errorCode) =&gt; {\n    console.warn(\"[watchPosition] error:\", errorCode);\n  }\n);\n\n// Later, stop watching.\nsubscription.remove();\n</code></pre>"},{"location":"cordova/WatchPositionRequest/#members","title":"Members","text":""},{"location":"cordova/WatchPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Target accuracy for location updates from the native API. Defaults to DesiredAccuracy.High.</p>"},{"location":"cordova/WatchPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to each location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"cordova/WatchPositionRequest/#interval","title":"interval","text":"<pre><code>interval?: number;\n</code></pre> <p>Interval in milliseconds between location updates.</p>"},{"location":"cordova/WatchPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist each location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"cordova/WatchPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in milliseconds to wait for each location fix before firing an error. Default <code>60000</code>.</p>"},{"location":"cordova/setup/","title":"Setup","text":""},{"location":"cordova/setup/#installation","title":"Installation","text":"cordovanpm <pre><code>cordova plugin add cordova-background-geolocation-lt\n</code></pre> <pre><code>npm install cordova-background-geolocation-lt\n</code></pre>"},{"location":"cordova/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"cordova/setup/#ios-infoplist","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"cordova/setup/#android-androidmanifestxml","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"cordova/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"cordova/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"cordova/setup/#infoplist","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Background modes --&gt;\n&lt;key&gt;UIBackgroundModes&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;location&lt;/string&gt;\n    &lt;string&gt;fetch&lt;/string&gt;\n    &lt;string&gt;processing&lt;/string&gt;\n    &lt;string&gt;audio&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Background task identifier --&gt;\n&lt;key&gt;BGTaskSchedulerPermittedIdentifiers&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;com.transistorsoft.fetch&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"cordova/setup/#android-setup","title":"Android Setup","text":""},{"location":"cordova/setup/#gradle-ext-vars","title":"Gradle ext vars","text":"<p>Set these in <code>android/build.gradle</code> (or <code>android/settings.gradle</code> for newer projects):</p> <p>The plugin reads two optional ext vars from your root project to pin dependency versions:</p> build.gradle.ktsbuild.gradle <pre><code>ext[\"playServicesLocationVersion\"] = \"21.3.0\"\next[\"tslocationmanagerVersion\"]    = \"4.0.+\"\n</code></pre> <pre><code>ext {\n    playServicesLocationVersion = \"21.3.0\"\n    tslocationmanagerVersion    = \"4.0.+\"\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Versions \u2265 21 use <code>tslocationmanager</code>; versions &lt; 21 use <code>tslocationmanager-gms20</code>. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"cordova/setup/#androidappbuildgradle","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>android {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"cordova/setup/#androidmanifestxml","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"cordova/setup/#example","title":"Example","text":"<pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"flutter/","title":"Flutter SDK Reference","text":"flutter_background_geolocation Buy Now <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"flutter/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"flutter/ActivityConfig/","title":"ActivityConfig","text":"<p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"flutter/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"flutter/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>BackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    // Poll activity recognition every 10 seconds (both platforms)\n    activityRecognitionInterval: 10000.0,\n\n    // Require \u226575% confidence before changing activity (both platforms)\n    minimumActivityRecognitionConfidence: 75,\n\n    // Disable automatic stop detection (both platforms)\n    disableStopDetection: false,\n\n    // Automatically stop when stationary (both platforms)\n    stopOnStationary: true,\n\n    // Delay motion-trigger transitions by 30s (Android only)\n    motionTriggerDelay: 30000.0,\n\n    // Disable platform motion activity APIs (both platforms)\n    disableMotionActivityUpdates: false,\n\n    // Delay stop-detection by 10s (iOS only)\n    stopDetectionDelay: 10000\n  )\n));\n</code></pre>"},{"location":"flutter/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeoConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"flutter/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Current\nBackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    activityRecognitionInterval: 10000.0,\n    disableStopDetection: true,\n    stopOnStationary: true\n  )\n));\n</code></pre>"},{"location":"flutter/ActivityConfig/#members","title":"Members","text":""},{"location":"flutter/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>int? activityRecognitionInterval\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"flutter/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>bool? disableMotionActivityUpdates\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"flutter/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"flutter/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>BackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    disableMotionActivityUpdates: true\n  )\n));\n</code></pre>"},{"location":"flutter/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>bool? disableStopDetection\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"flutter/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeoConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeoConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    disableStopDetection: true,\n  ),\n  geolocation: GeoConfig(\n    pausesLocationUpdatesAutomatically: false\n  )\n));\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BackgroundGeolocation.changePace).</p> <p></p>"},{"location":"flutter/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BackgroundGeolocation.changePace <code>false</code> or BackgroundGeolocation.stop explicitly.</p>"},{"location":"flutter/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>int? minimumActivityRecognitionConfidence\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"flutter/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>int? motionTriggerDelay\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    motionTriggerDelay: 30000.0\n  )\n));\n</code></pre>"},{"location":"flutter/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>int? stopDetectionDelay\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"flutter/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>bool? stopOnStationary\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the GeoConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeoConfig.stopTimeout timer elapses naturally. It does not fire when BackgroundGeolocation.changePace <code>false</code> is called manually.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    stopOnStationary: true,\n  )\n)).then(() {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"flutter/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<pre><code>String? triggerActivities\n</code></pre> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See TriggerActivity for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>// Only trigger tracking for vehicles\nBackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    triggerActivities: [\"in_vehicle\"]\n  )\n));\n\n// Only trigger tracking for on_foot, walking and running\nBackgroundGeolocation.ready(Config(\n  activity: ActivityConfig(\n    triggerActivities: [\"on_foot\", \"walking\", \"running\"]\n  )\n));\n</code></pre>"},{"location":"flutter/ActivityType/","title":"ActivityType","text":"<p>iOS Activity Type used with GeoConfig.activityType.</p> <p>Corresponds to Apple's <code>CLActivityType</code> enum: https://developer.apple.com/documentation/corelocation/clactivitytype</p> Name Value Description Other 1 Default/unspecified activity. AutomotiveNavigation 2 Automotive navigation mode. Fitness 3 Fitness-related activity. OtherNavigation 4 Non-automotive navigation mode. Airborne 5 Airborne activity (iOS 15+)."},{"location":"flutter/ActivityType/#members","title":"Members","text":""},{"location":"flutter/ActivityType/#airborne","title":"Airborne","text":"<p><code>ActivityType</code><code>.airborne</code></p> <p>Airborne activity (iOS 15+).</p>"},{"location":"flutter/ActivityType/#automotivenavigation","title":"AutomotiveNavigation","text":"<p><code>ActivityType</code><code>.automotiveNavigation</code></p> <p>Automotive navigation mode.</p>"},{"location":"flutter/ActivityType/#fitness","title":"Fitness","text":"<p><code>ActivityType</code><code>.fitness</code></p> <p>Fitness-related activity.</p>"},{"location":"flutter/ActivityType/#other","title":"Other","text":"<p><code>ActivityType</code><code>.other</code></p> <p>Default/unspecified activity.</p>"},{"location":"flutter/ActivityType/#othernavigation","title":"OtherNavigation","text":"<p><code>ActivityType</code><code>.otherNavigation</code></p> <p>Non-automotive navigation mode.</p>"},{"location":"flutter/AppConfig/","title":"AppConfig","text":"<p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"flutter/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"flutter/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"flutter/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BackgroundGeolocation.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"flutter/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BackgroundGeolocation.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"flutter/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See PermissionRationale for the full dialog configuration reference.</p>"},{"location":"flutter/AppConfig/#examples","title":"Examples","text":"<pre><code>await BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60.0,\n    backgroundPermissionRationale: PermissionRationale(\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    ),\n    notification: Notification(\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    ),\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  )\n));\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig(Config(\n  app: AppConfig(\n    heartbeatInterval: 120.0 // slow down heartbeats\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>await BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60.0,\n    backgroundPermissionRationale: PermissionRationale(\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    ),\n    notification: Notification(\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    ),\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>await BackgroundGeolocation.setConfig(Config(\n  app: AppConfig(\n    heartbeatInterval: 120.0 // slow down heartbeats\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#members","title":"Members","text":""},{"location":"flutter/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<p><code>PermissionRationale</code><code>? background</code><code>PermissionRationale</code></p> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    locationAuthorizationRequest: 'Always'\n  ),\n  app: AppConfig(\n    backgroundPermissionRationale: PermissionRationale(\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside PermissionRationale fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Location updates in Android 11</p>"},{"location":"flutter/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>bool? enableHeadless\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BackgroundGeolocation.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BackgroundGeolocation.registerHeadlessTask</p>"},{"location":"flutter/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>int? heartbeatInterval\n</code></pre> <p>Rate in seconds at which BackgroundGeolocation.onHeartbeat events fire while the app is in the background.</p>"},{"location":"flutter/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"flutter/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BackgroundGeolocation.onHeartbeat</p> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    preventSuspend: true,\n    heartbeatInterval: 60.0\n  )\n));\n\nBackgroundGeolocation.onHeartbeat((HeartbeatEvent event) {\n  print('[onHeartbeat] ${event}');\n\n  // Optionally request a new location during heartbeat.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) {\n    print('[getCurrentPosition] ${location}');\n  });\n});\n</code></pre>"},{"location":"flutter/AppConfig/#notification","title":"notification","text":"<pre><code>Notification? notification\n</code></pre> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    notification: Notification(\n      title: \"Background tracking engaged\",\n      text: \"My notification text\"\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>bool? preventSuspend\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BackgroundGeolocation.onHeartbeat</p> <pre><code>// Subscribe to heartbeat events\nBackgroundGeolocation.onHeartbeat((HeartbeatEvent event) {\n  print('[onHeartbeat] ${event}');\n});\n\n// Enable preventSuspend via AppConfig\nBackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    preventSuspend: true,\n    heartbeatInterval: 60.0,\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#schedule","title":"schedule","text":"<pre><code>List&lt;String&gt;? schedule\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"{DAY(s)} {START_TIME}-{END_TIME}\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\n      \"1 17:30-21:00\",     // Sunday: 5:30pm\u20139pm\n      \"2-6 09:00-17:00\",   // Mon\u2013Fri: 9am\u20135pm\n      \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm\u2013midnight\n      \"7 10:00-19:00\"      // Saturday: 10am\u20137pm\n    ]\n  )\n)).then((state) {\n  // Start the Scheduler\n  BackgroundGeolocation.startSchedule();\n});\n\n// Listen for schedule state changes\nBackgroundGeolocation.onSchedule((State state) {\n  print('[onSchedule] enabled? ${state.enabled}');\n});\n\n// Later (e.g., user logout)\nBackgroundGeolocation.stopSchedule();\nBackgroundGeolocation.stop(); // if tracking is currently active\n\n// Modify schedule using setConfig\nBackgroundGeolocation.setConfig(Config(\n  app: AppConfig(\n    schedule: [\n      \"1-7 09:00-10:00\",\n      \"1-7 11:00-12:00\",\n      \"1-7 13:00-14:00\",\n      \"1-7 15:00-16:00\",\n      \"1-7 17:00-18:00\",\n      \"2,4,6 19:00-22:00\"\n    ]\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\n      \"2018-01-01 09:00-17:00\"\n    ]\n  )\n));\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\n      \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-00:00 geofence\"\n    ]\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"flutter/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BackgroundGeolocation.startSchedule - BackgroundGeolocation.stopSchedule</p>"},{"location":"flutter/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-12:00 geofence\"\n    ]\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>bool? scheduleUseAlarmManager\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    schedule: [\"1-7 09:00-17:00\"],\n    scheduleUseAlarmManager: true\n  )\n));\n</code></pre>"},{"location":"flutter/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>bool? startOnBoot\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"flutter/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"flutter/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"flutter/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>bool? stopOnTerminate\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"flutter/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeoConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"flutter/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"flutter/AuthorizationConfig/","title":"AuthorizationConfig","text":"<p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"flutter/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"flutter/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>BackgroundGeolocation.onAuthorization((AuthorizationEvent event) {\n  if (event.success) {\n    print('[authorization] SUCCESS: ${event.response}');\n  } else {\n    print('[authorization] ERROR: ${event.error}');\n  }\n});\n\nBackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://app.your.server.com/users/locations\",\n    autoSync: true,\n  ),\n  authorization: Authorization(\n    strategy: \"JWT\",\n    accessToken: \"my-access-token\",\n    refreshToken: \"my-refresh-token\",\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshPayload: {\n      'the_refresh_token_field_name': \"{refreshToken}\"\n    },\n  )\n));\n</code></pre>"},{"location":"flutter/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"flutter/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"flutter/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>BackgroundGeolocation.onAuthorization((AuthorizationEvent event) {\n  if (event.success) {\n    print('[authorization] SUCCESS: ${event.response}');\n  } else {\n    print('[authorization] ERROR: ${event.error}');\n  }\n});\n</code></pre>"},{"location":"flutter/AuthorizationConfig/#members","title":"Members","text":""},{"location":"flutter/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>String? accessToken\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  authorization: Authorization(\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\"\n  )\n));\n</code></pre>"},{"location":"flutter/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>int? expires\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"flutter/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>Map? refreshHeaders\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>refreshHeaders: const {}\n</code></pre>"},{"location":"flutter/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>Map? refreshPayload\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  authorization: Authorization(\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.domain.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      \"grant_type\": \"refresh_token\",\n      \"refresh_token\": \"{refreshToken}\",   // &lt;-- will be replaced with the configured refreshToken\n      \"rememberMe\": \"true\"                 // &lt;-- non-strings must be stringified\n    }\n  )\n));\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"flutter/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>String? refreshToken\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"flutter/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>String? refreshUrl\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p>"},{"location":"flutter/AuthorizationConfig/#strategy","title":"strategy","text":"<pre><code>String? strategy\n</code></pre> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"flutter/AuthorizationEvent/","title":"AuthorizationEvent","text":"<p>Authorization token refresh result delivered to BackgroundGeolocation.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>BackgroundGeolocation.onAuthorization((AuthorizationEvent event) {\n  if (event.success) {\n    print('[onAuthorization] Token refreshed: ${event.response}');\n    // Update your app's stored token.\n    final token = event.response.access_token;\n  } else {\n    print('[onAuthorization] Refresh failed: ${event.error}');\n  }\n});\n</code></pre>"},{"location":"flutter/AuthorizationEvent/#members","title":"Members","text":""},{"location":"flutter/AuthorizationEvent/#error","title":"error","text":"<pre><code>String? error\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"flutter/AuthorizationEvent/#response","title":"response","text":"<pre><code>Map? response\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"flutter/AuthorizationEvent/#status","title":"status","text":"<pre><code>int status\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"flutter/AuthorizationEvent/#success","title":"success","text":"<pre><code>bool success\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"flutter/AuthorizationStatus/","title":"AuthorizationStatus","text":"<p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"flutter/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"flutter/BackgroundGeolocation/","title":"BackgroundGeolocation","text":"<p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>","boost":10},{"location":"flutter/BackgroundGeolocation/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>","boost":10},{"location":"flutter/BackgroundGeolocation/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeoConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li>Event</li> </ul> <pre><code>// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((Location location) {\n  print('[onLocation] ${location}');\n});\n\nBackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0\n  ),\n  http: HttpConfig(\n    url: \"https://example.com/locations\",\n    autoSync: true\n  ),\n  persistence: PersistenceConfig(\n    maxDaysToPersist: 7\n  )\n));\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a Subscription that must be removed when no longer needed:</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>BackgroundGeolocation.onLocation((Location location) {\n  print('New location: ${location}');\n});\n\nBackgroundGeolocation.onMotionChange((MotionChangeEvent event) {\n  print('Device is moving? ${event.isMoving}');\n});\n</code></pre> <pre><code>BackgroundGeolocation.onHttp((HttpEvent e) { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#examples","title":"Examples","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig( distanceFilter: 10.0 ),\n  http: HttpConfig( url: \"https://example.com/locations\", autoSync: true )\n));\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#compound-configuration","title":"Compound configuration","text":"<pre><code>// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((Location location) {\n  print('[onLocation] ${location}');\n});\n\nBackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0\n  ),\n  http: HttpConfig(\n    url: \"https://example.com/locations\",\n    autoSync: true\n  ),\n  persistence: PersistenceConfig(\n    maxDaysToPersist: 7\n  )\n));\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#event-listeners","title":"Event listeners","text":"<pre><code>BackgroundGeolocation.onLocation((Location location) {\n  print('New location: ${location}');\n});\n\nBackgroundGeolocation.onMotionChange((MotionChangeEvent event) {\n  print('Device is moving? ${event.isMoving}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>BackgroundGeolocation.onHttp((HttpEvent e) { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#getting-started","title":"Getting started","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig( distanceFilter: 10.0 ),\n  http: HttpConfig( url: \"https://example.com/locations\", autoSync: true )\n));\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#events_1","title":"Events","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#onactivitychange","title":"onActivityChange","text":"<pre><code>static void onActivityChange(Function(ActivityChangeEvent) callback)\n</code></pre> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#android","title":"Android","text":"<p>MotionActivityEvent.confidence always reports <code>100</code>.</p> <pre><code>BackgroundGeolocation.onActivityChange((ActivityChangeEvent event) {\n  print('[onActivityChange] ${event}');\n});\n</code></pre> <p>activitychange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onauthorization","title":"onAuthorization","text":"<p><code>static void onAuthorization(Function(</code><code>AuthorizationEvent</code><code>) callback)</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>BackgroundGeolocation.onAuthorization((Authorization event) {\n  print(\"[authorization] $event\");\n\n  if (event.success) {\n    print(\"- Authorization response: ${event.response}\");\n  } else {\n    print(\"- Authorization error: ${event.message}\");\n  }\n});\n</code></pre> <p>authorization</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>static void onConnectivityChange(Function(</code><code>ConnectivityChangeEvent</code><code>) callback)</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>BackgroundGeolocation.oConnectivityChange((ConnectivityChangeEvent event) {\n  print('[onConnectivityChange] ${event}');\n});\n</code></pre> <p>connectivitychange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onenabledchange","title":"onEnabledChange","text":"<pre><code>static void onEnabledChange(Function(bool) callback)\n</code></pre> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>BackgroundGeolocation.onEnabledChange'((bool isEnabled) {\n  print('[onEnabledChanged] isEnabled? ${isEnabled}');\n});\n</code></pre> <p>enabledchange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ongeofence","title":"onGeofence","text":"<p><code>static void on</code><code>Geofence</code><code>(Function(</code><code>GeofenceEvent</code><code>) callback)</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence]  ${event}');\n});\n</code></pre> <p>geofence</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>static void onGeofencesChange(Function(</code><code>GeofencesChangeEvent</code><code>) callback)</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeoConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.onGeofencesChange((GeofencesChangeEvent event) {\n  List&lt;String&gt; on = event.on;     //&lt;-- new geofences activated.\n  List&lt;Geofence&gt; off = event.off; //&lt;-- geofences that were just de-activated.\n\n  // Create map circles\n  on.forEach((Geofence geofence) {\n    createGeofenceMarker(geofence)\n  });\n\n  // Remove map circles\n  off.forEach((String identifier) {\n    removeGeofenceMarker(identifier);\n  }\n});\n</code></pre> <p>geofenceschange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onheartbeat","title":"onHeartbeat","text":"<p><code>static void onHeartbeat(Function(</code><code>HeartbeatEvent</code><code>) callback)</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The Location provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  heartbeatInterval: 60\n));\n\nBackgroundGeolocation.onHeartbeat((HeartbeatEvent event) {\n  print('[onHeartbeat] ${event}');\n\n  // You could request a new location if you wish.\n  BackgroundGeolocation.getCurrentPosition(\n    samples: 1,\n    persist: true\n  ).then((Location location) {\n    print('[getCurrentPosition] ${location}');\n  });\n})\n</code></pre> <p>heartbeat</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onhttp","title":"onHttp","text":"<p><code>static void onHttp(Function(</code><code>HttpEvent</code><code>) callback)</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.onHttp((HttpEvent event) {\n  int status = response.status;\n  bool success = response.success;\n  String responseText = response.responseText;\n  print('[onHttp] status: ${status}, success? ${success}, responseText: ${responseText}');\n});\n</code></pre> <p>http</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onlocation","title":"onLocation","text":"<p><code>static void on</code><code>Location</code><code>(Function(</code><code>Location</code><code>) success, [Function(</code><code>LocationError</code><code>)? failure])</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a LocationError code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with Location.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  print('[onLocation] success: ${location}');\n}, (LocationError error) {\n  print('[onLocation] ERROR: ${error}');\n});\n</code></pre> <p>location</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onmotionchange","title":"onMotionChange","text":"<p><code>static void onMotionChange(Function(</code><code>Location</code><code>) callback)</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeoConfig.stopTimeout</p> <pre><code>BackgroundGeolocation.onMotionChange((Location location) {\n  if (location.isMoving) {\n     print('[onMotionChange] Device has just started MOVING ${location}');\n  } else {\n     print('[onMotionChange] Device has just STOPPED:  ${location});\n  }\n});\n</code></pre> <p>motionchange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onnotificationaction","title":"onNotificationAction","text":"<pre><code>static void onNotificationAction(Function(String) callback)\n</code></pre> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    actions: [  // &lt;-- register button listeners\n      \"notificationButtonFoo\",\n      \"notificationButtonBar\"\n    ]\n  )\n));\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((String buttonId) {\n  print(\"[onNotificationAction] - ${buttonId}\");\n  switch(buttonId) {\n    case 'notificationButtonFoo':\n      break;\n    case 'notificationButtonBar':\n      break;\n  }\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#onpowersavechange","title":"onPowerSaveChange","text":"<pre><code>static void onPowerSaveChange(Function(bool) callback)\n</code></pre> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"flutter/BackgroundGeolocation/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>BackgroundGeolocation.onPowerSaveChange((bool isPowerSaveMode) {\n  print('[onPowerSaveChange: ${isPowerSaveMode}');\n});\n</code></pre> <p>powersavechange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onproviderchange","title":"onProviderChange","text":"<p><code>static void onProviderChange(Function(</code><code>ProviderChangeEvent</code><code>) callback)</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>BackgroundGeolocation.onProviderChange((ProviderChangeEvent event) {\n  print('[onProviderChange: ${event}');\n\n  switch(provider.status) {\n    case ProviderChangeEvent.AUTHORIZATION_STATUS_DENIED:\n      // Android &amp; iOS\n      print('- Location authorization denied');\n      break;\n    case ProviderChangeEvent.AUTHORIZATION_STATUS_ALWAYS:\n      // Android &amp; iOS\n      console.log('- Location always granted');\n      break;\n    case ProviderChangeEvent.AUTHORIZATION_STATUS_WHEN_IN_USE:\n      // iOS only\n      console.log('- Location WhenInUse granted');\n      break;\n  }\n});\n</code></pre> <p>providerchange</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#onschedule","title":"onSchedule","text":"<p><code>static void onSchedule(Function(</code><code>State</code><code>) callback)</code></p> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>BackgroundGeolocation.onSchedule((State state) {\n  if (state.enabled) {\n    print('[onSchedule] scheduled start tracking');\n  } else {\n    print('[onSchedule] scheduled stop tracking');\n  }\n});\n</code></pre> <p>schedule</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#methods","title":"Methods","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#changepace","title":"changePace","text":"<pre><code>static Future&lt;bool&gt; changePace(bool isMoving)\n</code></pre> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>BackgroundGeolocation.changePace(true);  // &lt;-- Location-services ON (\"moving\" state)\nBackgroundGeolocation.changePace(false); // &lt;-- Location-services OFF (\"stationary\" state)\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>static Future&lt;</code><code>Location</code><code>&gt; getCurrentPosition(</code></p> <p>Retrieve the current Location.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a LocationError.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#options","title":"Options","text":"<p>See CurrentPositionRequest.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#error-codes_1","title":"Error Codes","text":"<p>See LocationError.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with Location.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>Location location = await BackgroundGeolocation.getCurrentPosition(\n  timeout: 30,          // 30 second timeout to fetch location\n  maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.\n  desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.\n  samples: 3,           // How many location samples to attempt.\n  extras: {             // [Optional] Attach your own custom meta-data to this location.  This meta-data will be persisted to SQLite and POSTed to your server\n    \"foo\": \"bar\"\n  }\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#odometer","title":"odometer","text":"<pre><code>static Future&lt;double&gt; get odometer\n</code></pre> <p>Retrieve the current odometer reading in meters.</p> <p>The SDK continuously accumulates distance traveled between recorded locations.</p> <p>Warning</p> <p>Odometer accuracy depends on location accuracy. Noisy or inaccurate locations introduce error into accumulated distance. Use LocationFilter.odometerAccuracyThreshold to filter low-accuracy samples from odometer calculations.</p> <p>See also - LocationFilter.odometerAccuracyThreshold - <code>resetOdometer</code> / <code>setOdometer</code></p> <pre><code>double odometer = await BackgroundGeolocation.odometer;\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#ready","title":"ready","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; ready(</code><code>Config</code><code>config)</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>BackgroundGeolocation.ready(Config(\n  desiredAccuracy: Config.DESIRED_ACCURACY_HIGH,\n  distanceFilter: 10,\n  stopOnTerminate: false,\n  startOnBoot: true,\n  url: 'http://your.server.com',\n  headers: {\n    'my-auth-token': 'secret-token'\n  }\n)).then((State state) {\n  print('[ready] success: ${state}');\n});\n</code></pre> <pre><code>BackgroundGeolocation.reset();\n// Reset to documented default-values with overrides\nBackgroundGeolocation.reset(Config(\n  distanceFilter:  10\n));\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#registerheadlesstask","title":"registerHeadlessTask","text":"<p><code>static Future&lt;bool&gt; registerHeadlessTask( void Function(</code><code>HeadlessEvent</code><code>) callback)</code></p> <p>Android only Registers a headless-task callback for Android background events when the app has been terminated with AppConfig.stopOnTerminate<code>:false</code>.</p> <p>The callback receives a HeadlessEvent with a <code>name</code> (event name) and <code>params</code> (event data).</p> <p>Warning</p> <p>You must call <code>registerHeadlessTask</code> in your application root file (e.g. <code>index.js</code>), not inside a component or behind a UI action.</p> <p>Warning</p> <p>Your function must be declared <code>async</code>. Await all work inside it \u2014 the headless task is automatically terminated after the last line executes.</p> <p>Note</p> <p>Javascript headless callbacks are not supported by Cordova or Capacitor.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#debugging","title":"Debugging","text":"<p>While implementing your headless task, observe Android logs via:</p> <pre><code>$ adb logcat *:S TSLocationManager:V ReactNativeJS:V\n</code></pre> <p>See also - \ud83d\udcd8 Android Headless Mode - AppConfig.enableHeadless</p> <pre><code>import 'package:flutter_background_geolocation/flutter_background_geolocation.dart' as bg;\n\n/// Receives all events from BackgroundGeolocation while app is terminated:\nvoid headlessTask(HeadlessEvent headlessEvent) async {\n  print('[HeadlessTask]: ${headlessEvent}');\n\n  // Implement a 'case' for only those events you're interested in.\n  switch(headlessEvent.name) {\n    case Event.TERMINATE:\n      State state = headlessEvent.event;\n      print('- State: ${state}');\n      break;\n    case Event.HEARTBEAT:\n      HeartbeatEvent event = headlessEvent.event;\n      print('- HeartbeatEvent: ${event}');\n      break;\n    case Event.LOCATION:\n      Location location = headlessEvent.event;\n      print('- Location: ${location}');\n      break;\n    case Event.MOTIONCHANGE:\n      Location location = headlessEvent.event;\n      print('- Location: ${location}');\n      break;\n    case Event.GEOFENCE:\n      GeofenceEvent geofenceEvent = headlessEvent.event;\n      print('- GeofenceEvent: ${geofenceEvent}');\n      break;\n    case Event.GEOFENCESCHANGE:\n      GeofencesChangeEvent event = headlessEvent.event;\n      print('- GeofencesChangeEvent: ${event}');\n      break;\n    case Event.SCHEDULE:\n      State state = headlessEvent.event;\n      print('- State: ${state}');\n      break;\n    case Event.ACTIVITYCHANGE:\n      ActivityChangeEvent event = headlessEvent.event;\n      print('ActivityChangeEvent: ${event}');\n      break;\n    case Event.HTTP:\n      HttpEvent response = headlessEvent.event;\n      print('HttpEvent: ${response}');\n      break;\n    case Event.POWERSAVECHANGE:\n      bool enabled = headlessEvent.event;\n      print('ProviderChangeEvent: ${enabled}');\n      break;\n    case Event.CONNECTIVITYCHANGE:\n      ConnectivityChangeEvent event = headlessEvent.event;\n      print('ConnectivityChangeEvent: ${event}');\n      break;\n    case Event.ENABLEDCHANGE:\n      bool enabled = headlessEvent.event;\n      print('EnabledChangeEvent: ${enabled}');\n      break;\n  }\n}\n\nvoid main() {\n  runApp(HelloWorld());\n\n  // Register your headlessTask:\n  BackgroundGeolocation.registerHeadlessTask(headlessTask);\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#removelisteners","title":"removeListeners","text":"<pre><code>static Future&lt;bool&gt; removeListeners()\n</code></pre> <p>Remove all event listeners.</p> <p>Calls Subscription.remove on all active subscriptions.</p> <pre><code>BackgroundGeolocation.removeListeners();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#reset","title":"reset","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; reset([</code><code>Config</code><code>? config])</code></p> <p>Reset the SDK configuration to documented default values.</p> <p>If an optional Config is provided, it is applied after the reset.</p> <pre><code>BackgroundGeolocation.reset();\n// Reset to default values with overrides\nBackgroundGeolocation.reset(Config(\n  geolocation: GeoConfig( distanceFilter: 10.0 )\n));\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#setconfig","title":"setConfig","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; set</code><code>Config</code><code>(</code><code>Config</code><code>config)</code></p> <p>Update the SDK's Config at runtime.</p> <p>The supplied Config is merged into the current configuration and applied immediately. Use this after <code>ready</code> has been called to change settings dynamically.</p> <pre><code>BackgroundGeolocation.setConfig(Config(\n  desiredAccuracy: Config.DESIRED_ACCURACY_HIGH,\n  distanceFilter: 100.0,\n  stopOnTerminate: false,\n  startOnBoot: true\n)).then((State state) {\n  print('[setConfig] success: ${state}');\n})\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#setodometer","title":"setOdometer","text":"<p><code>static Future&lt;</code><code>Location</code><code>&gt; setOdometer(double value)</code></p> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>BackgroundGeolocation.setOdometer(1234.56).then((Location location) {\n  // This is the location where odometer was set at.\n  print('[setOdometer] success: ${location}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#start","title":"start","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; start()</code></p> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>BackgroundGeolocation.start().then((State state) {\n  print('[start] success - ${state}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#startgeofences","title":"startGeofences","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; startGeofences()</code></p> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Add a geofence.\nBackgroundGeolocation.addGeofence(Geofence(\n  notifyOnExit: true,\n  radius: 200,\n  identifier: 'ZONE_OF_INTEREST',\n  latitude: 37.234232,\n  longitude: 42.234234\n));\n\n// Listen to geofence events.\nBackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence] - ${event}')\n});\n\nBackgroundGeolocation.ready(Config(\n  url: 'http://my.server.com',\n  autoSync: true\n)).then((State state) {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#startschedule","title":"startSchedule","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; startSchedule()</code></p> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>BackgroundGeolocation.startSchedule.then((State state) {\n  print('[startSchedule] success: ${state}');\n})\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#stop","title":"stop","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; stop()</code></p> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>BackgroundGeolocation.stop();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#stop-tracking-and-the-scheduler","title":"Stop tracking and the scheduler","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#stopschedule","title":"stopSchedule","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; stopSchedule()</code></p> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>BackgroundGeolocation.stopSchedule.then((State state) {\n  print('[stopSchedule] success: ${state}');\n})\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nfinal state = await BackgroundGeolocation.state;\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#stop-the-scheduler-and-active-tracking","title":"Stop the scheduler and active tracking","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule().then((State state) {\n  if (state.enabled) {\n    BackgroundGeolocation.stop();\n  }\n})\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#watchposition","title":"watchPosition","text":"<pre><code>static Future&lt;int&gt; watchPosition(\n</code></pre> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a Subscription that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>onResume() async {\n  // Start watching position while app in foreground, retaining the return Subscription.\n  watchPositionSubscription = await BackgroundGeolocation.watchPosition(\n    {'interval': 1000, 'extras': {'foo': 'bar'}},\n    (Location location) {\n      print('[watchPosition] - ${location}');\n    },\n    (int errorCode) {\n      print('[watchPosition] ERROR - ${errorCode}');\n    }\n  );\n}\n\nonSuspend() {\n  // Halt watching position when app goes to background.\n  watchPositionSubscription?.remove();\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#geofencing","title":"Geofencing","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#addgeofence","title":"addGeofence","text":"<p><code>static Future&lt;bool&gt; add</code><code>Geofence</code><code>(</code><code>Geofence</code><code>geofence)</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"Home\",\n  radius: 150,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: false,\n  notifyOnDwell: true,\n  loiteringDelay: 30000,  // 30 seconds\n  extras: {               // Optional arbitrary meta-data\n    zone_id: 1234\n  }\n)).then((bool success) {\n  print('[addGeofence] success');\n}.catchError((error) {\n  print('[addGeofence] FAILURE: ${error}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#addgeofences","title":"addGeofences","text":"<p><code>static Future&lt;bool&gt; addGeofences(List&lt;</code><code>Geofence</code><code>&gt; geofences)</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>List&lt;Geofence&gt; geofences = [\n  Geofence(\n    identifier: 'foo',\n    radius: 200,\n    latitude: 45.51921926,\n    longitude: -73.61678581,\n    notifyOnEntry: true,\n  ),\n  Geofence(\n    identifier: 'bar',\n    radius: 200,\n    latitude: 45.51921926,\n    longitude: -73.61678581,\n    notifyOnEntry: true,\n  )\n];\n\nBackgroundGeolocation.addGeofences(geofences);\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#geofenceexists","title":"geofenceExists","text":"<pre><code>static Future&lt;bool&gt; geofenceExists(String identifier)\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>final exists = await BackgroundGeolocation.geofenceExists(\"HOME\");\nprint('[geofenceExists]  ${exists}');\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#getgeofence","title":"getGeofence","text":"<p><code>static Future&lt;</code><code>Geofence</code><code>?&gt; get</code><code>Geofence</code><code>(String identifier)</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>Geofence geofence = await BackgroundGeolocation.getGeofence(\"HOME\");\nprint('[getGeofence HOME: ${geofence}');\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#geofences","title":"geofences","text":"<p><code>static Future&lt;List&lt;</code><code>Geofence</code><code>&gt;&gt; get geofences</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>final geofences = await BackgroundGeolocation.geofences;\nprint('[getGeofences]:  ${geofences}');\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#removegeofence","title":"removeGeofence","text":"<p><code>static Future&lt;bool&gt; remove</code><code>Geofence</code><code>(String identifier)</code></p> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofence(\"Home\").then((success) {\n  print('[removeGeofence] success');\n}).catchError((error) {\n  print('[removeGeofence] FAILURE:  ${error}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#removegeofences","title":"removeGeofences","text":"<pre><code>static Future&lt;bool&gt; removeGeofences()\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofences();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#data-management","title":"Data Management","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#destroylocation","title":"destroyLocation","text":"<p><code>static Future&lt;bool&gt; destroy</code><code>Location</code><code>(String uuid)</code></p> <p>Remove a single location by Location.uuid.</p> <pre><code>try {\n  await BackgroundGeolocation.destroyLocation(location.uuid);\n} catch(error) {\n  print(\"[destroyLocation] failed: $error\");\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#destroylocations","title":"destroyLocations","text":"<pre><code>static Future&lt;bool&gt; destroyLocations()\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>bool success = BackgroundGeolocation.destroyLocations();\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#count","title":"count","text":"<pre><code>static Future&lt;int&gt; get count\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>int count = await BackgroundGeolocation.count;\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#locations","title":"locations","text":"<pre><code>static Future&lt;List&gt; get locations\n</code></pre> <p>Retrieve all Location records stored in the SDK's SQLite database.</p> <pre><code>List locations = await BackgroundGeolocation.locations;\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#sync","title":"sync","text":"<pre><code>static Future&lt;List&gt; sync()\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.sync((List records) {\n  print('[sync] success: ${records}');\n}).catchError((error) {\n  print('[sync] FAILURE: ${error}');\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#authorization","title":"Authorization","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#providerstate","title":"providerState","text":"<p><code>static Future&lt;</code><code>ProviderChangeEvent</code><code>&gt; get provider</code><code>State</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>final providerState = await BackgroundGeolocation.providerState;\nprint('- Provider state:  ${providerState}');\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#requestpermission","title":"requestPermission","text":"<pre><code>static Future&lt;int&gt; requestPermission()\n</code></pre> <p>Manually request location permission using the configured GeoConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeoConfig.locationAuthorizationRequest - GeoConfig.disableLocationAuthorizationAlert - GeoConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>initPlatformState async {\n  // Listen to onProviderChange to be notified when location authorization changes occur.\n  BackgroundGeolocation.onProviderChange((event) {\n    print(\"[providerchange] $event\");\n  });\n\n  // First ready the plugin with your configuration.\n  let State = await BackgroundGeolocation.ready(Config(\n    locationAuthorizationRequest: 'Always'\n  ));\n\n  // Manually request permission with configured locationAuthorizationRequest.\n  try {\n    int status = await BackgroundGeolocation.requestPermission();\n    if (status == ProviderChangeEvent.AUTHORIZATION_STATUS_ALWAYS) {\n      print(\"[requestPermission] Authorized Always $status\");\n    } else if (status == ProviderChangeEvent.AUTHORIZATION_STATUS_WHEN_IN_USE) {\n      print(\"[requestPermission] Authorized WhenInUse: $status\");\n    }\n  } catch(error) {\n    print(\"[requestPermission] DENIED: $error\");\n  }\n}\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<pre><code>static Future&lt;int&gt; requestTemporaryFullAccuracy(String purpose)\n</code></pre> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>","boost":10},{"location":"flutter/BackgroundGeolocation/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>BackgroundGeolocation.onProviderChange((ProviderChangeEvent event) {\n  if (event.accuracyAuthorization == ProviderChangeEvent.ACCURACY_AUTHORIZATION_REDUCED) {\n    // Supply \"Purpose\" key from Info.plist as 1st argument.\n    BackgroundGeolocation.requestTemporaryFullAccuracy(\"Delivery\").then((int accuracyAuthorization) {\n      if (accuracyAuthorization == ProviderChangeEvent.ACCURACY_AUTHORIZATION_FULL) {\n        print(\"[requestTemporaryFullAccuracy] GRANTED:  $accuracyAuthorization\");\n      } else {\n        print(\"[requestTemporaryFullAccuracy] DENIED:  $accuracyAuthorization\");\n      }\n    }).catchError((error) {\n      print(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: $error\");\n    });\n  }\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#background-tasks","title":"Background Tasks","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>static Future&lt;int&gt; startBackgroundTask()\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ios_2","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#android_2","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>BackgroundGeolocation.startBackgroundTask().then((int taskId) {\n  // Perform some long-running task (eg: HTTP request)\n  performLongRunningTask.then(() {\n    // When your long-running task is complete, signal completion of taskId.\n    BackgroundGeolocation.finish(taskId);\n  });\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>static Future&lt;int&gt; finish(int taskId)\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>BackgroundGeolocation.startBackgroundTask().then((int taskId) {\n  // Perform your long-running task here\n  BackgroundGeolocation.stopBackgroundTask(taskId);\n}).catchError((error) {\n  // Be sure to catch errors: never leave your background-task hanging.\n  print('${error}');\n  BackgroundGeolocation.stopBackgroundTask(0);\n});\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#logging","title":"Logging","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#playsound","title":"playSound","text":"<pre><code>static Future&lt;bool&gt; playSound(dynamic soundId)\n</code></pre> <p>Play a system sound.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#ios_3","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#android_3","title":"Android","text":"<p>Provide a sound name string.</p>","boost":10},{"location":"flutter/BackgroundGeolocation/#properties","title":"Properties","text":"","boost":10},{"location":"flutter/BackgroundGeolocation/#sensors","title":"sensors","text":"<p><code>static Future&lt;</code><code>Sensors</code><code>&gt; get sensors</code></p> <p>Sensors API \u2014 query the device motion hardware.</p> <pre><code>Sensors sensors = await BackgroundGeolocation.sensors;\nprint(sensors);\n</code></pre>","boost":10},{"location":"flutter/BackgroundGeolocation/#state","title":"state","text":"<p><code>static Future&lt;</code><code>State</code><code>&gt; get state</code></p> <p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>State state = await BackgroundGeolocation.state;\nif (state.enabled &amp;&amp; state.isMoving) {\n  print(\"Tracking in motion, odometer: ${state.odometer}\");\n}\n</code></pre>","boost":10},{"location":"flutter/Battery/","title":"Battery","text":"<p>Device battery state at the time a Location was recorded.</p>"},{"location":"flutter/Battery/#members","title":"Members","text":""},{"location":"flutter/Battery/#is_charging","title":"is_charging","text":"<pre><code>bool isCharging\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"flutter/Battery/#level","title":"level","text":"<pre><code>double level\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"flutter/Config/","title":"Config","text":"<p>Root configuration object passed to BackgroundGeolocation.ready and BackgroundGeolocation.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"flutter/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"flutter/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeoConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BackgroundGeolocation.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BackgroundGeolocation.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"flutter/Config/#examples","title":"Examples","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0,\n  ),\n  http: HttpConfig(\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  ),\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n  ),\n));\n</code></pre> <pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0,\n    stopTimeout: 5,\n    stationaryRadius: 150.0,\n  ),\n  activity: ActivityConfig(\n    disableStopDetection: false,\n    motionTriggerDelay: 30000.0,\n  ),\n  http: HttpConfig(\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  ),\n  persistence: PersistenceConfig(\n    persistMode: PersistMode.all,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  ),\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  ),\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 3,\n  ),\n));\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig(Config(\n  http: HttpConfig( headers: { Authorization: 'Bearer new-token' } ),\n  logger: LoggerConfig( logLevel: LogLevel.info ),\n));\n</code></pre>"},{"location":"flutter/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0,\n  ),\n  http: HttpConfig(\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  ),\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n  ),\n));\n</code></pre>"},{"location":"flutter/Config/#full-configuration","title":"Full configuration","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 20.0,\n    stopTimeout: 5,\n    stationaryRadius: 150.0,\n  ),\n  activity: ActivityConfig(\n    disableStopDetection: false,\n    motionTriggerDelay: 30000.0,\n  ),\n  http: HttpConfig(\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  ),\n  persistence: PersistenceConfig(\n    persistMode: PersistMode.all,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  ),\n  app: AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  ),\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 3,\n  ),\n));\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig(Config(\n  http: HttpConfig( headers: { Authorization: 'Bearer new-token' } ),\n  logger: LoggerConfig( logLevel: LogLevel.info ),\n));\n</code></pre>"},{"location":"flutter/Config/#members","title":"Members","text":""},{"location":"flutter/Config/#activity","title":"activity","text":"<p><code>ActivityConfig</code><code>? activity</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"flutter/Config/#app","title":"app","text":"<p><code>AppConfig</code><code>? app</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"flutter/Config/#authorization","title":"authorization","text":"<pre><code>Authorization? authorization\n</code></pre> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"flutter/Config/#geolocation","title":"geolocation","text":"<p><code>GeoConfig</code><code>? geolocation</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeoConfig.</p>"},{"location":"flutter/Config/#http","title":"http","text":"<p><code>HttpConfig</code><code>? http</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"flutter/Config/#logger","title":"logger","text":"<p><code>LoggerConfig</code><code>? logger</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"flutter/Config/#persistence","title":"persistence","text":"<p><code>PersistenceConfig</code><code>? persistence</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"flutter/Config/#reset","title":"reset","text":"<pre><code>bool? reset\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"flutter/Config/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>TransistorAuthorizationToken</code><code>? transistorAuthorizationToken</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>TransistorAuthorizationToken token = await\n  TransistorAuthorizationToken.findOrCreate('my-company-name', 'my-username');\n\nBackgroundGeolocation.ready(Config(\n  transistorAuthorizationToken: token\n));\n</code></pre> <pre><code>// Base url to Transistor Demo Server.\nconst String url = 'http://tracker.transistorsoft.com';\n\n// Register for an authorization token from server.\nTransistorAuthorizationToken token = await\n  TransistorAuthorizationToken.findOrCreate('my-company-name', 'my-username');\n\nBackgroundGeolocation.ready(Config(\n  /// This convenience option sets [HttpConfig.url] to the Demo server and configures [Authorization].\n  url: \"$url/api/locations\",\n  authorization: Authorization(\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: \"$url/api/refresh_token\",\n    refreshPayload: {\n      \"refresh_token\": \"{refreshToken}\"\n    },\n    expires: token.expires\n  )\n));\n</code></pre>"},{"location":"flutter/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<p>Network connectivity change delivered to BackgroundGeolocation.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>BackgroundGeolocation.onConnectivityChange((ConnectivityChangeEvent event) {\n  print('[onConnectivityChange] connected: ${event.connected}');\n});\n</code></pre>"},{"location":"flutter/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"flutter/ConnectivityChangeEvent/#connected","title":"connected","text":"<pre><code>bool connected\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"flutter/Coords/","title":"Coords","text":"<p>Geographic coordinates attached to a Location.</p>"},{"location":"flutter/Coords/#members","title":"Members","text":""},{"location":"flutter/Coords/#accuracy","title":"accuracy","text":"<pre><code>double accuracy\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"flutter/Coords/#altitude","title":"altitude","text":"<pre><code>double altitude\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"flutter/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"flutter/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"flutter/Coords/#altitude_accuracy","title":"altitude_accuracy","text":"<pre><code>double altitudeAccuracy\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"flutter/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"flutter/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"flutter/Coords/#ellipsoidal_altitude","title":"ellipsoidal_altitude","text":"<pre><code>double ellipsoidalAltitude\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"flutter/Coords/#floor","title":"floor","text":"<pre><code>int? floor\n</code></pre> <p>iOS only Floor within a building, when indoor-positioning hardware (e.g. Bluetooth beacons) is available.</p>"},{"location":"flutter/Coords/#heading","title":"heading","text":"<pre><code>double heading\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"flutter/Coords/#heading_accuracy","title":"heading_accuracy","text":"<pre><code>double headingAccuracy\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"flutter/Coords/#latitude","title":"latitude","text":"<pre><code>double latitude\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"flutter/Coords/#longitude","title":"longitude","text":"<pre><code>double longitude\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"flutter/Coords/#speed","title":"speed","text":"<pre><code>double speed\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"flutter/Coords/#speed_accuracy","title":"speed_accuracy","text":"<pre><code>double speedAccuracy\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"flutter/CurrentPositionRequest/","title":"CurrentPositionRequest","text":"<p>Options for BackgroundGeolocation.getCurrentPosition.</p> <p>All fields are optional. The SDK always requests location from the native API at maximum accuracy (DesiredAccuracy.High), regardless of the <code>desiredAccuracy</code> threshold set here.</p> <pre><code>final location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // seconds before giving up\n  maximumAge: 5000,     // accept a cached fix up to 5000 ms old\n  desiredAccuracy: 10,  // stop sampling when accuracy \u2264 10 m\n  samples: 3,           // take up to 3 samples and pick the best\n  extras: {\n    route_id: 123\n  }\n});\n</code></pre>"},{"location":"flutter/DesiredAccuracy/","title":"DesiredAccuracy","text":"<p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"flutter/DesiredAccuracy/#members","title":"Members","text":""},{"location":"flutter/DesiredAccuracy/#high","title":"High","text":"<p><code>DesiredAccuracy</code><code>.high</code></p> <p>High accuracy (e.g., 0-10m).</p>"},{"location":"flutter/DesiredAccuracy/#low","title":"Low","text":"<p><code>DesiredAccuracy</code><code>.low</code></p> <p>Low accuracy (e.g., 100m).</p>"},{"location":"flutter/DesiredAccuracy/#lowest","title":"Lowest","text":"<p><code>DesiredAccuracy</code><code>.lowest</code></p> <p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"flutter/DesiredAccuracy/#medium","title":"Medium","text":"<p><code>DesiredAccuracy</code><code>.medium</code></p> <p>Medium accuracy (e.g., 10m).</p>"},{"location":"flutter/DesiredAccuracy/#navigation","title":"Navigation","text":"<p><code>DesiredAccuracy</code><code>.navigation</code></p> <p>Navigation-level accuracy (e.g., 0-10m).</p>"},{"location":"flutter/DesiredAccuracy/#verylow","title":"VeryLow","text":"<p><code>DesiredAccuracy</code><code>.veryLow</code></p> <p>Very low accuracy (e.g., 1000m).</p>"},{"location":"flutter/DeviceInfo/","title":"DeviceInfo","text":"<p>Basic device hardware and OS information returned by BackgroundGeolocation.getDeviceInfo.</p> <pre><code>final info = await BackgroundGeolocation.deviceInfo;\nprint('[DeviceInfo] ${info.manufacturer} ${info.model} (${info.platform})');\n</code></pre>"},{"location":"flutter/DeviceInfo/#members","title":"Members","text":""},{"location":"flutter/DeviceInfo/#framework","title":"framework","text":"<pre><code>String framework\n</code></pre> <p>Development framework hosting the SDK.</p> <p>Examples: <code>\"react-native\"</code>, <code>\"capacitor\"</code>, <code>\"cordova\"</code>, <code>\"flutter\"</code></p>"},{"location":"flutter/DeviceInfo/#manufacturer","title":"manufacturer","text":"<pre><code>String manufacturer\n</code></pre> <p>Device manufacturer.</p> <p>Examples: <code>\"Apple\"</code>, <code>\"Google\"</code>, <code>\"Samsung\"</code>, <code>\"Huawei\"</code></p>"},{"location":"flutter/DeviceInfo/#model","title":"model","text":"<pre><code>String model\n</code></pre> <p>Device model identifier.</p> <p>Examples: <code>\"iPhone15,2\"</code>, <code>\"Pixel 8 Pro\"</code>, <code>\"SM-G991B\"</code></p>"},{"location":"flutter/DeviceInfo/#platform","title":"platform","text":"<pre><code>String platform\n</code></pre> <p>OS platform name: <code>\"iOS\"</code> or <code>\"Android\"</code>.</p>"},{"location":"flutter/DeviceInfo/#version","title":"version","text":"<pre><code>String version\n</code></pre> <p>OS version string.</p> <p>Examples: <code>\"18.1\"</code>, <code>\"14\"</code>, <code>\"14.1.1\"</code></p>"},{"location":"flutter/DeviceSettings/","title":"DeviceSettings Android only","text":"<p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>final isIgnoring = await BackgroundGeolocation.deviceSettings.isIgnoringBatteryOptimizations();\nif (!isIgnoring) {\n  final req = await BackgroundGeolocation.deviceSettings.showIgnoreBatteryOptimizations();\n  if (!req.seen) {\n    final confirmed = await showMyConfirmDialog();\n    if (confirmed) {\n      await BackgroundGeolocation.deviceSettings.show(req);\n    }\n  }\n}\n</code></pre>"},{"location":"flutter/DeviceSettings/#members","title":"Members","text":""},{"location":"flutter/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>static Future&lt;bool&gt; get isIgnoringBatteryOptimizations\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p> <pre><code>bool isIgnoring = await DeviceSettings.isIgnoringBatteryOptimizations;\n</code></pre>"},{"location":"flutter/DeviceSettings/#show","title":"show","text":"<p><code>static Future&lt;bool&gt; show(</code><code>DeviceSettingsRequest</code><code>request)</code></p> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>final req = await BackgroundGeolocation.deviceSettings.showPowerManager();\nif (!req.seen) {\n  await BackgroundGeolocation.deviceSettings.show(req);\n}\n</code></pre>"},{"location":"flutter/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<p><code>static Future&lt;</code><code>DeviceSettingsRequest</code><code>&gt; showIgnoreBatteryOptimizations()</code></p> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p> <pre><code>// Is Android device ignoring battery optimizations?\nbool isIgnoring = await DeviceSettings.isIgnoringBatteryOptimizations;\nif (!isIgnoring) {\n  DeviceSettings.showIgnoreBatteryOptimizations().then((DeviceSettingsRequest request) {\n    print(\"- Screen seen? ${request.seen} ${request.lastSeenAt}\");\n    print(\"- Device: ${request.manufacturer} ${request.model} ${request.version}\");\n\n    // If we've already shown this screen to the user, we don't want to annoy them.\n    if (request.seen) {\n      return;\n    }\n\n    // It's your responsibility to instruct the user what exactly\n    // to do here, perhaps with a Confirm Dialog:\n    showMyConfirmDialog(\n      title: \"Settings request\",\n      text: \"Please disable battery optimizations for your device\"\n    ).then((bool confirmed) {\n      if (confirmed) {\n        // User clicked [Confirm] button.  Execute the redirect to settings screen:\n        DeviceSettings.show(request);\n      }\n    });\n  }).catchError((dynamic error) {\n    // Depending on Manufacturer/Model/OS Version, a Device may not implement\n    // a particular Settings screen.\n    print(error);\n  });\n}\n</code></pre>"},{"location":"flutter/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<p><code>static Future&lt;</code><code>DeviceSettingsRequest</code><code>&gt; showPowerManager()</code></p> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p> <pre><code>DeviceSettings.showPowerManager().then((DeviceSettingsRequest request) {\n  print(\"- Screen seen? ${request.seen} ${request.lastSeenAt}\");\n  print(\"- Device: ${request.manufacturer} ${request.model} ${request.version}\");\n\n  // If we've already shown this screen to the user, we don't want to annoy them.\n  if (request.seen) {\n    return;\n  }\n  // It's your responsibility to instruct the user what exactly\n  // to do here, perhaps with a Confirm Dialog:\n  showMyConfirmDialog(\n    title: \"Device Power Management\",\n    text: \"Please white-list the app in your Device's Power Management settings by clicking this then selecting that.\"\n  ).then((bool confirmed) {\n    if (confirmed) {\n      // User clicked [Confirm] button.  Execute the redirect to settings screen:\n      DeviceSettings.show(request);\n    }\n  });\n}).catchError((dynamic error) {\n  // Depending on Manufacturer/Model/OS Version, a Device may not implement\n  // a particular Settings screen.\n  print(error);\n});\n</code></pre>"},{"location":"flutter/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"flutter/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"flutter/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>String action\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"flutter/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>DateTime? lastSeenAt\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"flutter/DeviceSettingsRequest/#manufacturer","title":"manufacturer","text":"<pre><code>String manufacturer\n</code></pre> <p>Device manufacturer (e.g. <code>\"Huawei\"</code>, <code>\"Samsung\"</code>).</p>"},{"location":"flutter/DeviceSettingsRequest/#model","title":"model","text":"<pre><code>String model\n</code></pre> <p>Device model name (e.g. <code>\"P40\"</code>, <code>\"SM-G991B\"</code>).</p>"},{"location":"flutter/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>bool seen\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"flutter/DeviceSettingsRequest/#version","title":"version","text":"<pre><code>String version\n</code></pre> <p>Android OS version string.</p>"},{"location":"flutter/Event/","title":"Event","text":"<p>Supported event names emitted by the SDK.</p>"},{"location":"flutter/Event/#members","title":"Members","text":""},{"location":"flutter/Event/#activitychange","title":"ActivityChange","text":"<pre><code>static const ACTIVITYCHANGE = \"activitychange\"\n</code></pre> <p>Emitted when a motion-activity change is detected.</p>"},{"location":"flutter/Event/#authorization","title":"Authorization","text":"<pre><code>static const AUTHORIZATION = \"authorization\"\n</code></pre> <p>Emitted when the plugin's authorization state changes.</p>"},{"location":"flutter/Event/#boot","title":"Boot","text":"<pre><code>static const BOOT = \"boot\"\n</code></pre> <p>Emitted when the plugin boots.</p>"},{"location":"flutter/Event/#connectivitychange","title":"ConnectivityChange","text":"<pre><code>static const CONNECTIVITYCHANGE = \"connectivitychange\"\n</code></pre> <p>Emitted when the device's network connectivity changes.</p>"},{"location":"flutter/Event/#enabledchange","title":"EnabledChange","text":"<pre><code>static const ENABLEDCHANGE = \"enabledchange\"\n</code></pre> <p>Emitted when the plugin's enabled-state changes.</p>"},{"location":"flutter/Event/#geofence","title":"Geofence","text":"<pre><code>static const GEOFENCE = \"geofence\"\n</code></pre> <p>Emitted when a geofence transition occurs.</p>"},{"location":"flutter/Event/#geofenceschange","title":"GeofencesChange","text":"<pre><code>static const GEOFENCESCHANGE = \"geofenceschange\"\n</code></pre> <p>Emitted when geofences are added/removed/activated/deactivated.</p>"},{"location":"flutter/Event/#heartbeat","title":"Heartbeat","text":"<pre><code>static const HEARTBEAT = \"heartbeat\"\n</code></pre> <p>Emitted at the configured <code>Config.heartbeatInterval</code> when the plugin is running.</p>"},{"location":"flutter/Event/#http","title":"Http","text":"<pre><code>static const HTTP = \"http\"\n</code></pre> <p>Emitted when an HTTP request is sent to the configured <code>Config.http.url</code> endpoint.</p>"},{"location":"flutter/Event/#location","title":"Location","text":"<pre><code>static const LOCATION = \"location\"\n</code></pre> <p>Emitted when a location is recorded.</p>"},{"location":"flutter/Event/#motionchange","title":"MotionChange","text":"<pre><code>static const MOTIONCHANGE = \"motionchange\"\n</code></pre> <p>Emitted when a motion-change is detected.</p>"},{"location":"flutter/Event/#notification","title":"Notification","text":"<pre><code>static const NOTIFICATIONACTION = \"notificationaction\"\n</code></pre> <p>Emitted when a user interacts with a plugin-generated notification.</p>"},{"location":"flutter/Event/#powersavechange","title":"PowerSaveChange","text":"<pre><code>static const POWERSAVECHANGE = \"powersavechange\"\n</code></pre> <p>Emitted when the device enters or exits power-saving mode.</p>"},{"location":"flutter/Event/#providerchange","title":"ProviderChange","text":"<pre><code>static const PROVIDERCHANGE = \"providerchange\"\n</code></pre> <p>Emitted when the device location-provider changes state.</p>"},{"location":"flutter/Event/#schedule","title":"Schedule","text":"<pre><code>static const SCHEDULE = \"schedule\"\n</code></pre> <p>Emitted when a configured schedule is triggered.</p>"},{"location":"flutter/Event/#terminate","title":"Terminate","text":"<pre><code>static const TERMINATE = \"terminate\"\n</code></pre> <p>Emitted when the plugin is terminated.</p>"},{"location":"flutter/GeoConfig/","title":"GeoConfig","text":"<p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"flutter/GeoConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"flutter/GeoConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BackgroundGeolocation.ready or BackgroundGeolocation.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 10.0,\n    stopTimeout: 5,\n    locationAuthorizationRequest: \"Always\"\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"flutter/GeoConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"flutter/GeoConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BackgroundGeolocation.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"flutter/GeoConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BackgroundGeolocation.onProviderChange.</p>"},{"location":"flutter/GeoConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"flutter/GeoConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilter for full documentation.</p>"},{"location":"flutter/GeoConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 10.0,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    useSignificantChangesOnly: true\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0.0\n  )\n));\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/GeoConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 10.0,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    useSignificantChangesOnly: true\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0.0\n  )\n));\nawait BackgroundGeolocation.startGeofences();\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0.0\n  )\n));\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/GeoConfig/#members","title":"Members","text":""},{"location":"flutter/GeoConfig/#activitytype","title":"activityType","text":"<p><code>ActivityType</code><code>? activityType</code></p> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on ActivityType.</p> Name ActivityType.Other ActivityType.AutomotiveNavigation ActivityType.Fitness ActivityType.OtherNavigation ActivityType.Airborne <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    activityType: ActivityType.other,\n  ),\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>bool? allowIdenticalLocations\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"flutter/GeoConfig/#defertime","title":"deferTime","text":"<pre><code>int? deferTime\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"flutter/GeoConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>DesiredAccuracy</code><code>? desiredAccuracy</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>bool? disableElasticity\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"flutter/GeoConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>bool? disableLocationAuthorizationAlert\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BackgroundGeolocation.onProviderChange.</p>"},{"location":"flutter/GeoConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"flutter/GeoConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BackgroundGeolocation.onProviderChange - BackgroundGeolocation.start - BackgroundGeolocation.requestPermission</p> <pre><code>BackgroundGeolocation.onProviderChange((ProviderChangeEvent event) {\n  print('[onProviderChange]  ${event}');\n\n  if (!provider.enabled) {\n    print(\"Please enable location services\");\n  }\n});\n\nBackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    disableLocationAuthorizationAlert: true\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>double? distanceFilter\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"flutter/GeoConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"flutter/GeoConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"flutter/GeoConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>double? elasticityMultiplier\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"flutter/GeoConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>bool? enableTimestampMeta\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS Location.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"flutter/GeoConfig/#android_1","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"flutter/GeoConfig/#ios_1","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"flutter/GeoConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>int? fastestLocationUpdateInterval\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    fastestLocationUpdateInterval: 5000 // Receive passive updates at most every 5 s\n  )\n));\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"flutter/GeoConfig/#filter","title":"filter","text":"<p><code>LocationFilter</code><code>? filter</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"flutter/GeoConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilter.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilter.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilter.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilter.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilter.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilter.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilter.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilter.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilter.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilter.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilter.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilter.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.defaultProfile,\n      trackingAccuracyThreshold: 100.0,\n      odometerAccuracyThreshold: 20.0\n    )\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough,\n      useKalman: false\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>bool? geofenceInitialTriggerEntry\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"flutter/GeoConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>bool? geofenceModeHighAccuracy\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BackgroundGeolocation.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeoConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeoConfig.locationUpdateInterval - GeoConfig.distanceFilter - GeoConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 50.0,\n  )\n)).then((state) {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"flutter/GeoConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>int? geofenceProximityRadius\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p>"},{"location":"flutter/GeoConfig/#ios_2","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"flutter/GeoConfig/#android_2","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"flutter/GeoConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>Map&lt;String, dynamic&gt;? locationAuthorizationAlert\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    locationAuthorizationAlert: LocationAuthorizationAlert(\n      titleWhenNotEnabled: \"Location services are not enabled\",\n      titleWhenOff: \"Location services are OFF\",\n      instructions:\n        \"Please enable 'Always' in Location Services to allow background tracking.\",\n      cancelButton: \"Cancel\",\n      settingsButton: \"Open Settings\"\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/GeoConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<pre><code>String? locationAuthorizationRequest\n</code></pre> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"flutter/GeoConfig/#ios_3","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Start with When-In-Use\nBackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig( locationAuthorizationRequest: \"WhenInUse\" )\n));\n\nFuture&lt;void&gt; onClickStartTracking() async {\n  await BackgroundGeolocation.start();\n\n  // Upgrade to Always at any time.\n  BackgroundGeolocation.setConfig(Config(\n    geolocation: GeoConfig( locationAuthorizationRequest: \"Always\" )\n  ));\n}\n</code></pre>"},{"location":"flutter/GeoConfig/#android_3","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig( locationAuthorizationRequest: \"Always\" ),\n  app: AppConfig(\n    backgroundPermissionRationale: PermissionRationale(\n      title: \"Allow access to this device's location in the background?\",\n      message: \"To track your trips, please enable 'Allow all the time' location permission.\",\n      positiveAction: \"Change to Allow all the time\",\n      negativeAction: \"Cancel\"\n    )\n  )\n));\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"flutter/GeoConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>int? locationUpdateInterval\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    distanceFilter: 0.0,           // Required \u2014 otherwise this field is ignored.\n    locationUpdateInterval: 5000 // Request a fix every ~5 seconds\n  )\n));\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"flutter/GeoConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>bool? pausesLocationUpdatesAutomatically\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BackgroundGeolocation.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"flutter/GeoConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>bool? showsBackgroundLocationIndicator\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"flutter/GeoConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>int? stationaryRadius\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"flutter/GeoConfig/#ios_4","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"flutter/GeoConfig/#android_4","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"flutter/GeoConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>int? stopAfterElapsedMinutes\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BackgroundGeolocation.stop after this many minutes have elapsed since BackgroundGeolocation.start was called.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    stopAfterElapsedMinutes: 30\n  )\n)).then((state) {\n  BackgroundGeolocation.start();  // SDK will automatically stop in 30 minutes\n});\n</code></pre>"},{"location":"flutter/GeoConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>int? stopTimeout\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BackgroundGeolocation.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"flutter/GeoConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>bool? useSignificantChangesOnly\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"flutter/GeoConfig/#ios_5","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"flutter/GeoConfig/#android_5","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"flutter/Geofence/","title":"Geofence","text":"<p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"flutter/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"flutter/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeoConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"flutter/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BackgroundGeolocation.addGeofence for a single geofence, or BackgroundGeolocation.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p> <pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n));\n</code></pre> <pre><code>await BackgroundGeolocation.addGeofences([Geofence(\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n), Geofence(\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n)]);\n</code></pre>"},{"location":"flutter/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BackgroundGeolocation.onGeofence. Subscribe to changes in the actively monitored set with BackgroundGeolocation.onGeofencesChange.</p> <pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence] ${event.identifier} ${event.action}');\n});\n\nBackgroundGeolocation.onGeofencesChange((GeofencesChangeEvent event) {\n  final on = event.on;   // newly activated geofences\n  final off = event.off; // deactivated geofence identifiers\n\n  for (final geofence in on) {\n    print('[geofencesChange] activated: ${geofence.identifier}');\n  }\n  for (final identifier in off) {\n    print('[geofencesChange] deactivated: ${identifier}');\n  }\n});\n</code></pre> <p>Note</p> <p>When all geofences are removed, BackgroundGeolocation.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"flutter/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n));\n</code></pre>"},{"location":"flutter/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeoConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeoConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"flutter/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre> <pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre> <pre><code>final geofences = await BackgroundGeolocation.geofences;\nprint('[getGeofences] ${geofences}');\n</code></pre>"},{"location":"flutter/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BackgroundGeolocation.startGeofences instead of BackgroundGeolocation.start to monitor geofences without continuous location tracking. Use GeoConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence] ${event}');\n});\n\nawait BackgroundGeolocation.ready(Config(\n  http: HttpConfig( url: \"https://your.server.com/geofences\", autoSync: true ),\n  geolocation: GeoConfig( geofenceModeHighAccuracy: true )\n));\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/Geofence/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  if (event.identifier == \"DANGER_ZONE\") {\n    if (event.action == \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action == \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n));\n\nawait BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 10.0,\n  ),\n  http: HttpConfig( url: \"https://your.server.com/locations\", autoSync: true )\n));\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n));\n</code></pre>"},{"location":"flutter/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>await BackgroundGeolocation.addGeofences([Geofence(\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n), Geofence(\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n)]);\n</code></pre>"},{"location":"flutter/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n));\n</code></pre>"},{"location":"flutter/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre>"},{"location":"flutter/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"flutter/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>final geofences = await BackgroundGeolocation.geofences;\nprint('[getGeofences] ${geofences}');\n</code></pre>"},{"location":"flutter/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence] ${event}');\n});\n\nawait BackgroundGeolocation.ready(Config(\n  http: HttpConfig( url: \"https://your.server.com/geofences\", autoSync: true ),\n  geolocation: GeoConfig( geofenceModeHighAccuracy: true )\n));\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  if (event.identifier == \"DANGER_ZONE\") {\n    if (event.action == \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action == \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence(Geofence(\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n));\n\nawait BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    desiredAccuracy: DesiredAccuracy.high,\n    distanceFilter: 10.0,\n  ),\n  http: HttpConfig( url: \"https://your.server.com/locations\", autoSync: true )\n));\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"flutter/Geofence/#members","title":"Members","text":""},{"location":"flutter/Geofence/#entrystate","title":"entryState","text":"<p><code>int? entry</code><code>State</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"flutter/Geofence/#extras","title":"extras","text":"<pre><code>Map&lt;String, dynamic&gt;? extras\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"flutter/Geofence/#hits","title":"hits","text":"<pre><code>int? hits\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"flutter/Geofence/#identifier","title":"identifier","text":"<pre><code>String identifier\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"flutter/Geofence/#latitude","title":"latitude","text":"<pre><code>double? latitude\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"flutter/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>int? loiteringDelay\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"flutter/Geofence/#longitude","title":"longitude","text":"<pre><code>double? longitude\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"flutter/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>bool? notifyOnDwell\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"flutter/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>bool? notifyOnEntry\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeoConfig.geofenceInitialTriggerEntry</p>"},{"location":"flutter/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>bool? notifyOnExit\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"flutter/Geofence/#radius","title":"radius","text":"<pre><code>double? radius\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"flutter/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>double? stateUpdatedAt\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"flutter/Geofence/#vertices","title":"vertices","text":"<pre><code>List&lt;List&lt;double&gt;&gt;? vertices\n</code></pre> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>BackgroundGeolocation.addGeofence(Geofence(\n  identifier: 'Home',\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],  // &lt;-- [lat, lng]\n    [45.5182711292279, -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813, -73.60604928622278]\n  ]\n));\n</code></pre> <p> </p>"},{"location":"flutter/GeofenceAction/","title":"GeofenceAction","text":"<p>Geofence transition actions emitted by the SDK</p> <p>Used with GeofenceEvent from BackgroundGeolocation.onGeofence.</p> Action Meaning ENTER Device entered the geofence EXIT Device exited the geofence DWELL Device dwelled inside"},{"location":"flutter/GeofenceEvent/","title":"GeofenceEvent","text":"<p>Geofence transition delivered to BackgroundGeolocation.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>BackgroundGeolocation.onGeofence((GeofenceEvent event) {\n  print('[onGeofence] ${event.identifier} ${event.action} ${event.location}');\n});\n</code></pre>"},{"location":"flutter/GeofenceEvent/#members","title":"Members","text":""},{"location":"flutter/GeofenceEvent/#action","title":"action","text":"<pre><code>String action\n</code></pre> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"flutter/GeofenceEvent/#extras","title":"extras","text":"<pre><code>Map? extras\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"flutter/GeofenceEvent/#geofence","title":"geofence","text":"<p><code>Geofence</code><code>geofence</code></p> <p>The complete Geofence record that triggered this event.</p>"},{"location":"flutter/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>String identifier\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"flutter/GeofenceEvent/#location","title":"location","text":"<p><code>Location</code><code>location</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"flutter/GeofenceEvent/#timestamp","title":"timestamp","text":"<pre><code>String timestamp\n</code></pre> <p>ISO-8601 UTC timestamp from the device system clock at the moment the OS delivered the geofence event.</p>"},{"location":"flutter/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<p>Geofence monitoring change delivered to BackgroundGeolocation.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>BackgroundGeolocation.onGeofencesChange((GeofencesChangeEvent event) {\n  for (final geofence of event.on) {\n    print('[onGeofencesChange] Activated: ${geofence.identifier}');\n  }\n  for (final identifier of event.off) {\n    print('[onGeofencesChange] Deactivated: ${identifier}');\n  }\n});\n</code></pre>"},{"location":"flutter/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"flutter/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>List&lt;String&gt; off\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"flutter/GeofencesChangeEvent/#on","title":"on","text":"<p><code>List&lt;</code><code>Geofence</code><code>&gt; on</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"flutter/HeadlessEvent/","title":"HeadlessEvent Android only","text":"<p>Event delivered to a headless task registered via BackgroundGeolocation.registerHeadlessTask.</p> <p>Headless tasks allow the app to handle SDK events while running in the background without a UI \u2014 including after the app has been terminated by the user. Each task invocation receives a <code>HeadlessEvent</code> describing which SDK event occurred and carrying the associated payload.</p>"},{"location":"flutter/HeadlessEvent/#members","title":"Members","text":""},{"location":"flutter/HeadlessEvent/#name","title":"name","text":"<pre><code>String name\n</code></pre> <p>The SDK event that triggered this headless task invocation (e.g. <code>location</code>, <code>http</code>, <code>geofence</code>, <code>heartbeat</code>, <code>terminate</code>).</p>"},{"location":"flutter/HeartbeatEvent/","title":"HeartbeatEvent","text":"<p>Periodic heartbeat delivered to BackgroundGeolocation.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>BackgroundGeolocation.onHeartbeat((HeartbeatEvent event) {\n  print('[onHeartbeat] ${event.location}');\n\n  // Request a fresh location fix if needed.\n  final location = await BackgroundGeolocation.getCurrentPosition({\n    samples: 1\n  });\n  print('[onHeartbeat] fresh position: ${location}');\n});\n</code></pre>"},{"location":"flutter/HeartbeatEvent/#members","title":"Members","text":""},{"location":"flutter/HeartbeatEvent/#location","title":"location","text":"<p><code>Location</code><code>? location</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BackgroundGeolocation.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"flutter/HttpConfig/","title":"HttpConfig","text":"<p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"flutter/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"flutter/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    params: { user_id: 1234 }\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BackgroundGeolocation.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BackgroundGeolocation.getCount or fetch records with BackgroundGeolocation.getLocations.</p>"},{"location":"flutter/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"flutter/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"flutter/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BackgroundGeolocation.onConnectivityChange fires - BackgroundGeolocation.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p> <pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"flutter/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BackgroundGeolocation.start <code>\"stop\"</code> \u2014 BackgroundGeolocation.stop <code>\"startGeofences\"</code> \u2014 BackgroundGeolocation.startGeofences <code>\"changePace\"</code> <code>boolean</code> BackgroundGeolocation.changePace <code>\"setConfig\"</code> <code>{Config}</code> BackgroundGeolocation.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BackgroundGeolocation.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BackgroundGeolocation.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BackgroundGeolocation.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"flutter/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p>"},{"location":"flutter/HttpConfig/#the-sdk-log-provides-a-trace-of-the-full-http-lifecycle","title":"The SDK log provides a trace of the full HTTP lifecycle","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"flutter/HttpConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  )\n));\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig(Config(\n  http: HttpConfig( url: \"https://api.example.com\", autoSync: false )\n));\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"flutter/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"flutter/HttpConfig/#http-lifecycle-log","title":"HTTP lifecycle log","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre>"},{"location":"flutter/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer ...\" }\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.setConfig(Config(\n  http: HttpConfig( url: \"https://api.example.com\", autoSync: false )\n));\nawait BackgroundGeolocation.sync();\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  )\n));\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig(Config(\n  http: HttpConfig( url: \"https://api.example.com\", autoSync: false )\n));\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"flutter/HttpConfig/#members","title":"Members","text":""},{"location":"flutter/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>bool? autoSync\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BackgroundGeolocation.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BackgroundGeolocation.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"flutter/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>int? autoSyncThreshold\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BackgroundGeolocation.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"flutter/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>bool? batchSync\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"flutter/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>bool? disableAutoSyncOnCellular\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BackgroundGeolocation.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BackgroundGeolocation.sync</p>"},{"location":"flutter/HttpConfig/#headers","title":"headers","text":"<pre><code>Map&lt;String, dynamic&gt;? headers\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my.server.com\",\n    headers: {\n      Authorization: \"Bearer &lt;a secret key&gt;\",\n      \"X-FOO\": \"BAR\"\n    }\n  )\n));\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"flutter/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>int? maxBatchSize\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"flutter/HttpConfig/#method","title":"method","text":"<pre><code>String? method\n</code></pre> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my-server.com/locations\",\n    method: \"PUT\"\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#params","title":"params","text":"<pre><code>Map&lt;String, dynamic&gt;? params\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my-server.com/locations\",\n    params: {\n      user_id: 1234,\n      device_id: \"abc123\"\n    }\n  )\n));\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"flutter/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>String? rootProperty\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    rootProperty: \"myData\",\n    url: \"https://my.server.com\",\n  )\n));\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"flutter/HttpConfig/#timeout","title":"timeout","text":"<pre><code>int? timeout\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BackgroundGeolocation.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>BackgroundGeolocation.onHttp((HttpEvent response) {\n  if (!response.success) {\n    print('[onHttp] FAILURE: ${response}');\n  }\n});\n\nBackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my-server.com/locations\",\n    timeout: 3000\n  )\n));\n</code></pre>"},{"location":"flutter/HttpConfig/#url","title":"url","text":"<pre><code>String? url\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>BackgroundGeolocation.onHttp((HttpEvent event) {\n  print('[onHttp] ${event}');\n});\n\nBackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my-server.com/locations\",\n    params: { user_id: 1234 },\n    headers: { Authorization: \"Basic my-secret-key\" },\n    autoSync: true,\n    method: \"POST\"\n  )\n));\n</code></pre>"},{"location":"flutter/HttpEvent/","title":"HttpEvent","text":"<p>HTTP response payload delivered to BackgroundGeolocation.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>BackgroundGeolocation.onHttp((HttpEvent event) {\n  if (event.success) {\n    print('[onHttp] Upload succeeded: ${event.status}');\n  } else {\n    print('[onHttp] Upload failed: ${event.status} ${event.responseText}');\n  }\n});\n</code></pre>"},{"location":"flutter/HttpEvent/#members","title":"Members","text":""},{"location":"flutter/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>String responseText\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"flutter/HttpEvent/#status","title":"status","text":"<pre><code>int status\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"flutter/HttpEvent/#success","title":"success","text":"<pre><code>bool success\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"flutter/HttpMethod/","title":"HttpMethod","text":"<p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"flutter/KalmanProfile/","title":"KalmanProfile","text":"<p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"flutter/KalmanProfile/#members","title":"Members","text":""},{"location":"flutter/KalmanProfile/#aggressive","title":"Aggressive","text":"<p><code>KalmanProfile</code><code>.aggressive</code></p> <p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"flutter/KalmanProfile/#conservative","title":"Conservative","text":"<p><code>KalmanProfile</code><code>.conservative</code></p> <p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"flutter/KalmanProfile/#default","title":"Default","text":"<p><code>KalmanProfile</code><code>.defaultProfile</code></p> <p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"flutter/Location/","title":"Location","text":"<p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"flutter/Location/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"flutter/Location/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"flutter/Location/#javascript-schema","title":"JavaScript schema","text":"<pre><code>// Location object properties:\n// location.timestamp         // String (ISO-8601 UTC)\n// location.uuid              // String\n// location.isMoving          // bool\n// location.event             // String (motionchange|geofence|heartbeat)\n// location.age               // double (milliseconds)\n// location.odometer          // double (meters)\n// location.coords.latitude   // double\n// location.coords.longitude  // double\n// location.coords.accuracy   // double\n// location.coords.speed      // double\n// location.coords.heading    // double\n// location.coords.altitude   // double\n// location.activity.type     // String (still|on_foot|walking|running|in_vehicle|on_bicycle)\n// location.activity.confidence // int (0-100)\n// location.battery.level     // double\n// location.battery.isCharging // bool\n// location.extras            // Map? (optional custom metadata)\n</code></pre>"},{"location":"flutter/Location/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>// HTTP POST schema \u2014 the JSON body posted to your server:\n// {\n//   \"location\": {\n//     \"coords\": {\n//       \"latitude\":   double,\n//       \"longitude\":  double,\n//       \"accuracy\":   double,\n//       \"speed\":      double,\n//       \"heading\":    double,\n//       \"altitude\":   double,\n//       \"ellipsoidal_altitude\": double\n//     },\n//     \"extras\": { ... },\n//     \"activity\": { \"type\": String, \"confidence\": int },\n//     \"geofence\": { \"identifier\": String, \"action\": String },\n//     \"battery\":  { \"level\": double, \"is_charging\": bool },\n//     \"timestamp\": String,   // ISO-8601 UTC\n//     \"uuid\":      String,\n//     \"event\":     String,   // motionchange|geofence|heartbeat\n//     \"is_moving\": bool,\n//     \"odometer\":  double    // meters\n//   }\n// }\n</code></pre>"},{"location":"flutter/Location/#members","title":"Members","text":""},{"location":"flutter/Location/#activity","title":"activity","text":"<pre><code>Activity activity\n</code></pre> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p> <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  activityType = location.activity.type;\n  int confidence = location.activity.confidence;\n\n  print(\"[onLocation] Motion activity, type: $activityType, confidence: $confidence\");\n});\n</code></pre>"},{"location":"flutter/Location/#age","title":"age","text":"<pre><code>double age\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"flutter/Location/#battery","title":"battery","text":"<p><code>Battery</code><code>battery</code></p> <p>Device battery state at the time this location was recorded.</p> <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  bool isCharging = location.battery.isCharging;\n  double level    = location.battery.level;\n\n  print(\"[onLocation] Battery state, isCharging: $isCharging, level: $level\");\n});\n</code></pre>"},{"location":"flutter/Location/#coords","title":"coords","text":"<p><code>Coords</code><code>coords</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p> <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  double accuracy = location.coords.accuracy;\n  double lat      = location.coords.latitude;\n  double lng      = location.coords.longitude;\n  double speed    = location.coords.speed;\n  double heading  = location.coords.heading;\n  double altitude = location.coords.altitude;\n\n  print(\"[onLocation] $location\");\n});\n</code></pre>"},{"location":"flutter/Location/#event","title":"event","text":"<pre><code>String event\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"flutter/Location/#extras","title":"extras","text":"<pre><code>Map? extras\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"flutter/Location/#geofence","title":"geofence","text":"<p><code>GeofenceEvent</code><code>? geofence</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"flutter/Location/#is_moving","title":"is_moving","text":"<pre><code>bool isMoving\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"flutter/Location/#mock","title":"mock","text":"<pre><code>bool mock\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"flutter/Location/#odometer","title":"odometer","text":"<pre><code>double odometer\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"flutter/Location/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilter evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilter.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via Location.odometer_error.</li> </ul>"},{"location":"flutter/Location/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"flutter/Location/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilter - LocationFilter.odometerAccuracyThreshold - BackgroundGeolocation.resetOdometer - BackgroundGeolocation.getOdometer</p> <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  print('Distance traveled: ${location.odometer}');\n});\n\n// Reset at the start of a trip\nawait BackgroundGeolocation.resetOdometer();\n</code></pre>"},{"location":"flutter/Location/#recorded_at","title":"recorded_at","text":"<pre><code>dynamic recordedAt\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"flutter/Location/#sample","title":"sample","text":"<pre><code>bool sample\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BackgroundGeolocation.getCurrentPosition to find the most accurate fix. These samples are delivered to BackgroundGeolocation.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"flutter/Location/#timestamp","title":"timestamp","text":"<pre><code>dynamic timestamp\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"flutter/Location/#uuid","title":"uuid","text":"<pre><code>String uuid\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"flutter/LocationError/","title":"LocationError","text":"<p>Error codes returned when the native location API fails to fetch a location.</p> Code Error <code>0</code> Location unknown <code>1</code> Location permission denied <code>2</code> Network error <code>3</code> Attempt to initiate location-services in background with WhenInUse authorization <code>408</code> Location timeout <code>499</code> Location request cancelled"},{"location":"flutter/LocationFilter/","title":"LocationFilter","text":"<p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"flutter/LocationFilter/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"flutter/LocationFilter/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.defaultProfile,\n      trackingAccuracyThreshold: 100.0,\n      odometerAccuracyThreshold: 20.0\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough,\n      useKalman: false\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#members","title":"Members","text":""},{"location":"flutter/LocationFilter/#burstwindow","title":"burstWindow","text":"<pre><code>double? burstWindow\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"flutter/LocationFilter/#filterdebug","title":"filterDebug","text":"<pre><code>bool? filterDebug\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"flutter/LocationFilter/#kalmandebug","title":"kalmanDebug","text":"<pre><code>bool? kalmanDebug\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"flutter/LocationFilter/#kalmanprofile","title":"kalmanProfile","text":"<p><code>KalmanProfile</code><code>? kalmanProfile</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"flutter/LocationFilter/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>double? maxBurstDistance\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"flutter/LocationFilter/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>double? maxImpliedSpeed\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"flutter/LocationFilter/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>double? odometerAccuracyThreshold\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"flutter/LocationFilter/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>bool? odometerUseKalmanFilter\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilter.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilter.useKalman and LocationFilter.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"flutter/LocationFilter/#policy","title":"policy","text":"<p><code>LocationFilterPolicy</code><code>? policy</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BackgroundGeolocation.onLocation. For more granular tuning, see LocationFilter.trackingAccuracyThreshold, LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</p> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust\n    )\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough\n    )\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.conservative\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.conservative\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilter/#rollingwindow","title":"rollingWindow","text":"<pre><code>int? rollingWindow\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"flutter/LocationFilter/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>double? trackingAccuracyThreshold\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"flutter/LocationFilter/#usekalman","title":"useKalman","text":"<pre><code>bool? useKalman\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"flutter/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilter.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BackgroundGeolocation.onLocation.</li> <li>For more granular tuning, see LocationFilter.trackingAccuracyThreshold,   LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</li> </ul> <p>Examples</p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust\n    )\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough\n    )\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.conservative\n    )\n  )\n));\n</code></pre> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p>"},{"location":"flutter/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.adjust\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.passThrough\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    filter: LocationFilter(\n      policy: LocationFilterPolicy.conservative\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"flutter/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p><code>LocationFilterPolicy</code><code>.adjust</code></p> <p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilter.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"flutter/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p><code>LocationFilterPolicy</code><code>.conservative</code></p> <p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"flutter/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p><code>LocationFilterPolicy</code><code>.passThrough</code></p> <p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"flutter/LocationRequest/","title":"LocationRequest","text":"<p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeoConfig.locationAuthorizationRequest</p>"},{"location":"flutter/LogLevel/","title":"LogLevel","text":"<p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"flutter/LogLevel/#members","title":"Members","text":""},{"location":"flutter/LogLevel/#debug","title":"Debug","text":"<p><code>LogLevel</code><code>.debug</code></p> <p>Developer-level debug output.</p>"},{"location":"flutter/LogLevel/#error","title":"Error","text":"<p><code>LogLevel</code><code>.error</code></p> <p>Log only critical failures.</p>"},{"location":"flutter/LogLevel/#info","title":"Info","text":"<p><code>LogLevel</code><code>.info</code></p> <p>Operational information.</p>"},{"location":"flutter/LogLevel/#off","title":"Off","text":"<p><code>LogLevel</code><code>.off</code></p> <p>Disable all logging.</p>"},{"location":"flutter/LogLevel/#verbose","title":"Verbose","text":"<p><code>LogLevel</code><code>.verbose</code></p> <p>Maximum detail.</p>"},{"location":"flutter/LogLevel/#warning","title":"Warning","text":"<p><code>LogLevel</code><code>.warning</code></p> <p>Log warnings + errors.</p>"},{"location":"flutter/Logger/","title":"Logger","text":"<p>SDK logging API \u2014 access via BackgroundGeolocation.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"flutter/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"flutter/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>final log = await Logger.getLog();\n</code></pre>"},{"location":"flutter/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional SQLQuery to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>final log = await Logger.getLog();\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"flutter/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>BackgroundGeolocation.onLocation((Location location) {\n  Logger.debug(\"Location received: \" + location.uuid);\n});\n</code></pre>"},{"location":"flutter/Logger/#examples","title":"Examples","text":"<pre><code>final log = await Logger.getLog();\nprint('[log] ${log}');\n</code></pre> <pre><code>await Logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre> <pre><code>await Logger.emailLog(\"support@example.com\");\n</code></pre>"},{"location":"flutter/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>final log = await Logger.getLog();\nprint('[log] ${log}');\n</code></pre>"},{"location":"flutter/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>await Logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre>"},{"location":"flutter/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>await Logger.emailLog(\"support@example.com\");\n</code></pre>"},{"location":"flutter/Logger/#members","title":"Members","text":""},{"location":"flutter/Logger/#debug","title":"debug","text":"<pre><code>static Future&lt;void&gt; debug(String msg)\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>Logger.debug(\"This is a debug message\");\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"flutter/Logger/#destroylog","title":"destroyLog","text":"<pre><code>static Future&lt;bool&gt; destroyLog()\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>Logger.destroyLog();\n</code></pre>"},{"location":"flutter/Logger/#emaillog","title":"emailLog","text":"<p><code>static Future&lt;bool&gt; emailLog(String email, [</code><code>SQLQuery</code><code>? query])</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional SQLQuery to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>Logger.emailLog('foo@bar.com').then((bool success) {\n  print('[emailLog] success');\n}).catchError((error) {\n  print('[emailLog] FAILURE: ${error}');\n});\n\n// Or constrain results by providing a [SQLQuery]:\nLogger.emailLog('foo@bar.com', SQLQuery(\n  start: DateTime.parse('2019-10-20 09:00'),\n  end: DateTime.parse('2019-10-20 11:59')\n)).then((bool success) {\n  print('[emailLog] success');\n}).catchError((error) {\n  print('[emailLog] FAILURE: ${error}');\n});\n</code></pre>"},{"location":"flutter/Logger/#error","title":"error","text":"<pre><code>static Future&lt;void&gt; error(String msg)\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>Logger.error(\"Something BAD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"flutter/Logger/#getlog","title":"getLog","text":"<p><code>static Future&lt;String&gt; getLog([</code><code>SQLQuery</code><code>? query])</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional SQLQuery to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Fetch entire contents of log.\nString log = await Logger.getLog();\n// Warning:  this string could be several megabytes.\nprint('[log] success: ${log}');\n\n// Or constrain results between optionl start/end dates using a SQLQuery\nString log = await Logger.getLog(SQLQuery(\n  start: DateTime.parse('2019-10-21 13:00'),  // &lt;-- optional HH:mm:ss\n  end: DateTime.parse('2019-10-22')\n));\n\n// Or just a start date\nString log = await Logger.getLog(SQLQuery(\n  start: DateTime.parse('2019-10-21 13:00')\n));\n\n// Or just an end date\nString log = await Logger.getLog(SQLQuery(\n  end: DateTime.parse('2019-10-21')\n));\n</code></pre>"},{"location":"flutter/Logger/#info","title":"info","text":"<pre><code>static Future&lt;void&gt; info(String msg)\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>Logger.info(\"Something informative\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"flutter/Logger/#notice","title":"notice","text":"<pre><code>static Future&lt;void&gt; notice(String msg)\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>Logger.notice(\"A Notice\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"flutter/Logger/#uploadlog","title":"uploadLog","text":"<p><code>static Future&lt;bool&gt; uploadLog(String url, [</code><code>SQLQuery</code><code>? query])</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional SQLQuery to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"flutter/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>Logger.uploadLog('https://my.server.com/users/123/logs').then((bool success) {\n  print('[uploadLog] success');\n}).catchError((error) {\n  print('[uploadLog] FAILURE: ${error}');\n});\n\n// Or constrain results by providing a [SQLQuery]:\nLogger.uploadLog('https://my.server.com/users/123/logs', SQLQuery(\n  start: DateTime.parse('2019-10-20 09:00'),\n  end: DateTime.parse('2019-10-20 11:59')\n)).then((bool success) {\n  print('[uploadLog] success');\n}).catchError((error) {\n  print('[uploadLog] FAILURE: ${error}');\n});\n</code></pre>"},{"location":"flutter/Logger/#warn","title":"warn","text":"<pre><code>static Future&lt;void&gt; warn(String msg)\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>Logger.warn(\"Something WEIRD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"flutter/LoggerConfig/","title":"LoggerConfig","text":"<p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"flutter/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"flutter/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 7,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"flutter/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"flutter/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"flutter/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"flutter/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"flutter/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"flutter/LoggerConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 7,\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: false,\n    logLevel: LogLevel.info,\n    logMaxDays: 3,\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.setConfig(Config(\n  logger: LoggerConfig(\n    logLevel: LogLevel.off,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 7,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: false,\n    logLevel: LogLevel.info,\n    logMaxDays: 3,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>BackgroundGeolocation.setConfig(Config(\n  logger: LoggerConfig(\n    logLevel: LogLevel.off,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 7,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    debug: false,\n    logLevel: LogLevel.info,\n    logMaxDays: 3,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>Config({\n  logger: LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 3,\n  )\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig(Config(\n  logger: LoggerConfig(\n    logLevel: LogLevel.off,\n  )\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#members","title":"Members","text":""},{"location":"flutter/LoggerConfig/#debug","title":"debug","text":"<pre><code>bool? debug\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"flutter/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"flutter/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"flutter/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>LogLevel</code><code>? logLevel</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    logLevel: LogLevel.verbose\n  ),\n));\n</code></pre>"},{"location":"flutter/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>int? logMaxDays\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>BackgroundGeolocation.ready(Config(\n  logger: LoggerConfig(\n    logMaxDays: 3\n  )\n));\n</code></pre>"},{"location":"flutter/MotionActivity/","title":"MotionActivity","text":"<p>The motion activity reported by the device at the time a Location was recorded.</p>"},{"location":"flutter/MotionActivity/#members","title":"Members","text":""},{"location":"flutter/MotionActivity/#confidence","title":"confidence","text":"<pre><code>int confidence\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"flutter/MotionActivity/#type","title":"type","text":"<pre><code>String type\n</code></pre> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"flutter/MotionActivityEvent/","title":"MotionActivityEvent","text":"<p>Activity recognition result delivered to BackgroundGeolocation.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>BackgroundGeolocation.onActivityChange((MotionActivityEvent event) {\n  print('[onActivityChange] ${event.activity} ${event.confidence}');\n});\n</code></pre>"},{"location":"flutter/MotionActivityType/","title":"MotionActivityType","text":"<p>Reported device motion activity names (legacy RN + native).</p>"},{"location":"flutter/MotionChangeEvent/","title":"MotionChangeEvent","text":"<p>Motion-state change delivered to BackgroundGeolocation.onMotionChange.</p> <p>The SDK fires this event each time it transitions between the moving and stationary states. A location is always recorded at the moment of transition.</p> <pre><code>BackgroundGeolocation.onMotionChange((MotionChangeEvent event) {\n  if (event.isMoving) {\n    print('[onMotionChange] Device is moving: ${event.location}');\n  } else {\n    print('[onMotionChange] Device is stationary: ${event.location}');\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/","title":"NotificationConfig Android only","text":"<p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"flutter/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"flutter/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    notification: Notification(\n      title: \"The Title\",\n      text: \"The Text\"\n    )\n  )\n));\n\n// Update notification fields at runtime.\n// Only changed keys must be provided; existing values persist.\nBackgroundGeolocation.setConfig(Config(\n  app: AppConfig(\n    notification: Notification(\n      title: \"The New Title\",\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"flutter/NotificationConfig/#members","title":"Members","text":""},{"location":"flutter/NotificationConfig/#actions","title":"actions","text":"<pre><code>List&lt;String&gt;? actions\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"flutter/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    notification: Notification(\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    )\n  )\n));\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) {\n  print('[onNotificationAction] -  ${buttonId}');\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    actions: [  // &lt;-- register button listeners\n      \"notificationButtonPause\"\n    ]\n  )\n));\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((String buttonId) {\n  print(\"[onNotificationAction] - ${buttonId}\");\n  switch(buttonId) {\n    case 'notificationButtonPause':\n      BackgroundGeolocation.changePace(false);\n      break;\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>String? channelId\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"flutter/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>String? channelName\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    channelName: \"Location Tracker\"\n  )\n));\n\n// or with #setConfig\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    channelName: \"My new channel name\"\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#color","title":"color","text":"<pre><code>String? color\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"flutter/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>String? largeIcon\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// 1. drawable\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    largeIcon: \"drawable/my_custom_notification_large_icon\"\n  )\n});\n\n// 2. mipmap\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    largeIcon: \"mipmap/my_custom_notification_large_icon\"\n  )\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#layout","title":"layout","text":"<pre><code>String? layout\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"flutter/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    layout: \"my_notification_layout\",  // &lt;-- custom layout xml file\n    title: \"The Notification Title\",\n    text: \"The Notification Text\",\n    smallIcon: \"mipmap/my_small_icon\", // &lt;-- defaults to app icon\n    largeIcon: \"mipmap/my_large_icon\"\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    strings: {\n      \"myCustomElement\": \"My Custom Element Text\"\n    }\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    actions: [  // &lt;-- register button listeners\n      \"notificationButtonFoo\",\n      \"notificationButtonBar\"\n    ]\n  )\n));\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((String buttonId) {\n  print(\"[onNotificationAction] - ${buttonId}\");\n  switch(buttonId) {\n    case 'notificationButtonFoo':\n      break;\n    case 'notificationButtonBar':\n      break;\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>// Listen to custom notification button clicks (notification.actions)\nBackgroundGeolocation.onNotificationAction((buttonId) {\n  print(\"[onNotificationAction] ${buttonId}\");\n  switch(buttonId) {\n    case 'notificationButtonFoo':\n      // Handle button click on [Foo]\n      break;\n    case 'notificationButtonBar':\n      // Handle button click on [Bar]\n      break;\n  }\n});\n\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    title: \"The title\",\n    text: \"The text\",\n    layout: \"notification_layout\",\n    actions: [  // &lt;-- register button listeners\n      \"notificationButtonFoo\",\n      \"notificationButtonBar\"\n    ]\n  )\n));\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    notification: Notification(\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    )\n  )\n));\n\nBackgroundGeolocation.onNotificationAction((buttonId) {\n  print('[onNotificationAction] ${buttonId}');\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  app: AppConfig(\n    notification: Notification(\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    )\n  )\n));\n\nBackgroundGeolocation.onNotificationAction((buttonId) {\n  print('[onNotificationAction] ${buttonId}');\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#priority","title":"priority","text":"<p><code>NotificationPriority</code><code>? priority</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    priority: NotificationPriority.min\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>String? smallIcon\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// 1. drawable\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    smallIcon: \"drawable/my_custom_notification_small_icon\"\n  )\n));\n\n// 2. mipmap\nBackgroundGeolocation.ready(Config(\n  notification: Notification(\n    smallIcon: \"mipmap/my_custom_notification_small_icon\"\n  }\n});\n</code></pre>"},{"location":"flutter/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>bool? sticky\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"flutter/NotificationConfig/#strings","title":"strings","text":"<pre><code>Map&lt;String, String&gt;? strings\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  notification: Notification(\n    strings: {\n      \"myCustomElement\": \"My Custom Element Text\"\n    }\n  )\n));\n</code></pre>"},{"location":"flutter/NotificationConfig/#text","title":"text","text":"<pre><code>String? text\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"flutter/NotificationConfig/#title","title":"title","text":"<pre><code>String? title\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"flutter/NotificationPriority/","title":"NotificationPriority","text":"<p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"flutter/NotificationPriority/#members","title":"Members","text":""},{"location":"flutter/NotificationPriority/#default","title":"Default","text":"<p><code>NotificationPriority</code><code>.defaultPriority</code></p> <p>Default notification priority (normal weighting).</p>"},{"location":"flutter/NotificationPriority/#high","title":"High","text":"<p><code>NotificationPriority</code><code>.high</code></p> <p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"flutter/NotificationPriority/#low","title":"Low","text":"<p><code>NotificationPriority</code><code>.low</code></p> <p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"flutter/NotificationPriority/#max","title":"Max","text":"<p><code>NotificationPriority</code><code>.max</code></p> <p>Same as NotificationPriority.High.</p>"},{"location":"flutter/NotificationPriority/#min","title":"Min","text":"<p><code>NotificationPriority</code><code>.min</code></p> <p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"flutter/PermissionRationale/","title":"PermissionRationale Android only","text":"<p>Configures the background location permission rationale dialog on Android 11 and later.</p> <p>Android 11 removed the Allow all the time option from the system location permission dialog. Apps may instead show a custom rationale dialog that explains why background access is needed and routes the user to the system Location Permissions screen to grant it explicitly.</p>"},{"location":"flutter/PermissionRationale/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Template tags</li> </ul>"},{"location":"flutter/PermissionRationale/#overview","title":"Overview","text":"<p>The SDK presents this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code> and AppConfig.backgroundPermissionRationale is configured.</p> <p>Selecting the dialog's positive action sends the user directly to the system Location Permissions screen, where they must enable Allow all the time themselves.</p> <ul> <li>The dialog is shown once only. After the user presses the positive action   it will not appear again. Pressing Cancel does not count as acceptance.</li> <li>If the user later resets your app's Location permission to Ask every time,   the dialog becomes eligible to be shown again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready(Config(\n  geolocation: GeoConfig(\n    locationAuthorizationRequest: 'Always'\n  ),\n  app: AppConfig(\n    backgroundPermissionRationale: PermissionRationale(\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location access.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    )\n  )\n));\n</code></pre>"},{"location":"flutter/PermissionRationale/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside any rationale field using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> \"Allow all the time\" (API 30+) Localized label for the background location permission option. <code>{applicationName}</code> App name Localized application name from <code>AndroidManifest</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Android 11 Location Updates</p>"},{"location":"flutter/PermissionRationale/#members","title":"Members","text":""},{"location":"flutter/PermissionRationale/#message","title":"message","text":"<pre><code>String? message\n</code></pre> <p>Body text of the dialog. Explain why your app needs background location access, similar in purpose to the iOS <code>NSLocationAlwaysAndWhenInUseUsageDescription</code>.</p>"},{"location":"flutter/PermissionRationale/#negativeaction","title":"negativeAction","text":"<pre><code>String? negativeAction\n</code></pre> <p>Text to display on the negative action button (e.g. \"Cancel\").</p>"},{"location":"flutter/PermissionRationale/#positiveaction","title":"positiveAction","text":"<pre><code>String? positiveAction\n</code></pre> <p>Text to display on the positive action button.</p>"},{"location":"flutter/PermissionRationale/#title","title":"title","text":"<pre><code>String? title\n</code></pre> <p>Title for the Android background location permission dialog.</p>"},{"location":"flutter/PersistMode/","title":"PersistMode","text":"<p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"flutter/PersistMode/#members","title":"Members","text":""},{"location":"flutter/PersistMode/#all","title":"All","text":"<p><code>PersistMode</code><code>.all</code></p> <p>Persist both location and geofence data.</p>"},{"location":"flutter/PersistMode/#geofence","title":"Geofence","text":"<p><code>PersistMode</code><code>.geofence</code></p> <p>Persist geofence data only.</p>"},{"location":"flutter/PersistMode/#location","title":"Location","text":"<p><code>PersistMode</code><code>.location</code></p> <p>Persist location data only.</p>"},{"location":"flutter/PersistMode/#none","title":"None","text":"<p><code>PersistMode</code><code>.none</code></p> <p>Do not persist any data.</p>"},{"location":"flutter/PersistenceConfig/","title":"PersistenceConfig","text":"<p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"flutter/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"flutter/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded Location and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    maxDaysToPersist: 3,\n    maxRecordsToPersist: 1000,\n    extras: {\n      'user_id': 123,\n      'appVersion': '1.2.3'\n    },\n    persistMode: PersistMode.all\n  )\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BackgroundGeolocation.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BackgroundGeolocation.getCount or BackgroundGeolocation.getLocations.</p>"},{"location":"flutter/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BackgroundGeolocation.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"flutter/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"flutter/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>final location = await BackgroundGeolocation.getCurrentPosition({\n  persist: true,\n  extras: { get_current_position: true },\n  samples: 3,\n});\n</code></pre>"},{"location":"flutter/PersistenceConfig/#examples","title":"Examples","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.all,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  ),\n  http: HttpConfig(\n    url: 'https://example.com/locations',\n    autoSync: true,\n  )\n));\n</code></pre> <pre><code>final pending = await BackgroundGeolocation.count;\nprint('Pending records: ${pending}');\n\n// Purge all records\nfinal ok = await BackgroundGeolocation.destroyLocations();\nprint('Destroyed all records? ${ok}');\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate: '{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }',\n    geofenceTemplate: '{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }',\n  ),\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>final state = await BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.all,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  ),\n  http: HttpConfig(\n    url: 'https://example.com/locations',\n    autoSync: true,\n  )\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>final pending = await BackgroundGeolocation.count;\nprint('Pending records: ${pending}');\n\n// Purge all records\nfinal ok = await BackgroundGeolocation.destroyLocations();\nprint('Destroyed all records? ${ok}');\n</code></pre>"},{"location":"flutter/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate: '{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }',\n    geofenceTemplate: '{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }',\n  ),\n));\n</code></pre> <pre><code>final pending = await BackgroundGeolocation.count;\nprint('Pending records: ${pending}');\n\n// Purge all records\nfinal ok = await BackgroundGeolocation.destroyLocations();\nprint('Destroyed all records? ${ok}');\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate: '{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }',\n    geofenceTemplate: '{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }',\n  ),\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#members","title":"Members","text":""},{"location":"flutter/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>bool? disableProviderChangeRecord\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BackgroundGeolocation.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    disableProviderChangeRecord: true\n )\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#extras","title":"extras","text":"<pre><code>Map&lt;String, dynamic&gt;? extras\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: \"https://my-server.com/locations\",\n    params: {\n      device_id: \"abc123\"  // appended to root JSON of each POST request\n    }\n  ),\n  persistence: PersistenceConfig(\n    extras: {\n      route_id: 1234       // merged onto each location record\n    }\n  )\n));\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"flutter/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>String? geofenceTemplate\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate:\n      '{ \"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\" }'\n  )\n));\n\n// Or a compact Array form:\nBackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate:\n      '[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]'\n  )\n));\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  )\n));\n</code></pre> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  )\n));\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"flutter/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"flutter/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  )\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  )\n));\n</code></pre>"},{"location":"flutter/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>String? locationTemplate\n</code></pre> <p>Optional custom ERB template for reshaping Location JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate:\n      '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}'\n  )\n));\n\n// Or use a compact Array template:\nBackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate:\n      '[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]'\n  )\n));\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    locationTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  )\n));\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready(Config(\n persistence: PersistenceConfig(\n  locationTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n )\n));\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"flutter/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  http: HttpConfig(\n    url: 'https://my.server.com/locations',\n    rootProperty: 'data',\n  ),\n  persistence: PersistenceConfig(\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}',\n    extras: { foo: \"bar\" }\n  )\n));\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"flutter/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"flutter/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<pre><code>String? locationsOrderDirection\n</code></pre> <p>Sort order used when selecting records for upload or returning them from BackgroundGeolocation.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"flutter/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>int? maxDaysToPersist\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"flutter/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>int? maxRecordsToPersist\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"flutter/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>PersistMode</code><code>? persistMode</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BackgroundGeolocation.onLocation and BackgroundGeolocation.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BackgroundGeolocation.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"flutter/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>final String? timestampFormat\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>BackgroundGeolocation.ready(Config(\n  persistence: PersistenceConfig(\n    timestampFormat: 'epoch',\n  ),\n));\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"flutter/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<p>Location-services provider state delivered to BackgroundGeolocation.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>BackgroundGeolocation.onProviderChange((ProviderChangeEvent event) {\n  print('[onProviderChange] enabled: ${event.enabled} status: ${event.status} gps: ${event.gps} network: ${event.network} accuracyAuthorization: ${event.accuracyAuthorization}');\n\n  if (!event.enabled) {\n    // Prompt the user to re-enable location services.\n  }\n});\n</code></pre>"},{"location":"flutter/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"flutter/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<pre><code>int accuracyAuthorization\n</code></pre> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p> <pre><code>BackgroundGeolocation.onProviderChange((ProviderChangeEvent event) {\n  int authorizationStatus = event.authorizationStatus;\n  if (authorizationStatus == ProviderChangeEvent.ACCURACY_AUTHORIZATION_REDUCED) {\n    BackgroundGeolocaiton.requestTemporaryFullAccuracy(\"MyPurposeString\").then((int accuracyAuthorization) {\n      print(\"RESULT:  $accuracyAuthorization\");\n    }).catchError((error) {\n      print(\"ERROR:  Failed to show dialog: $error\");\n    });\n  }\n});\n</code></pre>"},{"location":"flutter/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>bool enabled\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"flutter/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>bool gps\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"flutter/ProviderChangeEvent/#network","title":"network","text":"<pre><code>bool network\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"flutter/ProviderChangeEvent/#status","title":"status","text":"<pre><code>int status\n</code></pre> <p>Platform authorization status code. Use the AuthorizationStatus constants to interpret this value.</p>"},{"location":"flutter/SQLQuery/","title":"SQLQuery","text":"<p>Constrains a log query by date range, sort order, and record count.</p> <p>Pass to Logger.getLog, Logger.emailLog, or Logger.uploadLog to filter which log entries are included.</p> <pre><code>// Date range\nfinal log = await Logger.getLog();\n\n// First 100 records ascending\nawait Logger.emailLog(\"foo@bar.com\");\n\n// Upload a specific time window\nawait Logger.uploadLog(\"https://my.server.com/users/123/logs\", {\n  start: Date.parse(\"2019-10-20 09:00\"),\n  end:   Date.parse(\"2019-10-20 11:59\")\n});\n</code></pre>"},{"location":"flutter/SQLQuery/#members","title":"Members","text":""},{"location":"flutter/SQLQuery/#end","title":"end","text":"<pre><code>DateTime? end\n</code></pre> <p>End of the query window (unix timestamp in milliseconds).</p>"},{"location":"flutter/SQLQuery/#limit","title":"limit","text":"<pre><code>int? limit\n</code></pre> <p>Maximum number of records to return.</p>"},{"location":"flutter/SQLQuery/#order","title":"order","text":"<pre><code>int? order\n</code></pre> <p>Sort order for results: <code>SQLQueryOrder.Asc</code> (oldest first) or <code>SQLQueryOrder.Desc</code> (newest first).</p>"},{"location":"flutter/SQLQuery/#start","title":"start","text":"<pre><code>DateTime? start\n</code></pre> <p>Start of the query window (unix timestamp in milliseconds).</p>"},{"location":"flutter/SQLQueryOrder/","title":"SQLQueryOrder","text":"<p>Sort order for SQLQuery.order.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"flutter/Sensors/","title":"Sensors","text":"<p>Availability of motion sensors on the device, as returned by BackgroundGeolocation.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>final sensors = await BackgroundGeolocation.sensors;\nprint('[Sensors] ${sensors}');\n</code></pre>"},{"location":"flutter/Sensors/#members","title":"Members","text":""},{"location":"flutter/Sensors/#accelerometer","title":"accelerometer","text":"<pre><code>bool accelerometer\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"flutter/Sensors/#gyroscope","title":"gyroscope","text":"<pre><code>bool gyroscope\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"flutter/Sensors/#magnetometer","title":"magnetometer","text":"<pre><code>bool magentometer\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"flutter/Sensors/#motion_hardware","title":"motion_hardware","text":"<pre><code>bool? motionHardware\n</code></pre> <p>iOS only <code>true</code> when the device has a dedicated M-series motion co-processor (iPhone 5S and later). The M-series co-processor offloads motion activity recognition from the main CPU, enabling low-power step counting and activity detection.</p>"},{"location":"flutter/Sensors/#platform","title":"platform","text":"<pre><code>String platform\n</code></pre> <p>OS platform name: <code>\"ios\"</code> or <code>\"android\"</code>.</p>"},{"location":"flutter/Sensors/#significant_motion","title":"significant_motion","text":"<pre><code>bool? significantMotion\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"flutter/State/","title":"State","text":"<p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>final state = await BackgroundGeolocation.state;\nprint('[state] enabled: ${state.enabled}, trackingMode: ${state.trackingMode}');\n</code></pre>"},{"location":"flutter/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"flutter/State/#members","title":"Members","text":""},{"location":"flutter/State/#activity","title":"activity","text":"<p><code>ActivityConfig</code><code>get activity</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. See ActivityConfig.</p>"},{"location":"flutter/State/#app","title":"app","text":"<p><code>AppConfig</code><code>get app</code></p> <p>App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. See AppConfig.</p>"},{"location":"flutter/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>bool didDeviceReboot\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"flutter/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>bool didLaunchInBackground\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"flutter/State/#enabled","title":"enabled","text":"<pre><code>bool enabled\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BackgroundGeolocation.start or BackgroundGeolocation.startGeofences has been called and not yet stopped.</p>"},{"location":"flutter/State/#geolocation","title":"geolocation","text":"<p><code>GeoConfig</code><code>get geolocation</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. See GeoConfig.</p>"},{"location":"flutter/State/#http","title":"http","text":"<p><code>HttpConfig</code><code>get http</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. See HttpConfig.</p>"},{"location":"flutter/State/#isfirstboot","title":"isFirstBoot","text":"<pre><code>bool isFirstBoot\n</code></pre> <p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"flutter/State/#ismoving","title":"isMoving","text":"<pre><code>bool? isMoving\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// Toggle the SDK's motion state from stationary to moving.\nBackgroundGeolocation.onMotionChange((MotionChangeEvent event) {\n  print('[onMotionChange] isMoving? ${event.isMoving}');\n});\n\nawait BackgroundGeolocation.changePace(true);\n// State.isMoving is now true.\n</code></pre>"},{"location":"flutter/State/#logger","title":"logger","text":"<p><code>LoggerConfig</code><code>get logger</code></p> <p>Debug logging, log level, and log retention. See LoggerConfig.</p>"},{"location":"flutter/State/#odometer","title":"odometer","text":"<pre><code>double odometer\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BackgroundGeolocation.setOdometer - BackgroundGeolocation.getOdometer</p>"},{"location":"flutter/State/#persistence","title":"persistence","text":"<p><code>PersistenceConfig</code><code>get persistence</code></p> <p>SQLite storage, TTL, record limits, and custom extras. See PersistenceConfig.</p>"},{"location":"flutter/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>bool schedulerEnabled\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BackgroundGeolocation.startSchedule has been called. BackgroundGeolocation.stopSchedule sets this to <code>false</code>.</p>"},{"location":"flutter/State/#trackingmode","title":"trackingMode","text":"<pre><code>int trackingMode\n</code></pre> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>await BackgroundGeolocation.start();\nfinal state = await BackgroundGeolocation.state;\nprint('Tracking mode: ${state.trackingMode}');\n// &gt; 'Tracking mode: 1'\n\nawait BackgroundGeolocation.startGeofences();\nstate = await BackgroundGeolocation.state;\nprint('Tracking mode: ${state.trackingMode}');\n// &gt; 'Tracking mode: 0'\n</code></pre>"},{"location":"flutter/Subscription/","title":"Subscription","text":"<p>Handle returned by every <code>BackgroundGeolocation.on*</code> event-listener method.</p> <p><code>Subscription</code> exposes a single <code>remove</code> method used to stop listening to an event and free associated resources. Always call <code>remove()</code> when a listener is no longer needed to prevent memory leaks.</p>"},{"location":"flutter/Subscription/#removing-an-event-listener","title":"Removing an event-listener","text":"<pre><code>BackgroundGeolocation.onLocation((Location location) {\n  print('[onLocation] ${location}');\n});\n\n// Later, when the listener is no longer needed:\nsubscription.remove();\n</code></pre>"},{"location":"flutter/Subscription/#managing-multiple-subscriptions","title":"Managing multiple subscriptions","text":"<p>Collect subscriptions in an array and remove them all at once \u2014 for example when a view is destroyed.</p> <pre><code>List&lt;dynamic&gt; subscriptions = [];\n\nvoid addListeners() {\n  subscriptions.addAll([\n    BackgroundGeolocation.onLocation((Location location) {\n      print('[onLocation] ${location}');\n    }),\n    BackgroundGeolocation.onMotionChange((MotionChangeEvent event) {\n      print('[onMotionChange] ${event}');\n    }),\n    BackgroundGeolocation.onEnabledChange((enabled) {\n      print('[onEnabledChange] ${enabled}');\n    }),\n  ]);\n}\n\nvoid removeListeners() {\n  for (final sub in subscriptions) {\n    sub.remove();\n  }\n  subscriptions.clear();\n}\n</code></pre>"},{"location":"flutter/TrackingMode/","title":"TrackingMode","text":"<p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"flutter/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"flutter/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>final url = \"http://tracker.transistorsoft.com\";\nfinal orgname = \"my-company-name\";\nfinal username = \"my-username\";\n\n// Fetch a token from the server (the SDK caches it automatically).\nfinal token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  orgname, username, url\n);\n\nBackgroundGeolocation.ready(Config(\n  transistorAuthorizationToken: token\n));\n</code></pre>"},{"location":"flutter/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"flutter/TransistorAuthorizationService/#destroy","title":"destroy","text":"<pre><code>static Future&lt;bool?&gt; destroy(String url)\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p> <pre><code>// defaults to http://tracker.transistorsoft.com\nawait TransistorAuthorizationToken.destroy();\n\n// If using a custom instance of Demo Server:\n/// await TransistorAuthorizationToken.destroy(\"http://192.168.0.100:9000\");\n</code></pre>"},{"location":"flutter/TransistorAuthorizationService/#findorcreate","title":"findOrCreate","text":"<p><code>static Future&lt;</code><code>TransistorAuthorizationToken</code><code>&gt; findOrCreate( String orgname, String username, [String url = _DEFAULT_TRANSISTOR_URL])</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>String orgname = \"my-company-name\";\nString usernmae = \"my-username\";\n\n// Fetch an authoriztion token from server.  The SDK will cache the received token.\nTransistorAuthorizationToken token = await\n  TransistorAuthorizationToken.findOrCreate(orgname, username);\n\nBackgroundGeolocation.ready(Config(\n  transistorAuthorizationToken: token\n))\n</code></pre>"},{"location":"flutter/TransistorAuthorizationToken/","title":"TransistorAuthorizationToken","text":"<p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"flutter/TransistorAuthorizationToken/#members","title":"Members","text":""},{"location":"flutter/TransistorAuthorizationToken/#accesstoken","title":"accessToken","text":"<pre><code>String accessToken\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"flutter/TransistorAuthorizationToken/#expires","title":"expires","text":"<pre><code>int expires\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"flutter/TransistorAuthorizationToken/#refreshtoken","title":"refreshToken","text":"<pre><code>String refreshToken\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"flutter/TransistorAuthorizationToken/#url","title":"url","text":"<pre><code>String url\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"flutter/TriggerActivity/","title":"TriggerActivity","text":"<p>Allowed motion activity names that can trigger motion detection. Mirrors Flutter's TriggerActivity set.</p>"},{"location":"flutter/Vertices/","title":"Vertices","text":"<p>An array of <code>[latitude, longitude]</code> pairs defining a polygon geofence.</p> <p>See Geofence.vertices.</p>"},{"location":"flutter/WatchPositionRequest/","title":"WatchPositionRequest","text":"<p>Options for BackgroundGeolocation.watchPosition.</p> <p>Configures the interval, accuracy, persistence, and metadata for a continuous location stream. All fields are optional.</p> <p>Warning</p> <p><code>watchPosition</code> is intended for foreground use. On iOS it prevents the app from being suspended, which drains the battery. Remove the subscription when your app moves to the background.</p> <pre><code>final subscription = BackgroundGeolocation.watchPosition(\n  {'interval': 1000, 'desiredAccuracy': DesiredAccuracy.high},\n  (Location location) {\n    print('[watchPosition] ${location}');\n  },\n  (int errorCode) {\n    print('[watchPosition] error: ${errorCode}');\n  }\n);\n\n// Later, stop watching.\nsubscription.remove();\n</code></pre>"},{"location":"flutter/examples/","title":"Examples","text":""},{"location":"flutter/examples/#basic-tracking","title":"Basic tracking","text":"<pre><code>import 'package:flutter_background_geolocation/flutter_background_geolocation.dart' as bg;\n\n// Use the 'bg' namespace to avoid conflicts with Flutter's own Location/State types.\nbg.BackgroundGeolocation.ready(bg.Config(\n  geolocation: bg.GeoConfig(\n    desiredAccuracy: bg.DesiredAccuracy.high,\n    distanceFilter: 10.0,\n  ),\n  app: bg.AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n  ),\n  logger: bg.LoggerConfig(\n    debug: true,\n    logLevel: bg.LogLevel.verbose,\n  ),\n)).then((bg.State state) {\n  if (!state.enabled) {\n    bg.BackgroundGeolocation.start();\n  }\n});\n</code></pre>"},{"location":"flutter/setup/","title":"Setup","text":""},{"location":"flutter/setup/#installation","title":"Installation","text":"<p>Add the dependency to <code>pubspec.yaml</code>:</p> <pre><code>dependencies:\n  flutter_background_geolocation: '^5.1.1'\n</code></pre> <p>Then fetch:</p> <pre><code>flutter pub get\n</code></pre>"},{"location":"flutter/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"flutter/setup/#ios-infoplist","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"flutter/setup/#android-androidmanifestxml","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"flutter/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"flutter/setup/#podfile","title":"Podfile","text":"<p>Add <code>:linkage =&gt; :static</code> to your target in <code>ios/Podfile</code>:</p> <pre><code>target 'Runner' do\n  use_frameworks! :linkage =&gt; :static\n  # ...\nend\n</code></pre> <p>Then run:</p> <pre><code>cd ios &amp;&amp; pod install\n</code></pre>"},{"location":"flutter/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"flutter/setup/#infoplist","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Background modes --&gt;\n&lt;key&gt;UIBackgroundModes&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;location&lt;/string&gt;\n    &lt;string&gt;fetch&lt;/string&gt;\n    &lt;string&gt;processing&lt;/string&gt;\n    &lt;string&gt;audio&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Background task identifier --&gt;\n&lt;key&gt;BGTaskSchedulerPermittedIdentifiers&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;com.transistorsoft.fetch&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"flutter/setup/#android-setup","title":"Android Setup","text":""},{"location":"flutter/setup/#gradle-ext-vars","title":"Gradle ext vars","text":"<p>The plugin reads two optional ext vars from your root project. Set them in <code>android/build.gradle</code> to pin specific versions:</p> build.gradle.ktsbuild.gradle <pre><code>ext[\"playServicesLocationVersion\"] = \"21.3.0\"\next[\"tslocationmanagerVersion\"]    = \"4.0.+\"\n</code></pre> <pre><code>ext {\n    playServicesLocationVersion = \"21.3.0\"\n    tslocationmanagerVersion    = \"4.0.+\"\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"flutter/setup/#androidappbuildgradle","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>val backgroundGeolocation = project(\":flutter_background_geolocation\")\napply { from(\"${backgroundGeolocation.projectDir}/background_geolocation.gradle\") }\n\nandroid {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>Project background_geolocation = project(':flutter_background_geolocation')\napply from: \"${background_geolocation.projectDir}/background_geolocation.gradle\"\n\nandroid {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"flutter/setup/#androidmanifestxml","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"flutter/setup/#example","title":"Example","text":"<pre><code>import 'package:flutter_background_geolocation/flutter_background_geolocation.dart' as bg;\n\n// Use the 'bg' namespace to avoid conflicts with Flutter's own Location/State types.\nbg.BackgroundGeolocation.ready(bg.Config(\n  geolocation: bg.GeoConfig(\n    desiredAccuracy: bg.DesiredAccuracy.high,\n    distanceFilter: 10.0,\n  ),\n  app: bg.AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n  ),\n  logger: bg.LoggerConfig(\n    debug: true,\n    logLevel: bg.LogLevel.verbose,\n  ),\n)).then((bg.State state) {\n  if (!state.enabled) {\n    bg.BackgroundGeolocation.start();\n  }\n});\n</code></pre>"},{"location":"help/debugging/","title":"Debugging","text":""},{"location":"help/debugging/#log-database","title":"Log Database","text":"<p>The SDK maintains an extremely verbose internal log database on the device. Every significant event \u2014 motion transitions, location records, HTTP requests and responses, geofence activity, errors \u2014 is written to SQLite with a timestamp. By default the database retains the last 3 days of logs (<code>logger.logMaxDays</code>, configurable up to any number of days).</p> <p>When something unexpected happens, the log database is the first place to look. It contains far more detail than the console and captures events that occur while your app is backgrounded or terminated, including iOS silent relaunches and Android headless service activity.</p> <p>Always run <code>LogLevel.Verbose</code> during development</p> <p>The default log level records only key events. Set <code>logger.logLevel</code> to <code>LogLevel.Verbose</code> while developing \u2014 it captures the full internal trace of every decision the SDK makes, which is essential for diagnosing subtle issues like missed motion transitions, unexpected stop detection, or silent HTTP failures. Without verbose logging, the log database may not contain enough information to diagnose a problem after the fact.</p> <p>Retrieve and email the full log directly from the device:</p> TypeScriptDartSwiftKotlin <pre><code>// Email the log (opens the system mail composer)\nawait BackgroundGeolocation.logger.emailLog('you@example.com');\n\n// Or retrieve as a string for custom handling\nconst log = await BackgroundGeolocation.logger.getLog();\n</code></pre> <pre><code>await bg.Logger.emailLog('you@example.com');\n\nString log = await bg.Logger.getLog();\n</code></pre> <pre><code>try await BGGeo.shared.logger.emailLog(to: \"you@example.com\")\n\nlet log = try await BGGeo.shared.logger.getLog()\n</code></pre> <pre><code>// emailLog requires a coroutine scope and an Activity reference\nBGGeo.instance.logger.emailLog(\"you@example.com\", activity)\n\nval log = BGGeo.instance.logger.getLog()\n</code></pre> See the API reference for <code>Logger.emailLog</code> and <code>Logger.getLog</code> for full options, including filtering by log level and date range.See the API reference for <code>Logger.emailLog</code> and <code>Logger.getLog</code> for full options, including filtering by log level and date range.See the API reference for <code>Logger.emailLog</code> and <code>Logger.getLog</code> for full options, including filtering by log level and date range.See the API reference for <code>Logger.emailLog</code> and <code>Logger.getLog</code> for full options, including filtering by log level and date range."},{"location":"help/debugging/#debug-sounds","title":"Debug Sounds","text":"<p>The fastest way to understand what the SDK is doing in the field is to enable debug mode:</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n  },\n});\n</code></pre> <pre><code>await bg.BackgroundGeolocation.ready(bg.Config(\n  logger: bg.LoggerConfig(\n    debug: true,\n    logLevel: LogLevel.verbose,\n  ),\n));\n</code></pre> <pre><code>try await BGGeo.shared.ready { config in\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n}\n</code></pre> <pre><code>BGGeo.instance.ready {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n}\n</code></pre> <p>With <code>debug: true</code> the SDK plays a distinct sound for every significant event. Click to listen:</p> Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>iOS \u2014 hearing sounds in the background</p> <p>To hear debug sounds while the app is backgrounded, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"help/debugging/#log-verbosity-retention","title":"Log Verbosity &amp; Retention","text":"<p>Control how much the SDK logs and how long it keeps it:</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n});\n</code></pre> <pre><code>await bg.BackgroundGeolocation.ready(bg.Config(\n  logger: bg.LoggerConfig(\n    debug: false,\n    logLevel: LogLevel.verbose,\n    logMaxDays: 3,\n  ),\n));\n</code></pre> <pre><code>try await BGGeo.shared.ready { config in\n    config.logger.debug = false\n    config.logger.logLevel = .verbose\n    config.logger.logMaxDays = 3\n}\n</code></pre> <pre><code>BGGeo.instance.ready {\n    logger.debug = false\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"help/debugging/#http-inspection","title":"HTTP Inspection","text":"<p>Subscribe to <code>onHttp</code> to inspect every server response from the client side:</p> TypeScriptDartSwiftKotlin <pre><code>BackgroundGeolocation.onHttp(response =&gt; {\n  console.log('[onHttp]', response.status, response.responseText);\n});\n</code></pre> <pre><code>bg.BackgroundGeolocation.onHttp((bg.HttpEvent response) {\n  print('[onHttp] ${response.status} ${response.responseText}');\n});\n</code></pre> <pre><code>BGGeo.shared.onHttp { response in\n    print(\"[onHttp] \\(response.statusCode) \\(response.responseText ?? \"\")\")\n}\n</code></pre> <pre><code>BGGeo.instance.onHttp { response -&gt;\n    Log.d(TAG, \"[onHttp] ${response.statusCode} ${response.responseText}\")\n}\n</code></pre> <p>A <code>400 Bad Request</code> originates at your server, not the plugin. Check your server logs and use <code>http.rootProperty</code> or <code>http.locationTemplate</code> to adjust the payload structure if needed.</p>"},{"location":"help/debugging/#viewing-live-logs","title":"Viewing Live Logs","text":""},{"location":"help/debugging/#ios","title":"iOS","text":"<p>Launch your app through Xcode and watch the debug console. The SDK writes timestamped log entries in real time as events fire \u2014 motion transitions, location records, HTTP responses, geofence activity. With <code>logger: { debug: true }</code> each log line is also accompanied by the corresponding sound, making it easy to correlate what you hear with what you read.</p> <p>For background testing, keep the device connected and the Xcode console open. When iOS relaunches your app after a stationary geofence exit, logs resume automatically in the same console session.</p>"},{"location":"help/debugging/#android","title":"Android","text":"<p>Filter <code>adb logcat</code> to show only the SDK's native log output:</p> <pre><code>adb logcat '*:S' TSLocationManager:V\n</code></pre> <p>The single quotes around <code>*:S</code> suppress glob expansion in both zsh and bash. <code>*:S</code> silences all other log tags; <code>TSLocationManager:V</code> sets the SDK tag to Verbose \u2014 the most detailed level.</p> <p>Common level suffixes:</p> Suffix Level <code>V</code> Verbose \u2014 full SDK trace <code>D</code> Debug <code>I</code> Info <code>W</code> Warning <code>E</code> Error <code>S</code> Silent (suppress) <p>To capture your app's JS bridge logs alongside the SDK:</p> <pre><code>adb logcat '*:S' TSLocationManager:V ReactNative:V ReactNativeJS:V\n</code></pre> <p>To write the log to a file for sharing:</p> <pre><code>adb logcat '*:S' TSLocationManager:V | tee tslocationmanager.log\n</code></pre>"},{"location":"help/debugging/#example-log-output","title":"Example log output","text":"<p>The SDK uses box-drawing characters and emoji to make the log scannable at a glance. Here is a representative excerpt showing a <code>motionchange</code> event \u2014 the device transitions from stationary to moving, a location is persisted to SQLite, and a successful HTTP upload follows:</p> AndroidiOS <pre><code>TSLocationManager: [SingleLocationRequest trySatisfyLastLocation] \ud83d\udccd\nTSLocationManager: \u255f\u2500 age: 3041ms\nTSLocationManager: \u255f\u2500 maximumAge: 30000\nTSLocationManager: \u255f\u2500 desiredAccuracy: 20.0\nTSLocationManager: \u255f\u2500 meetsAccuracy: true\nTSLocationManager: \u255f\u2500 meetsStaleness: true\nTSLocationManager: [TSLocationManager logSingleLocationResult]\nTSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: \u2551 motionchange LocationResult: 7 (3043ms old)\nTSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: \u255f\u2500 \ud83d\udccd  Location[fused 45.518863,-73.600554 hAcc=19.8 alt=46.9 vel=0.04 sAcc=1.5]\nTSLocationManager: [TSLocationManager onSingleLocationResult]\nTSLocationManager:   \ud83d\udd35  MOTIONCHANGE isMoving=true df=50.0 \u2014 resetting short-term filter state\nTSLocationManager: [TSLocationManager onSingleLocationResult]\nTSLocationManager:   \ud83d\udd35  Acquired motionchange position, isMoving: true\nTSLocationManager: [TSLocationManager requestLocationUpdates]\nTSLocationManager:   \ud83c\udfbe  Location-services: ON\nTSLocationManager: [SQLiteLocationDAO persist]\nTSLocationManager:   \ud83d\udcbe \u2705 12d596fa-d089-42eb-a084-a50ac50cc11e\nTSLocationManager: [HttpService flush]\nTSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: \u2551 HTTP Service (count: 1)\nTSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: [AbstractService start]\nTSLocationManager:   \ud83c\udfbe  motionchange [TrackingService startId: 1, eventCount: 1]\nTSLocationManager: [TrackingService handleMotionChangeResult]\nTSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: \u2551 TrackingService motionchange: true\nTSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\nTSLocationManager: [AbstractService finish]\nTSLocationManager:   \u2699\ufe0f  FINISH [TrackingService startId: 1, eventCount: 0, sticky: true]\nTSLocationManager: [ActivityRecognitionService start]\nTSLocationManager:   \ud83c\udfbe  Start motion-activity updates\nTSLocationManager: [BackgroundTaskManager$Task start] \u23f3 startBackgroundTask: 3\nTSLocationManager: [SQLiteLocationDAO first]\nTSLocationManager:   \u2705  Locked 1 records\nTSLocationManager: [HttpService createRequest]\nTSLocationManager:   \ud83d\udd35  HTTP POST: 12d596fa-d089-42eb-a084-a50ac50cc11e\nTSLocationManager: [ActivityRecognitionService handleActivityRecognitionResult]\nTSLocationManager:   \ud83d\ude98  DetectedActivity [type=STILL, confidence=100]\nTSLocationManager:   \ud83d\udd35  Response: 200\nTSLocationManager: [EventManager fire]   \ud83d\udedc \u26a1\ufe0f http\nTSLocationManager: [SQLiteLocationDAO destroy]\nTSLocationManager:   \u2705  DESTROY: 12d596fa-d089-42eb-a084-a50ac50cc11e\nTSLocationManager: [BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 3\n</code></pre> <pre><code>\ud83d\udfe2-[TSTrackingService changePace:] isMoving: 1\n\ud83d\udfe2-[TSLocationRequestService requestLocation:] [motionchange] maximumAge: 5000\n\u2705-[TSBackgroundTaskManager stopBackgroundTask:]_block_invoke 2 OF {(\n    2\n)}\n\n1:\ud83d\udccd&lt;+37.33233141,-122.03121860&gt; +/- 5.00m (speed 0.00 mps / course -1.00) @ 2026-04-05, 10:59:11 PM EDT | age: 551 ms\n\n\u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\u2551 -[TSSingleLocationRequest trySatisfyWithLocation:now:] \ud83d\udccd\ud83d\udd0e [motionchange] desiredAccuracy: 20.0 m, maximumAge: 5000 ms\n\u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\u2705-[TSSingleLocationRequest trySatisfyWithLocation:now:] \ud83d\udccd\ud83d\udd0e meets accuracy &amp; staleness: &lt;+37.33233141,-122.03121860&gt; +/- 5.00m\n[Swift][location] sample: true, isMoving: true, odometer: 96396.96 \u00b1 164.32\n\ud83d\udfe2-[TSTrackingService startMonitoringSignificantLocationChanges]\n\u2139\ufe0f-[TSOdometer onMotionChange:] Filter state: odometer=96396.96m \u00b1164.32m\n\u2705-[TSDataStore persist:] \ud83d\udcbe INSERT: 9A409B53-92CE-4812-BD58-3823A5AA06AF (type: 0, persistMode: 2, maxRecords: -1)\n\ud83d\udfe2-[TSTrackingService startUpdatingLocation] Location-services: ON\n[Swift][location] sample: false, isMoving: true, odometer: 96396.96 \u00b1 164.32\n[Swift][motionchange] 1\n\n\u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\u2551 -[TSHttpService beginFlushWithCallback:overrideSyncThreshold:error:]\n\u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\u2705-[TSBackgroundTaskManager createBackgroundTask] Created background task: 3\n\u2705-[TSHttpService schedulePost] LOCKED: 9A409B53-92CE-4812-BD58-3823A5AA06AF\n\n\u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\u2551 -[TSTrackingService locationManager:didUpdateLocations:] Enabled: 1 | isMoving: 1 | df: 20.0m\n\u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n\ud83d\udccd&lt;+37.33233141,-122.03121860&gt; +/- 5.00m (speed 0.00 mps / course -1.00) @ 2026-04-05, 10:59:11 PM EDT | age: 567 ms\n\u2139\ufe0f-[TSMotionActivityClassifier updateSpeed:] speed=0.00\n\ud83d\udccd-[TSLocationFilter evaluateWithMetrics:] decision=Accepted reason=OK raw=0.0m effective=0.0m smoothed=0.0m cap=0.0m acc=5.0m speed=0.00 sigma=0.0m df=20.0m\n[Swift][location] sample: false, isMoving: true, odometer: 96396.96 \u00b1 164.32\n\ud83d\udd35-[TSHttpResponse handleResponse] Response: 200\n</code></pre>"},{"location":"help/debugging/#demo-app","title":"Demo App","text":"<p>Each SDK ships with a fully functional demo application. Run it to verify your installation and license key before integrating into your own app. The demo app uses <code>debug: true</code> and displays a live event log on screen, making it the most complete debugging environment available.</p> <ul> <li>React Native: react-native-background-geolocation/example</li> <li>Flutter: flutter_background_geolocation/example</li> <li>Capacitor: capacitor-background-geolocation/example</li> </ul>"},{"location":"help/debugging/#simulating-location","title":"Simulating Location","text":""},{"location":"help/debugging/#ios-simulator","title":"iOS Simulator","text":"<p>The iOS Simulator has a built-in location simulation feature. With your app running in the simulator:</p> <ol> <li>In the Xcode menu bar choose Debug \u2192 Simulate Location \u2192 Freeway Drive</li> </ol> <p>The simulator will generate a continuous stream of GPS coordinates following a highway route at realistic speeds, transitioning the SDK into moving state and producing location records just as a real device would. Other presets include City Run, City Bicycle Ride, and Pedestrian. You can also load a custom GPX file via Add GPX File to Project... for a specific route.</p>"},{"location":"help/debugging/#android_1","title":"Android","text":"<p>Android does not have a built-in location simulation tool. Use a third-party Mock Location app from the Play Store. A popular option is Lockito, which lets you define a route and replay it at a configurable speed.</p> <p>To enable mock locations on Android:</p> <ol> <li>Enable Developer Options on the device (tap Build Number seven times in Settings \u2192 About Phone)</li> <li>In Developer Options, set Select mock location app to your chosen app</li> <li>Start the mock route before launching your app</li> </ol> <p>Note</p> <p>Some devices require the mock location app to be running in the foreground, or the mock coordinates will stop when the screen locks. Check the app's documentation for background operation requirements.</p>"},{"location":"help/debugging/#common-issues","title":"Common Issues","text":"<p>Tracking stops on Samsung / Huawei / Xiaomi</p> <p>OEM battery optimisation layers on these devices can kill the foreground service. Add your app to the device's battery whitelist. See dontkillmyapp.com for per-manufacturer steps.</p> <p>No locations after iOS app termination</p> <p>Ensure <code>ready()</code> is called unconditionally at app launch \u2014 not inside a component mount or behind a conditional. When iOS relaunches your app silently after a stationary geofence event, the launch path must reach <code>ready()</code> for tracking to resume.</p> <p>HTTP requests not firing</p> <p>Check that <code>http.url</code> is set and <code>http.autoSync</code> is <code>true</code>. Inspect responses with <code>onHttp</code>. Verify the device has network access and your server is reachable from the device's network (not just localhost).</p>"},{"location":"help/faq/","title":"FAQ","text":"<p>Distilled from 11 590 support emails (2015\u20132026).</p>"},{"location":"help/faq/#licensing-pricing","title":"Licensing &amp; Pricing","text":""},{"location":"help/faq/#do-i-need-a-license-for-ios","title":"Do I need a license for iOS?","text":"<p>Yes. Both iOS and Android require a license key. One license purchase covers both platforms \u2014 you generate a separate key for each from the Customer Dashboard. The Android key is bound to your <code>applicationId</code>; the iOS key is bound to your <code>bundleIdentifier</code>. There is no extra charge \u2014 both keys are included in a single tier purchase.</p>"},{"location":"help/faq/#does-one-application-key-work-for-both-android-and-ios","title":"Does one application key work for both Android and iOS?","text":"<p>One license purchase covers both platforms, but you generate separate keys for iOS and Android from the Customer Dashboard. Enter the Android <code>applicationId</code> (e.g. <code>com.mycompany.myapp</code>) when creating the Android key, and the iOS <code>bundleIdentifier</code> (e.g. <code>com.mycompany.myapp</code>) when creating the iOS key.</p>"},{"location":"help/faq/#is-the-license-a-one-time-purchase-or-do-i-pay-every-year","title":"Is the license a one-time purchase, or do I pay every year?","text":"<p>Purchased keys unlock the plugin forever \u2014 there is no recurring charge for the key to keep working. The optional yearly support subscription ($199/year) provides priority tech support and access to the latest releases. If your support subscription lapses, your key and the version you have installed continue to work, but you cannot install newer releases until you renew.</p>"},{"location":"help/faq/#what-does-1-year-access-to-latest-updates-mean","title":"What does \"1 year access to latest updates\" mean?","text":"<p>It means your purchase includes one year of priority support responses and access to the latest releases. After one year, your key and the plugin version you have installed continue to work \u2014 but if a new release is published after your subscription lapses, you cannot install it until you renew. Renew via the Customer Dashboard to restore access.</p>"},{"location":"help/faq/#what-are-the-license-plans","title":"What are the license plans?","text":"Plan Price Keys When to use Starter $399 1 One app Venture $599 5 Up to 5 distinct apps Pro $749 25 Up to 25 distinct apps Studio $999 100 Up to 100 distinct apps <p>Ask yourself: \"How many distinct Android <code>applicationId</code>s will I publish to the Play Store?\" That is the number of keys you need. The number of users per app is unlimited.</p>"},{"location":"help/faq/#can-i-upgrade-from-starter-to-venture-later","title":"Can I upgrade from Starter to Venture later?","text":"<p>Yes. Log in to the Customer Dashboard and click the Upgrade button. The cost is $250 (Starter \u2192 Venture). You save $50 by purchasing Venture outright rather than upgrading after the fact. The upgrade price is calculated from the non-sale (strikethrough) price.</p>"},{"location":"help/faq/#is-there-a-free-trial-key-or-a-discount-code","title":"Is there a free trial key or a discount code?","text":"<p>There are no discount codes. Trial keys are available upon request \u2014 email info@transistorsoft.com. The plugin is also fully functional in debug builds without any key, despite the validation warning messages. Integrate, field-test, and satisfy yourself completely before purchasing.</p>"},{"location":"help/faq/#are-licenses-per-user-or-per-install","title":"Are licenses per-user or per-install?","text":"<p>Neither. There is no limit on the number of users or device installs. Each key is bound to one app identifier (<code>applicationId</code> on Android, <code>bundleIdentifier</code> on iOS) for unlimited users and installs. The plugin does not contact Transistor Software servers to validate keys at runtime.</p>"},{"location":"help/faq/#can-i-use-my-key-in-development-builds-with-a-different-applicationid-suffix","title":"Can I use my key in development builds with a different <code>applicationId</code> suffix?","text":"<p>Yes. License keys automatically allow a set of common development suffixes on the base <code>applicationId</code>:</p> <p><code>.dev</code> \u00b7 <code>.development</code> \u00b7 <code>.staging</code> \u00b7 <code>.stage</code> \u00b7 <code>.qa</code> \u00b7 <code>.uat</code> \u00b7 <code>.test</code> \u00b7 <code>.debug</code></p> <p>So a key generated for <code>com.mycompany.myapp</code> also validates for <code>com.mycompany.myapp.debug</code>, <code>com.mycompany.myapp.staging</code>, etc. \u2014 without generating additional keys. Custom suffixes beyond this list are available upon request at info@transistorsoft.com \u2014 include your order number.</p>"},{"location":"help/faq/#can-i-use-a-cordova-license-with-the-capacitor-plugin-or-a-react-native-license-with-flutter","title":"Can I use a Cordova license with the Capacitor plugin, or a React Native license with Flutter?","text":"<p>No. Each framework has its own plugin and its own license. Licenses are not transferable between frameworks. If you are migrating from Cordova to Capacitor, you need a new Capacitor license.</p>"},{"location":"help/faq/#what-is-cordova-background-geolocation-lt","title":"What is <code>cordova-background-geolocation-lt</code>?","text":"<p>It is the npm package name for the Transistor Software Cordova plugin. The <code>-lt</code> suffix is historical; there is no meaningful difference from <code>cordova-background-geolocation</code>. It is the same premium plugin.</p>"},{"location":"help/faq/#does-the-capacitor-plugin-require-a-separate-license-from-cordova","title":"Does the Capacitor plugin require a separate license from Cordova?","text":"<p>Yes. A Cordova key is not valid for the Capacitor plugin. Purchase a Capacitor license separately.</p>"},{"location":"help/faq/#can-i-use-the-plugin-in-an-ionic-or-angular-app","title":"Can I use the plugin in an Ionic or Angular app?","text":"<p>Yes. The Capacitor or Cordova plugin works with any Ionic/Angular project built on those platforms. The plugin does not care which JavaScript UI framework you use.</p>"},{"location":"help/faq/#what-is-the-firebase-adapter-do-i-need-it-to-upload-locations","title":"What is the Firebase adapter? Do I need it to upload locations?","text":"<p>The Firebase adapter is an optional paid add-on that writes location records directly into Firebase Firestore. You do not need it to upload locations \u2014 the plugin has a built-in HTTP service that posts to any REST endpoint. The adapter is only useful if you specifically want to target Firestore. It requires a Background Geolocation license for your platform in addition to the adapter license. It supports Firestore only \u2014 not the Realtime Database.</p>"},{"location":"help/faq/#does-polygon-geofencing-cost-extra","title":"Does polygon geofencing cost extra?","text":"<p>Yes. Polygon geofencing is a separate purchasable add-on. Circular geofencing is included with every Background Geolocation license at no extra cost.</p>"},{"location":"help/faq/#getting-started","title":"Getting Started","text":""},{"location":"help/faq/#can-i-try-the-plugin-before-buying","title":"Can I try the plugin before buying?","text":"<p>Yes. The plugin is fully functional in debug builds without a license key. Build in debug mode, go outside for a real-world walk of at least 1 km with <code>logger: { debug: true }</code> enabled, and confirm it meets your needs before purchasing.</p>"},{"location":"help/faq/#where-is-the-demo-app","title":"Where is the demo app?","text":"<p>Each platform's GitHub repository contains a <code>/example</code> folder with a ready-to-run demo app:</p> <ul> <li>React Native</li> <li>Flutter</li> <li>Capacitor</li> <li>Cordova</li> <li>Native (Swift / Kotlin)</li> </ul> <p>Clone the repo, follow the README setup instructions, and install it to a device.</p>"},{"location":"help/faq/#how-do-i-get-my-license-key-after-purchasing","title":"How do I get my license key after purchasing?","text":"<p>After purchase you receive two emails. The second email contains registration instructions and a link to the Customer Dashboard. Register your order there and click Create License Key \u2014 once for Android (entering your <code>applicationId</code>) and once for iOS (entering your <code>bundleIdentifier</code>). You self-generate both keys; they are not sent to you automatically.</p>"},{"location":"help/faq/#what-are-my-android-applicationid-and-ios-bundleidentifier","title":"What are my Android <code>applicationId</code> and iOS <code>bundleIdentifier</code>?","text":"<p>Android \u2014 the value of <code>applicationId</code> in <code>android/app/build.gradle</code>, for example <code>com.mycompany.myapp</code>.</p> <p>iOS \u2014 the value of <code>PRODUCT_BUNDLE_IDENTIFIER</code> in Xcode (or <code>bundleIdentifier</code> in <code>app.json</code> for React Native / Expo), typically the same value as your Android <code>applicationId</code>.</p> <p>These are what you enter when generating license keys in the Customer Dashboard \u2014 one key per platform.</p>"},{"location":"help/faq/#can-i-use-the-plugin-with-expo","title":"Can I use the plugin with Expo?","text":"<p>Yes \u2014 the plugin is fully supported across all Expo workflows: Expo Go, Expo Dev Client, managed workflow, and bare workflow. Follow the Expo Setup Instructions linked in the GitHub repo README.</p>"},{"location":"help/faq/#the-build-fails-with-could-not-find-any-matches-for-comtransistorsofttslocationmanager","title":"The build fails with <code>Could not find any matches for com.transistorsoft:tslocationmanager:+</code>.","text":"<p>The <code>tslocationmanager</code> library is published to Maven Central \u2014 there is no custom repository URL required. If Gradle cannot resolve it, the problem is in your project's repository configuration, not on Transistor Software's servers. Check that <code>mavenCentral()</code> is present in your root <code>android/build.gradle</code> (or <code>settings.gradle</code>) repositories block:</p> <pre><code>allprojects {\n    repositories {\n        google()\n        mavenCentral()  // \u2190 required\n    }\n}\n</code></pre> <p>If <code>mavenCentral()</code> is already there and the build still fails, your build environment cannot reach Maven Central \u2014 a network or proxy issue that affects all Android dependencies, not just this library.</p>"},{"location":"help/faq/#tracking-behaviour","title":"Tracking Behaviour","text":""},{"location":"help/faq/#does-the-plugin-track-when-the-app-is-killed-or-terminated","title":"Does the plugin track when the app is killed or terminated?","text":"<p>Yes, with <code>stopOnTerminate: false</code> \u2014 but the behaviour differs by platform.</p> <p>iOS: When the app is terminated, the SDK registers a stationary geofence of <code>stationaryRadius</code> metres (default 150 m) around the last known position. When the device moves approximately 200 m, iOS silently relaunches the app in the background and tracking resumes automatically. This is an OS-level mechanism.</p> <p>Android: The native background service keeps running after the app is killed. The JS/UI process is gone, but the service continues recording and uploading locations. Configure <code>http.url</code> so the service can upload while running headlessly. Enable <code>enableHeadless: true</code> only if you need to run custom JavaScript logic in the headless state (for example, posting a local notification); it is not required for location recording and uploading.</p> TypeScriptDartSwiftKotlin <pre><code>const state = await BackgroundGeolocation.ready({\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,  // Android only \u2014 only needed for custom headless JS logic\n  },\n  http: {\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  },\n});\n</code></pre> <pre><code>bg.State state = await bg.BackgroundGeolocation.ready(bg.Config(\n  app: bg.AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,  // Android only\n  ),\n  http: bg.HttpConfig(\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  ),\n));\n</code></pre> <pre><code>// enableHeadless is Android-only; not applicable on iOS\nlet state = try await BGGeo.shared.ready { config in\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n    config.http.url = \"https://your.server.com/api/locations\"\n    config.http.autoSync = true\n}\n</code></pre> <pre><code>val state = BGGeo.instance.ready {\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    app.enableHeadless = true  // only needed for custom headless logic\n    http.url = \"https://your.server.com/api/locations\"\n    http.autoSync = true\n}\n</code></pre>"},{"location":"help/faq/#does-the-plugin-restart-tracking-after-a-device-reboot","title":"Does the plugin restart tracking after a device reboot?","text":"<p>Yes, set <code>startOnBoot: true</code>. On Android this registers a <code>BOOT_COMPLETED</code> broadcast receiver. On iOS, the OS resumes monitoring the stationary geofence registered at termination, which relaunches the app and restarts tracking when the device next moves.</p>"},{"location":"help/faq/#why-does-tracking-stop-when-the-device-is-stationary","title":"Why does tracking stop when the device is stationary?","text":"<p>This is by design \u2014 the Philosophy of Operation. The SDK is motion-driven: it only requests location updates when the device is moving. When the device stops, the SDK transitions to the stationary state and stops firing location events. This saves battery significantly. Use <code>stopTimeout</code> (in minutes) to control how long the device must be stationary before the SDK confirms the stopped state. For periodic locations while stationary, use the companion <code>background-fetch</code> plugin (already included as a dependency) and call <code>BackgroundGeolocation.getCurrentPosition()</code> in its callback.</p>"},{"location":"help/faq/#tracking-stops-after-brief-stops-during-a-trip","title":"Tracking stops after brief stops during a trip.","text":"<p>Increase <code>stopTimeout</code> to give the SDK more time before declaring the device stopped. The default is 5 minutes. For vehicle use cases, <code>stopTimeout: 10</code> or higher is common. You can also configure <code>activity.stopDetectionDelay</code> to introduce a delay before stop-detection evaluates.</p> TypeScriptDartSwiftKotlin <pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    distanceFilter: 50,\n    stopTimeout: 10,\n  },\n  activity: {\n    stopDetectionDelay: 1,\n  },\n});\n</code></pre> <pre><code>bg.State state = await bg.BackgroundGeolocation.ready(bg.Config(\n  geolocation: bg.GeoConfig(\n    distanceFilter: 50.0,\n    stopTimeout: 10,\n  ),\n  activity: bg.ActivityConfig(\n    stopDetectionDelay: 1,\n  ),\n));\n</code></pre> <pre><code>let state = try await BGGeo.shared.ready { config in\n    config.geolocation.distanceFilter = 50\n    config.geolocation.stopTimeout = 10\n    config.activity.stopDetectionDelay = 1\n}\n</code></pre> <pre><code>val state = BGGeo.instance.ready {\n    geolocation.distanceFilter = 50f\n    geolocation.stopTimeout = 10\n    activity.stopDetectionDelay = 1\n}\n</code></pre>"},{"location":"help/faq/#can-i-get-a-location-update-every-n-minutes-on-a-fixed-schedule","title":"Can I get a location update every N minutes on a fixed schedule?","text":"<p>The SDK is event-driven, not interval-based. For periodic locations on a fixed schedule (including while stationary), use the <code>background-fetch</code> plugin (already a dependency) and call <code>BackgroundGeolocation.getCurrentPosition()</code> in the fetch callback. iOS enforces a minimum background-fetch interval of approximately 15 minutes.</p>"},{"location":"help/faq/#does-the-plugin-work-when-the-screen-is-locked","title":"Does the plugin work when the screen is locked?","text":"<p>Yes. The plugin runs as a background service and is unaffected by screen state.</p>"},{"location":"help/faq/#configuration","title":"Configuration","text":""},{"location":"help/faq/#how-do-i-configure-the-plugin","title":"How do I configure the plugin?","text":"<p>Call <code>BackgroundGeolocation.ready()</code> once per app launch before calling <code>start()</code>. Use the compound config API with grouped sub-configs:</p> TypeScriptDartSwiftKotlin <pre><code>import BackgroundGeolocation, { DesiredAccuracy } from 'react-native-background-geolocation';\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 50,\n    stopTimeout: 5,\n  },\n  http: {\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n  logger: {\n    debug: true,  // disable in production\n  },\n});\n\nif (!state.enabled) {\n  BackgroundGeolocation.start();\n}\n</code></pre> <pre><code>import 'package:flutter_background_geolocation/flutter_background_geolocation.dart' as bg;\n\nbg.State state = await bg.BackgroundGeolocation.ready(bg.Config(\n  geolocation: bg.GeoConfig(\n    desiredAccuracy: bg.DesiredAccuracy.high,\n    distanceFilter: 50.0,\n    stopTimeout: 5,\n  ),\n  app: bg.AppConfig(\n    stopOnTerminate: false,\n    startOnBoot: true,\n  ),\n  http: bg.HttpConfig(\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  ),\n));\n\nif (!state.enabled) {\n  bg.BackgroundGeolocation.start();\n}\n</code></pre> <pre><code>import TSLocationManager\n\nlet state = try await BGGeo.shared.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 50\n    config.geolocation.stopTimeout = 5\n    config.http.url = \"https://your.server.com/api/locations\"\n    config.http.autoSync = true\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n    config.logger.debug = true  // disable in production\n}\n\nif !state.enabled {\n    try await BGGeo.shared.start()\n}\n</code></pre> <pre><code>import com.transistorsoft.locationmanager.kotlin.BGGeo\n\nval state = BGGeo.instance.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 50f\n    geolocation.stopTimeout = 5\n    http.url = \"https://your.server.com/api/locations\"\n    http.autoSync = true\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    logger.debug = true  // disable in production\n}\n\nif (!state.enabled) {\n    BGGeo.instance.start()\n}\n</code></pre>"},{"location":"help/faq/#what-does-distancefilter-do","title":"What does <code>distanceFilter</code> do?","text":"<p><code>distanceFilter</code> (in metres) is the minimum displacement required before a new location sample is delivered while the device is in moving mode. The SDK automatically scales this value upward at higher speeds (<code>elasticityMultiplier</code>) to reduce the update rate when driving fast. Set <code>disableElasticity: true</code> to disable the speed-based scaling.</p>"},{"location":"help/faq/#what-does-desiredaccuracy-do","title":"What does <code>desiredAccuracy</code> do?","text":"<p>It sets the requested location accuracy and power trade-off. Use <code>DesiredAccuracy.High</code> (GPS-grade) for walking and cycling; lower accuracy values reduce battery consumption for vehicle tracking. Indoors, GPS does not work \u2014 location comes from Wi-Fi and cell towers at 40\u2013100 m accuracy regardless of this setting.</p>"},{"location":"help/faq/#how-do-i-stop-and-restart-tracking-programmatically","title":"How do I stop and restart tracking programmatically?","text":"TypeScriptDartSwiftKotlin <pre><code>// Stop tracking\nawait BackgroundGeolocation.stop();\n\n// Resume tracking\nawait BackgroundGeolocation.start();\n\n// Force moving state immediately (skips the stationary waiting period)\nawait BackgroundGeolocation.changePace(true);\n</code></pre> <pre><code>// Stop tracking\nawait bg.BackgroundGeolocation.stop();\n\n// Resume tracking\nawait bg.BackgroundGeolocation.start();\n\n// Force moving state immediately (skips the stationary waiting period)\nawait bg.BackgroundGeolocation.changePace(true);\n</code></pre> <pre><code>// Stop tracking\ntry await BGGeo.shared.stop()\n\n// Resume tracking\ntry await BGGeo.shared.start()\n\n// Force moving state immediately (skips the stationary waiting period)\nBGGeo.shared.changePace(true)\n</code></pre> <pre><code>// Stop tracking\nBGGeo.instance.stop()\n\n// Resume tracking\nBGGeo.instance.start()\n\n// Force moving state immediately (skips the stationary waiting period)\nBGGeo.instance.changePace(true)\n</code></pre>"},{"location":"help/faq/#how-do-i-send-custom-data-fields-with-each-location","title":"How do I send custom data fields with each location?","text":"<p>Use <code>http.params</code> for static key/value pairs added to every request, or <code>persistence.extras</code> for per-location payload fields:</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.ready({\n  http: {\n    url: 'https://your.server.com/api/locations',\n    params: { user_id: '123', trip_id: 'abc' },\n    headers: { Authorization: 'Bearer mytoken' },\n    autoSync: true,\n  },\n  persistence: {\n    extras: { vehicle_id: 'truck-42' },\n  },\n});\n</code></pre> <pre><code>await bg.BackgroundGeolocation.ready(bg.Config(\n  http: bg.HttpConfig(\n    url: 'https://your.server.com/api/locations',\n    params: {'user_id': '123', 'trip_id': 'abc'},\n    headers: {'Authorization': 'Bearer mytoken'},\n    autoSync: true,\n  ),\n  persistence: bg.PersistenceConfig(\n    extras: {'vehicle_id': 'truck-42'},\n  ),\n));\n</code></pre> <pre><code>try await BGGeo.shared.ready { config in\n    config.http.url = \"https://your.server.com/api/locations\"\n    config.http.params = [\"user_id\": \"123\", \"trip_id\": \"abc\"]\n    config.http.headers = [\"Authorization\": \"Bearer mytoken\"]\n    config.http.autoSync = true\n    config.persistence.extras = [\"vehicle_id\": \"truck-42\"]\n}\n</code></pre> <pre><code>BGGeo.instance.ready {\n    http.url = \"https://your.server.com/api/locations\"\n    http.params = mapOf(\"user_id\" to \"123\", \"trip_id\" to \"abc\")\n    http.headers = mapOf(\"Authorization\" to \"Bearer mytoken\")\n    http.autoSync = true\n    persistence.extras = mapOf(\"vehicle_id\" to \"truck-42\")\n}\n</code></pre>"},{"location":"help/faq/#what-does-debug-true-do-and-how-do-i-turn-off-the-sounds","title":"What does <code>debug: true</code> do and how do I turn off the sounds?","text":"<p>Setting <code>logger: { debug: true }</code> enables audible sound effects that signal each state transition (moving, stationary, location recorded, HTTP success/failure). It is the fastest way to understand what the plugin is doing during a field test. Set <code>logger: { debug: false }</code> (the default) to silence the sounds. Always disable debug mode in production.</p>"},{"location":"help/faq/#ios-specifics","title":"iOS Specifics","text":""},{"location":"help/faq/#after-terminating-the-ios-app-tracking-doesnt-resume-until-i-walk-about-200-m-is-that-normal","title":"After terminating the iOS app, tracking doesn't resume until I walk about 200 m. Is that normal?","text":"<p>Yes. This is expected behaviour. When the app terminates with <code>stopOnTerminate: false</code>, the SDK registers a stationary geofence of <code>stationaryRadius</code> metres (default 150 m) around the last known position. iOS monitors that OS-level geofence even while the app is not running. When the device exits the geofence, iOS silently relaunches the app in the background and tracking resumes.</p> <p>Note that 200 m is typical in urban environments but is not guaranteed \u2014 the exact distance is entirely at the discretion of the OS. When your app is relaunched, and how far you travel before that happens, is influenced by the surrounding network environment: the presence and density of Wi-Fi signals, the spacing of cell towers, and geographical obstructions all play a role. This is an iOS platform constraint, not a plugin limitation.</p>"},{"location":"help/faq/#do-i-need-always-location-permission-for-background-tracking","title":"Do I need \"Always\" location permission for background tracking?","text":"<p>Yes, for fully automatic background tracking. With \"Always\" permission the SDK manages the motion state machine autonomously, including relaunching after termination.</p> <p>With \"When In Use\" permission the SDK can still track \u2014 similar to a fitness app with a [Start Workout] button. Call <code>changePace(true)</code> while the app is in the foreground to begin a session. As the user moves the app to the background, iOS allows location updates to continue. When the device becomes stationary and <code>GeoConfig.stopTimeout</code> expires, the SDK calls <code>changePace(false)</code> on itself and stops tracking. The session cannot restart automatically \u2014 the user must return to the foreground to begin a new one.</p>"},{"location":"help/faq/#onlocation-events-stop-firing-on-ios-when-the-app-is-in-the-background","title":"<code>onLocation</code> events stop firing on iOS when the app is in the background.","text":"<p>Make sure <code>BackgroundGeolocation.ready()</code> is called unconditionally at app launch \u2014 not inside a UI component, a tab screen, or behind a conditional. When iOS relaunches your app in the background after a stationary geofence triggers, <code>ready()</code> must be reached in that launch path for tracking to resume correctly.</p>"},{"location":"help/faq/#should-i-use-watchposition-for-continuous-background-tracking","title":"Should I use <code>watchPosition</code> for continuous background tracking?","text":"<p>No. <code>watchPosition</code> is designed for short-duration foreground use (for example, a \"show my current position\" feature). Do not use it for ongoing background tracking. Call <code>start()</code> with \"Always\" permission for background tracking.</p>"},{"location":"help/faq/#android-specifics","title":"Android Specifics","text":""},{"location":"help/faq/#does-the-foreground-service-notification-appear-immediately-when-i-call-start","title":"Does the foreground service notification appear immediately when I call <code>start()</code>?","text":"<p>Not exactly. At startup the SDK briefly activates a foreground service to fetch the initial <code>motionchange</code> position \u2014 a notification appears during this time. Once the SDK confirms the device is stationary, it turns off location services and the foreground service terminates (notification disappears). After that, the notification reappears whenever the SDK is actively recording locations (i.e. while in the moving state) and disappears again when the device stops.</p>"},{"location":"help/faq/#android-tracking-stops-on-samsung-huawei-xiaomi-devices","title":"Android tracking stops on Samsung / Huawei / Xiaomi devices.","text":"<p>Some OEM Android builds (Samsung One UI, Huawei EMUI, Xiaomi MIUI) aggressively kill background processes. The plugin correctly sets up a foreground service, but the OEM battery optimisation layer can override it. Instruct users to add your app to the battery whitelist or exclusion list in device settings. This is an OEM issue; see dontkillmyapp.com for per-manufacturer instructions.</p>"},{"location":"help/faq/#what-android-permissions-does-the-plugin-require","title":"What Android permissions does the plugin require?","text":"<p>The plugin's Gradle configuration automatically adds all required permissions to your <code>AndroidManifest.xml</code>. You do not need to add them manually. They include <code>ACCESS_FINE_LOCATION</code>, <code>ACCESS_BACKGROUND_LOCATION</code>, <code>FOREGROUND_SERVICE</code>, <code>FOREGROUND_SERVICE_LOCATION</code>, <code>ACTIVITY_RECOGNITION</code>, and <code>RECEIVE_BOOT_COMPLETED</code>.</p>"},{"location":"help/faq/#can-i-remove-access_background_location-if-i-only-track-while-the-app-is-in-the-foreground","title":"Can I remove <code>ACCESS_BACKGROUND_LOCATION</code> if I only track while the app is in the foreground?","text":"<p>Yes, if your app never initiates background location tracking. Remove the permission from your merged manifest and verify in testing that the plugin behaves correctly for your specific use case before publishing.</p>"},{"location":"help/faq/#does-the-plugin-work-on-android-10-and-later","title":"Does the plugin work on Android 10 and later?","text":"<p>Yes. Android 10 and later are fully supported and regularly tested. If you see a specific problem on a particular device or Android version, post a GitHub issue with plugin logs from <code>BackgroundGeolocation.logger.emailLog()</code>.</p>"},{"location":"help/faq/#publishing-rejections","title":"Publishing Rejections","text":""},{"location":"help/faq/#my-app-was-rejected-by-the-app-store-or-play-store-because-of-background-location","title":"My app was rejected by the App Store or Play Store because of background location.","text":"<p>Both stores manually review apps that request background location permission. Reviewers look at what the app actually does \u2014 not just what permission it requests.</p> <p>Rejected use cases \u2014 background location (or a foreground service) used to keep the app alive for a purpose unrelated to location:</p> <ul> <li>Alarm clock or timer apps using a foreground service to prevent process termination</li> <li>Music or media players requesting background location to stay alive</li> <li>Any app where location is not the core feature visible to the end user</li> </ul> <p>Accepted use cases \u2014 apps where continuous location tracking is the primary feature and clearly disclosed to the user:</p> <ul> <li>Exercise and fitness tracking (running, cycling, hiking)</li> <li>Delivery and logistics (food delivery, courier, fleet tracking)</li> <li>Ride-sharing and taxi</li> <li>Field service and workforce management</li> <li>Asset and vehicle tracking</li> </ul> <p>If your app was rejected, review the rejection reason carefully. Reviewers often request a demo video or a reviewer account showing the location feature in action. Make sure your App Store / Play Store listing, privacy policy, and permission usage descriptions all clearly state that location is collected, why, and how it is used.</p> <p>See also: Required declarations for \"Foreground Services\" and \"Health\"</p>"},{"location":"help/faq/#google-play-store-declarations-for-targetsdkversion-34-android-14","title":"Google Play Store declarations for <code>targetSdkVersion 34</code> (Android 14)","text":"<p>With <code>targetSdkVersion 34</code>, Google Play requires declarations for two permissions the SDK automatically inserts into your <code>AndroidManifest.xml</code>. See the full walkthrough on Medium.</p>"},{"location":"help/faq/#activity_recognition","title":"<code>ACTIVITY_RECOGNITION</code>","text":"<p>The SDK uses this permission to monitor the Android Motion API and automatically toggle location tracking ON/OFF based on detected motion (walking, in-vehicle, still, etc.). In the Play Console declaration, select category \"Other\" and use this text:</p> <p>The app uses the ActivityTransition API to automatically toggle location-services ON when the device is detected to be in-motion. When the Motion API reports the device is \"still\", the app turns OFF location-services to conserve battery. This is a documented use-case for the Activity Transition API according to https://developer.android.com/develop/sensors-and-location/location/transitions</p> <p>Use the same text again when prompted for the <code>Activity recognition</code> permission description.</p> <p>Without <code>ACTIVITY_RECOGNITION</code>, the SDK falls back to location-only motion detection \u2014 requiring 200\u20131000 m of movement before tracking activates. If you want to remove it intentionally, add this to your <code>AndroidManifest.xml</code> and set <code>disableMotionActivityUpdates: true</code> in your config:</p> <pre><code>&lt;manifest&gt;\n  &lt;uses-permission\n    android:name=\"android.permission.ACTIVITY_RECOGNITION\"\n    tools:node=\"remove\" /&gt;\n&lt;/manifest&gt;\n</code></pre>"},{"location":"help/faq/#foreground_service_location","title":"<code>FOREGROUND_SERVICE_LOCATION</code>","text":"<p>The SDK uses this permission to run a foreground service that continuously monitors device location regardless of whether the app is terminated or the device rebooted. In the Play Console declaration, enable both \"User-initiated location sharing\" and \"Geofencing\". A short screen-recording of the app's location feature in action is sufficient for review \u2014 the Transistor Software demo app was approved with these declarations.</p>"},{"location":"help/faq/#geofencing","title":"Geofencing","text":""},{"location":"help/faq/#do-geofences-fire-when-the-app-is-terminated","title":"Do geofences fire when the app is terminated?","text":"<p>Yes. Both iOS and Android monitor geofences at the OS level, independent of whether the app is running. The SDK registers geofences with the native platform APIs, so ENTER/EXIT transitions fire even when the app is killed, provided the device has \"Always\" location permission.</p>"},{"location":"help/faq/#why-am-i-receiving-multiple-duplicate-exit-events","title":"Why am I receiving multiple duplicate <code>EXIT</code> events?","text":"<p>Android geofencing can produce spurious EXIT events, especially in poor GPS conditions or near a geofence boundary. The SDK includes a trigger-validation gate that confirms EXIT events with a follow-up location before firing them. If you still see duplicates, verify you have not registered the same <code>identifier</code> multiple times and that you are on the latest plugin version. Post a GitHub issue with logs if the problem persists.</p>"},{"location":"help/faq/#does-a-geofence-fire-an-enter-event-if-the-device-is-already-inside-when-monitoring-starts","title":"Does a geofence fire an <code>ENTER</code> event if the device is already inside when monitoring starts?","text":"<p>Yes, by default (<code>geofenceInitialTriggerEntry: true</code>). To suppress the immediate ENTER event when the device is already inside the fence at the time monitoring begins, set <code>geofenceInitialTriggerEntry: false</code> in your config.</p>"},{"location":"help/faq/#what-is-the-minimum-reliable-geofence-radius","title":"What is the minimum reliable geofence radius?","text":"<p>200 m or larger produces reliable results. Geofencing combines GPS, Wi-Fi, and cell towers. In areas without Wi-Fi, only cell tower changes trigger re-evaluation, making radii below 150\u2013200 m unreliable in rural environments.</p>"},{"location":"help/faq/#how-many-geofences-can-i-register","title":"How many geofences can I register?","text":"<p>Unlimited. The SDK has a purpose-built system for monitoring infinite geofences beyond what the OS natively supports. iOS allows ~20 simultaneously monitored geofences; Android allows ~100. When your total exceeds the OS maximum, the SDK performs periodic geospatial queries against its SQLite geofence database, activating those within <code>GeoConfig.geofenceProximityRadius</code> of the current position and swapping them in and out as the device moves.</p>"},{"location":"help/faq/#is-polygon-geofencing-supported","title":"Is polygon geofencing supported?","text":"<p>Yes, as an optional paid add-on (purchased separately). The system uses a two-stage algorithm: a minimum enclosing circle for coarse detection, then native C++ point-in-polygon checks for fine-grained precision. For large polygons in rural areas with sparse Wi-Fi, consider using multiple smaller circular geofences arranged at key boundaries instead.</p>"},{"location":"help/faq/#data-http","title":"Data &amp; HTTP","text":""},{"location":"help/faq/#how-does-the-plugin-send-locations-to-my-server","title":"How does the plugin send locations to my server?","text":"<p>The SDK uses a built-in HTTP service backed by a SQLite persist-first queue. A location is written to SQLite first, then the HTTP service uploads it to your configured <code>url</code>. On network failure, records stay in the queue and are retried automatically when connectivity restores. The plugin was originally built for disaster response where cellular networks are assumed unreliable.</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.ready({\n  http: {\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n    batchSync: false,       // true = multiple records per request\n    headers: { Authorization: 'Bearer yourtoken' },\n    method: 'POST',\n  },\n});\n</code></pre> <pre><code>await bg.BackgroundGeolocation.ready(bg.Config(\n  http: bg.HttpConfig(\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n    batchSync: false,       // true = multiple records per request\n    headers: {'Authorization': 'Bearer yourtoken'},\n    method: 'POST',\n  ),\n));\n</code></pre> <pre><code>try await BGGeo.shared.ready { config in\n    config.http.url = \"https://your.server.com/api/locations\"\n    config.http.autoSync = true\n    config.http.batchSync = false   // true = multiple records per request\n    config.http.headers = [\"Authorization\": \"Bearer yourtoken\"]\n    config.http.method = .post\n}\n</code></pre> <pre><code>BGGeo.instance.ready {\n    http.url = \"https://your.server.com/api/locations\"\n    http.autoSync = true\n    http.batchSync = false          // true = multiple records per request\n    http.headers = mapOf(\"Authorization\" to \"Bearer yourtoken\")\n    http.method = HttpMethod.POST\n}\n</code></pre>"},{"location":"help/faq/#my-server-receives-400-bad-request-errors","title":"My server receives <code>400 Bad Request</code> errors.","text":"<p>The error originates at your server, not the plugin. The plugin posts a JSON body with a <code>location</code> root property by default. Check your server logs, verify the expected JSON schema, and use <code>http.rootProperty</code> to change the root key name, or <code>persistence.locationTemplate</code> to customise the payload structure. Subscribe to <code>onHttp</code> to inspect the raw server response from the client side.</p>"},{"location":"help/faq/#i-changed-locationtemplate-but-old-400-errors-keep-retrying","title":"I changed <code>locationTemplate</code> but old 400 errors keep retrying.","text":"<p>The SDK encodes the JSON payload at the moment each location is recorded \u2014 it does not reformat existing records in SQLite when you change <code>locationTemplate</code>. New locations post correctly with the updated template, but previously recorded locations with the old format remain in the queue and keep retrying until <code>LoggerConfig.maxDaysToPersist</code> expires them (default 3 days). If your server is now returning 200 for new records but old ones keep failing, clear the queue immediately:</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.destroyLocations();\n</code></pre> <pre><code>await bg.BackgroundGeolocation.destroyLocations();\n</code></pre> <pre><code>try await BGGeo.shared.dataStore.destroyLocations()\n</code></pre> <pre><code>BGGeo.instance.dataStore.destroyLocations()\n</code></pre>"},{"location":"help/faq/#can-i-send-only-latitude-and-longitude-instead-of-the-full-location-object","title":"Can I send only latitude and longitude instead of the full location object?","text":"<p>Yes. Use <code>persistence.locationTemplate</code> to control the exact JSON structure posted per location:</p> TypeScriptDartSwiftKotlin <pre><code>await BackgroundGeolocation.ready({\n  http: {\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  },\n  persistence: {\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"ts\":\"&lt;%= timestamp %&gt;\"}',\n  },\n});\n</code></pre> <pre><code>await bg.BackgroundGeolocation.ready(bg.Config(\n  http: bg.HttpConfig(\n    url: 'https://your.server.com/api/locations',\n    autoSync: true,\n  ),\n  persistence: bg.PersistenceConfig(\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"ts\":\"&lt;%= timestamp %&gt;\"}',\n  ),\n));\n</code></pre> <pre><code>try await BGGeo.shared.ready { config in\n    config.http.url = \"https://your.server.com/api/locations\"\n    config.http.autoSync = true\n    config.persistence.locationTemplate = #\"{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"ts\":\"&lt;%= timestamp %&gt;\"}\"#\n}\n</code></pre> <pre><code>BGGeo.instance.ready {\n    http.url = \"https://your.server.com/api/locations\"\n    http.autoSync = true\n    persistence.locationTemplate = \"\"\"{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"ts\":\"&lt;%= timestamp %&gt;\"}\"\"\"\n}\n</code></pre>"},{"location":"help/faq/#does-the-plugin-work-offline","title":"Does the plugin work offline?","text":"<p>Yes. All locations are persisted to SQLite before any upload attempt. If the device is offline, records accumulate locally and upload automatically once connectivity is restored. The plugin handles this transparently with no extra configuration.</p>"},{"location":"help/faq/#what-is-the-uuid-field-on-each-location","title":"What is the <code>uuid</code> field on each location?","text":"<p><code>uuid</code> is a client-generated identifier attached to every recorded location. It lets you match a location received at your server with the corresponding entry in the plugin's debug logs. It has no other operational significance.</p>"},{"location":"help/faq/#can-i-use-firebase-firestore-instead-of-a-rest-endpoint","title":"Can I use Firebase Firestore instead of a REST endpoint?","text":"<p>Yes, via the optional Background Geolocation Firebase adapter (sold separately). It requires a Background Geolocation license for your platform in addition to the adapter license. The adapter supports Firestore only \u2014 not the Firebase Realtime Database.</p>"},{"location":"help/faq/#does-the-demo-server-at-trackertransistorsoftcom-store-my-data-permanently","title":"Does the demo server at <code>tracker.transistorsoft.com</code> store my data permanently?","text":"<p>No. It is a development and testing server only. Data is deleted automatically after a short period. Do not use it for production. The demo server is open-source \u2014 deploy your own instance for production use.</p>"},{"location":"help/faq/#account-access","title":"Account &amp; Access","text":""},{"location":"help/faq/#how-do-i-access-the-customer-dashboard","title":"How do I access the Customer Dashboard?","text":"<p>Visit transistorsoft.com/shop/customers and authenticate with your GitHub account. From there you can generate license keys, manage team members, view orders, and upgrade your plan.</p>"},{"location":"help/faq/#why-does-the-customer-dashboard-require-a-github-login-our-code-isnt-on-github","title":"Why does the Customer Dashboard require a GitHub login? Our code isn't on GitHub.","text":"<p>GitHub is used purely as an authentication mechanism \u2014 the same concept as \"Sign in with Google\" or \"Sign in with Apple\" \u2014 so Transistor Software doesn't have to manage passwords. It has nothing to do with where your source code is hosted. You can store your app on GitLab, Bitbucket, a private server, or nowhere at all; GitHub login is only used to verify your identity when accessing the Dashboard.</p>"},{"location":"help/faq/#the-customer-dashboard-shows-unauthorized","title":"The Customer Dashboard shows \"Unauthorized\".","text":"<p>This is almost always caused by an invalid or deleted GitHub account linked to your order. Contact support with your order number so the linked GitHub username can be corrected.</p>"},{"location":"help/faq/#how-do-i-change-my-github-account-or-transfer-the-license-to-a-new-owner","title":"How do I change my GitHub account or transfer the license to a new owner?","text":"<p>Log in to the Customer Dashboard to manage linked GitHub usernames. For administrative transfers (changing the Dashboard admin), contact support with your order number.</p>"},{"location":"help/faq/#can-i-change-the-android-applicationid-on-an-existing-key","title":"Can I change the Android <code>applicationId</code> on an existing key?","text":"<p>Keys are bound to the <code>applicationId</code> they were generated for and cannot be changed. If you need a key for a different <code>applicationId</code>, contact support with your order number. There is a one-time allowance for a regeneration; additional changes may incur a fee.</p>"},{"location":"help/faq/#where-is-my-invoice","title":"Where is my invoice?","text":"<p>Invoices are emailed automatically after purchase. Check your spam folder. If you still cannot find it, contact support with your order number and the correct billing email address.</p>"},{"location":"help/faq/#i-get-a-license-validation-failure-error-even-though-i-purchased-a-license","title":"I get a <code>LICENSE VALIDATION FAILURE</code> error even though I purchased a license.","text":"<p>Common causes:</p> <ol> <li>Wrong <code>applicationId</code> \u2014 the <code>applicationId</code> in <code>android/app/build.gradle</code> does not match what you entered when generating the key in the Customer Dashboard.</li> <li>Incorrect integration \u2014 the license key <code>&lt;meta-data&gt;</code> block is in the wrong location in <code>build.gradle</code>, or a required setup step was skipped.</li> <li>Debug build without key \u2014 in debug builds, a <code>LICENSE VALIDATION FAILURE</code> warning is expected and the plugin remains fully functional. It is not an error in debug mode.</li> </ol> <p>Enable verbose logging (<code>logger: { debug: true, logLevel: BackgroundGeolocation.LogLevel.Verbose }</code>), run the app, retrieve logs with <code>BackgroundGeolocation.logger.emailLog()</code>, and post a GitHub issue with the log file and your <code>android/app/build.gradle</code>.</p>"},{"location":"help/faq/#can-i-delete-a-key-i-generated-by-mistake","title":"Can I delete a key I generated by mistake?","text":"<p>Contact support with your order number. Keys can be destroyed and regenerated once without charge. Be precise when entering the <code>applicationId</code> on regeneration \u2014 subsequent changes may incur a fee. Once a key has been shipped in a production build, destroying the old key does not revoke it from devices already running that build.</p>"},{"location":"help/philosophy/","title":"Philosophy of Operation","text":"<p>The SDK is motion-driven, not interval-based. It does not fire location updates on a fixed schedule. Instead it watches the device's motion state and requests GPS only when the device is actually moving. This is the central design decision that makes continuous background tracking viable on a battery-powered device.</p>"},{"location":"help/philosophy/#two-states","title":"Two States","text":"<p>The SDK operates in exactly two states at any given time:</p> <p>Stationary \u2014 The device is not moving. The SDK holds a reference position and monitors for motion using the accelerometer and activity-recognition API. No GPS is requested. Power consumption is negligible.</p> <p>Moving \u2014 Motion has been confirmed. The SDK requests location updates from the OS according to your <code>distanceFilter</code> and <code>desiredAccuracy</code> settings. Locations are recorded and uploaded continuously.</p> <p>Transitions between the two states are automatic. You do not manage them manually.</p>"},{"location":"help/philosophy/#entering-moving-state","title":"Entering Moving State","text":"<p>When the device is stationary, the SDK listens to the platform's activity-recognition API (Core Motion on iOS, the Activity Recognition API on Android). When significant motion is detected \u2014 walking, running, cycling, in a vehicle \u2014 the SDK confirms it against the <code>motionTriggerDelay</code> window and transitions to moving state.</p> <p>On iOS, a supplementary mechanism is active while the app is terminated: the OS places a stationary geofence of <code>stationaryRadius</code> metres (default 150 m) around the last known position. When the device exits that geofence (~200 m of movement), iOS silently relaunches the app in the background and tracking resumes. This is an OS-level capability that requires no extra configuration beyond <code>stopOnTerminate: false</code>.</p>"},{"location":"help/philosophy/#entering-stationary-state","title":"Entering Stationary State","text":"<p>While moving, the SDK evaluates whether the device has stopped. A stop is confirmed only after the device has been stationary for <code>stopTimeout</code> minutes (default 5). This grace period prevents false stops at traffic lights, short pauses, or slow-speed crawl. Once confirmed, the SDK releases GPS, records the stop position, and returns to stationary monitoring.</p> <p>The <code>activity.stopDetectionDelay</code> setting adds an additional delay before stop-detection even begins evaluating, which is useful for vehicle tracking where brief parking should not be treated as a trip end.</p>"},{"location":"help/philosophy/#distance-filter-elasticity","title":"Distance Filter &amp; Elasticity","text":"<p>While moving, a new location is recorded only after the device has displaced at least <code>distanceFilter</code> metres from the last recorded position. This prevents redundant back-to-back samples when the device is moving slowly.</p> <p>At higher speeds the SDK automatically scales <code>distanceFilter</code> upward in proportion to speed \u2014 the faster the vehicle, the farther it must travel before another sample is taken. This elasticity keeps the sample rate approximately constant in time rather than exploding at highway speeds. Disable it with <code>geolocation.disableElasticity: true</code> if you need a fixed spatial resolution regardless of speed.</p>"},{"location":"help/philosophy/#persist-first-upload-second","title":"Persist First, Upload Second","text":"<p>Every location is written to an on-device SQLite database before any upload attempt. The HTTP service then reads from that queue and posts to your configured <code>http.url</code>. If the network is unavailable, records accumulate locally and are retried automatically when connectivity is restored. No location is ever lost to a network failure.</p> <p>This architecture was designed for disaster-response deployments where cellular coverage is assumed unreliable.</p>"},{"location":"help/philosophy/#what-the-sdk-does-not-do","title":"What the SDK Does Not Do","text":"<ul> <li>It does not poll on a fixed timer.</li> <li>It does not use <code>CLLocationManager.startUpdatingLocation</code> continuously in the background (iOS).</li> <li>It does not keep a wakelock permanently held (Android).</li> <li>It does not require the screen to be on.</li> <li>It does not bypass the OS's power-management policies \u2014 it works with them.</li> </ul>"},{"location":"kotlin/","title":"Kotlin SDK Reference","text":"native-background-geolocation Buy Now <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"kotlin/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<p><code>enum</code><code>AccuracyAuthorization</code><code>: Enum&lt;</code><code>AccuracyAuthorization</code><code>&gt;</code></p> <p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"kotlin/AccuracyAuthorization/#members","title":"Members","text":""},{"location":"kotlin/AccuracyAuthorization/#full","title":"FULL","text":"<p><code>val FULL:</code><code>AccuracyAuthorization</code></p> <p>Full accuracy authorized.</p>"},{"location":"kotlin/AccuracyAuthorization/#reduced","title":"REDUCED","text":"<p><code>val REDUCED:</code><code>AccuracyAuthorization</code></p> <p>Reduced accuracy granted.</p>"},{"location":"kotlin/ActivityChangeEvent/","title":"ActivityChangeEvent","text":"<p><code>class</code><code>ActivityChangeEvent</code></p> <p>Activity recognition result delivered to BGGeo.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onActivityChange { event -&gt;\n    Log.d(TAG, \"[onActivityChange] ${event.activity} ${event.confidence}\")\n}\n</code></pre>"},{"location":"kotlin/ActivityChangeEvent/#members","title":"Members","text":""},{"location":"kotlin/ActivityChangeEvent/#activity","title":"activity","text":"<pre><code>val activity: String\n</code></pre> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"kotlin/ActivityChangeEvent/#confidence","title":"confidence","text":"<pre><code>val confidence: Int\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"kotlin/ActivityConfig/","title":"ActivityConfig","text":"<p><code>class</code><code>ActivityConfig</code></p> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"kotlin/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"kotlin/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>import com.transistorsoft.locationmanager.kotlin.BGGeo\n\n// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    // Android: poll activity recognition every 10s\n    activity.activityRecognitionInterval = 10000.0\n\n    // Android: require &gt;=75% confidence before changing activity\n    activity.minimumActivityRecognitionConfidence = 75\n\n    // Cross-platform: enable/disable automatic stop detection\n    activity.disableStopDetection = false\n\n    // Cross-platform: automatically stop when stationary\n    activity.stopOnStationary = true\n\n    // Android: delay motion-trigger transitions by 30s\n    activity.motionTriggerDelay = 30000\n\n    // Android &amp; iOS: disable platform motion activity APIs\n    activity.disableMotionActivityUpdates = false\n\n    // iOS: delay stop-detection by 10s\n    // stopDetectionDelay \u2014 iOS only \u2014 not applicable on Android\n}\n</code></pre>"},{"location":"kotlin/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeolocationConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"kotlin/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.activityRecognitionInterval = 10000.0\n    activity.disableStopDetection = true\n    activity.stopOnStationary = true\n}\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.activityRecognitionInterval = 10000.0\n    activity.disableStopDetection = true\n    activity.stopOnStationary = true\n}\n</code></pre>"},{"location":"kotlin/ActivityConfig/#members","title":"Members","text":""},{"location":"kotlin/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>var activityRecognitionInterval: Double\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"kotlin/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>var disableMotionActivityUpdates: Boolean\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"kotlin/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"kotlin/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.disableMotionActivityUpdates = true\n}\n</code></pre>"},{"location":"kotlin/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>var disableStopDetection: Boolean\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"kotlin/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeolocationConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeolocationConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.disableStopDetection = true\n    // pausesLocationUpdatesAutomatically \u2014 iOS only \u2014 not applicable on Android\n}\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BGGeo.changePace).</p> <p></p>"},{"location":"kotlin/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BGGeo.changePace <code>false</code> or BGGeo.stop explicitly.</p>"},{"location":"kotlin/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>var minimumActivityRecognitionConfidence: Int\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"kotlin/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>var motionTriggerDelay: Int\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>// Delay Android motion-triggering by 30000ms\n// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.motionTriggerDelay = 30000\n}\n</code></pre>"},{"location":"kotlin/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>var stopOnStationary: Boolean\n</code></pre> <p>Automatically calls BGGeo.stop when the GeolocationConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeolocationConfig.stopTimeout timer elapses naturally. It does not fire when BGGeo.changePace <code>false</code> is called manually.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    activity.stopOnStationary = true\n}\nbgGeo.start()\n</code></pre>"},{"location":"kotlin/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<pre><code>var triggerActivities: String\n</code></pre> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See <code>TriggerActivity</code> for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>val bgGeo = BGGeo.instance\n// Only trigger tracking for vehicles\n// Within a coroutine scope\nbgGeo.ready {\n    activity.triggerActivities = \"in_vehicle\"\n}\n\n// Only trigger tracking for on_foot, walking and running\nbgGeo.ready {\n    activity.triggerActivities = \"on_foot, walking, running\"\n}\n</code></pre>"},{"location":"kotlin/App/","title":"App","text":"<p><code>class</code><code>App</code></p> <p>Background-task and power-save utilities.</p> <p>Access via <code>BGGeo.instance.app</code>.</p>"},{"location":"kotlin/App/#members","title":"Members","text":""},{"location":"kotlin/App/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>fun startBackgroundTask(callback: (taskId: Int) -&gt; Unit)\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>"},{"location":"kotlin/App/#ios","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>"},{"location":"kotlin/App/#android","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.app.startBackgroundTask { taskId -&gt;\n    try {\n        // Perform your long-running task here\n        Log.d(TAG, \"task executing\")\n        bgGeo.app.stopBackgroundTask(taskId)\n    } catch (e: Exception) {\n        Log.e(TAG, \"Error: $e\")\n        bgGeo.app.stopBackgroundTask(taskId)\n    }\n}\n</code></pre>"},{"location":"kotlin/App/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>fun stopBackgroundTask(taskId: Int)\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.app.startBackgroundTask { taskId -&gt;\n    // Perform your long-running task here\n    Log.d(TAG, \"task executing\")\n    // When your long-running task is complete, signal completion of taskId.\n    bgGeo.app.stopBackgroundTask(taskId)\n}\n</code></pre>"},{"location":"kotlin/AppConfig/","title":"AppConfig","text":"<p><code>class</code><code>AppConfig</code></p> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"kotlin/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"kotlin/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"kotlin/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BGGeo.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"kotlin/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BGGeo.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"kotlin/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeolocationConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See <code>PermissionRationale</code> for the full dialog configuration reference.</p>"},{"location":"kotlin/AppConfig/#examples","title":"Examples","text":""},{"location":"kotlin/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    app.enableHeadless = true\n    app.heartbeatInterval = 60.0\n    app.backgroundPermissionRationale = mapOf(\n        \"title\" to \"Allow MyApp to access this device's location even when closed.\",\n        \"message\" to \"This app collects location data to record your trips and mileage.\",\n        \"positiveAction\" to \"Change to \\\"{backgroundPermissionOptionLabel}\\\"\",\n        \"negativeAction\" to \"Cancel\"\n    )\n    app.schedule = listOf(\"1-5 09:00-17:00\")  // Weekdays 9-5\n    app.scheduleUseAlarmManager = true\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.config.edit {\n    app.heartbeatInterval = 120.0  // slow down heartbeats\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#members","title":"Members","text":""},{"location":"kotlin/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<pre><code>var backgroundPermissionRationale: Map&lt;String, String&gt;\n</code></pre> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeolocationConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.locationAuthorizationRequest = LocationAuthorizationRequest.ALWAYS\n    app.backgroundPermissionRationale = mapOf(\n        \"title\" to \"Allow {applicationName} to access this device's location in the background?\",\n        \"message\" to \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n        \"positiveAction\" to \"Change to {backgroundPermissionOptionLabel}\",\n        \"negativeAction\" to \"Cancel\"\n    )\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside <code>PermissionRationale</code> fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeolocationConfig.locationAuthorizationRequest - BGGeo.requestPermission - Location updates in Android 11</p>"},{"location":"kotlin/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>var enableHeadless: Boolean\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BGGeo.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BGGeo.registerHeadlessTask</p>"},{"location":"kotlin/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>var heartbeatInterval: Double\n</code></pre> <p>Rate in seconds at which BGGeo.onHeartbeat events fire while the app is in the background.</p>"},{"location":"kotlin/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"kotlin/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BGGeo.onHeartbeat</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.heartbeatInterval = 60.0\n}\n\nbgGeo.onHeartbeat { event -&gt;\n    Log.d(TAG, \"[onHeartbeat] $event\")\n\n    // Optionally request a new location during heartbeat.\n    kotlinx.coroutines.runBlocking {\n        val location = bgGeo.getCurrentPosition(samples = 1, persist = true)\n        Log.d(TAG, \"[getCurrentPosition] $location\")\n    }\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#notification","title":"notification","text":"<pre><code>val notification\n</code></pre> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    // notification is configured via AndroidManifest.xml or at runtime\n    // using the Android notification API directly\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#schedule","title":"schedule","text":"<pre><code>var schedule: List&lt;String&gt;\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"DAY(s) START_TIME-END_TIME\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    app.schedule = listOf(\n        \"1 17:30-21:00\",     // Sunday: 5:30pm-9pm\n        \"2-6 09:00-17:00\",   // Mon-Fri: 9am-5pm\n        \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm-midnight\n        \"7 10:00-19:00\"      // Saturday: 10am-7pm\n    )\n}\n\n// Start the Scheduler\nbgGeo.startSchedule()\n\n// Listen for schedule state changes\nbgGeo.onSchedule { event -&gt;\n    Log.d(TAG, \"[onSchedule] enabled? ${event.state}\")\n}\n\n// Later (e.g., user logout)\nbgGeo.stopSchedule()\nbgGeo.stop()  // if tracking is currently enabled\n\n// Modify schedule using config.edit\nbgGeo.config.edit {\n    app.schedule = listOf(\n        \"1-7 09:00-10:00\",\n        \"1-7 11:00-12:00\",\n        \"1-7 13:00-14:00\",\n        \"1-7 15:00-16:00\",\n        \"1-7 17:00-18:00\",\n        \"2,4,6 19:00-22:00\"\n    )\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.schedule = listOf(\"2018-01-01 09:00-17:00\")\n}\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.schedule = listOf(\"2018-01-01-09:00 2019-01-01-17:00\")  // track for 1 year\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.schedule = listOf(\n        \"1-7 09:00-17:00 location\",\n        \"1-7 18:00-12:00 geofence\"\n    )\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"kotlin/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BGGeo.startSchedule - BGGeo.stopSchedule</p>"},{"location":"kotlin/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.schedule = listOf(\n        \"1-7 09:00-17:00 location\",\n        \"1-7 18:00-12:00 geofence\"\n    )\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>var scheduleUseAlarmManager: Boolean\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    app.schedule = listOf(\"1-7 09:00-17:00\")\n    app.scheduleUseAlarmManager = true\n}\n</code></pre>"},{"location":"kotlin/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>var startOnBoot: Boolean\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"kotlin/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"kotlin/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"kotlin/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>var stopOnTerminate: Boolean\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"kotlin/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeolocationConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"kotlin/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"kotlin/Authorization/","title":"Authorization","text":"<p><code>class</code><code>Authorization</code></p> <p>Location permission management \u2014 request and monitor authorisation status.</p> <p>Access via <code>BGGeo.instance.authorization</code>.</p>"},{"location":"kotlin/Authorization/#members","title":"Members","text":""},{"location":"kotlin/Authorization/#getstate","title":"getState","text":"<p><code>fun get</code><code>State</code><code>():</code><code>ProviderChangeEvent</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval providerState = bgGeo.authorization.getState()\nLog.d(TAG, \"- Provider state: $providerState\")\n</code></pre>"},{"location":"kotlin/Authorization/#requestpermission","title":"requestPermission","text":"<p><code>suspend fun requestPermission():</code><code>PermissionStatus</code></p> <p>Manually request location permission using the configured GeolocationConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeolocationConfig.locationAuthorizationRequest - GeolocationConfig.disableLocationAuthorizationAlert - GeolocationConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\ntry {\n    val status = bgGeo.authorization.requestPermission()\n    Log.d(TAG, \"[requestPermission] success: $status\")\n} catch (e: Exception) {\n    Log.w(TAG, \"[requestPermission] FAILURE: $e\")\n}\n</code></pre>"},{"location":"kotlin/Authorization/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<p><code>suspend fun requestTemporaryFullAccuracy(purpose: String):</code><code>AccuracyAuthorization</code></p> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>"},{"location":"kotlin/Authorization/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onProviderChange { event -&gt;\n    if (AccuracyAuthorization.fromValue(event.accuracyAuthorization) == AccuracyAuthorization.REDUCED) {\n        try {\n            val accuracyAuthorization = kotlinx.coroutines.runBlocking {\n                bgGeo.authorization.requestTemporaryFullAccuracy(\"Delivery\")\n            }\n            if (accuracyAuthorization == AccuracyAuthorization.FULL) {\n                Log.d(TAG, \"[requestTemporaryFullAccuracy] GRANTED: $accuracyAuthorization\")\n            } else {\n                Log.d(TAG, \"[requestTemporaryFullAccuracy] DENIED: $accuracyAuthorization\")\n            }\n        } catch (e: Exception) {\n            Log.w(TAG, \"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: $e\")\n        }\n    }\n}\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/","title":"AuthorizationConfig","text":"<p><code>class</code><code>AuthorizationConfig</code></p> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"kotlin/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"kotlin/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>val bgGeo = BGGeo.instance\nval accessToken = \"my-access-token\"\nval refreshToken = \"my-refresh-token\"\n\n// Listen for authorization refresh results\nbgGeo.onAuthorization { event -&gt;\n    if (event.isSuccess) {\n        Log.d(TAG, \"[authorization] SUCCESS: ${event.response}\")\n    } else {\n        Log.e(TAG, \"[authorization] ERROR: ${event.error}\")\n    }\n}\n\n// Initialize with JWT authorization\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://app.your.server.com/users/locations\"\n    http.autoSync = true\n    authorization.strategy = AuthorizationStrategy.JWT\n    authorization.accessToken = accessToken\n    authorization.refreshToken = refreshToken\n    authorization.refreshUrl = \"https://auth.your.server.com/tokens\"\n    authorization.refreshPayload = mapOf(\n        \"the_refresh_token_field_name\" to \"{refreshToken}\"\n    )\n    authorization.expires = -1\n}\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"kotlin/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"kotlin/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BGGeo.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.onAuthorization { event -&gt;\n    if (event.isSuccess) {\n        Log.d(TAG, \"[authorization] SUCCESS: ${event.response}\")\n    } else {\n        Log.e(TAG, \"[authorization] ERROR: ${event.error}\")\n    }\n}\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/#members","title":"Members","text":""},{"location":"kotlin/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>var accessToken: String\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    authorization.strategy = AuthorizationStrategy.JWT\n    authorization.accessToken = \"XXX.YYY.ZZZ\"\n}\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>var expires: Long\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"kotlin/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>var refreshHeaders: Map&lt;String, Any&gt;\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    authorization.accessToken = \"XXX.YYY.ZZZ\"\n    authorization.refreshUrl = \"https://auth.domain.com/tokens\"\n    authorization.refreshToken = \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n    authorization.refreshPayload = mapOf(\n        \"my_refresh_token\" to \"{refreshToken}\",\n        \"grant_type\" to \"refresh_token\",\n        \"foo\" to \"another arbitrary field\"\n    )\n    authorization.refreshHeaders = emptyMap()  // Empty map to provide no refreshHeaders.\n}\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>var refreshPayload: Map&lt;String, Any&gt;\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    authorization.strategy = AuthorizationStrategy.JWT\n    authorization.accessToken = \"XXX.YYY.ZZZ\"\n    authorization.refreshUrl = \"https://auth.your.server.com/tokens\"\n    authorization.refreshToken = \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n    authorization.refreshPayload = mapOf(\n        \"my_refresh_token\" to \"{refreshToken}\",\n        \"grant_type\" to \"refresh_token\",\n        \"foo\" to \"another arbitrary field\"\n    )\n}\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"kotlin/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>var refreshToken: String\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"kotlin/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>var refreshUrl: String\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BGGeo.onAuthorization with an AuthorizationEvent.</p>"},{"location":"kotlin/AuthorizationConfig/#strategy","title":"strategy","text":"<p><code>var strategy:</code><code>AuthorizationStrategy</code></p> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"kotlin/AuthorizationEvent/","title":"AuthorizationEvent","text":"<p><code>class</code><code>AuthorizationEvent</code></p> <p>Authorization token refresh result delivered to BGGeo.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.onAuthorization { event -&gt;\n    if (event.isSuccess) {\n        Log.d(TAG, \"[onAuthorization] Token refreshed: ${event.response}\")\n        // Update your app's stored token.\n        val token = (event.response as? Map&lt;*, *&gt;)?.get(\"access_token\")\n    } else {\n        Log.w(TAG, \"[onAuthorization] Refresh failed: ${event.error}\")\n    }\n}\n</code></pre>"},{"location":"kotlin/AuthorizationEvent/#members","title":"Members","text":""},{"location":"kotlin/AuthorizationEvent/#error","title":"error","text":"<pre><code>val error: String?\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"kotlin/AuthorizationEvent/#issuccess","title":"isSuccess","text":"<pre><code>val isSuccess: Boolean\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"kotlin/AuthorizationEvent/#response","title":"response","text":"<pre><code>val response: Map&lt;String, Any&gt;?\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"kotlin/AuthorizationEvent/#statuscode","title":"statusCode","text":"<pre><code>val statusCode: Int\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"kotlin/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<p><code>enum</code><code>AuthorizationStrategy</code><code>: Enum&lt;</code><code>AuthorizationStrategy</code><code>&gt;</code></p> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"kotlin/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"kotlin/AuthorizationStrategy/#jwt","title":"JWT","text":"<p><code>val JWT:</code><code>AuthorizationStrategy</code></p> <p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"kotlin/AuthorizationStrategy/#sas","title":"SAS","text":"<p><code>val SAS:</code><code>AuthorizationStrategy</code></p> <p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"kotlin/BGGeo/","title":"BGGeo","text":"<p><code>class</code><code>BGGeo</code></p> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>"},{"location":"kotlin/BGGeo/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>"},{"location":"kotlin/BGGeo/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>"},{"location":"kotlin/BGGeo/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>"},{"location":"kotlin/BGGeo/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeolocationConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li><code>Event</code></li> </ul>"},{"location":"kotlin/BGGeo/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a <code>Subscription</code> that must be removed when no longer needed:</p>"},{"location":"kotlin/BGGeo/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    logger.logLevel = LogLevel.DEBUG\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#examples","title":"Examples","text":""},{"location":"kotlin/BGGeo/#compound-configuration","title":"Compound configuration","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 20f\n    http.url = \"https://example.com/locations\"\n    http.autoSync = true\n    persistence.maxDaysToPersist = 7\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#event-listeners","title":"Event listeners","text":"<pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onLocation { location -&gt;\n    Log.d(TAG, \"New location: $location\")\n}\n\nbgGeo.onMotionChange { event -&gt;\n    Log.d(TAG, \"Device is moving? ${event.isMoving}\")\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>val bgGeo = BGGeo.instance\n\nval sub = bgGeo.onHttp { event -&gt;\n    // Handle event\n}\n\nsub.close()\n</code></pre>"},{"location":"kotlin/BGGeo/#getting-started","title":"Getting started","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.distanceFilter = 10f\n    http.url = \"https://example.com/locations\"\n    http.autoSync = true\n}\n\nif (!bgGeo.enabled) {\n    bgGeo.start()\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#events_1","title":"Events","text":""},{"location":"kotlin/BGGeo/#onactivitychange","title":"onActivityChange","text":"<p><code>fun onActivityChange(callback: (</code><code>ActivityChangeEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>"},{"location":"kotlin/BGGeo/#android","title":"Android","text":"<p>ActivityChangeEvent.confidence always reports <code>100</code>.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onActivityChange { event -&gt;\n    Log.d(TAG, \"[onActivityChange] ${event.activity} (${event.confidence}%)\")\n}\n</code></pre> <p>activitychange</p>"},{"location":"kotlin/BGGeo/#onauthorization","title":"onAuthorization","text":"<p><code>fun on</code><code>Authorization</code><code>(callback: (</code><code>AuthorizationEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onAuthorization { event -&gt;\n    if (event.isSuccess) {\n        Log.d(TAG, \"[authorization] SUCCESS: ${event.response}\")\n    } else {\n        Log.d(TAG, \"[authorization] ERROR: ${event.error}\")\n    }\n}\n</code></pre> <p>authorization</p>"},{"location":"kotlin/BGGeo/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>fun onConnectivityChange(callback: (</code><code>ConnectivityChangeEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onConnectivityChange { event -&gt;\n    Log.d(TAG, \"[onConnectivityChange] isConnected: ${event.isConnected}\")\n}\n</code></pre> <p>connectivitychange</p>"},{"location":"kotlin/BGGeo/#onenabledchange","title":"onEnabledChange","text":"<pre><code>fun onEnabledChange(callback: (EnabledChangeEvent) -&gt; Unit): EventSubscription\n</code></pre> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onEnabledChange { event -&gt;\n    Log.d(TAG, \"[onEnabledChange] isEnabled? ${event.enabled}\")\n}\n</code></pre> <p>enabledchange</p>"},{"location":"kotlin/BGGeo/#ongeofence","title":"onGeofence","text":"<p><code>fun on</code><code>Geofence</code><code>(callback: (</code><code>GeofenceEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onGeofence { event -&gt;\n    Log.d(TAG, \"[onGeofence] $event\")\n}\n</code></pre> <p>geofence</p>"},{"location":"kotlin/BGGeo/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>fun onGeofencesChange(callback: (</code><code>GeofencesChangeEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeolocationConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onGeofencesChange { event -&gt;\n    val on = event.on     // new geofences activated\n    val off = event.off   // geofences that were just de-activated\n\n    // Create map circles\n    on.forEach { geofence -&gt;\n        Log.d(TAG, \"activated: ${geofence.identifier}\")\n    }\n\n    // Remove map circles\n    off.forEach { identifier -&gt;\n        Log.d(TAG, \"deactivated: $identifier\")\n    }\n}\n</code></pre> <p>geofenceschange</p>"},{"location":"kotlin/BGGeo/#onheartbeat","title":"onHeartbeat","text":"<p><code>fun onHeartbeat(callback: (</code><code>HeartbeatEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The LocationEvent provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.config.edit {\n    app.heartbeatInterval = 60.0\n}\n\nval subscription = bgGeo.onHeartbeat { event -&gt;\n    Log.d(TAG, \"[onHeartbeat] $event\")\n\n    // You could request a new location if you wish.\n    kotlinx.coroutines.runBlocking {\n        val location = bgGeo.getCurrentPosition(samples = 1, persist = true)\n        Log.d(TAG, \"[getCurrentPosition] $location\")\n    }\n}\n</code></pre> <p>heartbeat</p>"},{"location":"kotlin/BGGeo/#onhttp","title":"onHttp","text":"<p><code>fun onHttp(callback: (</code><code>HttpEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onHttp { response -&gt;\n    val status = response.statusCode\n    val success = response.isSuccess\n    val responseText = response.responseText\n    Log.d(TAG, \"[onHttp] $response\")\n}\n</code></pre> <p>http</p>"},{"location":"kotlin/BGGeo/#onlocation","title":"onLocation","text":"<p><code>fun on</code><code>Location</code><code>(callback: (</code><code>LocationEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>"},{"location":"kotlin/BGGeo/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a <code>LocationError</code> code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with LocationEvent.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onLocation { location -&gt;\n    Log.d(TAG, \"[onLocation] success: $location\")\n}\n</code></pre> <p>location</p>"},{"location":"kotlin/BGGeo/#onmotionchange","title":"onMotionChange","text":"<p><code>fun onMotionChange(callback: (</code><code>LocationEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeolocationConfig.stopTimeout</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onMotionChange { event -&gt;\n    if (event.isMoving) {\n        Log.d(TAG, \"[onMotionChange] Device has just started MOVING ${event.location}\")\n    } else {\n        Log.d(TAG, \"[onMotionChange] Device has just STOPPED: ${event.location}\")\n    }\n}\n</code></pre> <p>motionchange</p>"},{"location":"kotlin/BGGeo/#onnotificationaction","title":"onNotificationAction","text":"<pre><code>fun onNotificationAction(callback: (String) -&gt; Unit): EventSubscription\n</code></pre> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p>"},{"location":"kotlin/BGGeo/#onpowersavechange","title":"onPowerSaveChange","text":"<pre><code>fun onPowerSaveChange(callback: (PowerSaveChangeEvent) -&gt; Unit): EventSubscription\n</code></pre> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>"},{"location":"kotlin/BGGeo/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"kotlin/BGGeo/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onPowerSaveChange { event -&gt;\n    Log.d(TAG, \"[onPowerSaveChange] isPowerSaveMode: ${event.isPowerSaveMode}\")\n}\n</code></pre> <p>powersavechange</p>"},{"location":"kotlin/BGGeo/#onproviderchange","title":"onProviderChange","text":"<p><code>fun onProviderChange(callback: (</code><code>ProviderChangeEvent</code><code>) -&gt; Unit): EventSubscription</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onProviderChange { event -&gt;\n    Log.d(TAG, \"[onProviderChange] $event\")\n\n    when (PermissionStatus.fromValue(event.status)) {\n        PermissionStatus.DENIED -&gt;\n            Log.d(TAG, \"- Location authorization denied\")\n        PermissionStatus.ALWAYS -&gt;\n            Log.d(TAG, \"- Location always granted\")\n        PermissionStatus.WHEN_IN_USE -&gt;\n            Log.d(TAG, \"- Location WhenInUse granted\")\n    }\n}\n</code></pre> <p>providerchange</p>"},{"location":"kotlin/BGGeo/#onschedule","title":"onSchedule","text":"<pre><code>fun onSchedule(callback: (ScheduleEvent) -&gt; Unit): EventSubscription\n</code></pre> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>val bgGeo = BGGeo.instance\nval subscription = bgGeo.onSchedule { event -&gt;\n    if (event.state[\"enabled\"] == true) {\n        Log.d(TAG, \"[onSchedule] scheduled start tracking\")\n    } else {\n        Log.d(TAG, \"[onSchedule] scheduled stop tracking\")\n    }\n}\n</code></pre> <p>schedule</p>"},{"location":"kotlin/BGGeo/#methods","title":"Methods","text":""},{"location":"kotlin/BGGeo/#changepace","title":"changePace","text":"<pre><code>fun changePace(isMoving: Boolean)\n</code></pre> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.changePace(true)  // &lt;-- Location-services ON (\"moving\" state)\nbgGeo.changePace(false) // &lt;-- Location-services OFF (\"stationary\" state)\n</code></pre>"},{"location":"kotlin/BGGeo/#getcurrentposition","title":"getCurrentPosition","text":"<pre><code>suspend fun getCurrentPosition( timeout: Int = 30, desiredAccuracy: Double = 0.0, maximumAge: Long = 0, samples: Int = 3, persist: Boolean = true, extras: Map&lt;String, Any&gt;? = null )\n</code></pre> <p>Retrieve the current LocationEvent.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a <code>LocationError</code>.</p>"},{"location":"kotlin/BGGeo/#options","title":"Options","text":"<p>See <code>CurrentPositionRequest</code>.</p>"},{"location":"kotlin/BGGeo/#error-codes_1","title":"Error Codes","text":"<p>See <code>LocationError</code>.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with LocationEvent.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval location = bgGeo.getCurrentPosition(\n    timeout = 30,\n    maximumAge = 5000L,\n    desiredAccuracy = 10.0,\n    samples = 3,\n    extras = mapOf(\"route_id\" to 123)\n)\n</code></pre>"},{"location":"kotlin/BGGeo/#ready","title":"ready","text":"<p><code>suspend fun ready( reset: Boolean = true, transistorAuthorizationToken:</code><code>TransistorToken</code><code>? = null, configure: ConfigEditor.() -&gt; Unit )</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    http.url = \"http://your.server.com\"\n    http.headers = mapOf(\"my-auth-token\" to \"secret-token\")\n}\nLog.d(TAG, \"[ready] success\")\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.config.reset()\n// Reset to documented default-values with overrides\nbgGeo.config.edit {\n    geolocation.distanceFilter = 10f\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#removelisteners","title":"removeListeners","text":"<pre><code>fun removeListeners()\n</code></pre> <p>Remove all event listeners.</p> <p>Calls <code>Subscription.remove</code> on all active subscriptions.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.removeListeners()\n</code></pre>"},{"location":"kotlin/BGGeo/#resetodometer","title":"resetOdometer","text":"<pre><code>suspend fun resetOdometer()\n</code></pre> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval location = bgGeo.setOdometer(0.0)\nLog.d(TAG, \"[setOdometer] success: $location\")\n</code></pre>"},{"location":"kotlin/BGGeo/#setodometer","title":"setOdometer","text":"<pre><code>suspend fun setOdometer( value: Double, )\n</code></pre> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval location = bgGeo.setOdometer(1234.56)\nLog.d(TAG, \"[setOdometer] success: $location\")\n</code></pre>"},{"location":"kotlin/BGGeo/#start","title":"start","text":"<pre><code>suspend fun start()\n</code></pre> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.start()\nLog.d(TAG, \"[start] success\")\n</code></pre>"},{"location":"kotlin/BGGeo/#startgeofences","title":"startGeofences","text":"<pre><code>suspend fun startGeofences()\n</code></pre> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\n// Add a geofence.\nbgGeo.geofences.add(\n    Geofence.Builder()\n        .setIdentifier(\"ZONE_OF_INTEREST\")\n        .setLatitude(37.234232)\n        .setLongitude(42.234234)\n        .setRadius(200f)\n        .setNotifyOnExit(true)\n        .build()\n)\n\n// Listen to geofence events.\nbgGeo.onGeofence { event -&gt;\n    Log.d(TAG, \"[onGeofence] - $event\")\n}\n\n// Configure the plugin\nbgGeo.config.edit {\n    http.url = \"http://my.server.com\"\n    http.autoSync = true\n}\n\n// Start monitoring geofences.\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/BGGeo/#startschedule","title":"startSchedule","text":"<pre><code>fun startSchedule()\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.startSchedule()\nLog.d(TAG, \"[startSchedule] success\")\n</code></pre>"},{"location":"kotlin/BGGeo/#stop","title":"stop","text":"<pre><code>suspend fun stop()\n</code></pre> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.stop()\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (e.g., user logout)\nval bgGeo = BGGeo.instance\nbgGeo.stopSchedule()\n</code></pre>"},{"location":"kotlin/BGGeo/#stopschedule","title":"stopSchedule","text":"<pre><code>fun stopSchedule()\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.stopSchedule()\nLog.d(TAG, \"[stopSchedule] success\")\n</code></pre> <pre><code>// Within a coroutine scope\n// Later when you want to stop the Scheduler (e.g., user logout)\nval bgGeo = BGGeo.instance\nbgGeo.stopSchedule()\nif (bgGeo.enabled) {\n    bgGeo.stop()\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#watchposition","title":"watchPosition","text":"<p><code>fun watchPosition( interval: Long = 1000, timeout: Int = 60, persist: Boolean = false, extras: Map&lt;String, Any&gt;? = null, success: (</code><code>LocationEvent</code><code>) -&gt; Unit, failure: (Int) -&gt; Unit )</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a <code>Subscription</code> that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>"},{"location":"kotlin/BGGeo/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>val bgGeo = BGGeo.instance\nvar watchSubscription: EventSubscription? = null\n\nfun onResume() {\n    watchSubscription = bgGeo.watchPosition(\n        interval = 1000L,\n        persist = false,\n        extras = mapOf(\"foo\" to \"bar\"),\n        success = { location -&gt;\n            Log.d(TAG, \"[watchPosition] - $location\")\n        },\n        failure = { errorCode -&gt;\n            Log.d(TAG, \"[watchPosition] ERROR - $errorCode\")\n        }\n    )\n}\n\nfun onSuspend() {\n    watchSubscription?.close()\n    watchSubscription = null\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#properties","title":"Properties","text":""},{"location":"kotlin/BGGeo/#app","title":"app","text":"<pre><code>val app\n</code></pre> <p>App API \u2014 background-task and power-save utilities.</p>"},{"location":"kotlin/BGGeo/#authorization","title":"authorization","text":"<pre><code>val authorization\n</code></pre> <p>Authorization API \u2014 request and monitor location permissions.</p>"},{"location":"kotlin/BGGeo/#config","title":"config","text":"<pre><code>val config\n</code></pre> <p>Config API \u2014 read and update the SDK configuration.</p>"},{"location":"kotlin/BGGeo/#devicesettings","title":"deviceSettings","text":"<pre><code>val deviceSettings\n</code></pre> <p>DeviceSettings API</p>"},{"location":"kotlin/BGGeo/#geofences","title":"geofences","text":"<pre><code>val geofences\n</code></pre> <p>Access to the GeofenceManager \u2014 add, remove, and query geofences.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.geofences.add(geofence)\n</code></pre>"},{"location":"kotlin/BGGeo/#logger","title":"logger","text":"<pre><code>val logger\n</code></pre> <p>Logger API</p>"},{"location":"kotlin/BGGeo/#sensors","title":"sensors","text":"<pre><code>val sensors\n</code></pre> <p>Sensors API \u2014 query the device motion hardware.</p>"},{"location":"kotlin/BGGeo/#state","title":"state","text":"<p><code>val state:</code><code>State</code></p> <p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>val state = bgGeo.state\nif (state.enabled &amp;&amp; state.isMoving) {\n    Log.d(TAG, \"Tracking in motion, odometer: ${state.odometer}\")\n}\n</code></pre>"},{"location":"kotlin/BGGeo/#store","title":"store","text":"<pre><code>val store\n</code></pre> <p>DataStore API \u2014 query, upload, and destroy persisted location records.</p>"},{"location":"kotlin/Config/","title":"Config","text":"<p><code>class</code><code>Config</code></p> <p>Root configuration object passed to BGGeo.ready and BGGeo.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"kotlin/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"kotlin/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeolocationConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BGGeo.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BGGeo.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"kotlin/Config/#examples","title":"Examples","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 20f\n    geolocation.stopTimeout = 5\n    geolocation.stationaryRadius = 150\n\n    activity.disableStopDetection = false\n    activity.motionTriggerDelay = 30000\n\n    http.url = \"https://my.server.com/api/locations\"\n    http.method = HttpMethod.POST\n    http.autoSync = true\n    http.headers = mapOf(\"Authorization\" to \"Bearer secret-token\")\n    http.params = mapOf(\"user_id\" to 123)\n\n    persistence.persistMode = PersistMode.ALL\n    persistence.maxDaysToPersist = 14\n    persistence.extras = mapOf(\"appVersion\" to \"1.0.0\")\n\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    app.enableHeadless = true\n\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 3\n}\n\nLog.d(TAG, \"[ready] BackgroundGeolocation is configured and ready to use\")\n\nif (!bgGeo.enabled) {\n    bgGeo.start()\n}\n\n// To modify configuration after initialization, use config.edit.\nbgGeo.config.edit {\n    http.headers = mapOf(\"Authorization\" to \"Bearer new-token\")\n    logger.logLevel = LogLevel.INFO\n}\n\nbgGeo.store.sync()\n</code></pre>"},{"location":"kotlin/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 20f\n    http.url = \"https://my.server.com/api/locations\"\n    http.autoSync = true\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n}\n</code></pre>"},{"location":"kotlin/Config/#full-configuration","title":"Full configuration","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 20f\n    geolocation.stopTimeout = 5\n    geolocation.stationaryRadius = 150\n\n    activity.disableStopDetection = false\n    activity.motionTriggerDelay = 30000\n\n    http.url = \"https://my.server.com/api/locations\"\n    http.method = HttpMethod.POST\n    http.autoSync = true\n    http.headers = mapOf(\"Authorization\" to \"Bearer secret-token\")\n    http.params = mapOf(\"user_id\" to 123)\n\n    persistence.persistMode = PersistMode.ALL\n    persistence.maxDaysToPersist = 14\n    persistence.extras = mapOf(\"appVersion\" to \"1.0.0\")\n\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n    app.enableHeadless = true\n\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 3\n}\n\nif (!bgGeo.enabled) {\n    bgGeo.start()\n}\n\n// Update a subset of config at runtime.\nbgGeo.config.edit {\n    http.headers = mapOf(\"Authorization\" to \"Bearer new-token\")\n    logger.logLevel = LogLevel.INFO\n}\n</code></pre>"},{"location":"kotlin/Config/#members","title":"Members","text":""},{"location":"kotlin/Config/#activity","title":"activity","text":"<pre><code>val activity\n</code></pre> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"kotlin/Config/#app","title":"app","text":"<pre><code>val app\n</code></pre> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"kotlin/Config/#authorization","title":"authorization","text":"<pre><code>val authorization\n</code></pre> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"kotlin/Config/#geolocation","title":"geolocation","text":"<pre><code>val geolocation\n</code></pre> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeolocationConfig.</p>"},{"location":"kotlin/Config/#http","title":"http","text":"<pre><code>val http\n</code></pre> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"kotlin/Config/#logger","title":"logger","text":"<pre><code>val logger\n</code></pre> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"kotlin/Config/#persistence","title":"persistence","text":"<pre><code>val persistence\n</code></pre> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"kotlin/Config/#reset","title":"reset","text":"<pre><code>fun reset()\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BGGeo.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BGGeo.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"kotlin/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<p><code>class</code><code>ConnectivityChangeEvent</code></p> <p>Network connectivity change delivered to BGGeo.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onConnectivityChange { event -&gt;\n    Log.d(TAG, \"[onConnectivityChange] connected: ${event.isConnected}\")\n}\n</code></pre>"},{"location":"kotlin/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"kotlin/ConnectivityChangeEvent/#isconnected","title":"isConnected","text":"<pre><code>val isConnected: Boolean\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"kotlin/DataStore/","title":"DataStore","text":"<p><code>class</code><code>DataStore</code></p> <p>Persisted location record management \u2014 query, upload, and destroy stored locations.</p> <p>Access via <code>BGGeo.instance.store</code>.</p>"},{"location":"kotlin/DataStore/#members","title":"Members","text":""},{"location":"kotlin/DataStore/#all","title":"all","text":"<pre><code>suspend fun all(): List&lt;Map&lt;String, Any&gt;&gt;\n</code></pre> <p>Retrieve all LocationEvent records stored in the SDK's SQLite database.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval locations = bgGeo.store.all()\n</code></pre>"},{"location":"kotlin/DataStore/#count","title":"count","text":"<pre><code>val count: Int\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>val bgGeo = BGGeo.instance\n\nval count = bgGeo.store.count\n</code></pre>"},{"location":"kotlin/DataStore/#destroy","title":"destroy","text":"<pre><code>fun destroy(uuid: String)\n</code></pre> <p>Remove a single location by LocationEvent.uuid.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nval uuid = \"some-location-uuid\"\nbgGeo.store.destroy(uuid)\n</code></pre>"},{"location":"kotlin/DataStore/#destroyall","title":"destroyAll","text":"<pre><code>fun destroyAll()\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.store.destroyAll()\n</code></pre>"},{"location":"kotlin/DataStore/#sync","title":"sync","text":"<pre><code>suspend fun sync(): List&lt;Map&lt;String, Any&gt;&gt;\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\ntry {\n    val records = bgGeo.store.sync()\n    Log.d(TAG, \"[sync] success: $records\")\n} catch (e: Exception) {\n    Log.d(TAG, \"[sync] FAILURE: $e\")\n}\n</code></pre>"},{"location":"kotlin/DesiredAccuracy/","title":"DesiredAccuracy","text":"<p><code>enum</code><code>DesiredAccuracy</code><code>: Enum&lt;</code><code>DesiredAccuracy</code><code>&gt;</code></p> <p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"kotlin/DesiredAccuracy/#members","title":"Members","text":""},{"location":"kotlin/DesiredAccuracy/#high","title":"HIGH","text":"<p><code>val HIGH:</code><code>DesiredAccuracy</code></p> <p>High accuracy (e.g., 0-10m).</p>"},{"location":"kotlin/DesiredAccuracy/#low","title":"LOW","text":"<p><code>val LOW:</code><code>DesiredAccuracy</code></p> <p>Low accuracy (e.g., 100m).</p>"},{"location":"kotlin/DesiredAccuracy/#lowest","title":"LOWEST","text":"<p><code>val LOWEST:</code><code>DesiredAccuracy</code></p> <p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"kotlin/DesiredAccuracy/#medium","title":"MEDIUM","text":"<p><code>val MEDIUM:</code><code>DesiredAccuracy</code></p> <p>Medium accuracy (e.g., 10m).</p>"},{"location":"kotlin/DeviceSettings/","title":"DeviceSettings Android only","text":"<p><code>open class</code><code>DeviceSettings</code></p> <p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>val bgGeo = BGGeo.instance\n\nval isIgnoring = bgGeo.deviceSettings.isIgnoringBatteryOptimizations\nif (!isIgnoring) {\n    val req = bgGeo.deviceSettings.requestIgnoreBatteryOptimizations()\n    if (req != null &amp;&amp; !req.seen) {\n        bgGeo.deviceSettings.showIgnoreBatteryOptimizations()\n    }\n}\n</code></pre>"},{"location":"kotlin/DeviceSettings/#members","title":"Members","text":""},{"location":"kotlin/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>val isIgnoringBatteryOptimizations: Boolean\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p>"},{"location":"kotlin/DeviceSettings/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>val isPowerSaveMode: Boolean\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>"},{"location":"kotlin/DeviceSettings/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"kotlin/DeviceSettings/#android","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>val bgGeo = BGGeo.instance\n\nval isPowerSaveMode = bgGeo.deviceSettings.isPowerSaveMode\n</code></pre>"},{"location":"kotlin/DeviceSettings/#show","title":"show","text":"<pre><code>open fun show(context: Context, action: String): Boolean\n</code></pre> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>val bgGeo = BGGeo.instance\n\nval powerRequest = bgGeo.deviceSettings.requestPowerManager()\nif (powerRequest != null &amp;&amp; !powerRequest.seen) {\n    bgGeo.deviceSettings.showPowerManager()\n}\n</code></pre>"},{"location":"kotlin/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<pre><code>fun showIgnoreBatteryOptimizations(): Boolean\n</code></pre> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p>"},{"location":"kotlin/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<pre><code>fun showPowerManager(): Boolean\n</code></pre> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p>"},{"location":"kotlin/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<p><code>open class</code><code>DeviceSettingsRequest</code></p> <p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"kotlin/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"kotlin/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>val action: String\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"kotlin/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>val lastSeenAt: Long\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"kotlin/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>val seen: Boolean\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"kotlin/Geofence/","title":"Geofence","text":"<p><code>class</code><code>Geofence</code></p> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"kotlin/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"kotlin/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeolocationConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"kotlin/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BGGeo.addGeofence for a single geofence, or BGGeo.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p>"},{"location":"kotlin/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BGGeo.onGeofence. Subscribe to changes in the actively monitored set with BGGeo.onGeofencesChange.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Listen for geofence events.\nbgGeo.onGeofence { geofence -&gt;\n    Log.d(TAG, \"[geofence] ${geofence.identifier} ${geofence.action}\")\n}\n</code></pre> <p>Note</p> <p>When all geofences are removed, BGGeo.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"kotlin/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onGeofencesChange { event -&gt;\n    val on = event.on   // &lt;-- new geofences activated.\n    val off = event.off // &lt;-- geofences that were just de-activated.\n\n    // Create map circles\n    on.forEach { geofence -&gt;\n        Log.d(TAG, \"activated: ${geofence.identifier}\")\n    }\n\n    // Remove map circles\n    off.forEach { identifier -&gt;\n        Log.d(TAG, \"deactivated: $identifier\")\n    }\n}\n</code></pre>"},{"location":"kotlin/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeolocationConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeolocationConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"kotlin/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.geofences.remove(\"HOME\")\nLog.d(TAG, \"[removeGeofence] success\")\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.geofences.removeAll()\nLog.d(TAG, \"[removeGeofences] all geofences have been destroyed\")\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofences = bgGeo.geofences.getAll()\nLog.d(TAG, \"[getGeofences] $geofences\")\n</code></pre>"},{"location":"kotlin/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BGGeo.startGeofences instead of BGGeo.start to monitor geofences without continuous location tracking. Use GeolocationConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.onGeofence { geofence -&gt;\n    Log.d(TAG, \"[geofence] $geofence\")\n}\n\nbgGeo.ready {\n    http.url = \"http://your.server.com/geofences\"\n    http.autoSync = true\n    geolocation.geofenceModeHighAccuracy = true // &lt;-- consumes more power; default is false.\n}\n\n// engage geofences-only mode:\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/Geofence/#examples","title":"Examples","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\n// Listen to geofence events\nbgGeo.onGeofence { geofence -&gt;\n    Log.d(TAG, \"[geofence] $geofence\")\n    if (geofence.identifier == \"DANGER_ZONE\") {\n        if (geofence.action == \"ENTER\") {\n            // Entering the danger-zone, we want to aggressively track location.\n            kotlinx.coroutines.runBlocking { bgGeo.start() }\n        } else if (geofence.action == \"EXIT\") {\n            // Exiting the danger-zone, we resume geofences-only tracking.\n            kotlinx.coroutines.runBlocking { bgGeo.startGeofences() }\n        }\n    }\n}\n\n// Add a geofence.\nval geofence = Geofence.Builder()\n    .setIdentifier(\"DANGER_ZONE\")\n    .setRadius(1000f)\n    .setLatitude(45.51921926)\n    .setLongitude(-73.61678581)\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(true)\n    .build()\n\nbgGeo.geofences.add(geofence)\n\n// Ready the plugin.\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    http.url = \"http://your.server.com/locations\"\n    http.autoSync = true\n}\n\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofence = Geofence.Builder()\n    .setIdentifier(\"Home\")\n    .setRadius(200f)\n    .setLatitude(45.51921926)\n    .setLongitude(-73.61678581)\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(true)\n    .setExtras(mapOf(\"route_id\" to 1234))\n    .build()\n\ntry {\n    bgGeo.geofences.add(geofence)\n    Log.d(TAG, \"[addGeofence] success\")\n} catch (e: Exception) {\n    Log.d(TAG, \"[addGeofence] FAILURE: $e\")\n}\n</code></pre>"},{"location":"kotlin/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofences = listOf(\n    Geofence.Builder()\n        .setIdentifier(\"Home\")\n        .setRadius(200f)\n        .setLatitude(45.51921926)\n        .setLongitude(-73.61678581)\n        .setNotifyOnEntry(true)\n        .build(),\n    Geofence.Builder()\n        .setIdentifier(\"Work\")\n        .setRadius(200f)\n        .setLatitude(45.61921927)\n        .setLongitude(-73.71678582)\n        .setNotifyOnEntry(true)\n        .build()\n)\n\nbgGeo.geofences.addAll(geofences)\nLog.d(TAG, \"[addGeofences] success\")\n</code></pre>"},{"location":"kotlin/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofence = Geofence.Builder()\n    .setIdentifier(\"Park\")\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(true)\n    .setVertices(listOf(\n        listOf(45.518947279987714, -73.6049889209514),\n        listOf(45.5182711292279, -73.60338649600598),\n        listOf(45.517082240237634, -73.60432670908212),\n        listOf(45.51774871402813, -73.60604928622278)\n    ))\n    .build()\n\nbgGeo.geofences.add(geofence)\n</code></pre>"},{"location":"kotlin/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.geofences.remove(\"Home\")\n</code></pre>"},{"location":"kotlin/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.geofences.removeAll()\n</code></pre>"},{"location":"kotlin/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofences = bgGeo.geofences.getAll()\nLog.d(TAG, \"[getGeofences] $geofences\")\n</code></pre>"},{"location":"kotlin/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.onGeofence { event -&gt;\n    Log.d(TAG, \"[onGeofence] $event\")\n}\n\nbgGeo.ready {\n    http.url = \"https://your.server.com/geofences\"\n    http.autoSync = true\n    geolocation.geofenceModeHighAccuracy = true\n}\n\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.onGeofence { event -&gt;\n    if (event.identifier == \"DANGER_ZONE\") {\n        if (event.action == \"ENTER\") {\n            // Entering the zone \u2014 switch to full location tracking.\n            kotlinx.coroutines.runBlocking { bgGeo.start() }\n        } else if (event.action == \"EXIT\") {\n            // Exiting the zone \u2014 return to geofences-only mode.\n            kotlinx.coroutines.runBlocking { bgGeo.startGeofences() }\n        }\n    }\n}\n\nval geofence = Geofence.Builder()\n    .setIdentifier(\"DANGER_ZONE\")\n    .setRadius(1000f)\n    .setLatitude(45.51921926)\n    .setLongitude(-73.61678581)\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(true)\n    .build()\n\nbgGeo.geofences.add(geofence)\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    http.url = \"https://your.server.com/locations\"\n    http.autoSync = true\n}\n\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/Geofence/#members","title":"Members","text":""},{"location":"kotlin/Geofence/#entrystate","title":"entryState","text":"<p><code>val entry</code><code>State</code><code>: EntryState</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"kotlin/Geofence/#extras","title":"extras","text":"<pre><code>val extras: Map&lt;String, Any&gt;?\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"kotlin/Geofence/#hits","title":"hits","text":"<pre><code>val hits: Int\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"kotlin/Geofence/#identifier","title":"identifier","text":"<pre><code>val identifier: String\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"kotlin/Geofence/#latitude","title":"latitude","text":"<pre><code>val latitude: Double\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"kotlin/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>val loiteringDelay: Int\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"kotlin/Geofence/#longitude","title":"longitude","text":"<pre><code>val longitude: Double\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"kotlin/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>val notifyOnDwell: Boolean\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"kotlin/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>val notifyOnEntry: Boolean\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeolocationConfig.geofenceInitialTriggerEntry</p>"},{"location":"kotlin/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>val notifyOnExit: Boolean\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"kotlin/Geofence/#radius","title":"radius","text":"<pre><code>val radius: Float\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"kotlin/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>val stateUpdatedAt: Date?\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"kotlin/Geofence/#vertices","title":"vertices","text":"<pre><code>val vertices: List&lt;List&lt;Double&gt;&gt;?\n</code></pre> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofence = Geofence.Builder()\n    .setIdentifier(\"Home\")\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(true)\n    .setVertices(listOf(\n        listOf(45.518947279987714, -73.6049889209514),\n        listOf(45.5182711292279, -73.60338649600598),\n        listOf(45.517082240237634, -73.60432670908212),\n        listOf(45.51774871402813, -73.60604928622278)\n    ))\n    .build()\n\nbgGeo.geofences.add(geofence)\n</code></pre> <p> </p>"},{"location":"kotlin/GeofenceEvent/","title":"GeofenceEvent","text":"<p><code>class</code><code>GeofenceEvent</code></p> <p>Geofence transition delivered to BGGeo.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onGeofence { event -&gt;\n    Log.d(TAG, \"[onGeofence] ${event.identifier} ${event.action} ${event.location}\")\n}\n</code></pre>"},{"location":"kotlin/GeofenceEvent/#members","title":"Members","text":""},{"location":"kotlin/GeofenceEvent/#action","title":"action","text":"<pre><code>val action: String\n</code></pre> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"kotlin/GeofenceEvent/#extras","title":"extras","text":"<pre><code>val extras: Map&lt;String, Any&gt;?\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"kotlin/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>val identifier: String\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"kotlin/GeofenceEvent/#location","title":"location","text":"<p><code>val location:</code><code>LocationEvent</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"kotlin/GeofenceManager/","title":"GeofenceManager","text":"<p><code>class</code><code>GeofenceManager</code></p> <p>Manages the geofence database \u2014 add, remove, query, and check geofences.</p> <p>Access via <code>BGGeo.instance.geofences</code>. All methods are suspending coroutines (Kotlin) or async/await (Swift).</p>"},{"location":"kotlin/GeofenceManager/#methods","title":"Methods","text":"Method Description [add] Add a single geofence. [addAll] Add multiple geofences in bulk (~10\u00d7 faster than repeated [add]). [remove] Remove a geofence by identifier. [removeAll] Remove all geofences, or a specific subset. [get] Fetch a single geofence by identifier. [getAll] Fetch all stored geofences. [exists] Check whether a geofence with a given identifier exists."},{"location":"kotlin/GeofenceManager/#members","title":"Members","text":""},{"location":"kotlin/GeofenceManager/#add","title":"add","text":"<p><code>suspend fun add(geofence:</code><code>Geofence</code><code>)</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofence = Geofence.Builder()\n    .setIdentifier(\"Home\")\n    .setRadius(150f)\n    .setLatitude(45.51921926)\n    .setLongitude(-73.61678581)\n    .setNotifyOnEntry(true)\n    .setNotifyOnExit(false)\n    .setNotifyOnDwell(true)\n    .setLoiteringDelay(30000)  // 30 seconds\n    .setExtras(mapOf(\"zone_id\" to 1234))\n    .build()\n\ntry {\n    bgGeo.geofences.add(geofence)\n    Log.d(TAG, \"[addGeofence] success\")\n} catch (e: Exception) {\n    Log.d(TAG, \"[addGeofence] FAILURE: $e\")\n}\n</code></pre>"},{"location":"kotlin/GeofenceManager/#addall","title":"addAll","text":"<p><code>suspend fun addAll(geofences: List&lt;</code><code>Geofence</code><code>&gt;)</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofences = listOf(\n    Geofence.Builder()\n        .setIdentifier(\"foo\")\n        .setRadius(200f)\n        .setLatitude(45.51921926)\n        .setLongitude(-73.61678581)\n        .setNotifyOnEntry(true)\n        .build(),\n    Geofence.Builder()\n        .setIdentifier(\"bar\")\n        .setRadius(200f)\n        .setLatitude(45.51921926)\n        .setLongitude(-73.61678581)\n        .setNotifyOnEntry(true)\n        .build()\n)\n\nbgGeo.geofences.addAll(geofences)\n</code></pre>"},{"location":"kotlin/GeofenceManager/#exists","title":"exists","text":"<pre><code>suspend fun exists(identifier: String): Boolean\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval exists = bgGeo.geofences.exists(\"HOME\")\nLog.d(TAG, \"[geofenceExists] $exists\")\n</code></pre>"},{"location":"kotlin/GeofenceManager/#get","title":"get","text":"<p><code>suspend fun get(identifier: String):</code><code>Geofence</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofence = bgGeo.geofences.get(\"HOME\")\nLog.d(TAG, \"[getGeofence] $geofence\")\n</code></pre>"},{"location":"kotlin/GeofenceManager/#getall","title":"getAll","text":"<p><code>suspend fun getAll(): List&lt;</code><code>Geofence</code><code>&gt;</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval geofences = bgGeo.geofences.getAll()\nLog.d(TAG, \"[getGeofences] $geofences\")\n</code></pre>"},{"location":"kotlin/GeofenceManager/#remove","title":"remove","text":"<pre><code>suspend fun remove(identifier: String)\n</code></pre> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\ntry {\n    bgGeo.geofences.remove(\"Home\")\n    Log.d(TAG, \"[removeGeofence] success\")\n} catch (e: Exception) {\n    Log.d(TAG, \"[removeGeofence] FAILURE: $e\")\n}\n</code></pre>"},{"location":"kotlin/GeofenceManager/#removeall","title":"removeAll","text":"<pre><code>suspend fun removeAll(identifiers: List&lt;String&gt;? = null)\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.geofences.removeAll()\n</code></pre>"},{"location":"kotlin/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<p><code>class</code><code>GeofencesChangeEvent</code></p> <p>Geofence monitoring change delivered to BGGeo.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onGeofencesChange { event -&gt;\n    for (geofence in event.on) {\n        Log.d(TAG, \"[onGeofencesChange] Activated: ${geofence.identifier}\")\n    }\n    for (identifier in event.off) {\n        Log.d(TAG, \"[onGeofencesChange] Deactivated: $identifier\")\n    }\n}\n</code></pre>"},{"location":"kotlin/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"kotlin/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>val off: List&lt;String&gt;\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"kotlin/GeofencesChangeEvent/#on","title":"on","text":"<p><code>val on: List&lt;</code><code>Geofence</code><code>&gt;</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"kotlin/GeolocationConfig/","title":"GeolocationConfig","text":"<p><code>class</code><code>GeolocationConfig</code></p> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"kotlin/GeolocationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"kotlin/GeolocationConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BGGeo.ready or BGGeo.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // High-precision GPS\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n\n    // Move at least 50m before recording next location\n    geolocation.distanceFilter = 50f\n\n    // Consider stationary after 5 minutes with no motion\n    geolocation.stopTimeout = 5\n\n    // Automatically stop tracking after 120 minutes\n    geolocation.stopAfterElapsedMinutes = 120\n\n    // Noise-reduction / denoising filter\n    geolocation.filter.policy = LocationFilterPolicy.Adjust\n    geolocation.filter.maxImpliedSpeed = 60.0\n    geolocation.filter.odometerAccuracyThreshold = 20.0\n    geolocation.filter.trackingAccuracyThreshold = 100.0\n\n    // Geofencing behavior\n    geolocation.geofenceProximityRadius = 1000\n    geolocation.geofenceInitialTriggerEntry = true\n    geolocation.geofenceModeHighAccuracy = true\n\n    // Permissions\n    geolocation.locationAuthorizationRequest = LocationAuthorizationRequest.ALWAYS\n    geolocation.disableLocationAuthorizationAlert = false\n\n    http.url = \"https://example.com/api/locations\"\n    http.autoSync = true\n    http.batchSync = true\n    http.maxBatchSize = 10\n    http.method = HttpMethod.POST\n    http.params = mapOf(\"user_id\" to 1234, \"trip_id\" to 5678)\n    http.headers = mapOf(\"X-FOO\" to \"bar\")\n\n    app.stopOnTerminate = false\n    app.startOnBoot = true\n\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"kotlin/GeolocationConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"kotlin/GeolocationConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BGGeo.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"kotlin/GeolocationConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BGGeo.onProviderChange.</p>"},{"location":"kotlin/GeolocationConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BGGeo.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"kotlin/GeolocationConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilterConfig for full documentation.</p>"},{"location":"kotlin/GeolocationConfig/#examples","title":"Examples","text":"<pre><code>// Legacy (flat) \u2014 now use geolocation namespace\nval bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    geolocation.stopTimeout = 5\n    geolocation.stationaryRadius = 150\n    geolocation.locationTimeout = 60\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    geolocation.stopTimeout = 5\n    geolocation.stationaryRadius = 150\n    geolocation.locationTimeout = 60\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n    geolocation.distanceFilter = 10f\n    geolocation.stopTimeout = 5\n    // showsBackgroundLocationIndicator is iOS only \u2014 not applicable on Android\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.useSignificantChangesOnly = true\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.geofenceProximityRadius = 1000L\n    geolocation.geofenceInitialTriggerEntry = true\n    geolocation.geofenceModeHighAccuracy = true\n    geolocation.desiredAccuracy = DesiredAccuracy.MEDIUM\n    geolocation.locationUpdateInterval = 5000\n    geolocation.distanceFilter = 0f\n}\n\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#members","title":"Members","text":""},{"location":"kotlin/GeolocationConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>var allowIdenticalLocations: Boolean\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"kotlin/GeolocationConfig/#defertime","title":"deferTime","text":"<pre><code>var deferTime: Long\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"kotlin/GeolocationConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>var desiredAccuracy:</code><code>DesiredAccuracy</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>var disableElasticity: Boolean\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"kotlin/GeolocationConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>var disableLocationAuthorizationAlert: Boolean\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BGGeo.onProviderChange.</p>"},{"location":"kotlin/GeolocationConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"kotlin/GeolocationConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BGGeo.onProviderChange - BGGeo.start - BGGeo.requestPermission</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onProviderChange { event -&gt;\n    Log.d(TAG, \"[onProviderChange] $event\")\n\n    if (!event.enabled) {\n        // Show alert using your own UI mechanism\n    }\n}\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.disableLocationAuthorizationAlert = true\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>var distanceFilter: Float\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"kotlin/GeolocationConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"kotlin/GeolocationConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>var elasticityMultiplier: Float\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"kotlin/GeolocationConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>var enableTimestampMeta: Boolean\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS LocationEvent.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"kotlin/GeolocationConfig/#android_1","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#ios_1","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>var fastestLocationUpdateInterval: Long\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    // Receive passive updates as fast as 5 seconds\n    geolocation.fastestLocationUpdateInterval = 5000\n\n    // Active updates occur according to distanceFilter or locationUpdateInterval\n}\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"kotlin/GeolocationConfig/#filter","title":"filter","text":"<pre><code>val filter\n</code></pre> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeolocationConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"kotlin/GeolocationConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilterConfig.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilterConfig.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilterConfig.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilterConfig.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilterConfig.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilterConfig.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilterConfig.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilterConfig.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilterConfig.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilterConfig.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilterConfig.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilterConfig.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Adjust\n    geolocation.filter.useKalman = true\n    geolocation.filter.kalmanProfile = KalmanProfile.Default\n    geolocation.filter.trackingAccuracyThreshold = 100.0\n    geolocation.filter.odometerAccuracyThreshold = 20.0\n}\n</code></pre> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.PassThrough\n    geolocation.filter.useKalman = false\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>var geofenceInitialTriggerEntry: Boolean\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"kotlin/GeolocationConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>var geofenceModeHighAccuracy: Boolean\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BGGeo.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeolocationConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeolocationConfig.locationUpdateInterval - GeolocationConfig.distanceFilter - GeolocationConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.geofenceModeHighAccuracy = true\n    geolocation.desiredAccuracy = DesiredAccuracy.MEDIUM\n    geolocation.locationUpdateInterval = 5000\n    geolocation.distanceFilter = 50f\n}\n\nbgGeo.startGeofences()\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>var geofenceProximityRadius: Long\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BGGeo.onGeofencesChange.</p>"},{"location":"kotlin/GeolocationConfig/#ios_2","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"kotlin/GeolocationConfig/#android_2","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"kotlin/GeolocationConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<p><code>var locationAuthorizationRequest:</code><code>LocationAuthorizationRequest</code></p> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"kotlin/GeolocationConfig/#ios_3","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\n// Start with When-In-Use\nbgGeo.ready {\n    geolocation.locationAuthorizationRequest = LocationAuthorizationRequest.WHEN_IN_USE\n}\n\n// Later \u2014 you may upgrade to Always at any time.\nsuspend fun onClickStartTracking() {\n    bgGeo.start()\n\n    bgGeo.config.edit {\n        geolocation.locationAuthorizationRequest = LocationAuthorizationRequest.ALWAYS\n    }\n}\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#android_3","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.locationAuthorizationRequest = LocationAuthorizationRequest.ALWAYS\n    app.backgroundPermissionRationale = mapOf(\n        \"title\" to \"Allow access to this device's location in the background?\",\n        \"message\" to \"To track your trips, please enable 'Allow all the time' location permission.\",\n        \"positiveAction\" to \"Change to Allow all the time\",\n        \"negativeAction\" to \"Cancel\"\n    )\n}\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"kotlin/GeolocationConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>var locationUpdateInterval: Long\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    geolocation.distanceFilter = 0f           // Required \u2014 otherwise this field is ignored.\n    geolocation.locationUpdateInterval = 5000  // Request a fix every ~5 seconds\n}\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"kotlin/GeolocationConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>var stationaryRadius: Int\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"kotlin/GeolocationConfig/#ios_4","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"kotlin/GeolocationConfig/#android_4","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"kotlin/GeolocationConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>var stopAfterElapsedMinutes: Long\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BGGeo.stop after this many minutes have elapsed since BGGeo.start was called.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.stopAfterElapsedMinutes = 30\n}\n\nbgGeo.start()  // &lt;-- plugin will automatically stop in 30 minutes\n</code></pre>"},{"location":"kotlin/GeolocationConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>var stopTimeout: Long\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BGGeo.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"kotlin/GeolocationConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>var useSignificantChangesOnly: Boolean\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"kotlin/GeolocationConfig/#ios_5","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"kotlin/GeolocationConfig/#android_5","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"kotlin/HeartbeatEvent/","title":"HeartbeatEvent","text":"<p><code>class</code><code>HeartbeatEvent</code></p> <p>Periodic heartbeat delivered to BGGeo.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onHeartbeat { event -&gt;\n    Log.d(TAG, \"[onHeartbeat] ${event.location}\")\n}\n</code></pre>"},{"location":"kotlin/HeartbeatEvent/#members","title":"Members","text":""},{"location":"kotlin/HeartbeatEvent/#location","title":"location","text":"<p><code>val location:</code><code>LocationEvent</code><code>?</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BGGeo.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"kotlin/HttpConfig/","title":"HttpConfig","text":"<p><code>class</code><code>HttpConfig</code></p> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"kotlin/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"kotlin/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://example.com/locations\"\n    http.autoSync = true\n    http.params = mapOf(\"user_id\" to 1234)\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BGGeo.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BGGeo.getCount or fetch records with BGGeo.getLocations.</p>"},{"location":"kotlin/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"kotlin/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"kotlin/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BGGeo.onConnectivityChange fires - BGGeo.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p>"},{"location":"kotlin/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BGGeo.start <code>\"stop\"</code> \u2014 BGGeo.stop <code>\"startGeofences\"</code> \u2014 BGGeo.startGeofences <code>\"changePace\"</code> <code>boolean</code> BGGeo.changePace <code>\"setConfig\"</code> <code>{Config}</code> BGGeo.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BGGeo.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BGGeo.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BGGeo.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"kotlin/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"kotlin/HttpConfig/#examples","title":"Examples","text":""},{"location":"kotlin/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.store.sync()\n</code></pre>"},{"location":"kotlin/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://api.example.com\"\n    http.autoSync = true\n    http.headers = mapOf(\"Authorization\" to \"Bearer ...\")\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://api.example.com/locations\"\n    http.method = HttpMethod.POST\n    http.autoSync = true\n    http.headers = mapOf(\"Authorization\" to \"Bearer secret\")\n    http.params = mapOf(\"device_id\" to \"abc-123\")\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://api.example.com/locations/bulk\"\n    http.autoSync = true\n    http.batchSync = true\n    http.maxBatchSize = 25\n    http.autoSyncThreshold = 10\n    http.rootProperty = \"locations\"\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://api.example.com/locations\"\n    http.autoSync = true\n    http.disableAutoSyncOnCellular = true\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.config.edit {\n    http.url = \"https://api.example.com\"\n    http.autoSync = false\n}\n\nbgGeo.store.sync()\n</code></pre>"},{"location":"kotlin/HttpConfig/#members","title":"Members","text":""},{"location":"kotlin/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>var autoSync: Boolean\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BGGeo.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BGGeo.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"kotlin/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>var autoSyncThreshold: Int\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BGGeo.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"kotlin/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>var batchSync: Boolean\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"kotlin/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>var disableAutoSyncOnCellular: Boolean\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BGGeo.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BGGeo.sync</p>"},{"location":"kotlin/HttpConfig/#headers","title":"headers","text":"<pre><code>var headers: Map&lt;String, Any&gt;\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://my.server.com\"\n    http.headers = mapOf(\n        \"Authorization\" to \"Bearer &lt;a secret key&gt;\",\n        \"X-FOO\" to \"BAR\"\n    )\n}\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"kotlin/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>var maxBatchSize: Int\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"kotlin/HttpConfig/#method","title":"method","text":"<p><code>var method:</code><code>HttpMethod</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://my-server.com/locations\"\n    http.method = HttpMethod.PUT\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#params","title":"params","text":"<pre><code>var params: Map&lt;String, Any&gt;\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://my-server.com/locations\"\n    http.params = mapOf(\n        \"user_id\" to 1234,\n        \"device_id\" to \"abc123\"\n    )\n}\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"kotlin/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>var rootProperty: String\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>val bgGeo = BGGeo.instance\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.rootProperty = \"myData\"\n    http.url = \"https://my.server.com\"\n}\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"kotlin/HttpConfig/#timeout","title":"timeout","text":"<pre><code>var timeout: Int\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BGGeo.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onHttp { event -&gt;\n    if (!event.isSuccess) {\n        Log.d(TAG, \"[onHttp] FAILURE: $event\")\n    }\n}\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://my-server.com/locations\"\n    http.timeout = 3000   // 3-second timeout\n}\n</code></pre>"},{"location":"kotlin/HttpConfig/#url","title":"url","text":"<pre><code>var url: String\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onHttp { event -&gt;\n    Log.d(TAG, \"[onHttp] $event\")\n}\n\n// Within a coroutine scope\nbgGeo.ready {\n    http.url = \"https://my-server.com/locations\"\n    http.params = mapOf(\"user_id\" to 1234)\n    http.headers = mapOf(\"Authorization\" to \"Basic my-secret-key\")\n    http.autoSync = true\n    http.method = HttpMethod.POST\n    app.stopOnTerminate = false\n}\n</code></pre>"},{"location":"kotlin/HttpEvent/","title":"HttpEvent","text":"<p><code>class</code><code>HttpEvent</code></p> <p>HTTP response payload delivered to BGGeo.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onHttp { event -&gt;\n    if (event.isSuccess) {\n        Log.d(TAG, \"[onHttp] Upload succeeded: ${event.statusCode}\")\n    } else {\n        Log.w(TAG, \"[onHttp] Upload failed: ${event.statusCode} ${event.responseText}\")\n    }\n}\n</code></pre>"},{"location":"kotlin/HttpEvent/#members","title":"Members","text":""},{"location":"kotlin/HttpEvent/#issuccess","title":"isSuccess","text":"<pre><code>val isSuccess: Boolean\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"kotlin/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>val responseText: String\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"kotlin/HttpEvent/#statuscode","title":"statusCode","text":"<pre><code>val statusCode: Int\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"kotlin/HttpMethod/","title":"HttpMethod","text":"<p><code>enum</code><code>HttpMethod</code><code>: Enum&lt;</code><code>HttpMethod</code><code>&gt;</code></p> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"kotlin/HttpMethod/#members","title":"Members","text":""},{"location":"kotlin/HttpMethod/#patch","title":"PATCH","text":"<p><code>val PATCH:</code><code>HttpMethod</code></p> <p>HTTP PATCH method.</p>"},{"location":"kotlin/HttpMethod/#post","title":"POST","text":"<p><code>val POST:</code><code>HttpMethod</code></p> <p>HTTP POST method.</p>"},{"location":"kotlin/HttpMethod/#put","title":"PUT","text":"<p><code>val PUT:</code><code>HttpMethod</code></p> <p>HTTP PUT method.</p>"},{"location":"kotlin/KalmanProfile/","title":"KalmanProfile","text":"<p><code>enum</code><code>KalmanProfile</code></p> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"kotlin/KalmanProfile/#members","title":"Members","text":""},{"location":"kotlin/KalmanProfile/#aggressive","title":"Aggressive","text":"<p><code>val Aggressive:</code><code>KalmanProfile</code></p> <p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"kotlin/KalmanProfile/#conservative","title":"Conservative","text":"<p><code>val Conservative:</code><code>KalmanProfile</code></p> <p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"kotlin/KalmanProfile/#default","title":"Default","text":"<p><code>val Default:</code><code>KalmanProfile</code></p> <p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"kotlin/LocationAuthorizationRequest/","title":"LocationAuthorizationRequest","text":"<p><code>enum</code><code>LocationAuthorizationRequest</code><code>: Enum&lt;</code><code>LocationAuthorizationRequest</code><code>&gt;</code></p> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeolocationConfig.locationAuthorizationRequest</p>"},{"location":"kotlin/LocationAuthorizationRequest/#members","title":"Members","text":""},{"location":"kotlin/LocationAuthorizationRequest/#always","title":"ALWAYS","text":"<p><code>val ALWAYS:</code><code>LocationAuthorizationRequest</code></p> <p>Request full background + foreground authorization.</p>"},{"location":"kotlin/LocationAuthorizationRequest/#any","title":"ANY","text":"<p><code>val ANY:</code><code>LocationAuthorizationRequest</code></p> <p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"kotlin/LocationAuthorizationRequest/#when_in_use","title":"WHEN_IN_USE","text":"<p><code>val WHEN_IN_USE:</code><code>LocationAuthorizationRequest</code></p> <p>Request foreground-only authorization.</p>"},{"location":"kotlin/LocationEvent/","title":"LocationEvent","text":"<p><code>class</code><code>LocationEvent</code></p> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"kotlin/LocationEvent/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"kotlin/LocationEvent/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"kotlin/LocationEvent/#javascript-schema","title":"JavaScript schema","text":"<pre><code>// LocationEvent properties:\n// location.timestamp        // String\n// location.isMoving         // Boolean\n// location.odometer         // Double (meters)\n// location.coords?.latitude  // Double\n// location.coords?.longitude // Double\n// location.coords?.accuracy  // Double\n// location.coords?.speed     // Double\n// location.coords?.heading   // Double\n// location.coords?.altitude  // Double\n// location.activity?.type    // String (still|on_foot|walking|running|in_vehicle|on_bicycle)\n// location.activity?.confidence // Int (0-100)\n// location.battery?.level    // Double\n// location.battery?.isCharging // Boolean\n// location.extras            // JSONObject?\n</code></pre>"},{"location":"kotlin/LocationEvent/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>// HTTP POST schema \u2014 the JSON body posted to your server:\n// {\n//   \"location\": {\n//     \"coords\": {\n//       \"latitude\":   Double,\n//       \"longitude\":  Double,\n//       \"accuracy\":   Double,\n//       \"speed\":      Double,\n//       \"heading\":    Double,\n//       \"altitude\":   Double\n//     },\n//     \"extras\": { ... },\n//     \"activity\": { \"type\": String, \"confidence\": Int },\n//     \"geofence\": { \"identifier\": String, \"action\": String },\n//     \"battery\":  { \"level\": Double, \"is_charging\": Boolean },\n//     \"timestamp\": String,   // ISO-8601 UTC\n//     \"uuid\":      String,\n//     \"event\":     String,   // motionchange|geofence|heartbeat\n//     \"is_moving\": Boolean,\n//     \"odometer\":  Double    // meters\n//   }\n// }\n</code></pre>"},{"location":"kotlin/LocationEvent/#members","title":"Members","text":""},{"location":"kotlin/LocationEvent/#accuracy","title":"accuracy","text":"<pre><code>val accuracy: Float?\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"kotlin/LocationEvent/#activity","title":"activity","text":"<p><code>val activity:</code><code>LocationEvent</code><code>.Activity?</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"kotlin/LocationEvent/#age","title":"age","text":"<pre><code>val age: Double?\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"kotlin/LocationEvent/#altitude","title":"altitude","text":"<pre><code>val altitude: Double?\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"kotlin/LocationEvent/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"kotlin/LocationEvent/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"kotlin/LocationEvent/#altitudeaccuracy","title":"altitudeAccuracy","text":"<pre><code>val altitudeAccuracy: Float?\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"kotlin/LocationEvent/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"kotlin/LocationEvent/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"kotlin/LocationEvent/#battery","title":"battery","text":"<p><code>val battery:</code><code>LocationEvent</code><code>.</code><code>Battery</code><code>?</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"kotlin/LocationEvent/#confidence","title":"confidence","text":"<pre><code>val confidence: Int?\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"kotlin/LocationEvent/#coords","title":"coords","text":"<p><code>val coords:</code><code>LocationEvent</code><code>.</code><code>Coords</code><code>?</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"kotlin/LocationEvent/#event_","title":"event_","text":"<pre><code>val event_: String?\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"kotlin/LocationEvent/#extras","title":"extras","text":"<pre><code>val extras: Map&lt;String, Any&gt;?\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"kotlin/LocationEvent/#geofence","title":"geofence","text":"<p><code>val geofence:</code><code>LocationEvent</code><code>.GeofenceTrigger?</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"kotlin/LocationEvent/#heading","title":"heading","text":"<pre><code>val heading: Float?\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"kotlin/LocationEvent/#headingaccuracy","title":"headingAccuracy","text":"<pre><code>val headingAccuracy: Float?\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"kotlin/LocationEvent/#ischarging","title":"isCharging","text":"<pre><code>val isCharging: Boolean?\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"kotlin/LocationEvent/#ismock","title":"isMock","text":"<pre><code>val isMock: Boolean\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"kotlin/LocationEvent/#ismoving","title":"isMoving","text":"<pre><code>val isMoving: Boolean\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"kotlin/LocationEvent/#issample","title":"isSample","text":"<pre><code>val isSample: Boolean\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BGGeo.getCurrentPosition to find the most accurate fix. These samples are delivered to BGGeo.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"kotlin/LocationEvent/#latitude","title":"latitude","text":"<pre><code>val latitude: Double\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"kotlin/LocationEvent/#level","title":"level","text":"<pre><code>val level: Float?\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"kotlin/LocationEvent/#longitude","title":"longitude","text":"<pre><code>val longitude: Double\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"kotlin/LocationEvent/#odometer","title":"odometer","text":"<pre><code>val odometer: Double\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"kotlin/LocationEvent/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilterConfig evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilterConfig.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via LocationEvent.odometer_error.</li> </ul>"},{"location":"kotlin/LocationEvent/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"kotlin/LocationEvent/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilterConfig - LocationFilterConfig.odometerAccuracyThreshold - BGGeo.resetOdometer - BGGeo.getOdometer</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onLocation { location -&gt;\n    Log.d(TAG, \"Distance traveled: ${location.odometer}\")\n}\n\n// Reset at start of a trip\n// Within a coroutine scope\nbgGeo.setOdometer(0.0)\n</code></pre>"},{"location":"kotlin/LocationEvent/#odometererror","title":"odometerError","text":"<pre><code>val odometerError: Double\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"kotlin/LocationEvent/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onLocation { location -&gt;\n    Log.d(TAG, \"Odometer: ${location.odometer}\")\n    // Note: odometer_error is not exposed in the Kotlin LocationEvent API.\n    // Use location.toJson() to inspect the full JSON payload if needed.\n}\n</code></pre>"},{"location":"kotlin/LocationEvent/#recordedat","title":"recordedAt","text":"<pre><code>val recordedAt: String\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"kotlin/LocationEvent/#speed","title":"speed","text":"<pre><code>val speed: Float?\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"kotlin/LocationEvent/#speedaccuracy","title":"speedAccuracy","text":"<pre><code>val speedAccuracy: Float?\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"kotlin/LocationEvent/#timestamp","title":"timestamp","text":"<pre><code>val timestamp: String\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"kotlin/LocationEvent/#type","title":"type","text":"<pre><code>val type: String?\n</code></pre> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"kotlin/LocationEvent/#uuid","title":"uuid","text":"<pre><code>val uuid: String?\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"kotlin/LocationFilterConfig/","title":"LocationFilterConfig","text":"<p><code>class</code><code>LocationFilterConfig</code></p> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeolocationConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"kotlin/LocationFilterConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"kotlin/LocationFilterConfig/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Adjust\n    geolocation.filter.useKalman = true\n    geolocation.filter.kalmanProfile = KalmanProfile.Default\n    geolocation.filter.trackingAccuracyThreshold = 100.0\n    geolocation.filter.odometerAccuracyThreshold = 20.0\n}\n</code></pre>"},{"location":"kotlin/LocationFilterConfig/#examples","title":"Examples","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.PassThrough\n    geolocation.filter.useKalman = false\n}\n</code></pre>"},{"location":"kotlin/LocationFilterConfig/#members","title":"Members","text":""},{"location":"kotlin/LocationFilterConfig/#burstwindow","title":"burstWindow","text":"<pre><code>var burstWindow: Double\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"kotlin/LocationFilterConfig/#filterdebug","title":"filterDebug","text":"<pre><code>var filterDebug: Boolean\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"kotlin/LocationFilterConfig/#kalmandebug","title":"kalmanDebug","text":"<pre><code>var kalmanDebug: Boolean\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"kotlin/LocationFilterConfig/#kalmanprofile","title":"kalmanProfile","text":"<p><code>var kalmanProfile:</code><code>KalmanProfile</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"kotlin/LocationFilterConfig/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>var maxBurstDistance: Double\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"kotlin/LocationFilterConfig/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>var maxImpliedSpeed: Double\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"kotlin/LocationFilterConfig/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>var odometerAccuracyThreshold: Double\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"kotlin/LocationFilterConfig/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>var odometerUseKalmanFilter: Boolean\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilterConfig.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilterConfig.useKalman and LocationFilterConfig.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"kotlin/LocationFilterConfig/#policy","title":"policy","text":"<p><code>var policy:</code><code>LocationFilterPolicy</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BGGeo.onLocation. For more granular tuning, see LocationFilterConfig.trackingAccuracyThreshold, LocationFilterConfig.maxImpliedSpeed, and other LocationFilterConfig fields.</p> <p>See also - GeolocationConfig.filter - LocationFilterConfig - KalmanProfile</p>"},{"location":"kotlin/LocationFilterConfig/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Adjust\n}\n</code></pre>"},{"location":"kotlin/LocationFilterConfig/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.PassThrough\n}\n</code></pre>"},{"location":"kotlin/LocationFilterConfig/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Conservative\n}\n</code></pre>"},{"location":"kotlin/LocationFilterConfig/#rollingwindow","title":"rollingWindow","text":"<pre><code>var rollingWindow: Int\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"kotlin/LocationFilterConfig/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>var trackingAccuracyThreshold: Double\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"kotlin/LocationFilterConfig/#usekalman","title":"useKalman","text":"<pre><code>var useKalman: Boolean\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"kotlin/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<p><code>enum</code><code>LocationFilterPolicy</code></p> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilterConfig.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BGGeo.onLocation.</li> <li>For more granular tuning, see LocationFilterConfig.trackingAccuracyThreshold,   LocationFilterConfig.maxImpliedSpeed, and other LocationFilterConfig fields.</li> </ul> <p>Examples</p> <p>See also - GeolocationConfig.filter - LocationFilterConfig - KalmanProfile</p>"},{"location":"kotlin/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Adjust\n}\n</code></pre>"},{"location":"kotlin/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.PassThrough\n}\n</code></pre>"},{"location":"kotlin/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    geolocation.filter.policy = LocationFilterPolicy.Conservative\n}\n</code></pre>"},{"location":"kotlin/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"kotlin/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p><code>val Adjust:</code><code>LocationFilterPolicy</code></p> <p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilterConfig.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"kotlin/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p><code>val Conservative:</code><code>LocationFilterPolicy</code></p> <p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"kotlin/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p><code>val PassThrough:</code><code>LocationFilterPolicy</code></p> <p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"kotlin/LocationsOrderDirection/","title":"LocationsOrderDirection","text":"<p><code>enum</code><code>LocationsOrderDirection</code><code>: Enum&lt;</code><code>LocationsOrderDirection</code><code>&gt;</code></p> <p>Sort order for <code>SQLQuery.order</code>.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"kotlin/LocationsOrderDirection/#members","title":"Members","text":""},{"location":"kotlin/LocationsOrderDirection/#asc","title":"ASC","text":"<p><code>val ASC:</code><code>LocationsOrderDirection</code></p> <p>Ascending order (oldest to newest).</p>"},{"location":"kotlin/LocationsOrderDirection/#desc","title":"DESC","text":"<p><code>val DESC:</code><code>LocationsOrderDirection</code></p> <p>Descending order (newest to oldest).</p>"},{"location":"kotlin/LogLevel/","title":"LogLevel","text":"<p><code>enum</code><code>LogLevel</code><code>: Enum&lt;</code><code>LogLevel</code><code>&gt;</code></p> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"kotlin/LogLevel/#members","title":"Members","text":""},{"location":"kotlin/LogLevel/#debug","title":"DEBUG","text":"<p><code>val DEBUG:</code><code>LogLevel</code></p> <p>Developer-level debug output.</p>"},{"location":"kotlin/LogLevel/#error","title":"ERROR","text":"<p><code>val ERROR:</code><code>LogLevel</code></p> <p>Log only critical failures.</p>"},{"location":"kotlin/LogLevel/#info","title":"INFO","text":"<p><code>val INFO:</code><code>LogLevel</code></p> <p>Operational information.</p>"},{"location":"kotlin/LogLevel/#off","title":"OFF","text":"<p><code>val OFF:</code><code>LogLevel</code></p> <p>Disable all logging.</p>"},{"location":"kotlin/LogLevel/#verbose","title":"VERBOSE","text":"<p><code>val VERBOSE:</code><code>LogLevel</code></p> <p>Maximum detail.</p>"},{"location":"kotlin/LogLevel/#warning","title":"WARNING","text":"<p><code>val WARNING:</code><code>LogLevel</code></p> <p>Log warnings + errors.</p>"},{"location":"kotlin/Logger/","title":"Logger","text":"<p><code>class</code><code>Logger</code></p> <p>SDK logging API \u2014 access via BGGeo.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"kotlin/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"kotlin/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>val bgGeo = BGGeo.instance\n// Within a coroutine scope\nval log = bgGeo.logger.getLog()\n</code></pre>"},{"location":"kotlin/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional <code>SQLQuery</code> to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.onLocation { location -&gt;\n    bgGeo.logger.log(\"debug\", \"Location received in Kotlin: ${location.extras}\")\n}\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"kotlin/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onLocation { location -&gt;\n    bgGeo.logger.log(\"debug\", \"Location received: ${location.uuid}\")\n}\n</code></pre>"},{"location":"kotlin/Logger/#examples","title":"Examples","text":""},{"location":"kotlin/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval log = bgGeo.logger.getLog()\nLog.d(TAG, \"[log] $log\")\n</code></pre>"},{"location":"kotlin/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>// Not available in native Kotlin API\n</code></pre>"},{"location":"kotlin/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nval activity = android.app.Activity()\n\n// Requires an Activity reference\nbgGeo.logger.emailLog(\"support@example.com\", activity)\n</code></pre>"},{"location":"kotlin/Logger/#methods","title":"Methods","text":""},{"location":"kotlin/Logger/#debug","title":"debug","text":"<pre><code>fun debug(message: String)\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.logger.log(\"debug\", \"This is a debug message\")\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"kotlin/Logger/#destroylog","title":"destroyLog","text":"<pre><code>suspend fun destroyLog()\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.logger.destroyLog()\n</code></pre>"},{"location":"kotlin/Logger/#emaillog","title":"emailLog","text":"<pre><code>suspend fun emailLog( email: String, activity: Activity, start: Long = 0, end: Long = 0, order: Int = ORDER_ASC, limit: Int = 0 )\n</code></pre> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional <code>SQLQuery</code> to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>val bgGeo = BGGeo.instance\n// Within a coroutine scope\n// Requires an Activity reference\n// bgGeo.logger.emailLog(\"foo@bar.com\", activity)\n</code></pre>"},{"location":"kotlin/Logger/#error","title":"error","text":"<pre><code>fun error(message: String)\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.logger.log(\"error\", \"Something BAD\")\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"kotlin/Logger/#getlog","title":"getLog","text":"<pre><code>suspend fun getLog( start: Long = 0, end: Long = 0, order: Int = ORDER_ASC, limit: Int = 0 )\n</code></pre> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional <code>SQLQuery</code> to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>val bgGeo = BGGeo.instance\n// Within a coroutine scope\nval log = bgGeo.logger.getLog()\n// Warning: this string could be several megabytes.\nLog.d(TAG, \"[log] success: $log\")\n</code></pre>"},{"location":"kotlin/Logger/#info","title":"info","text":"<pre><code>fun info(message: String)\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.logger.log(\"info\", \"Something informative\")\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"kotlin/Logger/#loglevel","title":"logLevel","text":"<p><code>var logLevel:</code><code>LogLevel</code></p> <p>Sets the LoggerConfig.logLevel.</p>"},{"location":"kotlin/Logger/#notice","title":"notice","text":"<pre><code>fun notice(message: String)\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>val bgGeo = BGGeo.instance\n// No \"notice\" level in Kotlin \u2014 use \"info\" instead\nbgGeo.logger.log(\"info\", \"A Notice\")\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"kotlin/Logger/#playsound","title":"playSound","text":"<pre><code>fun playSound(soundId: Int)\n</code></pre> <p>Play a system sound.</p>"},{"location":"kotlin/Logger/#ios","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>"},{"location":"kotlin/Logger/#android","title":"Android","text":"<p>Provide a sound name string.</p>"},{"location":"kotlin/Logger/#uploadlog","title":"uploadLog","text":"<pre><code>suspend fun uploadLog( url: String, start: Long = 0, end: Long = 0, order: Int = ORDER_ASC, limit: Int = 0 )\n</code></pre> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional <code>SQLQuery</code> to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"kotlin/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Not available in native Kotlin API\n</code></pre>"},{"location":"kotlin/Logger/#warn","title":"warn","text":"<pre><code>fun warn(message: String)\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.logger.log(\"warn\", \"Something WEIRD\")\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"kotlin/Logger/#properties","title":"Properties","text":""},{"location":"kotlin/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>const val ORDER_ASC = 1\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"kotlin/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>const val ORDER_DESC = -1\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"kotlin/LoggerConfig/","title":"LoggerConfig","text":"<p><code>class</code><code>LoggerConfig</code></p> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"kotlin/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"kotlin/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 7\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"kotlin/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"kotlin/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"kotlin/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"kotlin/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"kotlin/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"kotlin/LoggerConfig/#examples","title":"Examples","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\n// Current compound config\nbgGeo.ready {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 7\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    logger.debug = false\n    logger.logLevel = LogLevel.INFO\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>val bgGeo = BGGeo.instance\nbgGeo.config.edit {\n    logger.logLevel = LogLevel.OFF\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 7\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    logger.debug = false\n    logger.logLevel = LogLevel.INFO\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>val bgGeo = BGGeo.instance\nbgGeo.config.edit {\n    logger.debug = true\n    logger.logLevel = LogLevel.VERBOSE\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#members","title":"Members","text":""},{"location":"kotlin/LoggerConfig/#debug","title":"debug","text":"<pre><code>var debug: Boolean\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"kotlin/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"kotlin/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"kotlin/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>var logLevel:</code><code>LogLevel</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    logger.logLevel = LogLevel.VERBOSE\n}\n</code></pre>"},{"location":"kotlin/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>var logMaxDays: Int\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/","title":"NotificationConfig Android only","text":"<p><code>class</code><code>NotificationConfig</code></p> <p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"kotlin/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"kotlin/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Notification config is managed via the Android notification builder\n    // or through the config.app namespace.\n}\n\n// Update notification fields at runtime.\nbgGeo.config.edit {\n    // Notification title/text are configured via Android's\n    // foreground service notification builder pattern.\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"kotlin/NotificationConfig/#members","title":"Members","text":""},{"location":"kotlin/NotificationConfig/#actions","title":"actions","text":"<pre><code>val actions: List&lt;String&gt;\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"kotlin/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre>"},{"location":"kotlin/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Register button actions via Android notification layout configuration.\n    // Custom notification actions are configured through the Android\n    // notification builder pattern with custom layout XML.\n}\n\n// Note: onNotificationAction is not exposed in the Kotlin API.\n// Use Android's notification action handling directly in your\n// headless job service or activity.\n</code></pre>"},{"location":"kotlin/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>val channelId: String\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"kotlin/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>val channelName: String\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // channelName is configured through the Android notification\n    // channel setup in your Application class.\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#color","title":"color","text":"<pre><code>val color: String\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"kotlin/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>val largeIcon: String\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Large icon is configured through the Android notification builder.\n    // Use NotificationCompat.Builder.setLargeIcon() in your notification setup.\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#layout","title":"layout","text":"<pre><code>val layout: String\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"kotlin/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Custom notification layout is configured through Android's\n    // NotificationCompat.Builder with RemoteViews pointing to\n    // your custom layout XML resource (e.g., R.layout.my_notification_layout).\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p>"},{"location":"kotlin/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Custom notification strings are set via Android's RemoteViews API:\n    // remoteViews.setTextViewText(R.id.myCustomElement, \"My Custom Element Text\")\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Notification button actions are configured via Android's\n    // notification builder with PendingIntent actions.\n}\n\n// Note: onNotificationAction is not exposed in the Kotlin API.\n// Handle notification button clicks via Android's BroadcastReceiver\n// or your headless job service.\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Custom notification layout, title, text, actions, and strings\n    // are configured through Android's NotificationCompat.Builder\n    // with RemoteViews and PendingIntent actions.\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#priority","title":"priority","text":"<p><code>val priority:</code><code>NotificationPriority</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    app.notification.priority = NotificationPriority.HIGH\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>val smallIcon: String\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Small icon is configured through the Android notification builder:\n    // NotificationCompat.Builder.setSmallIcon(R.drawable.my_custom_notification_small_icon)\n    // or\n    // NotificationCompat.Builder.setSmallIcon(R.mipmap.my_custom_notification_small_icon)\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>val sticky: Boolean\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"kotlin/NotificationConfig/#strings","title":"strings","text":"<pre><code>val strings: Map&lt;String, String&gt;\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.ready {\n    // Custom notification strings are set via Android's RemoteViews API:\n    // remoteViews.setTextViewText(R.id.myCustomElement, \"My Custom Element Text\")\n}\n</code></pre>"},{"location":"kotlin/NotificationConfig/#text","title":"text","text":"<pre><code>val text: String\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"kotlin/NotificationConfig/#title","title":"title","text":"<pre><code>val title: String\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"kotlin/NotificationPriority/","title":"NotificationPriority","text":"<p><code>enum</code><code>NotificationPriority</code><code>: Enum&lt;</code><code>NotificationPriority</code><code>&gt;</code></p> <p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"kotlin/NotificationPriority/#members","title":"Members","text":""},{"location":"kotlin/NotificationPriority/#default","title":"DEFAULT","text":"<p><code>val DEFAULT:</code><code>NotificationPriority</code></p> <p>Default notification priority (normal weighting).</p>"},{"location":"kotlin/NotificationPriority/#high","title":"HIGH","text":"<p><code>val HIGH:</code><code>NotificationPriority</code></p> <p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"kotlin/NotificationPriority/#low","title":"LOW","text":"<p><code>val LOW:</code><code>NotificationPriority</code></p> <p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"kotlin/NotificationPriority/#max","title":"MAX","text":"<p><code>val MAX:</code><code>NotificationPriority</code></p> <p>Same as NotificationPriority.High.</p>"},{"location":"kotlin/NotificationPriority/#min","title":"MIN","text":"<p><code>val MIN:</code><code>NotificationPriority</code></p> <p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"kotlin/PermissionStatus/","title":"PermissionStatus","text":"<p><code>enum</code><code>PermissionStatus</code><code>: Enum&lt;</code><code>PermissionStatus</code><code>&gt;</code></p> <p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"kotlin/PermissionStatus/#members","title":"Members","text":""},{"location":"kotlin/PermissionStatus/#always","title":"ALWAYS","text":"<p><code>val ALWAYS:</code><code>PermissionStatus</code></p> <p>Location permissions authorized for always use.</p>"},{"location":"kotlin/PermissionStatus/#denied","title":"DENIED","text":"<p><code>val DENIED:</code><code>PermissionStatus</code></p> <p>Location permissions denied by the user.</p>"},{"location":"kotlin/PermissionStatus/#when_in_use","title":"WHEN_IN_USE","text":"<p><code>val WHEN_IN_USE:</code><code>PermissionStatus</code></p> <p>Location permissions authorized for when-in-use (Android 10+).</p>"},{"location":"kotlin/PersistMode/","title":"PersistMode","text":"<p><code>enum</code><code>PersistMode</code><code>: Enum&lt;</code><code>PersistMode</code><code>&gt;</code></p> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"kotlin/PersistMode/#members","title":"Members","text":""},{"location":"kotlin/PersistMode/#all","title":"ALL","text":"<p><code>val ALL:</code><code>PersistMode</code></p> <p>Persist both location and geofence data.</p>"},{"location":"kotlin/PersistMode/#geofence","title":"GEOFENCE","text":"<p><code>val GEOFENCE:</code><code>PersistMode</code></p> <p>Persist geofence data only.</p>"},{"location":"kotlin/PersistMode/#location","title":"LOCATION","text":"<p><code>val LOCATION:</code><code>PersistMode</code></p> <p>Persist location data only.</p>"},{"location":"kotlin/PersistMode/#none","title":"NONE","text":"<p><code>val NONE:</code><code>PersistMode</code></p> <p>Do not persist any data.</p>"},{"location":"kotlin/PersistenceConfig/","title":"PersistenceConfig","text":"<p><code>class</code><code>PersistenceConfig</code></p> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"kotlin/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"kotlin/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded LocationEvent and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.maxDaysToPersist = 3\n    persistence.maxRecordsToPersist = 1000\n    persistence.extras = mapOf(\n        \"user_id\" to 123,\n        \"appVersion\" to \"1.2.3\"\n    )\n    persistence.persistMode = PersistMode.ALL\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BGGeo.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BGGeo.getCount or BGGeo.getLocations.</p>"},{"location":"kotlin/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BGGeo.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"kotlin/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"kotlin/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BGGeo.onLocation, BGGeo.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nval location = bgGeo.getCurrentPosition(\n    persist = true,\n    extras = mapOf(\"get_current_position\" to true),\n    samples = 3\n)\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#examples","title":"Examples","text":""},{"location":"kotlin/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>val bgGeo = BGGeo.instance\nbgGeo.config.edit {\n    persistence.maxDaysToPersist = 14\n    persistence.maxRecordsToPersist = 5000\n    persistence.locationsOrderDirection = LocationsOrderDirection.ASC\n    persistence.locationTemplate = \"{...}\"\n    persistence.geofenceTemplate = \"{...}\"\n    persistence.persistMode = PersistMode.ALL\n    persistence.extras = mapOf(\"user_id\" to 123)\n    persistence.disableProviderChangeRecord = true\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.maxDaysToPersist = 14\n    persistence.maxRecordsToPersist = 5000\n    persistence.locationsOrderDirection = LocationsOrderDirection.ASC\n    persistence.persistMode = PersistMode.ALL\n    persistence.extras = mapOf(\"user_id\" to 123, \"appVersion\" to \"1.2.3\")\n    http.url = \"https://example.com/locations\"\n    http.autoSync = true\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>val bgGeo = BGGeo.instance\nval pending = bgGeo.store.count\nLog.d(TAG, \"Pending records: $pending\")\n\n// Purge all records\nbgGeo.store.destroyAll()\nLog.d(TAG, \"Destroyed all records\")\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>val bgGeo = BGGeo.instance\nbgGeo.config.edit {\n    persistence.locationTemplate = \"\"\"\n    {\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }\n    \"\"\".trimIndent()\n    persistence.geofenceTemplate = \"\"\"\n    {\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }\n    \"\"\".trimIndent()\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#members","title":"Members","text":""},{"location":"kotlin/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>var disableProviderChangeRecord: Boolean\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BGGeo.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.disableProviderChangeRecord = true\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#extras","title":"extras","text":"<pre><code>var extras: Map&lt;String, Any&gt;\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    http.url = \"https://my-server.com/locations\"\n    http.params = mapOf(\"device_id\" to \"abc123\")\n    persistence.extras = mapOf(\"route_id\" to 1234)\n}\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"kotlin/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>var geofenceTemplate: String\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.geofenceTemplate =\n        \"\"\"{\"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\"}\"\"\"\n}\n\n// Or a compact Array form:\nbgGeo.config.edit {\n    persistence.geofenceTemplate =\n        \"\"\"[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]\"\"\"\n}\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeolocationConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"kotlin/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.geofenceTemplate = \"\"\"{\"timestamp\": &lt;%= timestamp %&gt;}\"\"\"\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.geofenceTemplate = \"\"\"{\"timestamp\": \"&lt;%= timestamp %&gt;\"}\"\"\"\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>var locationTemplate: String\n</code></pre> <p>Optional custom ERB template for reshaping LocationEvent JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.locationTemplate =\n        \"\"\"{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}\"\"\"\n}\n\n// Or use a compact Array template:\nbgGeo.config.edit {\n    persistence.locationTemplate =\n        \"\"\"[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]\"\"\"\n}\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.locationTemplate = \"\"\"{\"timestamp\": &lt;%= timestamp %&gt;}\"\"\"\n}\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.locationTemplate = \"\"\"{\"timestamp\": \"&lt;%= timestamp %&gt;\"}\"\"\"\n}\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    http.url = \"https://my.server.com/locations\"\n    http.rootProperty = \"data\"\n    persistence.locationTemplate = \"\"\"{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}\"\"\"\n    persistence.extras = mapOf(\"foo\" to \"bar\")\n}\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"kotlin/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeolocationConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"kotlin/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<p><code>var locationsOrderDirection:</code><code>LocationsOrderDirection</code></p> <p>Sort order used when selecting records for upload or returning them from BGGeo.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"kotlin/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>var maxDaysToPersist: Int\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"kotlin/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>var maxRecordsToPersist: Int\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"kotlin/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>var persistMode:</code><code>PersistMode</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BGGeo.onLocation and BGGeo.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BGGeo.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"kotlin/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>var timestampFormat: String\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BGGeo.onLocation, BGGeo.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nbgGeo.ready {\n    persistence.timestampFormat = \"epoch\"\n}\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"kotlin/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<p><code>class</code><code>ProviderChangeEvent</code></p> <p>Location-services provider state delivered to BGGeo.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>val bgGeo = BGGeo.instance\n\nbgGeo.onProviderChange { event -&gt;\n    Log.d(TAG, \"[onProviderChange] enabled: ${event.enabled} status: ${event.status} gps: ${event.gps} network: ${event.network} accuracyAuthorization: ${event.accuracyAuthorization}\")\n\n    if (!event.enabled) {\n        // Prompt the user to re-enable location services.\n    }\n}\n</code></pre>"},{"location":"kotlin/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"kotlin/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<p><code>val accuracy</code><code>Authorization</code><code>: Int</code></p> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"kotlin/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>val enabled: Boolean\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"kotlin/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>val gps: Boolean\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"kotlin/ProviderChangeEvent/#network","title":"network","text":"<pre><code>val network: Boolean\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"kotlin/ProviderChangeEvent/#status","title":"status","text":"<pre><code>val status: Int\n</code></pre> <p>Platform authorization status code. Use the PermissionStatus constants to interpret this value.</p>"},{"location":"kotlin/Sensors/","title":"Sensors","text":"<p><code>class</code><code>Sensors</code></p> <p>Availability of motion sensors on the device, as returned by BGGeo.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nval sensors = bgGeo.sensors\nLog.d(TAG, \"[Sensors] $sensors\")\n</code></pre>"},{"location":"kotlin/Sensors/#members","title":"Members","text":""},{"location":"kotlin/Sensors/#hasaccelerometer","title":"hasAccelerometer","text":"<pre><code>val hasAccelerometer: Boolean\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"kotlin/Sensors/#hasgyroscope","title":"hasGyroscope","text":"<pre><code>val hasGyroscope: Boolean\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"kotlin/Sensors/#hasmagnetometer","title":"hasMagnetometer","text":"<pre><code>val hasMagnetometer: Boolean\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"kotlin/Sensors/#hassignificantmotion","title":"hasSignificantMotion","text":"<pre><code>val hasSignificantMotion: Boolean\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"kotlin/State/","title":"State","text":"<p><code>data class</code><code>State</code></p> <p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>val bgGeo = BGGeo.instance\nval state = bgGeo.state\nLog.d(TAG, \"[state] enabled=${state.enabled} trackingMode=${state.trackingMode}\")\n</code></pre>"},{"location":"kotlin/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"kotlin/State/#members","title":"Members","text":""},{"location":"kotlin/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>val didDeviceReboot: Boolean\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"kotlin/State/#enabled","title":"enabled","text":"<pre><code>val enabled: Boolean\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BGGeo.start or BGGeo.startGeofences has been called and not yet stopped.</p>"},{"location":"kotlin/State/#isfirstboot","title":"isFirstBoot","text":"<pre><code>val isFirstBoot: Boolean\n</code></pre> <p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"kotlin/State/#ismoving","title":"isMoving","text":"<pre><code>val isMoving: Boolean\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// If the SDK is currently in the *stationary* state:\nval bgGeo = BGGeo.instance\n\nbgGeo.onMotionChange { location -&gt;\n    Log.d(TAG, \"[onMotionChange] isMoving? ${location.isMoving}\")\n}\n\nbgGeo.changePace(true)\n// bgGeo.enabled and motion state is now true.\n</code></pre>"},{"location":"kotlin/State/#odometer","title":"odometer","text":"<pre><code>val odometer: Double\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BGGeo.setOdometer - BGGeo.getOdometer</p>"},{"location":"kotlin/State/#odometererror","title":"odometerError","text":"<pre><code>val odometerError: Double\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilterConfig.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"kotlin/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>val schedulerEnabled: Boolean\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BGGeo.startSchedule has been called. BGGeo.stopSchedule sets this to <code>false</code>.</p>"},{"location":"kotlin/State/#trackingmode","title":"trackingMode","text":"<p><code>val trackingMode:</code><code>TrackingMode</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\n\nbgGeo.start()\n// Config.TRACKING_MODE_LOCATION == 1\n\nbgGeo.startGeofences()\n// Config.TRACKING_MODE_GEOFENCE == 0\n</code></pre>"},{"location":"kotlin/TrackingMode/","title":"TrackingMode","text":"<p><code>enum</code><code>TrackingMode</code><code>: Enum&lt;</code><code>TrackingMode</code><code>&gt;</code></p> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"kotlin/TrackingMode/#members","title":"Members","text":""},{"location":"kotlin/TrackingMode/#geofence","title":"GEOFENCE","text":"<p><code>val GEOFENCE:</code><code>TrackingMode</code></p> <p>Monitor geofences only.</p>"},{"location":"kotlin/TrackingMode/#location","title":"LOCATION","text":"<p><code>val LOCATION:</code><code>TrackingMode</code></p> <p>Monitor location + geofences.</p>"},{"location":"kotlin/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<p><code>object</code><code>TransistorAuthorizationService</code></p> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"kotlin/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>// Not available in native Kotlin API\n</code></pre>"},{"location":"kotlin/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"kotlin/TransistorAuthorizationService/#destroytoken","title":"destroyToken","text":"<pre><code>suspend fun destroyToken(context: Context, url: String)\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"kotlin/TransistorAuthorizationService/#findorcreatetoken","title":"findOrCreateToken","text":"<p><code>suspend fun findOrCreateToken(context: Context, org: String, username: String, url: String =</code><code>TransistorToken</code><code>.DEFAULT_URL):</code><code>TransistorToken</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>// Within a coroutine scope\nval bgGeo = BGGeo.instance\nval token = TransistorAuthorizationService.findOrCreateToken(\n    context = applicationContext,\n    org = \"my-company-name\",\n    username = \"my-username\",\n    url = \"http://tracker.transistorsoft.com\"\n)\nbgGeo.ready(transistorAuthorizationToken = token) {}\n</code></pre>"},{"location":"kotlin/TransistorToken/","title":"TransistorToken","text":"<p><code>data class</code><code>TransistorToken</code></p> <p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"kotlin/TransistorToken/#members","title":"Members","text":""},{"location":"kotlin/TransistorToken/#accesstoken","title":"accessToken","text":"<pre><code>val accessToken: String\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"kotlin/TransistorToken/#expires","title":"expires","text":"<pre><code>val expires: Long\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"kotlin/TransistorToken/#refreshtoken","title":"refreshToken","text":"<pre><code>val refreshToken: String\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"kotlin/TransistorToken/#url","title":"url","text":"<pre><code>val url: String\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"kotlin/examples/","title":"Examples","text":""},{"location":"kotlin/examples/#basic-tracking","title":"Basic tracking","text":"<pre><code>import com.transistorsoft.locationmanager.kotlin.BGGeo\nimport com.transistorsoft.locationmanager.kotlin.config.DesiredAccuracy\nimport com.transistorsoft.locationmanager.kotlin.config.LogLevel\n\n// Application.onCreate() \u2014 initialize once\nclass MyApp : Application() {\n    override fun onCreate() {\n        super.onCreate()\n        BGGeo.init(this)\n    }\n}\n\n// Activity / Fragment \u2014 call ready() in a coroutine scope\nclass MainActivity : AppCompatActivity() {\n    private val subscriptions = mutableSetOf&lt;EventSubscription&gt;()\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        val bgGeo = BGGeo.instance\n        bgGeo.setActivity(this)\n\n        // Register event listeners *before* calling ready()\n        bgGeo.onLocation { event -&gt;\n            Log.d(TAG, \"[onLocation] ${event.coords}\")\n        }.storeIn(subscriptions)\n\n        lifecycleScope.launch {\n            // ready() configures the SDK and restores persisted state.\n            // It does NOT start tracking \u2014 call start()/stop() separately.\n            bgGeo.ready {\n                geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n                geolocation.distanceFilter = 10f\n                app.stopOnTerminate = false\n                app.startOnBoot = true\n                logger.debug = true\n                logger.logLevel = LogLevel.VERBOSE\n            }\n        }\n    }\n\n    // Elsewhere \u2014 e.g. a toggle-button click listener in your UI\n    private fun onToggleTracking() {\n        lifecycleScope.launch {\n            val bgGeo = BGGeo.instance\n            val state = bgGeo.state\n            if (state.enabled) {\n                bgGeo.stop()\n            } else {\n                bgGeo.start()\n            }\n            Log.d(TAG, \"[state] enabled=${state.enabled} trackingMode=${state.trackingMode}\")\n        }\n    }\n\n    override fun onDestroy() {\n        subscriptions.forEach { it.close() }\n        subscriptions.clear()\n        super.onDestroy()\n    }\n}\n</code></pre>"},{"location":"kotlin/setup/","title":"Setup","text":""},{"location":"kotlin/setup/#installation","title":"Installation","text":""},{"location":"kotlin/setup/#1-add-the-maven-dependencies","title":"1. Add the Maven dependencies","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    compileSdk = 36\n    defaultConfig {\n        minSdk = 24\n    }\n}\n\ndependencies {\n    implementation(\"com.transistorsoft:tslocationmanager:4.0.+\")\n    implementation(\"com.google.android.gms:play-services-location:21.3.0\")\n}\n</code></pre> <pre><code>android {\n    compileSdk 36\n    defaultConfig {\n        minSdk 24\n    }\n}\n\ndependencies {\n    implementation 'com.transistorsoft:tslocationmanager:4.0.+'\n    implementation 'com.google.android.gms:play-services-location:21.3.0'\n}\n</code></pre> <p><code>tslocationmanager</code> is published to Maven Central \u2014 no additional repository configuration required for modern Android projects (Maven Central is included by default in <code>dependencyResolutionManagement</code> / <code>settings.gradle</code>).</p> <p><code>play-services-location</code> is a transitive dependency of <code>tslocationmanager</code>, but declaring it explicitly lets you control the version.</p> Dependency Default Browse versions <code>com.transistorsoft:tslocationmanager</code> <code>4.0.+</code> central.sonatype.com <code>com.google.android.gms:play-services-location</code> <code>21.3.0</code> maven.google.com <p>Play Services v20 (legacy)</p> <p>If your project targets Google Play Services \u2264 20 (rare for new projects), use the legacy artifact instead:</p> build.gradle.ktsbuild.gradle <pre><code>implementation(\"com.transistorsoft:tslocationmanager-gms20:4.0.+\")\nimplementation(\"com.google.android.gms:play-services-location:20.0.0\")\n</code></pre> <pre><code>implementation 'com.transistorsoft:tslocationmanager-gms20:4.0.+'\nimplementation 'com.google.android.gms:play-services-location:20.0.0'\n</code></pre>"},{"location":"kotlin/setup/#2-permissions-services-and-receivers","title":"2. Permissions, services, and receivers","text":"<p>The library's AAR manifest automatically merges all required permissions, services, and receivers into your app. No manual <code>AndroidManifest.xml</code> entries are needed for the SDK itself.</p>"},{"location":"kotlin/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p> <p>Add the license key as <code>&lt;meta-data&gt;</code> inside the <code>&lt;application&gt;</code> element of your <code>AndroidManifest.xml</code>:</p> <pre><code>&lt;manifest&gt;\n    &lt;application&gt;\n\n        &lt;meta-data\n            android:name=\"com.transistorsoft.locationmanager.license\"\n            android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n\n    &lt;/application&gt;\n&lt;/manifest&gt;\n</code></pre>"},{"location":"kotlin/setup/#example","title":"Example","text":"<pre><code>import com.transistorsoft.locationmanager.kotlin.BGGeo\nimport com.transistorsoft.locationmanager.kotlin.config.DesiredAccuracy\nimport com.transistorsoft.locationmanager.kotlin.config.LogLevel\n\n// Application.onCreate() \u2014 initialize once\nclass MyApp : Application() {\n    override fun onCreate() {\n        super.onCreate()\n        BGGeo.init(this)\n    }\n}\n\n// Activity / Fragment \u2014 call ready() in a coroutine scope\nclass MainActivity : AppCompatActivity() {\n    private val subscriptions = mutableSetOf&lt;EventSubscription&gt;()\n\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        val bgGeo = BGGeo.instance\n        bgGeo.setActivity(this)\n\n        // Register event listeners *before* calling ready()\n        bgGeo.onLocation { event -&gt;\n            Log.d(TAG, \"[onLocation] ${event.coords}\")\n        }.storeIn(subscriptions)\n\n        lifecycleScope.launch {\n            // ready() configures the SDK and restores persisted state.\n            // It does NOT start tracking \u2014 call start()/stop() separately.\n            bgGeo.ready {\n                geolocation.desiredAccuracy = DesiredAccuracy.HIGH\n                geolocation.distanceFilter = 10f\n                app.stopOnTerminate = false\n                app.startOnBoot = true\n                logger.debug = true\n                logger.logLevel = LogLevel.VERBOSE\n            }\n        }\n    }\n\n    // Elsewhere \u2014 e.g. a toggle-button click listener in your UI\n    private fun onToggleTracking() {\n        lifecycleScope.launch {\n            val bgGeo = BGGeo.instance\n            val state = bgGeo.state\n            if (state.enabled) {\n                bgGeo.stop()\n            } else {\n                bgGeo.start()\n            }\n            Log.d(TAG, \"[state] enabled=${state.enabled} trackingMode=${state.trackingMode}\")\n        }\n    }\n\n    override fun onDestroy() {\n        subscriptions.forEach { it.close() }\n        subscriptions.clear()\n        super.onDestroy()\n    }\n}\n</code></pre>"},{"location":"react-native/","title":"React Native SDK Reference","text":"react-native-background-geolocation Buy Now"},{"location":"react-native/#quick-links","title":"Quick links","text":"Section Description BackgroundGeolocation Primary entry point \u2014 start/stop, events, config AppConfig Lifecycle, heartbeat, scheduler, notifications HttpConfig HTTP upload endpoint, batching, auth headers GeoConfig Accuracy, distance filter, stop detection PersistenceConfig SQLite storage, TTL, templates LoggerConfig Log levels, retention, debug mode <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"react-native/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<pre><code>export const AccuracyAuthorization = {\n</code></pre> <p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"react-native/AccuracyAuthorization/#members","title":"Members","text":""},{"location":"react-native/AccuracyAuthorization/#full","title":"Full","text":"<p>Full accuracy authorized.</p>"},{"location":"react-native/AccuracyAuthorization/#reduced","title":"Reduced","text":"<p>Reduced accuracy granted.</p>"},{"location":"react-native/ActivityConfig/","title":"ActivityConfig","text":"<pre><code>export interface ActivityConfig {\n</code></pre> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"react-native/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"react-native/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    // Poll activity recognition every 10 seconds (both platforms)\n    activityRecognitionInterval: 10000,\n\n    // Require \u226575% confidence before changing activity (both platforms)\n    minimumActivityRecognitionConfidence: 75,\n\n    // Disable automatic stop detection (both platforms)\n    disableStopDetection: false,\n\n    // Automatically stop when stationary (both platforms)\n    stopOnStationary: true,\n\n    // Delay motion-trigger transitions by 30s (Android only)\n    motionTriggerDelay: 30000,\n\n    // Disable platform motion activity APIs (both platforms)\n    disableMotionActivityUpdates: false,\n\n    // Delay stop-detection by 10s (iOS only)\n    stopDetectionDelay: 10000\n  }\n});\n</code></pre>"},{"location":"react-native/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeoConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"react-native/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Legacy (deprecated)\nBackgroundGeolocation.ready({\n  activityRecognitionInterval: 10000,\n  disableStopDetection: true,\n  stopOnStationary: true\n});\n</code></pre> <pre><code>// Current\nBackgroundGeolocation.ready({\n  activity: {\n    activityRecognitionInterval: 10000,\n    disableStopDetection: true,\n    stopOnStationary: true\n  }\n});\n</code></pre>"},{"location":"react-native/ActivityConfig/#members","title":"Members","text":""},{"location":"react-native/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>activityRecognitionInterval?: number;\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"react-native/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>disableMotionActivityUpdates?: boolean;\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"react-native/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"react-native/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableMotionActivityUpdates: true\n  }\n});\n</code></pre>"},{"location":"react-native/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"react-native/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeoConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeoConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableStopDetection: true,\n  },\n  geolocation: {\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BackgroundGeolocation.changePace).</p> <p></p>"},{"location":"react-native/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BackgroundGeolocation.changePace <code>false</code> or BackgroundGeolocation.stop explicitly.</p>"},{"location":"react-native/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>minimumActivityRecognitionConfidence?: number;\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"react-native/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>motionTriggerDelay?: number;\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    motionTriggerDelay: 30000\n  }\n});\n</code></pre>"},{"location":"react-native/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>stopDetectionDelay?: number;\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"react-native/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the GeoConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeoConfig.stopTimeout timer elapses naturally. It does not fire when BackgroundGeolocation.changePace <code>false</code> is called manually.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    stopOnStationary: true,\n  }\n}).then(() =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"react-native/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<p><code>triggerActivities?:</code><code>TriggerActivity</code><code>|</code><code>TriggerActivity</code><code>[];</code></p> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See TriggerActivity for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>// Only trigger tracking for vehicles\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"in_vehicle\"]\n  }\n});\n\n// Only trigger tracking for on_foot, walking and running\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"on_foot\", \"walking\", \"running\"]\n  }\n});\n</code></pre>"},{"location":"react-native/ActivityType/","title":"ActivityType","text":"<pre><code>export const ActivityType = {\n</code></pre> <p>iOS Activity Type used with GeoConfig.activityType.</p> <p>Corresponds to Apple's <code>CLActivityType</code> enum: https://developer.apple.com/documentation/corelocation/clactivitytype</p> Name Value Description Other 1 Default/unspecified activity. AutomotiveNavigation 2 Automotive navigation mode. Fitness 3 Fitness-related activity. OtherNavigation 4 Non-automotive navigation mode. Airborne 5 Airborne activity (iOS 15+)."},{"location":"react-native/ActivityType/#members","title":"Members","text":""},{"location":"react-native/ActivityType/#airborne","title":"Airborne","text":"<p>Airborne activity (iOS 15+).</p>"},{"location":"react-native/ActivityType/#automotivenavigation","title":"AutomotiveNavigation","text":"<p>Automotive navigation mode.</p>"},{"location":"react-native/ActivityType/#fitness","title":"Fitness","text":"<p>Fitness-related activity.</p>"},{"location":"react-native/ActivityType/#other","title":"Other","text":"<p>Default/unspecified activity.</p>"},{"location":"react-native/ActivityType/#othernavigation","title":"OtherNavigation","text":"<p>Non-automotive navigation mode.</p>"},{"location":"react-native/AppConfig/","title":"AppConfig","text":"<pre><code>export interface AppConfig {\n</code></pre> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"react-native/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"react-native/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"react-native/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BackgroundGeolocation.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"react-native/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BackgroundGeolocation.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"react-native/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See PermissionRationale for the full dialog configuration reference.</p>"},{"location":"react-native/AppConfig/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"react-native/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#members","title":"Members","text":""},{"location":"react-native/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<p><code>background</code><code>PermissionRationale</code><code>?:</code><code>PermissionRationale</code><code>;</code></p> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside PermissionRationale fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Location updates in Android 11</p>"},{"location":"react-native/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BackgroundGeolocation.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BackgroundGeolocation.registerHeadlessTask</p>"},{"location":"react-native/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>heartbeatInterval?: number;\n</code></pre> <p>Rate in seconds at which BackgroundGeolocation.onHeartbeat events fire while the app is in the background.</p>"},{"location":"react-native/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"react-native/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BackgroundGeolocation.onHeartbeat</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60\n  }\n});\n\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n\n  // Optionally request a new location during heartbeat.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition]\", location);\n  });\n});\n</code></pre>"},{"location":"react-native/AppConfig/#notification","title":"notification","text":"<p><code>notification?:</code><code>NotificationConfig</code><code>;</code></p> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"Background tracking engaged\",\n      text: \"My notification text\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>preventSuspend?: boolean;\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BackgroundGeolocation.onHeartbeat</p> <pre><code>// Subscribe to heartbeat events\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n});\n\n// Enable preventSuspend via AppConfig\nBackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60,\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#schedule","title":"schedule","text":"<pre><code>schedule?: string[];\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"{DAY(s)} {START_TIME}-{END_TIME}\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1 17:30-21:00\",     // Sunday: 5:30pm\u20139pm\n      \"2-6 09:00-17:00\",   // Mon\u2013Fri: 9am\u20135pm\n      \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm\u2013midnight\n      \"7 10:00-19:00\"      // Saturday: 10am\u20137pm\n    ]\n  }\n}).then((state) =&gt; {\n  // Start the Scheduler\n  BackgroundGeolocation.startSchedule();\n});\n\n// Listen for schedule state changes\nBackgroundGeolocation.onSchedule((state) =&gt; {\n  console.log(\"[onSchedule] enabled?\", state.enabled);\n});\n\n// Later (e.g., user logout)\nBackgroundGeolocation.stopSchedule();\nBackgroundGeolocation.stop(); // if tracking is currently active\n\n// Modify schedule using setConfig\nBackgroundGeolocation.setConfig({\n  app: {\n    schedule: [\n      \"1-7 09:00-10:00\",\n      \"1-7 11:00-12:00\",\n      \"1-7 13:00-14:00\",\n      \"1-7 15:00-16:00\",\n      \"1-7 17:00-18:00\",\n      \"2,4,6 19:00-22:00\"\n    ]\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01 09:00-17:00\"\n    ]\n  }\n});\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-00:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"react-native/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BackgroundGeolocation.startSchedule - BackgroundGeolocation.stopSchedule</p>"},{"location":"react-native/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-12:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>scheduleUseAlarmManager?: boolean;\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\"1-7 09:00-17:00\"],\n    scheduleUseAlarmManager: true\n  }\n});\n</code></pre>"},{"location":"react-native/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"react-native/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"react-native/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"react-native/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"react-native/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeoConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"react-native/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"react-native/AuthorizationConfig/","title":"AuthorizationConfig","text":"<pre><code>export interface AuthorizationConfig {\n</code></pre> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"react-native/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"react-native/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>const token = getMyToken(); // Your own token fetch method\n\nBackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://app.your.server.com/users/locations\",\n    autoSync: true,\n  },\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshPayload: {\n      the_refresh_token_field_name: \"{refreshToken}\"\n    },\n    expires: token.expiresAt,\n  }\n});\n</code></pre>"},{"location":"react-native/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"react-native/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"react-native/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n</code></pre>"},{"location":"react-native/AuthorizationConfig/#members","title":"Members","text":""},{"location":"react-native/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\"\n  }\n});\n</code></pre>"},{"location":"react-native/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>expires?: number;\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"react-native/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>refreshHeaders?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.domain.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    },\n    refreshHeaders: {}\n  }\n});\n</code></pre>"},{"location":"react-native/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>refreshPayload?: Record&lt;string, string&gt;;\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    }\n  }\n});\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"react-native/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken?: string;\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"react-native/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>refreshUrl?: string;\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p>"},{"location":"react-native/AuthorizationConfig/#strategy","title":"strategy","text":"<pre><code>strategy: string;\n</code></pre> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"react-native/AuthorizationEvent/","title":"AuthorizationEvent","text":"<pre><code>export interface AuthorizationEvent {\n</code></pre> <p>Authorization token refresh result delivered to BackgroundGeolocation.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onAuthorization] Token refreshed:\", event.response);\n    // Update your app's stored token.\n    const token = event.response.access_token;\n  } else {\n    console.warn(\"[onAuthorization] Refresh failed:\", event.error);\n  }\n});\n</code></pre>"},{"location":"react-native/AuthorizationEvent/#members","title":"Members","text":""},{"location":"react-native/AuthorizationEvent/#error","title":"error","text":"<pre><code>error: string;\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"react-native/AuthorizationEvent/#response","title":"response","text":"<pre><code>response: any;\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"react-native/AuthorizationEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"react-native/AuthorizationEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"react-native/AuthorizationStatus/","title":"AuthorizationStatus","text":"<pre><code>export const AuthorizationStatus = {\n</code></pre> <p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"react-native/AuthorizationStatus/#members","title":"Members","text":""},{"location":"react-native/AuthorizationStatus/#always","title":"Always","text":"<p>Location permissions authorized for always use.</p>"},{"location":"react-native/AuthorizationStatus/#denied","title":"Denied","text":"<p>Location permissions denied by the user.</p>"},{"location":"react-native/AuthorizationStatus/#notdetermined","title":"NotDetermined","text":"<pre><code>NotDetermined: 0,\n</code></pre> <p>User has not yet made a choice regarding location permissions.</p>"},{"location":"react-native/AuthorizationStatus/#restricted","title":"Restricted","text":"<pre><code>Restricted: 1,\n</code></pre> <p>Location permissions are restricted (e.g., parental controls).</p>"},{"location":"react-native/AuthorizationStatus/#wheninuse","title":"WhenInUse","text":"<p>Location permissions authorized for when-in-use (Android 10+).</p>"},{"location":"react-native/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<pre><code>export const AuthorizationStrategy = {\n</code></pre> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"react-native/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"react-native/AuthorizationStrategy/#jwt","title":"Jwt","text":"<p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"react-native/AuthorizationStrategy/#sas","title":"Sas","text":"<p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"react-native/BackgroundGeolocation/","title":"BackgroundGeolocation","text":"<pre><code>export interface BackgroundGeolocation {\n</code></pre> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>","boost":10},{"location":"react-native/BackgroundGeolocation/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>","boost":10},{"location":"react-native/BackgroundGeolocation/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeoConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li>Event</li> </ul> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"react-native-background-geolocation\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a Subscription that must be removed when no longer needed:</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre> <pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#compound-configuration","title":"Compound configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"{{pluginName}}\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#event-listeners","title":"Event listeners","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getting-started","title":"Getting started","text":"<pre><code>import BackgroundGeolocation from \"{{pluginName}}\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#events_1","title":"Events","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#onactivitychange","title":"onActivityChange","text":"<p><code>onActivityChange(cb: (event:</code><code>MotionActivityEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#android","title":"Android","text":"<p>MotionActivityEvent.confidence always reports <code>100</code>.</p> <pre><code>const subscription = BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange] \", event);\n});\n</code></pre> <p>activitychange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onauthorization","title":"onAuthorization","text":"<p><code>onAuthorization(cb: (event:</code><code>AuthorizationEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>const subscription = BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS: \", event.response);\n  } else {\n    console.log(\"[authorization] ERROR: \", event.error);\n  }\n});\n</code></pre> <p>authorization</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>onConnectivityChange(cb: (event:</code><code>ConnectivityChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>const subscription = BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] \", event);\n});\n</code></pre> <p>connectivitychange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onenabledchange","title":"onEnabledChange","text":"<p><code>onEnabledChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>const subscription = BackgroundGeolocation.onEnabledChange((isEnabled) =&gt; {\n  console.log(\"[onEnabledChange] isEnabled? \", isEnabled);\n});\n</code></pre> <p>enabledchange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ongeofence","title":"onGeofence","text":"<p><code>on</code><code>Geofence</code><code>(cb: (event:</code><code>GeofenceEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] \", event);\n});\n</code></pre> <p>geofence</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>onGeofencesChange(cb: (event:</code><code>GeofencesChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeoConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  // Create map circles\n  on.forEach((geofence) =&gt; {\n    createGeofenceMarker(geofence);\n  });\n\n  // Remove map circles\n  off.forEach((identifier) =&gt; {\n    removeGeofenceMarker(identifier);\n  });\n});\n</code></pre> <p>geofenceschange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onheartbeat","title":"onHeartbeat","text":"<p><code>onHeartbeat(cb: (event:</code><code>HeartbeatEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The Location provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    heartbeatInterval: 60,\n    preventSuspend: true  // required for iOS\n  }\n});\n\nconst subscription = BackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat] \", event);\n\n  // Optionally fetch a fresh location.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition] \", location);\n  });\n});\n</code></pre> <p>heartbeat</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onhttp","title":"onHttp","text":"<p><code>onHttp(cb: (event:</code><code>HttpEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onHttp((response) =&gt; {\n  const status = response.status;\n  const success = response.success;\n  const responseText = response.responseText;\n  console.log(\"[onHttp] \", response);\n});\n</code></pre> <p>http</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onlocation","title":"onLocation","text":"<p><code>on</code><code>Location</code><code>(cb: (location:</code><code>Location</code><code>) =&gt; void, onError?: (err:</code><code>LocationError</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a LocationError code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with Location.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation] success: \", location);\n}, (error) =&gt; {\n  console.log(\"[onLocation] ERROR: \", error);\n});\n</code></pre> <p>location</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onmotionchange","title":"onMotionChange","text":"<p><code>onMotionChange(cb: (event:</code><code>MotionChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeoConfig.stopTimeout</p> <pre><code>const subscription = BackgroundGeolocation.onMotionChange((event: MotionChangeEvent) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device has just started MOVING \", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device has just STOPPED: \", event.location);\n  }\n});\n</code></pre> <p>motionchange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onnotificationaction","title":"onNotificationAction","text":"<p><code>onNotificationAction(cb: (buttonId: string) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onpowersavechange","title":"onPowerSaveChange","text":"<p><code>onPowerSaveChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"react-native/BackgroundGeolocation/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const subscription = BackgroundGeolocation.onPowerSaveChange((isPowerSaveMode) =&gt; {\n  console.log(\"[onPowerSaveChange]: \", isPowerSaveMode);\n});\n</code></pre> <p>powersavechange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onproviderchange","title":"onProviderChange","text":"<p><code>onProviderChange(cb: (event:</code><code>ProviderChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>const subscription = BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]: \", event);\n\n  switch (event.status) {\n    case BackgroundGeolocation.AuthorizationStatus.Denied:\n      // Android &amp; iOS\n      console.log(\"- Location authorization denied\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.Always:\n      // Android &amp; iOS\n      console.log(\"- Location always granted\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.WhenInUse:\n      // iOS only\n      console.log(\"- Location WhenInUse granted\");\n      break;\n  }\n});\n</code></pre> <p>providerchange</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#onschedule","title":"onSchedule","text":"<p><code>onSchedule(cb: (state:</code><code>State</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>const subscription = BackgroundGeolocation.onSchedule((state) =&gt; {\n  if (state.enabled) {\n    console.log(\"[onSchedule] scheduled start tracking\");\n  } else {\n    console.log(\"[onSchedule] scheduled stop tracking\");\n  }\n});\n</code></pre> <p>schedule</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#methods","title":"Methods","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#changepace","title":"changePace","text":"<p><code>changePace(isMoving: boolean): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>BackgroundGeolocation.changePace(true);  // location services ON (\"moving\")\nBackgroundGeolocation.changePace(false); // location services OFF (\"stationary\")\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#destroytransistorauthorizationtoken","title":"destroyTransistorAuthorizationToken","text":"<p><code>destroy</code><code>TransistorAuthorizationToken</code><code>(url: string): Promise&lt;void&gt;;</code></p> <p>Destroy a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#findorcreatetransistorauthorizationtoken","title":"findOrCreateTransistorAuthorizationToken","text":"<p><code>findOrCreateTransistorAuthorizationToken(orgname: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>getCurrentPosition(options?:</code><code>CurrentPositionRequest</code><code>): Promise&lt;</code><code>Location</code><code>&gt;;</code></p> <p>Retrieve the current Location.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a LocationError.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#options","title":"Options","text":"<p>See CurrentPositionRequest.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#error-codes_1","title":"Error Codes","text":"<p>See LocationError.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with Location.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // 30 second timeout to fetch location\n  maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.\n  desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.\n  samples: 3,           // How many location samples to attempt.\n  extras: {             // Custom meta-data.\n    \"route_id\": 123\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getdeviceinfo","title":"getDeviceInfo","text":"<p><code>get</code><code>DeviceInfo</code><code>(): Promise&lt;</code><code>DeviceInfo</code><code>&gt;;</code></p> <p>Returns device information.</p> <pre><code>const deviceInfo = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(deviceInfo);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getodometer","title":"getOdometer","text":"<pre><code>getOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the current odometer reading in meters.</p> <p>The SDK continuously accumulates distance traveled between recorded locations.</p> <p>Warning</p> <p>Odometer accuracy depends on location accuracy. Noisy or inaccurate locations introduce error into accumulated distance. Use LocationFilter.odometerAccuracyThreshold to filter low-accuracy samples from odometer calculations.</p> <p>See also - LocationFilter.odometerAccuracyThreshold - <code>resetOdometer</code> / <code>setOdometer</code></p> <pre><code>const odometer = await BackgroundGeolocation.getOdometer();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getstate","title":"getState","text":"<p><code>get</code><code>State</code><code>(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Return the current State of the SDK, including all Config parameters.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] \", state.enabled, state.trackingMode);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#ready","title":"ready","text":"<p><code>ready(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n  http: {\n    url: \"http://your.server.com\",\n    headers: { \"my-auth-token\": \"secret-token\" },\n  }\n});\nconsole.log(\"[ready] success\", state);\n</code></pre> <pre><code>BackgroundGeolocation.reset();\n// Reset to documented default-values with overrides\nbgGeo.reset({\n  distanceFilter:  10\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#registerheadlesstask","title":"registerHeadlessTask","text":"<p><code>registerHeadlessTask(callback: (event:</code><code>HeadlessEvent</code><code>) =&gt; Promise&lt;void&gt;): void;</code></p> <p>Android only Registers a headless-task callback for Android background events when the app has been terminated with AppConfig.stopOnTerminate<code>:false</code>.</p> <p>The callback receives a HeadlessEvent with a <code>name</code> (event name) and <code>params</code> (event data).</p> <p>Warning</p> <p>You must call <code>registerHeadlessTask</code> in your application root file (e.g. <code>index.js</code>), not inside a component or behind a UI action.</p> <p>Warning</p> <p>Your function must be declared <code>async</code>. Await all work inside it \u2014 the headless task is automatically terminated after the last line executes.</p> <p>Note</p> <p>Javascript headless callbacks are not supported by Cordova or Capacitor.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#debugging","title":"Debugging","text":"<p>While implementing your headless task, observe Android logs via:</p> <pre><code>$ adb logcat *:S TSLocationManager:V ReactNativeJS:V\n</code></pre> <p>See also - \ud83d\udcd8 Android Headless Mode - AppConfig.enableHeadless</p> <pre><code>const BackgroundGeolocationHeadlessTask = async (event) =&gt; {\n  const params = event.params;\n  console.log(\"[BackgroundGeolocation HeadlessTask] -\", event.name, params);\n\n  switch (event.name) {\n    case \"terminate\":\n      // Use await for async tasks\n      const location = await BackgroundGeolocation.getCurrentPosition({\n        samples: 1,\n        persist: false\n      });\n      console.log(\"[BackgroundGeolocation HeadlessTask] - getCurrentPosition:\", location);\n      break;\n  }\n  // You must await all work you do in your task.\n  // Headless-tasks are automatically terminated after executing the last line.\n  await doWork();\n}\n\nBackgroundGeolocation.registerHeadlessTask(BackgroundGeolocationHeadlessTask);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#removelisteners","title":"removeListeners","text":"<pre><code>removeListeners(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all event listeners.</p> <p>Calls Subscription.remove on all active subscriptions.</p> <pre><code>BackgroundGeolocation.removeListeners();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#reset","title":"reset","text":"<p><code>reset(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Reset the SDK configuration to documented default values.</p> <p>If an optional Config is provided, it is applied after the reset.</p> <pre><code>BackgroundGeolocation.reset();\n// Reset to default values with overrides\nBackgroundGeolocation.reset({\n  geolocation: { distanceFilter: 10 }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#resetodometer","title":"resetOdometer","text":"<pre><code>resetOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>const location = await BackgroundGeolocation.resetOdometer();\nconsole.log(\"[resetOdometer] reset at: \", location);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#setconfig","title":"setConfig","text":"<p><code>set</code><code>Config</code><code>(config: Partial&lt;</code><code>Config</code><code>&gt;): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Update the SDK's Config at runtime.</p> <p>The supplied Config is merged into the current configuration and applied immediately. Use this after <code>ready</code> has been called to change settings dynamically.</p> <pre><code>const state = await BackgroundGeolocation.setConfig({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 100.0,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\nconsole.log(\"[setConfig] success: \", state);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#setodometer","title":"setOdometer","text":"<pre><code>setOdometer(value: number): Promise&lt;number&gt;;\n</code></pre> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>const location = await BackgroundGeolocation.setOdometer(1234.56);\nconsole.log(\"[setOdometer] set at: \", location);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#start","title":"start","text":"<p><code>start(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>const state = await BackgroundGeolocation.start();\nconsole.log(\"[start] success - \", state);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#startgeofences","title":"startGeofences","text":"<p><code>startGeofences(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Add a geofence.\nBackgroundGeolocation.addGeofence({\n  notifyOnExit: true,\n  radius: 200,\n  identifier: \"ZONE_OF_INTEREST\",\n  latitude: 37.234232,\n  longitude: 42.234234\n});\n\n// Listen to geofence events.\nBackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] - \", event);\n});\n\n// Configure and start in geofences-only mode.\nBackgroundGeolocation.ready({\n  http: { url: \"http://my.server.com\", autoSync: true }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#startschedule","title":"startSchedule","text":"<pre><code>startSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>const state = await BackgroundGeolocation.startSchedule();\nconsole.log(\"[startSchedule] success: \", state);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#stop","title":"stop","text":"<p><code>stop(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>BackgroundGeolocation.stop();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#stop-tracking-and-the-scheduler","title":"Stop tracking and the scheduler","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#stopschedule","title":"stopSchedule","text":"<pre><code>stopSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>await BackgroundGeolocation.stopSchedule();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#stop-the-scheduler-and-active-tracking","title":"Stop the scheduler and active tracking","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#watchposition","title":"watchPosition","text":"<p><code>watchPosition(options:</code><code>WatchPositionRequest</code><code>, locationCallback: (location:</code><code>Location</code><code>) =&gt; void, errorCallback?: (errorCode: number) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a Subscription that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>onResume() async {\n  // Start watching position while app in foreground, retaining the return Subscription.\n  this.watchPositionSubscription = await BackgroundGeolocation.watchPosition({\n    interval: 1000,\n    extras: {foo: \"bar\"}\n  }, (location) =&gt; {\n    console.log(\"[watchPosition] -\", location);\n  }, (errorCode) =&gt; {\n    console.log(\"[watchPosition] ERROR -\", errorCode);\n  });\n}\n\nonSuspend() {\n  // Halt watching position when app goes to background.\n  this.watchPositionSubscription.remove();\n  this.watchPositionSubscription = null;\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#geofencing","title":"Geofencing","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#addgeofence","title":"addGeofence","text":"<p><code>add</code><code>Geofence</code><code>(geofence:</code><code>Geofence</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 150,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: false,\n  notifyOnDwell: true,\n  loiteringDelay: 30000,  // 30 seconds\n  extras: {               // Optional arbitrary meta-data\n    zone_id: 1234\n  }\n}).then((success) =&gt; {\n  console.log(\"[addGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[addGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#addgeofences","title":"addGeofences","text":"<p><code>addGeofences(geofences:</code><code>Geofence</code><code>[]): Promise&lt;boolean&gt;;</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>const geofences = [{\n  identifier: \"foo\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}, {\n  identifier: \"bar\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}];\n\nBackgroundGeolocation.addGeofences(geofences);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#geofenceexists","title":"geofenceExists","text":"<pre><code>geofenceExists(identifier: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const exists = await BackgroundGeolocation.geofenceExists(\"HOME\");\nconsole.log(\"[geofenceExists] \", exists);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getgeofence","title":"getGeofence","text":"<p><code>get</code><code>Geofence</code><code>(identifier: string): Promise&lt;</code><code>Geofence</code><code>&gt;;</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofence = await BackgroundGeolocation.getGeofence(\"HOME\");\nconsole.log(\"[getGeofence] \", geofence);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getgeofences","title":"getGeofences","text":"<p><code>getGeofences(): Promise&lt;</code><code>Geofence</code><code>[]&gt;;</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]: \", geofences);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#removegeofence","title":"removeGeofence","text":"<p><code>remove</code><code>Geofence</code><code>(identifier: string): Promise&lt;boolean&gt;;</code></p> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofence(\"Home\").then((success) =&gt; {\n  console.log(\"[removeGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[removeGeofence] FAILURE: \", error);\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#removegeofences","title":"removeGeofences","text":"<pre><code>removeGeofences(identifiers?: string[]): Promise&lt;boolean&gt;;\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofences();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#data-management","title":"Data Management","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#destroylocation","title":"destroyLocation","text":"<p><code>destroy</code><code>Location</code><code>(uuid: string): Promise&lt;void&gt;;</code></p> <p>Remove a single location by Location.uuid.</p> <pre><code>await BackgroundGeolocation.destroyLocation(location.uuid);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#destroylocations","title":"destroyLocations","text":"<pre><code>destroyLocations(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>await BackgroundGeolocation.destroyLocations();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getcount","title":"getCount","text":"<pre><code>getCount(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>const count = await BackgroundGeolocation.getCount();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#getlocations","title":"getLocations","text":"<pre><code>getLocations(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Retrieve all Location records stored in the SDK's SQLite database.</p> <pre><code>const locations = await BackgroundGeolocation.getLocations();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#sync","title":"sync","text":"<pre><code>sync(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const records = await BackgroundGeolocation.sync();\nconsole.log(\"[sync] success: \", records);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#authorization","title":"Authorization","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#getproviderstate","title":"getProviderState","text":"<p><code>getProviderState(): Promise&lt;</code><code>ProviderChangeEvent</code><code>&gt;;</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>const providerState = await BackgroundGeolocation.getProviderState();\nconsole.log(\"- Provider state: \", providerState);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#requestpermission","title":"requestPermission","text":"<p><code>requestPermission(): Promise&lt;</code><code>AuthorizationStatus</code><code>&gt;;</code></p> <p>Manually request location permission using the configured GeoConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeoConfig.locationAuthorizationRequest - GeoConfig.disableLocationAuthorizationAlert - GeoConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log('[providerchange]', event);\n});\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: 'Always' }\n});\n\ntry {\n  const status = await BackgroundGeolocation.requestPermission();\n  console.log('[requestPermission] success: ', status);\n} catch (status) {\n  console.warn('[requestPermission] FAILURE: ', status);\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<p><code>requestTemporaryFullAccuracy(purposeKey: string): Promise&lt;</code><code>AccuracyAuthorization</code><code>&gt;;</code></p> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>","boost":10},{"location":"react-native/BackgroundGeolocation/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Supply \"Purpose\" key from Info.plist as 1st argument.\n    BackgroundGeolocation.requestTemporaryFullAccuracy(\"Delivery\").then((accuracyAuthorization) =&gt; {\n      if (accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Full) {\n        console.log('[requestTemporaryFullAccuracy] GRANTED: ', accuracyAuthorization);\n      } else {\n        console.log('[requestTemporaryFullAccuracy] DENIED: ', accuracyAuthorization);\n      }\n    }).catch((error) =&gt; {\n      console.warn(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: \", error);\n    });\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#background-tasks","title":"Background Tasks","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>startBackgroundTask(): Promise&lt;number&gt;;\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ios_2","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#android_2","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>onLocation(location) {\n  console.log(\"[location] \", location);\n\n  // Perform some long-running task (eg: HTTP request)\n  BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n    performLongRunningTask().then(() =&gt; {\n      // When your long-running task is complete, signal completion of taskId.\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    }).catch((error) =&gt; {\n      // Be sure to catch errors: never leave your background-task hanging.\n      console.error(error);\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    });\n  });\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>stopBackgroundTask(taskId: number): Promise&lt;void&gt;;\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n  // Perform some long-running task (eg: HTTP request)\n  performLongRunningTask().then(() =&gt; {\n    // When your long-running task is complete, signal completion of taskId.\n    BackgroundGeolocation.stopBackgroundTask(taskId);\n  });\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#logging","title":"Logging","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#playsound","title":"playSound","text":"<pre><code>playSound(soundId: string | number): void;\n</code></pre> <p>Play a system sound.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ios_3","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#android_3","title":"Android","text":"<p>Provide a sound name string.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#setloglevel","title":"setLogLevel","text":"<p><code>set</code><code>LogLevel</code><code>(level:</code><code>LogLevel</code><code>): Promise&lt;void&gt;;</code></p> <p>Sets the LoggerConfig.logLevel.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#device","title":"Device","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#getsensors","title":"getSensors","text":"<p><code>get</code><code>Sensors</code><code>(): Promise&lt;</code><code>Sensors</code><code>&gt;;</code></p> <p>Returns the availability of motion sensors: accelerometer, gyroscope, and magnetometer.</p> <p>These sensors power the motion activity-recognition system. When any sensor is absent (particularly on low-end Android devices), motion recognition performance degrades significantly.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(sensors);\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>isPowerSaveMode(): Promise&lt;boolean&gt;;\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#ios_4","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>","boost":10},{"location":"react-native/BackgroundGeolocation/#android_4","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const isPowerSaveMode = await BackgroundGeolocation.isPowerSaveMode();\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#properties","title":"Properties","text":"","boost":10},{"location":"react-native/BackgroundGeolocation/#accuracyauthorization","title":"AccuracyAuthorization","text":"<p><code>AccuracyAuthorization</code><code>: object;</code></p> <p>Enum namespace indicating whether the user granted full or reduced location accuracy. [iOS 14+]</p> <p>Used by <code>ProviderChangeEvent.accuracyAuthorization</code> and <code>requestTemporaryFullAccuracy</code>.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization ===\n      BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Handle reduced-accuracy case\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#activitytype","title":"ActivityType","text":"<p><code>ActivityType</code><code>: object;</code></p> <p>iOS only Enum namespace specifying the type of user activity (AutomotiveNavigation, Fitness, OtherNavigation, etc).</p> <p>Used by GeoConfig.activityType.</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#authorizationstatus","title":"AuthorizationStatus","text":"<p><code>AuthorizationStatus</code><code>: object;</code></p> <p>Enum namespace representing the OS-level authorization state for location services (Denied, Restricted, Always, WhenInUse).</p> <p>Returned from <code>requestPermission()</code> and <code>onProviderChange</code>.</p> <pre><code>const status = await BackgroundGeolocation.requestPermission();\nif (status === BackgroundGeolocation.AuthorizationStatus.Always) {\n  // Good to start tracking\n}\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#authorizationstrategy","title":"AuthorizationStrategy","text":"<p><code>AuthorizationStrategy</code><code>: object;</code></p> <p>Enum namespace defining how the HTTP service performs authorization.</p> <p>Includes basic, JWT, and custom strategies. Used by <code>AuthorizationConfig.strategy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: BackgroundGeolocation.AuthorizationStrategy.Jwt\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#desiredaccuracy","title":"DesiredAccuracy","text":"<p><code>DesiredAccuracy</code><code>: object;</code></p> <p>Enum namespace controlling the native location engine's target accuracy.</p> <p>Higher accuracy consumes more battery. Used by <code>GeoConfig.desiredAccuracy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#event","title":"Event","text":"<p><code>Event</code><code>: object;</code></p> <p>Enum namespace of all event names emitted by the SDK (<code>location</code>, <code>geofence</code>, <code>motionchange</code>, <code>heartbeat</code>, etc).</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#httpmethod","title":"HttpMethod","text":"<p><code>HttpMethod</code><code>: object;</code></p> <p>Enum namespace defining the HTTP method used for location uploads (POST, PUT, etc).</p> <p>Used by <code>HttpConfig.method</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    method: BackgroundGeolocation.HttpMethod.Post\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#kalmanprofile","title":"KalmanProfile","text":"<p><code>KalmanProfile</code><code>: object;</code></p> <p>Enum namespace selecting a preset Kalman filter tuning profile (aggressive, moderate, or relaxed smoothing).</p> <p>Used by <code>LocationFilter.kalmanProfile</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      kalmanProfile: BackgroundGeolocation.KalmanProfile.Aggressive\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#locationfilterpolicy","title":"LocationFilterPolicy","text":"<p><code>LocationFilterPolicy</code><code>: object;</code></p> <p>Enum namespace selecting the filtering engine policy for noise-reduction and smoothing.</p> <p>Used by <code>LocationFilter.policy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: BackgroundGeolocation.LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#locationrequest","title":"LocationRequest","text":"<p><code>LocationRequest</code><code>: object;</code></p> <p>iOS only Enum namespace defining the type of location permission request (Always, WhenInUse, or Any). Used by <code>GeoConfig.locationAuthorizationRequest</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: BackgroundGeolocation.LocationRequest.Always\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#loglevel","title":"LogLevel","text":"<p><code>LogLevel</code><code>: object;</code></p> <p>Enum namespace controlling verbosity of the SDK logger.</p> <p>Used by <code>LoggerConfig.logLevel</code>. Values range from silent (<code>Off</code>) to extremely verbose (<code>Verbose</code>).</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#notificationpriority","title":"NotificationPriority","text":"<p><code>NotificationPriority</code><code>: object;</code></p> <p>Android only Enum namespace controlling Android foreground-service notification priority and icon placement (top, bottom, hidden).</p> <p>Used by <code>NotificationConfig.priority</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: BackgroundGeolocation.NotificationPriority.High\n    }\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#persistmode","title":"PersistMode","text":"<p><code>PersistMode</code><code>: object;</code></p> <p>Enum namespace controlling which records the SDK persists to SQLite: locations only, geofences only, both, or none.</p> <p>Used by <code>PersistenceConfig.persistMode</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    persistMode: BackgroundGeolocation.PersistMode.All\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#triggeractivity","title":"TriggerActivity","text":"<p><code>TriggerActivity</code><code>: object;</code></p> <p>Enum namespace defining which physical motion activities can trigger motion-detection transitions (still \u2192 moving).</p> <p>Used by <code>ActivityConfig.triggerActivities</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\n      BackgroundGeolocation.TriggerActivity.InVehicle\n    ]\n  }\n});\n</code></pre>","boost":10},{"location":"react-native/BackgroundGeolocation/#devicesettings","title":"deviceSettings","text":"<p><code>deviceSettings:</code><code>DeviceSettings</code><code>;</code></p> <p>DeviceSettings API</p>","boost":10},{"location":"react-native/BackgroundGeolocation/#logger","title":"logger","text":"<p><code>logger:</code><code>Logger</code><code>;</code></p> <p>Logger API</p>","boost":10},{"location":"react-native/Battery/","title":"Battery","text":"<pre><code>export interface Battery {\n</code></pre> <p>Device battery state at the time a Location was recorded.</p>"},{"location":"react-native/Battery/#members","title":"Members","text":""},{"location":"react-native/Battery/#is_charging","title":"is_charging","text":"<pre><code>is_charging: boolean;\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"react-native/Battery/#level","title":"level","text":"<pre><code>level: number;\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"react-native/Config/","title":"Config","text":"<pre><code>export interface Config {\n</code></pre> <p>Root configuration object passed to BackgroundGeolocation.ready and BackgroundGeolocation.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"react-native/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"react-native/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeoConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BackgroundGeolocation.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BackgroundGeolocation.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"react-native/Config/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"react-native/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre>"},{"location":"react-native/Config/#full-configuration","title":"Full configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from '{{pluginName}}';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"react-native/Config/#members","title":"Members","text":""},{"location":"react-native/Config/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"react-native/Config/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"react-native/Config/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"react-native/Config/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeoConfig.</p>"},{"location":"react-native/Config/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"react-native/Config/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"react-native/Config/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"react-native/Config/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"react-native/Config/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre> <pre><code>// Base url to Transistor Demo Server.\nconst url = \"http://tracker.transistorsoft.com\";\n\n// Register for an authorization token from server.\nconst token = await\n  BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\"my-company-name\", \"my-username\");\n\nBackgroundGeolocation.ready({\n  url: url + \"/api/locations\",\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: url + \"/v2/refresh_token\",\n    refreshPayload: {\n      refresh_token: \"{refreshToken}\"\n    },\n    expires: token.expires\n  }\n});\n</code></pre>"},{"location":"react-native/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<pre><code>export interface ConnectivityChangeEvent {\n</code></pre> <p>Network connectivity change delivered to BackgroundGeolocation.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] connected:\", event.connected);\n});\n</code></pre>"},{"location":"react-native/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"react-native/ConnectivityChangeEvent/#connected","title":"connected","text":"<pre><code>connected: boolean;\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"react-native/Coords/","title":"Coords","text":"<pre><code>export interface Coords {\n</code></pre> <p>Geographic coordinates attached to a Location.</p>"},{"location":"react-native/Coords/#members","title":"Members","text":""},{"location":"react-native/Coords/#accuracy","title":"accuracy","text":"<pre><code>accuracy: number;\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"react-native/Coords/#altitude","title":"altitude","text":"<pre><code>altitude?: number;\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"react-native/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"react-native/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"react-native/Coords/#altitude_accuracy","title":"altitude_accuracy","text":"<pre><code>altitude_accuracy?: number;\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"react-native/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"react-native/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"react-native/Coords/#ellipsoidal_altitude","title":"ellipsoidal_altitude","text":"<pre><code>ellipsoidal_altitude?: number;\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"react-native/Coords/#floor","title":"floor","text":"<pre><code>floor?: number;\n</code></pre> <p>iOS only Floor within a building, when indoor-positioning hardware (e.g. Bluetooth beacons) is available.</p>"},{"location":"react-native/Coords/#heading","title":"heading","text":"<pre><code>heading?: number;\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"react-native/Coords/#heading_accuracy","title":"heading_accuracy","text":"<pre><code>heading_accuracy?: number;\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"react-native/Coords/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"react-native/Coords/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"react-native/Coords/#speed","title":"speed","text":"<pre><code>speed?: number;\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"react-native/Coords/#speed_accuracy","title":"speed_accuracy","text":"<pre><code>speed_accuracy?: number;\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"react-native/CurrentPositionRequest/","title":"CurrentPositionRequest","text":"<pre><code>export interface CurrentPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.getCurrentPosition.</p> <p>All fields are optional. The SDK always requests location from the native API at maximum accuracy (DesiredAccuracy.High), regardless of the <code>desiredAccuracy</code> threshold set here.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // seconds before giving up\n  maximumAge: 5000,     // accept a cached fix up to 5000 ms old\n  desiredAccuracy: 10,  // stop sampling when accuracy \u2264 10 m\n  samples: 3,           // take up to 3 samples and pick the best\n  extras: {\n    route_id: 123\n  }\n});\n</code></pre>"},{"location":"react-native/CurrentPositionRequest/#members","title":"Members","text":""},{"location":"react-native/CurrentPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<pre><code>desiredAccuracy?: number;\n</code></pre> <p>Accuracy threshold in meters. The SDK stops sampling as soon as it receives a location with <code>accuracy \u2264 desiredAccuracy</code> and returns it immediately. Defaults to GeoConfig.stationaryRadius.</p> <p>Note</p> <p>This value is a stopping threshold, not a hardware accuracy setting. The SDK always requests locations at DesiredAccuracy.High from the native API regardless of this value.</p>"},{"location":"react-native/CurrentPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to the returned location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"react-native/CurrentPositionRequest/#maximumage","title":"maximumAge","text":"<pre><code>maximumAge?: number;\n</code></pre> <p>Accept the most recently recorded location if it is no older than this value in milliseconds. Default <code>0</code> (always fetch a fresh fix).</p>"},{"location":"react-native/CurrentPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist the returned location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"react-native/CurrentPositionRequest/#samples","title":"samples","text":"<pre><code>samples?: number;\n</code></pre> <p>Maximum number of location samples to collect before returning the most accurate result. Default <code>3</code>. Only the final selected location is persisted.</p>"},{"location":"react-native/CurrentPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in seconds to wait for a location fix. Default <code>30</code>.</p> <p>If the timeout expires before a satisfactory location is found, the Promise rejects with a LocationError.</p>"},{"location":"react-native/DesiredAccuracy/","title":"DesiredAccuracy","text":"<pre><code>export const DesiredAccuracy = {\n</code></pre> <p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"react-native/DesiredAccuracy/#members","title":"Members","text":""},{"location":"react-native/DesiredAccuracy/#high","title":"High","text":"<p>High accuracy (e.g., 0-10m).</p>"},{"location":"react-native/DesiredAccuracy/#low","title":"Low","text":"<p>Low accuracy (e.g., 100m).</p>"},{"location":"react-native/DesiredAccuracy/#lowest","title":"Lowest","text":"<p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"react-native/DesiredAccuracy/#medium","title":"Medium","text":"<p>Medium accuracy (e.g., 10m).</p>"},{"location":"react-native/DesiredAccuracy/#navigation","title":"Navigation","text":"<p>Navigation-level accuracy (e.g., 0-10m).</p>"},{"location":"react-native/DesiredAccuracy/#verylow","title":"VeryLow","text":"<p>Very low accuracy (e.g., 1000m).</p>"},{"location":"react-native/DeviceInfo/","title":"DeviceInfo","text":"<pre><code>export interface DeviceInfo {\n</code></pre> <p>Basic device hardware and OS information returned by BackgroundGeolocation.getDeviceInfo.</p> <pre><code>const info = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(`[DeviceInfo] ${info.manufacturer} ${info.model} (${info.platform})`);\n</code></pre>"},{"location":"react-native/DeviceInfo/#members","title":"Members","text":""},{"location":"react-native/DeviceInfo/#framework","title":"framework","text":"<pre><code>framework: string;\n</code></pre> <p>Development framework hosting the SDK.</p> <p>Examples: <code>\"react-native\"</code>, <code>\"capacitor\"</code>, <code>\"cordova\"</code>, <code>\"flutter\"</code></p>"},{"location":"react-native/DeviceInfo/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer.</p> <p>Examples: <code>\"Apple\"</code>, <code>\"Google\"</code>, <code>\"Samsung\"</code>, <code>\"Huawei\"</code></p>"},{"location":"react-native/DeviceInfo/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model identifier.</p> <p>Examples: <code>\"iPhone15,2\"</code>, <code>\"Pixel 8 Pro\"</code>, <code>\"SM-G991B\"</code></p>"},{"location":"react-native/DeviceInfo/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"iOS\"</code> or <code>\"Android\"</code>.</p>"},{"location":"react-native/DeviceInfo/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>OS version string.</p> <p>Examples: <code>\"18.1\"</code>, <code>\"14\"</code>, <code>\"14.1.1\"</code></p>"},{"location":"react-native/DeviceSettings/","title":"DeviceSettings Android only","text":"<pre><code>export interface DeviceSettings {\n</code></pre> <p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>const isIgnoring = await BackgroundGeolocation.deviceSettings.isIgnoringBatteryOptimizations();\nif (!isIgnoring) {\n  const req = await BackgroundGeolocation.deviceSettings.showIgnoreBatteryOptimizations();\n  if (!req.seen) {\n    const confirmed = await showMyConfirmDialog({\n      title: 'Settings request',\n      text: 'Please disable battery optimizations for this app'\n    });\n    if (confirmed) {\n      await BackgroundGeolocation.deviceSettings.show(req);\n    }\n  }\n}\n</code></pre>"},{"location":"react-native/DeviceSettings/#members","title":"Members","text":""},{"location":"react-native/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>isIgnoringBatteryOptimizations(): Promise&lt;boolean&gt;;\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p>"},{"location":"react-native/DeviceSettings/#show","title":"show","text":"<p><code>show(request:</code><code>DeviceSettingsRequest</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>const req = await BackgroundGeolocation.deviceSettings.showPowerManager();\nif (!req.seen) {\n  await BackgroundGeolocation.deviceSettings.show(req);\n}\n</code></pre>"},{"location":"react-native/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<p><code>showIgnoreBatteryOptimizations(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p>"},{"location":"react-native/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<p><code>showPowerManager(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p>"},{"location":"react-native/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<pre><code>export interface DeviceSettingsRequest {\n</code></pre> <p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"react-native/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"react-native/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>action: string;\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"react-native/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>lastSeenAt: Date;\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"react-native/DeviceSettingsRequest/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer (e.g. <code>\"Huawei\"</code>, <code>\"Samsung\"</code>).</p>"},{"location":"react-native/DeviceSettingsRequest/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model name (e.g. <code>\"P40\"</code>, <code>\"SM-G991B\"</code>).</p>"},{"location":"react-native/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>seen: boolean;\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"react-native/DeviceSettingsRequest/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>Android OS version string.</p>"},{"location":"react-native/Event/","title":"Event","text":"<pre><code>export const Event = {\n</code></pre> <p>Supported event names emitted by the SDK.</p>"},{"location":"react-native/Event/#members","title":"Members","text":""},{"location":"react-native/Event/#activitychange","title":"ActivityChange","text":"<p>Emitted when a motion-activity change is detected.</p>"},{"location":"react-native/Event/#authorization","title":"Authorization","text":"<p>Emitted when the plugin's authorization state changes.</p>"},{"location":"react-native/Event/#boot","title":"Boot","text":"<p>Emitted when the plugin boots.</p>"},{"location":"react-native/Event/#connectivitychange","title":"ConnectivityChange","text":"<p>Emitted when the device's network connectivity changes.</p>"},{"location":"react-native/Event/#enabledchange","title":"EnabledChange","text":"<p>Emitted when the plugin's enabled-state changes.</p>"},{"location":"react-native/Event/#geofence","title":"Geofence","text":"<p>Emitted when a geofence transition occurs.</p>"},{"location":"react-native/Event/#geofenceschange","title":"GeofencesChange","text":"<p>Emitted when geofences are added/removed/activated/deactivated.</p>"},{"location":"react-native/Event/#heartbeat","title":"Heartbeat","text":"<p>Emitted at the configured <code>Config.heartbeatInterval</code> when the plugin is running.</p>"},{"location":"react-native/Event/#http","title":"Http","text":"<p>Emitted when an HTTP request is sent to the configured <code>Config.http.url</code> endpoint.</p>"},{"location":"react-native/Event/#location","title":"Location","text":"<p>Emitted when a location is recorded.</p>"},{"location":"react-native/Event/#motionchange","title":"MotionChange","text":"<p>Emitted when a motion-change is detected.</p>"},{"location":"react-native/Event/#notification","title":"Notification","text":"<p>Emitted when a user interacts with a plugin-generated notification.</p>"},{"location":"react-native/Event/#powersavechange","title":"PowerSaveChange","text":"<p>Emitted when the device enters or exits power-saving mode.</p>"},{"location":"react-native/Event/#providerchange","title":"ProviderChange","text":"<p>Emitted when the device location-provider changes state.</p>"},{"location":"react-native/Event/#schedule","title":"Schedule","text":"<p>Emitted when a configured schedule is triggered.</p>"},{"location":"react-native/Event/#terminate","title":"Terminate","text":"<p>Emitted when the plugin is terminated.</p>"},{"location":"react-native/GeoConfig/","title":"GeoConfig","text":"<pre><code>export interface GeoConfig {\n</code></pre> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"react-native/GeoConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"react-native/GeoConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BackgroundGeolocation.ready or BackgroundGeolocation.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    locationAuthorizationRequest: \"Always\"\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"react-native/GeoConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"react-native/GeoConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BackgroundGeolocation.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"react-native/GeoConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BackgroundGeolocation.onProviderChange.</p>"},{"location":"react-native/GeoConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"react-native/GeoConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilter for full documentation.</p>"},{"location":"react-native/GeoConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/GeoConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/GeoConfig/#members","title":"Members","text":""},{"location":"react-native/GeoConfig/#activitytype","title":"activityType","text":"<p><code>activityType?:</code><code>ActivityType</code><code>;</code></p> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on ActivityType.</p> Name ActivityType.Other ActivityType.AutomotiveNavigation ActivityType.Fitness ActivityType.OtherNavigation ActivityType.Airborne <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    activityType: ActivityType.Other,\n  },\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>allowIdenticalLocations?: boolean;\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"react-native/GeoConfig/#defertime","title":"deferTime","text":"<pre><code>deferTime?: number;\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"react-native/GeoConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>disableElasticity?: boolean;\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"react-native/GeoConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>disableLocationAuthorizationAlert?: boolean;\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BackgroundGeolocation.onProviderChange.</p>"},{"location":"react-native/GeoConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"react-native/GeoConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BackgroundGeolocation.onProviderChange - BackgroundGeolocation.start - BackgroundGeolocation.requestPermission</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange] \", event);\n\n  if (!provider.enabled) {\n    alert(\"Please enable location services\");\n  }\n});\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    disableLocationAuthorizationAlert: true\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity\u2013based stop-detection system.</p> <p>When <code>true</code>, the SDK ignores platform motion-activity signals when determining whether the device is stationary. This affects how and when location services are automatically turned off on both platforms.</p>"},{"location":"react-native/GeoConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. iOS location services then turn off automatically after exactly 15 minutes of no motion \u2014 you lose control over <code>stopTimeout</code>.</p> <p>To prevent iOS from ever automatically disabling location services, also set</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    disableStopDetection: true,\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With <code>disableStopDetection: true</code> and <code>pausesLocationUpdatesAutomatically: false</code>, iOS location services never turn off. This can severely drain the battery. Only use this configuration if you fully control tracking manually (for example, a workout app toggling tracking via BackgroundGeolocation.changePace).</p> <p>iOS stop-detection timing:</p> <p></p>"},{"location":"react-native/GeoConfig/#android_1","title":"Android","text":"<p>When <code>true</code>, Android location services never turn off automatically. You must disable tracking manually by calling BackgroundGeolocation.changePace with <code>false</code>, or BackgroundGeolocation.stop.</p>"},{"location":"react-native/GeoConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>distanceFilter?: number;\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"react-native/GeoConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"react-native/GeoConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"react-native/GeoConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>elasticityMultiplier?: number;\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"react-native/GeoConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>enableTimestampMeta?: boolean;\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS Location.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"react-native/GeoConfig/#android_2","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"react-native/GeoConfig/#ios_2","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"react-native/GeoConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>fastestLocationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    fastestLocationUpdateInterval: 5000 // Receive passive updates at most every 5 s\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"react-native/GeoConfig/#filter","title":"filter","text":"<p><code>filter?:</code><code>LocationFilter</code><code>;</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"react-native/GeoConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilter.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilter.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilter.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilter.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilter.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilter.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilter.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilter.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilter.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilter.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilter.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilter.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>geofenceInitialTriggerEntry?: boolean;\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"react-native/GeoConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>geofenceModeHighAccuracy?: boolean;\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BackgroundGeolocation.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeoConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeoConfig.locationUpdateInterval - GeoConfig.distanceFilter - GeoConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 50,\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>geofenceProximityRadius?: number;\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p>"},{"location":"react-native/GeoConfig/#ios_3","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"react-native/GeoConfig/#android_3","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"react-native/GeoConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>locationAuthorizationAlert?: Record&lt;string, any&gt;;\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationAlert: {\n      titleWhenNotEnabled: \"Location services are not enabled\",\n      titleWhenOff: \"Location services are OFF\",\n      instructions:\n        \"Please enable 'Always' in Location Services to allow background tracking.\",\n      cancelButton: \"Cancel\",\n      settingsButton: \"Open Settings\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<pre><code>locationAuthorizationRequest?: \"Always\" | \"WhenInUse\" | \"Any\";\n</code></pre> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"react-native/GeoConfig/#ios_4","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Start with When-In-Use\nBackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"WhenInUse\" }\n});\n\nasync function onClickStartTracking() {\n  await BackgroundGeolocation.start();\n\n  // Upgrade to Always at any time.\n  BackgroundGeolocation.setConfig({\n    geolocation: { locationAuthorizationRequest: \"Always\" }\n  });\n}\n</code></pre>"},{"location":"react-native/GeoConfig/#android_4","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"Always\" },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow access to this device's location in the background?\",\n      message: \"To track your trips, please enable 'Allow all the time' location permission.\",\n      positiveAction: \"Change to Allow all the time\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"react-native/GeoConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>locationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    distanceFilter: 0,           // Required \u2014 otherwise this field is ignored.\n    locationUpdateInterval: 5000 // Request a fix every ~5 seconds\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"react-native/GeoConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>pausesLocationUpdatesAutomatically?: boolean;\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BackgroundGeolocation.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"react-native/GeoConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>showsBackgroundLocationIndicator?: boolean;\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"react-native/GeoConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>stationaryRadius?: number;\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"react-native/GeoConfig/#ios_5","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"react-native/GeoConfig/#android_5","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"react-native/GeoConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>stopAfterElapsedMinutes?: number;\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BackgroundGeolocation.stop after this many minutes have elapsed since BackgroundGeolocation.start was called.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopAfterElapsedMinutes: 30\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.start();  // SDK will automatically stop in 30 minutes\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the <code>stopTimeout</code> elapses.</p> <p>When <code>true</code>, the SDK stops itself the next time BackgroundGeolocation.onMotionChange fires into the stationary state after the <code>stopTimeout</code> timer elapses.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the SDK transitions to the stationary state due to <code>stopTimeout</code> expiry. It does not fire when you manually call BackgroundGeolocation.changePace with <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopOnStationary: true,\n  },\n  app: {\n    isMoving: true,\n  }\n}, (state) =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"react-native/GeoConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>stopTimeout?: number;\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BackgroundGeolocation.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"react-native/GeoConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>useSignificantChangesOnly?: boolean;\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"react-native/GeoConfig/#ios_6","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"react-native/GeoConfig/#android_6","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"react-native/Geofence/","title":"Geofence","text":"<pre><code>export interface Geofence {\n</code></pre> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"react-native/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"react-native/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeoConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"react-native/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BackgroundGeolocation.addGeofence for a single geofence, or BackgroundGeolocation.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"react-native/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BackgroundGeolocation.onGeofence. Subscribe to changes in the actively monitored set with BackgroundGeolocation.onGeofencesChange.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event.identifier, event.action);\n});\n\nBackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  on.forEach((geofence) =&gt; createGeofenceMarker(geofence));\n  off.forEach((identifier) =&gt; removeGeofenceMarker(identifier));\n});\n</code></pre> <p>Note</p> <p>When all geofences are removed, BackgroundGeolocation.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"react-native/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"react-native/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeoConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeoConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"react-native/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre> <pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"react-native/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BackgroundGeolocation.startGeofences instead of BackgroundGeolocation.start to monitor geofences without continuous location tracking. Use GeoConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/Geofence/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre>"},{"location":"react-native/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"react-native/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"react-native/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre>"},{"location":"react-native/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"react-native/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"react-native/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"react-native/Geofence/#members","title":"Members","text":""},{"location":"react-native/Geofence/#entrystate","title":"entryState","text":"<p><code>entry</code><code>State</code><code>?: number;</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"react-native/Geofence/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, unknown&gt;;\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"react-native/Geofence/#hits","title":"hits","text":"<pre><code>hits?: number;\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"react-native/Geofence/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"react-native/Geofence/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"react-native/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>loiteringDelay?: number;\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"react-native/Geofence/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"react-native/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>notifyOnDwell?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"react-native/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>notifyOnEntry?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeoConfig.geofenceInitialTriggerEntry</p>"},{"location":"react-native/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>notifyOnExit?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"react-native/Geofence/#radius","title":"radius","text":"<pre><code>radius: number;\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"react-native/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>stateUpdatedAt?: number;\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"react-native/Geofence/#vertices","title":"vertices","text":"<p><code>vertices?:</code><code>Vertices</code><code>;</code></p> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre> <p> </p>"},{"location":"react-native/GeofenceAction/","title":"GeofenceAction","text":"<pre><code>export const GeofenceAction = {\n</code></pre> <p>Geofence transition actions emitted by the SDK</p> <p>Used with GeofenceEvent from BackgroundGeolocation.onGeofence.</p> Action Meaning ENTER Device entered the geofence EXIT Device exited the geofence DWELL Device dwelled inside"},{"location":"react-native/GeofenceAction/#members","title":"Members","text":""},{"location":"react-native/GeofenceAction/#dwell","title":"Dwell","text":"<pre><code>Dwell: 'DWELL'\n</code></pre> <p>Device dwelled inside the geofence for at least the configured <code>Config.geofenceProximityRadius</code>.</p>"},{"location":"react-native/GeofenceAction/#enter","title":"Enter","text":"<pre><code>Enter: 'ENTER',\n</code></pre> <p>Device entered the geofence.</p>"},{"location":"react-native/GeofenceAction/#exit","title":"Exit","text":"<pre><code>Exit: 'EXIT',\n</code></pre> <p>Device exited the geofence.</p>"},{"location":"react-native/GeofenceEvent/","title":"GeofenceEvent","text":"<pre><code>export interface GeofenceEvent {\n</code></pre> <p>Geofence transition delivered to BackgroundGeolocation.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\n    \"[onGeofence]\",\n    event.identifier,\n    event.action,\n    event.location\n  );\n});\n</code></pre>"},{"location":"react-native/GeofenceEvent/#members","title":"Members","text":""},{"location":"react-native/GeofenceEvent/#action","title":"action","text":"<p><code>action:</code><code>GeofenceAction</code><code>;</code></p> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"react-native/GeofenceEvent/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"react-native/GeofenceEvent/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>Geofence</code><code>;</code></p> <p>The complete Geofence record that triggered this event.</p>"},{"location":"react-native/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"react-native/GeofenceEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"react-native/GeofenceEvent/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string;\n</code></pre> <p>ISO-8601 UTC timestamp from the device system clock at the moment the OS delivered the geofence event.</p>"},{"location":"react-native/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<pre><code>export interface GeofencesChangeEvent {\n</code></pre> <p>Geofence monitoring change delivered to BackgroundGeolocation.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  for (const geofence of event.on) {\n    console.log(\"[onGeofencesChange] Activated:\", geofence.identifier);\n  }\n  for (const identifier of event.off) {\n    console.log(\"[onGeofencesChange] Deactivated:\", identifier);\n  }\n});\n</code></pre>"},{"location":"react-native/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"react-native/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>off: string[];\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"react-native/GeofencesChangeEvent/#on","title":"on","text":"<p><code>on:</code><code>Geofence</code><code>[];</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"react-native/HeadlessEvent/","title":"HeadlessEvent Android only","text":"<pre><code>export interface HeadlessEvent {\n</code></pre> <p>Event delivered to a headless task registered via BackgroundGeolocation.registerHeadlessTask.</p> <p>Headless tasks allow the app to handle SDK events while running in the background without a UI \u2014 including after the app has been terminated by the user. Each task invocation receives a <code>HeadlessEvent</code> describing which SDK event occurred and carrying the associated payload.</p> <pre><code>const headlessTask = async (event: HeadlessEvent) =&gt; {\n  const { name, params } = event;\n  console.log(\"[HeadlessTask]\", name, params);\n\n  if (name === \"location\") {\n    const location = params as Location;\n    console.log(\"[HeadlessTask] location:\", location);\n  } else if (name === \"http\") {\n    const httpEvent = params as HttpEvent;\n    console.log(\"[HeadlessTask] http status:\", httpEvent.status);\n  }\n};\n\nBackgroundGeolocation.registerHeadlessTask(headlessTask);\n</code></pre>"},{"location":"react-native/HeadlessEvent/#members","title":"Members","text":""},{"location":"react-native/HeadlessEvent/#name","title":"name","text":"<p><code>name:</code><code>Event</code><code>;</code></p> <p>The SDK event that triggered this headless task invocation (e.g. <code>location</code>, <code>http</code>, <code>geofence</code>, <code>heartbeat</code>, <code>terminate</code>).</p>"},{"location":"react-native/HeadlessEvent/#params","title":"params","text":"<pre><code>params: Record&lt;string, any&gt;;\n</code></pre> <p>Event payload corresponding to <code>name</code>. Cast this to the appropriate event type based on the value of <code>name</code> (e.g. Location for <code>location</code>, HttpEvent for <code>http</code>).</p>"},{"location":"react-native/HeartbeatEvent/","title":"HeartbeatEvent","text":"<pre><code>export interface HeartbeatEvent {\n</code></pre> <p>Periodic heartbeat delivered to BackgroundGeolocation.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>BackgroundGeolocation.onHeartbeat(async (event) =&gt; {\n  console.log(\"[onHeartbeat]\", event.location);\n\n  // Request a fresh location fix if needed.\n  const location = await BackgroundGeolocation.getCurrentPosition({\n    samples: 1\n  });\n  console.log(\"[onHeartbeat] fresh position:\", location);\n});\n</code></pre>"},{"location":"react-native/HeartbeatEvent/#members","title":"Members","text":""},{"location":"react-native/HeartbeatEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BackgroundGeolocation.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"react-native/HttpConfig/","title":"HttpConfig","text":"<pre><code>export interface HttpConfig {\n</code></pre> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"react-native/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"react-native/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    params: { user_id: 1234 }\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BackgroundGeolocation.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BackgroundGeolocation.getCount or fetch records with BackgroundGeolocation.getLocations.</p>"},{"location":"react-native/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"react-native/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"react-native/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BackgroundGeolocation.onConnectivityChange fires - BackgroundGeolocation.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p> <pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"react-native/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BackgroundGeolocation.start <code>\"stop\"</code> \u2014 BackgroundGeolocation.stop <code>\"startGeofences\"</code> \u2014 BackgroundGeolocation.startGeofences <code>\"changePace\"</code> <code>boolean</code> BackgroundGeolocation.changePace <code>\"setConfig\"</code> <code>{Config}</code> BackgroundGeolocation.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BackgroundGeolocation.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BackgroundGeolocation.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BackgroundGeolocation.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"react-native/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p>"},{"location":"react-native/HttpConfig/#the-sdk-log-provides-a-trace-of-the-full-http-lifecycle","title":"The SDK log provides a trace of the full HTTP lifecycle","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"react-native/HttpConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"react-native/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"react-native/HttpConfig/#http-lifecycle-log","title":"HTTP lifecycle log","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre>"},{"location":"react-native/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer ...\" }\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"react-native/HttpConfig/#members","title":"Members","text":""},{"location":"react-native/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>autoSync?: boolean;\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BackgroundGeolocation.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BackgroundGeolocation.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"react-native/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>autoSyncThreshold?: number;\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BackgroundGeolocation.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"react-native/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>batchSync?: boolean;\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"react-native/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>disableAutoSyncOnCellular?: boolean;\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BackgroundGeolocation.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BackgroundGeolocation.sync</p>"},{"location":"react-native/HttpConfig/#headers","title":"headers","text":"<pre><code>headers?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my.server.com\",\n    headers: {\n      Authorization: \"Bearer &lt;a secret key&gt;\",\n      \"X-FOO\": \"BAR\"\n    }\n  }\n});\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"react-native/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>maxBatchSize?: number;\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"react-native/HttpConfig/#method","title":"method","text":"<p><code>method?:</code><code>HttpMethod</code><code>;</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    method: \"PUT\"\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#params","title":"params","text":"<pre><code>params?: Record&lt;string, any&gt;;\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      user_id: 1234,\n      device_id: \"abc123\"\n    }\n  }\n});\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"react-native/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>rootProperty?: string;\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    rootProperty: \"myData\",\n    url: \"https://my.server.com\",\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"react-native/HttpConfig/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BackgroundGeolocation.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>BackgroundGeolocation.onHttp((response) =&gt; {\n  if (!response.success) {\n    console.log(\"[onHttp] FAILURE:\", response);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    timeout: 3000\n  }\n});\n</code></pre>"},{"location":"react-native/HttpConfig/#url","title":"url","text":"<pre><code>url?: string;\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  console.log(\"[onHttp]\", event);\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: { user_id: 1234 },\n    headers: { Authorization: \"Basic my-secret-key\" },\n    autoSync: true,\n    method: \"POST\"\n  }\n});\n</code></pre>"},{"location":"react-native/HttpEvent/","title":"HttpEvent","text":"<pre><code>export interface HttpEvent {\n</code></pre> <p>HTTP response payload delivered to BackgroundGeolocation.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onHttp] Upload succeeded:\", event.status);\n  } else {\n    console.warn(\"[onHttp] Upload failed:\", event.status, event.responseText);\n  }\n});\n</code></pre>"},{"location":"react-native/HttpEvent/#members","title":"Members","text":""},{"location":"react-native/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>responseText: string;\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"react-native/HttpEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"react-native/HttpEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"react-native/HttpMethod/","title":"HttpMethod","text":"<pre><code>export const HttpMethod = {\n</code></pre> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"react-native/HttpMethod/#members","title":"Members","text":""},{"location":"react-native/HttpMethod/#patch","title":"Patch","text":"<p>HTTP PATCH method.</p>"},{"location":"react-native/HttpMethod/#post","title":"Post","text":"<p>HTTP POST method.</p>"},{"location":"react-native/HttpMethod/#put","title":"Put","text":"<p>HTTP PUT method.</p>"},{"location":"react-native/KalmanProfile/","title":"KalmanProfile","text":"<pre><code>export const KalmanProfile = {\n</code></pre> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"react-native/KalmanProfile/#members","title":"Members","text":""},{"location":"react-native/KalmanProfile/#aggressive","title":"Aggressive","text":"<p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"react-native/KalmanProfile/#conservative","title":"Conservative","text":"<p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"react-native/KalmanProfile/#default","title":"Default","text":"<p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"react-native/Location/","title":"Location","text":"<pre><code>export interface Location {\n</code></pre> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"react-native/Location/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"react-native/Location/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"react-native/Location/#javascript-schema","title":"JavaScript schema","text":"<pre><code>{\n   \"timestamp\":     [Date],     // &lt;-- Javascript Date instance\n   \"event\":         [String],   // &lt;-- motionchange|geofence|heartbeat\n   \"is_moving\":     [Boolean],  // &lt;-- The motion-state when location was recorded.\n   \"uuid\":          [String],   // &lt;-- Universally unique identifier\n   \"age\":           [Integer],  // &lt;-- Age of the location in milliseconds\n   \"coords\": {\n       \"latitude\":  [Double],\n       \"longitude\": [Double],\n       \"accuracy\":  [Double],\n       \"speed\":     [Double],\n       \"heading\":   [Double],\n       \"altitude\":  [Double]\n       \"ellipsoidal_altitude\":  [Double]\n   },\n   \"activity\": {\n       \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle],\n       \"confidence\": [0-100%]\n   },\n   \"battery\": {\n       \"level\": [Double],\n       \"is_charging\": [Boolean]\n   },\n   \"odometer\": [Double/meters]\n}\n</code></pre>"},{"location":"react-native/Location/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>{\n    \"location\": {\n        \"coords\": {\n            \"latitude\":   [Double],\n            \"longitude\":  [Double],\n            \"accuracy\":   [Double],\n            \"speed\":      [Double],\n            \"heading\":    [Double],\n            \"altitude\":   [Double],\n            \"ellipsoidal_altitude\": [Double]\n        },\n        \"extras\": {   // &lt;-- optional meta-data\n            \"foo\": \"bar\"\n        },\n        \"activity\": {\n            \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle|unknown],\n            \"confidence\": [0-100%]\n        },\n        \"geofence\": {  // &lt;-- Present only if a geofence was triggered at this location\n            \"identifier\": [String],\n            \"action\": [String ENTER|EXIT]\n        },\n        \"battery\": {\n            \"level\": [Double],\n            \"is_charging\": [Boolean]\n        },\n        \"timestamp\": [ISO-8601 UTC], // eg:  \"2015-05-05T04:31:54.123Z\"\n        \"age\":       [Integer],      // &lt;-- Age of the location in milliseconds\n        \"uuid\":      [String],       // &lt;-- Universally unique identifier\n        \"event\"      [String],       // &lt;-- motionchange|geofence|heartbeat\n        \"is_moving\": [Boolean],      // &lt;-- The motion-state when recorded.\n        \"odometer\": [Double/meters]\n    }\n }\n</code></pre>"},{"location":"react-native/Location/#members","title":"Members","text":""},{"location":"react-native/Location/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivity</code><code>;</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"react-native/Location/#age","title":"age","text":"<pre><code>age: number;\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"react-native/Location/#battery","title":"battery","text":"<p><code>battery:</code><code>Battery</code><code>;</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"react-native/Location/#coords","title":"coords","text":"<p><code>coords:</code><code>Coords</code><code>;</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"react-native/Location/#event","title":"event","text":"<pre><code>event?: string;\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"react-native/Location/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"react-native/Location/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>GeofenceEvent</code><code>;</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"react-native/Location/#is_moving","title":"is_moving","text":"<pre><code>is_moving: boolean;\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"react-native/Location/#mock","title":"mock","text":"<pre><code>mock?: boolean;\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"react-native/Location/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"react-native/Location/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilter evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilter.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via Location.odometer_error.</li> </ul>"},{"location":"react-native/Location/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"react-native/Location/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilter - LocationFilter.odometerAccuracyThreshold - BackgroundGeolocation.resetOdometer - BackgroundGeolocation.getOdometer</p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Distance traveled:\", location.odometer);\n});\n\n// Reset at the start of a trip\nawait BackgroundGeolocation.resetOdometer();\n</code></pre>"},{"location":"react-native/Location/#odometer_error","title":"odometer_error","text":"<pre><code>odometer_error: number;\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"react-native/Location/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Odometer:\", location.odometer);\n  if (location.odometer_error &gt; 50) {\n    console.warn(\"High odometer drift \u2014 signal quality degraded\");\n  }\n});\n</code></pre>"},{"location":"react-native/Location/#provider","title":"provider","text":"<p><code>provider?:</code><code>ProviderChangeEvent</code><code>;</code></p> <p>Location-services provider state at the time this location was recorded.</p> <p>Present only when <code>event</code> is <code>\"providerchange\"</code>.</p>"},{"location":"react-native/Location/#recorded_at","title":"recorded_at","text":"<pre><code>recorded_at: string | number;\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"react-native/Location/#sample","title":"sample","text":"<pre><code>sample?: boolean;\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BackgroundGeolocation.getCurrentPosition to find the most accurate fix. These samples are delivered to BackgroundGeolocation.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"react-native/Location/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string | number;\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"react-native/Location/#uuid","title":"uuid","text":"<pre><code>uuid: string;\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"react-native/LocationError/","title":"LocationError","text":"<pre><code>export const LocationError = {\n</code></pre> <p>Error codes returned when the native location API fails to fetch a location.</p> Code Error <code>0</code> Location unknown <code>1</code> Location permission denied <code>2</code> Network error <code>3</code> Attempt to initiate location-services in background with WhenInUse authorization <code>408</code> Location timeout <code>499</code> Location request cancelled"},{"location":"react-native/LocationError/#members","title":"Members","text":""},{"location":"react-native/LocationError/#backgroundwheninuse","title":"BackgroundWhenInUse","text":"<pre><code>BackgroundWhenInUse: 3,\n</code></pre> <p>Attempt to initiate location-services in background with WhenInUse authorization.</p>"},{"location":"react-native/LocationError/#cancelled","title":"Cancelled","text":"<pre><code>Cancelled: 499,\n</code></pre> <p>Location request was cancelled.</p>"},{"location":"react-native/LocationError/#locationunknown","title":"LocationUnknown","text":"<pre><code>LocationUnknown: 0,\n</code></pre> <p>Location could not be determined at this time.</p>"},{"location":"react-native/LocationError/#networkerror","title":"NetworkError","text":"<pre><code>NetworkError: 2,\n</code></pre> <p>Network error occurred while attempting to fetch location.</p>"},{"location":"react-native/LocationError/#permissiondenied","title":"PermissionDenied","text":"<pre><code>PermissionDenied: 1,\n</code></pre> <p>Location permission denied by the user.</p>"},{"location":"react-native/LocationError/#timeout","title":"Timeout","text":"<pre><code>Timeout: 408,\n</code></pre> <p>Location request timed out.</p>"},{"location":"react-native/LocationFilter/","title":"LocationFilter","text":"<pre><code>export interface LocationFilter {\n</code></pre> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"react-native/LocationFilter/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"react-native/LocationFilter/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#members","title":"Members","text":""},{"location":"react-native/LocationFilter/#burstwindow","title":"burstWindow","text":"<pre><code>burstWindow?: number;\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"react-native/LocationFilter/#filterdebug","title":"filterDebug","text":"<pre><code>filterDebug?: boolean;\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"react-native/LocationFilter/#kalmandebug","title":"kalmanDebug","text":"<pre><code>kalmanDebug?: boolean;\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"react-native/LocationFilter/#kalmanprofile","title":"kalmanProfile","text":"<p><code>kalmanProfile?:</code><code>KalmanProfile</code><code>;</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"react-native/LocationFilter/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>maxBurstDistance?: number;\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"react-native/LocationFilter/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>maxImpliedSpeed?: number;\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"react-native/LocationFilter/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>odometerAccuracyThreshold?: number;\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"react-native/LocationFilter/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>odometerUseKalmanFilter?: boolean;\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilter.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilter.useKalman and LocationFilter.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"react-native/LocationFilter/#policy","title":"policy","text":"<p><code>policy?:</code><code>LocationFilterPolicy</code><code>;</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BackgroundGeolocation.onLocation. For more granular tuning, see LocationFilter.trackingAccuracyThreshold, LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</p> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilter/#rollingwindow","title":"rollingWindow","text":"<pre><code>rollingWindow?: number;\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"react-native/LocationFilter/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>trackingAccuracyThreshold?: number;\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"react-native/LocationFilter/#usekalman","title":"useKalman","text":"<pre><code>useKalman?: boolean;\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"react-native/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<pre><code>export const LocationFilterPolicy = {\n</code></pre> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilter.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BackgroundGeolocation.onLocation.</li> <li>For more granular tuning, see LocationFilter.trackingAccuracyThreshold,   LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</li> </ul> <p>Examples</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p>"},{"location":"react-native/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"react-native/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilter.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"react-native/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"react-native/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"react-native/LocationRequest/","title":"LocationRequest","text":"<pre><code>export const LocationRequest = {\n</code></pre> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeoConfig.locationAuthorizationRequest</p>"},{"location":"react-native/LocationRequest/#members","title":"Members","text":""},{"location":"react-native/LocationRequest/#always","title":"Always","text":"<p>Request full background + foreground authorization.</p>"},{"location":"react-native/LocationRequest/#any","title":"Any","text":"<p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"react-native/LocationRequest/#wheninuse","title":"WhenInUse","text":"<p>Request foreground-only authorization.</p>"},{"location":"react-native/LogLevel/","title":"LogLevel","text":"<pre><code>export const LogLevel = {\n</code></pre> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"react-native/LogLevel/#members","title":"Members","text":""},{"location":"react-native/LogLevel/#debug","title":"Debug","text":"<p>Developer-level debug output.</p>"},{"location":"react-native/LogLevel/#error","title":"Error","text":"<p>Log only critical failures.</p>"},{"location":"react-native/LogLevel/#info","title":"Info","text":"<p>Operational information.</p>"},{"location":"react-native/LogLevel/#off","title":"Off","text":"<p>Disable all logging.</p>"},{"location":"react-native/LogLevel/#verbose","title":"Verbose","text":"<p>Maximum detail.</p>"},{"location":"react-native/LogLevel/#warning","title":"Warning","text":"<p>Log warnings + errors.</p>"},{"location":"react-native/Logger/","title":"Logger","text":"<pre><code>export interface Logger {\n</code></pre> <p>SDK logging API \u2014 access via BackgroundGeolocation.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"react-native/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"react-native/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\n</code></pre>"},{"location":"react-native/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional SQLQuery to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"react-native/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>const Logger = BackgroundGeolocation.logger;\nBackgroundGeolocation.onLocation((location) =&gt; {\n  Logger.debug(\"Location received: \" + location.uuid);\n});\n</code></pre>"},{"location":"react-native/Logger/#examples","title":"Examples","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"react-native/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre>"},{"location":"react-native/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre>"},{"location":"react-native/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"react-native/Logger/#methods","title":"Methods","text":""},{"location":"react-native/Logger/#debug","title":"debug","text":"<pre><code>debug(message: string): void;\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.debug(\"This is a debug message\");\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"react-native/Logger/#destroylog","title":"destroyLog","text":"<pre><code>destroyLog(): Promise&lt;void&gt;;\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.destroyLog();\n</code></pre>"},{"location":"react-native/Logger/#emaillog","title":"emailLog","text":"<p><code>emailLog(email: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional SQLQuery to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"foo@bar.com\");\n\n// Constrain by date range\nawait Logger.emailLog(\"foo@bar.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC,\n  limit: 1000\n});\n</code></pre>"},{"location":"react-native/Logger/#error","title":"error","text":"<pre><code>error(message: string): void;\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.error(\"Something BAD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"react-native/Logger/#getlog","title":"getLog","text":"<p><code>getLog(query?:</code><code>SQLQuery</code><code>): Promise&lt;string&gt;;</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional SQLQuery to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Fetch all logs\nconst log = await BackgroundGeolocation.logger.getLog();\nconsole.log(\"[log]\", log);\n\n// Constrain by date range\nconst Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-09-19 11:12\"),\n  end:   Date.parse(\"2019-09-19 11:13\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre>"},{"location":"react-native/Logger/#info","title":"info","text":"<pre><code>info(message: string): void;\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.info(\"Something informative\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"react-native/Logger/#notice","title":"notice","text":"<pre><code>notice(message: string): void;\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.notice(\"A Notice\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"react-native/Logger/#uploadlog","title":"uploadLog","text":"<p><code>uploadLog(url: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional SQLQuery to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"react-native/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n\n// Constrain by date range\nawait BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\",\n  { start: Date.parse(\"2019-10-20 09:00\"), end: Date.parse(\"2019-10-20 11:59\") }\n);\n</code></pre>"},{"location":"react-native/Logger/#warn","title":"warn","text":"<pre><code>warn(message: string): void;\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.warn(\"Something WEIRD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"react-native/Logger/#properties","title":"Properties","text":""},{"location":"react-native/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>ORDER_ASC: 1;\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"react-native/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>ORDER_DESC: -1;\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"react-native/LoggerConfig/","title":"LoggerConfig","text":"<pre><code>export interface LoggerConfig {\n</code></pre> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"react-native/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"react-native/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"react-native/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"react-native/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"react-native/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"react-native/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"react-native/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"react-native/LoggerConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>Config({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#members","title":"Members","text":""},{"location":"react-native/LoggerConfig/#debug","title":"debug","text":"<pre><code>debug?: boolean;\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"react-native/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"react-native/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"react-native/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>logLevel?:</code><code>LogLevel</code><code>;</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  },\n});\n</code></pre>"},{"location":"react-native/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>logMaxDays?: number;\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logMaxDays: 3\n  }\n});\n</code></pre>"},{"location":"react-native/MotionActivity/","title":"MotionActivity","text":"<pre><code>export interface MotionActivity {\n</code></pre> <p>The motion activity reported by the device at the time a Location was recorded.</p>"},{"location":"react-native/MotionActivity/#members","title":"Members","text":""},{"location":"react-native/MotionActivity/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"react-native/MotionActivity/#type","title":"type","text":"<p><code>type:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"react-native/MotionActivityEvent/","title":"MotionActivityEvent","text":"<pre><code>export interface MotionActivityEvent {\n</code></pre> <p>Activity recognition result delivered to BackgroundGeolocation.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange]\", event.activity, event.confidence);\n});\n</code></pre>"},{"location":"react-native/MotionActivityEvent/#members","title":"Members","text":""},{"location":"react-native/MotionActivityEvent/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"react-native/MotionActivityEvent/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"react-native/MotionActivityType/","title":"MotionActivityType","text":"<pre><code>export const MotionActivityType = {\n</code></pre> <p>Reported device motion activity names (legacy RN + native).</p>"},{"location":"react-native/MotionActivityType/#members","title":"Members","text":""},{"location":"react-native/MotionActivityType/#invehicle","title":"InVehicle","text":"<pre><code>InVehicle: 'in_vehicle',\n</code></pre> <p>Device is in vehicle.</p>"},{"location":"react-native/MotionActivityType/#onbicycle","title":"OnBicycle","text":"<pre><code>OnBicycle: 'on_bicycle',\n</code></pre> <p>Device is on bicycle.</p>"},{"location":"react-native/MotionActivityType/#onfoot","title":"OnFoot","text":"<pre><code>OnFoot: 'on_foot',\n</code></pre> <p>Device is on foot (walking or running).</p>"},{"location":"react-native/MotionActivityType/#running","title":"Running","text":"<pre><code>Running: 'running',\n</code></pre> <p>Device is running.</p>"},{"location":"react-native/MotionActivityType/#still","title":"Still","text":"<pre><code>Still: 'still',\n</code></pre> <p>Device is stationary.</p>"},{"location":"react-native/MotionActivityType/#unknown","title":"Unknown","text":"<pre><code>Unknown: 'unknown'\n</code></pre> <p>Device activity is unknown.</p>"},{"location":"react-native/MotionActivityType/#walking","title":"Walking","text":"<pre><code>Walking: 'walking',\n</code></pre> <p>Device is walking.</p>"},{"location":"react-native/MotionChangeEvent/","title":"MotionChangeEvent","text":"<pre><code>export interface MotionChangeEvent {\n</code></pre> <p>Motion-state change delivered to BackgroundGeolocation.onMotionChange.</p> <p>The SDK fires this event each time it transitions between the moving and stationary states. A location is always recorded at the moment of transition.</p> <pre><code>BackgroundGeolocation.onMotionChange((event) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device is moving:\", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device is stationary:\", event.location);\n  }\n});\n</code></pre>"},{"location":"react-native/MotionChangeEvent/#members","title":"Members","text":""},{"location":"react-native/MotionChangeEvent/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK has transitioned to the moving state; <code>false</code> when it has transitioned to the stationary state.</p>"},{"location":"react-native/MotionChangeEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the motion-state transition was detected.</p>"},{"location":"react-native/NotificationConfig/","title":"NotificationConfig Android only","text":"<pre><code>export interface NotificationConfig {\n</code></pre> <p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"react-native/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"react-native/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The Title\",\n      text: \"The Text\"\n    }\n  }\n});\n\n// Update notification fields at runtime.\n// Only changed keys must be provided; existing values persist.\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      title: \"The New Title\",\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"react-native/NotificationConfig/#members","title":"Members","text":""},{"location":"react-native/NotificationConfig/#actions","title":"actions","text":"<pre><code>actions?: string[];\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"react-native/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>channelId?: string;\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"react-native/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>channelName?: string;\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      channelName: \"Location Tracker\"\n    }\n  }\n});\n\n// Update at runtime\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      channelName: \"My new channel name\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#color","title":"color","text":"<pre><code>color?: string;\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"react-native/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>largeIcon?: string;\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"drawable/my_notification_large_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"mipmap/my_notification_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#layout","title":"layout","text":"<pre><code>layout?: string;\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"react-native/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      layout: \"my_notification_layout\",\n      title: \"The Notification Title\",\n      text: \"The Notification Text\",\n      smallIcon: \"mipmap/my_small_icon\",\n      largeIcon: \"mipmap/my_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#priority","title":"priority","text":"<p><code>priority?:</code><code>NotificationPriority</code><code>;</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: NotificationPriority.High\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>smallIcon?: string;\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"drawable/my_notification_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"mipmap/my_notification_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>sticky?: boolean;\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"react-native/NotificationConfig/#strings","title":"strings","text":"<pre><code>strings?: Record&lt;string, string&gt;;\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/NotificationConfig/#text","title":"text","text":"<pre><code>text?: string;\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"react-native/NotificationConfig/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"react-native/NotificationPriority/","title":"NotificationPriority","text":"<pre><code>export const NotificationPriority = {\n</code></pre> <p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"react-native/NotificationPriority/#members","title":"Members","text":""},{"location":"react-native/NotificationPriority/#default","title":"Default","text":"<p>Default notification priority (normal weighting).</p>"},{"location":"react-native/NotificationPriority/#high","title":"High","text":"<p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"react-native/NotificationPriority/#low","title":"Low","text":"<p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"react-native/NotificationPriority/#max","title":"Max","text":"<p>Same as NotificationPriority.High.</p>"},{"location":"react-native/NotificationPriority/#min","title":"Min","text":"<p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"react-native/PermissionRationale/","title":"PermissionRationale Android only","text":"<pre><code>export interface PermissionRationale {\n</code></pre> <p>Configures the background location permission rationale dialog on Android 11 and later.</p> <p>Android 11 removed the Allow all the time option from the system location permission dialog. Apps may instead show a custom rationale dialog that explains why background access is needed and routes the user to the system Location Permissions screen to grant it explicitly.</p>"},{"location":"react-native/PermissionRationale/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Template tags</li> </ul>"},{"location":"react-native/PermissionRationale/#overview","title":"Overview","text":"<p>The SDK presents this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code> and AppConfig.backgroundPermissionRationale is configured.</p> <p>Selecting the dialog's positive action sends the user directly to the system Location Permissions screen, where they must enable Allow all the time themselves.</p> <ul> <li>The dialog is shown once only. After the user presses the positive action   it will not appear again. Pressing Cancel does not count as acceptance.</li> <li>If the user later resets your app's Location permission to Ask every time,   the dialog becomes eligible to be shown again.</li> </ul> <p> </p> <pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location access.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"react-native/PermissionRationale/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside any rationale field using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> \"Allow all the time\" (API 30+) Localized label for the background location permission option. <code>{applicationName}</code> App name Localized application name from <code>AndroidManifest</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Android 11 Location Updates</p>"},{"location":"react-native/PermissionRationale/#members","title":"Members","text":""},{"location":"react-native/PermissionRationale/#message","title":"message","text":"<pre><code>message?: string;\n</code></pre> <p>Body text of the dialog. Explain why your app needs background location access, similar in purpose to the iOS <code>NSLocationAlwaysAndWhenInUseUsageDescription</code>.</p>"},{"location":"react-native/PermissionRationale/#negativeaction","title":"negativeAction","text":"<pre><code>negativeAction?: string;\n</code></pre> <p>Text to display on the negative action button (e.g. \"Cancel\").</p>"},{"location":"react-native/PermissionRationale/#positiveaction","title":"positiveAction","text":"<pre><code>positiveAction?: string;\n</code></pre> <p>Text to display on the positive action button.</p>"},{"location":"react-native/PermissionRationale/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title for the Android background location permission dialog.</p>"},{"location":"react-native/PersistMode/","title":"PersistMode","text":"<pre><code>export const PersistMode = {\n</code></pre> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"react-native/PersistMode/#members","title":"Members","text":""},{"location":"react-native/PersistMode/#all","title":"All","text":"<p>Persist both location and geofence data.</p>"},{"location":"react-native/PersistMode/#geofence","title":"Geofence","text":"<p>Persist geofence data only.</p>"},{"location":"react-native/PersistMode/#location","title":"Location","text":"<p>Persist location data only.</p>"},{"location":"react-native/PersistMode/#none","title":"None","text":"<p>Do not persist any data.</p>"},{"location":"react-native/PersistenceConfig/","title":"PersistenceConfig","text":"<pre><code>export interface PersistenceConfig {\n</code></pre> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"react-native/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"react-native/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded Location and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 3,\n    maxRecordsToPersist: 1000,\n    extras: {\n      'user_id': 123,\n      'appVersion': '1.2.3'\n    },\n    persistMode: PersistMode.All\n  }\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BackgroundGeolocation.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BackgroundGeolocation.getCount or BackgroundGeolocation.getLocations.</p>"},{"location":"react-native/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BackgroundGeolocation.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"react-native/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"react-native/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  persist: true,\n  extras: { get_current_position: true },\n  samples: 3,\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>{\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    locationTemplate: '{...}',\n    geofenceTemplate: '{...}',\n    persistMode: PersistMode.all,\n    extras: {'user_id': 123},\n    disableProviderChangeRecord: true,\n  },\n};\n</code></pre>"},{"location":"react-native/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre>"},{"location":"react-native/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#members","title":"Members","text":""},{"location":"react-native/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>disableProviderChangeRecord?: boolean;\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BackgroundGeolocation.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    disableProviderChangeRecord: true\n }\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      device_id: \"abc123\"  // appended to root JSON of each POST request\n    }\n  },\n  persistence: {\n    extras: {\n      route_id: 1234       // merged onto each location record\n    }\n  }\n});\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"react-native/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>geofenceTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '{ \"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\" }'\n  }\n});\n\n// Or a compact Array form:\nBackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"react-native/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"react-native/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre>"},{"location":"react-native/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>locationTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping Location JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}'\n  }\n});\n\n// Or use a compact Array template:\nBackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n persistence: {\n  locationTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"react-native/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: 'https://my.server.com/locations',\n    rootProperty: 'data',\n  },\n  persistence: {\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}',\n    extras: { foo: \"bar\" }\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"react-native/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"react-native/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<pre><code>locationsOrderDirection?: \"ASC\" | \"DESC\";\n</code></pre> <p>Sort order used when selecting records for upload or returning them from BackgroundGeolocation.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"react-native/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>maxDaysToPersist?: number;\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"react-native/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>maxRecordsToPersist?: number;\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"react-native/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>persistMode?:</code><code>PersistMode</code><code>;</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BackgroundGeolocation.onLocation and BackgroundGeolocation.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BackgroundGeolocation.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"react-native/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>timestampFormat?: 'iso' | 'epoch';\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    timestampFormat: 'epoch',\n  },\n});\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"react-native/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<pre><code>export interface ProviderChangeEvent {\n</code></pre> <p>Location-services provider state delivered to BackgroundGeolocation.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]\",\n    \"enabled:\", event.enabled,\n    \"status:\", event.status,\n    \"gps:\", event.gps,\n    \"network:\", event.network,\n    \"accuracyAuthorization:\", event.accuracyAuthorization\n  );\n\n  if (!event.enabled) {\n    // Prompt the user to re-enable location services.\n  }\n});\n</code></pre>"},{"location":"react-native/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"react-native/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<pre><code>accuracyAuthorization: number;\n</code></pre> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"react-native/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"react-native/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>gps: boolean;\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"react-native/ProviderChangeEvent/#network","title":"network","text":"<pre><code>network: boolean;\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"react-native/ProviderChangeEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>Platform authorization status code. Use the AuthorizationStatus constants to interpret this value.</p>"},{"location":"react-native/SQLQuery/","title":"SQLQuery","text":"<pre><code>export interface SQLQuery {\n</code></pre> <p>Constrains a log query by date range, sort order, and record count.</p> <p>Pass to Logger.getLog, Logger.emailLog, or Logger.uploadLog to filter which log entries are included.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\n\n// Date range\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\")\n});\n\n// First 100 records ascending\nawait Logger.emailLog(\"foo@bar.com\", {\n  order: SQLQueryOrder.Asc,\n  limit: 100\n});\n\n// Upload a specific time window\nawait Logger.uploadLog(\"https://my.server.com/users/123/logs\", {\n  start: Date.parse(\"2019-10-20 09:00\"),\n  end:   Date.parse(\"2019-10-20 11:59\")\n});\n</code></pre>"},{"location":"react-native/SQLQuery/#members","title":"Members","text":""},{"location":"react-native/SQLQuery/#end","title":"end","text":"<pre><code>end?: number;\n</code></pre> <p>End of the query window (unix timestamp in milliseconds).</p>"},{"location":"react-native/SQLQuery/#limit","title":"limit","text":"<pre><code>limit?: number;\n</code></pre> <p>Maximum number of records to return.</p>"},{"location":"react-native/SQLQuery/#offset","title":"offset","text":"<pre><code>offset?: number;\n</code></pre> <p>Number of matching records to skip before returning results (for paging).</p>"},{"location":"react-native/SQLQuery/#order","title":"order","text":"<p><code>order?:</code><code>SQLQueryOrder</code><code>;</code></p> <p>Sort order for results: <code>SQLQueryOrder.Asc</code> (oldest first) or <code>SQLQueryOrder.Desc</code> (newest first).</p>"},{"location":"react-native/SQLQuery/#start","title":"start","text":"<pre><code>start?: number;\n</code></pre> <p>Start of the query window (unix timestamp in milliseconds).</p>"},{"location":"react-native/SQLQueryOrder/","title":"SQLQueryOrder","text":"<pre><code>export const SQLQueryOrder = {\n</code></pre> <p>Sort order for SQLQuery.order.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"react-native/SQLQueryOrder/#members","title":"Members","text":""},{"location":"react-native/SQLQueryOrder/#asc","title":"Asc","text":"<p>Ascending order (oldest to newest).</p>"},{"location":"react-native/SQLQueryOrder/#desc","title":"Desc","text":"<p>Descending order (newest to oldest).</p>"},{"location":"react-native/Sensors/","title":"Sensors","text":"<pre><code>export interface Sensors {\n</code></pre> <p>Availability of motion sensors on the device, as returned by BackgroundGeolocation.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(\"[Sensors]\", sensors);\n</code></pre>"},{"location":"react-native/Sensors/#members","title":"Members","text":""},{"location":"react-native/Sensors/#accelerometer","title":"accelerometer","text":"<pre><code>accelerometer: boolean;\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"react-native/Sensors/#gyroscope","title":"gyroscope","text":"<pre><code>gyroscope: boolean;\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"react-native/Sensors/#magnetometer","title":"magnetometer","text":"<pre><code>magnetometer: boolean;\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"react-native/Sensors/#motion_hardware","title":"motion_hardware","text":"<pre><code>motion_hardware?: boolean;\n</code></pre> <p>iOS only <code>true</code> when the device has a dedicated M-series motion co-processor (iPhone 5S and later). The M-series co-processor offloads motion activity recognition from the main CPU, enabling low-power step counting and activity detection.</p>"},{"location":"react-native/Sensors/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"ios\"</code> or <code>\"android\"</code>.</p>"},{"location":"react-native/Sensors/#significant_motion","title":"significant_motion","text":"<pre><code>significant_motion?: boolean;\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"react-native/State/","title":"State","text":"<p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] enabled:\", state.enabled, \"trackingMode:\", state.trackingMode);\n</code></pre>"},{"location":"react-native/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"react-native/State/#members","title":"Members","text":""},{"location":"react-native/State/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. See ActivityConfig.</p>"},{"location":"react-native/State/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. See AppConfig.</p>"},{"location":"react-native/State/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. See AuthorizationConfig.</p>"},{"location":"react-native/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>didDeviceReboot: boolean;\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"react-native/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>didLaunchInBackground: boolean;\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"react-native/State/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BackgroundGeolocation.start or BackgroundGeolocation.startGeofences has been called and not yet stopped.</p>"},{"location":"react-native/State/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. See GeoConfig.</p>"},{"location":"react-native/State/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. See HttpConfig.</p>"},{"location":"react-native/State/#isfirstboot","title":"isFirstBoot","text":"<p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"react-native/State/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// Toggle the SDK's motion state from stationary to moving.\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log('[onMotionChange] isMoving?', event.isMoving);\n});\n\nawait BackgroundGeolocation.changePace(true);\n// State.isMoving is now true.\n</code></pre>"},{"location":"react-native/State/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. See LoggerConfig.</p>"},{"location":"react-native/State/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BackgroundGeolocation.setOdometer - BackgroundGeolocation.getOdometer</p>"},{"location":"react-native/State/#odometererror","title":"odometerError","text":"<pre><code>odometerError: number;\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilter.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"react-native/State/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. See PersistenceConfig.</p>"},{"location":"react-native/State/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"react-native/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>schedulerEnabled: boolean;\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BackgroundGeolocation.startSchedule has been called. BackgroundGeolocation.stopSchedule sets this to <code>false</code>.</p>"},{"location":"react-native/State/#trackingmode","title":"trackingMode","text":"<p><code>trackingMode:</code><code>TrackingMode</code><code>;</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>await BackgroundGeolocation.start();\nlet state = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 1'\n\nawait BackgroundGeolocation.startGeofences();\nstate = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 0'\n</code></pre>"},{"location":"react-native/State/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"react-native/Subscription/","title":"Subscription","text":"<pre><code>export interface Subscription {\n</code></pre> <p>Handle returned by every <code>BackgroundGeolocation.on*</code> event-listener method.</p> <p><code>Subscription</code> exposes a single <code>remove</code> method used to stop listening to an event and free associated resources. Always call <code>remove()</code> when a listener is no longer needed to prevent memory leaks.</p>"},{"location":"react-native/Subscription/#removing-an-event-listener","title":"Removing an event-listener","text":"<pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\n// Later, when the listener is no longer needed:\nsubscription.remove();\n</code></pre>"},{"location":"react-native/Subscription/#managing-multiple-subscriptions","title":"Managing multiple subscriptions","text":"<p>Collect subscriptions in an array and remove them all at once \u2014 for example when a view is destroyed.</p> <pre><code>import BackgroundGeolocation, {\n  Location,\n  Subscription\n} from \"react-native-background-geolocation\";\n\nconst subscriptions: Subscription[] = [];\n\nfunction addListeners() {\n  subscriptions.push(\n    BackgroundGeolocation.onLocation((location: Location) =&gt; {\n      console.log(\"[onLocation]\", location);\n    }),\n    BackgroundGeolocation.onMotionChange((event) =&gt; {\n      console.log(\"[onMotionChange]\", event);\n    }),\n    BackgroundGeolocation.onEnabledChange((enabled: boolean) =&gt; {\n      console.log(\"[onEnabledChange]\", enabled);\n    })\n  );\n}\n\nfunction removeListeners() {\n  subscriptions.forEach((sub) =&gt; sub.remove());\n  subscriptions.length = 0;\n}\n</code></pre>"},{"location":"react-native/Subscription/#members","title":"Members","text":""},{"location":"react-native/Subscription/#remove","title":"remove","text":"<pre><code>remove(): void;\n</code></pre> <p>Removes the event-listener and frees its resources. Call this when the listener is no longer needed to prevent memory leaks.</p>"},{"location":"react-native/TrackingMode/","title":"TrackingMode","text":"<pre><code>export const TrackingMode = {\n</code></pre> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"react-native/TrackingMode/#members","title":"Members","text":""},{"location":"react-native/TrackingMode/#geofences","title":"Geofences","text":"<p>Monitor geofences only.</p>"},{"location":"react-native/TrackingMode/#location","title":"Location","text":"<p>Monitor location + geofences.</p>"},{"location":"react-native/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<pre><code>export interface TransistorAuthorizationService {\n</code></pre> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"react-native/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>const url = \"http://tracker.transistorsoft.com\";\nconst orgname = \"my-company-name\";\nconst username = \"my-username\";\n\n// Fetch a token from the server (the SDK caches it automatically).\nconst token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  orgname, username, url\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"react-native/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"react-native/TransistorAuthorizationService/#applyif","title":"applyIf","text":"<p><code>applyIf(config: T):</code><code>Config</code><code>;</code></p> <p>Mutate a Config to apply a <code>transistorAuthorizationToken</code> if present.</p> <p>The implementation reads <code>config.transistorAuthorizationToken</code>, removes that property, sets <code>config.http.url</code> to <code>\"&lt;token.url&gt;/api/locations\"</code>, and injects <code>config.authorization</code> with a JWT strategy derived from the token. If no <code>transistorAuthorizationToken</code> is found, the config is returned unchanged.</p> <pre><code>const token = await service.findOrCreate('my-org', 'user@example.com');\n\nconst config = service.applyIf({\n  ...myConfig,\n  transistorAuthorizationToken: token\n});\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"react-native/TransistorAuthorizationService/#destroy","title":"destroy","text":"<pre><code>destroy(url?: string): Promise&lt;void&gt;;\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"react-native/TransistorAuthorizationService/#findorcreate","title":"findOrCreate","text":"<p><code>findOrCreate(orgName: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\",\n  \"http://tracker.transistorsoft.com\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"react-native/TransistorAuthorizationToken/","title":"TransistorAuthorizationToken","text":"<pre><code>export interface TransistorAuthorizationToken {\n</code></pre> <p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"react-native/TransistorAuthorizationToken/#members","title":"Members","text":""},{"location":"react-native/TransistorAuthorizationToken/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"react-native/TransistorAuthorizationToken/#expires","title":"expires","text":"<pre><code>expires: number;\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"react-native/TransistorAuthorizationToken/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken: string;\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"react-native/TransistorAuthorizationToken/#url","title":"url","text":"<pre><code>url: string;\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"react-native/TriggerActivity/","title":"TriggerActivity","text":"<pre><code>export const TriggerActivity = {\n</code></pre> <p>Allowed motion activity names that can trigger motion detection. Mirrors Flutter's TriggerActivity set.</p>"},{"location":"react-native/Vertices/","title":"Vertices","text":"<pre><code>export type Vertices = [number, number][];\n</code></pre> <p>An array of <code>[latitude, longitude]</code> pairs defining a polygon geofence.</p> <p>See Geofence.vertices.</p>"},{"location":"react-native/WatchPositionRequest/","title":"WatchPositionRequest","text":"<pre><code>export interface WatchPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.watchPosition.</p> <p>Configures the interval, accuracy, persistence, and metadata for a continuous location stream. All fields are optional.</p> <p>Warning</p> <p><code>watchPosition</code> is intended for foreground use. On iOS it prevents the app from being suspended, which drains the battery. Remove the subscription when your app moves to the background.</p> <pre><code>const subscription = BackgroundGeolocation.watchPosition(\n  { interval: 1000, desiredAccuracy: DesiredAccuracy.High },\n  (location) =&gt; {\n    console.log(\"[watchPosition]\", location);\n  },\n  (errorCode) =&gt; {\n    console.warn(\"[watchPosition] error:\", errorCode);\n  }\n);\n\n// Later, stop watching.\nsubscription.remove();\n</code></pre>"},{"location":"react-native/WatchPositionRequest/#members","title":"Members","text":""},{"location":"react-native/WatchPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Target accuracy for location updates from the native API. Defaults to DesiredAccuracy.High.</p>"},{"location":"react-native/WatchPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to each location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"react-native/WatchPositionRequest/#interval","title":"interval","text":"<pre><code>interval?: number;\n</code></pre> <p>Interval in milliseconds between location updates.</p>"},{"location":"react-native/WatchPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist each location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"react-native/WatchPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in milliseconds to wait for each location fix before firing an error. Default <code>60000</code>.</p>"},{"location":"react-native/examples/","title":"Examples","text":""},{"location":"react-native/examples/#basic-tracking","title":"Basic tracking","text":"<pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"react-native/setup/","title":"Setup","text":"React NativeExpo"},{"location":"react-native/setup/#react-native","title":"React Native","text":""},{"location":"react-native/setup/#installation","title":"Installation","text":"npmyarn <pre><code>npm install react-native-background-geolocation\n</code></pre> <pre><code>yarn add react-native-background-geolocation\n</code></pre>"},{"location":"react-native/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"react-native/setup/#ios-infoplist","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"react-native/setup/#android-androidmanifestxml","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"react-native/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"react-native/setup/#cocoapods","title":"CocoaPods","text":"<pre><code>cd ios &amp;&amp; pod install\n</code></pre>"},{"location":"react-native/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"react-native/setup/#infoplist","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Background modes --&gt;\n&lt;key&gt;UIBackgroundModes&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;location&lt;/string&gt;\n    &lt;string&gt;fetch&lt;/string&gt;\n    &lt;string&gt;processing&lt;/string&gt;\n    &lt;string&gt;audio&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Background task identifier --&gt;\n&lt;key&gt;BGTaskSchedulerPermittedIdentifiers&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;com.transistorsoft.fetch&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"react-native/setup/#appdelegate","title":"AppDelegate","text":"SwiftObjective-C <pre><code>import TSBackgroundFetch\n\nfunc application(_ application: UIApplication,\n                 didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -&gt; Bool {\n    TSBackgroundFetch.sharedInstance()?.didFinishLaunching()\n    return true\n}\n</code></pre> <pre><code>#import &lt;TSBackgroundFetch/TSBackgroundFetch.h&gt;\n\n- (BOOL)application:(UIApplication *)application\n    didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {\n    [[TSBackgroundFetch sharedInstance] didFinishLaunching];\n    return YES;\n}\n</code></pre>"},{"location":"react-native/setup/#android-setup","title":"Android Setup","text":""},{"location":"react-native/setup/#gradle-ext-vars","title":"Gradle ext vars","text":"<p>Set these in <code>android/build.gradle</code> (or <code>android/settings.gradle</code> for newer projects):</p> <p>The plugin reads two optional ext vars from your root project to pin dependency versions:</p> build.gradle.ktsbuild.gradle <pre><code>ext[\"playServicesLocationVersion\"] = \"21.3.0\"\next[\"tslocationmanagerVersion\"]    = \"4.0.+\"\n</code></pre> <pre><code>ext {\n    playServicesLocationVersion = \"21.3.0\"\n    tslocationmanagerVersion    = \"4.0.+\"\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Versions \u2265 21 use <code>tslocationmanager</code>; versions &lt; 21 use <code>tslocationmanager-gms20</code>. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"react-native/setup/#androidappbuildgradle","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>android {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"react-native/setup/#androidmanifestxml","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"react-native/setup/#example","title":"Example","text":"<pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"react-native/setup/#expo","title":"Expo","text":""},{"location":"react-native/setup/#installation_1","title":"Installation","text":"<pre><code>npx expo install react-native-background-geolocation\n</code></pre> <p>The SDK ships an Expo Config Plugin \u2014 it handles all native configuration automatically during <code>expo prebuild</code>. No manual edits to <code>Info.plist</code>, <code>AndroidManifest.xml</code>, or Gradle files are required.</p>"},{"location":"react-native/setup/#configure-your-license_1","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"react-native/setup/#appjson-appconfigjs","title":"<code>app.json</code> / <code>app.config.js</code>","text":"<pre><code>{\n  \"expo\": {\n    \"plugins\": [\n      [\n        \"react-native-background-geolocation\",\n        {\n          \"license\": \"YOUR_ANDROID_LICENSE_KEY_JWT\"\n        }\n      ],\n      [\n        \"expo-gradle-ext-vars\",\n        {\n          \"googlePlayServicesLocationVersion\": \"21.3.0\",\n          \"tslocationmanagerVersion\": \"4.0.+\"\n        }\n      ]\n    ],\n    \"ios\": {\n      \"infoPlist\": {\n        \"TSLocationManagerLicense\": \"YOUR_IOS_LICENSE_KEY_JWT\",\n        \"UIBackgroundModes\": [\"location\", \"fetch\", \"processing\", \"audio\"],\n        \"BGTaskSchedulerPermittedIdentifiers\": [\n          \"com.transistorsoft.fetch\"\n        ],\n        \"NSLocationAlwaysAndWhenInUseUsageDescription\": \"App requires location access at all times for background tracking.\",\n        \"NSLocationWhenInUseUsageDescription\": \"App requires location access while in use.\",\n        \"NSMotionUsageDescription\": \"Motion detection helps determine when the device is stationary.\"\n      }\n    }\n  }\n}\n</code></pre> <p><code>expo-gradle-ext-vars</code></p> <p>Install the companion package to control the Google Play Services location version: <pre><code>npx expo install expo-gradle-ext-vars\n</code></pre></p>"},{"location":"react-native/setup/#prebuild","title":"Prebuild","text":"<p>After configuring <code>app.json</code>, regenerate native projects:</p> <pre><code>npx expo prebuild\n</code></pre> <p>Then build as normal:</p> <pre><code>npx expo run:ios\nnpx expo run:android\n</code></pre>"},{"location":"react-native/setup/#example_1","title":"Example","text":"<p>The JavaScript API is identical to React Native.</p> <pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"swift/","title":"Swift SDK Reference","text":"native-background-geolocation Buy Now <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"swift/Activity/","title":"Activity","text":"<p><code>public struct</code><code>Activity</code></p> <p>The motion activity reported by the device at the time a LocationEvent was recorded.</p>"},{"location":"swift/Activity/#members","title":"Members","text":""},{"location":"swift/Activity/#confidence","title":"confidence","text":"<pre><code>public let confidence: Int?\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"swift/Activity/#type","title":"type","text":"<pre><code>public let type: String?\n</code></pre> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"swift/ActivityChangeEvent/","title":"ActivityChangeEvent","text":"<p><code>public struct</code><code>ActivityChangeEvent</code><code>: Sendable</code></p> <p>Activity recognition result delivered to BGGeo.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p>"},{"location":"swift/ActivityChangeEvent/#members","title":"Members","text":""},{"location":"swift/ActivityChangeEvent/#activity","title":"activity","text":"<pre><code>public let activity: String\n</code></pre> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"swift/ActivityChangeEvent/#confidence","title":"confidence","text":"<pre><code>public let confidence: Int\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"swift/ActivityConfig/","title":"ActivityConfig","text":"<p><code>public class</code><code>ActivityConfig</code></p> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"swift/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"swift/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    // Android: poll activity recognition every 10s\n    config.activity.activityRecognitionInterval = 10000.0\n\n    // Android: require &gt;=75% confidence before changing activity\n    config.activity.minimumActivityRecognitionConfidence = 75\n\n    // Cross-platform: enable/disable automatic stop detection\n    config.activity.disableStopDetection = false\n\n    // Cross-platform: automatically stop when stationary\n    config.activity.stopOnStationary = true\n\n    // motionTriggerDelay \u2014 Android only \u2014 not applicable on iOS\n\n    // Android &amp; iOS: disable platform motion activity APIs\n    config.activity.disableMotionActivityUpdates = false\n\n    // iOS: delay stop-detection by 10s\n    config.activity.stopDetectionDelay = 10000\n}\n</code></pre>"},{"location":"swift/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeolocationConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"swift/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.activity.activityRecognitionInterval = 10000.0\n    config.activity.disableStopDetection = true\n    config.activity.stopOnStationary = true\n}\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.activity.activityRecognitionInterval = 10000.0\n    config.activity.disableStopDetection = true\n    config.activity.stopOnStationary = true\n}\n</code></pre>"},{"location":"swift/ActivityConfig/#members","title":"Members","text":""},{"location":"swift/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>public var activityRecognitionInterval: TimeInterval\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"swift/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>public var disableMotionActivityUpdates: Bool\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"swift/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"swift/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.activity.disableMotionActivityUpdates = true\n}\n</code></pre>"},{"location":"swift/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>public var disableStopDetection: Bool\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"swift/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeolocationConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeolocationConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.activity.disableStopDetection = true\n    config.geolocation.pausesLocationUpdatesAutomatically = false\n}\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BGGeo.changePace).</p> <p></p>"},{"location":"swift/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BGGeo.changePace <code>false</code> or BGGeo.stop explicitly.</p>"},{"location":"swift/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>public var minimumActivityRecognitionConfidence: Int\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"swift/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>public var stopDetectionDelay: TimeInterval\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"swift/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>public var stopOnStationary: Bool\n</code></pre> <p>Automatically calls BGGeo.stop when the GeolocationConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeolocationConfig.stopTimeout timer elapses naturally. It does not fire when BGGeo.changePace <code>false</code> is called manually.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.activity.stopOnStationary = true\n}\nTask {\n    do {\n        try await bgGeo.start()\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<pre><code>public var triggerActivities: String\n</code></pre> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See <code>TriggerActivity</code> for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>let bgGeo = BGGeo.shared\n// Only trigger tracking for vehicles\nbgGeo.ready { config in\n    config.activity.triggerActivities = \"in_vehicle\"\n}\n\n// Only trigger tracking for on_foot, walking and running\nbgGeo.ready { config in\n    config.activity.triggerActivities = \"on_foot, walking, running\"\n}\n</code></pre>"},{"location":"swift/App/","title":"App","text":"<p><code>public class</code><code>App</code></p> <p>Background-task and power-save utilities.</p> <p>Access via <code>BGGeo.instance.app</code>.</p>"},{"location":"swift/App/#members","title":"Members","text":""},{"location":"swift/App/#createbackgroundtask","title":"createBackgroundTask","text":"<pre><code>func startBackgroundTask() async throws -&gt; Double\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>"},{"location":"swift/App/#ios","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>"},{"location":"swift/App/#android","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onLocation { location in\n    print(\"[location] \\(location)\")\n\n    // Perform some long-running task (eg: HTTP request)\n    let taskId = bgGeo.app.createBackgroundTask()\n    // Perform your long-running task here\n    print(\"[backgroundTask] executing taskId: \\(taskId)\")\n    // When your long-running task is complete, signal completion of taskId.\n    bgGeo.app.stopBackgroundTask(taskId)\n}\n</code></pre>"},{"location":"swift/App/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>public func stopBackgroundTask(_ taskId: UIBackgroundTaskIdentifier)\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>let bgGeo = BGGeo.shared\n// Perform some long-running task (e.g., HTTP request)\nlet taskId = bgGeo.app.createBackgroundTask()\n// Perform your long-running task here\nprint(\"[backgroundTask] executing taskId: \\(taskId)\")\n// When your long-running task is complete, signal completion of taskId.\nbgGeo.app.stopBackgroundTask(taskId)\n</code></pre>"},{"location":"swift/AppConfig/","title":"AppConfig","text":"<p><code>public class</code><code>AppConfig</code></p> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"swift/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"swift/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n}\n</code></pre>"},{"location":"swift/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"swift/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BGGeo.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"swift/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BGGeo.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"swift/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeolocationConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See <code>PermissionRationale</code> for the full dialog configuration reference.</p>"},{"location":"swift/AppConfig/#examples","title":"Examples","text":""},{"location":"swift/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n    // enableHeadless \u2014 Android only \u2014 not applicable on iOS\n    config.app.heartbeatInterval = 60\n    // backgroundPermissionRationale \u2014 Android only \u2014 not applicable on iOS\n    // notification \u2014 Android only \u2014 not applicable on iOS\n    config.app.schedule = [\"1-5 09:00-17:00\"] // Weekdays 9-5\n    // scheduleUseAlarmManager \u2014 Android only \u2014 not applicable on iOS\n}\n</code></pre>"},{"location":"swift/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.config.batchUpdate { config in\n    config.app.heartbeatInterval = 120 // slow down heartbeats\n}\n</code></pre>"},{"location":"swift/AppConfig/#members","title":"Members","text":""},{"location":"swift/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>public var heartbeatInterval: TimeInterval\n</code></pre> <p>Rate in seconds at which BGGeo.onHeartbeat events fire while the app is in the background.</p>"},{"location":"swift/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"swift/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BGGeo.onHeartbeat</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.preventSuspend = true\n    config.app.heartbeatInterval = 60\n}\n\nbgGeo.onHeartbeat { event in\n    print(\"[onHeartbeat]\", event)\n\n    // Optionally request a new location during heartbeat.\n    Task {\n        do {\n            let location = try await bgGeo.getCurrentPosition(samples: 1, persist: true)\n            print(\"[getCurrentPosition]\", location)\n        } catch {\n            print(\"Error: \\(error)\")\n        }\n    }\n}\n</code></pre>"},{"location":"swift/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>public var preventSuspend: Bool\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BGGeo.onHeartbeat</p> <pre><code>let bgGeo = BGGeo.shared\n\n// Subscribe to heartbeat events\nbgGeo.onHeartbeat { event in\n    print(\"[onHeartbeat]\", event)\n}\n\n// Enable preventSuspend via AppConfig\nbgGeo.ready { config in\n    config.app.preventSuspend = true\n    config.app.heartbeatInterval = 60\n}\n</code></pre>"},{"location":"swift/AppConfig/#schedule","title":"schedule","text":"<pre><code>public var schedule: [Any]\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"DAY(s) START_TIME-END_TIME\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.schedule = [\n        \"1 17:30-21:00\",     // Sunday: 5:30pm-9pm\n        \"2-6 09:00-17:00\",   // Mon-Fri: 9am-5pm\n        \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm-midnight\n        \"7 10:00-19:00\"      // Saturday: 10am-7pm\n    ]\n}\n\n// Start the Scheduler\nbgGeo.startSchedule()\n\n// Listen for schedule state changes\nbgGeo.onSchedule { event in\n    print(\"[onSchedule] enabled? \\(event.enabled)\")\n}\n\n// Later (e.g., user logout)\nbgGeo.stopSchedule()\nbgGeo.stop() // if tracking is currently enabled\n\n// Modify schedule using batchUpdate\nbgGeo.config.batchUpdate { config in\n    config.app.schedule = [\n        \"1-7 09:00-10:00\",\n        \"1-7 11:00-12:00\",\n        \"1-7 13:00-14:00\",\n        \"1-7 15:00-16:00\",\n        \"1-7 17:00-18:00\",\n        \"2,4,6 19:00-22:00\"\n    ]\n}\n</code></pre>"},{"location":"swift/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.schedule = [\n        \"2018-01-01 09:00-17:00\"\n    ]\n}\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.schedule = [\n        \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n}\n</code></pre>"},{"location":"swift/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.schedule = [\n        \"1-7 09:00-17:00 location\",\n        \"1-7 18:00-12:00 geofence\"\n    ]\n}\n</code></pre>"},{"location":"swift/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"swift/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BGGeo.startSchedule - BGGeo.stopSchedule</p>"},{"location":"swift/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.schedule = [\n        \"1-7 09:00-17:00 location\",\n        \"1-7 18:00-12:00 geofence\"\n    ]\n}\n</code></pre>"},{"location":"swift/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>public var startOnBoot: Bool\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"swift/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"swift/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"swift/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>public var stopOnTerminate: Bool\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"swift/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeolocationConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"swift/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"swift/Authorization/","title":"Authorization","text":"<p><code>public class</code><code>Authorization</code></p> <p>Location permission management \u2014 request and monitor authorisation status.</p> <p>Access via <code>BGGeo.instance.authorization</code>.</p>"},{"location":"swift/Authorization/#members","title":"Members","text":""},{"location":"swift/Authorization/#getstate","title":"getState","text":"<p><code>public func get</code><code>State</code><code>() -&gt;</code><code>BGGeo</code><code>.</code><code>ProviderChangeEvent</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>let bgGeo = BGGeo.shared\nlet providerState = bgGeo.authorization.getState()\nprint(\"- Provider state:\", providerState)\n</code></pre>"},{"location":"swift/Authorization/#requestpermission","title":"requestPermission","text":"<pre><code>public func requestPermission() async throws -&gt; CLAuthorizationStatus\n</code></pre> <p>Manually request location permission using the configured GeolocationConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeolocationConfig.locationAuthorizationRequest - GeolocationConfig.disableLocationAuthorizationAlert - GeolocationConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>let bgGeo = BGGeo.shared\n\n// Listen to onProviderChange to be notified when location authorization changes occur.\nlet subscription = bgGeo.onProviderChange { event in\n    print(\"[providerchange] \\(event)\")\n}\n\n// First ready the plugin with your configuration.\nbgGeo.ready { config in\n    config.geolocation.locationAuthorizationRequest = .always\n}\n\n// Manually request permission with configured locationAuthorizationRequest.\nTask {\n    do {\n        let status = try await bgGeo.authorization.requestPermission()\n        print(\"[requestPermission] success: \\(status)\")\n    } catch {\n        print(\"[requestPermission] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Authorization/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<pre><code>public func requestTemporaryFullAccuracy(purpose: String) async throws -&gt; CLAccuracyAuthorization\n</code></pre> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>"},{"location":"swift/Authorization/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns <code>AccuracyAuthorization.Full</code> immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onProviderChange { event in\n    if event.accuracyAuthorization == CLAccuracyAuthorization.reducedAccuracy.rawValue {\n        Task {\n            do {\n                let accuracyAuthorization = try await bgGeo.authorization.requestTemporaryFullAccuracy(purpose: \"Delivery\")\n                if accuracyAuthorization == CLAccuracyAuthorization.fullAccuracy.rawValue {\n                    print(\"[requestTemporaryFullAccuracy] GRANTED: \\(accuracyAuthorization)\")\n                } else {\n                    print(\"[requestTemporaryFullAccuracy] DENIED: \\(accuracyAuthorization)\")\n                }\n            } catch {\n                print(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: \\(error)\")\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"swift/AuthorizationConfig/","title":"AuthorizationConfig","text":"<p><code>public class</code><code>AuthorizationConfig</code></p> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"swift/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"swift/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>let bgGeo = BGGeo.shared\nlet token = \"my-auth-token\"\nlet refreshToken = \"my-refresh-token\"\n\n// Listen for authorization refresh results\nlet sub = bgGeo.onAuthorization { event in\n    if event.isSuccess {\n        print(\"[authorization] SUCCESS:\", event.response)\n    } else {\n        print(\"[authorization] ERROR:\", event.error ?? \"\")\n    }\n}\n\n// Initialize with JWT authorization\nbgGeo.ready { config in\n    config.http.url = \"https://app.your.server.com/users/locations\"\n    config.http.autoSync = true\n    config.authorization.strategy = \"JWT\"\n    config.authorization.accessToken = token\n    config.authorization.refreshToken = refreshToken\n    config.authorization.refreshUrl = \"https://auth.your.server.com/tokens\"\n    config.authorization.refreshPayload = [\n        \"the_refresh_token_field_name\": \"{refreshToken}\"\n    ]\n    config.authorization.expires = -1\n}\n</code></pre>"},{"location":"swift/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"swift/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"swift/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BGGeo.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.onAuthorization { event in\n    if event.isSuccess {\n        print(\"[authorization] SUCCESS:\", event.response)\n    } else {\n        print(\"[authorization] ERROR:\", event.error ?? \"\")\n    }\n}\n</code></pre>"},{"location":"swift/AuthorizationConfig/#members","title":"Members","text":""},{"location":"swift/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>public var accessToken: String?\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p>"},{"location":"swift/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>public var expires: TimeInterval\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"swift/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>public var refreshHeaders: [String: String]?\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.authorization.accessToken = \"XXX.YYY.ZZZ\"\n    config.authorization.refreshUrl = \"https://auth.domain.com/tokens\"\n    config.authorization.refreshToken = \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n    config.authorization.refreshPayload = [\n        \"my_refresh_token\": \"{refreshToken}\",\n        \"grant_type\": \"refresh_token\",\n        \"foo\": \"another arbitrary field\"\n    ]\n    config.authorization.refreshHeaders = [:]  // Empty dict to provide no refreshHeaders.\n}\n</code></pre>"},{"location":"swift/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>public var refreshPayload: [String: Any]?\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.authorization.strategy = \"JWT\"\n    config.authorization.accessToken = \"XXX.YYY.ZZZ\"\n    config.authorization.refreshUrl = \"https://auth.your.server.com/tokens\"\n    config.authorization.refreshToken = \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n    config.authorization.refreshPayload = [\n        \"my_refresh_token\": \"{refreshToken}\",\n        \"grant_type\": \"refresh_token\",\n        \"foo\": \"another arbitrary field\"\n    ]\n}\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"swift/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>public var refreshToken: String?\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"swift/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>public var refreshUrl: String?\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BGGeo.onAuthorization with an AuthorizationEvent.</p>"},{"location":"swift/AuthorizationConfig/#strategy","title":"strategy","text":"<p><code>public var strategy:</code><code>AuthorizationStrategy</code><code>?</code></p> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"swift/AuthorizationEvent/","title":"AuthorizationEvent","text":"<p><code>public struct</code><code>AuthorizationEvent</code></p> <p>Authorization token refresh result delivered to BGGeo.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p>"},{"location":"swift/AuthorizationEvent/#members","title":"Members","text":""},{"location":"swift/AuthorizationEvent/#error","title":"error","text":"<pre><code>public let error: NSError?\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"swift/AuthorizationEvent/#issuccess","title":"isSuccess","text":"<pre><code>public var isSuccess: Bool\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"swift/AuthorizationEvent/#response","title":"response","text":"<pre><code>public let response: [String: Any]\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"swift/AuthorizationEvent/#status","title":"status","text":"<pre><code>public let status: Int\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"swift/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<p><code>public enum</code><code>AuthorizationStrategy</code><code>: String</code></p> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"swift/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"swift/AuthorizationStrategy/#jwt","title":"jwt","text":"<pre><code>case jwt = \"JWT\"\n</code></pre> <p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"swift/AuthorizationStrategy/#sas","title":"sas","text":"<pre><code>case sas = \"SAS\"\n</code></pre> <p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"swift/BGGeo/","title":"BGGeo","text":"<p><code>public class</code><code>BGGeo</code></p> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>"},{"location":"swift/BGGeo/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>"},{"location":"swift/BGGeo/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>"},{"location":"swift/BGGeo/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>"},{"location":"swift/BGGeo/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeolocationConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li><code>DesiredAccuracy</code></li> <li>PersistMode</li> <li><code>Event</code></li> </ul>"},{"location":"swift/BGGeo/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a <code>Subscription</code> that must be removed when no longer needed:</p>"},{"location":"swift/BGGeo/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.logger.logLevel = .debug\n}\n</code></pre>"},{"location":"swift/BGGeo/#examples","title":"Examples","text":""},{"location":"swift/BGGeo/#compound-configuration","title":"Compound configuration","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 20\n    config.http.url = \"https://example.com/locations\"\n    config.http.autoSync = true\n    config.persistence.maxDaysToPersist = 7\n}\n</code></pre>"},{"location":"swift/BGGeo/#event-listeners","title":"Event listeners","text":"<pre><code>let bgGeo = BGGeo.shared\nlet locationSub = bgGeo.onLocation { location in\n    print(\"New location: \\(location)\")\n}\n\nlet motionSub = bgGeo.onMotionChange { event in\n    print(\"Device is moving? \\(event.isMoving)\")\n}\n</code></pre>"},{"location":"swift/BGGeo/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>let bgGeo = BGGeo.shared\nvar subscriptions = Set&lt;EventSubscription&gt;()\n\nlet subscription = bgGeo.onHttp { event in\n    // Handle event\n}\n// EventSubscription auto-removes on deinit; or store in a Set:\nsubscription.store(in: &amp;subscriptions)\n</code></pre>"},{"location":"swift/BGGeo/#getting-started","title":"Getting started","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.distanceFilter = 10\n    config.http.url = \"https://example.com/locations\"\n    config.http.autoSync = true\n}\n\nif !bgGeo.enabled {\n    Task {\n        try await bgGeo.start()\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#events_1","title":"Events","text":""},{"location":"swift/BGGeo/#onactivitychange","title":"onActivityChange","text":"<p><code>public func onActivityChange(_ callback: @escaping (</code><code>ActivityChangeEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>"},{"location":"swift/BGGeo/#android","title":"Android","text":"<p>ActivityChangeEvent.confidence always reports <code>100</code>.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onActivityChange { event in\n    print(\"[onActivityChange] \\(event.activity) (\\(event.confidence)%)\")\n}\n</code></pre> <p>activitychange</p>"},{"location":"swift/BGGeo/#onauthorization","title":"onAuthorization","text":"<p><code>public func on</code><code>Authorization</code><code>(_ callback: @escaping (</code><code>AuthorizationEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onAuthorization { event in\n    if event.isSuccess {\n        print(\"[authorization] SUCCESS:\", event.response)\n    } else {\n        print(\"[authorization] ERROR:\", event.error ?? \"\")\n    }\n}\n</code></pre> <p>authorization</p>"},{"location":"swift/BGGeo/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>public func onConnectivityChange(_ callback: @escaping (</code><code>ConnectivityChangeEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onConnectivityChange { event in\n    print(\"[onConnectivityChange] hasConnection:\", event.hasConnection)\n}\n</code></pre> <p>connectivitychange</p>"},{"location":"swift/BGGeo/#onenabledchange","title":"onEnabledChange","text":"<pre><code>public func onEnabledChange(_ callback: @escaping (EnabledChangeEvent) -&gt; Void) -&gt; EventSubscription\n</code></pre> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onEnabledChange { event in\n    print(\"[onEnabledChanged] isEnabled? \\(event.enabled)\")\n}\n</code></pre> <p>enabledchange</p>"},{"location":"swift/BGGeo/#ongeofence","title":"onGeofence","text":"<p><code>public func on</code><code>Geofence</code><code>(_ callback: @escaping (</code><code>GeofenceEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onGeofence { event in\n    print(\"[onGeofence] \\(event)\")\n}\n</code></pre> <p>geofence</p>"},{"location":"swift/BGGeo/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>public func onGeofencesChange(_ callback: @escaping (</code><code>GeofencesChangeEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeolocationConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onGeofencesChange { event in\n    let on = event.on    // new geofences activated\n    let off = event.off  // geofences that were just de-activated\n\n    // Create map circles\n    on.forEach { geofence in\n        print(\"[geofencesChange] activated: \\(geofence.identifier)\")\n    }\n\n    // Remove map circles\n    off.forEach { identifier in\n        print(\"[geofencesChange] deactivated: \\(identifier)\")\n    }\n}\n</code></pre> <p>geofenceschange</p>"},{"location":"swift/BGGeo/#onheartbeat","title":"onHeartbeat","text":"<p><code>public func onHeartbeat(_ callback: @escaping (</code><code>HeartbeatEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The LocationEvent provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.app.heartbeatInterval = 60\n    config.app.preventSuspend = true\n}\n\nlet subscription = bgGeo.onHeartbeat { event in\n    print(\"[onHeartbeat] \\(event)\")\n\n    // You could request a new location if you wish.\n    Task {\n        do {\n            let location = try await bgGeo.getCurrentPosition(samples: 1, persist: true)\n            print(\"[getCurrentPosition] \\(location)\")\n        } catch {\n            print(\"[getCurrentPosition] ERROR: \\(error)\")\n        }\n    }\n}\n</code></pre> <p>heartbeat</p>"},{"location":"swift/BGGeo/#onhttp","title":"onHttp","text":"<p><code>public func onHttp(_ callback: @escaping (</code><code>HttpEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onHttp { response in\n    let status = response.statusCode\n    let success = response.isSuccess\n    let responseText = response.responseText\n    print(\"[onHttp] \\(response)\")\n}\n</code></pre> <p>http</p>"},{"location":"swift/BGGeo/#onlocation","title":"onLocation","text":"<p><code>public func on</code><code>Location</code><code>(_ callback: @escaping (</code><code>LocationEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>"},{"location":"swift/BGGeo/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a <code>LocationError</code> code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with LocationEvent.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onLocation { location in\n    print(\"[onLocation] success: \\(location)\")\n}\n</code></pre> <p>location</p>"},{"location":"swift/BGGeo/#onmotionchange","title":"onMotionChange","text":"<p><code>public func onMotionChange(_ callback: @escaping (</code><code>LocationEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeolocationConfig.stopTimeout</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onMotionChange { event in\n    if event.isMoving {\n        print(\"[onMotionChange] Device has just started MOVING \\(event)\")\n    } else {\n        print(\"[onMotionChange] Device has just STOPPED: \\(event)\")\n    }\n}\n</code></pre> <p>motionchange</p>"},{"location":"swift/BGGeo/#onpowersavechange","title":"onPowerSaveChange","text":"<pre><code>public func onPowerSaveChange(_ callback: @escaping (PowerSaveChangeEvent) -&gt; Void) -&gt; EventSubscription\n</code></pre> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>"},{"location":"swift/BGGeo/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"swift/BGGeo/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onPowerSaveChange { event in\n    print(\"[onPowerSaveChange] isPowerSaveMode: \\(event.isPowerSaveMode)\")\n}\n</code></pre> <p>powersavechange</p>"},{"location":"swift/BGGeo/#onproviderchange","title":"onProviderChange","text":"<p><code>public func onProviderChange(_ callback: @escaping (</code><code>ProviderChangeEvent</code><code>) -&gt; Void) -&gt; EventSubscription</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onProviderChange { event in\n    print(\"[onProviderChange] \\(event)\")\n\n    switch event.status {\n    case .denied:\n        print(\"- Location authorization denied\")\n    case .authorizedAlways:\n        print(\"- Location always granted\")\n    case .authorizedWhenInUse:\n        print(\"- Location WhenInUse granted\")\n    default:\n        break\n    }\n}\n</code></pre> <p>providerchange</p>"},{"location":"swift/BGGeo/#onschedule","title":"onSchedule","text":"<pre><code>public func onSchedule(_ callback: @escaping (ScheduleEvent) -&gt; Void) -&gt; EventSubscription\n</code></pre> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>let bgGeo = BGGeo.shared\nlet subscription = bgGeo.onSchedule { event in\n    if event.enabled {\n        print(\"[onSchedule] scheduled start tracking\")\n    } else {\n        print(\"[onSchedule] scheduled stop tracking\")\n    }\n}\n</code></pre> <p>schedule</p>"},{"location":"swift/BGGeo/#methods","title":"Methods","text":""},{"location":"swift/BGGeo/#changepace","title":"changePace","text":"<pre><code>public func changePace(_ isMoving: Bool)\n</code></pre> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.changePace(true)  // &lt;-- Location-services ON (\"moving\" state)\nbgGeo.changePace(false) // &lt;-- Location-services OFF (\"stationary\" state)\n</code></pre>"},{"location":"swift/BGGeo/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>public func getCurrentPosition( timeout: TimeInterval = 10, desiredAccuracy: CLLocationAccuracy = 5, maximumAge: Int = 0, samples: Int = 3, allowStale: Bool = true, persist: Bool = true, label: String? = \"getCurrentPosition\", extras: [String: Any]? = nil ) async throws -&gt;</code><code>LocationEvent</code></p> <p>Retrieve the current LocationEvent.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a <code>LocationError</code>.</p>"},{"location":"swift/BGGeo/#options","title":"Options","text":"<p>See <code>CurrentPositionRequest</code>.</p>"},{"location":"swift/BGGeo/#error-codes_1","title":"Error Codes","text":"<p>See <code>LocationError</code>.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with LocationEvent.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let location = try await bgGeo.getCurrentPosition(\n            timeout: 30,\n            desiredAccuracy: 10,\n            maximumAge: 5000,\n            samples: 3,\n            extras: [\"route_id\": 123]\n        )\n        print(\"[getCurrentPosition]\", location)\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#ready","title":"ready","text":"<p><code>public func ready( reset: Bool = true, transistorAuthorizationToken token: TransistorToken? = nil, _ configure: @escaping (</code><code>Config</code><code>) -&gt; Void )</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 10\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n    config.http.url = \"http://your.server.com\"\n    config.http.headers = [\"my-auth-token\": \"secret-token\"]\n}\nprint(\"[ready] success\")\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.config.reset()\n// Reset to documented default-values with overrides\nbgGeo.config.batchUpdate { config in\n    config.geolocation.distanceFilter = 10\n}\n</code></pre>"},{"location":"swift/BGGeo/#removelisteners","title":"removeListeners","text":"<pre><code>public func removeListeners()\n</code></pre> <p>Remove all event listeners.</p> <p>Calls <code>Subscription.remove</code> on all active subscriptions.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.removeListeners()\n</code></pre>"},{"location":"swift/BGGeo/#resetodometer","title":"resetOdometer","text":"<p><code>public func resetOdometer( timeout: TimeInterval = 10, desiredAccuracy: CLLocationAccuracy = kCLLocationAccuracyBest, maximumAge: Int = 5000, samples: Int = 3, persist: Bool = true, extras: [String: Any]? = nil ) async throws -&gt;</code><code>LocationEvent</code></p> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let location = try await bgGeo.setOdometer(0)\n        // This is the location where odometer was set at.\n        print(\"[setOdometer] success: \\(location)\")\n    } catch {\n        print(\"[setOdometer] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#setodometer","title":"setOdometer","text":"<p><code>public func setOdometer( _ value: CLLocationDistance, timeout: TimeInterval = 10, desiredAccuracy: CLLocationAccuracy = kCLLocationAccuracyBest, maximumAge: Int = 5000, samples: Int = 3, persist: Bool = true, extras: [String: Any]? = nil ) async throws -&gt;</code><code>LocationEvent</code></p> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let location = try await bgGeo.setOdometer(1234.56)\n        // This is the location where odometer was set at.\n        print(\"[setOdometer] success: \\(location)\")\n    } catch {\n        print(\"[setOdometer] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#start","title":"start","text":"<pre><code>public func start() async throws\n</code></pre> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        try await bgGeo.start()\n        print(\"[start] success\")\n    } catch {\n        print(\"[start] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#startgeofences","title":"startGeofences","text":"<pre><code>public func startGeofences() async throws\n</code></pre> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\n\n// Add a geofence.\nlet geofence = Geofence(\n    identifier: \"ZONE_OF_INTEREST\",\n    radius: 200,\n    latitude: 37.234232,\n    longitude: 42.234234,\n    notifyOnExit: true\n)\nTask {\n    try await bgGeo.geofences.add(geofence)\n}\n\n// Listen to geofence events.\nlet subscription = bgGeo.onGeofence { event in\n    print(\"[onGeofence] - \\(event)\")\n}\n\n// Configure the plugin\nbgGeo.ready { config in\n    config.http.url = \"http://my.server.com\"\n    config.http.autoSync = true\n}\n\n// Start monitoring geofences.\nTask {\n    try await bgGeo.startGeofences()\n}\n</code></pre>"},{"location":"swift/BGGeo/#startschedule","title":"startSchedule","text":"<pre><code>public func startSchedule()\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.startSchedule()\nprint(\"[startSchedule] success\")\n</code></pre>"},{"location":"swift/BGGeo/#stop","title":"stop","text":"<pre><code>public func stop()\n</code></pre> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.stop()\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (e.g., user logout)\nlet bgGeo = BGGeo.shared\nbgGeo.stopSchedule()\n</code></pre>"},{"location":"swift/BGGeo/#stopschedule","title":"stopSchedule","text":"<pre><code>public func stopSchedule()\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.stopSchedule()\nprint(\"[stopSchedule] success\")\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (e.g., user logout)\nlet bgGeo = BGGeo.shared\nbgGeo.stopSchedule()\nif bgGeo.enabled {\n    bgGeo.stop()\n}\n</code></pre>"},{"location":"swift/BGGeo/#watchposition","title":"watchPosition","text":"<p><code>public func watchPosition( interval: Double = 1000, timeout: TimeInterval = 60, persist: Bool = false, extras: [String: Any]? = nil, success: @escaping (</code><code>LocationEvent</code><code>) -&gt; Void, failure: @escaping (Error) -&gt; Void ) -&gt; Int</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a <code>Subscription</code> that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>"},{"location":"swift/BGGeo/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>let bgGeo = BGGeo.shared\nvar watchId: Int?\n\nfunc onResume() {\n    watchId = bgGeo.watchPosition(\n        interval: 1000,\n        extras: [\"foo\": \"bar\"],\n        success: { location in\n            print(\"[watchPosition] - \\(location)\")\n        },\n        failure: { error in\n            print(\"[watchPosition] ERROR - \\(error)\")\n        }\n    )\n}\n\nfunc onSuspend() {\n    if let id = watchId {\n        bgGeo.stopWatchPosition(id)\n        watchId = nil\n    }\n}\n</code></pre>"},{"location":"swift/BGGeo/#properties","title":"Properties","text":""},{"location":"swift/BGGeo/#app","title":"app","text":"<p><code>public let app =</code><code>App</code><code>()</code></p> <p>App API \u2014 background-task and power-save utilities.</p>"},{"location":"swift/BGGeo/#authorization","title":"authorization","text":"<p><code>public let authorization =</code><code>Authorization</code><code>()</code></p> <p>Authorization API \u2014 request and monitor location permissions.</p>"},{"location":"swift/BGGeo/#config","title":"config","text":"<p><code>public let config =</code><code>Config</code><code>()</code></p> <p>Config API \u2014 read and update the SDK configuration.</p>"},{"location":"swift/BGGeo/#geofences","title":"geofences","text":"<p><code>public let geofences =</code><code>GeofenceManager</code><code>()</code></p> <p>Access to the GeofenceManager \u2014 add, remove, and query geofences.</p> <pre><code>val bgGeo = BGGeo.instance\nbgGeo.geofences.add(geofence)\n</code></pre>"},{"location":"swift/BGGeo/#logger","title":"logger","text":"<p><code>public let logger =</code><code>Logger</code><code>()</code></p> <p>Logger API</p>"},{"location":"swift/BGGeo/#sensors","title":"sensors","text":"<p><code>public let sensors =</code><code>Sensors</code><code>()</code></p> <p>Sensors API \u2014 query the device motion hardware.</p>"},{"location":"swift/BGGeo/#state","title":"state","text":"<p><code>public var state:</code><code>State</code></p> <p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>let state = bgGeo.state\nif state.enabled &amp;&amp; state.isMoving {\n    print(\"Tracking in motion, odometer: \\(state.odometer)\")\n}\n</code></pre>"},{"location":"swift/BGGeo/#store","title":"store","text":"<p><code>public let store =</code><code>DataStore</code><code>()</code></p> <p>DataStore API \u2014 query, upload, and destroy persisted location records.</p>"},{"location":"swift/Battery/","title":"Battery","text":"<p><code>public struct</code><code>Battery</code></p> <p>Device battery state at the time a LocationEvent was recorded.</p>"},{"location":"swift/Battery/#members","title":"Members","text":""},{"location":"swift/Battery/#ischarging","title":"isCharging","text":"<pre><code>public let isCharging: Bool?\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"swift/Battery/#level","title":"level","text":"<pre><code>public let level: Double?\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"swift/Config/","title":"Config","text":"<p><code>public class</code><code>Config</code></p> <p>Root configuration object passed to BGGeo.ready and BGGeo.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"swift/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"swift/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeolocationConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BGGeo.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BGGeo.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"swift/Config/#examples","title":"Examples","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 20\n    config.geolocation.stopTimeout = 5\n    config.geolocation.stationaryRadius = 150\n\n    config.activity.disableStopDetection = false\n\n    config.http.url = \"https://my.server.com/api/locations\"\n    config.http.method = .post\n    config.http.autoSync = true\n    config.http.headers = [\"Authorization\": \"Bearer secret-token\"]\n    config.http.params = [\"user_id\": 123]\n\n    config.persistence.persistMode = .all\n    config.persistence.maxDaysToPersist = 14\n    config.persistence.extras = [\"appVersion\": \"1.0.0\"]\n\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n    config.logger.logMaxDays = 3\n}\n\nprint(\"[ready] BackgroundGeolocation is configured and ready to use\")\n\nif !bgGeo.enabled {\n    Task { try await bgGeo.start() }\n}\n\n// To modify configuration after initialization, use config.batchUpdate.\nbgGeo.config.batchUpdate { config in\n    config.http.headers = [\"Authorization\": \"Bearer new-token\"]\n    config.logger.logLevel = .info\n}\n\nTask { try await bgGeo.store.sync() }\n</code></pre>"},{"location":"swift/Config/#members","title":"Members","text":""},{"location":"swift/Config/#activity","title":"activity","text":"<p><code>public let activity:</code><code>BGGeo</code><code>.</code><code>ActivityConfig</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"swift/Config/#app","title":"app","text":"<p><code>public let app:</code><code>BGGeo</code><code>.</code><code>AppConfig</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"swift/Config/#authorization","title":"authorization","text":"<p><code>public let authorization:</code><code>BGGeo</code><code>.</code><code>AuthorizationConfig</code></p> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"swift/Config/#geolocation","title":"geolocation","text":"<p><code>public let geolocation:</code><code>BGGeo</code><code>.</code><code>GeolocationConfig</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeolocationConfig.</p>"},{"location":"swift/Config/#http","title":"http","text":"<p><code>public let http:</code><code>BGGeo</code><code>.</code><code>HttpConfig</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"swift/Config/#logger","title":"logger","text":"<p><code>public let logger:</code><code>BGGeo</code><code>.</code><code>LoggerConfig</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"swift/Config/#persistence","title":"persistence","text":"<p><code>public let persistence:</code><code>BGGeo</code><code>.</code><code>PersistenceConfig</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"swift/Config/#reset","title":"reset","text":"<pre><code>public func reset()\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BGGeo.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BGGeo.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"swift/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<p><code>public struct</code><code>ConnectivityChangeEvent</code><code>: Sendable</code></p> <p>Network connectivity change delivered to BGGeo.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p>"},{"location":"swift/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"swift/ConnectivityChangeEvent/#isconnected","title":"isConnected","text":"<pre><code>public let isConnected: Bool\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"swift/Coords/","title":"Coords","text":"<p><code>public struct</code><code>Coords</code></p> <p>Geographic coordinates attached to a LocationEvent.</p>"},{"location":"swift/Coords/#members","title":"Members","text":""},{"location":"swift/Coords/#accuracy","title":"accuracy","text":"<pre><code>public let accuracy: Double?\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"swift/Coords/#altitude","title":"altitude","text":"<pre><code>public let altitude: Double?\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"swift/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"swift/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"swift/Coords/#altitudeaccuracy","title":"altitudeAccuracy","text":"<pre><code>public let altitudeAccuracy: Double?\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"swift/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"swift/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"swift/Coords/#ellipsoidalaltitude","title":"ellipsoidalAltitude","text":"<pre><code>public let ellipsoidalAltitude: Double?\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"swift/Coords/#heading","title":"heading","text":"<pre><code>public let heading: Double?\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"swift/Coords/#headingaccuracy","title":"headingAccuracy","text":"<pre><code>public let headingAccuracy: Double?\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"swift/Coords/#latitude","title":"latitude","text":"<pre><code>public let latitude: Double\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"swift/Coords/#longitude","title":"longitude","text":"<pre><code>public let longitude: Double\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"swift/Coords/#speed","title":"speed","text":"<pre><code>public let speed: Double?\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"swift/Coords/#speedaccuracy","title":"speedAccuracy","text":"<pre><code>public let speedAccuracy: Double?\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"swift/DataStore/","title":"DataStore","text":"<p><code>public class</code><code>DataStore</code></p> <p>Persisted location record management \u2014 query, upload, and destroy stored locations.</p> <p>Access via <code>BGGeo.instance.store</code>.</p>"},{"location":"swift/DataStore/#members","title":"Members","text":""},{"location":"swift/DataStore/#all","title":"all","text":"<pre><code>public func all() async throws -&gt; [[String: Any]]\n</code></pre> <p>Retrieve all LocationEvent records stored in the SDK's SQLite database.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let locations = try await bgGeo.store.all()\n        print(\"[getLocations]\", locations)\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/DataStore/#count","title":"count","text":"<pre><code>public var count: Int\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>let bgGeo = BGGeo.shared\nlet count = bgGeo.store.count\n</code></pre>"},{"location":"swift/DataStore/#destroy","title":"destroy","text":"<pre><code>public func destroy(_ uuid: String)\n</code></pre> <p>Remove a single location by LocationEvent.uuid.</p> <pre><code>let bgGeo = BGGeo.shared\nlet uuid = \"some-location-uuid\"\nbgGeo.store.destroy(uuid)\n</code></pre>"},{"location":"swift/DataStore/#destroyall","title":"destroyAll","text":"<pre><code>public func destroyAll() async throws\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        try await bgGeo.store.destroyAll()\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/DataStore/#sync","title":"sync","text":"<pre><code>public func sync() async throws -&gt; [[String: Any]]\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let records = try await bgGeo.store.sync()\n        print(\"[sync] success: \\(records)\")\n    } catch {\n        print(\"[sync] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/DeviceSettings/","title":"DeviceSettings","text":""},{"location":"swift/DeviceSettings/#members","title":"Members","text":""},{"location":"swift/DeviceSettings/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>public var isPowerSaveMode: Bool\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>"},{"location":"swift/DeviceSettings/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"swift/DeviceSettings/#android","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\nlet isPowerSaveMode = bgGeo.deviceSettings.isPowerSaveMode\n</code></pre>"},{"location":"swift/Geofence/","title":"Geofence","text":"<p><code>public struct</code><code>Geofence</code></p> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"swift/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"swift/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeolocationConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"swift/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BGGeo.addGeofence for a single geofence, or BGGeo.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p>"},{"location":"swift/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BGGeo.onGeofence. Subscribe to changes in the actively monitored set with BGGeo.onGeofencesChange.</p> <pre><code>let bgGeo = BGGeo.shared\n\n// Listen for geofence events.\nlet sub = bgGeo.onGeofence { geofence in\n    print(\"[geofence] \\(geofence.identifier) \\(geofence.action)\")\n}\n</code></pre> <p>Note</p> <p>When all geofences are removed, BGGeo.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"swift/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>let bgGeo = BGGeo.shared\n\nlet sub = bgGeo.onGeofencesChange { event in\n    let on = event.on   // &lt;-- new geofences activated.\n    let off = event.off // &lt;-- geofences that were just de-activated.\n\n    // Create map circles\n    on.forEach { geofence in\n        print(\"[geofencesChange] activated: \\(geofence.identifier)\")\n    }\n\n    // Remove map circles\n    off.forEach { identifier in\n        print(\"[geofencesChange] deactivated: \\(identifier)\")\n    }\n}\n</code></pre>"},{"location":"swift/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeolocationConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeolocationConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"swift/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        try await bgGeo.geofences.remove(\"HOME\")\n        print(\"[removeGeofence] success\")\n    } catch {\n        print(\"[removeGeofence] FAILURE: \\(error)\")\n    }\n}\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        try await bgGeo.geofences.removeAll()\n        print(\"[removeGeofences] all geofences have been destroyed\")\n    } catch {\n        print(\"[removeGeofences] FAILURE: \\(error)\")\n    }\n}\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\n\nlet geofences = bgGeo.geofences.getAll()\nprint(\"[getGeofences] \\(geofences)\")\n</code></pre>"},{"location":"swift/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BGGeo.startGeofences instead of BGGeo.start to monitor geofences without continuous location tracking. Use GeolocationConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>let bgGeo = BGGeo.shared\n\nlet sub = bgGeo.onGeofence { geofence in\n    print(\"[geofence] \\(geofence)\")\n}\n\nbgGeo.ready { config in\n    config.http.url = \"http://your.server.com/geofences\"\n    config.http.autoSync = true\n}\n\nTask {\n    do {\n        // engage geofences-only mode:\n        try await bgGeo.startGeofences()\n    } catch {\n        print(\"[startGeofences] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Geofence/#examples","title":"Examples","text":"<pre><code>let bgGeo = BGGeo.shared\n\n// Listen to geofence events\nlet sub = bgGeo.onGeofence { geofence in\n    print(\"[geofence] \\(geofence)\")\n    if geofence.identifier == \"DANGER_ZONE\" {\n        if geofence.action == \"ENTER\" {\n            // Entering the danger-zone, we want to aggressively track location.\n            Task { try await bgGeo.start() }\n        } else if geofence.action == \"EXIT\" {\n            // Exiting the danger-zone, we resume geofences-only tracking.\n            Task { try await bgGeo.startGeofences() }\n        }\n    }\n}\n\n// Add a geofence.\nlet geofence = Geofence(\n    identifier: \"DANGER_ZONE\",\n    radius: 1000,\n    latitude: 45.51921926,\n    longitude: -73.61678581,\n    notifyOnEntry: true,\n    notifyOnExit: true\n)\n\nTask {\n    try await bgGeo.geofences.add(geofence)\n}\n\n// Ready the plugin.\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 10\n    config.http.url = \"http://your.server.com/locations\"\n    config.http.autoSync = true\n}\n\nTask {\n    try await bgGeo.startGeofences()\n}\n</code></pre>"},{"location":"swift/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>let bgGeo = BGGeo.shared\n\nlet geofence = Geofence(\n    identifier: \"Home\",\n    radius: 200,\n    latitude: 45.51921926,\n    longitude: -73.61678581,\n    notifyOnEntry: true,\n    notifyOnExit: true,\n    extras: [\"route_id\": 1234]\n)\n\nTask {\n    do {\n        try await bgGeo.geofences.add(geofence)\n        print(\"[addGeofence] success\")\n    } catch {\n        print(\"[addGeofence] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>let bgGeo = BGGeo.shared\n\nlet geofences = [\n    Geofence(\n        identifier: \"Home\",\n        radius: 200,\n        latitude: 45.51921926,\n        longitude: -73.61678581,\n        notifyOnEntry: true\n    ),\n    Geofence(\n        identifier: \"Work\",\n        radius: 200,\n        latitude: 45.61921927,\n        longitude: -73.71678582,\n        notifyOnEntry: true\n    )\n]\n\nTask {\n    do {\n        try await bgGeo.geofences.addAll(geofences)\n        print(\"[addGeofences] success\")\n    } catch {\n        print(\"[addGeofences] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Geofence/#members","title":"Members","text":""},{"location":"swift/Geofence/#entrystate","title":"entryState","text":"<p><code>public let entry</code><code>State</code><code>: EntryState</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"swift/Geofence/#extras","title":"extras","text":"<pre><code>public let extras: [String: Any]?\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"swift/Geofence/#hits","title":"hits","text":"<pre><code>public let hits: Int\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"swift/Geofence/#identifier","title":"identifier","text":"<pre><code>public let identifier: String\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"swift/Geofence/#latitude","title":"latitude","text":"<pre><code>public let latitude: Double\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"swift/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>public let loiteringDelay: Double\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"swift/Geofence/#longitude","title":"longitude","text":"<pre><code>public let longitude: Double\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"swift/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>public let notifyOnDwell: Bool\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"swift/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>public let notifyOnEntry: Bool\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeolocationConfig.geofenceInitialTriggerEntry</p>"},{"location":"swift/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>public let notifyOnExit: Bool\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"swift/Geofence/#radius","title":"radius","text":"<pre><code>public let radius: Double\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"swift/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>public let stateUpdatedAt: Date?\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"swift/Geofence/#vertices","title":"vertices","text":"<pre><code>public let vertices: [[Double]]?\n</code></pre> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\n\nlet geofence = Geofence(\n    identifier: \"Home\",\n    vertices: [\n        [45.518947279987714, -73.6049889209514],   // &lt;-- [lat, lng]\n        [45.5182711292279, -73.60338649600598],\n        [45.517082240237634, -73.60432670908212],\n        [45.51774871402813, -73.60604928622278]\n    ],\n    notifyOnEntry: true,\n    notifyOnExit: true\n)\n\nTask {\n    do {\n        try await bgGeo.geofences.add(geofence)\n    } catch {\n        print(\"[addGeofence] FAILURE: \\(error)\")\n    }\n}\n</code></pre> <p> </p>"},{"location":"swift/GeofenceEvent/","title":"GeofenceEvent","text":"<p><code>public struct</code><code>GeofenceEvent</code></p> <p>Geofence transition delivered to BGGeo.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p>"},{"location":"swift/GeofenceEvent/#members","title":"Members","text":""},{"location":"swift/GeofenceEvent/#action","title":"action","text":"<pre><code>public let action: String\n</code></pre> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"swift/GeofenceEvent/#extras","title":"extras","text":"<pre><code>public let extras: [String: Any]?\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"swift/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>public let identifier: String\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"swift/GeofenceEvent/#location","title":"location","text":"<p><code>public let location:</code><code>BGGeo</code><code>.</code><code>LocationEvent</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"swift/GeofenceManager/","title":"GeofenceManager","text":"<p><code>public class</code><code>GeofenceManager</code></p> <p>Manages the geofence database \u2014 add, remove, query, and check geofences.</p> <p>Access via <code>BGGeo.instance.geofences</code>. All methods are suspending coroutines (Kotlin) or async/await (Swift).</p>"},{"location":"swift/GeofenceManager/#methods","title":"Methods","text":"Method Description [add] Add a single geofence. [addAll] Add multiple geofences in bulk (~10\u00d7 faster than repeated [add]). [remove] Remove a geofence by identifier. [removeAll] Remove all geofences, or a specific subset. [get] Fetch a single geofence by identifier. [getAll] Fetch all stored geofences. [exists] Check whether a geofence with a given identifier exists."},{"location":"swift/GeofenceManager/#members","title":"Members","text":""},{"location":"swift/GeofenceManager/#add","title":"add","text":"<p><code>public func add(_ geofence:</code><code>BGGeo</code><code>.</code><code>Geofence</code><code>) async throws</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet geofence = Geofence(\n    identifier: \"Home\",\n    radius: 150,\n    latitude: 45.51921926,\n    longitude: -73.61678581,\n    notifyOnEntry: true,\n    notifyOnExit: false,\n    notifyOnDwell: true,\n    loiteringDelay: 30000,  // 30 seconds\n    extras: [\"zone_id\": 1234]\n)\n\nTask {\n    do {\n        try await bgGeo.geofences.add(geofence)\n        print(\"[addGeofence] success\")\n    } catch {\n        print(\"[addGeofence] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/GeofenceManager/#addall","title":"addAll","text":"<p><code>public func addAll(_ geofences: [</code><code>BGGeo</code><code>.</code><code>Geofence</code><code>]) async throws</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet geofences = [\n    Geofence(\n        identifier: \"foo\",\n        radius: 200,\n        latitude: 45.51921926,\n        longitude: -73.61678581,\n        notifyOnEntry: true\n    ),\n    Geofence(\n        identifier: \"bar\",\n        radius: 200,\n        latitude: 45.51921926,\n        longitude: -73.61678581,\n        notifyOnEntry: true\n    )\n]\n\nTask {\n    do {\n        try await bgGeo.geofences.addAll(geofences)\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/GeofenceManager/#exists","title":"exists","text":"<pre><code>public func exists(_ identifier: String) async -&gt; Bool\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    let exists = await bgGeo.geofences.exists(\"HOME\")\n    print(\"[geofenceExists]\", exists)\n}\n</code></pre>"},{"location":"swift/GeofenceManager/#get","title":"get","text":"<p><code>public func get(_ identifier: String) async throws -&gt;</code><code>BGGeo</code><code>.</code><code>Geofence</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let geofence = try await bgGeo.geofences.get(\"HOME\")\n        print(\"[getGeofence]\", geofence)\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/GeofenceManager/#getall","title":"getAll","text":"<p><code>public func getAll() async -&gt; [</code><code>BGGeo</code><code>.</code><code>Geofence</code><code>]</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nlet geofences = bgGeo.geofences.getAll()\nprint(\"[getGeofences]\", geofences)\n</code></pre>"},{"location":"swift/GeofenceManager/#remove","title":"remove","text":"<pre><code>public func remove(_ identifier: String) async throws\n</code></pre> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        try await bgGeo.geofences.remove(\"Home\")\n        print(\"[removeGeofence] success\")\n    } catch {\n        print(\"[removeGeofence] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/GeofenceManager/#removeall","title":"removeAll","text":"<pre><code>public func removeAll(_ identifiers: [String]? = nil) async throws\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    try await bgGeo.geofences.removeAll()\n}\n</code></pre>"},{"location":"swift/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<p><code>public struct</code><code>GeofencesChangeEvent</code></p> <p>Geofence monitoring change delivered to BGGeo.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p>"},{"location":"swift/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"swift/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>public let off: [String]\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"swift/GeofencesChangeEvent/#on","title":"on","text":"<p><code>public let on: [</code><code>BGGeo</code><code>.</code><code>Geofence</code><code>]</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"swift/GeolocationConfig/","title":"GeolocationConfig","text":"<p><code>public class</code><code>GeolocationConfig</code></p> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"swift/GeolocationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"swift/GeolocationConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BGGeo.ready or BGGeo.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 50\n    config.geolocation.stopTimeout = 5\n    config.geolocation.stopAfterElapsedMinutes = 120\n    config.geolocation.showsBackgroundLocationIndicator = true\n\n    config.geolocation.filter.policy = .adjust\n    config.geolocation.filter.maxImpliedSpeed = 60\n    config.geolocation.filter.odometerAccuracyThreshold = 20\n    config.geolocation.filter.trackingAccuracyThreshold = 100\n\n    config.geolocation.geofenceProximityRadius = 1000\n    config.geolocation.geofenceInitialTriggerEntry = true\n\n    config.geolocation.locationAuthorizationRequest = .always\n    config.geolocation.disableLocationAuthorizationAlert = false\n    config.geolocation.locationAuthorizationAlert = [\n        \"titleWhenNotEnabled\": \"Location Required\",\n        \"message\": \"Enable location access for full functionality.\",\n        \"cancelButton\": \"Cancel\",\n        \"settingsButton\": \"Settings\"\n    ]\n\n    config.http.url = \"https://example.com/api/locations\"\n    config.http.autoSync = true\n    config.http.batchSync = true\n    config.http.maxBatchSize = 10\n    config.http.method = .post\n    config.http.params = [\"user_id\": 1234, \"trip_id\": 5678]\n    config.http.headers = [\"X-FOO\": \"bar\"]\n\n    config.app.stopOnTerminate = false\n    config.app.startOnBoot = true\n\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only <code>DesiredAccuracy.High</code> activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"swift/GeolocationConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"swift/GeolocationConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BGGeo.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"swift/GeolocationConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BGGeo.onProviderChange.</p>"},{"location":"swift/GeolocationConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BGGeo.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"swift/GeolocationConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilterConfig for full documentation.</p>"},{"location":"swift/GeolocationConfig/#examples","title":"Examples","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 10\n    config.geolocation.stopTimeout = 5\n    config.geolocation.stationaryRadius = 150\n    config.geolocation.locationTimeout = 60\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n    config.geolocation.distanceFilter = 10\n    config.geolocation.stopTimeout = 5\n    config.geolocation.stationaryRadius = 150\n    config.geolocation.locationTimeout = 60\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#members","title":"Members","text":""},{"location":"swift/GeolocationConfig/#activitytype","title":"activityType","text":"<pre><code>public var activityType: CLActivityType\n</code></pre> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on <code>ActivityType</code>.</p> Name <code>ActivityType.Other</code> <code>ActivityType.AutomotiveNavigation</code> <code>ActivityType.Fitness</code> <code>ActivityType.OtherNavigation</code> <code>ActivityType.Airborne</code> <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.activityType = .other\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<pre><code>public var desiredAccuracy: CLLocationAccuracy\n</code></pre> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to <code>DesiredAccuracy.High</code>.</p> Name Location Providers Description <code>DesiredAccuracy.Navigation</code> (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy <code>DesiredAccuracy.High</code> GPS + Wifi + Cellular Highest power; highest accuracy <code>DesiredAccuracy.Medium</code> Wifi + Cellular Medium power; Medium accuracy; <code>DesiredAccuracy.Low</code> Wifi (low power) + Cellular Lower power; No GPS <code>DesiredAccuracy.VeryLow</code> Cellular only Lowest power; lowest accuracy <code>DesiredAccuracy.Lowest</code> (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only <code>DesiredAccuracy.High</code> uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>public var disableElasticity: Bool\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"swift/GeolocationConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>public var disableLocationAuthorizationAlert: Bool\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BGGeo.onProviderChange.</p>"},{"location":"swift/GeolocationConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"swift/GeolocationConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested <code>DesiredAccuracy.High</code> (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BGGeo.onProviderChange - BGGeo.start - BGGeo.requestPermission</p> <pre><code>let bgGeo = BGGeo.shared\n\nlet subscription = bgGeo.onProviderChange { event in\n    print(\"[onProviderChange] \\(event)\")\n\n    if !event.enabled {\n        // Show your own alert using UIKit\n    }\n}\n\nbgGeo.ready { config in\n    config.geolocation.disableLocationAuthorizationAlert = true\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>public var distanceFilter: CLLocationDistance\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"swift/GeolocationConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"swift/GeolocationConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"swift/GeolocationConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>public var elasticityMultiplier: Double\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"swift/GeolocationConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>public var enableTimestampMeta: Bool\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS LocationEvent.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"swift/GeolocationConfig/#android_1","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#ios_1","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"swift/GeolocationConfig/#filter","title":"filter","text":"<p><code>public let filter:</code><code>BGGeo</code><code>.</code><code>LocationFilterConfig</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeolocationConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"swift/GeolocationConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilterConfig.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilterConfig.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilterConfig.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilterConfig.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilterConfig.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilterConfig.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilterConfig.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilterConfig.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilterConfig.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilterConfig.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilterConfig.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilterConfig.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .adjust\n    config.geolocation.filter.useKalman = true\n    config.geolocation.filter.kalmanProfile = .default\n    config.geolocation.filter.trackingAccuracyThreshold = 100\n    config.geolocation.filter.odometerAccuracyThreshold = 20\n}\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .passThrough\n    config.geolocation.filter.useKalman = false\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>public var geofenceInitialTriggerEntry: Bool\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"swift/GeolocationConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>public var geofenceProximityRadius: CLLocationDistance\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BGGeo.onGeofencesChange.</p>"},{"location":"swift/GeolocationConfig/#ios_2","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"swift/GeolocationConfig/#android_2","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"swift/GeolocationConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>public var locationAuthorizationAlert: [String: Any]\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.locationAuthorizationAlert = [\n        \"titleWhenNotEnabled\": \"Location services are not enabled\",\n        \"titleWhenOff\": \"Location services are OFF\",\n        \"instructions\": \"Please enable 'Always' in Location Services to allow background tracking.\",\n        \"cancelButton\": \"Cancel\",\n        \"settingsButton\": \"Open Settings\"\n    ]\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<p><code>public var locationAuthorizationRequest:</code><code>LocationAuthorizationRequest</code></p> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"swift/GeolocationConfig/#ios_3","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\n\n// Start with When-In-Use\nbgGeo.ready { config in\n    config.geolocation.locationAuthorizationRequest = .whenInUse\n}\n\nfunc onClickStartTracking() {\n    Task {\n        try await bgGeo.start()\n\n        // Later -- you may upgrade to Always at any time.\n        bgGeo.config.batchUpdate { config in\n            config.geolocation.locationAuthorizationRequest = .always\n        }\n    }\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#android_3","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.locationAuthorizationRequest = .always\n    // backgroundPermissionRationale is Android only \u2014 not applicable on iOS\n}\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"swift/GeolocationConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>public var pausesLocationUpdatesAutomatically: Bool\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BGGeo.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"swift/GeolocationConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>public var showsBackgroundLocationIndicator: Bool\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"swift/GeolocationConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>public var stationaryRadius: CLLocationDistance\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"swift/GeolocationConfig/#ios_4","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"swift/GeolocationConfig/#android_4","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"swift/GeolocationConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>public var stopAfterElapsedMinutes: TimeInterval\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BGGeo.stop after this many minutes have elapsed since BGGeo.start was called.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.ready { config in\n    config.geolocation.stopAfterElapsedMinutes = 30\n}\n\nTask {\n    try await bgGeo.start()  // &lt;-- plugin will automatically stop in 30 minutes\n}\n</code></pre>"},{"location":"swift/GeolocationConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>public var stopTimeout: TimeInterval\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BGGeo.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"swift/GeolocationConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>public var useSignificantChangesOnly: Bool\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"swift/GeolocationConfig/#ios_5","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"swift/GeolocationConfig/#android_5","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent <code>NotificationConfig</code> notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"swift/HeartbeatEvent/","title":"HeartbeatEvent","text":"<p><code>public struct</code><code>HeartbeatEvent</code></p> <p>Periodic heartbeat delivered to BGGeo.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p>"},{"location":"swift/HeartbeatEvent/#members","title":"Members","text":""},{"location":"swift/HeartbeatEvent/#location","title":"location","text":"<pre><code>public let location: CLLocation\n</code></pre> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BGGeo.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"swift/HttpConfig/","title":"HttpConfig","text":"<p><code>public class</code><code>HttpConfig</code></p> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"swift/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"swift/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://example.com/locations\"\n    config.http.autoSync = true\n    config.http.params = [\"user_id\": 1234]\n}\n</code></pre>"},{"location":"swift/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BGGeo.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BGGeo.getCount or fetch records with BGGeo.getLocations.</p>"},{"location":"swift/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"swift/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"swift/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BGGeo.onConnectivityChange fires - BGGeo.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p>"},{"location":"swift/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BGGeo.start <code>\"stop\"</code> \u2014 BGGeo.stop <code>\"startGeofences\"</code> \u2014 BGGeo.startGeofences <code>\"changePace\"</code> <code>boolean</code> BGGeo.changePace <code>\"setConfig\"</code> <code>{Config}</code> BGGeo.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BGGeo.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BGGeo.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BGGeo.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"swift/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"swift/HttpConfig/#examples","title":"Examples","text":""},{"location":"swift/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        let records = try await bgGeo.store.sync()\n    } catch {\n        print(\"[sync] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://api.example.com\"\n    config.http.autoSync = true\n    config.http.headers = [\"Authorization\": \"Bearer ...\"]\n}\n</code></pre>"},{"location":"swift/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://api.example.com/locations\"\n    config.http.method = .post\n    config.http.autoSync = true\n    config.http.headers = [\"Authorization\": \"Bearer secret\"]\n    config.http.params = [\"device_id\": \"abc-123\"]\n}\n</code></pre>"},{"location":"swift/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://api.example.com/locations/bulk\"\n    config.http.autoSync = true\n    config.http.batchSync = true\n    config.http.maxBatchSize = 25\n    config.http.autoSyncThreshold = 10\n    config.http.rootProperty = \"locations\"\n}\n</code></pre>"},{"location":"swift/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://api.example.com/locations\"\n    config.http.autoSync = true\n    config.http.disableAutoSyncOnCellular = true\n}\n</code></pre>"},{"location":"swift/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.config.batchUpdate { config in\n    config.http.url = \"https://api.example.com\"\n    config.http.autoSync = false\n}\n\nTask {\n    do {\n        let records = try await bgGeo.store.sync()\n    } catch {\n        print(\"[sync] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/HttpConfig/#members","title":"Members","text":""},{"location":"swift/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>public var autoSync: Bool\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BGGeo.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BGGeo.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"swift/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>public var autoSyncThreshold: Int\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BGGeo.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"swift/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>public var batchSync: Bool\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"swift/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>public var disableAutoSyncOnCellular: Bool\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BGGeo.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BGGeo.sync</p>"},{"location":"swift/HttpConfig/#headers","title":"headers","text":"<pre><code>public var headers: [String: Any]\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://my.server.com\"\n    config.http.headers = [\n        \"Authorization\": \"Bearer &lt;a secret key&gt;\",\n        \"X-FOO\": \"BAR\"\n    ]\n}\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"swift/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>public var maxBatchSize: Int\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"swift/HttpConfig/#method","title":"method","text":"<p><code>public var method:</code><code>HttpMethod</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://my-server.com/locations\"\n    config.http.method = .put\n}\n</code></pre>"},{"location":"swift/HttpConfig/#params","title":"params","text":"<pre><code>public var params: [String: Any]\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://my-server.com/locations\"\n    config.http.params = [\n        \"user_id\": 1234,\n        \"device_id\": \"abc123\"\n    ]\n}\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"swift/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>public var rootProperty: String\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.rootProperty = \"myData\"\n    config.http.url = \"https://my.server.com\"\n}\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"swift/HttpConfig/#timeout","title":"timeout","text":"<pre><code>public var timeout: Int\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BGGeo.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>let bgGeo = BGGeo.shared\n\nlet sub = bgGeo.onHttp { response in\n    if !response.isSuccess {\n        print(\"[onHttp] FAILURE: \\(response)\")\n    }\n}\n\nbgGeo.ready { config in\n    config.http.url = \"https://my-server.com/locations\"\n    config.http.timeout = 3000   // 3-second timeout\n}\n</code></pre>"},{"location":"swift/HttpConfig/#url","title":"url","text":"<pre><code>public var url: String\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>let bgGeo = BGGeo.shared\n\n// Listen to HTTP events.\nlet sub = bgGeo.onHttp { event in\n    print(\"[onHttp] \\(event)\")\n}\n\nbgGeo.ready { config in\n    config.http.url = \"https://my-server.com/locations\"\n    config.http.params = [\"user_id\": 1234]\n    config.http.headers = [\"Authorization\": \"Basic my-secret-key\"]\n    config.http.autoSync = true\n    config.http.method = .post\n    config.app.stopOnTerminate = false\n}\n</code></pre>"},{"location":"swift/HttpEvent/","title":"HttpEvent","text":"<p><code>public struct</code><code>HttpEvent</code><code>: Sendable</code></p> <p>HTTP response payload delivered to BGGeo.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p>"},{"location":"swift/HttpEvent/#members","title":"Members","text":""},{"location":"swift/HttpEvent/#issuccess","title":"isSuccess","text":"<pre><code>public let isSuccess: Bool\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"swift/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>public let responseText: String\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"swift/HttpEvent/#statuscode","title":"statusCode","text":"<pre><code>public let statusCode: Int\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"swift/HttpMethod/","title":"HttpMethod","text":"<p><code>public enum</code><code>HttpMethod</code><code>: String</code></p> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"swift/HttpMethod/#members","title":"Members","text":""},{"location":"swift/HttpMethod/#patch","title":"patch","text":"<pre><code>case patch = \"PATCH\"\n</code></pre> <p>HTTP PATCH method.</p>"},{"location":"swift/HttpMethod/#post","title":"post","text":"<pre><code>case post = \"POST\"\n</code></pre> <p>HTTP POST method.</p>"},{"location":"swift/HttpMethod/#put","title":"put","text":"<pre><code>case put = \"PUT\"\n</code></pre> <p>HTTP PUT method.</p>"},{"location":"swift/KalmanProfile/","title":"KalmanProfile","text":"<p><code>public enum</code><code>KalmanProfile</code><code>: Int</code></p> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"swift/KalmanProfile/#members","title":"Members","text":""},{"location":"swift/KalmanProfile/#aggressive","title":"aggressive","text":"<pre><code>case aggressive = 1\n</code></pre> <p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"swift/KalmanProfile/#conservative","title":"conservative","text":"<pre><code>case conservative = 2\n</code></pre> <p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"swift/KalmanProfile/#default","title":"default","text":"<pre><code>case `default` = 0\n</code></pre> <p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"swift/LocationAuthorizationRequest/","title":"LocationAuthorizationRequest","text":"<p><code>public enum</code><code>LocationAuthorizationRequest</code><code>: String</code></p> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeolocationConfig.locationAuthorizationRequest</p>"},{"location":"swift/LocationAuthorizationRequest/#members","title":"Members","text":""},{"location":"swift/LocationAuthorizationRequest/#always","title":"always","text":"<pre><code>case always = \"Always\"\n</code></pre> <p>Request full background + foreground authorization.</p>"},{"location":"swift/LocationAuthorizationRequest/#any","title":"any","text":"<pre><code>case any = \"Any\"\n</code></pre> <p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"swift/LocationAuthorizationRequest/#wheninuse","title":"whenInUse","text":"<pre><code>case whenInUse = \"WhenInUse\"\n</code></pre> <p>Request foreground-only authorization.</p>"},{"location":"swift/LocationEvent/","title":"LocationEvent","text":"<p><code>public struct</code><code>LocationEvent</code></p> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"swift/LocationEvent/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"swift/LocationEvent/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"swift/LocationEvent/#javascript-schema","title":"JavaScript schema","text":"<pre><code>// LocationEvent properties:\n// location.timestamp        // Date?\n// location.isMoving         // Bool\n// location.odometer         // Double?\n// location.coords?.latitude  // Double\n// location.coords?.longitude // Double\n// location.coords?.accuracy  // Double?\n// location.coords?.speed     // Double?\n// location.coords?.heading   // Double?\n// location.coords?.altitude  // Double?\n// location.coords?.ellipsoidalAltitude // Double?\n// location.activity?.type    // String? (still|on_foot|walking|running|in_vehicle|on_bicycle)\n// location.activity?.confidence // Int? (0-100)\n// location.battery?.level    // Double?\n// location.battery?.isCharging // Bool?\n</code></pre>"},{"location":"swift/LocationEvent/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>// HTTP POST schema \u2014 the JSON body posted to your server:\n// {\n//   \"location\": {\n//     \"coords\": {\n//       \"latitude\":   Double,\n//       \"longitude\":  Double,\n//       \"accuracy\":   Double,\n//       \"speed\":      Double,\n//       \"heading\":    Double,\n//       \"altitude\":   Double,\n//       \"ellipsoidal_altitude\": Double\n//     },\n//     \"extras\": { ... },\n//     \"activity\": { \"type\": String, \"confidence\": Int },\n//     \"geofence\": { \"identifier\": String, \"action\": String },\n//     \"battery\":  { \"level\": Double, \"is_charging\": Bool },\n//     \"timestamp\": String,   // ISO-8601 UTC\n//     \"uuid\":      String,\n//     \"event\":     String,   // motionchange|geofence|heartbeat\n//     \"is_moving\": Bool,\n//     \"odometer\":  Double    // meters\n//   }\n// }\n</code></pre>"},{"location":"swift/LocationEvent/#members","title":"Members","text":""},{"location":"swift/LocationEvent/#activity","title":"activity","text":"<p><code>public let activity:</code><code>Activity</code><code>?</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"swift/LocationEvent/#age","title":"age","text":"<pre><code>public let age: Int?\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"swift/LocationEvent/#battery","title":"battery","text":"<p><code>public let battery:</code><code>Battery</code><code>?</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"swift/LocationEvent/#coords","title":"coords","text":"<p><code>public let coords:</code><code>Coords</code><code>?</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"swift/LocationEvent/#event","title":"event","text":"<pre><code>public let event: String?\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"swift/LocationEvent/#extras","title":"extras","text":"<pre><code>public let extras: [String: Any]?\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"swift/LocationEvent/#geofence","title":"geofence","text":"<pre><code>public let geofence: GeofenceTrigger?\n</code></pre> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"swift/LocationEvent/#ismoving","title":"isMoving","text":"<pre><code>public let isMoving: Bool\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"swift/LocationEvent/#mock","title":"mock","text":"<pre><code>public let mock: Bool?\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"swift/LocationEvent/#odometer","title":"odometer","text":"<pre><code>public let odometer: Double?\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"swift/LocationEvent/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilterConfig evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilterConfig.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via LocationEvent.odometer_error.</li> </ul>"},{"location":"swift/LocationEvent/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"swift/LocationEvent/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilterConfig - LocationFilterConfig.odometerAccuracyThreshold - BGGeo.resetOdometer - BGGeo.getOdometer</p> <pre><code>let bgGeo = BGGeo.shared\n\nlet sub = bgGeo.onLocation { location in\n    print(\"Distance traveled:\", location.odometer)\n}\n\n// Reset at start of a trip\nTask {\n    do {\n        try await bgGeo.setOdometer(0)\n    } catch {\n        print(\"[setOdometer] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/LocationEvent/#odometererror","title":"odometerError","text":"<pre><code>public let odometerError: Double?\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"swift/LocationEvent/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>let bgGeo = BGGeo.shared\n\nlet sub = bgGeo.onLocation { location in\n    print(\"Odometer:\", location.odometer)\n    // Note: odometer_error may be available via location.data dictionary\n}\n</code></pre>"},{"location":"swift/LocationEvent/#recordedat","title":"recordedAt","text":"<pre><code>public let recordedAt: Date?\n</code></pre> <p>Timestamp when the SDK received the location from the native API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code>.</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"swift/LocationEvent/#sample","title":"sample","text":"<pre><code>public let sample: Bool\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BGGeo.getCurrentPosition to find the most accurate fix. These samples are delivered to BGGeo.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"swift/LocationEvent/#timestamp","title":"timestamp","text":"<pre><code>public let timestamp: Date?\n</code></pre> <p>Timestamp provided by the native location API.</p> <ul> <li>When PersistenceConfig.timestampFormat is <code>\"iso\"</code> (default): ISO-8601 UTC <code>string</code> (eg <code>\"2025-04-10T12:00:01.123Z\"</code>).</li> <li>When PersistenceConfig.timestampFormat is <code>\"epoch\"</code>: epoch milliseconds <code>number</code>.</li> </ul>"},{"location":"swift/LocationEvent/#uuid","title":"uuid","text":"<pre><code>public let uuid: String?\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"swift/LocationFilterConfig/","title":"LocationFilterConfig","text":"<p><code>public class</code><code>LocationFilterConfig</code></p> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeolocationConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"swift/LocationFilterConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"swift/LocationFilterConfig/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .adjust\n    config.geolocation.filter.useKalman = true\n    config.geolocation.filter.kalmanProfile = .default\n    config.geolocation.filter.trackingAccuracyThreshold = 100\n    config.geolocation.filter.odometerAccuracyThreshold = 20\n}\n</code></pre>"},{"location":"swift/LocationFilterConfig/#examples","title":"Examples","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .passThrough\n    config.geolocation.filter.useKalman = false\n}\n</code></pre>"},{"location":"swift/LocationFilterConfig/#members","title":"Members","text":""},{"location":"swift/LocationFilterConfig/#burstwindow","title":"burstWindow","text":"<pre><code>public var burstWindow: Double\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"swift/LocationFilterConfig/#filterdebug","title":"filterDebug","text":"<pre><code>public var filterDebug: Bool\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"swift/LocationFilterConfig/#kalmandebug","title":"kalmanDebug","text":"<pre><code>public var kalmanDebug: Bool\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"swift/LocationFilterConfig/#kalmanprofile","title":"kalmanProfile","text":"<p><code>public var kalmanProfile:</code><code>KalmanProfile</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"swift/LocationFilterConfig/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>public var maxBurstDistance: Double\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"swift/LocationFilterConfig/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>public var maxImpliedSpeed: Double\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"swift/LocationFilterConfig/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>public var odometerAccuracyThreshold: CLLocationAccuracy\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"swift/LocationFilterConfig/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>public var odometerUseKalmanFilter: Bool\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilterConfig.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilterConfig.useKalman and LocationFilterConfig.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"swift/LocationFilterConfig/#policy","title":"policy","text":"<p><code>public var policy:</code><code>LocationFilterPolicy</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BGGeo.onLocation. For more granular tuning, see LocationFilterConfig.trackingAccuracyThreshold, LocationFilterConfig.maxImpliedSpeed, and other LocationFilterConfig fields.</p> <p>See also - GeolocationConfig.filter - LocationFilterConfig - KalmanProfile</p>"},{"location":"swift/LocationFilterConfig/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .adjust\n}\n</code></pre>"},{"location":"swift/LocationFilterConfig/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .passThrough\n}\n</code></pre>"},{"location":"swift/LocationFilterConfig/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .conservative\n}\n</code></pre>"},{"location":"swift/LocationFilterConfig/#rollingwindow","title":"rollingWindow","text":"<pre><code>public var rollingWindow: Int\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"swift/LocationFilterConfig/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>public var trackingAccuracyThreshold: CLLocationAccuracy\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"swift/LocationFilterConfig/#usekalman","title":"useKalman","text":"<pre><code>public var useKalman: Bool\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"swift/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<p><code>public enum</code><code>LocationFilterPolicy</code><code>: Int</code></p> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilterConfig.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BGGeo.onLocation.</li> <li>For more granular tuning, see LocationFilterConfig.trackingAccuracyThreshold,   LocationFilterConfig.maxImpliedSpeed, and other LocationFilterConfig fields.</li> </ul> <p>Examples</p> <p>See also - GeolocationConfig.filter - LocationFilterConfig - KalmanProfile</p>"},{"location":"swift/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .adjust\n}\n</code></pre>"},{"location":"swift/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .passThrough\n}\n</code></pre>"},{"location":"swift/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.geolocation.filter.policy = .conservative\n}\n</code></pre>"},{"location":"swift/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"swift/LocationFilterPolicy/#adjust","title":"adjust","text":"<pre><code>case adjust = 1\n</code></pre> <p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilterConfig.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"swift/LocationFilterPolicy/#conservative","title":"conservative","text":"<pre><code>case conservative = 2\n</code></pre> <p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"swift/LocationFilterPolicy/#passthrough","title":"passThrough","text":"<pre><code>case passThrough = 0\n</code></pre> <p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"swift/LocationsOrderDirection/","title":"LocationsOrderDirection","text":"<p><code>public enum</code><code>LocationsOrderDirection</code><code>: String</code></p> <p>Sort order for <code>SQLQuery.order</code>.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"swift/LocationsOrderDirection/#members","title":"Members","text":""},{"location":"swift/LocationsOrderDirection/#ascending","title":"ascending","text":"<pre><code>case ascending = \"ASC\"\n</code></pre> <p>Ascending order (oldest to newest).</p>"},{"location":"swift/LocationsOrderDirection/#descending","title":"descending","text":"<pre><code>case descending = \"DESC\"\n</code></pre> <p>Descending order (newest to oldest).</p>"},{"location":"swift/LogLevel/","title":"LogLevel","text":"<p><code>public enum</code><code>LogLevel</code><code>: Int</code></p> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"swift/LogLevel/#members","title":"Members","text":""},{"location":"swift/LogLevel/#debug","title":"debug","text":"<pre><code>case debug = 4\n</code></pre> <p>Developer-level debug output.</p>"},{"location":"swift/LogLevel/#error","title":"error","text":"<pre><code>case error = 1\n</code></pre> <p>Log only critical failures.</p>"},{"location":"swift/LogLevel/#info","title":"info","text":"<pre><code>case info = 3\n</code></pre> <p>Operational information.</p>"},{"location":"swift/LogLevel/#off","title":"off","text":"<pre><code>case off = 0\n</code></pre> <p>Disable all logging.</p>"},{"location":"swift/LogLevel/#verbose","title":"verbose","text":"<pre><code>case verbose = 5\n</code></pre> <p>Maximum detail.</p>"},{"location":"swift/LogLevel/#warning","title":"warning","text":"<pre><code>case warning = 2\n</code></pre> <p>Log warnings + errors.</p>"},{"location":"swift/Logger/","title":"Logger","text":"<p><code>public class</code><code>Logger</code></p> <p>SDK logging API \u2014 access via BGGeo.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"swift/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"swift/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        let log = try await bgGeo.logger.getLog()\n    } catch {\n        print(\"[getLog] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional <code>SQLQuery</code> to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>let bgGeo = BGGeo.shared\nlet sub = bgGeo.onLocation { location in\n    bgGeo.logger.debug(\"Location received in Swift\")\n}\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"swift/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35"},{"location":"swift/Logger/#examples","title":"Examples","text":""},{"location":"swift/Logger/#methods","title":"Methods","text":""},{"location":"swift/Logger/#debug","title":"debug","text":"<pre><code>public func debug(_ message: String)\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.debug(\"This is a debug message\")\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"swift/Logger/#destroylog","title":"destroyLog","text":"<pre><code>public func destroyLog() async throws\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.destroyLog()\n</code></pre>"},{"location":"swift/Logger/#emaillog","title":"emailLog","text":"<p><code>public func emailLog( to email: String, start: Int64 = 0, end: Int64 = 0, order: Int =</code><code>Logger</code><code>.ORDER_ASC, limit: Int = 0 ) async throws</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional <code>SQLQuery</code> to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        try await bgGeo.logger.emailLog(to: \"foo@bar.com\")\n        print(\"[emailLog] success\")\n    } catch {\n        print(\"[emailLog] FAILURE: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Logger/#error","title":"error","text":"<pre><code>public func error(_ message: String)\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.error(\"Something BAD\")\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"swift/Logger/#getlog","title":"getLog","text":"<p><code>public func getLog( start: Int64 = 0, end: Int64 = 0, order: Int =</code><code>Logger</code><code>.ORDER_ASC, limit: Int = 0 ) async throws -&gt; String</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional <code>SQLQuery</code> to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        // Warning: this string could be several megabytes.\n        let log = try await bgGeo.logger.getLog()\n        print(\"[log] success: \\(log)\")\n    } catch {\n        print(\"[getLog] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/Logger/#info","title":"info","text":"<pre><code>public func info(_ message: String)\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.info(\"Something informative\")\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"swift/Logger/#loglevel","title":"logLevel","text":"<p><code>public var logLevel:</code><code>BGGeo</code><code>.</code><code>LogLevel</code></p> <p>Sets the LoggerConfig.logLevel.</p>"},{"location":"swift/Logger/#notice","title":"notice","text":"<pre><code>public func notice(_ message: String)\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.notice(\"A Notice\")\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"swift/Logger/#playsound","title":"playSound","text":"<pre><code>public func playSound(_ soundId: SystemSoundID)\n</code></pre> <p>Play a system sound.</p>"},{"location":"swift/Logger/#ios","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>"},{"location":"swift/Logger/#android","title":"Android","text":"<p>Provide a sound name string.</p>"},{"location":"swift/Logger/#uploadlog","title":"uploadLog","text":"<p><code>public func uploadLog( url: String, start: Int64 = 0, end: Int64 = 0, order: Int =</code><code>Logger</code><code>.ORDER_ASC, limit: Int = 0 ) async throws</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional <code>SQLQuery</code> to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"swift/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Not available in native Swift API\n</code></pre>"},{"location":"swift/Logger/#warn","title":"warn","text":"<pre><code>public func warn(_ message: String)\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>let bgGeo = BGGeo.shared\nbgGeo.logger.warn(\"Something WEIRD\")\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"swift/Logger/#properties","title":"Properties","text":""},{"location":"swift/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>public static let ORDER_ASC = 1\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"swift/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>public static let ORDER_DESC = -1\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"swift/LoggerConfig/","title":"LoggerConfig","text":"<p><code>public class</code><code>LoggerConfig</code></p> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"swift/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"swift/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n    config.logger.logMaxDays = 7\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"swift/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"swift/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"swift/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"swift/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"swift/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"swift/LoggerConfig/#examples","title":"Examples","text":""},{"location":"swift/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.config.batchUpdate { config in\n    config.logger.logLevel = .off\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n    config.logger.logMaxDays = 7\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.logger.debug = false\n    config.logger.logLevel = .info\n    config.logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.config.batchUpdate { config in\n    config.logger.debug = true\n    config.logger.logLevel = .verbose\n    config.logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#members","title":"Members","text":""},{"location":"swift/LoggerConfig/#debug","title":"debug","text":"<pre><code>public var debug: Bool\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"swift/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"swift/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"swift/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>public var logLevel:</code><code>LogLevel</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.logger.logLevel = .verbose\n}\n</code></pre>"},{"location":"swift/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>public var logMaxDays: Int\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.logger.logMaxDays = 3\n}\n</code></pre>"},{"location":"swift/PersistMode/","title":"PersistMode","text":"<p><code>public enum</code><code>PersistMode</code><code>: Int</code></p> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"swift/PersistMode/#members","title":"Members","text":""},{"location":"swift/PersistMode/#all","title":"all","text":"<pre><code>case all = 2\n</code></pre> <p>Persist both location and geofence data.</p>"},{"location":"swift/PersistMode/#geofence","title":"geofence","text":"<pre><code>case geofence = -1\n</code></pre> <p>Persist geofence data only.</p>"},{"location":"swift/PersistMode/#location","title":"location","text":"<pre><code>case location = 1\n</code></pre> <p>Persist location data only.</p>"},{"location":"swift/PersistMode/#none","title":"none","text":"<pre><code>case none = 0\n</code></pre> <p>Do not persist any data.</p>"},{"location":"swift/PersistenceConfig/","title":"PersistenceConfig","text":"<p><code>public class</code><code>PersistenceConfig</code></p> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"swift/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"swift/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded LocationEvent and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Timestamp format <code>timestampFormat</code> ISO-8601 string or Unix ms integer in stored/uploaded JSON. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.maxDaysToPersist = 3\n    config.persistence.maxRecordsToPersist = 1000\n    config.persistence.extras = [\n        \"user_id\": 123,\n        \"appVersion\": \"1.2.3\"\n    ]\n    config.persistence.persistMode = .all\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BGGeo.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BGGeo.getCount or BGGeo.getLocations.</p>"},{"location":"swift/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BGGeo.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"swift/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"swift/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BGGeo.onLocation, BGGeo.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let location = try await bgGeo.getCurrentPosition(\n            samples: 3,\n            persist: true,\n            extras: [\"get_current_position\": true]\n        )\n    } catch {\n        print(\"[getCurrentPosition] error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#examples","title":"Examples","text":""},{"location":"swift/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.config.batchUpdate { config in\n    config.persistence.maxDaysToPersist = 14\n    config.persistence.maxRecordsToPersist = 5000\n    config.persistence.locationsOrderDirection = .ascending\n    config.persistence.locationTemplate = \"{...}\"\n    config.persistence.geofenceTemplate = \"{...}\"\n    config.persistence.persistMode = .all\n    config.persistence.extras = [\"user_id\": 123]\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.maxDaysToPersist = 14\n    config.persistence.maxRecordsToPersist = 5000\n    config.persistence.locationsOrderDirection = .ascending\n    config.persistence.persistMode = .all\n    config.persistence.extras = [\"user_id\": 123, \"appVersion\": \"1.2.3\"]\n    config.http.url = \"https://example.com/locations\"\n    config.http.autoSync = true\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>let bgGeo = BGGeo.shared\n\nlet pending = bgGeo.store.count\nprint(\"Pending records: \\(pending)\")\n\n// Purge all records\nbgGeo.store.destroyAll()\nprint(\"Destroyed all records\")\n</code></pre>"},{"location":"swift/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.config.batchUpdate { config in\n    config.persistence.locationTemplate = \"\"\"\n    {\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }\n    \"\"\"\n    config.persistence.geofenceTemplate = \"\"\"\n    {\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }\n    \"\"\"\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#members","title":"Members","text":""},{"location":"swift/PersistenceConfig/#extras","title":"extras","text":"<pre><code>public var extras: [String: Any]\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://my-server.com/locations\"\n    config.http.params = [\"device_id\": \"abc123\"]\n    config.persistence.extras = [\"route_id\": 1234]\n}\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"swift/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>public var geofenceTemplate: String\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.geofenceTemplate = \"{ \\\"lat\\\":&lt;%= latitude %&gt;, \\\"lng\\\":&lt;%= longitude %&gt;, \\\"geofence\\\":\\\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\\\" }\"\n}\n\n// Or a compact Array form:\nbgGeo.config.batchUpdate { config in\n    config.persistence.geofenceTemplate = \"[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \\\"&lt;%= geofence.identifier %&gt;\\\", \\\"&lt;%= geofence.action %&gt;\\\"]\"\n}\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"swift/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeolocationConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"swift/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.geofenceTemplate = \"{\\\"timestamp\\\": &lt;%= timestamp %&gt;}\"\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.geofenceTemplate = \"{\\\"timestamp\\\": \\\"&lt;%= timestamp %&gt;\\\"}\"\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>public var locationTemplate: String\n</code></pre> <p>Optional custom ERB template for reshaping LocationEvent JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.locationTemplate = \"{\\\"lat\\\":&lt;%= latitude %&gt;,\\\"lng\\\":&lt;%= longitude %&gt;,\\\"event\\\":\\\"&lt;%= event %&gt;\\\",isMoving:&lt;%= is_moving %&gt;}\"\n}\n\n// Or use a compact Array template:\nbgGeo.config.batchUpdate { config in\n    config.persistence.locationTemplate = \"[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \\\"&lt;%=event%&gt;\\\", &lt;%=is_moving%&gt;]\"\n}\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.locationTemplate = \"{\\\"timestamp\\\": &lt;%= timestamp %&gt;}\"\n}\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.locationTemplate = \"{\\\"timestamp\\\": \\\"&lt;%= timestamp %&gt;\\\"}\"\n}\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"swift/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.http.url = \"https://my.server.com/locations\"\n    config.http.rootProperty = \"data\"\n    config.persistence.locationTemplate = \"{\\\"lat\\\":&lt;%= latitude %&gt;,\\\"lng\\\":&lt;%= longitude %&gt;}\"\n    config.persistence.extras = [\"foo\": \"bar\"]\n}\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"swift/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeolocationConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"swift/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<p><code>public var locationsOrderDirection:</code><code>LocationsOrderDirection</code></p> <p>Sort order used when selecting records for upload or returning them from BGGeo.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"swift/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>public var maxDaysToPersist: Int\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"swift/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>public var maxRecordsToPersist: Int\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"swift/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>public var persistMode:</code><code>PersistMode</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BGGeo.onLocation and BGGeo.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BGGeo.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"swift/PersistenceConfig/#timestampformat","title":"timestampFormat","text":"<pre><code>public var timestampFormat: String\n</code></pre> <p>Controls the format of the <code>timestamp</code> field in the persisted and uploaded location JSON.</p> <p>Because HttpConfig uploads the stored JSON blob as-is, this setting affects both the on-device SQLite record and the HTTP payload simultaneously.</p> Value Format Example <code>\"iso\"</code> (default) ISO-8601 string <code>\"2025-05-05T04:31:54.123Z\"</code> <code>\"epoch\"</code> Integer \u2014 milliseconds since Unix epoch <code>1746417114123</code> <p>Warning</p> <p>This option affects only the persisted blob \u2014 the data written to SQLite and subsequently uploaded by HttpConfig. Live callbacks (BGGeo.onLocation, BGGeo.getLocations) always return an ISO-8601 string regardless of this setting.</p> <p>When using <code>locationTemplate</code> or <code>geofenceTemplate</code>, the <code>&lt;%= timestamp %&gt;</code> tag always expands to an ISO-8601 string. <code>timestampFormat</code> does not affect template substitution.</p> <pre><code>let bgGeo = BGGeo.shared\n\nbgGeo.ready { config in\n    config.persistence.timestampFormat = \"epoch\"\n}\n</code></pre> <p>See also - <code>locationTemplate</code> - <code>geofenceTemplate</code> - HttpConfig.url</p>"},{"location":"swift/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<p><code>public struct</code><code>ProviderChangeEvent</code><code>: Sendable</code></p> <p>Location-services provider state delivered to BGGeo.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p>"},{"location":"swift/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"swift/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<p><code>public let accuracy</code><code>Authorization</code><code>: Int</code></p> <p>Precision location authorization level. Use the <code>AccuracyAuthorization</code> constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"swift/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>public let enabled: Bool\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"swift/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>public let gps: Bool\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"swift/ProviderChangeEvent/#network","title":"network","text":"<pre><code>public let network: Bool\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"swift/ProviderChangeEvent/#status","title":"status","text":"<pre><code>public let status: CLAuthorizationStatus\n</code></pre> <p>Platform authorization status code. Use the <code>AuthorizationStatus</code> constants to interpret this value.</p>"},{"location":"swift/Sensors/","title":"Sensors","text":"<p><code>public class</code><code>Sensors</code></p> <p>Availability of motion sensors on the device, as returned by BGGeo.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p>"},{"location":"swift/Sensors/#members","title":"Members","text":""},{"location":"swift/Sensors/#hasaccelerometer","title":"hasAccelerometer","text":"<pre><code>public var hasAccelerometer: Bool\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"swift/Sensors/#hasgyroscope","title":"hasGyroscope","text":"<pre><code>public var hasGyroscope: Bool\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"swift/Sensors/#hasmagnetometer","title":"hasMagnetometer","text":"<pre><code>public var hasMagnetometer: Bool\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"swift/Sensors/#hassignificantmotion","title":"hasSignificantMotion","text":"<pre><code>public var hasSignificantMotion: Bool\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"swift/State/","title":"State","text":"<p><code>public struct</code><code>State</code></p> <p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>let bgGeo = BGGeo.shared\nlet state = bgGeo.state\nprint(\"[state] enabled:\", state.enabled, \"trackingMode:\", state.trackingMode)\n</code></pre>"},{"location":"swift/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"swift/State/#members","title":"Members","text":""},{"location":"swift/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>public let didDeviceReboot: Bool\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"swift/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>public let didLaunchInBackground: Bool\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"swift/State/#enabled","title":"enabled","text":"<pre><code>public let enabled: Bool\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BGGeo.start or BGGeo.startGeofences has been called and not yet stopped.</p>"},{"location":"swift/State/#isfirstboot","title":"isFirstBoot","text":"<pre><code>public let isFirstBoot: Bool\n</code></pre> <p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"swift/State/#ismoving","title":"isMoving","text":"<pre><code>public let isMoving: Bool\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>let bgGeo = BGGeo.shared\n\n// If the SDK is currently in the *stationary* state:\nlet sub = bgGeo.onMotionChange { location in\n    print(\"[onMotionChange] isMoving? \\(location.isMoving)\")\n}\n\nbgGeo.changePace(true)\n// config.isMoving is now true.\n</code></pre>"},{"location":"swift/State/#odometer","title":"odometer","text":"<pre><code>public let odometer: Double\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BGGeo.setOdometer - BGGeo.getOdometer</p>"},{"location":"swift/State/#odometererror","title":"odometerError","text":"<pre><code>public let odometerError: Double\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilterConfig.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"swift/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>public let schedulerEnabled: Bool\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BGGeo.startSchedule has been called. BGGeo.stopSchedule sets this to <code>false</code>.</p>"},{"location":"swift/State/#trackingmode","title":"trackingMode","text":"<p><code>public let trackingMode:</code><code>TrackingMode</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>let bgGeo = BGGeo.shared\n\nTask {\n    do {\n        try await bgGeo.start()\n        print(\"Tracking mode: \\(bgGeo.config.trackingMode)\")\n        // trackingMode == .location\n\n        try await bgGeo.startGeofences()\n        print(\"Tracking mode: \\(bgGeo.config.trackingMode)\")\n        // trackingMode == .geofence\n    } catch {\n        print(\"Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/TrackingMode/","title":"TrackingMode","text":"<p><code>public enum</code><code>TrackingMode</code><code>: Int</code></p> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"swift/TrackingMode/#members","title":"Members","text":""},{"location":"swift/TrackingMode/#geofence","title":"geofence","text":"<pre><code>case geofence = 0\n</code></pre> <p>Monitor geofences only.</p>"},{"location":"swift/TrackingMode/#location","title":"location","text":"<pre><code>case location = 1\n</code></pre> <p>Monitor location + geofences.</p>"},{"location":"swift/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<p><code>public enum</code><code>TransistorAuthorizationService</code></p> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"swift/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>// Not available in native Swift API\n</code></pre>"},{"location":"swift/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"swift/TransistorAuthorizationService/#destroytoken","title":"destroyToken","text":"<pre><code>public static func destroyToken(url: String)\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"swift/TransistorAuthorizationService/#findorcreatetoken","title":"findOrCreateToken","text":"<p><code>public static func findOrCreateToken( org: String, username: String, url: String, framework: String = \"Swift\" ) async throws -&gt;</code><code>BGGeo</code><code>.TransistorToken</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>let bgGeo = BGGeo.shared\nTask {\n    do {\n        let token = try await BGGeo.TransistorAuthorizationService.findOrCreateToken(\n            org: \"my-company-name\",\n            username: \"my-username\",\n            url: \"http://tracker.transistorsoft.com\"\n        )\n        bgGeo.ready(transistorAuthorizationToken: token) { _ in }\n    } catch {\n        print(\"[findOrCreateToken] Error: \\(error)\")\n    }\n}\n</code></pre>"},{"location":"swift/examples/","title":"Examples","text":""},{"location":"swift/examples/#basic-tracking","title":"Basic tracking","text":"<pre><code>import SwiftUI\nimport BackgroundGeolocation\n\n@main\nstruct MyApp: App {\n    private var subscriptions = Set&lt;BGGeo.EventSubscription&gt;()\n\n    init() {\n        let bgGeo = BGGeo.shared\n\n        // Register event listeners *before* calling ready()\n        bgGeo.onLocation { event in\n            print(\"[onLocation] \\(event.coords)\")\n        }.store(in: &amp;subscriptions)\n\n        // ready() configures the SDK and restores persisted state.\n        // It does NOT start tracking \u2014 call start()/stop() separately.\n        bgGeo.ready { config in\n            config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n            config.geolocation.distanceFilter = 10\n            config.app.stopOnTerminate = false\n            config.app.startOnBoot = true\n            config.logger.debug = true\n            config.logger.logLevel = .verbose\n        }\n    }\n\n    var body: some Scene {\n        WindowGroup { ContentView() }\n    }\n}\n\n// Elsewhere \u2014 e.g. a toggle-button action in your UI\nstruct TrackingButton: View {\n    @State private var isTracking = BGGeo.shared.state.enabled\n\n    var body: some View {\n        Button(isTracking ? \"Stop\" : \"Start\") {\n            Task {\n                let bgGeo = BGGeo.shared\n                if isTracking {\n                    bgGeo.stop()\n                } else {\n                    try await bgGeo.start()\n                }\n                isTracking = bgGeo.state.enabled\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"swift/setup/","title":"Setup","text":""},{"location":"swift/setup/#installation","title":"Installation","text":"<p>Swift Package Manager is the only supported installation method.</p> <p>In Xcode: File \u2192 Add Package Dependencies, enter the repository URL:</p> <pre><code>https://github.com/transistorsoft/native-background-geolocation\n</code></pre> <p>Select Up to Next Major Version from the latest release, then add the BackgroundGeolocation library to your target.</p>"},{"location":"swift/setup/#app-initialisation","title":"App initialisation","text":"<p>Call <code>TSBackgroundFetch</code> from your <code>App</code> initialiser:</p> <pre><code>import SwiftUI\nimport BackgroundGeolocation\n\n@main\nstruct MyApp: App {\n    init() {\n        TSBackgroundFetch.sharedInstance()?.didFinishLaunching()\n    }\n    var body: some Scene {\n        WindowGroup { ContentView() }\n    }\n}\n</code></pre>"},{"location":"swift/setup/#license-key","title":"License key","text":"<p>Purchase a license</p> <p>The SDK requires a license for App Store builds. All other builds (debug, simulator, TestFlight, ad-hoc) are fully functional without one. Purchase at docs.transistorsoft.com.</p> <p>Add a User-Defined build setting to your target:</p> <p>Build Settings \u2192 + \u2192 User-Defined Setting</p> Setting Value <code>TRANSISTOR_LICENSE_KEY</code> <code>your-license-key-jwt</code> <p>Then reference it from your supplementary <code>Info.plist</code> via <code>$(TRANSISTOR_LICENSE_KEY)</code>.</p>"},{"location":"swift/setup/#signing-capabilities","title":"Signing &amp; Capabilities","text":"<p>Add the Background Modes capability to your target:</p> <p>Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes</p> <p>Enable:</p> <ul> <li>\u2611 Location updates</li> <li>\u2611 Background fetch</li> <li>\u2611 Background processing</li> <li>\u2611 Audio, AirPlay, and Picture in Picture (required for debug sound effects only)</li> </ul>"},{"location":"swift/setup/#info-tab","title":"Info tab","text":""},{"location":"swift/setup/#privacy-usage-strings","title":"Privacy usage strings","text":"<p>Target \u2192 Info \u2192 Custom iOS Target Properties</p> <p>Add the following keys:</p> Key (Xcode display name) Raw key Example value Privacy - Location Always and When In Use Usage Description <code>NSLocationAlwaysAndWhenInUseUsageDescription</code> <code>App requires location access at all times for background tracking.</code> Privacy - Location When In Use Usage Description <code>NSLocationWhenInUseUsageDescription</code> <code>App requires location access while in use.</code> Privacy - Motion Usage Description <code>NSMotionUsageDescription</code> <code>Motion detection helps determine when the device is stationary.</code>"},{"location":"swift/setup/#background-task-identifiers","title":"Background task identifiers","text":"<p>Still in Custom iOS Target Properties, add:</p> Key (Xcode display name) Raw key Value Permitted background task scheduler identifiers <code>BGTaskSchedulerPermittedIdentifiers</code> Array \u2192 <code>com.transistorsoft.fetch</code>"},{"location":"swift/setup/#supplementary-infoplist","title":"Supplementary <code>Info.plist</code>","text":"<p><code>TSLocationManagerLicense</code> is a custom key that cannot be expressed as a build setting. Create a supplementary property list file:</p> <ol> <li>File \u2192 New \u2192 File from Template \u2192 Property List \u2014 name it <code>Info.plist</code></li> <li>Remove it from all targets (leave every checkbox unchecked)</li> <li>Click Create \u2014 place the file next to your <code>.xcodeproj</code></li> <li>Build Settings \u2192 <code>INFOPLIST_FILE</code> \u2192 set to <code>Info.plist</code></li> </ol> <p>Add the license key, substituted from the <code>TRANSISTOR_LICENSE_KEY</code> build setting:</p> <pre><code>&lt;?xml version=\"1.0\" encoding=\"UTF-8\"?&gt;\n&lt;!DOCTYPE plist PUBLIC \"-//Apple//DTD PLIST 1.0//EN\" \"http://www.apple.com/DTDs/PropertyList-1.0.dtd\"&gt;\n&lt;plist version=\"1.0\"&gt;\n&lt;dict&gt;\n    &lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n    &lt;string&gt;$(TRANSISTOR_LICENSE_KEY)&lt;/string&gt;\n&lt;/dict&gt;\n&lt;/plist&gt;\n</code></pre> <p>Tip</p> <p>Because <code>INFOPLIST_FILE</code> is set alongside <code>GENERATE_INFOPLIST_FILE = YES</code>, Xcode merges the two sources. The file is never copied into your bundle directly.</p>"},{"location":"swift/setup/#example","title":"Example","text":"<pre><code>import SwiftUI\nimport BackgroundGeolocation\n\n@main\nstruct MyApp: App {\n    private var subscriptions = Set&lt;BGGeo.EventSubscription&gt;()\n\n    init() {\n        let bgGeo = BGGeo.shared\n\n        // Register event listeners *before* calling ready()\n        bgGeo.onLocation { event in\n            print(\"[onLocation] \\(event.coords)\")\n        }.store(in: &amp;subscriptions)\n\n        // ready() configures the SDK and restores persisted state.\n        // It does NOT start tracking \u2014 call start()/stop() separately.\n        bgGeo.ready { config in\n            config.geolocation.desiredAccuracy = kCLLocationAccuracyBest\n            config.geolocation.distanceFilter = 10\n            config.app.stopOnTerminate = false\n            config.app.startOnBoot = true\n            config.logger.debug = true\n            config.logger.logLevel = .verbose\n        }\n    }\n\n    var body: some Scene {\n        WindowGroup { ContentView() }\n    }\n}\n\n// Elsewhere \u2014 e.g. a toggle-button action in your UI\nstruct TrackingButton: View {\n    @State private var isTracking = BGGeo.shared.state.enabled\n\n    var body: some View {\n        Button(isTracking ? \"Stop\" : \"Start\") {\n            Task {\n                let bgGeo = BGGeo.shared\n                if isTracking {\n                    bgGeo.stop()\n                } else {\n                    try await bgGeo.start()\n                }\n                isTracking = bgGeo.state.enabled\n            }\n        }\n    }\n}\n</code></pre>"},{"location":"typescript/","title":"TypeScript SDK Reference","text":"react-native-background-geolocation Buy Now capacitor-background-geolocation Buy Now cordova-background-geolocation-lt Buy Now"},{"location":"typescript/#quick-links","title":"Quick links","text":"Section Description BackgroundGeolocation Primary entry point \u2014 start/stop, events, config AppConfig Lifecycle, heartbeat, scheduler, notifications HttpConfig HTTP upload endpoint, batching, auth headers GeoConfig Accuracy, distance filter, stop detection PersistenceConfig SQLite storage, TTL, templates LoggerConfig Log levels, retention, debug mode <p>Use the nav sidebar to browse all classes, events, and types.</p>"},{"location":"typescript/AccuracyAuthorization/","title":"AccuracyAuthorization","text":"<pre><code>export const AccuracyAuthorization = {\n</code></pre> <p>Accuracy authorization granted by the user.</p> Name Value Description Full 0 Full accuracy authorized. Reduced 1 Reduced accuracy granted. <p>Mirrors iOS <code>CLAccuracyAuthorization</code> and Android location accuracy permission levels.</p>"},{"location":"typescript/AccuracyAuthorization/#members","title":"Members","text":""},{"location":"typescript/AccuracyAuthorization/#full","title":"Full","text":"<p>Full accuracy authorized.</p>"},{"location":"typescript/AccuracyAuthorization/#reduced","title":"Reduced","text":"<p>Reduced accuracy granted.</p>"},{"location":"typescript/ActivityConfig/","title":"ActivityConfig","text":"<pre><code>export interface ActivityConfig {\n</code></pre> <p>Activity recognition configuration for the background geolocation SDK.</p> <p><code>ActivityConfig</code> controls how the SDK interprets transitions between moving and stationary states using platform motion APIs (Android Activity Recognition and iOS Core Motion).</p>"},{"location":"typescript/ActivityConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Stop detection</li> <li>Motion trigger</li> </ul>"},{"location":"typescript/ActivityConfig/#overview","title":"Overview","text":"Category Properties Notes Recognition cadence <code>activityRecognitionInterval</code>, <code>minimumActivityRecognitionConfidence</code> How often and how confidently the SDK polls platform motion APIs. Stop detection <code>disableStopDetection</code>, <code>stopOnStationary</code>, <code>stopDetectionDelay</code> Controls when the SDK transitions to stationary and stops tracking. <code>stopDetectionDelay</code> is iOS only. Motion trigger <code>motionTriggerDelay</code>, <code>triggerActivities</code> Controls which activities start tracking and how quickly. <code>motionTriggerDelay</code> is Android only. Permission <code>disableMotionActivityUpdates</code> Opt out of Motion &amp; Fitness / Physical Activity permission requests. <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    // Poll activity recognition every 10 seconds (both platforms)\n    activityRecognitionInterval: 10000,\n\n    // Require \u226575% confidence before changing activity (both platforms)\n    minimumActivityRecognitionConfidence: 75,\n\n    // Disable automatic stop detection (both platforms)\n    disableStopDetection: false,\n\n    // Automatically stop when stationary (both platforms)\n    stopOnStationary: true,\n\n    // Delay motion-trigger transitions by 30s (Android only)\n    motionTriggerDelay: 30000,\n\n    // Disable platform motion activity APIs (both platforms)\n    disableMotionActivityUpdates: false,\n\n    // Delay stop-detection by 10s (iOS only)\n    stopDetectionDelay: 10000\n  }\n});\n</code></pre>"},{"location":"typescript/ActivityConfig/#stop-detection","title":"Stop detection","text":"<p>The SDK uses platform motion APIs to detect when the device has stopped moving and transitions to the stationary state. In that state, location services are paused to conserve battery.</p> <ul> <li>ActivityConfig.disableStopDetection disables this mechanism entirely.   With it off, location services run continuously.</li> <li>ActivityConfig.stopOnStationary takes it a step further \u2014 the SDK   calls <code>stop()</code> on itself when the GeoConfig.stopTimeout elapses,   ending all tracking.</li> <li>ActivityConfig.stopDetectionDelay (iOS only) adds a grace period   before the stop-detection system engages, preventing false stops at traffic   lights or brief pauses.</li> </ul>"},{"location":"typescript/ActivityConfig/#motion-trigger","title":"Motion trigger","text":"<p>When in the stationary state, the SDK waits for a motion event before re-engaging location tracking:</p> <ul> <li>ActivityConfig.triggerActivities limits which activity types can   trigger the transition to moving. By default all moving activities trigger.</li> <li>ActivityConfig.motionTriggerDelay (Android only) adds a delay before   committing to the moving state. If the device returns to <code>still</code> before the   delay expires, the transition is cancelled.</li> </ul> <p>See also - Config.activity</p> <pre><code>// Legacy (deprecated)\nBackgroundGeolocation.ready({\n  activityRecognitionInterval: 10000,\n  disableStopDetection: true,\n  stopOnStationary: true\n});\n</code></pre> <pre><code>// Current\nBackgroundGeolocation.ready({\n  activity: {\n    activityRecognitionInterval: 10000,\n    disableStopDetection: true,\n    stopOnStationary: true\n  }\n});\n</code></pre>"},{"location":"typescript/ActivityConfig/#members","title":"Members","text":""},{"location":"typescript/ActivityConfig/#activityrecognitioninterval","title":"activityRecognitionInterval","text":"<pre><code>activityRecognitionInterval?: number;\n</code></pre> <p>Interval in milliseconds between motion-activity polls. Defaults to <code>10000</code> ms (10 seconds).</p> <p>Lower values increase responsiveness to state changes at the cost of additional battery use. The minimum supported value is <code>500</code> ms.</p>"},{"location":"typescript/ActivityConfig/#disablemotionactivityupdates","title":"disableMotionActivityUpdates","text":"<pre><code>disableMotionActivityUpdates?: boolean;\n</code></pre> <p>Disables the SDK's request for Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission. Defaults to <code>false</code>.</p> <p>Set to <code>true</code> to suppress the permission request. The SDK remains functional but without motion data it relies on less efficient fallback mechanisms to detect moving/stationary transitions.</p>"},{"location":"typescript/ActivityConfig/#ios","title":"iOS","text":"<p>The SDK is highly optimized around motion-activity updates. Disabling them increases battery consumption. Provide an <code>NSMotionUsageDescription</code> in your <code>Info.plist</code> to explain the benefit to users \u2014 for example:</p> <p>\"Motion activity detection increases battery efficiency by intelligently toggling location tracking off when your device is stationary.\"</p> <p></p>"},{"location":"typescript/ActivityConfig/#android","title":"Android","text":"<p>Android 10+ requires runtime permission for Physical Activity. Without it, the SDK falls back to a stationary geofence mechanism \u2014 the device must move 200\u2013500 m before tracking re-engages, compared to just a few meters when the Motion API is authorized.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableMotionActivityUpdates: true\n  }\n});\n</code></pre>"},{"location":"typescript/ActivityConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity-based stop-detection system. Defaults to <code>false</code>.</p>"},{"location":"typescript/ActivityConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. When disabled, the SDK falls back to the default iOS behaviour of automatically turning off location services after the device has been stationary for exactly 15 minutes. GeoConfig.stopTimeout has no effect in this state.</p> <p>To prevent iOS from ever turning off location services automatically, also set GeoConfig.pausesLocationUpdatesAutomatically to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    disableStopDetection: true,\n  },\n  geolocation: {\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With the above configuration, iOS location services will never turn off and the battery will drain rapidly. Use this only when you have explicit start/stop controls in your app (for example, a workout app with Start / Stop buttons calling BackgroundGeolocation.changePace).</p> <p></p>"},{"location":"typescript/ActivityConfig/#android_1","title":"Android","text":"<p>Location services will never turn off while <code>disableStopDetection</code> is <code>true</code>. The only way to stop tracking is to call BackgroundGeolocation.changePace <code>false</code> or BackgroundGeolocation.stop explicitly.</p>"},{"location":"typescript/ActivityConfig/#minimumactivityrecognitionconfidence","title":"minimumActivityRecognitionConfidence","text":"<pre><code>minimumActivityRecognitionConfidence?: number;\n</code></pre> <p>Minimum confidence level (0\u2013100) required before a detected activity triggers a moving/stationary state change.</p> <p>Defaults to <code>70</code> on iOS and <code>75</code> on Android. Higher values reduce false-positive transitions but may cause the SDK to react more slowly to genuine activity changes.</p>"},{"location":"typescript/ActivityConfig/#motiontriggerdelay","title":"motionTriggerDelay","text":"<pre><code>motionTriggerDelay?: number;\n</code></pre> <p>Android only Adds a delay in milliseconds before the SDK commits to the moving state when the Motion API reports movement. Defaults to <code>0</code> (no delay).</p> <p>When the Motion API reports a moving activity (e.g. <code>on_foot</code>, <code>in_vehicle</code>), the SDK waits <code>motionTriggerDelay</code> milliseconds before transitioning to moving and starting location tracking. If the Motion API returns to <code>still</code> before the delay expires, the transition is cancelled. This prevents false-positive triggers when briefly moving around a stationary location (for example, walking around a home).</p> <p>The following log shows a device detecting <code>on_foot</code> but returning to <code>still</code> before the delay expires, cancelling the transition:</p> <pre><code> 04-08 10:58:03.419 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:03.419 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: still\n 04-08 10:58:03.419 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: on_foot\n 04-08 10:58:03.419 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:03.416 TSLocationManager:   \u23f0 Scheduled OneShot: MOTION_TRIGGER_DELAY in 30000ms\n .\n . &lt;motionTriggerDelay timer started&gt;\n .\n 04-08 10:58:19.385 TSLocationManager: \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u2551 Motion Transition Result\n 04-08 10:58:19.385 TSLocationManager: \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83d\udd34  EXIT: on_foot\n 04-08 10:58:19.385 TSLocationManager: \u255f\u2500 \ud83c\udfbe  ENTER: still\n 04-08 10:58:19.385 TSLocationManager: \u255a\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n 04-08 10:58:19.381 TSLocationManager: [c.t.l.s.TSScheduleManager cancelOneShot]\n 04-08 10:58:19.381 TSLocationManager:   \u23f0 Cancel OneShot: MOTION_TRIGGER_DELAY &lt;-- timer cancelled\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    motionTriggerDelay: 30000\n  }\n});\n</code></pre>"},{"location":"typescript/ActivityConfig/#stopdetectiondelay","title":"stopDetectionDelay","text":"<pre><code>stopDetectionDelay?: number;\n</code></pre> <p>iOS only Delays the iOS stop-detection system from engaging after the device becomes stationary. Defaults to <code>0</code> (engage immediately). When the stop-detection system engages, location services are temporarily turned off and only the accelerometer is monitored. The delay timer starts when the device is detected as stationary and is cancelled if any movement is detected before it expires. Setting <code>stopDetectionDelay</code> to a non-zero value helps avoid false stops at traffic lights or brief pauses.</p> <p>Enable LoggerConfig.debug to observe stop-detection in action \u2014 the SDK emits a sound effect and local notifications when location services toggle on and off.</p> <p></p>"},{"location":"typescript/ActivityConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the GeoConfig.stopTimeout elapses. Defaults to <code>false</code>.</p> <p>When the SDK transitions to the stationary state and the <code>stopTimeout</code> timer expires, the SDK calls <code>stop()</code> on itself \u2014 ending all tracking. The next tracking session must be started manually.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the GeoConfig.stopTimeout timer elapses naturally. It does not fire when BackgroundGeolocation.changePace <code>false</code> is called manually.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    stopOnStationary: true,\n  }\n}).then(() =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"typescript/ActivityConfig/#triggeractivities","title":"triggerActivities","text":"<p><code>triggerActivities?:</code><code>TriggerActivity</code><code>|</code><code>TriggerActivity</code><code>[];</code></p> <p>Restricts which detected activities can trigger a transition from the stationary state to the moving state.</p> <p>By default the SDK triggers on any moving activity: <code>in_vehicle</code>, <code>on_bicycle</code>, <code>on_foot</code>, <code>running</code>, or <code>walking</code>. Configure this to limit tracking to specific use cases \u2014 for example, vehicles only.</p> <p>See TriggerActivity for all valid activity names.</p> <p>Warning</p> <p>Using <code>triggerActivities</code> requires the user to grant the Motion &amp; Fitness (iOS) or Physical Activity (Android 10+) permission.</p> <pre><code>// Only trigger tracking for vehicles\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"in_vehicle\"]\n  }\n});\n\n// Only trigger tracking for on_foot, walking and running\nBackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\"on_foot\", \"walking\", \"running\"]\n  }\n});\n</code></pre>"},{"location":"typescript/ActivityType/","title":"ActivityType","text":"<pre><code>export const ActivityType = {\n</code></pre> <p>iOS Activity Type used with GeoConfig.activityType.</p> <p>Corresponds to Apple's <code>CLActivityType</code> enum: https://developer.apple.com/documentation/corelocation/clactivitytype</p> Name Value Description Other 1 Default/unspecified activity. AutomotiveNavigation 2 Automotive navigation mode. Fitness 3 Fitness-related activity. OtherNavigation 4 Non-automotive navigation mode. Airborne 5 Airborne activity (iOS 15+)."},{"location":"typescript/ActivityType/#members","title":"Members","text":""},{"location":"typescript/ActivityType/#airborne","title":"Airborne","text":"<p>Airborne activity (iOS 15+).</p>"},{"location":"typescript/ActivityType/#automotivenavigation","title":"AutomotiveNavigation","text":"<p>Automotive navigation mode.</p>"},{"location":"typescript/ActivityType/#fitness","title":"Fitness","text":"<p>Fitness-related activity.</p>"},{"location":"typescript/ActivityType/#other","title":"Other","text":"<p>Default/unspecified activity.</p>"},{"location":"typescript/ActivityType/#othernavigation","title":"OtherNavigation","text":"<p>Non-automotive navigation mode.</p>"},{"location":"typescript/AppConfig/","title":"AppConfig","text":"<pre><code>export interface AppConfig {\n</code></pre> <p>Application and lifecycle configuration for the background geolocation SDK.</p> <p><code>AppConfig</code> controls how the SDK integrates with your app's lifecycle: tracking behavior on terminate and reboot, headless operation, periodic heartbeats, automated scheduler windows, foreground notifications, and the Android background-permission rationale dialog.</p>"},{"location":"typescript/AppConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Heartbeat</li> <li>Scheduler</li> <li>Background permission</li> <li>Examples</li> </ul>"},{"location":"typescript/AppConfig/#overview","title":"Overview","text":"Category Properties Notes Lifecycle <code>stopOnTerminate</code>, <code>startOnBoot</code> Controls tracking across app terminate and device reboot. Headless <code>enableHeadless</code> Android only Tracking continues with no JS/UI process. Heartbeat <code>heartbeatInterval</code>, <code>preventSuspend</code> Periodic background callbacks. <code>preventSuspend</code> is iOS only. Scheduler <code>schedule</code>, <code>scheduleUseAlarmManager</code> Automated start/stop windows. <code>scheduleUseAlarmManager</code> is Android only. Notification <code>notification</code> Android only Foreground service notification. Permission <code>backgroundPermissionRationale</code> Android only Android 11+ background location rationale. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#lifecycle","title":"Lifecycle","text":"<p>AppConfig.stopOnTerminate (default <code>true</code>) controls whether tracking stops when the user terminates the app. Set to <code>false</code> to continue tracking after termination \u2014 iOS and Android handle this differently:</p> <ul> <li>iOS creates a stationary geofence and relaunches the app when the device   exits it (~200 m). Tracking resumes automatically.</li> <li>Android continues the native background service headlessly. Pair with   HttpConfig.url to ensure uploads continue without the UI layer.</li> </ul> <p>AppConfig.startOnBoot (default <code>false</code>) controls whether tracking resumes after a device reboot. On Android, the background service restarts automatically. On iOS, the OS must first wake the app via a stationary geofence exit or a Background Fetch event.</p> <p>AppConfig.enableHeadless (default <code>false</code>) allows Android to respond to SDK events even after the app is terminated. Requires <code>stopOnTerminate: false</code>. Android only</p>"},{"location":"typescript/AppConfig/#heartbeat","title":"Heartbeat","text":"<p>AppConfig.heartbeatInterval fires BackgroundGeolocation.onHeartbeat periodically while the app is in the background. On iOS, this requires AppConfig.preventSuspend <code>true</code> to keep the app alive. On Android, the minimum interval is 60 seconds.</p> <p>AppConfig.preventSuspend (default <code>false</code>) prevents iOS from suspending the app when location services are inactive. Required for heartbeat support on iOS but increases battery consumption \u2014 use only for specific, time-limited use cases. iOS only</p>"},{"location":"typescript/AppConfig/#scheduler","title":"Scheduler","text":"<p>AppConfig.schedule accepts an array of schedule strings that tell the SDK when to automatically start and stop tracking. Each string follows the format <code>\"DAY(s) HH:mm-HH:mm\"</code> using 24h time and Locale.US day numbers (Sunday=1, Saturday=7).</p> <p>Call BackgroundGeolocation.startSchedule to activate the scheduler after calling <code>ready()</code>.</p> <p>AppConfig.scheduleUseAlarmManager (default <code>false</code>) forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Android only</p>"},{"location":"typescript/AppConfig/#background-permission","title":"Background permission","text":"<p>Android 11 removed the Allow all the time option from the system location dialog. Configure AppConfig.backgroundPermissionRationale to show a custom rationale dialog that routes the user to the system Location Permissions screen to grant background access. The SDK shows this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>. Android only</p> <p>See PermissionRationale for the full dialog configuration reference.</p>"},{"location":"typescript/AppConfig/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#configure-once-at-startup","title":"Configure once at startup:","text":"<pre><code>import BackgroundGeolocation, {\n  type Config,\n  type AppConfig,\n  PermissionRationale,\n  Notification\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n    heartbeatInterval: 60,\n    backgroundPermissionRationale: {\n      title: \"Allow MyApp to access this device's location even when closed.\",\n      message: \"This app collects location data to record your trips and mileage.\",\n      positiveAction: 'Change to \"{backgroundPermissionOptionLabel}\"',\n      negativeAction: 'Cancel'\n    },\n    notification: {\n      title: 'Background Geolocation',\n      text: 'Tracking location',\n      smallIcon: 'mipmap/ic_launcher'\n    },\n    schedule: ['1-5 09:00-17:00'], // Weekdays 9\u20135\n    scheduleUseAlarmManager: true\n  }\n};\n\nawait BackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"typescript/AppConfig/#update-later-at-runtime","title":"Update later at runtime:","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  app: {\n    heartbeatInterval: 120 // slow down heartbeats\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#members","title":"Members","text":""},{"location":"typescript/AppConfig/#backgroundpermissionrationale","title":"backgroundPermissionRationale","text":"<p><code>background</code><code>PermissionRationale</code><code>?:</code><code>PermissionRationale</code><code>;</code></p> <p>Android only Configures the dialog shown when requesting Always background location permission on Android 11 and later.</p> <p>Android 11 removed the Allow all the time button from the system permission dialog. Apps may instead present a custom rationale dialog explaining why background access is required. Selecting the dialog's <code>positiveAction</code> sends the user directly to the system Location Permissions screen, where they must enable Allow all the time.</p> <p>The SDK shows this dialog automatically when AppConfig.backgroundPermissionRationale is configured and GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code>.</p> <ul> <li>The rationale dialog is shown once only. After the user presses the   <code>positiveAction</code>, it will not appear again. Pressing Cancel does not   count.</li> <li>If the user later resets your app's Location permission to Ask every   time, the dialog may be presented again.</li> </ul> <p> </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location permission.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside PermissionRationale fields using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> Allow all the time Localized label for the background permission option (API 30+). <code>{applicationName}</code> Your app name From <code>AndroidManifest.xml</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Location updates in Android 11</p>"},{"location":"typescript/AppConfig/#enableheadless","title":"enableHeadless","text":"<pre><code>enableHeadless?: boolean;\n</code></pre> <p>Android only Enables headless operation so the SDK can respond to events after the app has been terminated with AppConfig.stopOnTerminate <code>false</code>. Defaults to <code>false</code>.</p> <p>In the headless state only the native foreground service is running \u2014 the JS/UI process is gone. Register a callback with BackgroundGeolocation.registerHeadlessTask to handle events in this state.</p> <p>Note</p> <ul> <li>Requires AppConfig.stopOnTerminate <code>false</code>.</li> <li>With <code>stopOnTerminate: false</code>, the SDK records and uploads locations   regardless of <code>enableHeadless</code>. Enable this only if you need to perform   custom work during the headless state (for example, posting a local   notification).</li> </ul> <p>See also - Android Headless Mode - BackgroundGeolocation.registerHeadlessTask</p>"},{"location":"typescript/AppConfig/#heartbeatinterval","title":"heartbeatInterval","text":"<pre><code>heartbeatInterval?: number;\n</code></pre> <p>Rate in seconds at which BackgroundGeolocation.onHeartbeat events fire while the app is in the background.</p>"},{"location":"typescript/AppConfig/#ios","title":"iOS","text":"<p>Requires AppConfig.preventSuspend <code>true</code>. Defaults to <code>60</code> seconds when enabled.</p>"},{"location":"typescript/AppConfig/#android","title":"Android","text":"<p>Heartbeat is disabled by default (<code>-1</code>). Set to a positive integer of at least <code>60</code> to enable it. Values below <code>60</code> are not supported.</p> <p>Warning</p> <ul> <li>On iOS, heartbeat fires only when AppConfig.preventSuspend is   <code>true</code>.</li> <li>On Android, the minimum supported interval is <code>60</code> seconds.</li> </ul> <p>See also - BackgroundGeolocation.onHeartbeat</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60\n  }\n});\n\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n\n  // Optionally request a new location during heartbeat.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition]\", location);\n  });\n});\n</code></pre>"},{"location":"typescript/AppConfig/#notification","title":"notification","text":"<p><code>notification?:</code><code>NotificationConfig</code><code>;</code></p> <p>Android only Configures the persistent foreground-service notification required by Android.</p> <p></p> <p>See NotificationConfig for detailed usage.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"Background tracking engaged\",\n      text: \"My notification text\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#preventsuspend","title":"preventSuspend","text":"<pre><code>preventSuspend?: boolean;\n</code></pre> <p>iOS only Prevents iOS from suspending the application after location services have been disabled while running in the background. Defaults to <code>false</code>. Set to <code>true</code> to keep the app alive in the background even after iOS disables location services. This is required for AppConfig.heartbeatInterval to fire on iOS.</p> <p>Warning</p> <p><code>preventSuspend: true</code> has a large and noticeable impact on battery consumption. Use it only for specific, time-limited use cases \u2014 it is not suitable for continuous 24/7 operation.</p> <p>When the device is unplugged with the screen off, iOS still throttles heartbeat events approximately 2 minutes after entering the background. Heartbeats resume immediately when the screen turns on or any device motion is detected.</p> <p>See also - AppConfig.heartbeatInterval - BackgroundGeolocation.onHeartbeat</p> <pre><code>// Subscribe to heartbeat events\nBackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat]\", event);\n});\n\n// Enable preventSuspend via AppConfig\nBackgroundGeolocation.ready({\n  app: {\n    preventSuspend: true,\n    heartbeatInterval: 60,\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#schedule","title":"schedule","text":"<pre><code>schedule?: string[];\n</code></pre> <p>Configures a cron-like automated schedule that tells the SDK when to <code>start</code> and <code>stop</code> tracking.</p> <pre><code>\"{DAY(s)} {START_TIME}-{END_TIME}\"\n</code></pre> <ul> <li>Times are in 24h format.</li> <li><code>DAY</code> uses Locale.US day numbers: Sunday = 1, Saturday = 7.</li> <li>Day values may be a single day (<code>\"1\"</code>), a comma-separated list (<code>\"2,4,6\"</code>),   or a range (<code>\"2-6\"</code>).</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1 17:30-21:00\",     // Sunday: 5:30pm\u20139pm\n      \"2-6 09:00-17:00\",   // Mon\u2013Fri: 9am\u20135pm\n      \"2,4,6 20:00-00:00\", // Mon, Wed, Fri: 8pm\u2013midnight\n      \"7 10:00-19:00\"      // Saturday: 10am\u20137pm\n    ]\n  }\n}).then((state) =&gt; {\n  // Start the Scheduler\n  BackgroundGeolocation.startSchedule();\n});\n\n// Listen for schedule state changes\nBackgroundGeolocation.onSchedule((state) =&gt; {\n  console.log(\"[onSchedule] enabled?\", state.enabled);\n});\n\n// Later (e.g., user logout)\nBackgroundGeolocation.stopSchedule();\nBackgroundGeolocation.stop(); // if tracking is currently active\n\n// Modify schedule using setConfig\nBackgroundGeolocation.setConfig({\n  app: {\n    schedule: [\n      \"1-7 09:00-10:00\",\n      \"1-7 11:00-12:00\",\n      \"1-7 13:00-14:00\",\n      \"1-7 15:00-16:00\",\n      \"1-7 17:00-18:00\",\n      \"2,4,6 19:00-22:00\"\n    ]\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#literal-dates","title":"Literal dates","text":"<p>A schedule entry may use a literal date range:</p> <pre><code>\"yyyy-mm-dd HH:mm-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01 09:00-17:00\"\n    ]\n  }\n});\n</code></pre> <p>Or specify distinct start and stop dates:</p> <pre><code>\"yyyy-mm-dd-HH:mm yyyy-mm-dd-HH:mm\"\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"2018-01-01-09:00 2019-01-01-17:00\" // track for 1 year\n    ]\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#scheduling-modes","title":"Scheduling modes","text":"<p>Append <code>geofence</code> or <code>location</code> to explicitly choose a tracking mode.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-00:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#ios_1","title":"iOS","text":"<ul> <li>iOS cannot evaluate the schedule exactly at the configured time.   Evaluation occurs only when the app is awakened.</li> <li>During a scheduled off period, iOS continues monitoring low-power   Significant Location Changes (SLC) to guarantee periodic re-evaluation,   especially when AppConfig.stopOnTerminate is <code>false</code> and the OS   halts traditional Background Fetch.</li> <li>Schedule evaluation occurs when the app pauses/resumes, any location is   recorded (including SLC), or a Background Fetch event fires.</li> </ul>"},{"location":"typescript/AppConfig/#android_1","title":"Android","text":"<p>Uses <code>AlarmManager.setExactAndAllowWhileIdle</code>, typically evaluating on-the-minute.</p> <p>See also - BackgroundGeolocation.startSchedule - BackgroundGeolocation.stopSchedule</p>"},{"location":"typescript/AppConfig/#since-location-is-the-default-mode-it-may-be-omitted","title":"Since <code>location</code> is the default mode, it may be omitted","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\n      \"1-7 09:00-17:00 location\",\n      \"1-7 18:00-12:00 geofence\"\n    ]\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#scheduleusealarmmanager","title":"scheduleUseAlarmManager","text":"<pre><code>scheduleUseAlarmManager?: boolean;\n</code></pre> <p>Android only Forces the Android scheduler to use <code>AlarmManager</code> (exact, on-the-minute) instead of <code>JobScheduler</code> (inexact). Defaults to <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    schedule: [\"1-7 09:00-17:00\"],\n    scheduleUseAlarmManager: true\n  }\n});\n</code></pre>"},{"location":"typescript/AppConfig/#startonboot","title":"startOnBoot","text":"<pre><code>startOnBoot?: boolean;\n</code></pre> <p>Controls whether tracking resumes automatically after the device reboots. Defaults to <code>false</code>.</p>"},{"location":"typescript/AppConfig/#ios_2","title":"iOS","text":"<p>iOS cannot begin tracking immediately after a reboot. Similar to AppConfig.stopOnTerminate <code>false</code>, the SDK waits until one of the following occurs: - The device exits the stationary geofence created around the last known   location. - A system Background Fetch event fires (typically every ~15 minutes).</p>"},{"location":"typescript/AppConfig/#android_2","title":"Android","text":"<p>When <code>true</code>, Android relaunches the SDK background service after reboot and initial device unlock. If AppConfig.enableHeadless is also <code>true</code>, tracking resumes even before the JS/UI layer starts.</p> <p>See also - AppConfig.enableHeadless - AppConfig.stopOnTerminate</p>"},{"location":"typescript/AppConfig/#stoponterminate","title":"stopOnTerminate","text":"<pre><code>stopOnTerminate?: boolean;\n</code></pre> <p>Controls whether tracking stops when the application is terminated by the user. Defaults to <code>true</code>.</p> <p>When <code>true</code>, the SDK calls <code>stop()</code> as the app terminates. Set to <code>false</code> to keep tracking alive after termination \u2014 iOS and Android behave differently in this state.</p>"},{"location":"typescript/AppConfig/#ios_3","title":"iOS","text":"<p>Before termination, the SDK registers a stationary geofence of GeoConfig.stationaryRadius meters around the last known position. When the device exits that geofence (~200 m), iOS fully relaunches the app in the background and tracking resumes automatically. This mechanism survives device reboot because iOS monitors geofences at the OS level.</p> <p>Demo video: https://www.youtube.com/watch?v=aR6r8qV1TI8&amp;t=214s</p> <p></p>"},{"location":"typescript/AppConfig/#android_3","title":"Android","text":"<p>Android does not pause tracking when the user terminates the app. The native background service continues running headlessly without the JS/UI process. Configure HttpConfig.url so the service can continue uploading locations to your server.</p> <p>See also - AppConfig.enableHeadless - Android Headless Mode</p>"},{"location":"typescript/AuthorizationConfig/","title":"AuthorizationConfig","text":"<pre><code>export interface AuthorizationConfig {\n</code></pre> <p>Token-based authorization configuration for the background geolocation SDK.</p> <p><code>AuthorizationConfig</code> enables the SDK to authenticate HTTP uploads with an <code>access token</code> and to automatically refresh that token when it expires or when the server returns <code>401 Unauthorized</code>.</p> <p>When using Config.authorization, do not set <code>Authorization</code> manually inside HttpConfig.headers \u2014 the SDK manages the authorization header automatically.</p>"},{"location":"typescript/AuthorizationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Token refresh</li> <li>Refresh payload</li> <li>Authorization events</li> </ul>"},{"location":"typescript/AuthorizationConfig/#overview","title":"Overview","text":"<p>The SDK attaches the configured access token to every HTTP upload request as an <code>Authorization</code> header. If you supply AuthorizationConfig.refreshUrl, AuthorizationConfig.refreshToken, and AuthorizationConfig.refreshPayload, the SDK can automatically refresh expired tokens when a <code>401 Unauthorized</code> response is received.</p> Category Properties Notes Token <code>strategy</code>, <code>accessToken</code>, <code>expires</code> Token value and expiry. Refresh <code>refreshUrl</code>, <code>refreshToken</code>, <code>refreshPayload</code>, <code>refreshHeaders</code> Auto-refresh on <code>401</code> or expiry. <pre><code>const token = getMyToken(); // Your own token fetch method\n\nBackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://app.your.server.com/users/locations\",\n    autoSync: true,\n  },\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshPayload: {\n      the_refresh_token_field_name: \"{refreshToken}\"\n    },\n    expires: token.expiresAt,\n  }\n});\n</code></pre>"},{"location":"typescript/AuthorizationConfig/#token-refresh","title":"Token refresh","text":"<p>When a <code>401 Unauthorized</code> response is received, or when <code>expires</code> is reached, the SDK sends an <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> with the <code>refreshPayload</code> fields encoded in the body.</p> <p>On receiving the response, the SDK recursively iterates through the JSON keys to locate:</p> <ol> <li>A new access token</li> <li>A new refresh token (if present)</li> <li>An expiry time (if present)</li> </ol> <p>The SDK recognizes a wide variety of response shapes automatically. For example, a nested response:</p> <pre><code>{\n  \"token\": {\n    \"access_token\": \"XXX.YYY.ZZZ\",\n    \"expires_at\": 3900\n  },\n  \"refresh_token\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\"\n}\n</code></pre> <p>And a flat response:</p> <pre><code>{\n  \"accessToken\": \"XXX.YYY.ZZZ\",\n  \"refreshToken\": \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n  \"expiry\": 3900\n}\n</code></pre> <p>Both are handled automatically.</p>"},{"location":"typescript/AuthorizationConfig/#refresh-payload","title":"Refresh payload","text":"<p><code>refreshPayload</code> is a key/value map sent as form fields in the refresh POST. At least one field must use the template variable <code>\"{refreshToken}\"</code>, which the SDK replaces with the current <code>refreshToken</code> value at request time. Additional arbitrary fields required by your authorization server may also be included.</p> <p>If <code>refreshHeaders</code> is not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into each refresh request. Pass an empty object <code>{}</code> to send no headers with the refresh request.</p>"},{"location":"typescript/AuthorizationConfig/#authorization-events","title":"Authorization events","text":"<p>Whenever the SDK receives a response from <code>refreshUrl</code>, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p> <ul> <li>On success, AuthorizationEvent.response contains the parsed   JSON from the refresh server.</li> <li>On error, AuthorizationEvent.error contains the error message.</li> </ul> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS:\", event.response);\n  } else {\n    console.log(\"[authorization] ERROR:\", event.error);\n  }\n});\n</code></pre>"},{"location":"typescript/AuthorizationConfig/#members","title":"Members","text":""},{"location":"typescript/AuthorizationConfig/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>The access token attached to each HTTP request as an <code>Authorization</code> header.</p> <p>The SDK automatically applies the token to every upload request sent to HttpConfig.url. Do not set <code>Authorization</code> manually in HttpConfig.headers.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\"\n  }\n});\n</code></pre>"},{"location":"typescript/AuthorizationConfig/#expires","title":"expires","text":"<pre><code>expires?: number;\n</code></pre> <p>Token expiry time in seconds since epoch. Defaults to <code>-1</code> (not set).</p> <p>When set, the SDK proactively refreshes the token before expiry rather than waiting for a <code>401 Unauthorized</code> response. When not set (default <code>-1</code>), the SDK relies on a <code>401</code> response to trigger a refresh.</p>"},{"location":"typescript/AuthorizationConfig/#refreshheaders","title":"refreshHeaders","text":"<pre><code>refreshHeaders?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers sent with each request to <code>refreshUrl</code>.</p> <p>If not set, the SDK automatically injects <code>Authorization: Bearer {accessToken}</code> into refresh requests, where <code>{accessToken}</code> is replaced with the current access token. To send no headers with refresh requests, provide an empty object <code>{}</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.domain.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    },\n    refreshHeaders: {}\n  }\n});\n</code></pre>"},{"location":"typescript/AuthorizationConfig/#refreshpayload","title":"refreshPayload","text":"<pre><code>refreshPayload?: Record&lt;string, string&gt;;\n</code></pre> <p>Form fields sent in the <code>application/x-www-form-urlencoded</code> POST to <code>refreshUrl</code> when requesting a new <code>accessToken</code> after expiration.</p> <p>Include at least one field whose value is the template string <code>\"{refreshToken}\"</code>. The SDK automatically replaces this with the configured <code>refreshToken</code> value before sending the request.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: \"XXX.YYY.ZZZ\",\n    refreshUrl: \"https://auth.your.server.com/tokens\",\n    refreshToken: \"smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb\",\n    refreshPayload: {\n      my_refresh_token: \"{refreshToken}\",\n      grant_type: \"refresh_token\",\n      foo: \"another arbitrary field\"\n    }\n  }\n});\n</code></pre> <p>The equivalent <code>curl</code> representation of the SDK's form POST:</p> <pre><code>$ curl -X POST \\\n  -F 'my_refresh_token=smTsfaspfgaadsfgqZerUt0wueflasdfkaxjdfeKIacb' \\\n  -F 'grant_type=refresh_token' \\\n  -F 'foo=another arbitrary field' \\\n  https://auth.your.server.com/tokens\n</code></pre>"},{"location":"typescript/AuthorizationConfig/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken?: string;\n</code></pre> <p>The token sent to <code>refreshUrl</code> when a new <code>accessToken</code> is needed after <code>expires</code> or when HTTP <code>401 Unauthorized</code> is received.</p> <p>The SDK encodes this value into the <code>refreshPayload</code> by substituting the <code>\"{refreshToken}\"</code> template variable before sending the refresh request.</p>"},{"location":"typescript/AuthorizationConfig/#refreshurl","title":"refreshUrl","text":"<pre><code>refreshUrl?: string;\n</code></pre> <p>The URL of the authorization server that provides a new <code>accessToken</code> when the current token expires.</p> <p>The SDK POSTs to this URL using <code>application/x-www-form-urlencoded</code> when the current token expires or when the location server returns <code>401 Unauthorized</code>. The response is parsed automatically \u2014 see Token refresh for supported response formats.</p> <p>When the SDK receives a response from the server, it fires BackgroundGeolocation.onAuthorization with an AuthorizationEvent.</p>"},{"location":"typescript/AuthorizationConfig/#strategy","title":"strategy","text":"<pre><code>strategy: string;\n</code></pre> <p>Authorization strategy used when attaching the token to HTTP requests. Defaults to <code>\"JWT\"</code>.</p> <ul> <li><code>\"JWT\"</code> \u2014 sends the token as <code>Authorization: Bearer &lt;token&gt;</code>.</li> <li><code>\"SAS\"</code> \u2014 sends the token as <code>Authorization: &lt;token&gt;</code> (no <code>Bearer</code> prefix).</li> </ul>"},{"location":"typescript/AuthorizationEvent/","title":"AuthorizationEvent","text":"<pre><code>export interface AuthorizationEvent {\n</code></pre> <p>Authorization token refresh result delivered to BackgroundGeolocation.onAuthorization.</p> <p>The SDK fires this event after each token refresh attempt triggered by AuthorizationConfig.refreshUrl. Use it to update your app's local credential store, handle refresh failures, or react to expired sessions.</p> <pre><code>BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onAuthorization] Token refreshed:\", event.response);\n    // Update your app's stored token.\n    const token = event.response.access_token;\n  } else {\n    console.warn(\"[onAuthorization] Refresh failed:\", event.error);\n  }\n});\n</code></pre>"},{"location":"typescript/AuthorizationEvent/#members","title":"Members","text":""},{"location":"typescript/AuthorizationEvent/#error","title":"error","text":"<pre><code>error: string;\n</code></pre> <p>Error message when <code>success</code> is <code>false</code>; <code>null</code> otherwise.</p>"},{"location":"typescript/AuthorizationEvent/#response","title":"response","text":"<pre><code>response: any;\n</code></pre> <p>Decoded JSON body returned by the refresh server when <code>success</code> is <code>true</code>; <code>null</code> otherwise.</p>"},{"location":"typescript/AuthorizationEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by AuthorizationConfig.refreshUrl, or <code>0</code> if the request failed before reaching the server.</p>"},{"location":"typescript/AuthorizationEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the token refresh request returned a 2xx status code.</p>"},{"location":"typescript/AuthorizationStatus/","title":"AuthorizationStatus","text":"<pre><code>export const AuthorizationStatus = {\n</code></pre> <p>iOS/Android location authorization status.</p> Name Value Platform NotDetermined 0 iOS only Restricted 1 iOS only Denied 2 iOS &amp; Android Always 3 iOS &amp; Android WhenInUse 4 iOS &amp; Android 10+"},{"location":"typescript/AuthorizationStatus/#members","title":"Members","text":""},{"location":"typescript/AuthorizationStatus/#always","title":"Always","text":"<p>Location permissions authorized for always use.</p>"},{"location":"typescript/AuthorizationStatus/#denied","title":"Denied","text":"<p>Location permissions denied by the user.</p>"},{"location":"typescript/AuthorizationStatus/#notdetermined","title":"NotDetermined","text":"<pre><code>NotDetermined: 0,\n</code></pre> <p>User has not yet made a choice regarding location permissions.</p>"},{"location":"typescript/AuthorizationStatus/#restricted","title":"Restricted","text":"<pre><code>Restricted: 1,\n</code></pre> <p>Location permissions are restricted (e.g., parental controls).</p>"},{"location":"typescript/AuthorizationStatus/#wheninuse","title":"WhenInUse","text":"<p>Location permissions authorized for when-in-use (Android 10+).</p>"},{"location":"typescript/AuthorizationStrategy/","title":"AuthorizationStrategy","text":"<pre><code>export const AuthorizationStrategy = {\n</code></pre> <p>Supported authorization strategies. Mirrors Flutter's Authorization.strategy values.</p>"},{"location":"typescript/AuthorizationStrategy/#members","title":"Members","text":""},{"location":"typescript/AuthorizationStrategy/#jwt","title":"Jwt","text":"<p>JWT-based authorization with accessToken and refreshToken. Access token is sent in the Authorization header of each request. Refresh token is used to obtain new access tokens when they expire.</p>"},{"location":"typescript/AuthorizationStrategy/#sas","title":"Sas","text":"<p>Shared Access Signature (SAS) token-based authorization. SAS token is sent in the Authorization header of each request. Client is responsible for refreshing the SAS token before it expires.</p>"},{"location":"typescript/BackgroundGeolocation/","title":"BackgroundGeolocation","text":"<pre><code>export interface BackgroundGeolocation {\n</code></pre> <p>Primary SDK API \u2014 the single entry point for all geolocation, geofencing, HTTP sync, and configuration operations.</p>"},{"location":"typescript/BackgroundGeolocation/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Lifecycle</li> <li>Configuration</li> <li>Events</li> <li>Examples</li> </ul>"},{"location":"typescript/BackgroundGeolocation/#overview","title":"Overview","text":"<p>The SDK operates around a motion-based state machine: it tracks aggressively while the device is moving and pauses location services when stationary, delivering high-quality background tracking with minimal battery impact.</p> Area Key methods Lifecycle <code>ready</code>, <code>start</code>, <code>stop</code>, <code>setConfig</code>, <code>reset</code> Location <code>getCurrentPosition</code>, <code>watchPosition</code>, <code>getOdometer</code> Geofencing <code>addGeofence</code>, <code>startGeofences</code>, <code>onGeofence</code> Events <code>onLocation</code>, <code>onMotionChange</code>, <code>onHttp</code>, <code>onProviderChange</code> Persistence <code>getLocations</code>, <code>getCount</code>, <code>sync</code>, <code>destroyLocations</code> Background tasks <code>startBackgroundTask</code>, <code>stopBackgroundTask</code>"},{"location":"typescript/BackgroundGeolocation/#lifecycle","title":"Lifecycle","text":"<p>Think of the SDK like a stereo receiver:</p> <ul> <li> <p>Wiring the speakers \u2014 Register event listeners (<code>onLocation</code>, <code>onGeofence</code>, etc.)   before calling <code>ready</code>. The SDK buffers events until <code>ready</code> resolves, so listeners   registered afterward may miss them. You do not need to remove listeners when you call   <code>stop</code> \u2014 the SDK simply stops emitting events when it isn't running.</p> </li> <li> <p>Plugging in the power cord \u2014 <code>ready</code> initializes the SDK, restores   persisted state, and applies your configuration. Call it once per launch, before any   method that acquires a location or requests permissions. Your config is not applied   until <code>ready</code> resolves.</p> </li> <li> <p>The power button \u2014 <code>start</code> and <code>stop</code> begin and halt location tracking. The SDK   persists its enabled state across launches. If the app is terminated while tracking   is active, the next call to <code>ready</code> will automatically resume tracking \u2014 you do not   need to call <code>start</code> again.</p> </li> </ul> <p>Always call <code>ready</code> on every launch \u2014 no exceptions. The SDK buffers all events from the moment the app starts, and holds them until <code>ready</code> is called. If your app launches and never calls <code>ready</code>, the SDK sits silently waiting: no events fire, no locations are recorded, no uploads are attempted. It does not matter whether tracking was already active from a previous session \u2014 <code>ready</code> is the signal that tells the SDK your app is alive and listening. This is why the method is named <code>ready</code>.</p> <p>Calling methods before <code>ready</code> resolves is perfectly fine, provided they do not request a location or trigger a permission dialog. Methods that only read from the SDK's SQLite database are safe \u2014 for example <code>getState</code>, <code>getLocations</code>, <code>getGeofences</code>, <code>removeGeofences</code>. Avoid <code>start</code>, <code>requestPermission</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code> until after <code>ready</code> resolves. The SDK defaults apply until your config arrives \u2014 calling a permission-sensitive method too early will use those defaults, not your configured values.</p>"},{"location":"typescript/BackgroundGeolocation/#configuration","title":"Configuration","text":"<p>The SDK uses a compound-configuration model. Options are grouped into typed sub-interfaces (GeoConfig, HttpConfig, AppConfig, etc.) passed as a single Config object. All SDK constants are available as strongly-typed enum namespaces on the default export:</p> <ul> <li>LogLevel</li> <li>DesiredAccuracy</li> <li>PersistMode</li> <li>Event</li> </ul> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"react-native-background-geolocation\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#events","title":"Events","text":"<p>Each <code>onX</code> method returns a Subscription that must be removed when no longer needed:</p>"},{"location":"typescript/BackgroundGeolocation/#these-can-also-be-imported-individually","title":"These can also be imported individually","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre> <pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#examples","title":"Examples","text":"<pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#compound-configuration","title":"Compound configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  HttpConfig\n} from \"{{pluginName}}\";\n\n// Register event-listeners before calling .ready()\nBackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 20\n  },\n  http: {\n    url: \"https://example.com/locations\",\n    autoSync: true\n  },\n  persistence: {\n    maxDaysToPersist: 7\n  }\n};\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#event-listeners","title":"Event listeners","text":"<pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"New location:\", location);\n});\n\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log(\"Device is moving?\", event.isMoving);\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#removing-event-listeners","title":"Removing event listeners","text":"<pre><code>const sub = BackgroundGeolocation.onHttp((e) =&gt; { ... });\nsub.remove();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getting-started","title":"Getting started","text":"<pre><code>import BackgroundGeolocation from \"{{pluginName}}\";\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { distanceFilter: 10 },\n  http: { url: \"https://example.com/locations\", autoSync: true }\n});\n\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#events_1","title":"Events","text":""},{"location":"typescript/BackgroundGeolocation/#onactivitychange","title":"onActivityChange","text":"<p><code>onActivityChange(cb: (event:</code><code>MotionActivityEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-activity changes.</p> <p>Fires each time the activity-recognition system reports a new activity (<code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>, <code>on_bicycle</code>, <code>running</code>).</p>"},{"location":"typescript/BackgroundGeolocation/#android","title":"Android","text":"<p>MotionActivityEvent.confidence always reports <code>100</code>.</p> <pre><code>const subscription = BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange] \", event);\n});\n</code></pre> <p>activitychange</p>"},{"location":"typescript/BackgroundGeolocation/#onauthorization","title":"onAuthorization","text":"<p><code>onAuthorization(cb: (event:</code><code>AuthorizationEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to Config.authorization events.</p> <p>Fires when AuthorizationConfig.refreshUrl responds, either successfully or not. On success, AuthorizationEvent.success is <code>true</code> and AuthorizationEvent.response contains the decoded JSON response. On failure, AuthorizationEvent.error contains the error message.</p> <pre><code>const subscription = BackgroundGeolocation.onAuthorization((event) =&gt; {\n  if (event.success) {\n    console.log(\"[authorization] SUCCESS: \", event.response);\n  } else {\n    console.log(\"[authorization] ERROR: \", event.error);\n  }\n});\n</code></pre> <p>authorization</p>"},{"location":"typescript/BackgroundGeolocation/#onconnectivitychange","title":"onConnectivityChange","text":"<p><code>onConnectivityChange(cb: (event:</code><code>ConnectivityChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to network connectivity changes.</p> <p>Fires when the device's network connectivity transitions between connected and disconnected. By default, the SDK also fires this event at <code>start</code> time with the current connectivity state. When connectivity is restored and the SDK has queued locations, it automatically initiates an upload to HttpConfig.url.</p> <pre><code>const subscription = BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] \", event);\n});\n</code></pre> <p>connectivitychange</p>"},{"location":"typescript/BackgroundGeolocation/#onenabledchange","title":"onEnabledChange","text":"<p><code>onEnabledChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in plugin State.enabled.</p> <p>Fires when State.enabled changes. Calling <code>start</code> or <code>stop</code> triggers this event.</p> <pre><code>const subscription = BackgroundGeolocation.onEnabledChange((isEnabled) =&gt; {\n  console.log(\"[onEnabledChange] isEnabled? \", isEnabled);\n});\n</code></pre> <p>enabledchange</p>"},{"location":"typescript/BackgroundGeolocation/#ongeofence","title":"onGeofence","text":"<p><code>on</code><code>Geofence</code><code>(cb: (event:</code><code>GeofenceEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to geofence transition events.</p> <p>Fires when any monitored geofence crossing occurs.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] \", event);\n});\n</code></pre> <p>geofence</p>"},{"location":"typescript/BackgroundGeolocation/#ongeofenceschange","title":"onGeofencesChange","text":"<p><code>onGeofencesChange(cb: (event:</code><code>GeofencesChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to changes in the set of actively monitored geofences.</p> <p>Fires when the SDK's active geofence set changes. The SDK can monitor any number of geofences in its database \u2014 even thousands \u2014 despite native platform limits (20 for iOS; 100 for Android). It achieves this with a geospatial query that activates only the geofences nearest to the device's current location (see GeoConfig.geofenceProximityRadius). When the device is moving, the query runs periodically and the active set may change \u2014 that change triggers this event.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  // Create map circles\n  on.forEach((geofence) =&gt; {\n    createGeofenceMarker(geofence);\n  });\n\n  // Remove map circles\n  off.forEach((identifier) =&gt; {\n    removeGeofenceMarker(identifier);\n  });\n});\n</code></pre> <p>geofenceschange</p>"},{"location":"typescript/BackgroundGeolocation/#onheartbeat","title":"onHeartbeat","text":"<p><code>onHeartbeat(cb: (event:</code><code>HeartbeatEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to periodic heartbeat events.</p> <p>Fires at each AppConfig.heartbeatInterval while the device is in the stationary state. On iOS, AppConfig.preventSuspend must also be <code>true</code> to receive heartbeats in the background.</p> <p>Note</p> <p>The Location provided by the HeartbeatEvent is only the last-known location \u2014 the heartbeat does not engage location services. To fetch a fresh location inside your callback, call <code>getCurrentPosition</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    heartbeatInterval: 60,\n    preventSuspend: true  // required for iOS\n  }\n});\n\nconst subscription = BackgroundGeolocation.onHeartbeat((event) =&gt; {\n  console.log(\"[onHeartbeat] \", event);\n\n  // Optionally fetch a fresh location.\n  BackgroundGeolocation.getCurrentPosition({\n    samples: 1,\n    persist: true\n  }).then((location) =&gt; {\n    console.log(\"[getCurrentPosition] \", location);\n  });\n});\n</code></pre> <p>heartbeat</p>"},{"location":"typescript/BackgroundGeolocation/#onhttp","title":"onHttp","text":"<p><code>onHttp(cb: (event:</code><code>HttpEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to HTTP responses from your server HttpConfig.url.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const subscription = BackgroundGeolocation.onHttp((response) =&gt; {\n  const status = response.status;\n  const success = response.success;\n  const responseText = response.responseText;\n  console.log(\"[onHttp] \", response);\n});\n</code></pre> <p>http</p>"},{"location":"typescript/BackgroundGeolocation/#onlocation","title":"onLocation","text":"<p><code>on</code><code>Location</code><code>(cb: (location:</code><code>Location</code><code>) =&gt; void, onError?: (err:</code><code>LocationError</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location events.</p> <p>Every location recorded by the SDK is delivered to your callback, including locations from <code>onMotionChange</code>, <code>getCurrentPosition</code>, and <code>watchPosition</code>.</p>"},{"location":"typescript/BackgroundGeolocation/#error-codes","title":"Error Codes","text":"<p>If the native location API fails, the error callback receives a LocationError code.</p> <p>Note</p> <p>During <code>onMotionChange</code> and <code>getCurrentPosition</code>, the SDK requests multiple location samples to find the most accurate fix. These intermediate samples are not persisted, but are delivered to this callback with Location.sample set to <code>true</code>. Filter out sample locations before manually posting to your server.</p> <pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation] success: \", location);\n}, (error) =&gt; {\n  console.log(\"[onLocation] ERROR: \", error);\n});\n</code></pre> <p>location</p>"},{"location":"typescript/BackgroundGeolocation/#onmotionchange","title":"onMotionChange","text":"<p><code>onMotionChange(cb: (event:</code><code>MotionChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to motion-change events.</p> <p>Fires each time the device transitions between the moving and stationary states.</p> <p>Warning</p> <p>When a motion-change event fires, HttpConfig.autoSyncThreshold is ignored \u2014 all queued locations are uploaded immediately. The SDK flushes eagerly before going dormant (moving\u2192stationary) and immediately after waking up (stationary\u2192moving).</p> <p>See also - GeoConfig.stopTimeout</p> <pre><code>const subscription = BackgroundGeolocation.onMotionChange((event: MotionChangeEvent) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device has just started MOVING \", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device has just STOPPED: \", event.location);\n  }\n});\n</code></pre> <p>motionchange</p>"},{"location":"typescript/BackgroundGeolocation/#onnotificationaction","title":"onNotificationAction","text":"<p><code>onNotificationAction(cb: (buttonId: string) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Android only Subscribe to button-click actions on the Android foreground-service notification.</p> <p>Fires when the user taps a button defined in a custom NotificationConfig.layout.</p>"},{"location":"typescript/BackgroundGeolocation/#onpowersavechange","title":"onPowerSaveChange","text":"<p><code>onPowerSaveChange(cb: (enabled: boolean) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to OS power-saving mode changes.</p> <p>Fires when the operating system's power-saving mode is enabled or disabled. Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>isPowerSaveMode</code></p>"},{"location":"typescript/BackgroundGeolocation/#ios","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"typescript/BackgroundGeolocation/#android_1","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const subscription = BackgroundGeolocation.onPowerSaveChange((isPowerSaveMode) =&gt; {\n  console.log(\"[onPowerSaveChange]: \", isPowerSaveMode);\n});\n</code></pre> <p>powersavechange</p>"},{"location":"typescript/BackgroundGeolocation/#onproviderchange","title":"onProviderChange","text":"<p><code>onProviderChange(cb: (event:</code><code>ProviderChangeEvent</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to location-services authorization changes.</p> <p>Fires whenever the state of the device's location-services authorization changes (e.g. GPS enabled, WiFi-only, permission revoked). The SDK also fires this event immediately after <code>ready</code> completes, so you always receive the current authorization state on each app launch.</p> <p>See also - <code>getProviderState</code></p> <pre><code>const subscription = BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]: \", event);\n\n  switch (event.status) {\n    case BackgroundGeolocation.AuthorizationStatus.Denied:\n      // Android &amp; iOS\n      console.log(\"- Location authorization denied\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.Always:\n      // Android &amp; iOS\n      console.log(\"- Location always granted\");\n      break;\n    case BackgroundGeolocation.AuthorizationStatus.WhenInUse:\n      // iOS only\n      console.log(\"- Location WhenInUse granted\");\n      break;\n  }\n});\n</code></pre> <p>providerchange</p>"},{"location":"typescript/BackgroundGeolocation/#onschedule","title":"onSchedule","text":"<p><code>onSchedule(cb: (state:</code><code>State</code><code>) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Subscribe to AppConfig.schedule events.</p> <p>Fires each time a schedule event activates or deactivates tracking. Check <code>state.enabled</code> in your callback to determine whether tracking was started or stopped.</p> <pre><code>const subscription = BackgroundGeolocation.onSchedule((state) =&gt; {\n  if (state.enabled) {\n    console.log(\"[onSchedule] scheduled start tracking\");\n  } else {\n    console.log(\"[onSchedule] scheduled stop tracking\");\n  }\n});\n</code></pre> <p>schedule</p>"},{"location":"typescript/BackgroundGeolocation/#methods","title":"Methods","text":""},{"location":"typescript/BackgroundGeolocation/#changepace","title":"changePace","text":"<p><code>changePace(isMoving: boolean): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Manually toggle the SDK's motion state between stationary and moving.</p> <p>Passing <code>true</code> immediately engages location services and begins tracking, bypassing stationary monitoring. Passing <code>false</code> turns off location services and returns the SDK to the stationary state.</p> <p>Use this in workout-style apps where you want explicit start/stop control independent of the device's motion sensors.</p> <pre><code>BackgroundGeolocation.changePace(true);  // location services ON (\"moving\")\nBackgroundGeolocation.changePace(false); // location services OFF (\"stationary\")\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#destroytransistorauthorizationtoken","title":"destroyTransistorAuthorizationToken","text":"<p><code>destroy</code><code>TransistorAuthorizationToken</code><code>(url: string): Promise&lt;void&gt;;</code></p> <p>Destroy a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>"},{"location":"typescript/BackgroundGeolocation/#findorcreatetransistorauthorizationtoken","title":"findOrCreateTransistorAuthorizationToken","text":"<p><code>findOrCreateTransistorAuthorizationToken(orgname: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create a Transistor authorization token.</p> <p>See TransistorAuthorizationService for more information.</p>"},{"location":"typescript/BackgroundGeolocation/#getcurrentposition","title":"getCurrentPosition","text":"<p><code>getCurrentPosition(options?:</code><code>CurrentPositionRequest</code><code>): Promise&lt;</code><code>Location</code><code>&gt;;</code></p> <p>Retrieve the current Location.</p> <p>Instructs the SDK to fetch a single location at maximum power and accuracy. The location is persisted to SQLite and posted to HttpConfig.url just like any other recorded location. If an error occurs, the promise rejects with a LocationError.</p>"},{"location":"typescript/BackgroundGeolocation/#options","title":"Options","text":"<p>See CurrentPositionRequest.</p>"},{"location":"typescript/BackgroundGeolocation/#error-codes_1","title":"Error Codes","text":"<p>See LocationError.</p> <p>Note</p> <p>The SDK requests multiple location samples internally to find the best fix. All intermediate samples are delivered to <code>onLocation</code> with Location.sample set to <code>true</code>. Filter these out if you are manually posting locations to your server.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // 30 second timeout to fetch location\n  maximumAge: 5000,     // Accept the last-known-location if not older than 5000 ms.\n  desiredAccuracy: 10,  // Try to fetch a location with an accuracy of `10` meters.\n  samples: 3,           // How many location samples to attempt.\n  extras: {             // Custom meta-data.\n    \"route_id\": 123\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getdeviceinfo","title":"getDeviceInfo","text":"<p><code>get</code><code>DeviceInfo</code><code>(): Promise&lt;</code><code>DeviceInfo</code><code>&gt;;</code></p> <p>Returns device information.</p> <pre><code>const deviceInfo = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(deviceInfo);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getodometer","title":"getOdometer","text":"<pre><code>getOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the current odometer reading in meters.</p> <p>The SDK continuously accumulates distance traveled between recorded locations.</p> <p>Warning</p> <p>Odometer accuracy depends on location accuracy. Noisy or inaccurate locations introduce error into accumulated distance. Use LocationFilter.odometerAccuracyThreshold to filter low-accuracy samples from odometer calculations.</p> <p>See also - LocationFilter.odometerAccuracyThreshold - <code>resetOdometer</code> / <code>setOdometer</code></p> <pre><code>const odometer = await BackgroundGeolocation.getOdometer();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getstate","title":"getState","text":"<p><code>get</code><code>State</code><code>(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Return the current State of the SDK, including all Config parameters.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] \", state.enabled, state.trackingMode);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#ready","title":"ready","text":"<p><code>ready(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Signal to the SDK that your app is launched and ready, supplying the default Config.</p> <p>Call <code>ready</code> exactly once per app launch, before calling <code>start</code>. The SDK applies your configuration, restores persisted state, and prepares for tracking. On subsequent launches after first install, it loads the persisted configuration and merges your supplied Config on top. See Config.reset for finer control over this behaviour.</p> <p>Warning</p> <p>Call <code>ready</code> once per app launch from your application root \u2014 not inside a component or behind a UI action. On iOS, the OS can relaunch your app in the background when the device starts moving; if <code>ready</code> is not called in that path, tracking will not resume.</p> <p>See also - Config.reset - <code>setConfig</code></p> <pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n  http: {\n    url: \"http://your.server.com\",\n    headers: { \"my-auth-token\": \"secret-token\" },\n  }\n});\nconsole.log(\"[ready] success\", state);\n</code></pre> <pre><code>BackgroundGeolocation.reset();\n// Reset to documented default-values with overrides\nbgGeo.reset({\n  distanceFilter:  10\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#registerheadlesstask","title":"registerHeadlessTask","text":"<p><code>registerHeadlessTask(callback: (event:</code><code>HeadlessEvent</code><code>) =&gt; Promise&lt;void&gt;): void;</code></p> <p>Android only Registers a headless-task callback for Android background events when the app has been terminated with AppConfig.stopOnTerminate<code>:false</code>.</p> <p>The callback receives a HeadlessEvent with a <code>name</code> (event name) and <code>params</code> (event data).</p> <p>Warning</p> <p>You must call <code>registerHeadlessTask</code> in your application root file (e.g. <code>index.js</code>), not inside a component or behind a UI action.</p> <p>Warning</p> <p>Your function must be declared <code>async</code>. Await all work inside it \u2014 the headless task is automatically terminated after the last line executes.</p> <p>Note</p> <p>Javascript headless callbacks are not supported by Cordova or Capacitor.</p>"},{"location":"typescript/BackgroundGeolocation/#debugging","title":"Debugging","text":"<p>While implementing your headless task, observe Android logs via:</p> <pre><code>$ adb logcat *:S TSLocationManager:V ReactNativeJS:V\n</code></pre> <p>See also - \ud83d\udcd8 Android Headless Mode - AppConfig.enableHeadless</p> <pre><code>const BackgroundGeolocationHeadlessTask = async (event) =&gt; {\n  const params = event.params;\n  console.log(\"[BackgroundGeolocation HeadlessTask] -\", event.name, params);\n\n  switch (event.name) {\n    case \"terminate\":\n      // Use await for async tasks\n      const location = await BackgroundGeolocation.getCurrentPosition({\n        samples: 1,\n        persist: false\n      });\n      console.log(\"[BackgroundGeolocation HeadlessTask] - getCurrentPosition:\", location);\n      break;\n  }\n  // You must await all work you do in your task.\n  // Headless-tasks are automatically terminated after executing the last line.\n  await doWork();\n}\n\nBackgroundGeolocation.registerHeadlessTask(BackgroundGeolocationHeadlessTask);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#removelisteners","title":"removeListeners","text":"<pre><code>removeListeners(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all event listeners.</p> <p>Calls Subscription.remove on all active subscriptions.</p> <pre><code>BackgroundGeolocation.removeListeners();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#reset","title":"reset","text":"<p><code>reset(config:</code><code>Config</code><code>): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Reset the SDK configuration to documented default values.</p> <p>If an optional Config is provided, it is applied after the reset.</p> <pre><code>BackgroundGeolocation.reset();\n// Reset to default values with overrides\nBackgroundGeolocation.reset({\n  geolocation: { distanceFilter: 10 }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#resetodometer","title":"resetOdometer","text":"<pre><code>resetOdometer(): Promise&lt;number&gt;;\n</code></pre> <p>Reset the odometer to <code>0</code>.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was reset. Equivalent to <code>.setOdometer(0)</code>.</p> <pre><code>const location = await BackgroundGeolocation.resetOdometer();\nconsole.log(\"[resetOdometer] reset at: \", location);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#setconfig","title":"setConfig","text":"<p><code>set</code><code>Config</code><code>(config: Partial&lt;</code><code>Config</code><code>&gt;): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Update the SDK's Config at runtime.</p> <p>The supplied Config is merged into the current configuration and applied immediately. Use this after <code>ready</code> has been called to change settings dynamically.</p> <pre><code>const state = await BackgroundGeolocation.setConfig({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 100.0,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\nconsole.log(\"[setConfig] success: \", state);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#setodometer","title":"setOdometer","text":"<pre><code>setOdometer(value: number): Promise&lt;number&gt;;\n</code></pre> <p>Set the odometer to an arbitrary value.</p> <p>Internally performs a <code>getCurrentPosition</code> to record the exact location where the odometer was set.</p> <pre><code>const location = await BackgroundGeolocation.setOdometer(1234.56);\nconsole.log(\"[setOdometer] set at: \", location);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#start","title":"start","text":"<p><code>start(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Enable location and geofence tracking.</p> <p>This is the SDK's power ON switch. The SDK enters its stationary state, acquires an initial location, then turns off location services until motion is detected. On Android, the Activity Recognition System monitors for motion; on iOS, a stationary geofence is created around the current location.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>start</code> overrides the schedule and begins tracking immediately.</p> <p>See also - <code>stop</code> - <code>startGeofences</code></p> <pre><code>const state = await BackgroundGeolocation.start();\nconsole.log(\"[start] success - \", state);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#startgeofences","title":"startGeofences","text":"<p><code>startGeofences(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Switch to geofences-only tracking mode.</p> <p>In this mode no active location tracking occurs \u2014 only geofences are monitored. Use the usual <code>stop</code> method to exit geofences-only mode.</p> <p><code>start</code> and <code>startGeofences</code> are mutually exclusive \u2014 call one or the other, never both. <code>start</code> enables full tracking: location recording and geofence monitoring run together. <code>startGeofences</code> enables geofence monitoring only, with no continuous location recording. Calling <code>start</code> while already in geofences-only mode (or vice versa) switches modes; there is no need to call <code>stop</code> first.</p> <p>See also - <code>stop</code> - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>// Add a geofence.\nBackgroundGeolocation.addGeofence({\n  notifyOnExit: true,\n  radius: 200,\n  identifier: \"ZONE_OF_INTEREST\",\n  latitude: 37.234232,\n  longitude: 42.234234\n});\n\n// Listen to geofence events.\nBackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence] - \", event);\n});\n\n// Configure and start in geofences-only mode.\nBackgroundGeolocation.ready({\n  http: { url: \"http://my.server.com\", autoSync: true }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#startschedule","title":"startSchedule","text":"<pre><code>startSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Activate the configured AppConfig.schedule.</p> <p>Initiates the schedule defined in AppConfig.schedule. The SDK automatically starts or stops tracking according to the schedule. To halt scheduled tracking, call <code>stopSchedule</code>.</p> <p>See also - AppConfig.schedule - <code>stopSchedule</code></p> <pre><code>const state = await BackgroundGeolocation.startSchedule();\nconsole.log(\"[startSchedule] success: \", state);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#stop","title":"stop","text":"<p><code>stop(): Promise&lt;</code><code>State</code><code>&gt;;</code></p> <p>Disable location and geofence monitoring.</p> <p>This is the SDK's power OFF switch.</p> <p>Note</p> <p>If a AppConfig.schedule is configured, <code>stop</code> does not halt the scheduler. Call <code>stopSchedule</code> explicitly if you also want to stop scheduled tracking (for example, on user logout).</p> <pre><code>BackgroundGeolocation.stop();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#stop-tracking-and-the-scheduler","title":"Stop tracking and the scheduler","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nBackgroundGeolocation.stopSchedule();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#stopschedule","title":"stopSchedule","text":"<pre><code>stopSchedule(): Promise&lt;void&gt;;\n</code></pre> <p>Halt scheduled tracking.</p> <p>Warning</p> <p><code>stopSchedule</code> does not call <code>stop</code> if the SDK is currently tracking. Call <code>stop</code> explicitly if you also want to end the current tracking session.</p> <p>See also - <code>startSchedule</code></p> <pre><code>await BackgroundGeolocation.stopSchedule();\n</code></pre> <pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#stop-the-scheduler-and-active-tracking","title":"Stop the scheduler and active tracking","text":"<pre><code>// Later when you want to stop the Scheduler (eg: user logout)\nawait BackgroundGeolocation.stopSchedule();\nconst state = await BackgroundGeolocation.getState();\nif (state.enabled) {\n  BackgroundGeolocation.stop();\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#watchposition","title":"watchPosition","text":"<p><code>watchPosition(options:</code><code>WatchPositionRequest</code><code>, locationCallback: (location:</code><code>Location</code><code>) =&gt; void, errorCallback?: (errorCode: number) =&gt; void):</code><code>Subscription</code><code>;</code></p> <p>Start a continuous stream of location updates.</p> <p>Each location is persisted to SQLite (when the SDK is State.enabled) and posted to HttpConfig.url if HTTP is configured. Returns a Subscription that must be retained to halt the stream.</p> <p>Warning</p> <p><code>watchPosition</code> is designed for foreground use only \u2014 not for long-term background monitoring. The SDK's motion-based tracking model does not require it.</p>"},{"location":"typescript/BackgroundGeolocation/#ios_1","title":"iOS","text":"<p><code>watchPosition</code> continues running in the background, preventing iOS from suspending your app. Remove the subscription in your app's suspend handler to avoid draining the battery.</p> <pre><code>onResume() async {\n  // Start watching position while app in foreground, retaining the return Subscription.\n  this.watchPositionSubscription = await BackgroundGeolocation.watchPosition({\n    interval: 1000,\n    extras: {foo: \"bar\"}\n  }, (location) =&gt; {\n    console.log(\"[watchPosition] -\", location);\n  }, (errorCode) =&gt; {\n    console.log(\"[watchPosition] ERROR -\", errorCode);\n  });\n}\n\nonSuspend() {\n  // Halt watching position when app goes to background.\n  this.watchPositionSubscription.remove();\n  this.watchPositionSubscription = null;\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#geofencing","title":"Geofencing","text":""},{"location":"typescript/BackgroundGeolocation/#addgeofence","title":"addGeofence","text":"<p><code>add</code><code>Geofence</code><code>(geofence:</code><code>Geofence</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Add a Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If a geofence with the same Geofence.identifier already exists, it is deleted before the new one is inserted. When adding multiple geofences, <code>addGeofences</code> is approximately 10\u00d7 faster.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 150,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: false,\n  notifyOnDwell: true,\n  loiteringDelay: 30000,  // 30 seconds\n  extras: {               // Optional arbitrary meta-data\n    zone_id: 1234\n  }\n}).then((success) =&gt; {\n  console.log(\"[addGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[addGeofence] FAILURE: \", error);\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#addgeofences","title":"addGeofences","text":"<p><code>addGeofences(geofences:</code><code>Geofence</code><code>[]): Promise&lt;boolean&gt;;</code></p> <p>Add a list of Geofence to be monitored by the native geofencing API.</p> <p>Note</p> <p>If any geofence already exists with a matching Geofence.identifier, it is deleted before the new one is inserted.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - <code>addGeofence</code></p> <pre><code>const geofences = [{\n  identifier: \"foo\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}, {\n  identifier: \"bar\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true\n}];\n\nBackgroundGeolocation.addGeofences(geofences);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#geofenceexists","title":"geofenceExists","text":"<pre><code>geofenceExists(identifier: string): Promise&lt;boolean&gt;;\n</code></pre> <p>Determine whether a geofence with the given identifier exists in the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const exists = await BackgroundGeolocation.geofenceExists(\"HOME\");\nconsole.log(\"[geofenceExists] \", exists);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getgeofence","title":"getGeofence","text":"<p><code>get</code><code>Geofence</code><code>(identifier: string): Promise&lt;</code><code>Geofence</code><code>&gt;;</code></p> <p>Fetch a single Geofence by identifier from the SDK's database.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofence = await BackgroundGeolocation.getGeofence(\"HOME\");\nconsole.log(\"[getGeofence] \", geofence);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getgeofences","title":"getGeofences","text":"<p><code>getGeofences(): Promise&lt;</code><code>Geofence</code><code>[]&gt;;</code></p> <p>Fetch all Geofence records from the SDK's database.</p> <p>Returns an empty array if no geofences are stored.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]: \", geofences);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#removegeofence","title":"removeGeofence","text":"<p><code>remove</code><code>Geofence</code><code>(identifier: string): Promise&lt;boolean&gt;;</code></p> <p>Remove the Geofence with the given Geofence.identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofence(\"Home\").then((success) =&gt; {\n  console.log(\"[removeGeofence] success\");\n}).catch((error) =&gt; {\n  console.log(\"[removeGeofence] FAILURE: \", error);\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#removegeofences","title":"removeGeofences","text":"<pre><code>removeGeofences(identifiers?: string[]): Promise&lt;boolean&gt;;\n</code></pre> <p>Remove all monitored Geofence records, or a specific subset by identifier.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code></p> <pre><code>BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#data-management","title":"Data Management","text":""},{"location":"typescript/BackgroundGeolocation/#destroylocation","title":"destroyLocation","text":"<p><code>destroy</code><code>Location</code><code>(uuid: string): Promise&lt;void&gt;;</code></p> <p>Remove a single location by Location.uuid.</p> <pre><code>await BackgroundGeolocation.destroyLocation(location.uuid);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#destroylocations","title":"destroyLocations","text":"<pre><code>destroyLocations(): Promise&lt;void&gt;;\n</code></pre> <p>Remove all records from the SDK's SQLite database.</p> <pre><code>await BackgroundGeolocation.destroyLocations();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getcount","title":"getCount","text":"<pre><code>getCount(): Promise&lt;number&gt;;\n</code></pre> <p>Retrieve the count of all locations currently stored in the SDK's SQLite database.</p> <pre><code>const count = await BackgroundGeolocation.getCount();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#getlocations","title":"getLocations","text":"<pre><code>getLocations(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Retrieve all Location records stored in the SDK's SQLite database.</p> <pre><code>const locations = await BackgroundGeolocation.getLocations();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#sync","title":"sync","text":"<pre><code>sync(): Promise&lt;Object[]&gt;;\n</code></pre> <p>Manually upload all queued locations to HttpConfig.url.</p> <p>Initiates a POST of all records in the SQLite database to your configured HttpConfig.url. Records that receive a <code>200 OK</code> response are deleted from the database. If HttpConfig.batchSync is <code>true</code>, all locations are sent in a single request; otherwise one request is made per location. If no HTTP service is configured, all records are deleted from the database.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>const records = await BackgroundGeolocation.sync();\nconsole.log(\"[sync] success: \", records);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#authorization","title":"Authorization","text":""},{"location":"typescript/BackgroundGeolocation/#getproviderstate","title":"getProviderState","text":"<p><code>getProviderState(): Promise&lt;</code><code>ProviderChangeEvent</code><code>&gt;;</code></p> <p>Retrieve the current location-services authorization state.</p> <p>See also - <code>onProviderChange</code> to subscribe to future authorization changes.</p> <pre><code>const providerState = await BackgroundGeolocation.getProviderState();\nconsole.log(\"- Provider state: \", providerState);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#requestpermission","title":"requestPermission","text":"<p><code>requestPermission(): Promise&lt;</code><code>AuthorizationStatus</code><code>&gt;;</code></p> <p>Manually request location permission using the configured GeoConfig.locationAuthorizationRequest.</p> <p>Resolves successfully if either <code>WhenInUse</code> or <code>Always</code> is granted, regardless of the requested level. Rejects if the user denies.</p> <p>If permission is already granted, resolves immediately. If iOS has already shown the authorization dialog and the current grant does not match the configured request, the SDK presents an alert offering to direct the user to your app's Settings page.</p> <p>Note</p> <p>The SDK automatically requests permission when you call <code>start</code>, <code>startGeofences</code>, or <code>getCurrentPosition</code>. You do not need to call this method in typical use.</p> <p>See also - GeoConfig.locationAuthorizationRequest - GeoConfig.disableLocationAuthorizationAlert - GeoConfig.locationAuthorizationAlert - AppConfig.backgroundPermissionRationale (Android) - <code>requestTemporaryFullAccuracy</code> (iOS 14+)</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log('[providerchange]', event);\n});\n\nconst state = await BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: 'Always' }\n});\n\ntry {\n  const status = await BackgroundGeolocation.requestPermission();\n  console.log('[requestPermission] success: ', status);\n} catch (status) {\n  console.warn('[requestPermission] FAILURE: ', status);\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#requesttemporaryfullaccuracy","title":"requestTemporaryFullAccuracy","text":"<p><code>requestTemporaryFullAccuracy(purposeKey: string): Promise&lt;</code><code>AccuracyAuthorization</code><code>&gt;;</code></p> <p>Request temporary full-accuracy location authorization. [iOS 14+]</p> <p>iOS 14 allows users to grant only reduced location accuracy. This method presents the system dialog (<code>requestTemporaryFullAccuracyAuthorization</code>) requesting full accuracy for the lifetime of the current app session.</p> <p></p>"},{"location":"typescript/BackgroundGeolocation/#configuration-infoplist","title":"Configuration \u2014 Info.plist","text":"<p>Add the <code>Privacy - Location Temporary Usage Description Dictionary</code> key to your <code>Info.plist</code>:</p> <p></p> <p>The dictionary keys (e.g. <code>Delivery</code>) are passed as <code>purposeKey</code>. The corresponding value is the message shown to the user explaining the purpose of your request.</p> <p>The dialog fails to present if: - The <code>Info.plist</code> entry for <code>purposeKey</code> is missing. - The app is already authorized for full accuracy. - The app is in the background.</p> <p>Note</p> <p>On Android and iOS versions below 14, this method returns AccuracyAuthorization.Full immediately without presenting a dialog.</p> <p>See also - ProviderChangeEvent.accuracyAuthorization</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Supply \"Purpose\" key from Info.plist as 1st argument.\n    BackgroundGeolocation.requestTemporaryFullAccuracy(\"Delivery\").then((accuracyAuthorization) =&gt; {\n      if (accuracyAuthorization == BackgroundGeolocation.AccuracyAuthorization.Full) {\n        console.log('[requestTemporaryFullAccuracy] GRANTED: ', accuracyAuthorization);\n      } else {\n        console.log('[requestTemporaryFullAccuracy] DENIED: ', accuracyAuthorization);\n      }\n    }).catch((error) =&gt; {\n      console.warn(\"[requestTemporaryFullAccuracy] FAILED TO SHOW DIALOG: \", error);\n    });\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#background-tasks","title":"Background Tasks","text":""},{"location":"typescript/BackgroundGeolocation/#startbackgroundtask","title":"startBackgroundTask","text":"<pre><code>startBackgroundTask(): Promise&lt;number&gt;;\n</code></pre> <p>Signal to the OS that you need to perform a long-running task.</p> <p>The OS keeps the app running in the background until you signal completion with <code>stopBackgroundTask</code>. Your callback receives a <code>taskId</code> which you must pass to <code>stopBackgroundTask</code> when finished \u2014 always call it, even if an error occurs, to avoid hanging the background task.</p>"},{"location":"typescript/BackgroundGeolocation/#ios_2","title":"iOS","text":"<p>Uses <code>beginBackgroundTaskWithExpirationHandler</code>. iOS provides exactly 180 seconds of background time. The SDK automatically stops the task before the OS force-kills the app.</p> <pre><code>\u2705-[BackgroundTaskManager createBackgroundTask] 1\n\u2705-[BackgroundTaskManager stopBackgroundTask:]_block_invoke 1 OF (1)\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#android_2","title":"Android","text":"<p>Uses <code>WorkManager</code>. The SDK imposes a 3-minute limit before automatically force-killing the task.</p> <pre><code> I TSLocationManager: [c.t.l.u.BackgroundTaskManager onStartJob] \u23f3 startBackgroundTask: 6\n I TSLocationManager: [c.t.l.u.BackgroundTaskManager$Task stop] \u23f3 stopBackgroundTask: 6\n</code></pre> <pre><code>onLocation(location) {\n  console.log(\"[location] \", location);\n\n  // Perform some long-running task (eg: HTTP request)\n  BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n    performLongRunningTask().then(() =&gt; {\n      // When your long-running task is complete, signal completion of taskId.\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    }).catch((error) =&gt; {\n      // Be sure to catch errors: never leave your background-task hanging.\n      console.error(error);\n      BackgroundGeolocation.stopBackgroundTask(taskId);\n    });\n  });\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#stopbackgroundtask","title":"stopBackgroundTask","text":"<pre><code>stopBackgroundTask(taskId: number): Promise&lt;void&gt;;\n</code></pre> <p>Signal completion of a <code>startBackgroundTask</code> to the OS.</p> <p>The OS may now suspend the app if appropriate.</p> <pre><code>BackgroundGeolocation.startBackgroundTask().then((taskId) =&gt; {\n  // Perform some long-running task (eg: HTTP request)\n  performLongRunningTask().then(() =&gt; {\n    // When your long-running task is complete, signal completion of taskId.\n    BackgroundGeolocation.stopBackgroundTask(taskId);\n  });\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#logging","title":"Logging","text":""},{"location":"typescript/BackgroundGeolocation/#playsound","title":"playSound","text":"<pre><code>playSound(soundId: string | number): void;\n</code></pre> <p>Play a system sound.</p>"},{"location":"typescript/BackgroundGeolocation/#ios_3","title":"iOS","text":"<p>Provide a numeric <code>SystemSoundID</code>.</p>"},{"location":"typescript/BackgroundGeolocation/#android_3","title":"Android","text":"<p>Provide a sound name string.</p>"},{"location":"typescript/BackgroundGeolocation/#setloglevel","title":"setLogLevel","text":"<p><code>set</code><code>LogLevel</code><code>(level:</code><code>LogLevel</code><code>): Promise&lt;void&gt;;</code></p> <p>Sets the LoggerConfig.logLevel.</p>"},{"location":"typescript/BackgroundGeolocation/#device","title":"Device","text":""},{"location":"typescript/BackgroundGeolocation/#getsensors","title":"getSensors","text":"<p><code>get</code><code>Sensors</code><code>(): Promise&lt;</code><code>Sensors</code><code>&gt;;</code></p> <p>Returns the availability of motion sensors: accelerometer, gyroscope, and magnetometer.</p> <p>These sensors power the motion activity-recognition system. When any sensor is absent (particularly on low-end Android devices), motion recognition performance degrades significantly.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(sensors);\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#ispowersavemode","title":"isPowerSaveMode","text":"<pre><code>isPowerSaveMode(): Promise&lt;boolean&gt;;\n</code></pre> <p>Returns the current state of the operating system's power-saving mode.</p> <p>Power-saving mode can throttle background services such as GPS and HTTP uploads.</p> <p>See also - <code>onPowerSaveChange</code> to subscribe to future changes.</p>"},{"location":"typescript/BackgroundGeolocation/#ios_4","title":"iOS","text":"<p>Power Saving mode is enabled manually in Settings \u2192 Battery or via an automatic OS prompt.</p> <p></p>"},{"location":"typescript/BackgroundGeolocation/#android_4","title":"Android","text":"<p>Battery Saver is enabled manually in Settings \u2192 Battery \u2192 Battery Saver or automatically when the battery drops below a configured threshold.</p> <p></p> <pre><code>const isPowerSaveMode = await BackgroundGeolocation.isPowerSaveMode();\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#properties","title":"Properties","text":""},{"location":"typescript/BackgroundGeolocation/#accuracyauthorization","title":"AccuracyAuthorization","text":"<p><code>AccuracyAuthorization</code><code>: object;</code></p> <p>Enum namespace indicating whether the user granted full or reduced location accuracy. [iOS 14+]</p> <p>Used by <code>ProviderChangeEvent.accuracyAuthorization</code> and <code>requestTemporaryFullAccuracy</code>.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  if (event.accuracyAuthorization ===\n      BackgroundGeolocation.AccuracyAuthorization.Reduced) {\n    // Handle reduced-accuracy case\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#activitytype","title":"ActivityType","text":"<p><code>ActivityType</code><code>: object;</code></p> <p>iOS only Enum namespace specifying the type of user activity (AutomotiveNavigation, Fitness, OtherNavigation, etc).</p> <p>Used by GeoConfig.activityType.</p>"},{"location":"typescript/BackgroundGeolocation/#authorizationstatus","title":"AuthorizationStatus","text":"<p><code>AuthorizationStatus</code><code>: object;</code></p> <p>Enum namespace representing the OS-level authorization state for location services (Denied, Restricted, Always, WhenInUse).</p> <p>Returned from <code>requestPermission()</code> and <code>onProviderChange</code>.</p> <pre><code>const status = await BackgroundGeolocation.requestPermission();\nif (status === BackgroundGeolocation.AuthorizationStatus.Always) {\n  // Good to start tracking\n}\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#authorizationstrategy","title":"AuthorizationStrategy","text":"<p><code>AuthorizationStrategy</code><code>: object;</code></p> <p>Enum namespace defining how the HTTP service performs authorization.</p> <p>Includes basic, JWT, and custom strategies. Used by <code>AuthorizationConfig.strategy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  authorization: {\n    strategy: BackgroundGeolocation.AuthorizationStrategy.Jwt\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#desiredaccuracy","title":"DesiredAccuracy","text":"<p><code>DesiredAccuracy</code><code>: object;</code></p> <p>Enum namespace controlling the native location engine's target accuracy.</p> <p>Higher accuracy consumes more battery. Used by <code>GeoConfig.desiredAccuracy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#event","title":"Event","text":"<p><code>Event</code><code>: object;</code></p> <p>Enum namespace of all event names emitted by the SDK (<code>location</code>, <code>geofence</code>, <code>motionchange</code>, <code>heartbeat</code>, etc).</p>"},{"location":"typescript/BackgroundGeolocation/#httpmethod","title":"HttpMethod","text":"<p><code>HttpMethod</code><code>: object;</code></p> <p>Enum namespace defining the HTTP method used for location uploads (POST, PUT, etc).</p> <p>Used by <code>HttpConfig.method</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    method: BackgroundGeolocation.HttpMethod.Post\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#kalmanprofile","title":"KalmanProfile","text":"<p><code>KalmanProfile</code><code>: object;</code></p> <p>Enum namespace selecting a preset Kalman filter tuning profile (aggressive, moderate, or relaxed smoothing).</p> <p>Used by <code>LocationFilter.kalmanProfile</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      kalmanProfile: BackgroundGeolocation.KalmanProfile.Aggressive\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#locationfilterpolicy","title":"LocationFilterPolicy","text":"<p><code>LocationFilterPolicy</code><code>: object;</code></p> <p>Enum namespace selecting the filtering engine policy for noise-reduction and smoothing.</p> <p>Used by <code>LocationFilter.policy</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: BackgroundGeolocation.LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#locationrequest","title":"LocationRequest","text":"<p><code>LocationRequest</code><code>: object;</code></p> <p>iOS only Enum namespace defining the type of location permission request (Always, WhenInUse, or Any). Used by <code>GeoConfig.locationAuthorizationRequest</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: BackgroundGeolocation.LocationRequest.Always\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#loglevel","title":"LogLevel","text":"<p><code>LogLevel</code><code>: object;</code></p> <p>Enum namespace controlling verbosity of the SDK logger.</p> <p>Used by <code>LoggerConfig.logLevel</code>. Values range from silent (<code>Off</code>) to extremely verbose (<code>Verbose</code>).</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#notificationpriority","title":"NotificationPriority","text":"<p><code>NotificationPriority</code><code>: object;</code></p> <p>Android only Enum namespace controlling Android foreground-service notification priority and icon placement (top, bottom, hidden).</p> <p>Used by <code>NotificationConfig.priority</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: BackgroundGeolocation.NotificationPriority.High\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#persistmode","title":"PersistMode","text":"<p><code>PersistMode</code><code>: object;</code></p> <p>Enum namespace controlling which records the SDK persists to SQLite: locations only, geofences only, both, or none.</p> <p>Used by <code>PersistenceConfig.persistMode</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    persistMode: BackgroundGeolocation.PersistMode.All\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#triggeractivity","title":"TriggerActivity","text":"<p><code>TriggerActivity</code><code>: object;</code></p> <p>Enum namespace defining which physical motion activities can trigger motion-detection transitions (still \u2192 moving).</p> <p>Used by <code>ActivityConfig.triggerActivities</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  activity: {\n    triggerActivities: [\n      BackgroundGeolocation.TriggerActivity.InVehicle\n    ]\n  }\n});\n</code></pre>"},{"location":"typescript/BackgroundGeolocation/#devicesettings","title":"deviceSettings","text":"<p><code>deviceSettings:</code><code>DeviceSettings</code><code>;</code></p> <p>DeviceSettings API</p>"},{"location":"typescript/BackgroundGeolocation/#logger","title":"logger","text":"<p><code>logger:</code><code>Logger</code><code>;</code></p> <p>Logger API</p>"},{"location":"typescript/Battery/","title":"Battery","text":"<pre><code>export interface Battery {\n</code></pre> <p>Device battery state at the time a Location was recorded.</p>"},{"location":"typescript/Battery/#members","title":"Members","text":""},{"location":"typescript/Battery/#is_charging","title":"is_charging","text":"<pre><code>is_charging: boolean;\n</code></pre> <p><code>true</code> when the device is connected to a power source.</p>"},{"location":"typescript/Battery/#level","title":"level","text":"<pre><code>level: number;\n</code></pre> <p>Battery charge level: <code>0.0</code> = empty, <code>1.0</code> = fully charged.</p>"},{"location":"typescript/Config/","title":"Config","text":"<pre><code>export interface Config {\n</code></pre> <p>Root configuration object passed to BackgroundGeolocation.ready and BackgroundGeolocation.setConfig.</p> <p><code>Config</code> groups all SDK options into typed sub-interfaces. Each key maps to a dedicated configuration area \u2014 set only the keys relevant to your use case.</p>"},{"location":"typescript/Config/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"typescript/Config/#overview","title":"Overview","text":"Key Type Description <code>geolocation</code> GeoConfig Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. <code>activity</code> ActivityConfig Motion recognition, stop-detection triggers, and motion-trigger delay. <code>http</code> HttpConfig Upload URL, sync cadence, batching, headers, and params. <code>persistence</code> PersistenceConfig SQLite storage, TTL, record limits, and custom extras. <code>app</code> AppConfig App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. <code>logger</code> LoggerConfig Debug logging, log level, and log retention. <code>authorization</code> AuthorizationConfig JWT and SAS token management with automatic refresh. <p>The SDK persists its configuration across app launches. Call BackgroundGeolocation.ready once at startup \u2014 subsequent launches load the persisted config automatically. Use BackgroundGeolocation.setConfig to update individual keys at runtime without restarting.</p>"},{"location":"typescript/Config/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre> <pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from 'react-native-background-geolocation';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"typescript/Config/#minimal-configuration","title":"Minimal configuration","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    autoSync: true,\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n  },\n});\n</code></pre>"},{"location":"typescript/Config/#full-configuration","title":"Full configuration","text":"<pre><code>import BackgroundGeolocation, {\n  Config,\n  GeoConfig,\n  ActivityConfig,\n  HttpConfig,\n  PersistenceConfig,\n  DesiredAccuracy,\n  PersistMode,\n  LogLevel,\n  AppConfig,\n  LoggerConfig,\n  State\n} from '{{pluginName}}';\n\nconst config: Config = {\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 20,\n    stopTimeout: 5,\n    stationaryRadius: 150,\n  },\n  activity: {\n    disableStopDetection: false,\n    motionTriggerDelay: 30000,\n  },\n  http: {\n    url: 'https://my.server.com/api/locations',\n    method: 'POST',\n    autoSync: true,\n    headers: { Authorization: 'Bearer secret-token' },\n    params: { user_id: 123 },\n  },\n  persistence: {\n    persistMode: PersistMode.All,\n    maxDaysToPersist: 14,\n    extras: { appVersion: '1.0.0' },\n  },\n  app: {\n    stopOnTerminate: false,\n    startOnBoot: true,\n    enableHeadless: true,\n  },\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  },\n};\n\nconst state: State = await BackgroundGeolocation.ready(config);\nif (!state.enabled) {\n  await BackgroundGeolocation.start();\n}\n\n// Update a subset of config at runtime.\nawait BackgroundGeolocation.setConfig({\n  http: { headers: { Authorization: 'Bearer new-token' } },\n  logger: { logLevel: LogLevel.Info },\n});\n</code></pre>"},{"location":"typescript/Config/#members","title":"Members","text":""},{"location":"typescript/Config/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. Access via <code>BGGeo.instance.config.activity</code>. See ActivityConfig.</p>"},{"location":"typescript/Config/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle, background operation, boot behaviour, headless mode, and foreground notification. Access via <code>BGGeo.instance.app</code>. See AppConfig.</p>"},{"location":"typescript/Config/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. Access via <code>BGGeo.instance.authorization</code>. See AuthorizationConfig.</p>"},{"location":"typescript/Config/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. Access via <code>BGGeo.instance.config.geolocation</code>. See GeoConfig.</p>"},{"location":"typescript/Config/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. Access via <code>BGGeo.instance.config.http</code>. See HttpConfig.</p>"},{"location":"typescript/Config/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. Access via <code>BGGeo.instance.config.logger</code>. See LoggerConfig.</p>"},{"location":"typescript/Config/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. Access via <code>BGGeo.instance.config.persistence</code>. See PersistenceConfig.</p>"},{"location":"typescript/Config/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"typescript/Config/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre> <pre><code>// Base url to Transistor Demo Server.\nconst url = \"http://tracker.transistorsoft.com\";\n\n// Register for an authorization token from server.\nconst token = await\n  BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\"my-company-name\", \"my-username\");\n\nBackgroundGeolocation.ready({\n  url: url + \"/api/locations\",\n  authorization: {\n    strategy: \"JWT\",\n    accessToken: token.accessToken,\n    refreshToken: token.refreshToken,\n    refreshUrl: url + \"/v2/refresh_token\",\n    refreshPayload: {\n      refresh_token: \"{refreshToken}\"\n    },\n    expires: token.expires\n  }\n});\n</code></pre>"},{"location":"typescript/ConnectivityChangeEvent/","title":"ConnectivityChangeEvent","text":"<pre><code>export interface ConnectivityChangeEvent {\n</code></pre> <p>Network connectivity change delivered to BackgroundGeolocation.onConnectivityChange.</p> <p>The SDK fires this event whenever the device transitions between an online and offline state. When connectivity is restored, the SDK automatically resumes uploading any locations queued in the SQLite database.</p> <pre><code>BackgroundGeolocation.onConnectivityChange((event) =&gt; {\n  console.log(\"[onConnectivityChange] connected:\", event.connected);\n});\n</code></pre>"},{"location":"typescript/ConnectivityChangeEvent/#members","title":"Members","text":""},{"location":"typescript/ConnectivityChangeEvent/#connected","title":"connected","text":"<pre><code>connected: boolean;\n</code></pre> <p><code>true</code> when the device has network connectivity; <code>false</code> when it is offline.</p>"},{"location":"typescript/Coords/","title":"Coords","text":"<pre><code>export interface Coords {\n</code></pre> <p>Geographic coordinates attached to a Location.</p>"},{"location":"typescript/Coords/#members","title":"Members","text":""},{"location":"typescript/Coords/#accuracy","title":"accuracy","text":"<pre><code>accuracy: number;\n</code></pre> <p>Horizontal accuracy radius in meters.</p>"},{"location":"typescript/Coords/#altitude","title":"altitude","text":"<pre><code>altitude?: number;\n</code></pre> <p>Altitude above a reference plane in meters.</p>"},{"location":"typescript/Coords/#ios","title":"iOS","text":"<p>Altitude above mean sea level.</p>"},{"location":"typescript/Coords/#android","title":"Android","text":"<p>Altitude above the WGS84 reference ellipsoid. See also <code>ellipsoidal_altitude</code>.</p>"},{"location":"typescript/Coords/#altitude_accuracy","title":"altitude_accuracy","text":"<pre><code>altitude_accuracy?: number;\n</code></pre> <p>Vertical accuracy in meters. Returns <code>-1</code> if unavailable.</p>"},{"location":"typescript/Coords/#ios_1","title":"iOS","text":"<p>When positive, the <code>altitude</code> value is within \u00b1<code>altitude_accuracy</code> meters. When negative, <code>altitude</code> is invalid. Determining altitude accuracy requires a GPS-capable device; on some devices this value is always negative.</p>"},{"location":"typescript/Coords/#android_1","title":"Android","text":"<p>Defined as the 1-sigma vertical accuracy at 68% confidence \u2014 the half-width of the range above and below <code>altitude</code> within which the true altitude has a 68% probability of falling.</p>"},{"location":"typescript/Coords/#ellipsoidal_altitude","title":"ellipsoidal_altitude","text":"<pre><code>ellipsoidal_altitude?: number;\n</code></pre> <p>Altitude above the WGS84 reference ellipsoid in meters.</p>"},{"location":"typescript/Coords/#floor","title":"floor","text":"<pre><code>floor?: number;\n</code></pre> <p>iOS only Floor within a building, when indoor-positioning hardware (e.g. Bluetooth beacons) is available.</p>"},{"location":"typescript/Coords/#heading","title":"heading","text":"<pre><code>heading?: number;\n</code></pre> <p>Direction of travel in degrees (0\u2013360, clockwise from true north).</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"typescript/Coords/#heading_accuracy","title":"heading_accuracy","text":"<pre><code>heading_accuracy?: number;\n</code></pre> <p>Heading accuracy in degrees.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"typescript/Coords/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude in decimal degrees.</p>"},{"location":"typescript/Coords/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude in decimal degrees.</p>"},{"location":"typescript/Coords/#speed","title":"speed","text":"<pre><code>speed?: number;\n</code></pre> <p>Ground speed in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"typescript/Coords/#speed_accuracy","title":"speed_accuracy","text":"<pre><code>speed_accuracy?: number;\n</code></pre> <p>Speed accuracy in meters per second.</p> <p>Note</p> <p>Only present when the location came from GPS. Returns <code>-1</code> otherwise.</p>"},{"location":"typescript/CurrentPositionRequest/","title":"CurrentPositionRequest","text":"<pre><code>export interface CurrentPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.getCurrentPosition.</p> <p>All fields are optional. The SDK always requests location from the native API at maximum accuracy (DesiredAccuracy.High), regardless of the <code>desiredAccuracy</code> threshold set here.</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  timeout: 30,          // seconds before giving up\n  maximumAge: 5000,     // accept a cached fix up to 5000 ms old\n  desiredAccuracy: 10,  // stop sampling when accuracy \u2264 10 m\n  samples: 3,           // take up to 3 samples and pick the best\n  extras: {\n    route_id: 123\n  }\n});\n</code></pre>"},{"location":"typescript/CurrentPositionRequest/#members","title":"Members","text":""},{"location":"typescript/CurrentPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<pre><code>desiredAccuracy?: number;\n</code></pre> <p>Accuracy threshold in meters. The SDK stops sampling as soon as it receives a location with <code>accuracy \u2264 desiredAccuracy</code> and returns it immediately. Defaults to GeoConfig.stationaryRadius.</p> <p>Note</p> <p>This value is a stopping threshold, not a hardware accuracy setting. The SDK always requests locations at DesiredAccuracy.High from the native API regardless of this value.</p>"},{"location":"typescript/CurrentPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to the returned location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"typescript/CurrentPositionRequest/#maximumage","title":"maximumAge","text":"<pre><code>maximumAge?: number;\n</code></pre> <p>Accept the most recently recorded location if it is no older than this value in milliseconds. Default <code>0</code> (always fetch a fresh fix).</p>"},{"location":"typescript/CurrentPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist the returned location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"typescript/CurrentPositionRequest/#samples","title":"samples","text":"<pre><code>samples?: number;\n</code></pre> <p>Maximum number of location samples to collect before returning the most accurate result. Default <code>3</code>. Only the final selected location is persisted.</p>"},{"location":"typescript/CurrentPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in seconds to wait for a location fix. Default <code>30</code>.</p> <p>If the timeout expires before a satisfactory location is found, the Promise rejects with a LocationError.</p>"},{"location":"typescript/DesiredAccuracy/","title":"DesiredAccuracy","text":"<pre><code>export const DesiredAccuracy = {\n</code></pre> <p>Desired accuracy presets (meters / platform-tuned).</p>"},{"location":"typescript/DesiredAccuracy/#members","title":"Members","text":""},{"location":"typescript/DesiredAccuracy/#high","title":"High","text":"<p>High accuracy (e.g., 0-10m).</p>"},{"location":"typescript/DesiredAccuracy/#low","title":"Low","text":"<p>Low accuracy (e.g., 100m).</p>"},{"location":"typescript/DesiredAccuracy/#lowest","title":"Lowest","text":"<p>Lowest accuracy (e.g., 3000m).</p>"},{"location":"typescript/DesiredAccuracy/#medium","title":"Medium","text":"<p>Medium accuracy (e.g., 10m).</p>"},{"location":"typescript/DesiredAccuracy/#navigation","title":"Navigation","text":"<p>Navigation-level accuracy (e.g., 0-10m).</p>"},{"location":"typescript/DesiredAccuracy/#verylow","title":"VeryLow","text":"<p>Very low accuracy (e.g., 1000m).</p>"},{"location":"typescript/DeviceInfo/","title":"DeviceInfo","text":"<pre><code>export interface DeviceInfo {\n</code></pre> <p>Basic device hardware and OS information returned by BackgroundGeolocation.getDeviceInfo.</p> <pre><code>const info = await BackgroundGeolocation.getDeviceInfo();\nconsole.log(`[DeviceInfo] ${info.manufacturer} ${info.model} (${info.platform})`);\n</code></pre>"},{"location":"typescript/DeviceInfo/#members","title":"Members","text":""},{"location":"typescript/DeviceInfo/#framework","title":"framework","text":"<pre><code>framework: string;\n</code></pre> <p>Development framework hosting the SDK.</p> <p>Examples: <code>\"react-native\"</code>, <code>\"capacitor\"</code>, <code>\"cordova\"</code>, <code>\"flutter\"</code></p>"},{"location":"typescript/DeviceInfo/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer.</p> <p>Examples: <code>\"Apple\"</code>, <code>\"Google\"</code>, <code>\"Samsung\"</code>, <code>\"Huawei\"</code></p>"},{"location":"typescript/DeviceInfo/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model identifier.</p> <p>Examples: <code>\"iPhone15,2\"</code>, <code>\"Pixel 8 Pro\"</code>, <code>\"SM-G991B\"</code></p>"},{"location":"typescript/DeviceInfo/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"iOS\"</code> or <code>\"Android\"</code>.</p>"},{"location":"typescript/DeviceInfo/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>OS version string.</p> <p>Examples: <code>\"18.1\"</code>, <code>\"14\"</code>, <code>\"14.1.1\"</code></p>"},{"location":"typescript/DeviceSettings/","title":"DeviceSettings Android only","text":"<pre><code>export interface DeviceSettings {\n</code></pre> <p>API for directing users to Android device settings screens that can affect background geolocation performance.</p> <p>Many Android manufacturers apply aggressive battery optimizations that can kill background services, including the geolocation SDK. This API surfaces the relevant settings screens so users can whitelist your app.</p> <p>For a comprehensive list of affected devices and manufacturers, see dontkillmyapp.com.</p> <pre><code>const isIgnoring = await BackgroundGeolocation.deviceSettings.isIgnoringBatteryOptimizations();\nif (!isIgnoring) {\n  const req = await BackgroundGeolocation.deviceSettings.showIgnoreBatteryOptimizations();\n  if (!req.seen) {\n    const confirmed = await showMyConfirmDialog({\n      title: 'Settings request',\n      text: 'Please disable battery optimizations for this app'\n    });\n    if (confirmed) {\n      await BackgroundGeolocation.deviceSettings.show(req);\n    }\n  }\n}\n</code></pre>"},{"location":"typescript/DeviceSettings/#members","title":"Members","text":""},{"location":"typescript/DeviceSettings/#isignoringbatteryoptimizations","title":"isIgnoringBatteryOptimizations","text":"<pre><code>isIgnoringBatteryOptimizations(): Promise&lt;boolean&gt;;\n</code></pre> <p>Android only Returns <code>true</code> if the OS is ignoring battery optimizations for your app.</p> <p>In most cases the SDK performs acceptably even when battery optimizations are active, but aggressive manufacturer-specific restrictions may interfere with background operation.</p>"},{"location":"typescript/DeviceSettings/#show","title":"show","text":"<p><code>show(request:</code><code>DeviceSettingsRequest</code><code>): Promise&lt;boolean&gt;;</code></p> <p>Android only Execute a previously prepared DeviceSettingsRequest to open the target settings screen.</p> <p>Resolves <code>true</code> if the redirect was attempted.</p> <pre><code>const req = await BackgroundGeolocation.deviceSettings.showPowerManager();\nif (!req.seen) {\n  await BackgroundGeolocation.deviceSettings.show(req);\n}\n</code></pre>"},{"location":"typescript/DeviceSettings/#showignorebatteryoptimizations","title":"showIgnoreBatteryOptimizations","text":"<p><code>showIgnoreBatteryOptimizations(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show the Android Ignore Battery Optimizations settings screen.</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting, so you can inspect DeviceSettingsRequest.seen and decide whether to prompt the user.</p> <p>Warning</p> <p>On some devices and OS versions this screen may not be available. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>isIgnoringBatteryOptimizations</code> - <code>show</code></p>"},{"location":"typescript/DeviceSettings/#showpowermanager","title":"showPowerManager","text":"<p><code>showPowerManager(): Promise&lt;</code><code>DeviceSettingsRequest</code><code>&gt;;</code></p> <p>Android only Prepare a request to show a vendor-specific Power Manager settings screen (Huawei, Xiaomi, Vivo, Oppo, etc.).</p> <p>Returns a DeviceSettingsRequest rather than immediately redirecting.</p> <p>Warning</p> <p>Not all manufacturers or OS versions implement this screen. Always wrap calls to DeviceSettings.show in a <code>try/catch</code>.</p> <p>See also - <code>show</code></p>"},{"location":"typescript/DeviceSettingsRequest/","title":"DeviceSettingsRequest Android only","text":"<pre><code>export interface DeviceSettingsRequest {\n</code></pre> <p>Returned by a DeviceSettings request \u2014 describes which settings screen to show and whether the user has already been prompted.</p> <p>Contains device metadata (<code>manufacturer</code>, <code>model</code>, <code>version</code>) and a <code>seen</code> flag so you can avoid showing the same screen repeatedly. Pass this object to DeviceSettings.show to redirect the user.</p>"},{"location":"typescript/DeviceSettingsRequest/#members","title":"Members","text":""},{"location":"typescript/DeviceSettingsRequest/#action","title":"action","text":"<pre><code>action: string;\n</code></pre> <p>The Android Intent action used to open the target settings screen.</p> <p>Warning</p> <p>This field is set automatically by the native layer. Do not set it manually.</p>"},{"location":"typescript/DeviceSettingsRequest/#lastseenat","title":"lastSeenAt","text":"<pre><code>lastSeenAt: Date;\n</code></pre> <p>Timestamp of the last time this screen was shown to the user.</p>"},{"location":"typescript/DeviceSettingsRequest/#manufacturer","title":"manufacturer","text":"<pre><code>manufacturer: string;\n</code></pre> <p>Device manufacturer (e.g. <code>\"Huawei\"</code>, <code>\"Samsung\"</code>).</p>"},{"location":"typescript/DeviceSettingsRequest/#model","title":"model","text":"<pre><code>model: string;\n</code></pre> <p>Device model name (e.g. <code>\"P40\"</code>, <code>\"SM-G991B\"</code>).</p>"},{"location":"typescript/DeviceSettingsRequest/#seen","title":"seen","text":"<pre><code>seen: boolean;\n</code></pre> <p><code>true</code> if this settings screen has already been shown to the user on this device. Use this to avoid showing the same prompt repeatedly.</p>"},{"location":"typescript/DeviceSettingsRequest/#version","title":"version","text":"<pre><code>version: string;\n</code></pre> <p>Android OS version string.</p>"},{"location":"typescript/Event/","title":"Event","text":"<pre><code>export const Event = {\n</code></pre> <p>Supported event names emitted by the SDK.</p>"},{"location":"typescript/Event/#members","title":"Members","text":""},{"location":"typescript/Event/#activitychange","title":"ActivityChange","text":"<p>Emitted when a motion-activity change is detected.</p>"},{"location":"typescript/Event/#authorization","title":"Authorization","text":"<p>Emitted when the plugin's authorization state changes.</p>"},{"location":"typescript/Event/#boot","title":"Boot","text":"<p>Emitted when the plugin boots.</p>"},{"location":"typescript/Event/#connectivitychange","title":"ConnectivityChange","text":"<p>Emitted when the device's network connectivity changes.</p>"},{"location":"typescript/Event/#enabledchange","title":"EnabledChange","text":"<p>Emitted when the plugin's enabled-state changes.</p>"},{"location":"typescript/Event/#geofence","title":"Geofence","text":"<p>Emitted when a geofence transition occurs.</p>"},{"location":"typescript/Event/#geofenceschange","title":"GeofencesChange","text":"<p>Emitted when geofences are added/removed/activated/deactivated.</p>"},{"location":"typescript/Event/#heartbeat","title":"Heartbeat","text":"<p>Emitted at the configured <code>Config.heartbeatInterval</code> when the plugin is running.</p>"},{"location":"typescript/Event/#http","title":"Http","text":"<p>Emitted when an HTTP request is sent to the configured <code>Config.http.url</code> endpoint.</p>"},{"location":"typescript/Event/#location","title":"Location","text":"<p>Emitted when a location is recorded.</p>"},{"location":"typescript/Event/#motionchange","title":"MotionChange","text":"<p>Emitted when a motion-change is detected.</p>"},{"location":"typescript/Event/#notification","title":"Notification","text":"<p>Emitted when a user interacts with a plugin-generated notification.</p>"},{"location":"typescript/Event/#powersavechange","title":"PowerSaveChange","text":"<p>Emitted when the device enters or exits power-saving mode.</p>"},{"location":"typescript/Event/#providerchange","title":"ProviderChange","text":"<p>Emitted when the device location-provider changes state.</p>"},{"location":"typescript/Event/#schedule","title":"Schedule","text":"<p>Emitted when a configured schedule is triggered.</p>"},{"location":"typescript/Event/#terminate","title":"Terminate","text":"<p>Emitted when the plugin is terminated.</p>"},{"location":"typescript/GeoConfig/","title":"GeoConfig","text":"<pre><code>export interface GeoConfig {\n</code></pre> <p>Geolocation configuration for the background geolocation SDK.</p> <p><code>GeoConfig</code> controls how the SDK acquires, filters, and records device locations \u2014 the accuracy tier, sampling rate, speed-based elasticity, stop-detection, platform permissions, geofence proximity, and GPS noise filtering.</p>"},{"location":"typescript/GeoConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Accuracy and sampling</li> <li>Elasticity</li> <li>Motion detection</li> <li>Permissions</li> <li>Geofencing</li> <li>Filtering</li> <li>Examples</li> </ul>"},{"location":"typescript/GeoConfig/#overview","title":"Overview","text":"<p><code>GeoConfig</code> is supplied via Config.geolocation when calling BackgroundGeolocation.ready or BackgroundGeolocation.setConfig.</p> Category Properties Accuracy <code>desiredAccuracy</code>, <code>distanceFilter</code>, <code>locationUpdateInterval</code>, <code>fastestLocationUpdateInterval</code> Elasticity <code>disableElasticity</code>, <code>elasticityMultiplier</code> Motion detection <code>stationaryRadius</code>, <code>stopTimeout</code>, <code>stopAfterElapsedMinutes</code>, <code>disableStopDetection</code> Permissions <code>locationAuthorizationRequest</code>, <code>locationAuthorizationAlert</code>, <code>disableLocationAuthorizationAlert</code> Geofencing <code>geofenceProximityRadius</code>, <code>geofenceModeHighAccuracy</code>, <code>geofenceInitialTriggerEntry</code> Filtering <code>filter</code> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    locationAuthorizationRequest: \"Always\"\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#accuracy-and-sampling","title":"Accuracy and sampling","text":"<p><code>desiredAccuracy</code> selects the location-provider tier. Only DesiredAccuracy.High activates GPS \u2014 lower tiers use Wi-Fi and cell towers and consume significantly less power.</p> <p><code>distanceFilter</code> sets the minimum horizontal movement in meters before a new location is recorded. By default it scales automatically with speed \u2014 see Elasticity.</p> <p>On Android, <code>locationUpdateInterval</code> and <code>fastestLocationUpdateInterval</code> replace <code>distanceFilter</code> for time-based sampling. Set <code>distanceFilter</code> to <code>0</code> to activate these. <code>deferTime</code> batches location deliveries to reduce power consumption.</p> <p><code>useSignificantChangesOnly</code> disables continuous tracking in favour of coarse periodic updates (~500\u20131000 m) with dramatically lower power consumption.</p>"},{"location":"typescript/GeoConfig/#elasticity","title":"Elasticity","text":"<p>By default, the SDK scales <code>distanceFilter</code> automatically as the device's speed changes \u2014 recording fewer locations at highway speed and more at walking speed.</p> <p>The formula rounds speed to the nearest 5 m/s then multiplies:</p> <pre><code>adjusted_distanceFilter = (round(speed, 5) / 5) \u00d7 distanceFilter \u00d7 elasticityMultiplier\n</code></pre> <p>Set <code>disableElasticity</code> to <code>true</code> to use a fixed <code>distanceFilter</code>. Increase <code>elasticityMultiplier</code> to space locations further apart at speed.</p>"},{"location":"typescript/GeoConfig/#motion-detection","title":"Motion detection","text":"<p>The SDK uses platform motion APIs to switch between moving and stationary states, turning location services off when the device is idle to save power.</p> <ul> <li><code>stationaryRadius</code> \u2014 minimum distance from the last stationary fix before   continuous tracking re-engages.</li> <li><code>stopTimeout</code> \u2014 minutes to wait after the activity recognition system reports   <code>STILL</code> before transitioning to the stationary state.</li> <li><code>stopAfterElapsedMinutes</code> \u2014 automatically stop tracking after N minutes.</li> <li><code>stopOnStationary</code> \u2014 automatically call BackgroundGeolocation.stop   when the device enters the stationary state.</li> <li><code>disableStopDetection</code> \u2014 disable the motion-activity\u2013based stop-detection   system entirely.</li> <li><code>pausesLocationUpdatesAutomatically</code> \u2014 iOS only whether iOS may   automatically suspend location updates.</li> </ul>"},{"location":"typescript/GeoConfig/#permissions","title":"Permissions","text":"<p><code>locationAuthorizationRequest</code> declares which authorization level your app requires (<code>\"Always\"</code>, <code>\"WhenInUse\"</code>, or <code>\"Any\"</code>). The SDK guides users through the platform permission flow and presents <code>locationAuthorizationAlert</code> when the granted level falls below what was requested.</p> <p>Set <code>disableLocationAuthorizationAlert</code> to <code>true</code> to suppress the SDK's automatic alert and handle authorization changes manually via BackgroundGeolocation.onProviderChange.</p>"},{"location":"typescript/GeoConfig/#geofencing","title":"Geofencing","text":"<p>The SDK removes the platform limit on monitored geofences by maintaining a spatial database and activating only the geofences within <code>geofenceProximityRadius</code> of the current position. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p> <p><code>geofenceModeHighAccuracy</code> Android only runs the geofence-only service with a foreground service and active location updates for near-instant transition detection.</p> <p><code>geofenceInitialTriggerEntry</code> controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p>"},{"location":"typescript/GeoConfig/#filtering","title":"Filtering","text":"<p><code>filter</code> applies Kalman smoothing, rolling-window averaging, and accuracy and speed constraints to raw platform samples before they are recorded. This reduces GPS jitter and improves odometer accuracy. See LocationFilter for full documentation.</p>"},{"location":"typescript/GeoConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#these-options-now-belong-to-this-link-geoconfig-group","title":"These options now belong to this {@link GeoConfig} group","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/GeoConfig/#high-accuracy-tracking","title":"High-accuracy tracking","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n    distanceFilter: 10,\n    stopTimeout: 5,\n    showsBackgroundLocationIndicator: true\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#low-power-significant-changes-mode","title":"Low-power significant-changes mode","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    useSignificantChangesOnly: true\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#geofencing-with-high-accuracy","title":"Geofencing with high accuracy","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceProximityRadius: 1000,\n    geofenceInitialTriggerEntry: true,\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 0\n  }\n});\nawait BackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/GeoConfig/#members","title":"Members","text":""},{"location":"typescript/GeoConfig/#activitytype","title":"activityType","text":"<p><code>activityType?:</code><code>ActivityType</code><code>;</code></p> <p>iOS only Specifies the Core Motion activity type used by iOS to optimize its internal stop-detection algorithm. Apple is intentionally vague about how this affects motion interpretation, but each activity type provides platform hints about the expected movement pattern (e.g., automotive navigation, fitness, airborne).</p> <p>Available values are defined as constants on ActivityType.</p> Name ActivityType.Other ActivityType.AutomotiveNavigation ActivityType.Fitness ActivityType.OtherNavigation ActivityType.Airborne <p>Note</p> <p>For more details, see Apple's documentation: https://developer.apple.com/reference/corelocation/cllocationmanager/1620567-activitytype</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    activityType: ActivityType.Other,\n  },\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#allowidenticallocations","title":"allowIdenticalLocations","text":"<pre><code>allowIdenticalLocations?: boolean;\n</code></pre> <p>Android only Allows duplicate locations to be recorded when consecutive fixes are identical.</p> <p>Defaults to <code>false</code>. By default, the SDK ignores a location that is identical to the previous one. Set <code>true</code> to record every location regardless of duplication.</p> <p>When a location is ignored, the log shows: <pre><code>TSLocationManager:   \u2139\ufe0f  IGNORED: same as last location\n</code></pre></p> <p>Note</p> <p>Identical locations are common when transitioning from stationary \u2192 moving (where a single fix is requested before continuous updates begin) or when geolocation config parameters change (e.g., <code>distanceFilter</code>).</p>"},{"location":"typescript/GeoConfig/#defertime","title":"deferTime","text":"<pre><code>deferTime?: number;\n</code></pre> <p>Android only Sets the maximum wait time in milliseconds before batched location updates are delivered.</p> <p>Defaults to <code>0</code> (no deferral). When set to a value at least 2\u00d7 the <code>locationUpdateInterval</code>, the system may delay delivery and send multiple locations at once. This can reduce battery consumption and improve accuracy on capable hardware.</p> <p>Set this as large as your use-case allows if immediate location delivery is not required.</p>"},{"location":"typescript/GeoConfig/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Specifies the desired accuracy of the geolocation system.</p> <p>Defaults to DesiredAccuracy.High.</p> Name Location Providers Description DesiredAccuracy.Navigation (iOS only) GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.High GPS + Wifi + Cellular Highest power; highest accuracy DesiredAccuracy.Medium Wifi + Cellular Medium power; Medium accuracy; DesiredAccuracy.Low Wifi (low power) + Cellular Lower power; No GPS DesiredAccuracy.VeryLow Cellular only Lowest power; lowest accuracy DesiredAccuracy.Lowest (iOS only) Lowest power; lowest accuracy <p>Note</p> <p>Only DesiredAccuracy.High uses GPS. <code>speed</code>, <code>heading</code>, and <code>altitude</code> are available only when GPS is active.</p> <p>See also - Android location accuracy - iOS desiredAccuracy</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: DesiredAccuracy.High,\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#disableelasticity","title":"disableElasticity","text":"<pre><code>disableElasticity?: boolean;\n</code></pre> <p>Disables automatic speed-based <code>distanceFilter</code> scaling.</p> <p>Defaults to <code>false</code>. When <code>false</code>, the SDK automatically increases <code>distanceFilter</code> as speed increases (and decreases it as speed decreases) to record fewer locations and conserve energy.</p> <p>The following example shows elasticity in action on highway 101 towards San Francisco \u2014 locations become compressed as <code>distanceFilter</code> decreases when the driver slows into traffic.</p> <p></p> <p>See also - <code>elasticityMultiplier</code> - <code>distanceFilter</code></p>"},{"location":"typescript/GeoConfig/#disablelocationauthorizationalert","title":"disableLocationAuthorizationAlert","text":"<pre><code>disableLocationAuthorizationAlert?: boolean;\n</code></pre> <p>Disables the SDK's automatic alert when location authorization is insufficient.</p> <p>Defaults to <code>false</code>. By default, the SDK shows a native alert directing the user to the Settings screen when location services are disabled or the authorization level falls below what <code>locationAuthorizationRequest</code> requires.</p> <p>When set to <code>true</code>, you are responsible for handling authorization changes by listening to BackgroundGeolocation.onProviderChange.</p>"},{"location":"typescript/GeoConfig/#ios","title":"iOS","text":"<p>The alert dialog text can be customized via <code>locationAuthorizationAlert</code>.</p> <p></p>"},{"location":"typescript/GeoConfig/#android","title":"Android","text":"<p>Android detects when the device's Settings \u2192 Location mode does not satisfy your location request. For example, if the user selects Battery Saving (Wi-Fi only) but you requested DesiredAccuracy.High (GPS), Android shows a resolution dialog asking the user to confirm the required change.</p> <p></p> <p>This dialog appears automatically on: - BackgroundGeolocation.onProviderChange - BackgroundGeolocation.start - BackgroundGeolocation.requestPermission</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange] \", event);\n\n  if (!provider.enabled) {\n    alert(\"Please enable location services\");\n  }\n});\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    disableLocationAuthorizationAlert: true\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#disablestopdetection","title":"disableStopDetection","text":"<pre><code>disableStopDetection?: boolean;\n</code></pre> <p>Disables the motion-activity\u2013based stop-detection system.</p> <p>When <code>true</code>, the SDK ignores platform motion-activity signals when determining whether the device is stationary. This affects how and when location services are automatically turned off on both platforms.</p>"},{"location":"typescript/GeoConfig/#ios_1","title":"iOS","text":"<p>Disables the accelerometer-based stop-detection system. iOS location services then turn off automatically after exactly 15 minutes of no motion \u2014 you lose control over <code>stopTimeout</code>.</p> <p>To prevent iOS from ever automatically disabling location services, also set</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    disableStopDetection: true,\n    pausesLocationUpdatesAutomatically: false\n  }\n});\n</code></pre> <p>Warning</p> <p>With <code>disableStopDetection: true</code> and <code>pausesLocationUpdatesAutomatically: false</code>, iOS location services never turn off. This can severely drain the battery. Only use this configuration if you fully control tracking manually (for example, a workout app toggling tracking via BackgroundGeolocation.changePace).</p> <p>iOS stop-detection timing:</p> <p></p>"},{"location":"typescript/GeoConfig/#android_1","title":"Android","text":"<p>When <code>true</code>, Android location services never turn off automatically. You must disable tracking manually by calling BackgroundGeolocation.changePace with <code>false</code>, or BackgroundGeolocation.stop.</p>"},{"location":"typescript/GeoConfig/#distancefilter","title":"distanceFilter","text":"<pre><code>distanceFilter?: number;\n</code></pre> <p>The minimum distance (meters) a device must move horizontally before a new location is recorded.</p> <p>Defaults to <code>10</code> meters.</p> <p>By default, <code>distanceFilter</code> is elastically auto-scaled by the SDK: when speed increases, <code>distanceFilter</code> increases; when speed decreases, so too does <code>distanceFilter</code>.</p> <p>Note</p> <ul> <li>To disable auto-scaling, set <code>disableElasticity</code> to <code>true</code>.</li> <li>To control the scale of the automatic calculation, see <code>elasticityMultiplier</code>.</li> </ul> <p><code>distanceFilter</code> is auto-scaled by rounding speed to the nearest <code>5 m/s</code> and multiplying <code>distanceFilter</code> by the result:</p>"},{"location":"typescript/GeoConfig/#at-biking-speed-77-ms-distancefilter-30","title":"At biking speed (7.7 m/s, distanceFilter: 30)","text":"<pre><code>rounded_speed = round(7.7, 5)  =&gt; 10\nmultiplier = rounded_speed / 5  =&gt; 10 / 5 = 2\nadjusted_distanceFilter = multiplier * distanceFilter\n  =&gt; 2 * 30 = 60 meters\n</code></pre>"},{"location":"typescript/GeoConfig/#at-highway-speed-27-ms-distancefilter-50","title":"At highway speed (27 m/s, distanceFilter: 50)","text":"<pre><code>rounded_speed = round(27, 5)    =&gt; 30\nmultiplier = rounded_speed / 5  =&gt; 30 / 5 = 6\nadjusted_distanceFilter = multiplier * distanceFilter * elasticityMultiplier\n  =&gt; 6 * 50 = 300 meters\n</code></pre> <p>The following example shows elasticity on highway 101 towards San Francisco as the driver slows into traffic \u2014 locations compress as <code>distanceFilter</code> decreases.</p> <p></p> <p>Compare background-geolocation at city scale. The left-hand track is from a cab-ride; the right-hand track is walking speed.</p> <p></p>"},{"location":"typescript/GeoConfig/#elasticitymultiplier","title":"elasticityMultiplier","text":"<pre><code>elasticityMultiplier?: number;\n</code></pre> <p>Controls the scale of automatic speed-based <code>distanceFilter</code> elasticity.</p> <p>Defaults to <code>1.0</code>. Increasing <code>elasticityMultiplier</code> results in fewer location samples as speed increases. A value of <code>0</code> has the same effect as setting <code>disableElasticity</code> to <code>true</code>.</p>"},{"location":"typescript/GeoConfig/#enabletimestampmeta","title":"enableTimestampMeta","text":"<pre><code>enableTimestampMeta?: boolean;\n</code></pre> <p>Appends extra timestamp metadata to each recorded location, including system time.</p> <p>Defaults to <code>false</code>. Some devices report GPS Location.timestamp values that differ from the device's system clock. Enabling this option appends additional timing fields to each location for debugging and cross-referencing.</p>"},{"location":"typescript/GeoConfig/#android_2","title":"Android","text":"<pre><code>JSONObject timestampMeta = new JSONObject();\ntimestampMeta.put(\"time\", mLocation.getTime());\nif (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR1) {\n    timestampMeta.put(\"systemClockElaspsedRealtime\", SystemClock.elapsedRealtimeNanos()/1000000);\n    timestampMeta.put(\"elapsedRealtime\", mLocation.getElapsedRealtimeNanos()/1000000);\n} else {\n    timestampMeta.put(\"systemTime\", System.currentTimeMillis());\n}\n</code></pre>"},{"location":"typescript/GeoConfig/#ios_2","title":"iOS","text":"<pre><code>long long systemTime = (long long)([[NSDate date] timeIntervalSince1970] * 1000.0);\nlong long locationTime = (long long)([_location.timestamp timeIntervalSince1970] * 1000.0);\nlong long uptime = (long long) [self.class uptime] * 1000;\n\nreturn @{\n    @\"time\": @(locationTime),\n    @\"systemTime\": @(systemTime),\n    @\"systemClockElapsedRealtime\": @(uptime)\n};\n</code></pre>"},{"location":"typescript/GeoConfig/#fastestlocationupdateinterval","title":"fastestLocationUpdateInterval","text":"<pre><code>fastestLocationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the fastest interval for location updates, in milliseconds.</p> <p>Defaults to <code>-1</code> (not set). When other apps or system components trigger location updates at a faster rate, the SDK receives those updates passively at up to this rate without increasing power usage.</p> <p>Unlike <code>locationUpdateInterval</code>, this value is a hard cap \u2014 the SDK never receives updates faster than this interval.</p> <p>Note</p> <ul> <li>A value of <code>0</code> is allowed but not recommended, since some devices may deliver   extremely rapid updates.</li> <li>If <code>fastestLocationUpdateInterval</code> is slower than <code>locationUpdateInterval</code>,   the effective fastest interval becomes <code>locationUpdateInterval</code>.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    fastestLocationUpdateInterval: 5000 // Receive passive updates at most every 5 s\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setFastestInterval</p>"},{"location":"typescript/GeoConfig/#filter","title":"filter","text":"<p><code>filter?:</code><code>LocationFilter</code><code>;</code></p> <p>Defines how raw GPS samples are filtered, denoised, and smoothed before being recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p> <p>The native platform continuously produces raw <code>CLLocation</code> (iOS) or <code>Location</code> (Android) samples. The filter applies Kalman filtering, rolling-window averaging, and speed, distance, and accuracy constraints to produce smoother paths, reduce jitter, and improve odometer stability.</p>"},{"location":"typescript/GeoConfig/#filtering-flow","title":"Filtering flow","text":"Field Description LocationFilter.policy Selects which filtering policy to apply. See LocationFilterPolicy. LocationFilter.useKalman Enables Kalman filtering of speed and position (default: <code>true</code>). LocationFilter.kalmanDebug Enables verbose Kalman diagnostic logs. LocationFilter.kalmanProfile Selects a Kalman tuning profile (see KalmanProfile). LocationFilter.rollingWindow Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. LocationFilter.burstWindow Duration of each averaging burst (seconds). Default: <code>10</code>. LocationFilter.maxBurstDistance Maximum distance (meters) for samples to be included in the same burst window. Default: <code>300</code>. LocationFilter.trackingAccuracyThreshold Minimum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. LocationFilter.maxImpliedSpeed Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (~216 km/h). LocationFilter.filterDebug Enables verbose logging of filter decisions (<code>ACCEPTED</code>, <code>REJECTED</code>, etc). LocationFilter.odometerUseKalmanFilter Applies Kalman smoothing to odometer calculations. LocationFilter.odometerAccuracyThreshold Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>100</code>. <p>Note</p> <ul> <li>Distances are in meters.</li> <li>Time fields are in milliseconds unless otherwise specified.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#geofenceinitialtriggerentry","title":"geofenceInitialTriggerEntry","text":"<pre><code>geofenceInitialTriggerEntry?: boolean;\n</code></pre> <p>Controls whether a geofence fires an entry event immediately if the device is already inside it when the geofence is registered.</p> <p>Defaults to <code>true</code>. Set <code>false</code> to suppress the immediate entry trigger and wait until the device exits and re-enters the geofence.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code>.</p>"},{"location":"typescript/GeoConfig/#geofencemodehighaccuracy","title":"geofenceModeHighAccuracy","text":"<pre><code>geofenceModeHighAccuracy?: boolean;\n</code></pre> <p>Android only Enables high-accuracy mode for geofence-only tracking.</p> <p>Defaults to <code>true</code>. Runs BackgroundGeolocation.startGeofences with a foreground service (and its corresponding persistent AppConfig.notification), making geofence transition events significantly more responsive.</p> <p>In high-accuracy mode, location-service options apply directly: - GeoConfig.desiredAccuracy (DesiredAccuracy.Medium works well) - GeoConfig.locationUpdateInterval - GeoConfig.distanceFilter - GeoConfig.deferTime</p> <p>Warning</p> <p>High-accuracy mode consumes more power. The more aggressively you configure the location-update params above, the more responsive geofence triggering will be \u2014 and the higher the power cost.</p> <p><code>geofenceModeHighAccuracy: false</code> \u2014 Transition events are delayed: </p> <p><code>geofenceModeHighAccuracy: true</code> \u2014 Transition events are nearly instantaneous: </p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    geofenceModeHighAccuracy: true,\n    desiredAccuracy: DesiredAccuracy.Medium,\n    locationUpdateInterval: 5000,\n    distanceFilter: 50,\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.startGeofences();\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#geofenceproximityradius","title":"geofenceProximityRadius","text":"<pre><code>geofenceProximityRadius?: number;\n</code></pre> <p>Defines the radius (meters) around the device used to query for geofences that should be actively monitored.</p> <p>Mobile platforms allow only a limited number of concurrently monitored geofences (iOS: 20, Android: ~100). The SDK removes this limitation by storing all registered geofences in an internal database and activating only those within this radius. As the device moves, the active set updates automatically, firing BackgroundGeolocation.onGeofencesChange.</p>"},{"location":"typescript/GeoConfig/#ios_3","title":"iOS","text":"<p>Defaults to <code>2000</code> meters (minimum <code>100</code> meters for reliable detection).</p>"},{"location":"typescript/GeoConfig/#android_3","title":"Android","text":"<p>Defaults to <code>1000</code> meters.</p> <p>See also - \ud83d\udcd8 <code>Geofencing Guide</code> - Animation of this behavior</p> <p></p>"},{"location":"typescript/GeoConfig/#locationauthorizationalert","title":"locationAuthorizationAlert","text":"<pre><code>locationAuthorizationAlert?: Record&lt;string, any&gt;;\n</code></pre> <p>iOS only Customizes the text displayed in the SDK's location-authorization alert dialog. When <code>locationAuthorizationRequest</code> is <code>\"Always\"</code> or <code>\"WhenInUse\"</code> and the user subsequently downgrades or disables location permission in iOS Settings, the SDK presents an alert directing the user back to the Settings screen.</p> <p>Supply an object with all required fields to override the default strings:</p> <p></p> <p>Warning</p> <p>You must supply all fields \u2014 not just a subset. Omitting any field will cause the alert to display an empty or unexpected string.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationAlert: {\n      titleWhenNotEnabled: \"Location services are not enabled\",\n      titleWhenOff: \"Location services are OFF\",\n      instructions:\n        \"Please enable 'Always' in Location Services to allow background tracking.\",\n      cancelButton: \"Cancel\",\n      settingsButton: \"Open Settings\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#locationauthorizationrequest","title":"locationAuthorizationRequest","text":"<pre><code>locationAuthorizationRequest?: \"Always\" | \"WhenInUse\" | \"Any\";\n</code></pre> <p>Declares the location authorization level the app requires from the user.</p> <p>Defaults to <code>\"Always\"</code>. Valid values are: - <code>\"Always\"</code> \u2014 background and foreground location access - <code>\"WhenInUse\"</code> \u2014 foreground location access only - <code>\"Any\"</code> \u2014 accept whichever level the user grants</p> <p>If you request <code>\"Always\"</code> but the user grants only When-In-Use, the SDK displays <code>locationAuthorizationAlert</code> unless suppressed via <code>disableLocationAuthorizationAlert</code>.</p>"},{"location":"typescript/GeoConfig/#ios_4","title":"iOS","text":"<p>iOS 13+ no longer grants Always Allow on the initial dialog. After granting While Using the App, iOS may later prompt the user to upgrade.</p> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>The user first sees the While-Using dialog, then an upgrade prompt for Always Allow:</p> <p></p> <p>If the user denies Always, the SDK displays <code>locationAuthorizationAlert</code> (unless disabled):</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial dialog appears:</p> <p></p> <p>Upgrading the config to <code>\"Always\"</code> later triggers the upgrade prompt immediately:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>The SDK requests Always internally but accepts either result. iOS may show the upgrade prompt spontaneously later:</p> <p></p> <pre><code>// Start with When-In-Use\nBackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"WhenInUse\" }\n});\n\nasync function onClickStartTracking() {\n  await BackgroundGeolocation.start();\n\n  // Upgrade to Always at any time.\n  BackgroundGeolocation.setConfig({\n    geolocation: { locationAuthorizationRequest: \"Always\" }\n  });\n}\n</code></pre>"},{"location":"typescript/GeoConfig/#android_4","title":"Android","text":"<p>Android 11+ (targetSdkVersion \u2265 30)</p> <p>Android 11 removes Allow all the time from the initial dialog. Apps must present a custom rationale UI before navigating the user to the system Location Permissions screen. The SDK automatically shows AppConfig.backgroundPermissionRationale when configured.</p> <p>The rationale dialog is shown only once unless the user resets permissions.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: { locationAuthorizationRequest: \"Always\" },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow access to this device's location in the background?\",\n      message: \"To track your trips, please enable 'Allow all the time' location permission.\",\n      positiveAction: \"Change to Allow all the time\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre> <p>1. When <code>locationAuthorizationRequest: \"Always\"</code></p> <p>After granting While Using, Android immediately displays the rationale dialog:</p> <p></p> <p>2. When <code>locationAuthorizationRequest: \"WhenInUse\"</code></p> <p>Only the initial system dialog appears:</p> <p></p> <p>Upgrading to <code>\"Always\"</code> later triggers the rationale dialog:</p> <p></p> <p>3. When <code>locationAuthorizationRequest: \"Any\"</code></p> <p>Treated the same as <code>\"Always\"</code>.</p>"},{"location":"typescript/GeoConfig/#locationupdateinterval","title":"locationUpdateInterval","text":"<pre><code>locationUpdateInterval?: number;\n</code></pre> <p>Android only Sets the desired interval for active location updates, in milliseconds.</p> <p>Defaults to <code>1000</code> ms. The system honors this interval on a best-effort basis \u2014 updates may arrive slower (no providers available), faster (another app requests faster updates), or not at all (permission or system constraints).</p> <p>Apps with only coarse location permission may have this interval silently throttled.</p> <p>Warning</p> <p><code>locationUpdateInterval</code> is ignored when <code>distanceFilter</code> is greater than <code>0</code>. Set <code>distanceFilter</code> to <code>0</code> to activate time-based sampling.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    distanceFilter: 0,           // Required \u2014 otherwise this field is ignored.\n    locationUpdateInterval: 5000 // Request a fix every ~5 seconds\n  }\n});\n</code></pre> <p>See also - Android LocationRequest.setInterval</p>"},{"location":"typescript/GeoConfig/#pauseslocationupdatesautomatically","title":"pausesLocationUpdatesAutomatically","text":"<pre><code>pausesLocationUpdatesAutomatically?: boolean;\n</code></pre> <p>iOS only Prevents the iOS location API from automatically pausing location updates. When set to <code>false</code>, iOS location services never turn off automatically. By default (<code>true</code>), the SDK manages this automatically \u2014 turning location services off after <code>stopTimeout</code> minutes of inactivity.</p> <p>Setting this to <code>false</code> forces: - ActivityConfig.disableStopDetection to <code>true</code> automatically - You to manage tracking lifetime manually</p> <p>Warning</p> <p>Setting this to <code>false</code> can cause severe battery drain. Only use this for highly specialised use-cases where you fully control tracking lifecycle (for example, a workout app that calls BackgroundGeolocation.changePace directly). AppConfig.preventSuspend will no longer function in this mode.</p>"},{"location":"typescript/GeoConfig/#showsbackgroundlocationindicator","title":"showsBackgroundLocationIndicator","text":"<pre><code>showsBackgroundLocationIndicator?: boolean;\n</code></pre> <p>iOS only Controls whether the status bar indicator appears when location services are active in the background with <code>Always</code> authorization. Defaults to <code>true</code>. When <code>true</code>, iOS displays a blue bar or pill in the status bar while the app uses location in the background. Users can tap the indicator to return to your app.</p> <p>This property affects only apps with <code>Always</code> authorization. For apps with When-In-Use authorization, iOS always changes the status bar appearance when location services are active in the background.</p>"},{"location":"typescript/GeoConfig/#stationaryradius","title":"stationaryRadius","text":"<pre><code>stationaryRadius?: number;\n</code></pre> <p>The minimum distance the device must move beyond the stationary location before aggressive background-tracking re-engages.</p>"},{"location":"typescript/GeoConfig/#ios_5","title":"iOS","text":"<p>Defaults to <code>25</code> meters. In practice, iOS requires approximately 200 meters of movement before triggering exit from the stationary state \u2014 the platform does not detect departure at the exact radius boundary.</p>"},{"location":"typescript/GeoConfig/#android_5","title":"Android","text":"<p>Defaults to <code>150</code> meters (minimum <code>25</code>, maximum <code>1000</code>).</p> <p>Warning</p> <p>Setting <code>stationaryRadius: 0</code> has no effect \u2014 a minimum of <code>25</code> meters is enforced. In practice, the native API does not respond until the device has moved approximately 200 meters.</p> <p>The following image shows the typical distance iOS requires to detect exit from the stationary radius: - Green polylines: transition from stationary \u2192 moving (~200 meters). - Red circles: locations where the SDK entered the stationary state.</p> <p></p> <p>See also - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"typescript/GeoConfig/#stopafterelapsedminutes","title":"stopAfterElapsedMinutes","text":"<pre><code>stopAfterElapsedMinutes?: number;\n</code></pre> <p>Automatically stops tracking after the specified number of minutes.</p> <p>Disabled by default. When set, the SDK automatically calls BackgroundGeolocation.stop after this many minutes have elapsed since BackgroundGeolocation.start was called.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopAfterElapsedMinutes: 30\n  }\n}).then((state) =&gt; {\n  BackgroundGeolocation.start();  // SDK will automatically stop in 30 minutes\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#stoponstationary","title":"stopOnStationary","text":"<pre><code>stopOnStationary?: boolean;\n</code></pre> <p>Automatically calls BackgroundGeolocation.stop when the <code>stopTimeout</code> elapses.</p> <p>When <code>true</code>, the SDK stops itself the next time BackgroundGeolocation.onMotionChange fires into the stationary state after the <code>stopTimeout</code> timer elapses.</p> <p>Warning</p> <p><code>stopOnStationary</code> fires only when the SDK transitions to the stationary state due to <code>stopTimeout</code> expiry. It does not fire when you manually call BackgroundGeolocation.changePace with <code>false</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    stopOnStationary: true,\n  },\n  app: {\n    isMoving: true,\n  }\n}, (state) =&gt; {\n  BackgroundGeolocation.start();\n});\n</code></pre>"},{"location":"typescript/GeoConfig/#stoptimeout","title":"stopTimeout","text":"<pre><code>stopTimeout?: number;\n</code></pre> <p>Minutes to wait in the moving state with no detected movement before transitioning to the stationary state.</p> <p>Defaults to <code>5</code> minutes.</p> <p>When in the moving state, the SDK waits this many minutes after the activity recognition system reports <code>STILL</code> before turning off location services. A common use-case is to delay GPS OFF while a car is stopped at a traffic light.</p> <p>Warning</p> <p>Setting a very long <code>stopTimeout</code> keeps location services active while the device is potentially motionless for extended periods, which may significantly impact battery life.</p> <p>See also - BackgroundGeolocation.onMotionChange - \ud83d\udcd8 Philosophy of Operation</p>"},{"location":"typescript/GeoConfig/#usesignificantchangesonly","title":"useSignificantChangesOnly","text":"<pre><code>useSignificantChangesOnly?: boolean;\n</code></pre> <p>Disables continuous background tracking in favour of periodic, coarse location updates.</p> <p>Defaults to <code>false</code>. When <code>true</code>, a location is recorded only every 500\u20131000 meters (higher in non-urban environments, depending on cell tower spacing). Many configuration parameters have no effect in this mode, including <code>distanceFilter</code>, <code>stationaryRadius</code>, and <code>activityType</code>.</p> <p>Using <code>useSignificantChangesOnly: true</code> provides significant power savings at the expense of fewer recorded locations.</p>"},{"location":"typescript/GeoConfig/#ios_6","title":"iOS","text":"<p>Engages the iOS Significant Location Changes API, delivering updates every 500\u20131000 meters.</p> <p>Note</p> <p>If Apple has rejected your app for using <code>UIBackgroundMode: \"location\"</code>, this mode can be a viable alternative.</p>"},{"location":"typescript/GeoConfig/#android_6","title":"Android","text":"<p>A location is recorded several times per hour while the device is in the moving state. No foreground service is run (and no persistent NotificationConfig notification is shown).</p> <p>Example 1 \u2014 <code>useSignificantChangesOnly: true</code>:</p> <p></p> <p>Example 2 \u2014 <code>useSignificantChangesOnly: false</code> (default):</p> <p></p>"},{"location":"typescript/Geofence/","title":"Geofence","text":"<pre><code>export interface Geofence {\n</code></pre> <p>A geofence definition for monitoring circular or polygon regions.</p> <p>The SDK implements the native iOS and Android Geofencing APIs, extended with polygon support and a proximity-based infinite-geofencing system that overcomes the platform limits of 20 (iOS) and 100 (Android) simultaneous geofences.</p>"},{"location":"typescript/Geofence/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Adding geofences</li> <li>Listening for events</li> <li>Polygon geofencing</li> <li>Infinite geofencing</li> <li>Removing geofences</li> <li>Geofences-only mode</li> <li>Examples</li> </ul>"},{"location":"typescript/Geofence/#overview","title":"Overview","text":"Field Required Description <code>identifier</code> \u2705 Unique name for this geofence. <code>latitude</code> \u2705* Center latitude (*omit for polygon geofences). <code>longitude</code> \u2705* Center longitude (*omit for polygon geofences). <code>radius</code> \u2705* Radius in meters (*omit for polygon geofences). <code>notifyOnEntry</code> \u2014 Fire event on entry. <code>notifyOnExit</code> \u2014 Fire event on exit. <code>notifyOnDwell</code> \u2014 Fire event after loitering for <code>loiteringDelay</code> ms. <code>vertices</code> \u2014 Polygon geofence vertices (replaces lat/lng/radius). <code>extras</code> \u2014 Arbitrary key-value metadata posted with each event. <p>Warning</p> <p>Both platforms require GeoConfig.locationAuthorizationRequest to be <code>\"Always\"</code>. Geofencing does not work with <code>\"WhenInUse\"</code> only.</p>"},{"location":"typescript/Geofence/#adding-geofences","title":"Adding geofences","text":"<p>Use BackgroundGeolocation.addGeofence for a single geofence, or BackgroundGeolocation.addGeofences for bulk inserts (approximately 10\u00d7 faster than inserting individually).</p> <p>If a geofence with the same <code>identifier</code> already exists in the database, it is replaced.</p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"typescript/Geofence/#listening-for-events","title":"Listening for events","text":"<p>Subscribe to geofence transitions with BackgroundGeolocation.onGeofence. Subscribe to changes in the actively monitored set with BackgroundGeolocation.onGeofencesChange.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event.identifier, event.action);\n});\n\nBackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  const on = event.on;   // newly activated geofences\n  const off = event.off; // deactivated geofence identifiers\n\n  on.forEach((geofence) =&gt; createGeofenceMarker(geofence));\n  off.forEach((identifier) =&gt; removeGeofenceMarker(identifier));\n});\n</code></pre> <p>Note</p> <p>When all geofences are removed, BackgroundGeolocation.onGeofencesChange fires with empty arrays for both <code>on</code> and <code>off</code>.</p>"},{"location":"typescript/Geofence/#polygon-geofencing","title":"Polygon geofencing","text":"<p>The SDK supports polygon geofences of any shape via the <code>vertices</code> field. Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p>When defining a polygon geofence, omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK calculates the minimum enclosing circle automatically from the polygon geometry.</p> <p> </p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"typescript/Geofence/#infinite-geofencing","title":"Infinite geofencing","text":"<p>The SDK stores all geofences in its database and uses a geospatial query to activate only the nearest geofences within GeoConfig.geofenceProximityRadius, staying within the platform limit. As the device moves, the active set is periodically refreshed.</p> <ul> <li>The minimum GeoConfig.geofenceProximityRadius is enforced at <code>1000</code> meters.</li> <li>Geofences within the radius (green in the diagram below) are actively monitored.</li> <li>Geofences outside the radius (grey) remain in the database but are dormant.</li> </ul> <p></p>"},{"location":"typescript/Geofence/#removing-geofences","title":"Removing geofences","text":"<p>Geofences persist in the SDK's database until explicitly removed. If AppConfig.stopOnTerminate is <code>false</code> and AppConfig.startOnBoot is <code>true</code>, geofences continue to be monitored across app termination and device reboots.</p> <pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre> <pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre> <pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"typescript/Geofence/#geofences-only-mode","title":"Geofences-only mode","text":"<p>Call BackgroundGeolocation.startGeofences instead of BackgroundGeolocation.start to monitor geofences without continuous location tracking. Use GeoConfig.geofenceModeHighAccuracy to improve event responsiveness at the cost of higher power usage.</p> <p>The SDK can switch between full tracking and geofences-only mode at any time by calling the corresponding start method.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/Geofence/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/Geofence/#single-geofence","title":"Single geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  extras: { route_id: 1234 }\n});\n</code></pre>"},{"location":"typescript/Geofence/#multiple-geofences","title":"Multiple geofences","text":"<pre><code>await BackgroundGeolocation.addGeofences([{\n  identifier: \"Home\",\n  radius: 200,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n}, {\n  identifier: \"Work\",\n  radius: 200,\n  latitude: 45.61921927,\n  longitude: -73.71678582,\n  notifyOnEntry: true\n}]);\n</code></pre>"},{"location":"typescript/Geofence/#polygon-geofence","title":"Polygon geofence","text":"<pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Park\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre>"},{"location":"typescript/Geofence/#remove-a-single-geofence","title":"Remove a single geofence","text":"<pre><code>await BackgroundGeolocation.removeGeofence(\"Home\");\n</code></pre>"},{"location":"typescript/Geofence/#remove-all-geofences","title":"Remove all geofences","text":"<pre><code>await BackgroundGeolocation.removeGeofences();\n</code></pre>"},{"location":"typescript/Geofence/#fetch-all-stored-geofences","title":"Fetch all stored geofences","text":"<pre><code>const geofences = await BackgroundGeolocation.getGeofences();\nconsole.log(\"[getGeofences]\", geofences);\n</code></pre>"},{"location":"typescript/Geofence/#geofences-only-mode_1","title":"Geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\"[onGeofence]\", event);\n});\n\nawait BackgroundGeolocation.ready({\n  http: { url: \"https://your.server.com/geofences\", autoSync: true },\n  geolocation: { geofenceModeHighAccuracy: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/Geofence/#toggle-between-location-tracking-and-geofences-only-mode","title":"Toggle between location tracking and geofences-only mode","text":"<pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  if (event.identifier === \"DANGER_ZONE\") {\n    if (event.action === \"ENTER\") {\n      // Entering the zone \u2014 switch to full location tracking.\n      BackgroundGeolocation.start();\n    } else if (event.action === \"EXIT\") {\n      // Exiting the zone \u2014 return to geofences-only mode.\n      BackgroundGeolocation.startGeofences();\n    }\n  }\n});\n\nBackgroundGeolocation.addGeofence({\n  identifier: \"DANGER_ZONE\",\n  radius: 1000,\n  latitude: 45.51921926,\n  longitude: -73.61678581,\n  notifyOnEntry: true,\n  notifyOnExit: true,\n});\n\nawait BackgroundGeolocation.ready({\n  geolocation: {\n    desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n    distanceFilter: 10,\n  },\n  http: { url: \"https://your.server.com/locations\", autoSync: true }\n});\nBackgroundGeolocation.startGeofences();\n</code></pre>"},{"location":"typescript/Geofence/#members","title":"Members","text":""},{"location":"typescript/Geofence/#entrystate","title":"entryState","text":"<p><code>entry</code><code>State</code><code>?: number;</code></p> <p>Current entry state of the geofence.</p> Value State <code>0</code> Outside <code>1</code> Inside"},{"location":"typescript/Geofence/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, unknown&gt;;\n</code></pre> <p>Arbitrary key-value metadata attached to each geofence event and included in the payload posted to HttpConfig.url.</p>"},{"location":"typescript/Geofence/#hits","title":"hits","text":"<pre><code>hits?: number;\n</code></pre> <p>Number of times this geofence has been triggered since it was added.</p>"},{"location":"typescript/Geofence/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>Unique identifier for this geofence.</p> <p>Used to reference the geofence in events and removal calls. Adding a geofence with an identifier that already exists replaces the existing one.</p>"},{"location":"typescript/Geofence/#latitude","title":"latitude","text":"<pre><code>latitude: number;\n</code></pre> <p>Latitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"typescript/Geofence/#loiteringdelay","title":"loiteringDelay","text":"<pre><code>loiteringDelay?: number;\n</code></pre> <p>Minimum time in milliseconds the device must remain inside the geofence before a <code>notifyOnDwell</code> event fires. Default <code>0</code>.</p>"},{"location":"typescript/Geofence/#longitude","title":"longitude","text":"<pre><code>longitude: number;\n</code></pre> <p>Longitude of the circular geofence center.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the center automatically.</p>"},{"location":"typescript/Geofence/#notifyondwell","title":"notifyOnDwell","text":"<pre><code>notifyOnDwell?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device has remained inside this geofence for <code>loiteringDelay</code> milliseconds.</p>"},{"location":"typescript/Geofence/#notifyonentry","title":"notifyOnEntry","text":"<pre><code>notifyOnEntry?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device enters this geofence.</p> <p>See also - GeoConfig.geofenceInitialTriggerEntry</p>"},{"location":"typescript/Geofence/#notifyonexit","title":"notifyOnExit","text":"<pre><code>notifyOnExit?: boolean;\n</code></pre> <p>Fire a GeofenceEvent when the device exits this geofence.</p>"},{"location":"typescript/Geofence/#radius","title":"radius","text":"<pre><code>radius: number;\n</code></pre> <p>Radius of the circular geofence in meters.</p> <p>Omit when defining a polygon geofence via <code>vertices</code> \u2014 the SDK calculates the enclosing radius automatically.</p> <p>Warning</p> <p>The minimum reliable radius is <code>200</code> meters. Below this threshold, geofences may not trigger reliably on either platform.</p> <p>Apple documents this explicitly:</p> <p>\"For testing purposes, you can assume that the minimum distance is approximately 200 meters.\"</p>"},{"location":"typescript/Geofence/#stateupdatedat","title":"stateUpdatedAt","text":"<pre><code>stateUpdatedAt?: number;\n</code></pre> <p>Epoch timestamp in seconds of the last geofence transition.</p>"},{"location":"typescript/Geofence/#vertices","title":"vertices","text":"<p><code>vertices?:</code><code>Vertices</code><code>;</code></p> <p>Polygon geofence vertices as <code>[[lat, lng], ...]</code> pairs.</p> <p>When provided, the geofence is treated as a polygon rather than a circle. Omit <code>latitude</code>, <code>longitude</code>, and <code>radius</code> \u2014 the SDK solves the minimum enclosing circle from the vertex geometry and registers that as the native circular geofence.</p> <p>When the device enters the enclosing circle, the SDK begins C++ hit-testing against the polygon at high frequency. When it exits the circle, polygon monitoring ceases.</p> <p>Note</p> <p>Polygon geofencing is sold as a separate add-on but is fully functional in DEBUG builds.</p> <p></p> <pre><code>BackgroundGeolocation.addGeofence({\n  identifier: \"Home\",\n  notifyOnEntry: true,\n  notifyOnExit: true,\n  vertices: [\n    [45.518947279987714, -73.6049889209514],\n    [45.5182711292279,   -73.60338649600598],\n    [45.517082240237634, -73.60432670908212],\n    [45.51774871402813,  -73.60604928622278]\n  ]\n});\n</code></pre> <p> </p>"},{"location":"typescript/GeofenceAction/","title":"GeofenceAction","text":"<pre><code>export const GeofenceAction = {\n</code></pre> <p>Geofence transition actions emitted by the SDK</p> <p>Used with GeofenceEvent from BackgroundGeolocation.onGeofence.</p> Action Meaning ENTER Device entered the geofence EXIT Device exited the geofence DWELL Device dwelled inside"},{"location":"typescript/GeofenceAction/#members","title":"Members","text":""},{"location":"typescript/GeofenceAction/#dwell","title":"Dwell","text":"<pre><code>Dwell: 'DWELL'\n</code></pre> <p>Device dwelled inside the geofence for at least the configured <code>Config.geofenceProximityRadius</code>.</p>"},{"location":"typescript/GeofenceAction/#enter","title":"Enter","text":"<pre><code>Enter: 'ENTER',\n</code></pre> <p>Device entered the geofence.</p>"},{"location":"typescript/GeofenceAction/#exit","title":"Exit","text":"<pre><code>Exit: 'EXIT',\n</code></pre> <p>Device exited the geofence.</p>"},{"location":"typescript/GeofenceEvent/","title":"GeofenceEvent","text":"<pre><code>export interface GeofenceEvent {\n</code></pre> <p>Geofence transition delivered to BackgroundGeolocation.onGeofence.</p> <p>The SDK fires this event each time the device crosses a monitored geofence boundary. Each event carries the transition type (<code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>), the location recorded at the moment of the crossing, and the full Geofence record that triggered it.</p> <pre><code>BackgroundGeolocation.onGeofence((event) =&gt; {\n  console.log(\n    \"[onGeofence]\",\n    event.identifier,\n    event.action,\n    event.location\n  );\n});\n</code></pre>"},{"location":"typescript/GeofenceEvent/#members","title":"Members","text":""},{"location":"typescript/GeofenceEvent/#action","title":"action","text":"<p><code>action:</code><code>GeofenceAction</code><code>;</code></p> <p>Transition type that fired: <code>ENTER</code>, <code>EXIT</code>, or <code>DWELL</code>.</p>"},{"location":"typescript/GeofenceEvent/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional metadata originally configured on the Geofence. Present only when the geofence was created with an <code>extras</code> map.</p>"},{"location":"typescript/GeofenceEvent/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>Geofence</code><code>;</code></p> <p>The complete Geofence record that triggered this event.</p>"},{"location":"typescript/GeofenceEvent/#identifier","title":"identifier","text":"<pre><code>identifier: string;\n</code></pre> <p>The Geofence.identifier of the geofence that triggered this event.</p>"},{"location":"typescript/GeofenceEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the geofence boundary was crossed.</p>"},{"location":"typescript/GeofenceEvent/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string;\n</code></pre> <p>ISO-8601 UTC timestamp from the device system clock at the moment the OS delivered the geofence event.</p>"},{"location":"typescript/GeofencesChangeEvent/","title":"GeofencesChangeEvent","text":"<pre><code>export interface GeofencesChangeEvent {\n</code></pre> <p>Geofence monitoring change delivered to BackgroundGeolocation.onGeofencesChange.</p> <p>The SDK can only monitor a finite number of geofences simultaneously. As the device moves, it activates the geofences closest to the current position and deactivates those that are too far away. This event fires whenever that set changes \u2014 reporting which geofences just became active (<code>on</code>) and which were just removed from active monitoring (<code>off</code>).</p> <pre><code>BackgroundGeolocation.onGeofencesChange((event) =&gt; {\n  for (const geofence of event.on) {\n    console.log(\"[onGeofencesChange] Activated:\", geofence.identifier);\n  }\n  for (const identifier of event.off) {\n    console.log(\"[onGeofencesChange] Deactivated:\", identifier);\n  }\n});\n</code></pre>"},{"location":"typescript/GeofencesChangeEvent/#members","title":"Members","text":""},{"location":"typescript/GeofencesChangeEvent/#off","title":"off","text":"<pre><code>off: string[];\n</code></pre> <p>Identifiers of geofences that were just deactivated and are no longer being monitored.</p>"},{"location":"typescript/GeofencesChangeEvent/#on","title":"on","text":"<p><code>on:</code><code>Geofence</code><code>[];</code></p> <p>Geofences that just became active and are now being monitored.</p>"},{"location":"typescript/HeadlessEvent/","title":"HeadlessEvent Android only","text":"<pre><code>export interface HeadlessEvent {\n</code></pre> <p>Event delivered to a headless task registered via BackgroundGeolocation.registerHeadlessTask.</p> <p>Headless tasks allow the app to handle SDK events while running in the background without a UI \u2014 including after the app has been terminated by the user. Each task invocation receives a <code>HeadlessEvent</code> describing which SDK event occurred and carrying the associated payload.</p> <pre><code>const headlessTask = async (event: HeadlessEvent) =&gt; {\n  const { name, params } = event;\n  console.log(\"[HeadlessTask]\", name, params);\n\n  if (name === \"location\") {\n    const location = params as Location;\n    console.log(\"[HeadlessTask] location:\", location);\n  } else if (name === \"http\") {\n    const httpEvent = params as HttpEvent;\n    console.log(\"[HeadlessTask] http status:\", httpEvent.status);\n  }\n};\n\nBackgroundGeolocation.registerHeadlessTask(headlessTask);\n</code></pre>"},{"location":"typescript/HeadlessEvent/#members","title":"Members","text":""},{"location":"typescript/HeadlessEvent/#name","title":"name","text":"<p><code>name:</code><code>Event</code><code>;</code></p> <p>The SDK event that triggered this headless task invocation (e.g. <code>location</code>, <code>http</code>, <code>geofence</code>, <code>heartbeat</code>, <code>terminate</code>).</p>"},{"location":"typescript/HeadlessEvent/#params","title":"params","text":"<pre><code>params: Record&lt;string, any&gt;;\n</code></pre> <p>Event payload corresponding to <code>name</code>. Cast this to the appropriate event type based on the value of <code>name</code> (e.g. Location for <code>location</code>, HttpEvent for <code>http</code>).</p>"},{"location":"typescript/HeartbeatEvent/","title":"HeartbeatEvent","text":"<pre><code>export interface HeartbeatEvent {\n</code></pre> <p>Periodic heartbeat delivered to BackgroundGeolocation.onHeartbeat.</p> <p>The SDK fires this event on a fixed interval while running in the background, providing a regular opportunity to perform work even when no location events are occurring. Configure the interval with AppConfig.heartbeatInterval.</p> <pre><code>BackgroundGeolocation.onHeartbeat(async (event) =&gt; {\n  console.log(\"[onHeartbeat]\", event.location);\n\n  // Request a fresh location fix if needed.\n  const location = await BackgroundGeolocation.getCurrentPosition({\n    samples: 1\n  });\n  console.log(\"[onHeartbeat] fresh position:\", location);\n});\n</code></pre>"},{"location":"typescript/HeartbeatEvent/#members","title":"Members","text":""},{"location":"typescript/HeartbeatEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Most recent location recorded by the SDK.</p> <p>The heartbeat event does not actively request a fresh location fix. Call BackgroundGeolocation.getCurrentPosition if an up-to-date reading is required.</p>"},{"location":"typescript/HttpConfig/","title":"HttpConfig","text":"<pre><code>export interface HttpConfig {\n</code></pre> <p>HTTP and networking configuration for the background geolocation SDK.</p> <p><code>HttpConfig</code> controls how recorded locations are uploaded to your server \u2014 the endpoint, HTTP verb, headers, params, batching behaviour, and request timeouts.</p>"},{"location":"typescript/HttpConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Upload lifecycle</li> <li>Payload composition</li> <li>Sync strategy</li> <li>Error handling</li> <li>Remote control</li> <li>Logging</li> <li>Examples</li> </ul>"},{"location":"typescript/HttpConfig/#overview","title":"Overview","text":"<p>The SDK persistently stores each recorded location in an internal SQLite database before attempting to upload it. The HTTP service continuously consumes this queue in the background, surviving app termination and device reboot. When connectivity returns it resumes automatically.</p> Category Properties Notes Destination <code>url</code>, <code>method</code> <code>method</code> defaults to <code>POST</code>. Payload <code>rootProperty</code>, <code>params</code>, <code>headers</code> Controls JSON body and headers. Sync cadence <code>autoSync</code>, <code>autoSyncThreshold</code>, <code>batchSync</code>, <code>maxBatchSize</code> Immediate vs batched uploads. Network policy <code>disableAutoSyncOnCellular</code>, <code>timeout</code> Conserve bandwidth and battery. <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    params: { user_id: 1234 }\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#upload-lifecycle","title":"Upload lifecycle","text":"<p>Each location follows this path from recording to delivery:</p> <ol> <li>A location is recorded and immediately rendered to JSON \u2014 using the default schema or    PersistenceConfig.locationTemplate if configured \u2014 then written to SQLite.</li> <li>A record-level lock is acquired to prevent duplicate uploads.</li> <li>The stored JSON is read from SQLite and posted to <code>url</code>.</li> <li>A 2xx response marks the record as delivered \u2014 it is deleted from the database.</li> <li>On failure, the lock is released and the record is retried later.</li> </ol> <p>The SQLite buffer acts as a rolling queue and is normally empty. Records are removed when: - your server returns a 2xx (see HttpEvent) - BackgroundGeolocation.destroyLocations is called - TTL from PersistenceConfig.maxDaysToPersist expires - PersistenceConfig.maxRecordsToPersist is exceeded</p> <p>Inspect queue size with BackgroundGeolocation.getCount or fetch records with BackgroundGeolocation.getLocations.</p>"},{"location":"typescript/HttpConfig/#payload-composition","title":"Payload composition","text":"<ul> <li>Body: JSON. If <code>batchSync</code> is <code>true</code>, an array of records is sent.   If <code>rootProperty</code> is set, the payload becomes <code>{ \"&lt;rootProperty&gt;\": [...] }</code>.</li> <li>Headers: Merged from <code>headers</code> plus any authorization headers   injected by AuthorizationConfig.</li> <li>Params: Merged into every payload at the root level, or under   <code>rootProperty</code> if configured.</li> </ul> <p>Uploads use <code>application/json</code>. Authorization refresh requests use <code>application/x-www-form-urlencoded</code>.</p>"},{"location":"typescript/HttpConfig/#sync-strategy","title":"Sync strategy","text":"Option Behaviour <code>autoSync</code> Upload immediately after each recorded location. <code>autoSyncThreshold</code> Wait until N records are queued before uploading. <code>batchSync</code> Bundle all queued records into a single HTTP request. <code>maxBatchSize</code> Cap the number of records per batch request. <code>disableAutoSyncOnCellular</code> Defer uploads until Wi-Fi is available. <code>timeout</code> Abort requests exceeding this duration (ms)."},{"location":"typescript/HttpConfig/#error-handling","title":"Error handling","text":"<p>On a non-2xx response or network failure, records remain locked in the queue and are retried when: - new locations are recorded - the app resumes or the device reboots - BackgroundGeolocation.onConnectivityChange fires - BackgroundGeolocation.onHeartbeat fires - iOS background fetch runs</p> <p>To trigger a manual upload at any time:</p> <pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"typescript/HttpConfig/#remote-control","title":"Remote control","text":"<p>Your server can instruct the SDK to execute commands by including a <code>background_geolocation</code> key in any HTTP response body.</p> Command Arguments Effect <code>\"start\"</code> \u2014 BackgroundGeolocation.start <code>\"stop\"</code> \u2014 BackgroundGeolocation.stop <code>\"startGeofences\"</code> \u2014 BackgroundGeolocation.startGeofences <code>\"changePace\"</code> <code>boolean</code> BackgroundGeolocation.changePace <code>\"setConfig\"</code> <code>{Config}</code> BackgroundGeolocation.setConfig <code>\"addGeofence\"</code> <code>{Geofence}</code> BackgroundGeolocation.addGeofence <code>\"addGeofences\"</code> <code>[{Geofence}, ...]</code> BackgroundGeolocation.addGeofences <code>\"removeGeofence\"</code> <code>identifier: string</code> BackgroundGeolocation.removeGeofence <code>\"removeGeofences\"</code> list or none Remove some or all geofences. <code>\"uploadLog\"</code> <code>url: string</code> Upload the plugin log. <code>\"destroyLog\"</code> \u2014 Delete the plugin log."},{"location":"typescript/HttpConfig/#logging","title":"Logging","text":"<p>The SDK log provides a trace of the full HTTP lifecycle.</p>"},{"location":"typescript/HttpConfig/#the-sdk-log-provides-a-trace-of-the-full-http-lifecycle","title":"The SDK log provides a trace of the full HTTP lifecycle","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre> # Entry Meaning 1 Location Raw location recorded 2 INSERT Persisted to SQLite 3 Locked Marked for upload 4 HTTP POST/PUT Upload attempted 5 Response Server status code 6 DESTROY / UNLOCK Delivered / failed, queued for retry"},{"location":"typescript/HttpConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"typescript/HttpConfig/#manual-sync","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.sync();\n</code></pre>"},{"location":"typescript/HttpConfig/#http-lifecycle-log","title":"HTTP lifecycle log","text":"<pre><code>Location\nINSERT: record stored\nLocked 1 records\nHTTP POST\nResponse: 200\nDESTROY\n</code></pre>"},{"location":"typescript/HttpConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer ...\" }\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#simple-upload","title":"Simple upload","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    method: \"POST\",\n    autoSync: true,\n    headers: { Authorization: \"Bearer secret\" },\n    params: { device_id: \"abc-123\" }\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#batched-uploads","title":"Batched uploads","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations/bulk\",\n    batchSync: true,\n    maxBatchSize: 25,\n    autoSyncThreshold: 10,\n    rootProperty: \"locations\"\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#conserve-cellular-data","title":"Conserve cellular data","text":"<pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#manual-sync_1","title":"Manual sync","text":"<pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://api.example.com/locations\",\n    autoSync: true,\n    disableAutoSyncOnCellular: true\n  }\n});\n</code></pre> <pre><code>await BackgroundGeolocation.setConfig({\n  http: { url: \"https://api.example.com\", autoSync: false }\n});\nawait BackgroundGeolocation.sync();\n</code></pre>"},{"location":"typescript/HttpConfig/#members","title":"Members","text":""},{"location":"typescript/HttpConfig/#autosync","title":"autoSync","text":"<pre><code>autoSync?: boolean;\n</code></pre> <p>Uploads each recorded location to <code>url</code> immediately after it is recorded.</p> <p>Defaults to <code>true</code>. When a <code>url</code> is configured, the SDK attempts to upload each location as soon as it is written to SQLite. All recorded locations are persisted in the internal database until successfully delivered, regardless of this setting.</p> <p>When <code>autoSync</code> is <code>false</code>, call BackgroundGeolocation.sync to trigger uploads manually.</p> <p>Note</p> <p>With <code>autoSync: false</code>, the queue grows until you call BackgroundGeolocation.sync or until uploads succeed automatically on the next retry trigger.</p> <p>See also - <code>autoSyncThreshold</code> - <code>batchSync</code> - <code>maxBatchSize</code></p>"},{"location":"typescript/HttpConfig/#autosyncthreshold","title":"autoSyncThreshold","text":"<pre><code>autoSyncThreshold?: number;\n</code></pre> <p>Minimum number of queued records required before an automatic upload fires.</p> <p>Defaults to <code>0</code> (no threshold \u2014 upload after every recorded location).</p> <p>When set above <code>0</code>, the SDK waits until at least this many locations are queued before uploading. Combining <code>autoSyncThreshold</code> with <code>batchSync</code> significantly reduces battery consumption by minimizing the number of HTTP requests.</p> <p>Warning</p> <p><code>autoSyncThreshold</code> is ignored during BackgroundGeolocation.onMotionChange transitions. When the device enters the moving state, any queued locations are uploaded immediately. When it enters the stationary state, all remaining queued locations are flushed before the SDK goes idle.</p> <p>See also - <code>autoSync</code> - <code>batchSync</code></p>"},{"location":"typescript/HttpConfig/#batchsync","title":"batchSync","text":"<pre><code>batchSync?: boolean;\n</code></pre> <p>Bundles all queued locations into a single HTTP request.</p> <p>Defaults to <code>false</code>. When <code>true</code>, the SDK sends all records currently in the SQLite queue in one HTTP request rather than one request per location. Use <code>maxBatchSize</code> to cap the number of records per request.</p> <p>Batching is most effective when combined with <code>autoSyncThreshold</code> to reduce upload frequency and conserve power.</p> <p>See also - <code>autoSync</code> - <code>autoSyncThreshold</code> - <code>maxBatchSize</code></p>"},{"location":"typescript/HttpConfig/#disableautosynconcellular","title":"disableAutoSyncOnCellular","text":"<pre><code>disableAutoSyncOnCellular?: boolean;\n</code></pre> <p>Defers automatic uploads until the device is on Wi-Fi.</p> <p>Defaults to <code>false</code>. When <code>true</code>, <code>autoSync</code> uploads occur only when a Wi-Fi connection is active. Locations continue to be recorded and queued while on cellular \u2014 they are uploaded once Wi-Fi becomes available.</p> <p>Warning</p> <p>This setting is ignored when calling BackgroundGeolocation.sync manually. Manual syncs always proceed regardless of connection type.</p> <p>See also - <code>autoSync</code> - BackgroundGeolocation.sync</p>"},{"location":"typescript/HttpConfig/#headers","title":"headers","text":"<pre><code>headers?: Record&lt;string, string&gt;;\n</code></pre> <p>HTTP headers applied to every outbound upload request.</p> <p>The supplied headers are merged with the SDK's automatically applied headers (including <code>\"content-type\": \"application/json\"</code>). When AuthorizationConfig is configured, the SDK also injects an <code>Authorization</code> header automatically.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my.server.com\",\n    headers: {\n      Authorization: \"Bearer &lt;a secret key&gt;\",\n      \"X-FOO\": \"BAR\"\n    }\n  }\n});\n</code></pre> <p>Incoming request headers at your server:</p> <pre><code>POST /locations\n{\n  \"host\": \"tracker.transistorsoft.com\",\n  \"content-type\": \"application/json\",\n  \"content-length\": \"456\",\n  ...\n  \"authorization\": \"Bearer &lt;a secret key&gt;\",\n  \"X-FOO\": \"BAR\"\n}\n</code></pre> <p>See also - AuthorizationConfig</p>"},{"location":"typescript/HttpConfig/#maxbatchsize","title":"maxBatchSize","text":"<pre><code>maxBatchSize?: number;\n</code></pre> <p>Maximum number of records included in each batched HTTP request.</p> <p>Defaults to <code>-1</code> (no limit \u2014 all queued records in one request).</p> <p>When <code>batchSync</code> is <code>true</code> and the queue exceeds <code>maxBatchSize</code>, the SDK generates multiple requests until the queue is fully drained. This prevents excessively large request bodies after the device has been offline for an extended period.</p> <p>See also - <code>batchSync</code></p>"},{"location":"typescript/HttpConfig/#method","title":"method","text":"<p><code>method?:</code><code>HttpMethod</code><code>;</code></p> <p>The HTTP method used when uploading locations to <code>url</code>.</p> <p>Defaults to <code>POST</code>. Valid values: <code>POST</code>, <code>PUT</code>, <code>PATCH</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    method: \"PUT\"\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#params","title":"params","text":"<pre><code>params?: Record&lt;string, any&gt;;\n</code></pre> <p>Key/value pairs merged into the JSON body of every outbound upload request.</p> <p>Params are merged at the root level of the payload, or nested under <code>rootProperty</code> when that is configured.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      user_id: 1234,\n      device_id: \"abc123\"\n    }\n  }\n});\n</code></pre> <p>Request body received by your server:</p> <pre><code>{\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927,\n      \"longitude\": -73.61650\n    }\n  },\n  \"user_id\": 1234,\n  \"device_id\": \"abc123\"\n}\n</code></pre> <p>See also - <code>rootProperty</code></p>"},{"location":"typescript/HttpConfig/#rootproperty","title":"rootProperty","text":"<pre><code>rootProperty?: string;\n</code></pre> <p>Wraps the location payload under a named root key in the JSON body.</p> <p>Defaults to <code>\"location\"</code>. When set, outgoing payloads nest the serialized</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    rootProperty: \"myData\",\n    url: \"https://my.server.com\",\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"myData\": {\n    \"coords\": {\n      \"latitude\": 23.232323,\n      \"longitude\": 37.373737\n    }\n  }\n}\n</code></pre> <p>Set to <code>\".\"</code> to place the location data directly at the root of the JSON body:</p> <pre><code>{\n  \"coords\": {\n    \"latitude\": 23.232323,\n    \"longitude\": 37.373737\n  }\n}\n</code></pre> <p>See also - PersistenceConfig.locationTemplate - PersistenceConfig.geofenceTemplate</p>"},{"location":"typescript/HttpConfig/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>HTTP request timeout in milliseconds.</p> <p>Defaults to <code>60000</code> ms (60 seconds). When a request exceeds this duration, the SDK fires BackgroundGeolocation.onHttp with a failure event and the record is unlocked for retry.</p> <pre><code>BackgroundGeolocation.onHttp((response) =&gt; {\n  if (!response.success) {\n    console.log(\"[onHttp] FAILURE:\", response);\n  }\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    timeout: 3000\n  }\n});\n</code></pre>"},{"location":"typescript/HttpConfig/#url","title":"url","text":"<pre><code>url?: string;\n</code></pre> <p>The server URL where the SDK posts recorded locations.</p> <p>The SDK hosts a robust HTTP service that continuously uploads recorded locations to your server in the background, surviving app termination and device reboot.</p> <p>Warning</p> <p>Use the SDK's built-in HTTP service rather than posting locations from your own code. When AppConfig.stopOnTerminate is <code>false</code>, your app component terminates but the native background service continues recording and uploading. The SDK automatically retries on failure \u2014 ad-hoc HTTP requests from application code cannot provide the same reliability.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  console.log(\"[onHttp]\", event);\n});\n\nBackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: { user_id: 1234 },\n    headers: { Authorization: \"Basic my-secret-key\" },\n    autoSync: true,\n    method: \"POST\"\n  }\n});\n</code></pre>"},{"location":"typescript/HttpEvent/","title":"HttpEvent","text":"<pre><code>export interface HttpEvent {\n</code></pre> <p>HTTP response payload delivered to BackgroundGeolocation.onHttp.</p> <p>The SDK fires this event after every upload attempt \u2014 whether the request succeeded or failed. Use it to inspect the server response, handle errors, or trigger follow-up logic in your app.</p> <pre><code>BackgroundGeolocation.onHttp((event) =&gt; {\n  if (event.success) {\n    console.log(\"[onHttp] Upload succeeded:\", event.status);\n  } else {\n    console.warn(\"[onHttp] Upload failed:\", event.status, event.responseText);\n  }\n});\n</code></pre>"},{"location":"typescript/HttpEvent/#members","title":"Members","text":""},{"location":"typescript/HttpEvent/#responsetext","title":"responseText","text":"<pre><code>responseText: string;\n</code></pre> <p>Raw response body returned by the server.</p> <p>Parse this string to extract server-side data, error messages, or remote-control commands embedded in the response.</p>"},{"location":"typescript/HttpEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>HTTP status code returned by the server (e.g. <code>200</code>, <code>201</code>, <code>404</code>, <code>500</code>).</p>"},{"location":"typescript/HttpEvent/#success","title":"success","text":"<pre><code>success: boolean;\n</code></pre> <p><code>true</code> when the server returned a 2xx status code.</p>"},{"location":"typescript/HttpMethod/","title":"HttpMethod","text":"<pre><code>export const HttpMethod = {\n</code></pre> <p>Allowed HTTP methods for uploading data. Mirrors Flutter's HttpConfig.method and native support.</p>"},{"location":"typescript/HttpMethod/#members","title":"Members","text":""},{"location":"typescript/HttpMethod/#patch","title":"Patch","text":"<p>HTTP PATCH method.</p>"},{"location":"typescript/HttpMethod/#post","title":"Post","text":"<p>HTTP POST method.</p>"},{"location":"typescript/HttpMethod/#put","title":"Put","text":"<p>HTTP PUT method.</p>"},{"location":"typescript/KalmanProfile/","title":"KalmanProfile","text":"<pre><code>export const KalmanProfile = {\n</code></pre> <p>Specifies the preset tuning profile for the Kalman filter used in location denoising.</p> <p>Each profile adjusts the Kalman filter\u2019s process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behavior <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <ul> <li>If no profile is provided, <code>defaultProfile</code> is applied.</li> <li>Use <code>aggressive</code> for fast-changing activities requiring rapid updates.</li> <li>Use <code>conservative</code> when stability and track smoothness are more important   than immediate responsiveness.</li> </ul> <p>This maps directly to native Kalman tuning implementations on both Android and iOS.</p>"},{"location":"typescript/KalmanProfile/#members","title":"Members","text":""},{"location":"typescript/KalmanProfile/#aggressive","title":"Aggressive","text":"<p>Aggressive \u2014 Fast response; minimal smoothing.</p>"},{"location":"typescript/KalmanProfile/#conservative","title":"Conservative","text":"<p>Conservative \u2014 Maximum smoothing; slowest response to changes.</p>"},{"location":"typescript/KalmanProfile/#default","title":"Default","text":"<p>Balanced \u2014 General-purpose; suitable for most movement types.</p>"},{"location":"typescript/Location/","title":"Location","text":"<pre><code>export interface Location {\n</code></pre> <p>A location record captured by the device's native location API and delivered by the SDK.</p> <p>iOS uses <code>CLLocationManager</code> / <code>CLLocation</code>; Android uses <code>FusedLocationProviderClient</code> / <code>android.location.Location</code>.</p>"},{"location":"typescript/Location/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>JavaScript schema</li> <li>HTTP POST schema</li> </ul>"},{"location":"typescript/Location/#overview","title":"Overview","text":"Field Description <code>timestamp</code> ISO-8601 UTC timestamp from the native API. <code>uuid</code> Universally unique identifier for this record. <code>coords</code> Latitude, longitude, accuracy, speed, heading, altitude. <code>activity</code> Motion activity at time of recording. <code>battery</code> Battery level and charging state. <code>odometer</code> Accumulated distance traveled in meters. <code>event</code> SDK event that triggered this location. <code>is_moving</code> <code>true</code> when recorded in the moving state. <code>sample</code> <code>true</code> for intermediate accuracy-sampling locations. <code>extras</code> Optional custom metadata."},{"location":"typescript/Location/#javascript-schema","title":"JavaScript schema","text":"<pre><code>{\n   \"timestamp\":     [Date],     // &lt;-- Javascript Date instance\n   \"event\":         [String],   // &lt;-- motionchange|geofence|heartbeat\n   \"is_moving\":     [Boolean],  // &lt;-- The motion-state when location was recorded.\n   \"uuid\":          [String],   // &lt;-- Universally unique identifier\n   \"age\":           [Integer],  // &lt;-- Age of the location in milliseconds\n   \"coords\": {\n       \"latitude\":  [Double],\n       \"longitude\": [Double],\n       \"accuracy\":  [Double],\n       \"speed\":     [Double],\n       \"heading\":   [Double],\n       \"altitude\":  [Double]\n       \"ellipsoidal_altitude\":  [Double]\n   },\n   \"activity\": {\n       \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle],\n       \"confidence\": [0-100%]\n   },\n   \"battery\": {\n       \"level\": [Double],\n       \"is_charging\": [Boolean]\n   },\n   \"odometer\": [Double/meters]\n}\n</code></pre>"},{"location":"typescript/Location/#http-post-schema","title":"HTTP POST schema","text":"<pre><code>{\n    \"location\": {\n        \"coords\": {\n            \"latitude\":   [Double],\n            \"longitude\":  [Double],\n            \"accuracy\":   [Double],\n            \"speed\":      [Double],\n            \"heading\":    [Double],\n            \"altitude\":   [Double],\n            \"ellipsoidal_altitude\": [Double]\n        },\n        \"extras\": {   // &lt;-- optional meta-data\n            \"foo\": \"bar\"\n        },\n        \"activity\": {\n            \"type\": [still|on_foot|walking|running|in_vehicle|on_bicycle|unknown],\n            \"confidence\": [0-100%]\n        },\n        \"geofence\": {  // &lt;-- Present only if a geofence was triggered at this location\n            \"identifier\": [String],\n            \"action\": [String ENTER|EXIT]\n        },\n        \"battery\": {\n            \"level\": [Double],\n            \"is_charging\": [Boolean]\n        },\n        \"timestamp\": [ISO-8601 UTC], // eg:  \"2015-05-05T04:31:54.123Z\"\n        \"age\":       [Integer],      // &lt;-- Age of the location in milliseconds\n        \"uuid\":      [String],       // &lt;-- Universally unique identifier\n        \"event\"      [String],       // &lt;-- motionchange|geofence|heartbeat\n        \"is_moving\": [Boolean],      // &lt;-- The motion-state when recorded.\n        \"odometer\": [Double/meters]\n    }\n }\n</code></pre>"},{"location":"typescript/Location/#members","title":"Members","text":""},{"location":"typescript/Location/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivity</code><code>;</code></p> <p>Motion activity detected by the device at the time this location was recorded (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"typescript/Location/#age","title":"age","text":"<pre><code>age: number;\n</code></pre> <p>Age of the location in milliseconds, measured from the device system clock at the time the location was received.</p> <p><code>location.timestamp</code> + <code>location.age</code> = device system time when the SDK received the location from the native API.</p>"},{"location":"typescript/Location/#battery","title":"battery","text":"<p><code>battery:</code><code>Battery</code><code>;</code></p> <p>Device battery state at the time this location was recorded.</p>"},{"location":"typescript/Location/#coords","title":"coords","text":"<p><code>coords:</code><code>Coords</code><code>;</code></p> <p>Geographic coordinates: latitude, longitude, accuracy, speed, heading, and altitude.</p>"},{"location":"typescript/Location/#event","title":"event","text":"<pre><code>event?: string;\n</code></pre> <p>SDK event that triggered this location record.</p> <p>One of: <code>\"motionchange\"</code>, <code>\"providerchange\"</code>, <code>\"geofence\"</code>, <code>\"heartbeat\"</code>.</p>"},{"location":"typescript/Location/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary metadata attached to this location.</p> <p>Merged with configured PersistenceConfig.extras before persisting and included in the payload posted to HttpConfig.url.</p>"},{"location":"typescript/Location/#geofence","title":"geofence","text":"<p><code>geofence?:</code><code>GeofenceEvent</code><code>;</code></p> <p>The geofence event that triggered this location, if applicable.</p> <p>Present only when <code>event</code> is <code>\"geofence\"</code>.</p>"},{"location":"typescript/Location/#is_moving","title":"is_moving","text":"<pre><code>is_moving: boolean;\n</code></pre> <p><code>true</code> when the SDK was in the moving state when this location was recorded.</p>"},{"location":"typescript/Location/#mock","title":"mock","text":"<pre><code>mock?: boolean;\n</code></pre> <p><code>true</code> when the location was generated by a mock location app or simulator.</p>"},{"location":"typescript/Location/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled in meters since the last odometer reset.</p> <p>The SDK integrates the distance between each pair of accepted locations to maintain a running total. This value survives app restarts unless explicitly reset.</p>"},{"location":"typescript/Location/#how-its-calculated","title":"How it's calculated","text":"<ul> <li>Distance is computed between each consecutive pair of accepted locations.</li> <li>The LocationFilter evaluates accuracy and motion context for   each sample. Low-quality samples may be rejected or down-weighted   based on LocationFilter.odometerAccuracyThreshold.</li> <li>Accumulated drift is exposed via Location.odometer_error.</li> </ul>"},{"location":"typescript/Location/#when-it-increases","title":"When it increases","text":"<ul> <li>After the device moves and a new location is recorded.</li> <li>During both moving and stationary states when minor motion is detected.</li> <li>In geofences-only mode, at each geofence transition or stationary exit.</li> </ul>"},{"location":"typescript/Location/#when-it-does-not-increase","title":"When it does not increase","text":"<ul> <li>When a sample fails accuracy thresholds.</li> <li>When the device is stationary and no sufficient movement is detected.</li> </ul> <p>See also - LocationFilter - LocationFilter.odometerAccuracyThreshold - BackgroundGeolocation.resetOdometer - BackgroundGeolocation.getOdometer</p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Distance traveled:\", location.odometer);\n});\n\n// Reset at the start of a trip\nawait BackgroundGeolocation.resetOdometer();\n</code></pre>"},{"location":"typescript/Location/#odometer_error","title":"odometer_error","text":"<pre><code>odometer_error: number;\n</code></pre> <p>Accumulated odometer drift in meters.</p> <p>Represents the uncertainty that has built up in the <code>odometer</code> value due to GPS noise, tunnel blackouts, and other inaccurate samples.</p>"},{"location":"typescript/Location/#how-to-use-it","title":"How to use it","text":"<p>Treat this as a confidence interval for <code>odometer</code>. For example, if <code>odometer = 12000</code> and <code>odometer_error = 40</code>, the true traveled distance is likely within \u00b140 meters of the reported value.</p> <p>Resetting or setting a new <code>odometer</code> value automatically resets <code>odometer_error</code> to <code>0</code>. Values typically remain low (&lt; 10 m) under good GPS conditions but grow during long tunnels, dense urban canyons, or extended indoor tracking.</p> <p>See also - <code>odometer</code></p> <pre><code>BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"Odometer:\", location.odometer);\n  if (location.odometer_error &gt; 50) {\n    console.warn(\"High odometer drift \u2014 signal quality degraded\");\n  }\n});\n</code></pre>"},{"location":"typescript/Location/#provider","title":"provider","text":"<p><code>provider?:</code><code>ProviderChangeEvent</code><code>;</code></p> <p>Location-services provider state at the time this location was recorded.</p> <p>Present only when <code>event</code> is <code>\"providerchange\"</code>.</p>"},{"location":"typescript/Location/#sample","title":"sample","text":"<pre><code>sample?: boolean;\n</code></pre> <p><code>true</code> for intermediate sample locations collected during accuracy convergence.</p> <p>The SDK collects multiple samples when transitioning between motion states or during BackgroundGeolocation.getCurrentPosition to find the most accurate fix. These samples are delivered to BackgroundGeolocation.onLocation but are not persisted to SQLite. Filter them out before manually posting locations to your server.</p>"},{"location":"typescript/Location/#timestamp","title":"timestamp","text":"<pre><code>timestamp: string;\n</code></pre> <p>ISO-8601 UTC timestamp provided by the native location API.</p>"},{"location":"typescript/Location/#uuid","title":"uuid","text":"<pre><code>uuid: string;\n</code></pre> <p>Universally unique identifier for this location record.</p> <p>Use this to correlate locations in your server database with those in the SDK logs, or to detect whether a location has been delivered more than once.</p>"},{"location":"typescript/LocationError/","title":"LocationError","text":"<pre><code>export const LocationError = {\n</code></pre> <p>Error codes returned when the native location API fails to fetch a location.</p> Code Error <code>0</code> Location unknown <code>1</code> Location permission denied <code>2</code> Network error <code>3</code> Attempt to initiate location-services in background with WhenInUse authorization <code>408</code> Location timeout <code>499</code> Location request cancelled"},{"location":"typescript/LocationError/#members","title":"Members","text":""},{"location":"typescript/LocationError/#backgroundwheninuse","title":"BackgroundWhenInUse","text":"<pre><code>BackgroundWhenInUse: 3,\n</code></pre> <p>Attempt to initiate location-services in background with WhenInUse authorization.</p>"},{"location":"typescript/LocationError/#cancelled","title":"Cancelled","text":"<pre><code>Cancelled: 499,\n</code></pre> <p>Location request was cancelled.</p>"},{"location":"typescript/LocationError/#locationunknown","title":"LocationUnknown","text":"<pre><code>LocationUnknown: 0,\n</code></pre> <p>Location could not be determined at this time.</p>"},{"location":"typescript/LocationError/#networkerror","title":"NetworkError","text":"<pre><code>NetworkError: 2,\n</code></pre> <p>Network error occurred while attempting to fetch location.</p>"},{"location":"typescript/LocationError/#permissiondenied","title":"PermissionDenied","text":"<pre><code>PermissionDenied: 1,\n</code></pre> <p>Location permission denied by the user.</p>"},{"location":"typescript/LocationError/#timeout","title":"Timeout","text":"<pre><code>Timeout: 408,\n</code></pre> <p>Location request timed out.</p>"},{"location":"typescript/LocationFilter/","title":"LocationFilter","text":"<pre><code>export interface LocationFilter {\n</code></pre> <p>Filters, denoises, and smooths raw GPS samples before they are recorded or used for odometer calculations.</p> <p><code>LocationFilter</code> is supplied via GeoConfig.filter and provides fine-grained control over how the SDK handles noisy or inconsistent location data from the underlying platform.</p>"},{"location":"typescript/LocationFilter/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Examples</li> </ul>"},{"location":"typescript/LocationFilter/#overview","title":"Overview","text":"<p>The SDK applies several processing stages to each raw GPS sample before recording it: Kalman filtering, rolling-window burst averaging, and speed/distance/accuracy constraints. These produce smoother paths, reduce jitter, and improve odometer stability.</p> <p></p> Field Description <code>policy</code> Selects which filtering policy to apply. See LocationFilterPolicy. <code>useKalman</code> Enables Kalman filtering of speed and position (default: <code>true</code>). <code>kalmanDebug</code> Enables verbose Kalman diagnostic logs (default: <code>false</code>). <code>kalmanProfile</code> Selects a Kalman tuning profile (see KalmanProfile). <code>rollingWindow</code> Number of samples for rolling burst averaging. Larger values increase smoothness but reduce responsiveness. Default: <code>5</code>. <code>burstWindow</code> Duration of each averaging burst (seconds). Default: <code>10</code>. <code>maxBurstDistance</code> Maximum distance (meters) for samples included in the same burst window. Default: <code>300</code>. <code>trackingAccuracyThreshold</code> Maximum GPS horizontal accuracy (meters) required to accept a location. Default: <code>100</code>. <code>maxImpliedSpeed</code> Maximum implied speed (m/s) before rejecting a sample as unrealistic. Default: <code>60</code> (\u2248 216 km/h). <code>filterDebug</code> Enables verbose logging of filter decisions (default: <code>false</code>). <code>odometerUseKalmanFilter</code> Applies Kalman smoothing to odometer calculations (default: <code>true</code>). <code>odometerAccuracyThreshold</code> Maximum accuracy (meters) allowed for a sample to affect the odometer. Default: <code>20</code>. <ul> <li>Distances are in meters.</li> <li>Time fields are in seconds unless noted otherwise.</li> <li>Filtering affects recorded locations only \u2014 it does not influence real-time motion detection.</li> </ul> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust,\n      useKalman: true,\n      kalmanProfile: KalmanProfile.Default,\n      trackingAccuracyThreshold: 100,\n      odometerAccuracyThreshold: 20\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough,\n      useKalman: false\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#members","title":"Members","text":""},{"location":"typescript/LocationFilter/#burstwindow","title":"burstWindow","text":"<pre><code>burstWindow?: number;\n</code></pre> <p>Duration in seconds of each burst window used for averaging samples. Defaults to <code>10</code> seconds. Valid range: 0.2\u2013120.</p> <p>The SDK groups all locations received within this time window into a single averaged output sample.</p>"},{"location":"typescript/LocationFilter/#filterdebug","title":"filterDebug","text":"<pre><code>filterDebug?: boolean;\n</code></pre> <p>Enables verbose debug logging for the filtering engine. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs detailed decisions for each incoming sample, including whether it was ACCEPTED, REJECTED, or IGNORED by the filtering pipeline.</p>"},{"location":"typescript/LocationFilter/#kalmandebug","title":"kalmanDebug","text":"<pre><code>kalmanDebug?: boolean;\n</code></pre> <p>Enables verbose Kalman debug output in logs. Defaults to <code>false</code>.</p> <p>When enabled, the SDK logs additional diagnostic data about the Kalman filter's internal state (for example, variance and innovation) for each incoming sample.</p>"},{"location":"typescript/LocationFilter/#kalmanprofile","title":"kalmanProfile","text":"<p><code>kalmanProfile?:</code><code>KalmanProfile</code><code>;</code></p> <p>Selects the preset tuning profile for the Kalman filter. Defaults to <code>KalmanProfile.Default</code>.</p> <p>Each profile adjusts the Kalman filter's process and measurement noise parameters, trading off between responsiveness and smoothness:</p> Profile Behaviour <code>defaultProfile</code> Balanced \u2014 General-purpose; suitable for most movement types. <code>aggressive</code> Aggressive \u2014 Fast response; minimal smoothing. <code>conservative</code> Conservative \u2014 Maximum smoothing; slowest response to changes. <p>Use <code>aggressive</code> for fast-changing activities requiring rapid updates. Use <code>conservative</code> when stability and track smoothness are more important than immediate responsiveness.</p>"},{"location":"typescript/LocationFilter/#maxburstdistance","title":"maxBurstDistance","text":"<pre><code>maxBurstDistance?: number;\n</code></pre> <p>Maximum distance in meters between samples considered part of the same burst. Defaults to <code>300</code> meters. Valid range: 5\u20132000.</p> <p>Prevents the filter from merging location samples that are too far apart into a single averaged point. Works alongside <code>maxImpliedSpeed</code> \u2014 whichever flags a sample first applies.</p>"},{"location":"typescript/LocationFilter/#maximpliedspeed","title":"maxImpliedSpeed","text":"<pre><code>maxImpliedSpeed?: number;\n</code></pre> <p>Maximum implied speed in meters/second permitted before a location sample is rejected. Defaults to <code>60</code> m/s (\u2248 216 km/h). Valid range: 1\u2013200.</p> <p>If a raw sample implies a speed greater than this threshold \u2014 calculated as distance divided by elapsed time between samples \u2014 it is treated as an unrealistic outlier and discarded by the filter.</p>"},{"location":"typescript/LocationFilter/#odometeraccuracythreshold","title":"odometerAccuracyThreshold","text":"<pre><code>odometerAccuracyThreshold?: number;\n</code></pre> <p>Maximum horizontal accuracy in meters allowed for a sample to affect the odometer. Defaults to <code>20</code> meters. Valid range: 0\u2013500.</p> <p>Any location whose accuracy exceeds this threshold is ignored for odometer updates.</p>"},{"location":"typescript/LocationFilter/#odometerusekalmanfilter","title":"odometerUseKalmanFilter","text":"<pre><code>odometerUseKalmanFilter?: boolean;\n</code></pre> <p>Applies a Kalman filter to odometer calculations, independent of LocationFilter.useKalman. Defaults to <code>true</code>.</p> <p>When enabled, the odometer's internal distance-accumulation logic applies a Kalman filter to smooth incoming samples, reducing jitter and noise without modifying the recorded track points.</p> <p>This is useful when smoother odometer readings are needed while preserving the raw location stream for mapping, analysis, or debugging.</p> <p>If both LocationFilter.useKalman and LocationFilter.odometerUseKalmanFilter are <code>true</code>, the SDK maintains independent Kalman filter instances for tracking and odometer calculations.</p>"},{"location":"typescript/LocationFilter/#policy","title":"policy","text":"<p><code>policy?:</code><code>LocationFilterPolicy</code><code>;</code></p> <p>Selects the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected. Defaults to <code>LocationFilterPolicy.Conservative</code>.</p> <p>The filtering policy determines how aggressively the SDK removes noisy, inaccurate, or redundant location updates. It is the first stage in the data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> Policy Description Use case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and caps only clearly invalid samples. Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. (Default) Analytics, long-term background logging, noise-sensitive applications. <p>Choosing the correct policy depends on your app's tolerance for jitter versus responsiveness: - Fitness or vehicle-tracking apps often prefer <code>Adjust</code> for lower latency. - Survey or analytics apps may prefer <code>Conservative</code> for maximum stability,   or <code>PassThrough</code> to capture unmodified raw samples.</p> <p>Note</p> <p>This policy affects only the SDK's internal filtering pipeline. It does not modify the raw values returned to BackgroundGeolocation.onLocation. For more granular tuning, see LocationFilter.trackingAccuracyThreshold, LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</p> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics:","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilter/#rollingwindow","title":"rollingWindow","text":"<pre><code>rollingWindow?: number;\n</code></pre> <p>Number of samples in the rolling window used for burst averaging. Defaults to <code>5</code>. Valid range: 3\u201320.</p> <p>Higher values produce smoother averaged locations but introduce greater latency in responsiveness to movement.</p>"},{"location":"typescript/LocationFilter/#trackingaccuracythreshold","title":"trackingAccuracyThreshold","text":"<pre><code>trackingAccuracyThreshold?: number;\n</code></pre> <p>Maximum acceptable horizontal accuracy in meters for a sample to qualify for tracking. Defaults to <code>100</code> meters. Valid range: 0\u2013500.</p> <p>Locations with an accuracy value greater (worse) than this threshold are discarded and not used in path or odometer calculations.</p>"},{"location":"typescript/LocationFilter/#usekalman","title":"useKalman","text":"<pre><code>useKalman?: boolean;\n</code></pre> <p>Enables the Kalman filter to smooth incoming location speed and position. Defaults to <code>true</code>.</p> <p>When enabled, the SDK applies a Kalman filter to reduce noise and stabilize velocity and distance calculations.</p>"},{"location":"typescript/LocationFilterPolicy/","title":"LocationFilterPolicy","text":"<pre><code>export const LocationFilterPolicy = {\n</code></pre> <p>Defines the filtering policy applied to incoming raw GPS samples before they are accepted, averaged, or rejected by the LocationFilter.</p> <p>The filtering policy determines how aggressively the plugin removes noisy, inaccurate, or redundant location updates. It represents the first stage in the SDK\u2019s data-quality pipeline \u2014 before Kalman smoothing, burst averaging, or other denoising steps are applied.</p> <p>Choosing the correct policy depends on your app\u2019s tolerance for jitter versus responsiveness:</p> <ul> <li>Fitness or vehicle-tracking apps often prefer stronger filtering.</li> <li>Survey or scientific apps may prefer capturing unmodified raw samples.</li> </ul> <p>Profiles</p> Policy Description Use Case LocationFilterPolicy.PassThrough No filtering. Every received sample is recorded, even if noisy or identical to the previous one. Debugging, diagnostics, scenarios requiring raw data. LocationFilterPolicy.Adjust Balanced filtering. Smooths and rejects only clearly invalid samples. (Default) Most use cases \u2014 walking, cycling, automotive tracking. LocationFilterPolicy.Conservative Strict filtering. Strongly smooths data and rejects high-variance samples, prioritizing stability over responsiveness. Analytics, long-term background logging, noise-sensitive applications. <p>Notes</p> <ul> <li>This policy affects only the SDK\u2019s internal filtering pipeline.   It does not modify the raw values returned to BackgroundGeolocation.onLocation.</li> <li>For more granular tuning, see LocationFilter.trackingAccuracyThreshold,   LocationFilter.maxImpliedSpeed, and other LocationFilter fields.</li> </ul> <p>Examples</p> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre> <p>See also - GeoConfig.filter - LocationFilter - KalmanProfile</p>"},{"location":"typescript/LocationFilterPolicy/#balanced-default-filtering","title":"Balanced default filtering","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Adjust\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilterPolicy/#no-filtering-capture-all-raw-locations","title":"No filtering \u2014 capture all raw locations","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.PassThrough\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilterPolicy/#maximum-smoothing-for-analytics","title":"Maximum smoothing for analytics","text":"<pre><code>BackgroundGeolocation.ready({\n  geolocation: {\n    filter: {\n      policy: LocationFilterPolicy.Conservative\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/LocationFilterPolicy/#members","title":"Members","text":""},{"location":"typescript/LocationFilterPolicy/#adjust","title":"Adjust","text":"<p>Balanced (default) \u2014 applies moderate filtering to reject noisy samples.</p> <p>Dynamically adjusts acceptance thresholds for incoming samples, but never alters the raw latitude/longitude coordinates.</p> <p>When using LocationFilterPolicy.Adjust (the default), the SDK computes motion metrics such as:</p> <ul> <li>distance deltas</li> <li>implied speed</li> <li>accuracy variance</li> <li>heading stability</li> </ul> <p>It then applies adaptive gating rules to decide whether each sample should be:</p> <ul> <li>accepted</li> <li>ignored</li> <li>rejected as noise</li> </ul> <p>Coordinates are never modified. This policy adjusts which samples are included, not how they are positioned.</p> <p>If LocationFilter.useKalman is enabled, additional smoothing may occur, but the physical coordinates of each accepted sample remain untouched.</p>"},{"location":"typescript/LocationFilterPolicy/#conservative","title":"Conservative","text":"<p>Aggressive \u2014 filters heavily, preferring stability over responsiveness.</p>"},{"location":"typescript/LocationFilterPolicy/#passthrough","title":"PassThrough","text":"<p>No filtering \u2014 accept all samples. Useful for debugging.</p>"},{"location":"typescript/LocationRequest/","title":"LocationRequest","text":"<pre><code>export const LocationRequest = {\n</code></pre> <p>Indicates what level of location authorization the SDK should request.</p> Name Value Description Always <code>\"Always\"</code> Request full background + foreground authorization. WhenInUse <code>\"WhenInUse\"</code> Request foreground-only authorization. Any <code>\"Any\"</code> Accept either Always or WhenInUse (no specific request). <p>Mirrors native iOS authorization request options and existing RN adapter keys.</p> <p>See GeoConfig.locationAuthorizationRequest</p>"},{"location":"typescript/LocationRequest/#members","title":"Members","text":""},{"location":"typescript/LocationRequest/#always","title":"Always","text":"<p>Request full background + foreground authorization.</p>"},{"location":"typescript/LocationRequest/#any","title":"Any","text":"<p>Accept either Always or WhenInUse (no specific request).</p>"},{"location":"typescript/LocationRequest/#wheninuse","title":"WhenInUse","text":"<p>Request foreground-only authorization.</p>"},{"location":"typescript/LogLevel/","title":"LogLevel","text":"<pre><code>export const LogLevel = {\n</code></pre> <p>Controls the verbosity of plugin logging.</p> Level Value Description Off 0 Disable all logging. Error 1 Log only critical failures. Warning 2 Log warnings + errors. Info 3 Operational information. Debug 4 Developer-level debug output. Verbose 5 Maximum detail. <p>Mirrors native logging constants on iOS &amp; Android.</p>"},{"location":"typescript/LogLevel/#members","title":"Members","text":""},{"location":"typescript/LogLevel/#debug","title":"Debug","text":"<p>Developer-level debug output.</p>"},{"location":"typescript/LogLevel/#error","title":"Error","text":"<p>Log only critical failures.</p>"},{"location":"typescript/LogLevel/#info","title":"Info","text":"<p>Operational information.</p>"},{"location":"typescript/LogLevel/#off","title":"Off","text":"<p>Disable all logging.</p>"},{"location":"typescript/LogLevel/#verbose","title":"Verbose","text":"<p>Maximum detail.</p>"},{"location":"typescript/LogLevel/#warning","title":"Warning","text":"<p>Log warnings + errors.</p>"},{"location":"typescript/Logger/","title":"Logger","text":"<pre><code>export interface Logger {\n</code></pre> <p>SDK logging API \u2014 access via BackgroundGeolocation.logger.</p> <p>The SDK writes structured log entries to an internal SQLite database for up to LoggerConfig.logMaxDays days (default <code>3</code>). Log volume is controlled by LoggerConfig.logLevel (default <code>LogLevel.Off</code>). Logs can be fetched as a string, emailed from the device, or uploaded to a URL.</p>"},{"location":"typescript/Logger/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Retrieving logs</li> <li>Writing log entries</li> <li>Examples</li> </ul>"},{"location":"typescript/Logger/#overview","title":"Overview","text":"Method Description <code>getLog</code> Fetch all log entries as a string. <code>emailLog</code> Send logs via the device mail client. <code>uploadLog</code> Upload logs to a URL as a gzipped multipart file. <code>destroyLog</code> Clear the log database. <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code>, <code>notice</code> Write custom log entries. <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\n</code></pre>"},{"location":"typescript/Logger/#retrieving-logs","title":"Retrieving logs","text":"<p>All three retrieval methods accept an optional SQLQuery to constrain results by date range, sort order, and record count. Without a query, all records up to LoggerConfig.logMaxDays days old are returned.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre> <p>Sample log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre>"},{"location":"typescript/Logger/#writing-log-entries","title":"Writing log entries","text":"<p>Insert custom messages into the SDK's log database at any severity level. Custom entries appear inline with SDK entries, making it easy to correlate your app's actions with location events.</p> Method Level Icon <code>error</code> <code>ERROR</code> \u2757\ufe0f <code>warn</code> <code>WARNING</code> \u26a0\ufe0f <code>debug</code> <code>DEBUG</code> \ud83d\udc1e <code>info</code> <code>INFO</code> \u2139\ufe0f <code>notice</code> <code>INFO</code> \ud83d\udd35 <pre><code>const Logger = BackgroundGeolocation.logger;\nBackgroundGeolocation.onLocation((location) =&gt; {\n  Logger.debug(\"Location received: \" + location.uuid);\n});\n</code></pre>"},{"location":"typescript/Logger/#examples","title":"Examples","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"typescript/Logger/#fetch-and-display-the-full-log","title":"Fetch and display the full log","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog();\nconsole.log(\"[log]\", log);\n</code></pre>"},{"location":"typescript/Logger/#upload-log-to-your-server","title":"Upload log to your server","text":"<pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n</code></pre>"},{"location":"typescript/Logger/#email-log-with-a-date-range","title":"Email log with a date range","text":"<pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"support@example.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC\n});\n</code></pre>"},{"location":"typescript/Logger/#methods","title":"Methods","text":""},{"location":"typescript/Logger/#debug","title":"debug","text":"<pre><code>debug(message: string): void;\n</code></pre> <p>Insert a debug-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.debug(\"This is a debug message\");\n</code></pre> <pre><code>D TSLocationManager: [c.t.l.logger.TSLog log] This is a debug message\n</code></pre>"},{"location":"typescript/Logger/#destroylog","title":"destroyLog","text":"<pre><code>destroyLog(): Promise&lt;void&gt;;\n</code></pre> <p>Delete all records from the SDK's log database.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.destroyLog();\n</code></pre>"},{"location":"typescript/Logger/#emaillog","title":"emailLog","text":"<p><code>emailLog(email: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Send the SDK's log database to an email address via the device mail client.</p> <p>Provide an optional SQLQuery to constrain which records are included.</p> <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>const Logger = BackgroundGeolocation.logger;\nawait Logger.emailLog(\"foo@bar.com\");\n\n// Constrain by date range\nawait Logger.emailLog(\"foo@bar.com\", {\n  start: Date.parse(\"2019-09-19\"),\n  end:   Date.parse(\"2019-09-20\"),\n  order: Logger.ORDER_ASC,\n  limit: 1000\n});\n</code></pre>"},{"location":"typescript/Logger/#error","title":"error","text":"<pre><code>error(message: string): void;\n</code></pre> <p>Insert an error-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.error(\"Something BAD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u203c Something BAD\n</code></pre>"},{"location":"typescript/Logger/#getlog","title":"getLog","text":"<p><code>getLog(query?:</code><code>SQLQuery</code><code>): Promise&lt;string&gt;;</code></p> <p>Fetch the SDK's log database as a string.</p> <p>Provide an optional SQLQuery to constrain results by date range, sort order, and record count. Depending on LoggerConfig.logLevel, the result may be several megabytes.</p> <p>See also - LoggerConfig.logMaxDays - LoggerConfig.logLevel - <code>emailLog</code> - <code>uploadLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>// Fetch all logs\nconst log = await BackgroundGeolocation.logger.getLog();\nconsole.log(\"[log]\", log);\n\n// Constrain by date range\nconst Logger = BackgroundGeolocation.logger;\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-09-19 11:12\"),\n  end:   Date.parse(\"2019-09-19 11:13\"),\n  order: Logger.ORDER_ASC,\n  limit: 100\n});\n</code></pre>"},{"location":"typescript/Logger/#info","title":"info","text":"<pre><code>info(message: string): void;\n</code></pre> <p>Insert an info-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.info(\"Something informative\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \u2139\ufe0f  Something informative\n</code></pre>"},{"location":"typescript/Logger/#notice","title":"notice","text":"<pre><code>notice(message: string): void;\n</code></pre> <p>Insert a notice-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.notice(\"A Notice\");\n</code></pre> <pre><code>I TSLocationManager: [c.t.l.logger.TSLog log]\nI TSLocationManager:   \ud83d\udd35  A Notice\n</code></pre>"},{"location":"typescript/Logger/#uploadlog","title":"uploadLog","text":"<p><code>uploadLog(url: string, query?:</code><code>SQLQuery</code><code>): Promise&lt;boolean | void&gt;;</code></p> <p>Upload the SDK's log database to a URL as a gzipped multipart file.</p> <p>Provide an optional SQLQuery to constrain which records are included. The upload includes your configured HttpConfig.headers for authentication.</p>"},{"location":"typescript/Logger/#multipart-upload","title":"Multipart upload","text":"<p>The log is posted as a gzipped multipart file \u2014 the same file produced by <code>emailLog</code>. The request body also includes a form with the following fields:</p> Key Value <code>state</code> JSON-encoded result of <code>getState</code> <code>model</code> Device model <code>manufacturer</code> Device manufacturer <code>platform</code> <code>iOS</code> or <code>Android</code> <code>version</code> OS version <p>See also - LoggerConfig.logLevel - <code>getLog</code> - <code>emailLog</code> - <code>destroyLog</code> - \ud83d\udcd8Debugging Guide</p> <pre><code>await BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\"\n);\n\n// Constrain by date range\nawait BackgroundGeolocation.logger.uploadLog(\n  \"https://my.server.com/users/123/logs\",\n  { start: Date.parse(\"2019-10-20 09:00\"), end: Date.parse(\"2019-10-20 11:59\") }\n);\n</code></pre>"},{"location":"typescript/Logger/#warn","title":"warn","text":"<pre><code>warn(message: string): void;\n</code></pre> <p>Insert a warning-level message into the SDK's log database.</p> <pre><code>BackgroundGeolocation.logger.warn(\"Something WEIRD\");\n</code></pre> <pre><code>E TSLocationManager: [c.t.l.logger.TSLog log]\nE TSLocationManager: \u26a0\ufe0f  Something WEIRD\n</code></pre>"},{"location":"typescript/Logger/#properties","title":"Properties","text":""},{"location":"typescript/Logger/#order_asc","title":"ORDER_ASC","text":"<pre><code>ORDER_ASC: 1;\n</code></pre> <p>Sort-order constant for ascending log queries (oldest first).</p>"},{"location":"typescript/Logger/#order_desc","title":"ORDER_DESC","text":"<pre><code>ORDER_DESC: -1;\n</code></pre> <p>Sort-order constant for descending log queries (newest first).</p>"},{"location":"typescript/LoggerConfig/","title":"LoggerConfig","text":"<pre><code>export interface LoggerConfig {\n</code></pre> <p>Logging and diagnostics configuration for the background geolocation SDK.</p> <p><code>LoggerConfig</code> controls how much the SDK writes to its internal log, how long entries are retained on device, and whether developer debug aids such as audible soundFX are active.</p>"},{"location":"typescript/LoggerConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Log levels</li> <li>Debug mode</li> <li>Retention</li> <li>Diagnostics</li> <li>Examples</li> </ul>"},{"location":"typescript/LoggerConfig/#overview","title":"Overview","text":"<p>The SDK maintains a persistent, SQLite-backed log that survives app restarts. Logging serves two purposes:</p> <ol> <li> <p>Development &amp; QA \u2014 High-verbosity logs and optional audible soundFX    make it easy to follow SDK behaviour without watching a console. You can    hear when locations are recorded, when motion transitions occur, and when    geofences fire.</p> </li> <li> <p>Production diagnostics \u2014 Lower verbosity preserves essential    operational traces without excessive storage use or privacy impact.</p> </li> </ol> Category Properties Notes Verbosity <code>logLevel</code> <code>Off</code> \u2192 <code>Verbose</code> Debug aids <code>debug</code> Audible soundFX and debug notifications Retention <code>logMaxDays</code> Rolling on-device TTL <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#log-levels","title":"Log levels","text":"<p>Choose the level appropriate for your environment:</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Use <code>Verbose</code> during active development. Use <code>Info</code> or <code>Warning</code> in production. Avoid <code>Off</code> unless required \u2014 logs are invaluable for field diagnostics.</p>"},{"location":"typescript/LoggerConfig/#debug-mode","title":"Debug mode","text":"<p>When <code>debug</code> is <code>true</code>, the SDK plays short, distinct soundFX as key events occur \u2014 location recorded, motion change, geofence enter/exit/dwell, HTTP upload \u2014 and may show transient developer notifications on Android to visualize state transitions.</p> <p>Warning</p> <p>Never enable <code>debug</code> in a production build. Disable it before submitting your app to the App Store or Google Play.</p>"},{"location":"typescript/LoggerConfig/#retention","title":"Retention","text":"<p><code>logMaxDays</code> controls how long log entries remain on device. Entries older than the configured limit are purged automatically on a rolling basis.</p> <p>Recommended values: - Development: 5\u20137 days - Production: 1\u20133 days</p>"},{"location":"typescript/LoggerConfig/#diagnostics","title":"Diagnostics","text":"<p>Logs are your first resource when something unexpected happens. The SDK writes a detailed trace of every lifecycle event \u2014 location recording, motion transitions, HTTP uploads, geofence activity \u2014 to an internal SQLite database. When behaviour is unclear, set <code>logLevel</code> to LogLevel.Verbose and fetch the log before doing anything else:</p> <pre><code>await BackgroundGeolocation.setConfig({\n  logger: { logLevel: BackgroundGeolocation.LogLevel.Verbose }\n});\n</code></pre> <p>Use Logger.emailLog to send <code>logMaxDays</code> worth of logs as an email attachment directly from the device:</p> <pre><code>await BackgroundGeolocation.logger.emailLog(\"you@example.com\");\n</code></pre>"},{"location":"typescript/LoggerConfig/#android","title":"Android","text":"<p>Stream live SDK output directly to your terminal with <code>adb</code>:</p> <pre><code>adb logcat \"*:S\" TSLocationManager:V\n</code></pre>"},{"location":"typescript/LoggerConfig/#ios","title":"iOS","text":"<p>Run the app from Xcode to stream SDK output to the console in real time. For issues that only reproduce in the background, attach the device and monitor the Xcode console while the app runs in the background.</p>"},{"location":"typescript/LoggerConfig/#examples","title":"Examples","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#development-profile","title":"Development profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: BackgroundGeolocation.LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#production-profile","title":"Production profile","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: BackgroundGeolocation.LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#disable-all-logging","title":"Disable all logging","text":"<pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#development-profile-maximum-visibility","title":"Development profile (maximum visibility)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 7,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#production-profile-quiet-conservative","title":"Production profile (quiet &amp; conservative)","text":"<pre><code>BackgroundGeolocation.ready({\n  logger: {\n    debug: false,\n    logLevel: LogLevel.Info,\n    logMaxDays: 3,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#new-compound-config","title":"New compound Config","text":"<pre><code>Config({\n  logger: {\n    debug: true,\n    logLevel: LogLevel.Verbose,\n    logMaxDays: 3,\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.setConfig({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Off,\n  }\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#members","title":"Members","text":""},{"location":"typescript/LoggerConfig/#debug","title":"debug","text":"<pre><code>debug?: boolean;\n</code></pre> <p>Enables audible soundFX and debug notifications during development.</p> <p>When <code>true</code>, the SDK plays a distinct sound for each key lifecycle event so you can follow SDK activity without watching the console. It may also display transient notifications on Android to visualize background state transitions.</p> <p>Defaults to <code>false</code>.</p>"},{"location":"typescript/LoggerConfig/#ios_1","title":"iOS","text":"<p>To hear debug sounds while the app is in the background, enable the Audio and AirPlay background mode in Xcode under Signing &amp; Capabilities \u2192 Background Modes.</p>"},{"location":"typescript/LoggerConfig/#debug-sound-events","title":"Debug sound events","text":"Event iOS Android <code>LOCATION_RECORDED</code> <code>LOCATION_SAMPLE</code> <code>LOCATION_ERROR</code> <code>LOCATION_SERVICES_ON</code> n/a <code>LOCATION_SERVICES_OFF</code> n/a <code>STATIONARY_GEOFENCE_EXIT</code> <code>MOTIONCHANGE_FALSE</code> <code>MOTIONCHANGE_TRUE</code> <code>MOTION_TRIGGER_DELAY_START</code> n/a <code>MOTION_TRIGGER_DELAY_CANCEL</code> n/a <code>STOP_DETECTION_DELAY_INITIATED</code> n/a <code>STOP_TIMER_ON</code> <code>STOP_TIMER_OFF</code> <code>HEARTBEAT</code> <code>GEOFENCE_ENTER</code> <code>GEOFENCE_EXIT</code> <code>GEOFENCE_DWELL_START</code> n/a <code>GEOFENCE_DWELL_CANCEL</code> n/a <code>GEOFENCE_DWELL</code> <code>GEOFENCE_ENTER</code> after <code>GEOFENCE_DWELL_START</code> <code>ERROR</code> <code>WARNING</code> n/a <code>BACKGROUND_FETCH</code> n/a <p>Warning</p> <p>Never enable <code>debug</code> in a production build.</p>"},{"location":"typescript/LoggerConfig/#loglevel","title":"logLevel","text":"<p><code>logLevel?:</code><code>LogLevel</code><code>;</code></p> <p>Controls the verbosity of the SDK's internal log.</p> <p>Defaults to LogLevel.Off. The log is stored in an internal SQLite database and retained for <code>logMaxDays</code> days (default: <code>3</code>).</p> Level Value Description LogLevel.Off 0 Disable logging entirely LogLevel.Error 1 Failures and critical errors LogLevel.Warning 2 Problems that may affect behaviour LogLevel.Info 3 Operational milestones (start/stop, HTTP, geofence state) LogLevel.Debug 4 Granular detail during integration LogLevel.Verbose 5 Maximum detail; full introspection <p>Example log output:</p> <pre><code>09-19 11:12:18.716 \u2554\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.716 \u2551 BackgroundGeolocation Service started\n09-19 11:12:18.716 \u2560\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n09-19 11:12:18.723   \u2705  Started in foreground\n09-19 11:12:18.737   \ud83c\udfbe  Start activity updates: 10000\n09-19 11:12:18.778   \ud83d\udd35  setPace: null \u2192 false\n09-19 11:12:21.405   \u2705  INSERT: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:21.446   \u2705  Locked 1 records\n09-19 11:12:21.454   \ud83d\udd35  HTTP POST: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n09-19 11:12:22.083   \ud83d\udd35  Response: 200\n09-19 11:12:22.100   \u2705  DESTROY: bca5acc8-e358-4d8f-827f-b8c0d556b7bb\n</code></pre> <p>Warning</p> <p>Set <code>logLevel</code> to LogLevel.Error or lower before submitting to production. At <code>Verbose</code>, logs can grow to several megabytes over <code>logMaxDays</code> days.</p> <p>See also - <code>logMaxDays</code> - Logger.getLog - Logger.emailLog - Logger.destroyLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logLevel: BackgroundGeolocation.LogLevel.Verbose\n  },\n});\n</code></pre>"},{"location":"typescript/LoggerConfig/#logmaxdays","title":"logMaxDays","text":"<pre><code>logMaxDays?: number;\n</code></pre> <p>Number of days to retain log entries in the internal SQLite database.</p> <p>Defaults to <code>3</code> days. Entries older than this limit are purged automatically on a rolling basis.</p> <p>See also - <code>logLevel</code> - Logger.emailLog</p> <pre><code>BackgroundGeolocation.ready({\n  logger: {\n    logMaxDays: 3\n  }\n});\n</code></pre>"},{"location":"typescript/MotionActivity/","title":"MotionActivity","text":"<pre><code>export interface MotionActivity {\n</code></pre> <p>The motion activity reported by the device at the time a Location was recorded.</p>"},{"location":"typescript/MotionActivity/#members","title":"Members","text":""},{"location":"typescript/MotionActivity/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the reported activity as a percentage (0\u2013100).</p>"},{"location":"typescript/MotionActivity/#type","title":"type","text":"<p><code>type:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"typescript/MotionActivityEvent/","title":"MotionActivityEvent","text":"<pre><code>export interface MotionActivityEvent {\n</code></pre> <p>Activity recognition result delivered to BackgroundGeolocation.onActivityChange.</p> <p>The SDK fires this event each time the device's motion-activity classifier detects a change in the current activity type (e.g. from <code>still</code> to <code>on_foot</code>). The event includes both the detected activity and the classifier's confidence level.</p> <pre><code>BackgroundGeolocation.onActivityChange((event) =&gt; {\n  console.log(\"[onActivityChange]\", event.activity, event.confidence);\n});\n</code></pre>"},{"location":"typescript/MotionActivityEvent/#members","title":"Members","text":""},{"location":"typescript/MotionActivityEvent/#activity","title":"activity","text":"<p><code>activity:</code><code>MotionActivityType</code><code>;</code></p> <p>Detected motion activity type (e.g. <code>still</code>, <code>on_foot</code>, <code>in_vehicle</code>).</p>"},{"location":"typescript/MotionActivityEvent/#confidence","title":"confidence","text":"<pre><code>confidence: number;\n</code></pre> <p>Confidence of the detected activity as a percentage (<code>0</code>\u2013<code>100</code>).</p>"},{"location":"typescript/MotionActivityType/","title":"MotionActivityType","text":"<pre><code>export const MotionActivityType = {\n</code></pre> <p>Reported device motion activity names (legacy RN + native).</p>"},{"location":"typescript/MotionActivityType/#members","title":"Members","text":""},{"location":"typescript/MotionActivityType/#invehicle","title":"InVehicle","text":"<pre><code>InVehicle: 'in_vehicle',\n</code></pre> <p>Device is in vehicle.</p>"},{"location":"typescript/MotionActivityType/#onbicycle","title":"OnBicycle","text":"<pre><code>OnBicycle: 'on_bicycle',\n</code></pre> <p>Device is on bicycle.</p>"},{"location":"typescript/MotionActivityType/#onfoot","title":"OnFoot","text":"<pre><code>OnFoot: 'on_foot',\n</code></pre> <p>Device is on foot (walking or running).</p>"},{"location":"typescript/MotionActivityType/#running","title":"Running","text":"<pre><code>Running: 'running',\n</code></pre> <p>Device is running.</p>"},{"location":"typescript/MotionActivityType/#still","title":"Still","text":"<pre><code>Still: 'still',\n</code></pre> <p>Device is stationary.</p>"},{"location":"typescript/MotionActivityType/#unknown","title":"Unknown","text":"<pre><code>Unknown: 'unknown'\n</code></pre> <p>Device activity is unknown.</p>"},{"location":"typescript/MotionActivityType/#walking","title":"Walking","text":"<pre><code>Walking: 'walking',\n</code></pre> <p>Device is walking.</p>"},{"location":"typescript/MotionChangeEvent/","title":"MotionChangeEvent","text":"<pre><code>export interface MotionChangeEvent {\n</code></pre> <p>Motion-state change delivered to BackgroundGeolocation.onMotionChange.</p> <p>The SDK fires this event each time it transitions between the moving and stationary states. A location is always recorded at the moment of transition.</p> <pre><code>BackgroundGeolocation.onMotionChange((event) =&gt; {\n  if (event.isMoving) {\n    console.log(\"[onMotionChange] Device is moving:\", event.location);\n  } else {\n    console.log(\"[onMotionChange] Device is stationary:\", event.location);\n  }\n});\n</code></pre>"},{"location":"typescript/MotionChangeEvent/#members","title":"Members","text":""},{"location":"typescript/MotionChangeEvent/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK has transitioned to the moving state; <code>false</code> when it has transitioned to the stationary state.</p>"},{"location":"typescript/MotionChangeEvent/#location","title":"location","text":"<p><code>location:</code><code>Location</code><code>;</code></p> <p>Location recorded at the moment the motion-state transition was detected.</p>"},{"location":"typescript/NotificationConfig/","title":"NotificationConfig Android only","text":"<pre><code>export interface NotificationConfig {\n</code></pre> <p>Foreground service notification configuration for the background geolocation SDK.</p> <p>Android requires a persistent notification whenever the SDK runs its foreground service. <code>NotificationConfig</code> controls that notification's content, appearance, and behaviour while tracking is active.</p> <p></p>"},{"location":"typescript/NotificationConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Custom layout</li> </ul>"},{"location":"typescript/NotificationConfig/#overview","title":"Overview","text":"<p>Configure this via NotificationConfig on AppConfig.notification.</p> Category Properties Notes Content <code>title</code>, <code>text</code>, <code>color</code> Notification text and accent color. Icons <code>smallIcon</code>, <code>largeIcon</code> Resource references in <code>type/name</code> format. Channel <code>channelName</code>, <code>channelId</code> Android O+ notification channel settings. Behaviour <code>sticky</code>, <code>priority</code> Persistence and ordering. Custom layout <code>layout</code>, <code>strings</code>, <code>actions</code> Custom XML layout with text fields and buttons. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The Title\",\n      text: \"The Text\"\n    }\n  }\n});\n\n// Update notification fields at runtime.\n// Only changed keys must be provided; existing values persist.\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      title: \"The New Title\",\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#custom-layout","title":"Custom layout","text":"<p>Supply a custom Android Layout XML file via NotificationConfig.layout for complete control over the notification appearance.</p> <p>See the Android Custom Notification Layout guide for full setup instructions.</p>"},{"location":"typescript/NotificationConfig/#members","title":"Members","text":""},{"location":"typescript/NotificationConfig/#actions","title":"actions","text":"<pre><code>actions?: string[];\n</code></pre> <p>Declare click listeners for <code>&lt;Button /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p></p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare <code>&lt;Button /&gt;</code> elements in your layout XML, then list their <code>android:id</code> values in the <code>actions</code> array to register click listeners.</p>"},{"location":"typescript/NotificationConfig/#custom-button-element","title":"Custom <code>&lt;Button /&gt;</code> element","text":"<pre><code>&lt;Button\n  android:id=\"@+id/notificationButtonPause\"\n  style=\"@style/Widget.AppCompat.Button.Small\"\n  android:layout_width=\"60dp\"\n  android:layout_height=\"40dp\"\n  android:text=\"Foo\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#register-button-listeners","title":"Register button listeners","text":"<pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonPause\"   // &lt;-- register button listeners\n      ]\n    }\n  }\n});\n\n// Listen to custom button clicks:\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction] - \", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonPause\":\n      BackgroundGeolocation.changePace(false);\n      break;\n    // ...\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#channelid","title":"channelId","text":"<pre><code>channelId?: string;\n</code></pre> <p>Identifier of the Android notification channel used for the foreground service notification. Defaults to <code>\"bggeo\"</code>.</p> <p>Note</p> <p>Changing this is not typically required. A use case is sharing an existing notification channel with another foreground service in the same app.</p>"},{"location":"typescript/NotificationConfig/#channelname","title":"channelName","text":"<pre><code>channelName?: string;\n</code></pre> <p>Name of the Android notification channel used for the foreground service notification. Defaults to <code>\"BackgroundGeolocation\"</code>.</p> <p>On Android O and above, foreground services require a notification channel. The channel name is visible to users under:</p> <p>Settings \u2192 Apps &amp; Notifications \u2192 Your App</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      channelName: \"Location Tracker\"\n    }\n  }\n});\n\n// Update at runtime\nBackgroundGeolocation.setConfig({\n  app: {\n    notification: {\n      channelName: \"My new channel name\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#color","title":"color","text":"<pre><code>color?: string;\n</code></pre> <p>Accent color of the notification icon. Not set by default.</p> <p>Applies to API level 21 and above. Supported formats: - <code>#RRGGBB</code> - <code>#AARRGGBB</code></p>"},{"location":"typescript/NotificationConfig/#largeicon","title":"largeIcon","text":"<pre><code>largeIcon?: string;\n</code></pre> <p>Large icon for the foreground notification. Not set by default.</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>smallIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"drawable/my_notification_large_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      largeIcon: \"mipmap/my_notification_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#layout","title":"layout","text":"<pre><code>layout?: string;\n</code></pre> <p>Name of a custom Android Layout XML file for the foreground notification.</p> <p>See also - Android Custom Notification Layout</p> <p></p> <p>Custom layouts support <code>&lt;TextView /&gt;</code>, <code>&lt;ImageView /&gt;</code>, and <code>&lt;Button /&gt;</code> elements. All <code>android:id</code> values must be prefixed with <code>notification</code> (e.g. <code>notificationText</code>, <code>notificationTitle</code>). The one exception is <code>applicationName</code>, which the SDK populates with the app name automatically.</p>"},{"location":"typescript/NotificationConfig/#layout-special-elements","title":"Layout special elements","text":"<p>When rendering a custom notification, the SDK searches for the following IDs and populates them from the associated data source:</p> Layout element <code>android:id</code> Data source <code>applicationName</code> Application name from AndroidManifest <code>notificationTitle</code> <code>title</code> <code>notificationText</code> <code>text</code> <code>notificationSmallIcon</code> <code>smallIcon</code> <code>notificationLargeIcon</code> <code>largeIcon</code> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      layout: \"my_notification_layout\",\n      title: \"The Notification Title\",\n      text: \"The Notification Text\",\n      smallIcon: \"mipmap/my_small_icon\",\n      largeIcon: \"mipmap/my_large_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#custom-textview-elements","title":"Custom <code>&lt;TextView /&gt;</code> elements","text":"<p>You may define your own custom text fields and populate them using <code>strings</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#custom-button-elements","title":"Custom <code>&lt;Button /&gt;</code> elements","text":"<p>Define your own buttons and register click listeners using <code>actions</code>.</p> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#sample-layout","title":"Sample layout","text":"<pre><code>&lt;?xml version=\"1.0\" encoding=\"utf-8\"?&gt;\n&lt;LinearLayout xmlns:android=\"http://schemas.android.com/apk/res/android\"\n    xmlns:tools=\"http://schemas.android.com/tools\"\n    android:layout_width=\"match_parent\"\n    android:layout_height=\"135dp\"\n    android:gravity=\"start\"\n    android:orientation=\"vertical\"\n    android:padding=\"15dp\"&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:layout_marginBottom=\"15dp\"\n        android:gravity=\"center\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;ImageView\n            android:id=\"@+id/notificationSmallIcon\"\n            android:layout_width=\"16dp\"\n            android:layout_height=\"16dp\"\n            android:tint=\"@android:color/background_dark\"\n            tools:srcCompat=\"@tools:sample/avatars\" /&gt;\n\n        &lt;TextView\n            android:id=\"@+id/applicationName\"\n            android:layout_width=\"match_parent\"\n            android:layout_height=\"match_parent\"\n            android:paddingLeft=\"10dp\"\n            android:text=\"applicationName\"\n            android:textAppearance=\"@style/TextAppearance.Compat.Notification.Title\"\n            android:textColor=\"#888888\"\n            android:textSize=\"12sp\" /&gt;\n    &lt;/LinearLayout&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationTitle\"\n        style=\"@style/TextAppearance.Compat.Notification.Title\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationTitle\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;TextView\n        android:id=\"@+id/notificationText\"\n        style=\"@style/TextAppearance.Compat.Notification.Line2\"\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:text=\"notificationText\"\n        android:textSize=\"14sp\" /&gt;\n\n    &lt;LinearLayout\n        android:layout_width=\"match_parent\"\n        android:layout_height=\"wrap_content\"\n        android:gravity=\"right\"\n        android:orientation=\"horizontal\"&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonFoo\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"FooA\" /&gt;\n\n        &lt;Button\n            android:id=\"@+id/notificationButtonBar\"\n            style=\"@style/Widget.AppCompat.Button.Small\"\n            android:layout_width=\"60dp\"\n            android:layout_height=\"40dp\"\n            android:text=\"Bar\" /&gt;\n    &lt;/LinearLayout&gt;\n&lt;/LinearLayout&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ]\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      title: \"The title\",\n      text: \"The text\",\n      layout: \"notification_layout\",\n      actions: [\n        \"notificationButtonFoo\",\n        \"notificationButtonBar\"\n      ],\n      strings: {\n        myCustomTextBox1: \"custom TextBox element\"\n      }\n    }\n  }\n});\n\nBackgroundGeolocation.onNotificationAction((buttonId) =&gt; {\n  console.log(\"[onNotificationAction]\", buttonId);\n  switch (buttonId) {\n    case \"notificationButtonFoo\":\n      break;\n    case \"notificationButtonBar\":\n      break;\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#priority","title":"priority","text":"<p><code>priority?:</code><code>NotificationPriority</code><code>;</code></p> <p>Controls the position and visibility of the foreground notification in the system shade. Defaults to <code>PRIORITY_LOW</code> (<code>-1</code>).</p> <p><code>priority</code> affects both the ordering of the notification in the notification shade and the position/visibility of the small status-bar icon.</p> Value Description NotificationPriority.Default Weighted toward the top; status-bar icon left-aligned. NotificationPriority.High Strongly weighted to the top; icon strongly left-aligned. NotificationPriority.Low Weighted toward the bottom; icon right-aligned. NotificationPriority.Max Equivalent to <code>NOTIFICATION_PRIORITY_HIGH</code>. NotificationPriority.Min Strongly weighted to the bottom; icon hidden. <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      priority: NotificationPriority.High\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#smallicon","title":"smallIcon","text":"<pre><code>smallIcon?: string;\n</code></pre> <p>Small status-bar icon for the foreground notification. Defaults to <code>\"mipmap/ic_launcher\"</code> (the app launcher icon).</p> <p>Warning</p> <ul> <li>Specify the resource type (<code>drawable</code> or <code>mipmap</code>) followed by the icon   name in the format <code>type/icon_name</code>.</li> <li>Do not include the file extension (e.g. <code>.png</code>).</li> </ul> <p>See also - <code>largeIcon</code></p> <pre><code>// drawable resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"drawable/my_notification_icon\"\n    }\n  }\n});\n\n// mipmap resource\nBackgroundGeolocation.ready({\n  app: {\n    notification: {\n      smallIcon: \"mipmap/my_notification_icon\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#sticky","title":"sticky","text":"<pre><code>sticky?: boolean;\n</code></pre> <p>Keeps the foreground service notification visible at all times, regardless of motion state. Defaults to <code>false</code>.</p> <p>By default the notification is shown only while the SDK detects the device is moving. Set to <code>true</code> to show the notification continuously \u2014 for example, when full transparency to users is a requirement.</p>"},{"location":"typescript/NotificationConfig/#strings","title":"strings","text":"<pre><code>strings?: Record&lt;string, string&gt;;\n</code></pre> <p>Custom strings to render into <code>&lt;TextView /&gt;</code> elements of a custom notification <code>layout</code>.</p> <p>See Android Custom Notification Layout for setup instructions.</p> <p>Declare your own <code>&lt;TextView /&gt;</code> elements in the layout XML and populate them by supplying matching key/value pairs in <code>strings</code>, where the key matches the <code>android:id</code> of the element.</p> <pre><code>&lt;TextView\n  android:id=\"@+id/myCustomElement\"\n  android:layout_width=\"match_parent\"\n  android:layout_height=\"wrap_content\"\n  android:text=\"notificationTitle\" /&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  app: {\n    notification: {\n      strings: {\n        myCustomElement: \"My Custom Element Text\"\n      }\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/NotificationConfig/#text","title":"text","text":"<pre><code>text?: string;\n</code></pre> <p>Body text of the foreground service notification. Defaults to <code>\"Tracking location\"</code>.</p>"},{"location":"typescript/NotificationConfig/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title of the foreground service notification. Defaults to <code>\"Background Geolocation\"</code>.</p>"},{"location":"typescript/NotificationPriority/","title":"NotificationPriority","text":"<pre><code>export const NotificationPriority = {\n</code></pre> <p>Defines the priority / visibility of the Android foreground-service notification.</p> <p>This affects how the notification appears in the status bar and how strongly the OS ranks it among other notifications.</p> <p>Mirrors Flutter\u2019s <code>NotificationPriority</code> enum.</p>"},{"location":"typescript/NotificationPriority/#members","title":"Members","text":""},{"location":"typescript/NotificationPriority/#default","title":"Default","text":"<p>Default notification priority (normal weighting).</p>"},{"location":"typescript/NotificationPriority/#high","title":"High","text":"<p>Notification strongly weighted to top of list; icon strongly weighted to the left.</p>"},{"location":"typescript/NotificationPriority/#low","title":"Low","text":"<p>Notification weighted to bottom of list; icon weighted to the right.</p>"},{"location":"typescript/NotificationPriority/#max","title":"Max","text":"<p>Same as NotificationPriority.High.</p>"},{"location":"typescript/NotificationPriority/#min","title":"Min","text":"<p>Notification strongly weighted to bottom of list; icon hidden.</p>"},{"location":"typescript/PermissionRationale/","title":"PermissionRationale Android only","text":"<pre><code>export interface PermissionRationale {\n</code></pre> <p>Configures the background location permission rationale dialog on Android 11 and later.</p> <p>Android 11 removed the Allow all the time option from the system location permission dialog. Apps may instead show a custom rationale dialog that explains why background access is needed and routes the user to the system Location Permissions screen to grant it explicitly.</p>"},{"location":"typescript/PermissionRationale/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Template tags</li> </ul>"},{"location":"typescript/PermissionRationale/#overview","title":"Overview","text":"<p>The SDK presents this dialog automatically when GeoConfig.locationAuthorizationRequest is <code>\"Always\"</code> and AppConfig.backgroundPermissionRationale is configured.</p> <p>Selecting the dialog's positive action sends the user directly to the system Location Permissions screen, where they must enable Allow all the time themselves.</p> <ul> <li>The dialog is shown once only. After the user presses the positive action   it will not appear again. Pressing Cancel does not count as acceptance.</li> <li>If the user later resets your app's Location permission to Ask every time,   the dialog becomes eligible to be shown again.</li> </ul> <p> </p> <pre><code>import BackgroundGeolocation from \"react-native-background-geolocation\";\n\nBackgroundGeolocation.ready({\n  geolocation: {\n    locationAuthorizationRequest: 'Always'\n  },\n  app: {\n    backgroundPermissionRationale: {\n      title: \"Allow {applicationName} to access this device's location in the background?\",\n      message: \"To track your activity in the background, please enable {backgroundPermissionOptionLabel} location access.\",\n      positiveAction: \"Change to {backgroundPermissionOptionLabel}\",\n      negativeAction: \"Cancel\"\n    }\n  }\n});\n</code></pre>"},{"location":"typescript/PermissionRationale/#template-tags","title":"Template tags","text":"<p>Embed the following variables inside any rationale field using <code>{tagName}</code> syntax:</p> Template Tag Default value Description <code>{backgroundPermissionOptionLabel}</code> \"Allow all the time\" (API 30+) Localized label for the background location permission option. <code>{applicationName}</code> App name Localized application name from <code>AndroidManifest</code>. <p>See also - GeoConfig.locationAuthorizationRequest - BackgroundGeolocation.requestPermission - Android 11 Location Updates</p>"},{"location":"typescript/PermissionRationale/#members","title":"Members","text":""},{"location":"typescript/PermissionRationale/#message","title":"message","text":"<pre><code>message?: string;\n</code></pre> <p>Body text of the dialog. Explain why your app needs background location access, similar in purpose to the iOS <code>NSLocationAlwaysAndWhenInUseUsageDescription</code>.</p>"},{"location":"typescript/PermissionRationale/#negativeaction","title":"negativeAction","text":"<pre><code>negativeAction?: string;\n</code></pre> <p>Text to display on the negative action button (e.g. \"Cancel\").</p>"},{"location":"typescript/PermissionRationale/#positiveaction","title":"positiveAction","text":"<pre><code>positiveAction?: string;\n</code></pre> <p>Text to display on the positive action button.</p>"},{"location":"typescript/PermissionRationale/#title","title":"title","text":"<pre><code>title?: string;\n</code></pre> <p>Title for the Android background location permission dialog.</p>"},{"location":"typescript/PersistMode/","title":"PersistMode","text":"<pre><code>export const PersistMode = {\n</code></pre> <p>Persistence modes for BackgroundGeolocation SDK. Mirrors Flutter's <code>PersistMode</code> enum.</p>"},{"location":"typescript/PersistMode/#members","title":"Members","text":""},{"location":"typescript/PersistMode/#all","title":"All","text":"<p>Persist both location and geofence data.</p>"},{"location":"typescript/PersistMode/#geofence","title":"Geofence","text":"<p>Persist geofence data only.</p>"},{"location":"typescript/PersistMode/#location","title":"Location","text":"<p>Persist location data only.</p>"},{"location":"typescript/PersistMode/#none","title":"None","text":"<p>Do not persist any data.</p>"},{"location":"typescript/PersistenceConfig/","title":"PersistenceConfig","text":"<pre><code>export interface PersistenceConfig {\n</code></pre> <p>Persistence and storage configuration for the background geolocation SDK.</p> <p><code>PersistenceConfig</code> controls how the SDK stores, orders, templates, and purges records in its on-device SQLite database \u2014 the durable buffer between data producers (locations, geofences) and consumers (your app and the HTTP service).</p>"},{"location":"typescript/PersistenceConfig/#contents","title":"Contents","text":"<ul> <li>Overview</li> <li>Storage lifecycle</li> <li>Ordering</li> <li>Templates</li> <li>Persist mode</li> <li>Examples</li> </ul>"},{"location":"typescript/PersistenceConfig/#overview","title":"Overview","text":"<p>The SDK maintains an internal SQLite database as a rolling queue. Each recorded Location and geofence event is written immediately, then consumed and deleted by downstream services. The SDK prefers an empty database \u2014 records exist only while awaiting upload or retrieval.</p> Category Properties Notes Retention <code>maxDaysToPersist</code>, <code>maxRecordsToPersist</code> TTL and count-based purge limits. Ordering <code>locationsOrderDirection</code> Controls upload and retrieval order. Templates <code>locationTemplate</code>, <code>geofenceTemplate</code> Custom JSON structure for uploads. Extras <code>extras</code> Static key/value pairs merged into every record. Persist mode <code>persistMode</code> Which event types are written to SQLite. Diagnostics <code>disableProviderChangeRecord</code> Suppress provider-change records. <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 3,\n    maxRecordsToPersist: 1000,\n    extras: {\n      'user_id': 123,\n      'appVersion': '1.2.3'\n    },\n    persistMode: PersistMode.All\n  }\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#storage-lifecycle","title":"Storage lifecycle","text":"<p>Records are written to SQLite immediately upon recording and deleted when any of the following occur:</p> <ul> <li>Your server returns a <code>20x</code> response for the HTTP upload (see HttpConfig)</li> <li>You call BackgroundGeolocation.destroyLocations</li> <li><code>maxDaysToPersist</code> elapses (rolling TTL purge)</li> <li><code>maxRecordsToPersist</code> would be exceeded (oldest records are dropped)</li> </ul> <p>Inspect the pending queue with BackgroundGeolocation.getCount or BackgroundGeolocation.getLocations.</p>"},{"location":"typescript/PersistenceConfig/#ordering","title":"Ordering","text":"<p><code>locationsOrderDirection</code> controls the order in which records are selected for upload or returned by BackgroundGeolocation.getLocations:</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest first (default)</li> <li><code>\"DESC\"</code> \u2014 newest first</li> </ul>"},{"location":"typescript/PersistenceConfig/#templates","title":"Templates","text":"<p>By default the SDK serializes locations and geofence events using its standard JSON schema. Use <code>locationTemplate</code> and <code>geofenceTemplate</code> to substitute a custom ERB template that reshapes the JSON to match your backend.</p> <p>Use <code>extras</code> to merge static key/value metadata into every record at write time, without a custom template.</p>"},{"location":"typescript/PersistenceConfig/#persist-mode","title":"Persist mode","text":"<p><code>persistMode</code> controls which event types are written to SQLite. All events are still delivered to their live callbacks (BackgroundGeolocation.onLocation, BackgroundGeolocation.onGeofence) regardless of this setting. Events that are not persisted are not eligible for HTTP uploads via HttpConfig.url.</p> <p>The <code>persist</code> option on individual API calls can override this setting per-request:</p> <pre><code>const location = await BackgroundGeolocation.getCurrentPosition({\n  persist: true,\n  extras: { get_current_position: true },\n  samples: 3,\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#examples","title":"Examples","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#now-grouped-under-persistenceconfig","title":"Now grouped under PersistenceConfig","text":"<pre><code>{\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    locationTemplate: '{...}',\n    geofenceTemplate: '{...}',\n    persistMode: PersistMode.all,\n    extras: {'user_id': 123},\n    disableProviderChangeRecord: true,\n  },\n};\n</code></pre>"},{"location":"typescript/PersistenceConfig/#configure-persistence-behavior","title":"Configure persistence behavior","text":"<pre><code>const state = await BackgroundGeolocation.ready({\n  persistence: {\n    maxDaysToPersist: 14,\n    maxRecordsToPersist: 5000,\n    locationsOrderDirection: 'ASC',\n    persistMode: PersistMode.All,\n    extras: { user_id: 123, appVersion: '1.2.3' },\n  },\n  http: {\n    url: 'https://example.com/locations',\n    autoSync: true,\n  }\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#inspect-and-purge-the-database","title":"Inspect and purge the database","text":"<pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre>"},{"location":"typescript/PersistenceConfig/#custom-json-templates","title":"Custom JSON templates","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre> <pre><code>const pending = await BackgroundGeolocation.getCount();\nconsole.log('Pending records:', pending);\n\n// Purge all records\nconst ok = await BackgroundGeolocation.destroyLocations();\nconsole.log('Destroyed all records?', ok);\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: `{\n      \"lat\": &lt;%= latitude %&gt;,\n      \"lng\": &lt;%= longitude %&gt;,\n      \"ts\": \"&lt;%= timestamp %&gt;\",\n      \"meta\": &lt;%= JSON.stringify(extras) %&gt;\n    }`,\n    geofenceTemplate: `{\n      \"id\": \"&lt;%= identifier %&gt;\",\n      \"action\": \"&lt;%= action %&gt;\",\n      \"ts\": \"&lt;%= timestamp %&gt;\"\n    }`,\n  },\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#members","title":"Members","text":""},{"location":"typescript/PersistenceConfig/#disableproviderchangerecord","title":"disableProviderChangeRecord","text":"<pre><code>disableProviderChangeRecord?: boolean;\n</code></pre> <p>Android only Disables the automatic insertion of a synthetic \"provider-change\" location into the SDK's SQLite database and its subsequent HTTP upload.</p> <p>By default, when an BackgroundGeolocation.onProviderChange event fires, the Android SDK records a special location documenting when and where the device's location-services state changed (e.g., GPS disabled). This behavior historically existed to support platforms with limited or unreliable Headless Task implementations (e.g., Cordova, Capacitor).</p> <p>Set this to <code>true</code> if your server-side JSON schema or <code>locationTemplate</code> cannot accommodate the automatically injected <code>provider</code> field.</p> <p></p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    disableProviderChangeRecord: true\n }\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional arbitrary key/value pairs merged into each recorded location at write time.</p> <p>These values are persisted and included in all HTTP uploads, making them ideal for attaching static contextual metadata such as <code>user_id</code>, <code>route_id</code>, or <code>session_id</code>.</p> <p>See also - <code>HTTP Guide</code></p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: \"https://my-server.com/locations\",\n    params: {\n      device_id: \"abc123\"  // appended to root JSON of each POST request\n    }\n  },\n  persistence: {\n    extras: {\n      route_id: 1234       // merged onto each location record\n    }\n  }\n});\n</code></pre> <p>The resulting HTTP request body: <pre><code>POST /locations\n{\n  \"device_id\": \"abc123\",\n  \"location\": {\n    \"coords\": {\n      \"latitude\": 45.51927004945047,\n      \"longitude\": -73.61650072045029\n    },\n    \"extras\": {\n      \"route_id\": 1234\n    }\n  }\n}\n</code></pre></p>"},{"location":"typescript/PersistenceConfig/#geofencetemplate","title":"geofenceTemplate","text":"<pre><code>geofenceTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping GeofenceEvent JSON in HTTP uploads.</p> <p>Behaves like <code>locationTemplate</code> but includes two additional tags: <code>geofence.identifier</code> and <code>geofence.action</code>. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '{ \"lat\":&lt;%= latitude %&gt;, \"lng\":&lt;%= longitude %&gt;, \"geofence\":\"&lt;%= geofence.identifier %&gt;:&lt;%= geofence.action %&gt;\" }'\n  }\n});\n\n// Or a compact Array form:\nBackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate:\n      '[&lt;%= latitude %&gt;, &lt;%= longitude %&gt;, \"&lt;%= geofence.identifier %&gt;\", \"&lt;%= geofence.action %&gt;\"]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically apply double-quotes around string data. Templates are JSON-encoded exactly as written.</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>Produces invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"typescript/PersistenceConfig/#template-tags","title":"Template tags","text":"<p>Identical to <code>locationTemplate</code> with the following additions:</p> Tag Type Description <code>geofence.identifier</code> <code>String</code> Identifier of the activated geofence <code>geofence.action</code> <code>String</code> <code>\"ENTER\"</code> or <code>\"EXIT\"</code> <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Whether device is plugged in <code>mock</code> <code>Boolean</code> True when location was generated from a mock app <code>is_moving</code> <code>Boolean</code> True if recorded while in moving state <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>locationTemplate</code> - HttpConfig.rootProperty - <code>HTTP Guide</code></p>"},{"location":"typescript/PersistenceConfig/#incorrect","title":"Incorrect","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#correct","title":"Correct","text":"<pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    geofenceTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n  }\n});\n</code></pre>"},{"location":"typescript/PersistenceConfig/#locationtemplate","title":"locationTemplate","text":"<pre><code>locationTemplate?: string;\n</code></pre> <p>Optional custom ERB template for reshaping Location JSON in HTTP uploads.</p> <p>When set, the SDK evaluates the template string against each location record before serializing it. Use Ruby-style ERB tags to interpolate field values:</p> <pre><code>&lt;%= variable_name %&gt;\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;,\"event\":\"&lt;%= event %&gt;\",isMoving:&lt;%= is_moving %&gt;}'\n  }\n});\n\n// Or use a compact Array template:\nBackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate:\n      '[&lt;%=latitude%&gt;, &lt;%=longitude%&gt;, \"&lt;%=event%&gt;\", &lt;%=is_moving%&gt;]'\n  }\n});\n</code></pre> <p>Warning</p> <p>The SDK does not automatically insert quotes around string values. Templates are JSON-encoded exactly as written.</p> <p>The following produces invalid JSON because <code>timestamp</code> is a string but is</p> <pre><code>BackgroundGeolocation.ready({\n  persistence: {\n    locationTemplate: '{\"timestamp\": &lt;%= timestamp %&gt;}'\n  }\n});\n</code></pre> <p>This renders invalid JSON:</p> <pre><code>{\"timestamp\": 2018-01-01T12:01:01.123Z}\n</code></pre> <pre><code>BackgroundGeolocation.ready({\n persistence: {\n  locationTemplate: '{\"timestamp\": \"&lt;%= timestamp %&gt;\"}'\n }\n});\n</code></pre> <pre><code>{\"timestamp\": \"2018-01-01T12:01:01.123Z\"}\n</code></pre>"},{"location":"typescript/PersistenceConfig/#configured-extras","title":"Configured <code>extras</code>","text":"<p>If <code>extras</code> are configured, the key/value pairs are merged directly into the rendered location JSON.</p> <pre><code>BackgroundGeolocation.ready({\n  http: {\n    url: 'https://my.server.com/locations',\n    rootProperty: 'data',\n  },\n  persistence: {\n    locationTemplate: '{\"lat\":&lt;%= latitude %&gt;,\"lng\":&lt;%= longitude %&gt;}',\n    extras: { foo: \"bar\" }\n  }\n});\n</code></pre> <p>Produces:</p> <pre><code>{\n  \"data\": {\n    \"lat\": 23.23232323,\n    \"lng\": 37.37373737,\n    \"foo\": \"bar\"\n  }\n}\n</code></pre>"},{"location":"typescript/PersistenceConfig/#template-tags_1","title":"Template tags","text":"Tag Type Description <code>latitude</code> <code>Float</code> <code>longitude</code> <code>Float</code> <code>speed</code> <code>Float</code> Meters <code>heading</code> <code>Float</code> Degrees <code>accuracy</code> <code>Float</code> Meters <code>altitude</code> <code>Float</code> Meters <code>altitude_accuracy</code> <code>Float</code> Meters <code>timestamp</code> <code>String</code> ISO-8601 <code>uuid</code> <code>String</code> Unique ID <code>event</code> <code>String</code> <code>motionchange</code>, <code>geofence</code>, <code>heartbeat</code>, <code>providerchange</code> <code>odometer</code> <code>Float</code> Meters <code>activity.type</code> <code>String</code> <code>still</code>, <code>on_foot</code>, <code>running</code>, <code>on_bicycle</code>, <code>in_vehicle</code>, <code>unknown</code> <code>activity.confidence</code> <code>Integer</code> 0\u2013100% <code>battery.level</code> <code>Float</code> 0\u2013100% <code>battery.is_charging</code> <code>Boolean</code> Is device plugged in? <code>mock</code> <code>Boolean</code> True if generated by mock provider <code>is_moving</code> <code>Boolean</code> Device was moving when recorded <code>timestampMeta</code> <code>Object</code> Timestamp metadata; see GeoConfig.enableTimestampMeta <p>See also - <code>HTTP Guide</code> - <code>geofenceTemplate</code> - HttpConfig.rootProperty</p>"},{"location":"typescript/PersistenceConfig/#locationsorderdirection","title":"locationsOrderDirection","text":"<pre><code>locationsOrderDirection?: \"ASC\" | \"DESC\";\n</code></pre> <p>Sort order used when selecting records for upload or returning them from BackgroundGeolocation.getLocations. Defaults to <code>\"ASC\"</code> (oldest first).</p> <ul> <li><code>\"ASC\"</code> \u2014 oldest records first</li> <li><code>\"DESC\"</code> \u2014 newest records first</li> </ul>"},{"location":"typescript/PersistenceConfig/#maxdaystopersist","title":"maxDaysToPersist","text":"<pre><code>maxDaysToPersist?: number;\n</code></pre> <p>Maximum number of days to retain a persisted record in the SDK's on-device SQLite database. Defaults to <code>1</code> day.</p> <p>When your server fails to return a <code>20x</code> response, the SDK continues retrying uploads. If a record remains unuploaded for longer than <code>maxDaysToPersist</code> days, it is permanently discarded to prevent unbounded database growth.</p>"},{"location":"typescript/PersistenceConfig/#maxrecordstopersist","title":"maxRecordsToPersist","text":"<pre><code>maxRecordsToPersist?: number;\n</code></pre> <p>Maximum number of records the SDK may retain in its on-device SQLite database. Defaults to <code>-1</code> (no limit).</p> <p>When a limit is set and the stored record count would exceed it, the oldest records are purged to make room for the newest.</p> <p>See HttpEvent for details on upload behavior.</p>"},{"location":"typescript/PersistenceConfig/#persistmode","title":"persistMode","text":"<p><code>persistMode?:</code><code>PersistMode</code><code>;</code></p> <p>Controls which event types the SDK writes to its internal SQLite database. Defaults to PersistMode.All.</p> <p>All recorded events are always delivered to their live callbacks (BackgroundGeolocation.onLocation and BackgroundGeolocation.onGeofence). This setting only determines what is written to persistent storage. Events that are not persisted are also not eligible for HTTP uploads via HttpConfig.url.</p> Value Description PersistMode.All Default \u2014 persist both location and geofence events. PersistMode.Location Persist location events only. PersistMode.Geofence Persist geofence events only. PersistMode.None Persist nothing. <p>Warning</p> <p>This setting is intended for specialized cases. For example, if you need continuous location tracking via BackgroundGeolocation.start but only want to store geofence events, configure <code>persistMode: PersistMode.Geofence</code>.</p>"},{"location":"typescript/ProviderChangeEvent/","title":"ProviderChangeEvent","text":"<pre><code>export interface ProviderChangeEvent {\n</code></pre> <p>Location-services provider state delivered to BackgroundGeolocation.onProviderChange.</p> <p>The SDK fires this event when the user changes location permissions, toggles device location settings, or \u2014 on iOS 14 and later \u2014 when the precision authorization level changes between full and reduced accuracy.</p> <pre><code>BackgroundGeolocation.onProviderChange((event) =&gt; {\n  console.log(\"[onProviderChange]\",\n    \"enabled:\", event.enabled,\n    \"status:\", event.status,\n    \"gps:\", event.gps,\n    \"network:\", event.network,\n    \"accuracyAuthorization:\", event.accuracyAuthorization\n  );\n\n  if (!event.enabled) {\n    // Prompt the user to re-enable location services.\n  }\n});\n</code></pre>"},{"location":"typescript/ProviderChangeEvent/#members","title":"Members","text":""},{"location":"typescript/ProviderChangeEvent/#accuracyauthorization","title":"accuracyAuthorization","text":"<pre><code>accuracyAuthorization: number;\n</code></pre> <p>Precision location authorization level. Use the AccuracyAuthorization constants to interpret this value.</p> <p>On iOS 14 and later this reflects whether the user has granted full or reduced accuracy. On Android this value is always <code>AccuracyAuthorization.Full</code>.</p>"},{"location":"typescript/ProviderChangeEvent/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the device has at least one location provider enabled.</p>"},{"location":"typescript/ProviderChangeEvent/#gps","title":"gps","text":"<pre><code>gps: boolean;\n</code></pre> <p><code>true</code> when the GPS-based location provider is available.</p>"},{"location":"typescript/ProviderChangeEvent/#network","title":"network","text":"<pre><code>network: boolean;\n</code></pre> <p><code>true</code> when the network-based location provider is available.</p>"},{"location":"typescript/ProviderChangeEvent/#status","title":"status","text":"<pre><code>status: number;\n</code></pre> <p>Platform authorization status code. Use the AuthorizationStatus constants to interpret this value.</p>"},{"location":"typescript/SQLQuery/","title":"SQLQuery","text":"<pre><code>export interface SQLQuery {\n</code></pre> <p>Constrains a log query by date range, sort order, and record count.</p> <p>Pass to Logger.getLog, Logger.emailLog, or Logger.uploadLog to filter which log entries are included.</p> <pre><code>const Logger = BackgroundGeolocation.logger;\n\n// Date range\nconst log = await Logger.getLog({\n  start: Date.parse(\"2019-10-21 13:00\"),\n  end:   Date.parse(\"2019-10-22\")\n});\n\n// First 100 records ascending\nawait Logger.emailLog(\"foo@bar.com\", {\n  order: SQLQueryOrder.Asc,\n  limit: 100\n});\n\n// Upload a specific time window\nawait Logger.uploadLog(\"https://my.server.com/users/123/logs\", {\n  start: Date.parse(\"2019-10-20 09:00\"),\n  end:   Date.parse(\"2019-10-20 11:59\")\n});\n</code></pre>"},{"location":"typescript/SQLQuery/#members","title":"Members","text":""},{"location":"typescript/SQLQuery/#end","title":"end","text":"<pre><code>end?: number;\n</code></pre> <p>End of the query window (unix timestamp in milliseconds).</p>"},{"location":"typescript/SQLQuery/#limit","title":"limit","text":"<pre><code>limit?: number;\n</code></pre> <p>Maximum number of records to return.</p>"},{"location":"typescript/SQLQuery/#offset","title":"offset","text":"<pre><code>offset?: number;\n</code></pre> <p>Number of matching records to skip before returning results (for paging).</p>"},{"location":"typescript/SQLQuery/#order","title":"order","text":"<p><code>order?:</code><code>SQLQueryOrder</code><code>;</code></p> <p>Sort order for results: <code>SQLQueryOrder.Asc</code> (oldest first) or <code>SQLQueryOrder.Desc</code> (newest first).</p>"},{"location":"typescript/SQLQuery/#start","title":"start","text":"<pre><code>start?: number;\n</code></pre> <p>Start of the query window (unix timestamp in milliseconds).</p>"},{"location":"typescript/SQLQueryOrder/","title":"SQLQueryOrder","text":"<pre><code>export const SQLQueryOrder = {\n</code></pre> <p>Sort order for SQLQuery.order.</p> Name Value Asc <code>1</code> Desc <code>-1</code>"},{"location":"typescript/SQLQueryOrder/#members","title":"Members","text":""},{"location":"typescript/SQLQueryOrder/#asc","title":"Asc","text":"<p>Ascending order (oldest to newest).</p>"},{"location":"typescript/SQLQueryOrder/#desc","title":"Desc","text":"<p>Descending order (newest to oldest).</p>"},{"location":"typescript/Sensors/","title":"Sensors","text":"<pre><code>export interface Sensors {\n</code></pre> <p>Availability of motion sensors on the device, as returned by BackgroundGeolocation.getSensors.</p> <p>These sensors power the SDK's motion activity-recognition system. When a sensor is absent \u2014 particularly on low-end Android devices \u2014 motion recognition performance degrades and the SDK may rely more heavily on location-based heuristics.</p> <pre><code>const sensors = await BackgroundGeolocation.getSensors();\nconsole.log(\"[Sensors]\", sensors);\n</code></pre>"},{"location":"typescript/Sensors/#members","title":"Members","text":""},{"location":"typescript/Sensors/#accelerometer","title":"accelerometer","text":"<pre><code>accelerometer: boolean;\n</code></pre> <p><code>true</code> when the device has an accelerometer.</p>"},{"location":"typescript/Sensors/#gyroscope","title":"gyroscope","text":"<pre><code>gyroscope: boolean;\n</code></pre> <p><code>true</code> when the device has a gyroscope.</p>"},{"location":"typescript/Sensors/#magnetometer","title":"magnetometer","text":"<pre><code>magnetometer: boolean;\n</code></pre> <p><code>true</code> when the device has a magnetometer (compass).</p>"},{"location":"typescript/Sensors/#motion_hardware","title":"motion_hardware","text":"<pre><code>motion_hardware?: boolean;\n</code></pre> <p>iOS only <code>true</code> when the device has a dedicated M-series motion co-processor (iPhone 5S and later). The M-series co-processor offloads motion activity recognition from the main CPU, enabling low-power step counting and activity detection.</p>"},{"location":"typescript/Sensors/#platform","title":"platform","text":"<pre><code>platform: string;\n</code></pre> <p>OS platform name: <code>\"ios\"</code> or <code>\"android\"</code>.</p>"},{"location":"typescript/Sensors/#significant_motion","title":"significant_motion","text":"<pre><code>significant_motion?: boolean;\n</code></pre> <p>Android only <code>true</code> when the device supports the Significant Motion hardware trigger.</p> <p>When present, this sensor allows the SDK to wake from a deep idle state only when the device actually moves, saving significant battery.</p>"},{"location":"typescript/State/","title":"State","text":"<p>Read-only snapshot of the SDK's current runtime state.</p> <pre><code>const state = await BackgroundGeolocation.getState();\nconsole.log(\"[state] enabled:\", state.enabled, \"trackingMode:\", state.trackingMode);\n</code></pre>"},{"location":"typescript/State/#properties","title":"Properties","text":"Property Type Description enabled Boolean Whether the SDK is currently tracking. isMoving Boolean Whether the device is currently in motion. trackingMode TrackingMode Current tracking mode: <code>LOCATION</code> or <code>GEOFENCE</code>. odometer Double Distance travelled in metres since last reset. schedulerEnabled Boolean Whether the scheduler is currently active. didDeviceReboot Boolean Whether the device rebooted since the SDK was last running."},{"location":"typescript/State/#members","title":"Members","text":""},{"location":"typescript/State/#activity","title":"activity","text":"<p><code>activity?:</code><code>ActivityConfig</code><code>;</code></p> <p>Motion recognition, stop-detection triggers, and motion-trigger delay. See ActivityConfig.</p>"},{"location":"typescript/State/#app","title":"app","text":"<p><code>app?:</code><code>AppConfig</code><code>;</code></p> <p>App lifecycle \u2014 background operation, boot behaviour, headless mode, and foreground notification. See AppConfig.</p>"},{"location":"typescript/State/#authorization","title":"authorization","text":"<p><code>authorization?:</code><code>AuthorizationConfig</code><code>;</code></p> <p>JWT and SAS token management with automatic refresh. See AuthorizationConfig.</p>"},{"location":"typescript/State/#diddevicereboot","title":"didDeviceReboot","text":"<pre><code>didDeviceReboot: boolean;\n</code></pre> <p><code>true</code> when the app was launched after a device reboot.</p>"},{"location":"typescript/State/#didlaunchinbackground","title":"didLaunchInBackground","text":"<pre><code>didLaunchInBackground: boolean;\n</code></pre> <p>iOS only <code>true</code> when the app was relaunched in the background by the OS \u2014 for example, due to a background fetch, geofence exit, or stationary geofence transition. On Android, background launch is handled by the Headless Task mechanism \u2014 this property does not apply.</p>"},{"location":"typescript/State/#enabled","title":"enabled","text":"<pre><code>enabled: boolean;\n</code></pre> <p><code>true</code> when the SDK is actively tracking \u2014 i.e. BackgroundGeolocation.start or BackgroundGeolocation.startGeofences has been called and not yet stopped.</p>"},{"location":"typescript/State/#geolocation","title":"geolocation","text":"<p><code>geolocation?:</code><code>GeoConfig</code><code>;</code></p> <p>Accuracy, sampling, elasticity, stop-detection, permissions, and geofencing. See GeoConfig.</p>"},{"location":"typescript/State/#http","title":"http","text":"<p><code>http?:</code><code>HttpConfig</code><code>;</code></p> <p>Upload URL, HTTP method, sync cadence, batching, headers, and params. See HttpConfig.</p>"},{"location":"typescript/State/#isfirstboot","title":"isFirstBoot","text":"<p><code>true</code> on the very first launch after the app is installed.</p>"},{"location":"typescript/State/#ismoving","title":"isMoving","text":"<pre><code>isMoving: boolean;\n</code></pre> <p><code>true</code> when the SDK is in the moving state (location services active); <code>false</code> when stationary.</p> <pre><code>// Toggle the SDK's motion state from stationary to moving.\nBackgroundGeolocation.onMotionChange((event) =&gt; {\n  console.log('[onMotionChange] isMoving?', event.isMoving);\n});\n\nawait BackgroundGeolocation.changePace(true);\n// State.isMoving is now true.\n</code></pre>"},{"location":"typescript/State/#logger","title":"logger","text":"<p><code>logger?:</code><code>LoggerConfig</code><code>;</code></p> <p>Debug logging, log level, and log retention. See LoggerConfig.</p>"},{"location":"typescript/State/#odometer","title":"odometer","text":"<pre><code>odometer: number;\n</code></pre> <p>Accumulated distance traveled since the last odometer reset, in meters.</p> <p>See also - <code>odometerError</code> - BackgroundGeolocation.setOdometer - BackgroundGeolocation.getOdometer</p>"},{"location":"typescript/State/#odometererror","title":"odometerError","text":"<pre><code>odometerError: number;\n</code></pre> <p>Accumulated positional error in the odometer measurement, in meters.</p> <p>Reflects noise introduced by low-accuracy location samples. Use LocationFilter.odometerAccuracyThreshold to filter out low-accuracy samples from odometer calculations.</p>"},{"location":"typescript/State/#persistence","title":"persistence","text":"<p><code>persistence?:</code><code>PersistenceConfig</code><code>;</code></p> <p>SQLite storage, TTL, record limits, and custom extras. See PersistenceConfig.</p>"},{"location":"typescript/State/#reset","title":"reset","text":"<pre><code>reset?: boolean;\n</code></pre> <p>Controls whether the SDK resets to factory defaults before applying this configuration. Defaults to <code>true</code>.</p> <p>When <code>true</code> (the default), every call to BackgroundGeolocation.ready applies the supplied <code>Config</code> on top of fresh defaults. When <code>false</code>, the SDK applies the supplied <code>Config</code> only on the first install. On subsequent launches it ignores the <code>Config</code> argument entirely \u2014 the only way to change settings is via BackgroundGeolocation.setConfig.</p> <p>Warning</p> <p>During development, always leave <code>reset: true</code> (or omit it). Setting <code>reset: false</code> causes the SDK to ignore your <code>Config</code> after the first launch, so configuration changes made between development builds will not take effect.</p>"},{"location":"typescript/State/#schedulerenabled","title":"schedulerEnabled","text":"<pre><code>schedulerEnabled: boolean;\n</code></pre> <p><code>true</code> when a AppConfig.schedule is configured and BackgroundGeolocation.startSchedule has been called. BackgroundGeolocation.stopSchedule sets this to <code>false</code>.</p>"},{"location":"typescript/State/#trackingmode","title":"trackingMode","text":"<p><code>trackingMode:</code><code>TrackingMode</code><code>;</code></p> <p>Current tracking mode.</p> Value Mode Description <code>0</code> Geofences Geofence monitoring only \u2014 no active location tracking. <code>1</code> Location Location tracking and geofence monitoring. <pre><code>await BackgroundGeolocation.start();\nlet state = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 1'\n\nawait BackgroundGeolocation.startGeofences();\nstate = await BackgroundGeolocation.getState();\nconsole.log('Tracking mode:', state.trackingMode);\n// &gt; 'Tracking mode: 0'\n</code></pre>"},{"location":"typescript/State/#transistorauthorizationtoken","title":"transistorAuthorizationToken","text":"<p><code>transistorAuthorizationToken?:</code><code>TransistorAuthorizationToken</code><code>;</code></p> <p>Convenience option that automatically configures the SDK to upload locations to the Transistor Software demo server at tracker.transistorsoft.com, or a local instance of background-geolocation-console.</p> <p>Setting this option automatically sets HttpConfig.url and the required AuthorizationConfig values. See TransistorAuthorizationService for how to obtain a token.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"typescript/Subscription/","title":"Subscription","text":"<pre><code>export interface Subscription {\n</code></pre> <p>Handle returned by every <code>BackgroundGeolocation.on*</code> event-listener method.</p> <p><code>Subscription</code> exposes a single <code>remove</code> method used to stop listening to an event and free associated resources. Always call <code>remove()</code> when a listener is no longer needed to prevent memory leaks.</p>"},{"location":"typescript/Subscription/#removing-an-event-listener","title":"Removing an event-listener","text":"<pre><code>const subscription = BackgroundGeolocation.onLocation((location) =&gt; {\n  console.log(\"[onLocation]\", location);\n});\n\n// Later, when the listener is no longer needed:\nsubscription.remove();\n</code></pre>"},{"location":"typescript/Subscription/#managing-multiple-subscriptions","title":"Managing multiple subscriptions","text":"<p>Collect subscriptions in an array and remove them all at once \u2014 for example when a view is destroyed.</p> <pre><code>import BackgroundGeolocation, {\n  Location,\n  Subscription\n} from \"react-native-background-geolocation\";\n\nconst subscriptions: Subscription[] = [];\n\nfunction addListeners() {\n  subscriptions.push(\n    BackgroundGeolocation.onLocation((location: Location) =&gt; {\n      console.log(\"[onLocation]\", location);\n    }),\n    BackgroundGeolocation.onMotionChange((event) =&gt; {\n      console.log(\"[onMotionChange]\", event);\n    }),\n    BackgroundGeolocation.onEnabledChange((enabled: boolean) =&gt; {\n      console.log(\"[onEnabledChange]\", enabled);\n    })\n  );\n}\n\nfunction removeListeners() {\n  subscriptions.forEach((sub) =&gt; sub.remove());\n  subscriptions.length = 0;\n}\n</code></pre>"},{"location":"typescript/Subscription/#members","title":"Members","text":""},{"location":"typescript/Subscription/#remove","title":"remove","text":"<pre><code>remove(): void;\n</code></pre> <p>Removes the event-listener and frees its resources. Call this when the listener is no longer needed to prevent memory leaks.</p>"},{"location":"typescript/TrackingMode/","title":"TrackingMode","text":"<pre><code>export const TrackingMode = {\n</code></pre> <p>SDK tracking mode (legacy RN values preserved).</p> Value Name Description 0 Geofences Monitor geofences only. 1 Location Monitor location + geofences."},{"location":"typescript/TrackingMode/#members","title":"Members","text":""},{"location":"typescript/TrackingMode/#geofences","title":"Geofences","text":"<p>Monitor geofences only.</p>"},{"location":"typescript/TrackingMode/#location","title":"Location","text":"<p>Monitor location + geofences.</p>"},{"location":"typescript/TransistorAuthorizationService/","title":"TransistorAuthorizationService","text":"<pre><code>export interface TransistorAuthorizationService {\n</code></pre> <p>Client for the Transistor Software demo tracking server.</p> <p>Transistor Software hosts a public demo server at tracker.transistorsoft.com that consumes location data from devices running the Background Geolocation SDK. You can also run a local instance \u2014 see background-geolocation-console.</p> <p>The demo server is useful for evaluating the SDK or for sharing tracking results with Transistor Support when debugging.</p> <p></p>"},{"location":"typescript/TransistorAuthorizationService/#viewing-results","title":"Viewing results","text":"<p>To view tracking results in a browser, visit:</p> <p><code>http://tracker.transistorsoft.com/&lt;your-organization-name&gt;</code></p> <pre><code>const url = \"http://tracker.transistorsoft.com\";\nconst orgname = \"my-company-name\";\nconst username = \"my-username\";\n\n// Fetch a token from the server (the SDK caches it automatically).\nconst token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  orgname, username, url\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"typescript/TransistorAuthorizationService/#members","title":"Members","text":""},{"location":"typescript/TransistorAuthorizationService/#applyif","title":"applyIf","text":"<p><code>applyIf(config: T):</code><code>Config</code><code>;</code></p> <p>Mutate a Config to apply a <code>transistorAuthorizationToken</code> if present.</p> <p>The implementation reads <code>config.transistorAuthorizationToken</code>, removes that property, sets <code>config.http.url</code> to <code>\"&lt;token.url&gt;/api/locations\"</code>, and injects <code>config.authorization</code> with a JWT strategy derived from the token. If no <code>transistorAuthorizationToken</code> is found, the config is returned unchanged.</p> <pre><code>const token = await service.findOrCreate('my-org', 'user@example.com');\n\nconst config = service.applyIf({\n  ...myConfig,\n  transistorAuthorizationToken: token\n});\n\nBackgroundGeolocation.ready(config);\n</code></pre>"},{"location":"typescript/TransistorAuthorizationService/#destroy","title":"destroy","text":"<pre><code>destroy(url?: string): Promise&lt;void&gt;;\n</code></pre> <p>Remove the cached token associated with the given tracker URL.</p>"},{"location":"typescript/TransistorAuthorizationService/#findorcreate","title":"findOrCreate","text":"<p><code>findOrCreate(orgName: string, username: string, url?: string): Promise&lt;</code><code>TransistorAuthorizationToken</code><code>&gt;;</code></p> <p>Find or create an authorization token for the given organization and username.</p> <p>If a token already exists in the local cache for this <code>orgName</code> + <code>url</code> combination, the cached token is returned. Otherwise a new token is requested from the server and cached for future calls.</p> <pre><code>const token = await BackgroundGeolocation.findOrCreateTransistorAuthorizationToken(\n  \"my-company-name\",\n  \"my-username\",\n  \"http://tracker.transistorsoft.com\"\n);\n\nBackgroundGeolocation.ready({\n  transistorAuthorizationToken: token\n});\n</code></pre>"},{"location":"typescript/TransistorAuthorizationToken/","title":"TransistorAuthorizationToken","text":"<pre><code>export interface TransistorAuthorizationToken {\n</code></pre> <p>Authorization token issued by a Transistor Software tracking server.</p> <p>Returned from TransistorAuthorizationService.findOrCreate and used to configure the SDK's HTTP and authorization settings for the demo tracker.</p>"},{"location":"typescript/TransistorAuthorizationToken/#members","title":"Members","text":""},{"location":"typescript/TransistorAuthorizationToken/#accesstoken","title":"accessToken","text":"<pre><code>accessToken: string;\n</code></pre> <p>JWT access token sent as <code>Authorization: Bearer &lt;token&gt;</code> on each upload.</p>"},{"location":"typescript/TransistorAuthorizationToken/#expires","title":"expires","text":"<pre><code>expires: number;\n</code></pre> <p>Expiry time of the access token (epoch milliseconds). Typically used to populate AuthorizationConfig.expires.</p>"},{"location":"typescript/TransistorAuthorizationToken/#refreshtoken","title":"refreshToken","text":"<pre><code>refreshToken: string;\n</code></pre> <p>JWT refresh token presented to the AuthorizationConfig.refreshUrl endpoint when the access token expires.</p>"},{"location":"typescript/TransistorAuthorizationToken/#url","title":"url","text":"<pre><code>url: string;\n</code></pre> <p>Base URL of the tracker server that issued this token.</p>"},{"location":"typescript/TriggerActivity/","title":"TriggerActivity","text":"<pre><code>export const TriggerActivity = {\n</code></pre> <p>Allowed motion activity names that can trigger motion detection. Mirrors Flutter's TriggerActivity set.</p>"},{"location":"typescript/Vertices/","title":"Vertices","text":"<pre><code>export type Vertices = [number, number][];\n</code></pre> <p>An array of <code>[latitude, longitude]</code> pairs defining a polygon geofence.</p> <p>See Geofence.vertices.</p>"},{"location":"typescript/WatchPositionRequest/","title":"WatchPositionRequest","text":"<pre><code>export interface WatchPositionRequest {\n</code></pre> <p>Options for BackgroundGeolocation.watchPosition.</p> <p>Configures the interval, accuracy, persistence, and metadata for a continuous location stream. All fields are optional.</p> <p>Warning</p> <p><code>watchPosition</code> is intended for foreground use. On iOS it prevents the app from being suspended, which drains the battery. Remove the subscription when your app moves to the background.</p> <pre><code>const subscription = BackgroundGeolocation.watchPosition(\n  { interval: 1000, desiredAccuracy: DesiredAccuracy.High },\n  (location) =&gt; {\n    console.log(\"[watchPosition]\", location);\n  },\n  (errorCode) =&gt; {\n    console.warn(\"[watchPosition] error:\", errorCode);\n  }\n);\n\n// Later, stop watching.\nsubscription.remove();\n</code></pre>"},{"location":"typescript/WatchPositionRequest/#members","title":"Members","text":""},{"location":"typescript/WatchPositionRequest/#desiredaccuracy","title":"desiredAccuracy","text":"<p><code>desiredAccuracy?:</code><code>DesiredAccuracy</code><code>;</code></p> <p>Target accuracy for location updates from the native API. Defaults to DesiredAccuracy.High.</p>"},{"location":"typescript/WatchPositionRequest/#extras","title":"extras","text":"<pre><code>extras?: Record&lt;string, any&gt;;\n</code></pre> <p>Optional key-value metadata to attach to each location. Merged with any configured PersistenceConfig.extras before persisting or uploading to HttpConfig.url.</p>"},{"location":"typescript/WatchPositionRequest/#interval","title":"interval","text":"<pre><code>interval?: number;\n</code></pre> <p>Interval in milliseconds between location updates.</p>"},{"location":"typescript/WatchPositionRequest/#persist","title":"persist","text":"<pre><code>persist?: boolean;\n</code></pre> <p>Whether to persist each location to the SDK's SQLite database and upload it to HttpConfig.url. Defaults to <code>true</code> when the SDK is enabled; <code>false</code> when stopped.</p>"},{"location":"typescript/WatchPositionRequest/#timeout","title":"timeout","text":"<pre><code>timeout?: number;\n</code></pre> <p>Maximum time in milliseconds to wait for each location fix before firing an error. Default <code>60000</code>.</p>"},{"location":"typescript/example/","title":"Example","text":"React NativeExpoCapacitor <pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre> <p>The JavaScript API is identical to React Native.</p> <pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre> AngularReactVue <pre><code>import { Injectable, OnDestroy } from '@angular/core';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\n@Injectable({ providedIn: 'root' })\nexport class BackgroundGeolocationService implements OnDestroy {\n  private subscriptions: Subscription[] = [];\n\n  init() {\n    // Register event listeners *before* calling ready()\n    this.subscriptions.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n  }\n\n  // e.g. wired to a toggle button in your template\n  async onToggleTracking() {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      await BackgroundGeolocation.stop();\n    } else {\n      await BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  }\n\n  ngOnDestroy() {\n    this.subscriptions.forEach(s =&gt; s.remove());\n  }\n}\n</code></pre> <pre><code>import React, { useEffect, useRef } from 'react';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n        geolocation: {\n          desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n          distanceFilter: 10,\n        },\n        app: {\n          stopOnTerminate: false,\n          startOnBoot: true,\n        },\n        logger: {\n          debug: true,\n          logLevel: BackgroundGeolocation.LogLevel.Verbose,\n        },\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;IonButton onClick={onToggleTracking}&gt;Toggle Tracking&lt;/IonButton&gt;\n  );\n}\n</code></pre> <pre><code>&lt;script setup lang=\"ts\"&gt;\nimport { onMounted, onUnmounted } from 'vue';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nconst subscriptions: Subscription[] = [];\n\nonMounted(() =&gt; {\n  // Register event listeners *before* calling ready()\n  subscriptions.push(\n    BackgroundGeolocation.onLocation(location =&gt; {\n      console.log('[onLocation]', location);\n    })\n  );\n\n  // ready() configures the SDK and restores persisted state.\n  // It does NOT start tracking \u2014 call start()/stop() separately.\n  BackgroundGeolocation.ready({\n      geolocation: {\n        desiredAccuracy: BackgroundGeolocation.DesiredAccuracy.High,\n        distanceFilter: 10,\n      },\n      app: {\n        stopOnTerminate: false,\n        startOnBoot: true,\n      },\n      logger: {\n        debug: true,\n        logLevel: BackgroundGeolocation.LogLevel.Verbose,\n      },\n  });\n});\n\n// e.g. a toggle-button handler in your template\nasync function onToggleTracking() {\n  const state = await BackgroundGeolocation.getState();\n  if (state.enabled) {\n    await BackgroundGeolocation.stop();\n  } else {\n    await BackgroundGeolocation.start();\n  }\n  console.log('[state]', state.enabled, state.trackingMode);\n}\n\nonUnmounted(() =&gt; {\n  subscriptions.forEach(s =&gt; s.remove());\n});\n&lt;/script&gt;\n</code></pre>"},{"location":"typescript/examples/","title":"Examples","text":"React Native / ExpoCapacitor"},{"location":"typescript/examples/#basic-tracking","title":"Basic tracking","text":"<pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"typescript/examples/#basic-tracking_1","title":"Basic tracking","text":"AngularReactVue <pre><code>import { Injectable, OnDestroy } from '@angular/core';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\n@Injectable({ providedIn: 'root' })\nexport class BackgroundGeolocationService implements OnDestroy {\n  private subscriptions: Subscription[] = [];\n\n  init() {\n    // Register event listeners *before* calling ready()\n    this.subscriptions.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n  }\n\n  // e.g. wired to a toggle button in your template\n  async onToggleTracking() {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      await BackgroundGeolocation.stop();\n    } else {\n      await BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  }\n\n  ngOnDestroy() {\n    this.subscriptions.forEach(s =&gt; s.remove());\n  }\n}\n</code></pre> <pre><code>import React, { useEffect, useRef } from 'react';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;IonButton onClick={onToggleTracking}&gt;Toggle Tracking&lt;/IonButton&gt;\n  );\n}\n</code></pre> <pre><code>&lt;script setup lang=\"ts\"&gt;\nimport { onMounted, onUnmounted } from 'vue';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nconst subscriptions: Subscription[] = [];\n\nonMounted(() =&gt; {\n  // Register event listeners *before* calling ready()\n  subscriptions.push(\n    BackgroundGeolocation.onLocation(location =&gt; {\n      console.log('[onLocation]', location);\n    })\n  );\n\n  // ready() configures the SDK and restores persisted state.\n  // It does NOT start tracking \u2014 call start()/stop() separately.\n  BackgroundGeolocation.ready({\n    {{&gt; ts-ready-config.md}}\n  });\n});\n\n// e.g. a toggle-button handler in your template\nasync function onToggleTracking() {\n  const state = await BackgroundGeolocation.getState();\n  if (state.enabled) {\n    await BackgroundGeolocation.stop();\n  } else {\n    await BackgroundGeolocation.start();\n  }\n  console.log('[state]', state.enabled, state.trackingMode);\n}\n\nonUnmounted(() =&gt; {\n  subscriptions.forEach(s =&gt; s.remove());\n});\n&lt;/script&gt;\n</code></pre>"},{"location":"typescript/setup/","title":"Setup","text":"React NativeExpoCapacitor"},{"location":"typescript/setup/#react-native","title":"React Native","text":""},{"location":"typescript/setup/#installation","title":"Installation","text":"npmyarn <pre><code>npm install react-native-background-geolocation\n</code></pre> <pre><code>yarn add react-native-background-geolocation\n</code></pre>"},{"location":"typescript/setup/#configure-your-license","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"typescript/setup/#ios-infoplist","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"typescript/setup/#android-androidmanifestxml","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"typescript/setup/#ios-setup","title":"iOS Setup","text":""},{"location":"typescript/setup/#cocoapods","title":"CocoaPods","text":"<pre><code>cd ios &amp;&amp; pod install\n</code></pre>"},{"location":"typescript/setup/#background-modes","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"typescript/setup/#infoplist","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Background modes --&gt;\n&lt;key&gt;UIBackgroundModes&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;location&lt;/string&gt;\n    &lt;string&gt;fetch&lt;/string&gt;\n    &lt;string&gt;processing&lt;/string&gt;\n    &lt;string&gt;audio&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Background task identifier --&gt;\n&lt;key&gt;BGTaskSchedulerPermittedIdentifiers&lt;/key&gt;\n&lt;array&gt;\n    &lt;string&gt;com.transistorsoft.fetch&lt;/string&gt;\n&lt;/array&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"typescript/setup/#appdelegate","title":"AppDelegate","text":"SwiftObjective-C <pre><code>import TSBackgroundFetch\n\nfunc application(_ application: UIApplication,\n                 didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -&gt; Bool {\n    TSBackgroundFetch.sharedInstance()?.didFinishLaunching()\n    return true\n}\n</code></pre> <pre><code>#import &lt;TSBackgroundFetch/TSBackgroundFetch.h&gt;\n\n- (BOOL)application:(UIApplication *)application\n    didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {\n    [[TSBackgroundFetch sharedInstance] didFinishLaunching];\n    return YES;\n}\n</code></pre>"},{"location":"typescript/setup/#android-setup","title":"Android Setup","text":""},{"location":"typescript/setup/#gradle-ext-vars","title":"Gradle ext vars","text":"<p>The plugin reads two optional ext vars from your root project. Set them in <code>android/build.gradle</code> to pin specific versions:</p> build.gradle.ktsbuild.gradle <pre><code>ext[\"playServicesLocationVersion\"] = \"21.3.0\"\next[\"tslocationmanagerVersion\"]    = \"4.0.+\"\n</code></pre> <pre><code>ext {\n    playServicesLocationVersion = \"21.3.0\"\n    tslocationmanagerVersion    = \"4.0.+\"\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Versions \u2265 21 use <code>tslocationmanager</code>; versions &lt; 21 use <code>tslocationmanager-gms20</code>. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"typescript/setup/#androidappbuildgradle","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>android {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"typescript/setup/#androidmanifestxml","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"typescript/setup/#example","title":"Example","text":"<pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"typescript/setup/#expo","title":"Expo","text":""},{"location":"typescript/setup/#installation_1","title":"Installation","text":"<pre><code>npx expo install react-native-background-geolocation\n</code></pre> <p>The SDK ships an Expo Config Plugin \u2014 it handles all native configuration automatically during <code>expo prebuild</code>. No manual edits to <code>Info.plist</code>, <code>AndroidManifest.xml</code>, or Gradle files are required.</p>"},{"location":"typescript/setup/#configure-your-license_1","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"typescript/setup/#appjson-appconfigjs","title":"<code>app.json</code> / <code>app.config.js</code>","text":"<pre><code>{\n  \"expo\": {\n    \"plugins\": [\n      [\n        \"react-native-background-geolocation\",\n        {\n          \"license\": \"YOUR_ANDROID_LICENSE_KEY_JWT\"\n        }\n      ],\n      [\n        \"expo-gradle-ext-vars\",\n        {\n          \"googlePlayServicesLocationVersion\": \"21.3.0\",\n          \"tslocationmanagerVersion\": \"4.0.+\"\n        }\n      ]\n    ],\n    \"ios\": {\n      \"infoPlist\": {\n        \"TSLocationManagerLicense\": \"YOUR_IOS_LICENSE_KEY_JWT\",\n        \"UIBackgroundModes\": [\"location\", \"fetch\", \"processing\", \"audio\"],\n        \"BGTaskSchedulerPermittedIdentifiers\": [\n          \"com.transistorsoft.fetch\"\n        ],\n        \"NSLocationAlwaysAndWhenInUseUsageDescription\": \"App requires location access at all times for background tracking.\",\n        \"NSLocationWhenInUseUsageDescription\": \"App requires location access while in use.\",\n        \"NSMotionUsageDescription\": \"Motion detection helps determine when the device is stationary.\"\n      }\n    }\n  }\n}\n</code></pre> <p><code>expo-gradle-ext-vars</code></p> <p>Install the companion package to control the Google Play Services location version: <pre><code>npx expo install expo-gradle-ext-vars\n</code></pre></p>"},{"location":"typescript/setup/#prebuild","title":"Prebuild","text":"<p>After configuring <code>app.json</code>, regenerate native projects:</p> <pre><code>npx expo prebuild\n</code></pre> <p>Then build as normal:</p> <pre><code>npx expo run:ios\nnpx expo run:android\n</code></pre>"},{"location":"typescript/setup/#example_1","title":"Example","text":"<p>The JavaScript API is identical to React Native.</p> <pre><code>import React, { useEffect, useRef } from 'react';\nimport { Button, View } from 'react-native';\nimport BackgroundGeolocation, {\n  Subscription,\n} from 'react-native-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;View&gt;\n      &lt;Button title=\"Toggle Tracking\" onPress={onToggleTracking} /&gt;\n    &lt;/View&gt;\n  );\n}\n</code></pre>"},{"location":"typescript/setup/#capacitor","title":"Capacitor","text":""},{"location":"typescript/setup/#installation_2","title":"Installation","text":"<pre><code>npm install @transistorsoft/capacitor-background-geolocation\nnpx cap sync\n</code></pre> <p>Requires Capacitor 5 or higher.</p>"},{"location":"typescript/setup/#configure-your-license_2","title":"Configure your license","text":"<p>Purchase a license</p> <p>The SDK requires a license for release builds. Debug builds work without one. Purchase at docs.transistorsoft.com.</p>"},{"location":"typescript/setup/#ios-infoplist_1","title":"iOS \u2014 Info.plist","text":"<pre><code>&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n</code></pre>"},{"location":"typescript/setup/#android-androidmanifestxml_1","title":"Android \u2014 AndroidManifest.xml","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"typescript/setup/#ios-setup_1","title":"iOS Setup","text":""},{"location":"typescript/setup/#background-modes_1","title":"Background Modes","text":"<p>In Xcode, select your target \u2192 Signing &amp; Capabilities \u2192 + Capability \u2192 Background Modes. Enable:</p> <ul> <li>[x] Location updates</li> <li>[x] Background fetch</li> <li>[x] Audio (optional \u2014 enables debug sound FX)</li> </ul>"},{"location":"typescript/setup/#infoplist_1","title":"Info.plist","text":"<pre><code>&lt;!-- License key --&gt;\n&lt;key&gt;TSLocationManagerLicense&lt;/key&gt;\n&lt;string&gt;YOUR_LICENSE_KEY_JWT&lt;/string&gt;\n\n&lt;!-- Location usage descriptions --&gt;\n&lt;key&gt;NSLocationAlwaysAndWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access at all times for background tracking.&lt;/string&gt;\n\n&lt;key&gt;NSLocationWhenInUseUsageDescription&lt;/key&gt;\n&lt;string&gt;App requires location access while in use.&lt;/string&gt;\n\n&lt;key&gt;NSMotionUsageDescription&lt;/key&gt;\n&lt;string&gt;Motion detection helps determine when the device is stationary.&lt;/string&gt;\n</code></pre>"},{"location":"typescript/setup/#android-setup_1","title":"Android Setup","text":""},{"location":"typescript/setup/#gradle-ext-vars_1","title":"Gradle ext vars","text":"<p>The plugin reads two optional ext vars from your root project. Set them in <code>android/variables.gradle</code>:</p> <pre><code>ext {\n    minSdkVersion               = 24\n    compileSdkVersion           = 35\n    targetSdkVersion            = 35\n    playServicesLocationVersion = '21.3.0'\n    tslocationmanagerVersion    = '4.0.+'\n}\n</code></pre> Variable Default Description <code>playServicesLocationVersion</code> <code>21.3.0</code> <code>com.google.android.gms:play-services-location</code> version. Versions \u2265 21 use <code>tslocationmanager</code>; versions &lt; 21 use <code>tslocationmanager-gms20</code>. Browse: maven.google.com <code>tslocationmanagerVersion</code> <code>4.0.+</code> Transistor Android SDK version. Browse: central.sonatype.com"},{"location":"typescript/setup/#androidappbuildgradle_1","title":"<code>android/app/build.gradle</code>","text":"build.gradle.ktsbuild.gradle <pre><code>android {\n    buildTypes {\n        release {\n            isMinifyEnabled = true\n            isShrinkResources = false   // required\n        }\n    }\n}\n</code></pre> <pre><code>android {\n    buildTypes {\n        release {\n            minifyEnabled true\n            shrinkResources false   // required\n        }\n    }\n}\n</code></pre>"},{"location":"typescript/setup/#androidmanifestxml_1","title":"<code>AndroidManifest.xml</code>","text":"<pre><code>&lt;application&gt;\n    &lt;meta-data\n        android:name=\"com.transistorsoft.locationmanager.license\"\n        android:value=\"YOUR_LICENSE_KEY_JWT\" /&gt;\n&lt;/application&gt;\n</code></pre>"},{"location":"typescript/setup/#example_2","title":"Example","text":"AngularReactVue <pre><code>import { Injectable, OnDestroy } from '@angular/core';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\n@Injectable({ providedIn: 'root' })\nexport class BackgroundGeolocationService implements OnDestroy {\n  private subscriptions: Subscription[] = [];\n\n  init() {\n    // Register event listeners *before* calling ready()\n    this.subscriptions.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n  }\n\n  // e.g. wired to a toggle button in your template\n  async onToggleTracking() {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      await BackgroundGeolocation.stop();\n    } else {\n      await BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  }\n\n  ngOnDestroy() {\n    this.subscriptions.forEach(s =&gt; s.remove());\n  }\n}\n</code></pre> <pre><code>import React, { useEffect, useRef } from 'react';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nexport default function App() {\n  const subscriptions = useRef&lt;Subscription[]&gt;([]);\n\n  useEffect(() =&gt; {\n    const subs = subscriptions.current;\n\n    // Register event listeners *before* calling ready()\n    subs.push(\n      BackgroundGeolocation.onLocation(location =&gt; {\n        console.log('[onLocation]', location);\n      })\n    );\n\n    // ready() configures the SDK and restores persisted state.\n    // It does NOT start tracking \u2014 call start()/stop() separately.\n    BackgroundGeolocation.ready({\n      {{&gt; ts-ready-config.md}}\n    });\n\n    return () =&gt; subs.forEach(s =&gt; s.remove());\n  }, []);\n\n  // e.g. a toggle-button handler in your UI\n  const onToggleTracking = async () =&gt; {\n    const state = await BackgroundGeolocation.getState();\n    if (state.enabled) {\n      BackgroundGeolocation.stop();\n    } else {\n      BackgroundGeolocation.start();\n    }\n    console.log('[state]', state.enabled, state.trackingMode);\n  };\n\n  return (\n    &lt;IonButton onClick={onToggleTracking}&gt;Toggle Tracking&lt;/IonButton&gt;\n  );\n}\n</code></pre> <pre><code>&lt;script setup lang=\"ts\"&gt;\nimport { onMounted, onUnmounted } from 'vue';\nimport BackgroundGeolocation, {\n  Subscription,\n} from '@transistorsoft/capacitor-background-geolocation';\n\nconst subscriptions: Subscription[] = [];\n\nonMounted(() =&gt; {\n  // Register event listeners *before* calling ready()\n  subscriptions.push(\n    BackgroundGeolocation.onLocation(location =&gt; {\n      console.log('[onLocation]', location);\n    })\n  );\n\n  // ready() configures the SDK and restores persisted state.\n  // It does NOT start tracking \u2014 call start()/stop() separately.\n  BackgroundGeolocation.ready({\n    {{&gt; ts-ready-config.md}}\n  });\n});\n\n// e.g. a toggle-button handler in your template\nasync function onToggleTracking() {\n  const state = await BackgroundGeolocation.getState();\n  if (state.enabled) {\n    await BackgroundGeolocation.stop();\n  } else {\n    await BackgroundGeolocation.start();\n  }\n  console.log('[state]', state.enabled, state.trackingMode);\n}\n\nonUnmounted(() =&gt; {\n  subscriptions.forEach(s =&gt; s.remove());\n});\n&lt;/script&gt;\n</code></pre>"}]}